# Decisions Index

Auto-generated by `scripts/ledger.ts` — one line per `docs/DECISIONS.md` row, so a
session can find a decision by number or keyword without loading the full ledger.
Do not hand-edit; run `tsx scripts/ledger.ts reindex` to regenerate.

- **#1** (Phase 0): Money = integer cents, branded type; round-half-away-from-zero | Auditability; floats forbidden by spec
- **#2** (Phase 0): Business dates are date-only (YYYY-MM-DD), single dates.ts utility | Timezone bugs are the top fintech date hazard
- **#3** (Phase 0): Obligation timeline computed on demand, not stored | Single source of truth; avoids cache-invalidation bugs
- **#4** (Phase 0): Weekend/holiday due dates adjust to PRIOR business day (conservative) | Funds present early is safe; late is not
- **#5** (Phase 0): v1 minimum-path interest = simple monthly (carried × APR/12), labeled approximate **[SUPERSEDED by #29: now average-daily-balance]** | Hand-verifiable; ADB method is roadmap
- **#6** (Phase 1): SQLite for local dev/test via Prisma 7 (`prisma-client` generator → `src/generated/prisma`); schema kept Postgres-portable (no SQLite-only features; enums/Json represented as String columns with documented values) | Post…
- **#7** (Phase 1): Business dates stored as String `YYYY-MM-DD` (not DateTime @db.Date) | SQLite has no DATE type; lexicographic compare works; single dates.ts utility validates
- **#8** (Phase 1): Added `BalanceSnapshot` model (month-end balance per account) | Drives the net-worth trend chart deterministically from seeded history; additive over the Phase 0 draft shape
- **#9** (Phase 1): "This cycle" = obligations from GENERATED statements; a not-yet-generated statement (estimate) is next cycle — shown as upcoming, excluded from headline/projection — unless no generated statement exists at all (then the …
- **#10** (Phase 1): Within a projected day: scheduled flows post BEFORE card payments are drawn | ACH credits typically post before card-payment cutoffs; deterministic, tested, surfaced in assumptions
- **#11** (Phase 1): Seed's engineered intra-period dip placed between the two due dates (rent 06-24, payroll 06-26): endpoints positive, mid-period −$1,012.33 | With checking ≈$3,400 and total due $5,412.33 fixed by SEED_SPEC, a pre-06-15 b…
- **#12** (Phase 1): Demo mode pins "today" via `DEMO_TODAY` env (default = seed asOf 2026-06-10) | Engines take `today` as input; keeps the seeded dataset coherent and e2e deterministic
- **#13** (Phase 1): Phase 1 auth = Auth.js v5 Credentials "demo sign-in" (one click, demo user only); magic-link/Google land in Phase 4 with the security pass | Demo mode must work with zero secrets; full auth is a Phase 4 acceptance item
- **#14** (Phase 1): Seed statement history balances are plausible PRNG values, not exact sums of that cycle's card transactions | Reconciliation adds heavy complexity with no Phase 1–3 test value; noted as a known limitation in STATUS
- **#15** (Phase 1): Transfer recommendation byDate clamped to `today` (critic P1-1) | "Transfer by yesterday" is an impossible instruction; same-day transfer is the correct remedy
- **#16** (Phase 1): Current statement = most recent with dueDate ≥ today OR an unpaid remainder (critic P1-2) | A delinquent statement is real debt due NOW, never a next-cycle estimate
- **#17** (Phase 2): Mid-band amounts on a merchant with user amount-banded rules go to review | The user declared the merchant context-dependent; the gap between bands is genuinely ambiguous (EDGE_CASES §Categorization)
- **#18** (Phase 2): "Possibly unused" subscription heuristic = fitness-category memberships (usage is not observable in transaction data) | Surfaced as a question to the user, never an accusation; consistent with coach guardrails
- **#19** (Phase R1): Cash-needed engine `notes`/`assumptions` ARE presentation strings (formatCents/formatISODate inside the engine) — a deliberate exemption from the "format only at the UI boundary" convention, same as coach-copy.ts | The e…
- **#20** (Phase R1): TanStack Query (named in CLAUDE.md conventions) is intentionally NOT used | Server components + server actions made client-side query caching unnecessary; zero client data-fetching exists
- **#21** (Phase R1): v1 minimum-path interest remains simple-monthly; PHASE_0 §4's "binding" ADB wording is superseded by DECISIONS #5 **[now itself superseded by #29 — ADB implemented, matching PHASE_0 §4's original intent]** | The in-app l…
- **#22** (Phase R2): Required ingestion pipeline order for ANY future transaction source (Plaid, CSV, manual): normalizeMerchant → merchant upsert → loadUserRules → categorize(rules) → persist category/confidence/needsReview → detectTransfer…
- **#23** (Phase R2): Batch "apply to all" scopes to the EXACT raw descriptor for aggregate pseudo-merchants (Zelle/checks), to the merchant otherwise; aggregate rules are also dropped at the rule READ path | "All 6 payments to J. Park" is a …
- **#24** (Phase R4): A manually-entered transaction records ACTIVITY only — it does NOT mutate the owning account's `currentBalanceCents` | Balances are provider-authoritative (the institution/seed is the source of truth); rewriting one from…
- **#25** (Phase R4): Manual entry runs the categorize() arm of the #22 pipeline (normalizeMerchant → loadUserRules → categorize → persist category/confidence/needsReview, with isTransfer set from the verdict/explicit category) but intentiona…
- **#26** (Phase R4): Plaid integration split into a TESTED pure mapping layer (plaid-map.ts: sign flip, account-type, liability→statement, per-row categorize) and an UNVERIFIED network layer (plaid.ts). Access tokens + sync cursor live in a …
- **#27** (Phase R4): CSV import is the third ingest source (DECISIONS #22), fully testable without credentials. Contract: required `date`/`description`/`amount` (+ optional `category`) headers with aliases; amount SIGNED in Pulse convention …
- **#28** (Phase R5): Money Dials settings/onboarding UI (`/settings`) edits the five per-user dials the engines already read but nothing could set: payment account, SWR, expected return, hourly wage, money dials. Validation lives in a pure e…
- **#29** (Phase R5): Minimum-path interest upgraded from the labeled simple-monthly approximation (carried × APR/12) to the **average-daily-balance method** (ROADMAP #3, supersedes #5/#21). Per card not paid in full: interest = round(DPR × Σ…
- **#30** (Phase R5): Budget-targets UI (ROADMAP #7): set/clear a per-category monthly target against actuals on `/budgets`. Pure engine `engine/budgets/status.ts` (`summarizeBudgets` over the union of spent+target categories with over/remain…
- **#31** (Phase R5): Account deletion / "delete my data" UI (ROADMAP #10, PRIVACY.md §Deletion). Typed-confirmation gate (`confirmationMatches`, pure-tested) → ownership-scoped `prisma.user.delete` that cascades every user-owned row (`onDele…
- **#32** (Phase R5): Offline PWA service worker (ROADMAP #5): `public/sw.js` + a precached `/offline` shell + production-only registration (`sw-register.tsx`). Deliberately minimal/conservative: navigations are NETWORK-FIRST (online always f…
- **#33** (Phase R5): App-wide refund netting (ROADMAP #4, supersedes STATUS #8's "refunds count as inflows"): in `monthlyFlows` (the single income/expense classifier feeding savings rate + FI), a positive transaction in a NON-income category…
- **#34** (Phase R5): Recurring detection is refund-robust (ROADMAP #4 tail, fixes STATUS #7's "refund+rebill drops the series"): `detectRecurring` now analyzes only the DOMINANT sign within each merchant group — a stray opposite-sign txn (a …
- **#35** (Phase R5): Postgres deploy path (ROADMAP, "engine before UI" extended to infra): the app runs on **SQLite locally + in every test** (zero credentials, `file:./dev.db`) and on **Postgres in production**, chosen at runtime by connect…
- **#36** (Phase R5): Inline recategorization on the transaction register (user request). The pipeline auto-files transactions and routes only low-confidence ones (`needsReview`) to the triage inbox — so a CONFIDENT-but-wrong guess was uncorr…
- **#37** (Phase R5): Categorization accuracy + calibration metric (user request, option B; CLAUDE.md §adaptive-runtime "log every prediction, track Brier, surface in UI"). New `CategoryPrediction` table logs one row per transaction — predict…
- **#38** (Phase R5): LLM-assisted categorization behind a tested no-key fallback (user request, option C). For UNKNOWN merchants the deterministic pipeline routes to review; an optional LLM can propose a category instead. Pure pieces (`engin…
- **#39** (Phase R5): Manual net-worth items — assets & liabilities a bank feed can't see (user request: "housing, assets… net worth calculator like Mint"). Users add a home/vehicle/cash/other ASSET and a mortgage/other LIABILITY, stored as A…
- **#40** (Phase R5): Net worth over time on /accounts + one shared series engine (user request). A trend already existed on the dashboard but was computed inline in finance.ts with a THIRD hardcoded liability set (['CREDIT','LOAN']) — blind …
- **#41** (Phase R5): Plaid Link "Connect a bank or brokerage" UI (user request: bank/brokerage/all account types like Mint/Simplifi). The PlaidProvider backend (createLinkToken/exchangePublicToken/sync accounts+transactions+liabilities) was …
- **#42** (Phase R5): Toward near-zero manual categorization (user request): (1) the register's "Always" action now shows the COUNT it will touch — "Always — re-file all N <merchant>" + "Re-files all N past <merchant> charges and auto-files e…
- **#43** (Phase R5): Real multi-user auth — email/password now, Google dormant (user request; the gate to private/production Plaid). Split the Auth.js config: `auth.config.ts` is EDGE-SAFE (Prisma-free) and used by middleware (session checks…
- **#44** (Phase R5): Hostile-critic pass over the session's work (3 parallel adversarial critics per CRITIC_RUBRIC.md; every P0/P1 verified against the code before fixing). ZERO P0. Fixed P1s: (1) Google sign-in upsert could throw an unhandl…
- **#45** (Phase R5): Manual BANK accounts (user request: "get me + my wife's finances without Plaid"). The CSV-import page needs an existing account to import into, but the only "Add account" options were net-worth items (home/car/etc.) — so…
- **#46** (Phase R5): Manual card statements (extends #45, the no-Plaid household). A manual CREDIT card has `type='CREDIT'` so the Cash-Needed Engine treats it as a card, but with no Statement AND no cycle days `buildObligation` returns null…
- **#47** (Phase R5): Payment reminders (ROADMAP #6) — the delivery MECHANISM (the calendar badged due days, but nothing was delivered). Three credential-free parts + an in-app surface: (1) pure `engine/reminders/select.ts` — `selectPaymentRe…
- **#48** (Phase R5): Production hardening — split-race concurrency (#9) + durable rate limiting/throttle (#8). (#9) `splitTransaction` had a DOUBLE-SPLIT race: it read `isSplitParent` BEFORE its transaction, so two concurrent splits both pas…
- **#49** (Phase R5): Finished the deferred hardening (ROADMAP #9 tail + the Critic SEC-2 item #48 documented-and-deferred). (A) **Always/Undo orphan-rule race** (STATUS #10): `undoCorrections` now deletes a correction's rule with a CONDITION…
- **#50** (Phase R5): Closed the last STATUS #10 concurrency item — the double-undo TOCTOU. `undoCorrections` had a pre-read (`alreadyUndone`) outside its transaction, so two concurrent undos of the SAME correction could both pass it and each…
- **#51** (Phase R5): Register pagination (ROADMAP #8). `getTransactions` loaded the full set and silently capped DISPLAY at 200 rows with no way to reach older ones ("Showing the most recent 200 — refine with filters"). Now it paginates: a p…
- **#52** (Phase R5): Plaid webhook JWT verification (ROADMAP #1c). The webhook handler verified NOTHING; DECISIONS #44 added an interim 404-unless-`DATA_PROVIDER=plaid` gate, but in Plaid mode any request could trigger a sync. Now `verifyPla…
- **#53** (Phase R5): Plaid recurring re-detection + scheduled refresh after ingest (ROADMAP #1b, the DECISIONS #22 tail). After a Plaid sync ingests transactions (per-row normalize→rules→categorize→transfer was already wired), `PlaidProvider…
- **#54** (Phase R5): Reproducible capability benchmark (`scripts/benchmark.ts`, `npm run benchmark`) — the honest answer to "is it actually good?". Runs the app's REAL pure engines over the seed dataset and prints MEASURED numbers (determini…
- **#55** (Phase R5): Self-improving loop in action — an adversarial categorization eval found and fixed a real misclassification. `scripts/categorize-eval.ts` (`npm run eval:categorize`) stresses the categorizer with 30 realistic, mostly-NOV…
- **#56** (Phase R5): SimpleFIN integration — a cheaper, less-gatekept Plaid alternative (user asked "can we build an aggregator like Plaid?"; answer: don't reverse-engineer a Plaid clone — wire SimpleFIN, a documented read-only protocol with…
- **#57** (Phase R5): Invite-only signup allowlist (user request: the app is for the owner + spouse + chosen testers, not the public). Pure env-driven gate `src/lib/auth/allowlist.ts` (`parseAllowlist`/`isSignupAllowed`) wired into `signUpWit…
- **#58** (Phase R5): Real-clock "business today" for real users (found while prepping the multi-user deploy). The app was demo-first: `DemoProvider.today()` and `getAccountsView` resolved `today = DEMO_TODAY ?? DEFAULT_AS_OF('2026-06-10')`, …
- **#59** (Phase R5): Vercel build `prisma db push --accept-data-loss` (deploy of ca23eac ERRORED — inspected the real build log, not assumed). The build failed at `db push` against the existing Neon `pulse` DB because two NEW unique constrai…
- **#60** (Phase R5): Household owners always allowed to sign up on the deployed app (production incident: the owner was locked out of his own live app — `CredentialsSignin` in the Vercel runtime logs = the account was never created, i.e. sig…
- **#61** (Phase R5): Account-type classifier + first-sync history window, from the FIRST real-bank SimpleFIN sync (27 accounts across Amex/Capital One/Chase/Schwab/Truist/US Bank/Vanguard; balances correct, but two issues). (1) Zero-balance …
- **#62** (Phase R5): Transactions = spending only (bank + cards); brokerage/loan activity excluded (user: "do not show brokerage holdings as transactions"). A real SimpleFIN sync pulled INVESTMENT/LOAN accounts whose "transactions" are trade…
- **#63** (Phase R5): Mint/Simplifi-breadth category taxonomy + generic keyword auto-categorization (user: "need at least as many categories as simplifi and mint"; real merchants were landing in Uncategorized because the categorizer only knew…
- **#64** (Phase R5): xAI/Grok as the (preferred, cheaper) LLM categorization provider + wiring the LLM assist into SimpleFIN sync (user: "use xai. cheaper"). `suggestCategoryViaLLM` is now provider-agnostic: `XAI_API_KEY` set → xAI Grok via …
- **#65** (Phase R5): Two-level categories + subcategories with the missing fields filled in (user: "make categories... and subcategories. there's no field for lots of things… incomplete"). Each CATEGORIES entry now carries its parent `group`…
- **#66** (Phase R5): Spending Plan — Simplifi's signature "safe to spend" view, surpassed by exactness (user: "match and surpass... what anyone has", starting feature). Pure `computeSpendingPlan` (engine/spending-plan/plan.ts): expected mont…
- **#67** (Phase R5): Reports & charts (feature #2 of the "match and surpass" build). Pure `spendingByCategory` (engine/reports/reports.ts): expenses only (transfers/splits/income excluded, refunds net down their category, net-refund categori…
- **#68** (Phase R5): Type-to-filter category picker (surpass: Monarch/Copilot have it, Mint/Simplifi don't). The register's inline category menu now has ~83 subcategories under 13 parent headers (#65); scrolling that is slow AND brittle (a P…
- **#69** (Phase R5): Dashboard redesign (surpass feature #3): the home is now a real at-a-glance command center, not just the cash-needed answer. Kept THE answer (CashNeededCard) first/above-the-fold (phase1 golden spec unchanged), then comp…
- **#70** (Phase R5): Visual polish pass (surpass feature #4) — cohesion over flash, all green. (A) Upgraded the base `Card` primitive (`ui/card.tsx`) from `rounded-xl` + `ring-1` to `rounded-2xl` + `border` + `shadow-sm`, so every card app-w…
- **#71** (Phase R5): Recurring & subscriptions UI (surpass feature #5) — the feature people switch apps for (Rocket Money / Monarch). The detection ENGINE already existed and was tested (Phase 2 `engine/recurring/detect.ts`: cadence from gap…
- **#72** (Phase R5): Cash-flow forecast (surpass feature #6) — the forward-looking complement to the cash-needed "this cycle" answer, and something none of Mint/Simplifi/Monarch do with a real engine. New pure engine `engine/forecast/forecas…
- **#73** (Phase R5): Real-bank bug: a checking/card account's expenses missing from the register (user: "you're missing Zone Pest Solutions… some expenses come from checking"). Diagnosed on live data via a temporary token-guarded owner endpo…
- **#74** (Phase R5): Spending Trends / insights (surpass feature #7 — the "what changed & what to look at" lens Copilot/Cleo/Monarch lead with, which our category/recurring/forecast views never exposed). New PURE engine `engine/trends/trends…
- **#75** (Phase R5): Ask Aimplifi — grounded natural-language assistant (surpass feature #8; the conversational surface the app is literally named for, which none of #1–#7 provided). Cardinal design: the LLM NEVER originates a fact. A PURE r…
- **#76** (Phase R5): SimpleFIN connect test flake hardened — the parallel unit suite (71 files) shares ONE SQLite dev.db across worker processes; in the default rollback journal a writer is starved by concurrent readers past the 15s busy_tim…
- **#77** (Phase R5): Investments engine — pure portfolio valuation + performance math (src/lib/engine/investments/portfolio.ts), the FIRST increment closing the one dimension an honest competitive review scored a clear loss to Simplifi (and …
- **#78** (Phase R5): Investments persistence + server layer (wires the #77 engine into the app): a new additive Holding Prisma model (position breakdown of an INVESTMENT account; quantity Float, priceCents/costBasisCents Int; @@unique([accou…
- **#79** (Phase R5): Production-seed safety: `prisma db seed` WIPES every table (deleteMany → demo dataset) — catastrophic against the live Neon DB, which holds the owner's real SimpleFIN-synced data. Added a guard in prisma/seed.ts that REF…
- **#80** (Phase R5): Investments UI + production-readiness pass (from an 11-agent UX/prod audit). (a) /investments view: src/app/(app)/investments/page.tsx + src/components/finance/investments-view.tsx (portfolio value, total unrealized gain…
- **#81** (Phase R5): Production-readiness a11y + resilience batch (audit roadmap; additive — no existing screens rewritten): (1) src/app/(app)/loading.tsx — a route-group Suspense skeleton so every (app) page streams instead of blocking on i…
- **#82** (Phase R5): Delete-confirmation on manual accounts (audit roadmap — accidental deletion is the worst UX failure on financial data). accounts-list.tsx ManualRow Delete is now a two-step inline confirm (Delete → "Delete? Yes / Cancel"…
- **#83** (Phase R5): Delete confirmation on savings goals + a "no credit cards" empty state (audit roadmap). (a) Goals delete: the one-click ✕ server-action form is now a small client component (delete-goal-button.tsx) with the same two-step…
- **#84** (Phase R5): Empty-state completion (audit roadmap #2). The Reports income-vs-spending chart now renders "No income or spending recorded in the last 6 months" (data-testid income-expense-empty) instead of a blank Recharts canvas when…
- **#85** (Phase R5): Per-page <title>s (audit roadmap tail). Added `export const metadata = { title }` to all 18 (app) pages — Dashboard, Accounts, Transactions, Import transactions, Add transaction, Settings, Review, Credit cards, Coach, Go…
- **#86** (Phase R5): Mobile nav tap targets (audit roadmap, owner-approved). The phone top-bar secondary destinations were a cramped row of sub-44px icon links; each is now a 44px-tall touch target (min-h-11 on mobile; the labelled text link…
- **#87** (Phase R5): Budgets first-run consistency (audit alsoConsider): /budgets was the lone section that did NOT return <EmptyDashboard/> for a zero-account user — added the same `prisma.account.count === 0` guard the other pages use, so …
- **#88** (Phase R5): CardTitle-as-heading + per-page <h1> (a11y document outline; closes the item deferred in #81). The shared `CardTitle` now renders a real heading — default `<h2>`, overridable via an `as` prop (`as="div"` for a value card…
- **#89** (Phase R5): Triage split — selectable second category + live preview (the split's part B was hard-coded to `'shopping'`). The split panel now renders a labeled `<select>` (sr-only "Second part category" label; default = `shopping` w…
- **#90** (Phase R5): iOS safe-area inset for the fixed mobile bottom tab bar (closes the item #86 reverted after 3 e2e failures). FIRST measured the assumption: a throwaway Playwright probe (Pixel 5 + 380px, the e2e device) showed `env(safe-…
- **#91** (Phase R5): Auto-sync SimpleFIN on page load (reported bug: real transactions "not populating / missing a bunch"). ROOT CAUSE (diagnosed, not assumed): the ONLY path that refreshed real bank data was the manual `syncSimplefinNow()` …
- **#92** (Phase R5): Coach Principles — Wave 1 (principle captions, content-only) from docs/COACH_PRINCIPLES_PLAN.md, owner-selected scope "Full recommended (+ debt)". All new user-facing strings live in `COACH_COPY` (src/lib/engine/fi/coach…
- **#93** (Phase R5): Coach Principles — P0.4 Conscious-Spending bucket lens (C6 · Sethi). New pure engine `src/lib/engine/spending-plan/conscious.ts` `mapToConsciousBuckets(plan: SpendingPlan): ConsciousBuckets` — a strict RE-PARTITION of `c…
- **#94** (Phase R5): Coach Principles — P0.5 Automation blueprint (C7 · Sethi/Babylon/Ramsey). New pure engine `src/lib/engine/automation/blueprint.ts` `buildAutomationBlueprint(input): BlueprintStep[]` — orders pay-yourself-first SAVINGS (g…
- **#95** (Phase R5): Coach Principles — Wave 3 debt payoff ENGINE (C9 · Ramsey; Conflict A). New pure module `src/lib/engine/debt/payoff.ts` `planDebtPayoff(input): DebtPayoffResult` — fixed-installment amortization with snowball (smallest b…
- **#96** (Phase R5): Coach Principles — Wave 3 schema migration: per-Account `minimumPaymentCents Int?` for non-card LOAN/MORTGAGE liabilities (cards keep using Statement.minimumPaymentCents; the map proved no Account-level minimum existed a…
- **#97** (Phase R5): Coach Principles — Wave 3 Debt Freedom planner UI + Ask intent (C9 · Ramsey; Conflict A). Shared read-path `src/server/debt.ts` `loadDebtAccounts(userId): DebtInput[]` — reads the finance snapshot, filters CREDIT/LOAN wi…
- **#98** (Phase R5): Coach Principles — M7 hostile-critic pass (8 read-only dimension reviewers + the e2e a11y gate). **6 P1s confirmed and FIXED, each regression-locked.** (1) DEBT divergence guard tested the PORTFOLIO TOTAL, so one never-a…
- **#99** (Phase R5): Coach Principles — Wave 4 book-coverage completion: surface the last 2 of the 9 books as content lines (the plan §2 line-69 owner option, taken on "continue"), bringing visible coverage 7/9 → **9/9**. **C11 Kiyosaki** (R…
- **#100** (Phase R6): Google OAuth signIn now enforces the SAME invite-only allowlist as email/password signup (new `src/lib/auth/google-provision.ts`; `effectiveAllowlist`/`OWNER_ALLOWLIST` moved to `allowlist.ts`); existing accounts still s…
- **#101** (Phase R6): Plaid integration hardening pass (pre-sandbox, off the 6-agent audit: 1 P0, 8 P1). The integration was real code but had never run against a live Plaid — these fixes make the FIRST run correct + diagnosable rather than s…
- **#102** (Phase R6): Plaid Link UI robustness pass (off a 6-agent adversarial audit of the never-executed browser Link flow; the wiring mapped clean but two P1s would bite a real session). **P1a — a successful link could report failure.** `l…
- **#103** (Phase R6): Dev-mode CSP fix — the Plaid Link button (and ALL client interactivity) was dead under `npm run dev`. Root cause found by driving headless Chromium (Playwright) against the running dev server: the page threw `EvalError: …
- **#104** (Phase R6): OAuth redirect support for real OAuth banks (Chase/BofA), gated on `PLAID_REDIRECT_URI` so it stays fully dormant until configured. (1) Extracted pure, unit-tested `linkTokenParams(userId, redirectUri?)` in plaid.ts; `cr…
- **#105** (Phase R6): Wired the background-sync cron in vercel.json — `"crons": [{ "path": "/api/cron/sync", "schedule": "0 11 * * *" }]` (once daily, ~06:00 ET). The route already existed and was Vercel-cron-ready (constant-time `checkCronBe…
- **#106** (Phase R6): Scheduled `/api/cron/reminders` daily (`0 12 * * *`, one hour after the sync cron at `0 11`) in vercel.json — same missing-schedule gap as #105. The route is DORMANT-SAFE: with no `RESEND_API_KEY`, `emailProviderConfigur…
- **#107** (Phase R6): Gated `/plaid-oauth`'s `receivedRedirectUri` on token presence (follow-up to #104, surfaced by a real signed-in Playwright render against dev :3003). Visiting the OAuth return URL with an `oauth_state_id` but no stored l…
- **#108** (Phase R6): Inbox full-category picker. The triage inbox only ever surfaced the AI suggestion + 3 computed alternatives; there was NO path to the other ~80 categories in the normal accept flow (the full `ALL_CATEGORIES` was passed i…
- **#109** (Phase R6): Golf keyword categorization. User report: "northwest golf course literally says golf in the name. shouldn't this auto golf rather than shopping?" Root cause: in `normalize.ts`, "GOLF" existed ONLY inside "TOPGOLF" — stan…
- **#110** (Phase R7): Category hide/show ("delete the ones we don't use"). User asked for "an area to create new categories and delete the ones we don't use at least for each individual account"; on the scope fork the user chose **one shared …
- **#111** (Phase R7): Custom (user-created) categories — architecture + foundation (R7b-1/2). User said "yes" to building real custom categories (e.g. a "Golf" line). A 20-file read-only audit workflow (21 agents) mapped EVERY category id→nam…
- **#112** (Phase R7): Custom categories — implementation complete (R7b-3..6) + 2 Hostile-Critic cycles, all on the #111 optional-meta foundation. **R7b-3** threaded the optional `meta` map through every analytics engine (reports/trends/fi-ins…
- **#115** (Phase R7): Insurance carriers → insurance-family subcategories (owner taxonomy decision). User: dental insurance like Delta Dental should file "under insurance with subcategory of dental, medical, eye, etc." The taxonomy is ALREADY…
- **#116** (Phase R7): Backfill categorization — re-run the improved categorizer over the existing review pile. User asked to build a backfill (and to enable the prod LLM, handled separately as setup). Problem: the keyword/merchant layer runs …
- **#117** (Phase R7): Backfill LLM second pass + concurrent-confirm (TOCTOU) guard. User approved extending the #116 backfill with the LLM (and is setting XAI_API_KEY in Vercel separately). Split the backfill into a testable core + thin actio…
- **#118** (Phase R7): REC-2 — recurring INCOME raises must not read as "price increases". The 2026-06-26 empty-state hostile critic flagged that a recurring income series whose amount ROSE (a pay raise) surfaced as a red "price increase" cost…
- **#119** (Phase R7): Production HSTS header + privacy-doc accuracy (2026-06-26 handoff pending list). (1) next.config.ts securityHeaders now appends `Strict-Transport-Security: max-age=63072000; includeSubDomains`, PRODUCTION-GATED via `...(…
- **#120** (Phase R7): Test + e2e SQLite DB relocated OFF the OneDrive-synced tree (the deferred #16/#17 durable fix). Root cause: the unit suite + the e2e server/seed resolved DATABASE_URL to the repo-root file:./dev.db, under OneDrive, whose…
- **#121** (Phase R7): Working tree RELOCATED off OneDrive to C:\dev\Aimplifi — the owner-gated COMPLETE half of the #16/#17/#120 e2e fix, now executed and verified. Non-destructive: robocopy'd the active checkout (excluding regenerable node_m…
- **#122** (Phase R7): Retirement planner — pure DECUMULATION/accumulation projection (the declared investments gap, a clear Simplifi win). The FI engine (#3) already owns accumulation-to-FI (monthsToFI/coastFI/fiNumberCents); the genuine miss…
- **#123** (Phase R7): Retirement planner — user-editable planning inputs + interactive what-if (the #122 follow-up; owner-chosen). #122 hardcoded the planning ages/inflation (RETIREMENT_ASSUMPTIONS 40/65/95/250); this makes them PERSONAL. **S…
- **#124** (Phase R7): Live brokerage-HOLDINGS ingest from SimpleFIN — real positions from a connected SimpleFIN INVESTMENT account now flow into the Holding model + the #77 portfolio engine (the roadmap's named investments LATER item; the las…
- **#125** (Phase R7): "Plan in Words" debt slice — INVERSE debt-free-by-date planner (AI Differentiation Plan §1.1, the only build-now concept that survived adversarial review; owner-chosen). State a goal date ("be debt-free by December 2027"…
- **#126** (Phase R7): "Plan in Words" savings slice — INVERSE savings-goal-by-date planner (AI Differentiation Plan §1.1, the next sequenced slice after #125; "continue"). State an AMOUNT + a DATE ("save $20,000 by December 2028") and the app…
- **#127** (Phase R7): Live-provider ingest CONTRACT AUDIT + first money-bug fixes (prompted by the owner correcting a stale claim: Plaid is on PLAID_ENV=production and SimpleFIN Bridge has all their accounts linked, so the mappers — written/t…
- **#128** (Phase R7): SimpleFIN PENDING-transaction reconcile (live-ingest backlog #4, the #127 audit's item 1). SimpleFIN sends a stateless per-window snapshot with NO removed[], and the spec lets a pending txn's id CHANGE when it posts — so…
- **#129** (Phase R7): SimpleFIN holdings AUTHORITATIVE total — live-ingest backlog #5 (the #127 audit's item 2). #124 stored ONLY a per-share priceCents and the engine recomputed marketValue=round(quantity×priceCents), which LOSES low-price/h…
- **#130** (Phase R7): Plaid per-sync account-balance REFRESH — live-ingest backlog #6 (the #127 audit's item 3). `PlaidProvider.syncTransactions` refreshed balances ONLY for accounts echoed in the `/transactions/sync` `accounts` array (deposi…
- **#131** (Phase R7): "Plan in Words" retirement slice — INVERSE retire-at-age planner (AI Differentiation Plan §1.1; the THIRD and final Plan-in-Words slice after debt #125 + savings #126, completing the owner-set sequence debt→savings→retir…
- **#132** (Phase R7): Plaid credit-liability statement-field correctness — live-ingest backlog (the #127 audit's remaining credit-card P2s), on the owner's REAL connected Plaid cards. `mapPlaidLiabilityToStatement` had two field bugs corrupti…
- **#133** (Phase R7): SimpleFIN all-unmappable-holdings DATA-LOSS guard — live-ingest backlog (the #127 audit's P2), on the owner's REAL linked brokerage. `syncFromSimplefin`'s INVESTMENT branch reconciled holdings whenever `acct.holdings !==…
- **#134** (Phase R7): Plaid mortgage/student loans → calendar + reminders (the BIGGEST remaining #127 live-money item; owner picked the surface via AskUserQuestion = "Calendar + reminders", NOT the cash-needed dollar headline). Two parts. **(…
- **#135** (Phase R7): Currency guard — withhold non-USD accounts from net worth + spending (the #127 live-ingest audit's item #3/#10, "currency never read"). The app does NO foreign exchange, so a non-USD / zero-decimal (JPY/KRW) feed balance…
- **#136** (Phase R7): Triage write-in custom categories (increment 1 of the owner's "clunky inbox + write-in" sweep). (1) UI: "+ New category" in triage alternatives mode (triage-inbox.tsx) — inline mini-form (name / parent-group select prefi…
- **#137** (Phase R7): Searchable triage category picker (#136 increment 2). Replaced the alternatives-mode native <select> (~84 unsearchable options — the owner's "clunky" complaint) with a labeled search input + scrollable option-button list…
- **#138** (Phase R7): Register write-in category (#136 increment 3, completes the owner's 3-step sweep). "+ New category" button + mini-form INSIDE the register's category-menu; on success it hands the new id to the EXISTING two-step confirm …
- **#139** (Phase Write-in prefill: the picker search query IS the new-category name (owner request, live-prod feedback on #136-#138). Both write-in surfaces prefill the mini-form's name from the live search query at open (triage openNewCategory + register openNewCat; .trim() only, still editable, submit re-normalizes as before) — the user searches "Golf", misses, and creates it without retyping. Triage additionally: Enter on a ZERO-match query opens the prefilled form (the keyboard completion of the "create it below" hint); the register search deliberately gains NO Enter semantics (it has none today — keyboard parity is the shared-CategoryPicker follow-up). Focused Checker (wf_e902ad02, 3 lenses → adversarial verify; 6 raw → 5 confirmed, 1 refuted): 2 P1 classes FIXED pre-commit + e2e-locked — (a) the new Enter branch lacked !newCatOpen: the search box stays interactive beside the open form, so a fresh zero-match Enter re-ran openNewCategory and SILENTLY clobbered the user's edited name/group/discretionary; (b) a HELD Enter auto-repeats into the just-mounted name input (autoFocus moves focus mid-press) and created+filed a category with never-reviewed defaults → e.repeat guards on the search Enter branch and the name-input Enter. The stale-build e2e run REPRODUCED (b) exactly (rule prompt offering "Pickleball Dues") before the guards were served — the finding is demonstrated, not theoretical; a Playwright probe confirmed CDP delivers repeat=true on a held key. Test-adequacy P2 fixed: locks added for multi-match-Enter-does-nothing, the repeat-chain no-op, and draft-survival. Accepted residual (STATUS): two DISCRETE rapid Enters still create+file — indistinguishable from intent (filing undoable, category deletable in Settings, durable rule stays consensual). Process lock learned: playwright's webServer is `next start` + reuseExistingServer — e2e must NEVER run concurrently with verify's `next build` (a stale .next silently tests the previous tree; that's what the first "failure" was). Gate (real 2026-07-01): bash scripts/verify.sh → ✅ VERIFY GREEN, 1476 unit/116 files, tsc/eslint/build clean. E2E (honest): triage write-in spec with ALL new locks GREEN 7.9s on the final tree; register race lock GREEN; register happy path witnessed green through the prefill assert + confirm pane ×3, tail = the documented code-independent action-apply stall (A/B'd at HEAD line 230 this session) — full pass UNVERIFIED until reboot.): The owner's live feedback named exactly the friction the creatable-combobox pattern removes; prefill is one line per surface, but the checker pass proved the Enter affordance needed two guards the happy path would never …
- **#140** (Phase iOS focus-zoom fix (owner report on #139 in prod: "formatting is terrible — it zooms in with that dropdown box"). Root cause: iOS Safari force-zooms the viewport when a focused form control's font-size is <16px; the app has NO shared Input component — every raw input/select is Tailwind text-sm (14px), so the entire app's forms zoom on focus on iPhones, and the register category-menu (which AUTOFOCUSES its search on open) zoomed the moment it appeared. Fix at the ROOT, not the call sites: a documented globals.css rule floors form-control text at 1rem under `@media (pointer: coarse)` — `input[class]:not([type=checkbox]):not([type=radio]), select[class], textarea[class]` (the [class] selector reaches 0-1-1 specificity, outranking the 0-1-0 text-sm/text-xs utilities without !important); checkboxes/radios excluded; desktop (fine pointer) keeps compact 14px, so the rule is inert everywhere the bug cannot occur. Also: the register category-menu widens w-56→w-72 (a 224px dropdown hosting a create form on a 380px phone was genuinely cramped) with max-w-[calc(100vw-2rem)] so it can never exceed a narrow viewport. e2e-locked: computed font-size ≥16px asserted in the touch-emulated mobile project for the triage search + write-in name input AND the register search (proves (pointer:coarse) matches in the Pixel-5 emulation — measured, not assumed). Gate (real 2026-07-01): verify.sh → ✅ GREEN 1476 unit/116 files; triage write-in spec GREEN 7.7s incl. both zoom locks; register race GREEN 4.6s; register happy path witnessed green through the zoom lock + prefill + confirm pane — its once-click tail is the session's documented environmental stall (A/B'd at HEAD ×2 today).): One media query kills the whole bug class app-wide (auth, manual entry, settings, statement forms all had it) instead of patching the one dropdown the owner happened to touch; per-control text-base sm:text-sm would have …
- **#141** (Phase Currency-guard disclosure banner (#135 accepted residual 18 — "a withheld figure beats a silently wrong one, but it must not vanish SILENTLY"). One shared `CurrencyExclusionBanner` on the dashboard + /accounts: count + currency list of what the guard withholds, `role="status"` (persistent qualifier, not an interruption), renders NULL for all-USD users (byte-identical pages). Design: (a) ALL copy lives in the pure `withheldBannerCopy()` (currency.ts) so every singular/plural/opaque grammar branch is unit-locked — the component only places it; (b) title says "not in U.S. dollars", NOT "foreign currency" (crypto via Plaid `unofficial_currency_code` is a first-class withheld case and isn't foreign); (c) display tokens = letter codes 3–5 chars, uppercased + deduped — numeric ISO ('840'), 2-letter fragments ('US'), and SimpleFIN currency URLs fold into "other currencies" instead of being pasted into user copy; (d) disclosure inputs are the exact complement of the withhold (`summarizeWithheldAccounts` re-applies `isSupportedCurrency`; the dashboard's DB where-clause is `NOT` the guard's OR-predicate) so banner and figures cannot disagree; (e) all-foreign /accounts gets a withheld-aware empty-state variant ("No U.S.-dollar accounts yet") so the page can't claim "No accounts yet" under a banner saying they're saved; (f) e2e strategy: the seeded demo user is all-USD and SHARED across fully-parallel specs, so the positive path uses an ad-hoc @aimplifi.test signup + the guarded, idempotent scripts/e2e-add-foreign-account.ts (refuses unless DATABASE_URL === E2E_DB_URL exactly). Scope stays dashboard + /accounts per residual 18; register//investments follow-up recorded as STATUS residual 23. Hostile Checker wf_de889cf4: 11 confirmed (1 P1 — the negative spec's zero-render anchor was ABOVE the Suspense boundary and passed against the loading skeleton, re-anchored on page content — + 10 P2, 7 fixed, 3 accepted as STATUS 23–25), 6 refuted. Gate (real 2026-07-02): verify.sh → VERIFY GREEN, 1492 unit / 116 files, tsc/eslint/build clean; disclosure e2e 2/2 GREEN incl. axe AA with the banner present; auth.spec 3/3 GREEN.): Alternatives considered: per-page banners everywhere the guard filters (rejected for now — highest-traffic disclosure first, one consistent sentence that reads app-wide from the landing page; extending is one import + on…
- **#142** (Phase Merchant-identity normalization upgrade (PULSE_CATEGORIZATION_FIX Phase 3a; measured baseline: 50 real merchants → 63 pipeline identities, 33% review rate). (a) Clean-SECOND-CHANCE table matching: after the raw string misses KNOWN_MERCHANTS, the cleaned descriptor is retried but accepted ONLY if the pattern consumes the ENTIRE cleaned name — 'SQ *STARBUCKS #4471' → 'Starbucks' converges (the fix-doc's canonical example) while 'SQ *NETFLIX AND CHILL BAR' stays local (the cycle-2 critic's anchored-pattern lock holds, re-asserted). (b) cleanDescriptor: interior processor asterisks scrubbed; trailing 'CITY ST' location suffixes stripped with two safety rails — greedy state match (never eats a name token: 'OLD NAVY CA' keeps its name) and the city token only goes when a state was just stripped AND ≥2 name tokens remain. (c) Pattern robustifications on EXISTING entries (canonical/category unchanged): Kroger \b (QFC sub-banner form), TARGET(\.COM)?\b (bare store numbers + domain), (THE )?HOME ?DEPOT(\.COM)?, SHELL (OIL): SERVICE), UBER \*? (TRIP|EATS) phone-variants. (d) Uber Eats two-table drift RESOLVED to 'food-delivery' (same canonical previously filed dining or food-delivery depending on which variant arrived); seed impact hand-veri…
- **#143** (Phase Merchant-group triage queue + one-action group filing (PULSE_CATEGORIZATION_FIX Phase 3b). Queue unit = normalized merchant: pure `groupReviewRows` (src/lib/engine/categorize/group.ts) groups review rows by merchantId — EXACT rawDescriptor for aggregates (mirrors similarTransactionsWhere, #23, so a card's count can never drift from what its action mutates) — sorted for LEVERAGE (count desc: one decision clears the most rows). `getTriageGroups` = ONE findMany + pure grouping (kills the per-card N+1 similarCount); `getReviewCount` now counts GROUPS (the badge means "decisions left", same groupKey source of truth). HONEST suggestions: the pipeline's verdict when unanimous across the group, else null — the amount-based bestGuess ('Shopping' on 144/144 baseline cards) is not offered at group level. `fileMerchantGroup(anchor, category)`: scope re-derived server-side from the anchor (client lists never trusted); one $transaction = per-row Corrections (undo restores each) + prediction ground truth + transaction updateMany + a durable priority-100 rule when rule-eligible → the NEXT ingest of the merchant auto-files silently at 9900 (trust on repeat = certainty, fix-doc mechanic 2; locked end-to-end in triage-groups.test.ts via loadUserRules → categorize). CONSENT: the group card is explicitly merchant-scoped ("File all N · future ones file automatically"), so the card IS the #36 consent — no second prompt on the group surface; the register's per-transaction two-step confirm is unchanged, and aggregates never create rules. getTriageItems retained (register/tests); UI switch is Phase 3c. Gate: verify GREEN 1513 unit / 119 files (+6 integration locks incl. ownership rejection + aggregate no-rule + mixed-suggestion honesty).): Alternatives: extending applyToAllSimilar with a createRule flag (rejected — #36's rule-less batch contract is load-bearing for the old surface and its consent doc); grouping by Merchant.canonical instead of merchantId (…
- **#144** (Phase Merchant-group triage UI (PULSE_CATEGORIZATION_FIX Phase 3c). triage-inbox.tsx rebuilt group-first: one card = one merchant (count, total, date range, descriptor variants), counter reframed "N merchants left" (+ "first 15 this pass" when >15), data-remaining = groups. HONEST suggestion contract: a real unanimous verdict shows as the one-tap "✓ File all N"; otherwise the card says "none yet — pick once for all N" and BOTH the accept button and swipe-right refuse to fabricate (swipe snaps back — the old flow silently filed bestGuess='Shopping' here). Picker/write-in (#136-#139 with every checker lock: focus-on-open, group-label search, Enter-single-match, zero-match prefill, e.repeat + IME guards, inline errors, overlay prune) and split (both parts now selectable — split-first-cat) are retargeted through shared render helpers; "☰ One by one" (or long-press on multi-row groups) drills into per-row pick/split where the classic "Always / Just this once" prompt survives for singles. undoCorrections/undoSplit return the GROUP queue. e2e phase2-triage.spec adapted; THROUGHPUT LOCK RESCOPED honestly: the old "<15 interactions to clear the backlog" only passed by mass-accepting fake suggestions on checks/Zelle (fast, silently wrong); the new lock asserts (a) SPEC.md:28's actual promise — the WEEK slice <15 interactions/<60s modeled — and (b) the structural claim: full 60-day backlog ≤ 2×groups+2 interactions (cost scales with decisions, never rows). MEASURED on the demo seed (isolated runs ×2 + in-suite pass): groups=12, total=25 interactions (≤26), week-slice=8 ≈ 32s. Gate: verify GREEN 1513/119; phase2 e2e 4/4 witnessed (throughput green ×3 total; one serial-mode run hit the documented #16/#17 pending-stall — position varies, reproduced today at 69a335b too).): Alternatives: keeping the batch button + rule prompt on group cards (rejected — the group card IS the batch and IS the consent; two more taps per decision for ceremony); modal chunk gates every 15 groups (rejected — fram…
- **#145** (Phase Currency-disclosure banner extended to /investments (STATUS residual 23, the checker-recommended first target). page.tsx adds getWithheldAccountSummary(userId) to its existing Promise.all (mirrors the dashboard wiring — a page-level call avoids changing getInvestments' return shape) and passes `withheld` to InvestmentsView; the banner renders after the title, before RetirementOutlookCard, copy still built ONLY by the pure withheldBannerCopy(). The empty state is made withheld-aware: "No investment holdings yet" would be FALSE for a user whose only holdings sit in a withheld non-USD brokerage (the same contradiction shape the #141 checker confirmed on /accounts) → withheld.count>0 branches to "No U.S.-dollar investment holdings yet. Add holdings to a U.S.-dollar investment account…", which deliberately does NOT claim withheld holdings exist (a withheld account may have none); zero-withheld users get the original copy byte-identical. e2e extends BOTH paths: the negative zero-render lock anchors on investments-summary (page content below the route-group Suspense — the #141 P1 anchor rule; the demo user always has seeded holdings), the positive path asserts banner presence/copy + the withheld-aware empty state + withheld account names absent + a second axe AA scan (the empty-state variant renders only on this surface, so the /accounts scan can't cover it). Checker (wf_637cc5e5, 3 lenses → adversarial verify): 0 P0/P1; 2 P2 CONFIRMED + fixed (zero-withheld empty-state branch unlocked → new byte-identity e2e on a --usd-only fixture; /investments name-absence assertions structurally couldn't witness the currency guard → fixture gains an INVESTMENT-typed EUR brokerage so the absence assertion is a real guard witness, banner counts 2→3); 2 refuted (all-foreign gates to EmptyDashboard = accepted-22 pattern; "U.S.-dollar" qualifier reads fine for any withheld mix). Gate: verify GREEN 1520/120; targeted e2e currency-disclosure 3/3 (15.4s) + investments 4/4, no stall.): Alternatives: embedding withheld in getInvestments' view type (rejected — a server-shape change for a display concern; the page-level Promise.all matches the dashboard idiom); leaving the empty state untouched (rejected …
- **#146** (Phase Checker cycle-2 fixes (20/20 confirmed against the cycle-1 stack). CONCURRENCY DESIGN: all five check-then-act guard sites (Plaid + SimpleFIN sync guards, the pending→posted transplant, fileMerchantGroup, recategorize) now run through `serializableTx` — one helper in src/lib/db.ts issuing the interactive transaction at SERIALIZABLE isolation with a bounded (3-attempt) retry on P2034 only; every other error fails loudly on attempt 1. Why Serializable over compare-and-set: the preserve predicate depends on Correction rows, which have NO relation/FK to Transaction, so an updateMany WHERE cannot re-assert "corrected"; and CAS cannot stop two concurrent group-files from both missing the rule-dedupe findFirst. Postgres detects the write-write conflict (first-updater-wins on the row) even when the OTHER writer runs at READ COMMITTED, and SSI predicate locking catches the read-then-insert dedupe race between two serializable transactions; a detected conflict re-runs the whole fn against fresh state (DB-only side effects at every site). Probed 2026-07-02: the better-sqlite3 driver adapter ACCEPTS the explicit level (semantically a no-op — single-writer), so one uniform code path. SPLIT LIFECYCLE INVARIANT: "a split parent's children always sum to its amount, and children share the parent's status; when the bank breaks the sum (amount drift), the split DISSOLVES to one row on the fresh verdict, back into review" — enforced across Plaid id-churn transplant (carries the split: new container, children re-pointed + posted, corrections follow), same-id refresh in both providers (drift dissolves; preserved splits post their children), and buffered removed[] (a canceled split charge cascades its children — phantom spending otherwise). Splitting PENDING rows stays SUPPORTED (critic2 F1 models the seeded pending Zelle split) — the status-guard alternative was rejected as a capability regression. Removed[] is now buffered per item and applied after the page loop (Plaid doesn't guarantee the removed/added twin share a page). Create/create sync races: catch P2002 → guarded-update fallback (both providers — restores CQ-2 without losing the cycle-1 verdict guard that forced the upsert split). Merchantless file scope pins merchantId (IS NULL) so the raw: card's action partitions exactly like groupKey; aggregates stay descriptor-only (one agg: card legitimately mixes merchantless CSV + merchanted synced rows). ensureUnconditionalRule (shared by fileMerchantGroup + recategorize) dedupes on the FIVE condition columns being null. groupEmptied is derived BEFORE dispatch from committed state (React updaters are pure; eager evaluation is an optimization, not a contract).): Alternatives: per-site CAS updateManys (rejected — cannot express the corrected predicate, leaves the dedupe race); a DB unique constraint on (userId,merchantId,categoryId) (rejected — NULL condition columns make the con…
- **#147** (Phase Checker cycle-3 fixes (16/16 confirmed on the cycle-2 increment). SPLIT-LIFECYCLE COMPLETION (P0): SimpleFIN pending→posted NEW-ID churn has no id link, so the split cannot follow — reconcilePendingTransactions now DISSOLVES stale pending split parents (both passes: in-window per synced account + the 32-day age-out) by deleting parent AND children inside a serializable tx with the parent list read in-tx; pass 2's fresh posted row goes through normal ingest. Deletion IS the dissolve here: the alternative (heuristic matching of the new posted row by account/amount/date to transplant the split) was rejected — the house already ruled heuristic identity out (cycle-1 residual) and a wrong match would misfile real money. This also invalidates DECISIONS #128's "self-healing ≤32d" premise FOR SPLITS — the old isSplitParent exclusions made the stale split immortal. DISSOLVE-FORCES-REVIEW (P1): all three dissolve sites now write needsReview:true + confidenceBps:null over the pipeline verdict — a destroyed user decision re-decides UNCONDITIONALLY; the user's own merchant rule still supplies the SUGGESTION (categoryId) but never the auto-file. THE SIXTH WRITER (P1): applyCategory rebuilt as ONE serializableTx with fresh in-tx reads (fromCategoryId from current state; rule mint via the shared helper; row update + prediction truth in-tx) — it was four bare statements on the highest-traffic path (singles fileRow, recategorize scope:'one'). makeRuleFromCorrection likewise. RETIRE-ON-CHANGED-MIND (P1): ensureUnconditionalRule now SUPERSEDES unconditional rules of the same merchant to a DIFFERENT category before reuse-or-mint — "this merchant files to C" is a statement about the merchant, not just the pair — closing the stale-rule-wins tie-break (probed: coffee→dining recategorize previously kept filing coffee forever). Conditional rules are never superseded (narrower intent). Undo of the new correction cannot resurrect a retired rule — accepted: re-minting is one tap, a silent wrong-category auto-file is not. WIRING LOCK (P1 gate): tests/unit/serializable-wiring.test.ts pins the guard-site wiring (spy on the four triage actions + provider source pin ≥3 serializableTx sites and zero interactive prisma.$transaction) — the checker's sed-strip experiment now fails. P2s: removed[]-cascade read moved in-tx; guardedVerdictRefresh skips deleted-in-window rows (was: P2025 aborted the whole SimpleFIN pass-2 loop); audit events gated rule.create vs rule.reuse on minted (provenance-honest); ledger miscounts corrected in place.): Alternatives: forbidding pending splits (rejected again — capability, critic2 F1); keeping deleted-rule tombstones for undo-resurrection (rejected — schema change for a one-tap re-mint); catching P2025 outside the tx (re…
- **#148** (Phase Cycle-5 fixes (OWNER-AUTHORIZED beyond the 4-cycle cap; cycle-4 confirmed 1 P1 + 8 P2). REVIEW PIN (the P1): `Transaction.reviewPinned Boolean @default(false)` — set by every sync DISSOLVE (both providers' same-id drift + the Plaid transplant dissolve), respected by the preserve predicate (preserve = isSplitParent): | reviewPinned || (corrected && !needsReview)), carried ACROSS id churn by the transplant (a pinned predecessor re-creates pinned — otherwise churn was a pin-laundering path), and cleared by every USER filing action (app…
- **#149** (Phase Currency-disclosure banner extended to the FINAL FIVE surfaces (register `/transactions`, `/triage`, `/recurring`, `/reports`, `/coach`) — CLOSES STATUS residual 23: every money surface now discloses withheld non-USD accounts (only residual 25, the inline per-projection assumption copy, remains as a broader deferred item). Reuses the shipped mechanism UNCHANGED: each server page adds `getWithheldAccountSummary(userId)` (via Promise.all) and mounts `<CurrencyExclusionBanner summary={withheld}/>`; copy still built ONLY by the pure `withheldBannerCopy()`; the banner SELF-NULLS at count 0, so all-USD users — including the seeded demo user — render ZERO banner DOM → demo/golden byte-identical (all pinned values live outside the banner subtree). Mount style follows the #141/#145 convention per surface: mounted INLINE in the page for the three inline-JSX server pages (transactions/triage after the intro/header, coach after the `<h1>` — each placed strictly AFTER that page's zero-account `EmptyDashboard` gate so auth.spec's onboarding contract holds), and THREADED as a `withheld` prop into the VIEW for the two view-backed pages (`RecurringView` server, `ReportsView` `'use client'` — a plain `{count,currencies}` crosses the RSC boundary), mounting inside the view's own `max-w` column so no redundant wrapper is added and the demo DOM stays byte-identical. e2e (`currency-disclosure.spec`) extends BOTH paths across all 5: the negative zero-render lock anchors on each page's below-Suspense content testid (`txn-list`/`triage-inbox`/`recurring-hero`/`income-expense-chart`/`fi-number` — the #141 anchor rule; each proven to render for the demo user so the absence assertion is non-vacuous), and the positive path asserts banner-present + `'EUR, GBP'` + a per-surface axe A/AA scan WITH THE BANNER PRESENT (phase5-a11y's triage/coach pins run on the all-USD demo user, where the banner self-nulls, so they never exercise it). Focused Checker (wf_a7eaf280, 3 lenses → adversarial verify): **0 P0/P1**; 2 P2 CONFIRMED + FIXED — (a) axe covered only /recurring → scan folded into the 5-surface positive loop; (b) the initial /recurring + /reports page wrappers duplicated the view's own `max-w` root (an inert extra `<div>`, so NOT strictly byte-identical) → re-threaded `withheld` into both views per the shipped convention, wrapper removed. 8 candidates refuted (self-null holds; EmptyDashboard gates preserved; `role="status"` wins over Alert's default `role="alert"`; single-mount [bare-locator strict mode]; RSC boundary valid; anchors non-vacuous; copy matches; pure-all-foreign→EmptyDashboard is a PRE-EXISTING documented residual, not introduced here). Gate (real 2026-07-02): `bash scripts/verify.sh` → VERIFY GREEN, **1554 unit / 122 files**, tsc/eslint/build clean; currency-disclosure e2e **3/3** (19.2s, no stall).): Alternatives: one uniform wrapper style for all 5 (rejected — the two view-backed pages need prop-threading to stay byte-identical, matching the shipped accounts/investments siblings); accept-and-document the wrapper red…
- **#150** (Phase Currency-exclusion assumption stated INLINE at the flagship projections (#135 residual 25 — CLOSED). New pure `withheldInlineNote(summary)` in currency.ts returns a compact basis note ("Excludes N account(s) not in U.S. dollars — Aimplifi doesn't convert other currencies yet.") when count>0, else null. Rendered at the /coach FI card (after `fi-basis`, via a `currencyNote` prop) and the /reports spending total (computed in ReportsView from the `withheld` prop it already receives — #149), each gated so all-USD users — incl. the seeded demo — render nothing (byte-identical). Rationale: the app's own coaching style REPEATS each projection's assumptions inline ("assuming X% returns" recurs across fiNumber/yearsToFI/coastFI/opportunity), so stating the currency basis where the FI number + spending total are shown — not only in the page-top banner (#149) — matches that pattern; the note is ACCURATE because the currency guard filters transactions AND accounts AND investments to USD-only in the shared snapshot (verified: getCoachData portfolio/flows + getReports months/breakdown both read the filtered `getFinanceSnapshot`), so these figures genuinely exclude withheld accounts (live Plaid delegates to the same method). Copy lives in currency.ts (not COACH_COPY), mirroring withheldBannerCopy; guardrails locked in currency.test.ts (null at 0, singular/plural, no-shame + states-assumption scan). e2e extends currency-disclosure.spec's existing loops: the note is asserted PRESENT on /coach + /reports for the withheld fx user (axe-scanned with it rendered) and ABSENT for the all-USD demo user (byte-identity). Focused adversarial Checker (single reviewer, sonnet): **0 P0/P1/P2** — byte-identity, accuracy (both consumers read the filtered snapshot), grammar, and axe coverage independently confirmed; tsc/eslint/vitest re-run clean. Gate (real 2026-07-02): verify GREEN **1558 / 122** (+4), currency-disclosure e2e **3/3** (21.1s).): Alternatives: duplicate the full banner sentence per projection (rejected — noise; the compact basis note states just the assumption, matching the terse "assuming X%" style); put the copy in COACH_COPY (rejected — it's a…
- **#151** (Phase Loan payments folded into the /forecast balance projection (#134 follow-up — the owner-gated de-dup design decision, delegated by "do all recommended"). GROUND TRUTH (understand workflow wf_aae820f1): a loan payment has TWO representations — a loan-due obligation (keyed by the loan Account → calendar+reminders) and, potentially, a recurring-detected checking scheduled row (→ forecast+calendar) — and there is NO structural key linking a checking-account scheduled row to the loan Account it pays (a scheduled row carries only accountId=checking + free-text description; the loan side keys on the loan Account's type/minimumPayment/dueDay). A cross-source de-dup would require heuristic money-matching (amount/servicer/cadence) — the class the house has repeatedly rejected (a wrong match misfiles money). DECISION = **Option D**: the forecast (a balance trajectory) UNDER-projected checking because a loan payment really debits checking but is NOT a scheduled row (the demo's $385/mo auto-loan went invisible after #134 removed the `sched-autoloan` stand-in); feed loan obligations into the forecast from their ONE safe structural source (the loan Account) via a pure `loanObligationsToScheduledFlows` (MONTHLY outflow on the RAW `dueDate`, never `effectiveDueDate` — a business-day-shifted anchor would drag the shift into every future month). `getCashFlowForecast` concats them onto the checking scheduled flows (same holiday+`selectLoanObligations` derivation as finance.ts:59-76). NO de-dup attempted (none is safe). Rejected: A (loan-due canonical + suppress the scheduled row) & B (scheduled canonical + suppress loan-due) — both need the nonexistent key → heuristic; C (split by surface + re-add a seed scheduled row) — reintroduces the calendar double-display the seed removed. The companion carve-out removal (detect.ts:83-85 `'auto-loan'`) was DECLINED as out of scope: `refreshRecurringForUser` runs ONLY on real Plaid/SimpleFIN sync (never for the seeded demo), so the "latent post-refresh double-count" is NOT demo-reachable, and removing the carve-out would churn ~8 recurring goldens for zero demo benefit — recorded as an optional owner-gated follow-up. Golden-safe: no unit/e2e pins demo forecast milestones (verified); the forecast e2e pins only the unchanged $3,400 starting balance + now the "Auto Loan" upcoming label. Hostile Checker (wf_1a6616ee, 3 lenses → adversarial verify; money-math ⇒ maker/checker): **0 P0/P1**, probe-confirmed the loan folds in exactly 3× at −$385, correct sign, currency-guard-filtered, no golden moves. P2s FIXED pre-commit: (P2-A honesty) EDGE_CASES §LO-H relabeled — the $3,015/$2,630/$2,245 are the loan's ISOLATED contribution / the `forecast.test.ts` known-answer, NOT the on-screen milestone (which also carries payroll/rent/savings; a maintainer diffing them would read a phantom regression); (P2-C) a quantitative server-path test added (`forecast-server.test.ts` drives `getCashFlowForecast('user-demo')` → Auto Loan ×3 @ −$385, closing the wiring coverage gap the text-only e2e left). ACCEPTED residuals (STATUS #134): (1) the narrow non-transfer-ACH double-count (a loan whose ACH is also a recurring checking row now doubles on forecast too — same population already doubling on the calendar, no new victims, no safe fix); (2) a day-31 loan anchored in a short month sticks a day early (pre-existing `expandScheduled` MONTHLY-clamp property, not demo-reachable, ≤1-day shift). Gate (real 2026-07-02): verify GREEN **1563 / 123**, forecast e2e **2/2**.): Alternatives: A/B/C + carve-out (above); anchoring the mapper on `effectiveDueDate` (rejected — monthly drift); attaching loan flows to the loan account's own projection (moot — the forecast projects the single payment/c…
- **#152** (Phase #127 live-ingest tail — two P2 edges closed. **(a) SHARED TICKER VALIDATOR:** the SimpleFIN holdings mapper and manual `addHolding` each carried their OWN copy of the ticker regex (the #127 audit flagged them as drift-prone coupled rules); extracted ONE `parseTicker`/`TICKER_RE` (`src/lib/engine/investments/ticker.ts`) used by both, and WIDENED the charset to accept "/" so slash share-class + crypto-pair tickers (BRK/B, BTC/USD) are no longer silently dropped by the sync mapper (and are now enterable manually). Space-bearing OCC option symbols stay OUT by design (a space is too easily a mis-parsed descriptor, and the full symbol exceeds 20 chars) — a documented skip. **(b) EPOCH→DATE CONVENTION:** `simplefinPostedToDate` floors the unix-seconds instant to its UTC calendar day; SimpleFIN carries no per-account timezone, so a mid-day-stamped feed can land ±1 day from the holder's LOCAL date. This is inherently unresolvable without tz data (any fixed offset just moves the error to another region), so — NO logic change — the convention is now stated precisely in the docstring and PINNED by a boundary test (midnight→day, 23:59:59→same day, +1s→next day); the app treats all business dates as calendar dates (no tz) and no money figure depends on the exact day (only its month). Focused adversarial Checker (single reviewer, sonnet): **0 P0/P1/P2** — regex charset exact (no range trap; `-` literal at class end, `/` literal), no downstream "/" breakage (CSV export doesn't touch `symbol`; the `accountId_symbol` composite key + the portfolio engine treat it as an opaque string; no URL/route embed), coupling genuinely single-source (no residual second regex), all prior tickers still valid, epoch boundary math independently confirmed; tsc + the 3 affected test files (48/48) re-run clean. Gate (real 2026-07-02): verify GREEN **1570 / 124** (+7: parseTicker 5 + slash-holdings + epoch-boundary). Live SimpleFIN path stays UNVERIFIED-labeled (no token); the ledger-corrupting logic is unit + mocked-server tested.): Alternatives: widen the two regexes in place with a "keep in sync" comment (rejected — the audit already saw them drift; one shared rule removes the failure mode); accept space/option symbols by raising the length + allo…
- **#153** (Phase Category picker — SR-listbox a11y parity (the safe half of the shared-CategoryPicker follow-up). The triage picker's option list was plain buttons in a plain div (#137 chose that to ship the searchable picker fast, deferring ARIA); the register's category menu ALREADY uses `role="listbox"` + `role="option"` + `aria-selected` (transaction-list.tsx, axe-passing). Brought triage to the SAME proven pattern: `role="listbox"` + `aria-label` on the `triage-all-categories` container, `role="option"` + `aria-selected={c.id===suggestedCategoryId}` on each option — a screen reader now announces the option list as a listbox with the suggested category selected, consistent across both pickers. Surgical (attributes only; the buttons' testid/click/keyboard behavior is unchanged) and axe-clean — the triage search input sits OUTSIDE the listbox container, so it's actually cleaner than the register's (whose search input is nested inside `role=listbox`). DEFERRED (documented, owner-gated): the FULL shared `<CategoryPicker>` component extraction — the two pickers have genuinely DIVERGENT filing behaviors (triage create-then-file immediately vs register create→once/always confirm #121) + drop-up positioning + row-binding, so one component would be a large parameterized refactor of two just-stabilized, 5-checker-cycle files for low user value (against "surgical changes / don't refactor the unbroken"); AND the register happy-path e2e that would verify it is currently blocked by the documented environmental action-apply stall (reboot-gated). `filterCategoryOptions` (the search/group-label engine) is ALREADY the shared single source. Gate (real 2026-07-02): verify GREEN **1570 / 124**; e2e — the triage write-in AXE test (phase2-triage:109, scans the picker OPEN with the new `role=listbox`) GREEN isolated; the gesture/undo test (:50) GREEN **4/5** runs, the lone failure being the documented `triage-undo` disabled-pending environmental stall (line 105, OUTSIDE this diff), proven a flake by 3 consecutive with-change passes + a pre-change pass (stash-rebuild A/B).): Alternatives: full component extraction now (deferred — better as a deliberate post-reboot task with the register e2e actually verifiable); a strict arrow-key ARIA listbox with roving tabindex (rejected — changes the int…
- **#154** (Phase Household **utility category split** (owner ask: "have you put in basic things like utilities: gas, electricity, water… give the user the option to remove redundant categories"). Added FOUR purely-additive leaves under the "Bills & Utilities" group — `electricity` ("Electricity"), `natural-gas` ("Natural Gas"), `water` ("Water & Sewer"), `trash` ("Trash & Recycling"), all `discretionary:false` — and taught the categorizer to ROUTE real billers into them, split by the SERVICE WORD in the descriptor rather than only the catch-all `utilities`. The catch-all `utilities`="Internet & Utilities" id **and name are preserved byte-identical** (kept as the bucket for combined/municipal bills), so no golden moves: the demo seed has no home-utility biller (COSTCO GAS→`fuel`, COMCAST/XFINITY→`utilities` both via KNOWN_MERCHANTS, unchanged), verified by the unchanged seed metrics (842 txns / 11 recurring / 17 needsReview). Routing changed in BOTH normalize layers — the KNOWN utility-EPAY entry and the GENERIC keyword rule — each split into ordered `trash → water → gas → electricity → utilities` branches; **gas precedes electricity** so a gas biller named "…ENERGY"/"…GAS LIGHT" (CENTERPOINT ENERGY, ATLANTA GAS LIGHT) files as gas, not electricity; every EPAY branch still REQUIRES a biller-payment token so card payments (CHASE EPAY/AMEX EPAYMENT) stay transfers (STATUS #11 invariant preserved). Recurring detection's SUBSCRIPTION_CATEGORIES gained the 4 leaves so monthly utility bills still register as recurring. Ask-assistant synonyms split so "electricity"/"water bill"/"trash" hit their leaf. Answer to ask (2) — "remove redundant categories": this **already exists** and now covers the 4 new leaves — the per-user hide toggle (Settings→Categories, #110; `isHideable` true for all four, test-locked) removes any system category from the pickers without deleting history, and custom categories delete via #111. **Adversarial Hostile-Critic (Workflow, 4 lenses → per-finding verify; then a 3-lens fix-verify): 0 P0/P1/P2 residual after fixes.** Two real self-introduced defects the critic caught and I FIXED before commit: **(P1)** the new `natural-gas` Ask synonym was DEAD — shadowed by the pre-existing bare-`gas`→`fuel` rule (first-match-wins), so "how much on my gas bill?" returned GASOLINE spend; moved the natural-gas synonym ABOVE the fuel rule (bare "gas" still → fuel). **(P2)** the "utilities" umbrella query under-reported — after the split, `catTarget('utilities')` summed only the residual leaf; added a new `SpendTarget` variant `{type:'categories'; categoryIds[]}` and pointed the umbrella at {utilities,electricity,natural-gas,water,trash} (excludes phone/internet/insurance), wired through `isSpendTarget`+`answerSpendByCategory`. P3 precision hardening (same commit): qualified bare brand tokens that misfiled look-alikes (CENTERPOINT→CENTERPOINT ENERGY, SPIRE→SPIRE (ENERGY\): GAS), CASELLA→CASELLA WASTE), dropped bare CITY WATER / REFUSE, fixed the dead `GFL ENVIRO`→`GFL ENVIRONMENTAL` token, and repaired the `\bUTILIT\b` EPAY branch (never matched "UTILITY"/"UTILITIES" — a latent STATUS #11-…
- **#155** (Phase Plaid `personal_finance_category` passthrough (owner-discussed): Plaid returns a per-transaction ML category (`{primary, detailed, confidence_level}`) that ingest already received but IGNORED. Wired it as a DETERMINISTIC (no model call — LOOP #5) rescue signal. New pure `mapPlaidPersonalFinanceCategory(pfc)` in plaid-map.ts → `{categoryId, confidenceBps}): null`: DETAILED leaf → specific Pulse category, PRIMARY fallback, confidence-gated (VERY_HIGH 8800 / HIGH 8000 / MEDIUM 7200; LOW/UNKNOWN/absent → null), all inside `[AUTO_FLAGGED 7000, AUTO_SILENT 9000)` so a PFC-filed …
- **#156** (Phase **SimpleFIN holding-level currency guard** (residual 20 CLOSED): `mapSimplefinHoldings` (simplefin-holdings.ts) received each position's `currency` but never read it, so a non-USD lot inside a USD-labeled brokerage summed into `/investments` at a fabricated 1:1 — the account-level currency guard (#135) only fires on the whole account. Fix: read `h.currency` and WITHHOLD confidently-non-USD positions before aggregation, counting them in a new `withheldNonUsd` field kept DISTINCT from `skipped` (a foreign lot is working-as-intended, not an un-mappable glitch). Threaded through `syncFromSimplefin` -> `SyncResult.holdings` (types.ts) -> `SimplefinResult.holdings` (simplefin-actions.ts). PREDICATE = **account-consistent** `!isSupportedCurrency(canonicalizeCurrency(h.currency))` (one currency rule shared with #135): null/omitted -> USD (golden-safe: demo/CSV/manual carry no currency), 'usd'/'USD' -> USD, any non-USD ISO code / crypto-or-non-ISO URL / opaque token -> withheld. Gate refinement at simplefin.ts (~475): `): | (withheldNonUsd > 0 && skipped === 0)` so a CLEANLY-interpreted all-foreign feed reconciles (prunes stale USD rows) while a MIXED foreign+glitch feed still preserves rows (#133 intact). NO schema change (withhold at ma…
- **#157** (Phase **Root 404 / not-found chrome** (ROADMAP prod-readiness backlog): the error chrome had global-error.tsx (root-layout crash) + (app)/error.tsx (in-shell render throw) but NO not-found.tsx, so any unmatched URL rendered Next's unstyled default 404. Added a branded root `src/app/not-found.tsx` — a lean server component (no 'use client'/reset) rendered INSIDE the root layout (globals.css + dark theme loaded), so unlike global-error.tsx it uses Tailwind + buttonVariants like (app)/error.tsx: an Aimplifi wordmark, a single `<h1>` "Page not found", muted copy, one recovery `<Link>` to /dashboard. `metadata:{title:'Page not found'}` flows through the root `title.template` -> "Page not found · Aimplifi" (empirically confirmed applied by the e2e). ZERO notFound() callers exist (grep-verified) -> an unmatched URL is the only 404 path and resolves OUTSIDE the (app) group, so a single root not-found.tsx is exactly right; no (app)/not-found.tsx (YAGNI — a segment-level one matters only if a route calls notFound()). NO schema change; purely additive UI chrome -> demo/seed/golden byte-identical (a static page touches no financial data). e2e lock (tests/e2e/not-found.spec.ts, 2 tests): authed unmatched -> 404 status + branded content + "Page not found · Aimplifi" title + no nav-chrome + axe WCAG-AA + working /dashboard recovery; unauthed unmatched -> /sign-in redirect (the intended middleware boundary). Gate: verify GREEN 1666/125 (no unit delta — UI chrome, e2e-locked per the #145/#156 precedent); e2e 2/2.): Chose 404-chrome over the higher-visibility "Investments in nav" item: the latter needs an 8th phone nav icon (SECONDARY renders as 7 icons on the phone top bar), exactly the #71 "bar full at 7" constraint prior sessions…
- **#158** (Phase **Register recategorize-picker Escape / outside-click dismissal** (ROADMAP prod-readiness: "Escape/outside-click dismissal for the inline recategorize popover"): the inline category picker on /transactions (transaction-list.tsx, single-controller `openId` model) only closed by re-tapping the chip — no Escape, no outside-click, a real keyboard-operability + usability gap on the app's most-used flow. Added (client-only, NO server/engine touch): (1) a `useEffect` scoped to `openId != null` adding a document `mousedown` listener that closes the picker when the click is outside `menuRef` (a ref on the open row's chip+menu wrapper), gated on `!pending` so a stray outside click can't abandon an in-flight create/refile; (2) a container-level `onKeyDown` on the menu that closes on Escape and RETURNS focus to the chip (WCAG 2.4.3); (3) `close()` promoted to `useCallback` (stable effect dep). Two-level Escape preserved+hardened: the "+ New category" sub-form's Escape closes ONLY the sub-form (back to the list), now handled on the sub-form CONTAINER so Escape from ANY sub-form control (name/group/checkbox/buttons), not just the name input, steps back one level. Golden byte-identical. 4 e2e locks (transactions.spec.ts): Escape closes + focus returns to chip; outside-click closes; sub-form name-input Escape closes only the sub-form; sub-form group-select Escape closes only the sub-form (the last a fail-old lock for the container-level fix). Gate: verify GREEN 1666/125 (no unit delta — client UI, e2e-locked per #145/#156/#157); the 4 #158 tests pass.): Escape is deliberately NOT gated on `pending` (only the outside-click is) so it stays a keyboard escape hatch even if a server action stalls (this machine's documented #16/#17 stall) — a false-lock trap is worse than a r…
- **#159** (Phase **INVESTMENT account rows link to /investments (surgical discoverability row-link)** (owner-chosen next increment, over an 8th phone nav icon / the #71 mobile-nav redesign): a linked brokerage was reachable only via a tiny top-of-page "View investments ->" text link, and its own /accounts row dead-ended at `/transactions?account=<id>`. Now `LinkedRow` routes `account.type === 'INVESTMENT'` to `/investments` (a portfolio view: holdings, TWR/XIRR, retirement outlook) and carries an inline "· View holdings ->" cue so the destination is discoverable + screen-reader announced; every other linked account still opens its transactions (byte-identical else-branch). `/investments` takes NO account param (portfolio-wide over ALL INVESTMENT accounts — server/investments.ts) so the link is the plain `/investments` already used by the text link. `ManualRow` is deliberately UNTOUCHED: INVESTMENT is manual-eligible (networth/manual.ts), but a manual brokerage is just a typed balance with no holdings and carries inline edit/delete controls — linking it would be semantically empty AND would nest buttons inside an `<a>` (the exact a11y trap the owner flagged). Client/nav-only: no engine/schema/server change, golden + demo byte-identical. e2e lock (tests/e2e/investments.spec.ts, new test): click the seeded "Brokerage" account-row -> lands /investments + $142,000.00 portfolio + the "View holdings" cue present; the non-investment row -> /transactions path stays locked by the pre-existing transactions.spec.ts:29. Gate (real, 2026-07-03): core `bash scripts/verify.sh` -> VERIFY GREEN, typecheck/lint clean, 1666 unit / 125 files (no unit delta — client UI, e2e-locked per #145/#156/#157/#158), build clean; the new #159 e2e PASSES; /accounts stays WCAG-AA (transactions.spec.ts:313's accounts+register+add+import axe scan passed WITH the cue span live).): Chose the surgical row-link over the "Investments in nav" 8th-icon item (SECONDARY nav is full at 7 on the phone — the owner-scoped #71 redesign). `LinkedRow` is a lone `<Link>`, so the type-conditional href adds ZERO ne…
- **#160** (Phase **/investments account scoping (`?account=<id>`) — the #159 follow-up (P3-b the owner named in the #159 NEXT list)**: value is for the OWNER's real MULTI-brokerage production usage (Plaid+SimpleFIN linked accounts), with the single-brokerage demo as a golden-safe test vehicle. The #159 row-link now carries the id (`LinkedRow` INVESTMENT href `/investments` -> `/investments?account=${account.id}`; non-investment else-branch byte-identical), and `/investments` narrows its per-account holdings list to that account so a multi-account user lands on THAT account's holdings with a "Show all accounts ->" reset instead of the whole portfolio. Built VIEW-LAYER, NOT via a `getInvestments` Prisma-`where` (which would redefine the pinned "Portfolio value" headline to one account + enlarge the golden blast radius — explicitly rejected). `getInvestments()` / net worth / retirement UNCHANGED; the portfolio-wide summary card (data-testid investments-summary/investments-total-value, the pinned $142k golden) reads `data.overall` EXCLUSIVELY (never `scope`) so it stays whole-portfolio on every input. Core logic = PURE `resolveInvestmentScope(accounts, scopedAccountId)` (src/lib/engine/investments/scope.ts): returns the FULL unchanged list ("inert") when there's no id, `<=1` investment account, an unknown id, OR a matched-but-empty account — else `{accounts:[found], scopedName, showAllAccounts:true}`. The `<=1`-account INERTNESS rule is the golden-safety keystone: the single-brokerage demo renders BYTE-IDENTICAL with or without `?account`, so no golden ($142k, allocation, net worth, retirement) can move; scoping activates only for a genuine multi-account user. page.tsx reads Next-15 async `searchParams` (string[]/absent -> undefined -> full view). Chip copy "Showing <name> holdings" (clarifies the LIST is scoped, not the portfolio-value headline). 8 known-answer unit tests (investments-scope.test.ts: filters-OUT-others on 2/3 accounts, all inert/fallback paths, order-preserving); e2e (investments.spec.ts): #159 test updated to `waitForURL(/\/investments\?account=/)` + asserts $142k + NO scope chip on the inert demo; NEW `?account=does-not-exist` -> full $142k portfolio + AAPL + no chip. Gate (real, 2026-07-03): core verify GREEN **1674 unit / 126 files** (+8: scope resolver known-answers), tsc/eslint/build clean; investments e2e 6/6 (incl. #159 ?account inert-demo + #160 unknown-id fallback + axe WCAG-AA); transactions:29 (non-investment row -> /transactions) + :313 (/accounts axe) PASS.): Chose the owner's stated P3-b ("anchor to that account's card") over: middleware-matcher-anchoring (HIGH-value security but NOT demo-reachable + changes owner-flagged LIVE /iconzzz behavior at high regression risk = owne…
- **#161** (Phase **Categorization LEARNS from repeated user corrections (passive learning — owner: "the categorization should learn from users' inputs; the user shouldn't have to recreate the wheel each time")**: today a `Correction` is per-transaction and consulted by NOTHING at categorize time — it only helps future rows if the user manually promotes it to an explicit "Always" rule (a step easy to miss, and BLOCKED outright for aggregates), so a descriptor the user re-files every sync ("credit card paid" / "check paid" -> transfer) never sticks and the 159-row triage inbox keeps re-serving it. New pure `deriveLearnedRules(corrections)` (src/lib/engine/categorize/learn.ts) turns a user's correction HISTORY into synthetic `RuleLike[]` ("learned rules") APPENDED — in the server loader (src/server/rules.ts `loadUserRules` = explicit ++ learned, ownership-scoped) — to the SAME `rules[]` array `categorize()` already applies at every ingest + backfill path; `categorize()` core is unchanged. Every learned rule keys on an IDENTITY-PRESERVING descriptor SIGNATURE (src/lib/engine/categorize/signature.ts `computeDescriptorSignature`): the raw descriptor with ONLY dates + money amounts stripped and everything that identifies the counterparty KEPT (account / phone / check / reference numbers). Two occurrences of the SAME payee therefore share one signature ("CREDIT CARD PAID 07/01" and "...08/01" -> "CREDIT CARD PAID"; "ZELLE PAYMENT TO LANDLORD" recurs verbatim) while two DIFFERENT payees NEVER do (a differing phone/account number is a different signature) — that STRUCTURAL property, not any word list, is the load-bearing over-generalization guard. Safety is EARNED BY REPETITION: a signature must be corrected to the SAME category >= LEARN_THRESHOLD (2) times across DISTINCT transactions with ZERO conflicts; the #44 sign guard holds at derive time AND at match time (`learnedSignOk` in pipeline.ts — an income-learned merchant's later refund/fee falls through to review, never books as income); a secondary `hasDistinguishingToken` guard refuses a genuinely payee-less residue ("CHECK PAID", "CARD PAYMENT"); latest-correction-wins folds undos and change-your-mind sequences. Computed ON THE FLY from the undoable, user-visible Correction rows — NO schema change, NO DB writes — so the demo seed (zero corrections) derives ZERO learned rules and every golden is BYTE-IDENTICAL; undo is automatic (it changes the history, so the derived rule updates or disappears on the next read). A learned rule auto-files at `LEARNED_RULE_CONFIDENCE_BPS` (8500, the FLAGGED band) with the visible "AI" badge + needsReview:false — a correctable guess, NOT the silent 9900 an explicit "Always" earns — which is the structural backstop bounding any over-broad learned signature to a VISIBLE guess, never a silent misfile. Also shipped the two owner-reported normalize misses: Google One -> software, Round1 (arcade) -> entertainment (normalize.ts KNOWN_MERCHANTS + adversarial-eval corpus rows). Gate (real, 2026-07-04): `bash scripts/verify.sh` -> VERIFY GREEN **1704 unit / 128 files** (+30 over the 1674/126 baseline: learn.test.ts known-answer canaries + hostile-critic regressions, learn-loader.test.ts drives the real recategorize action -> loadUserRules -> categorize chain on a throwaway user), tsc/eslint/next build clean; adversarial `eval:categorize` 100% auto-file precision / 0 confidently-wrong (43 descriptors, Google One + Round1 now auto-file).): Chose compute-on-the-fly learned rules appended to the existing `rules[]` over a schema/DB-materialized rule table: zero migration, zero golden movement (demo has 0 corrections), free undo (re-derive), and it reuses the …
- **#162** (Phase **"Accept all confident" — one-tap triage-pile drain (owner: "drain the pile" queue-UX pick).** SUBSYSTEM-MAP FINDING FIRST: the handoff's premise ("the merchant-grouped bulk-apply UI is the gap") was STALE — `/triage` already groups the pile by merchant and files a whole group in one action (`fileMerchantGroup`, DECISIONS #143 Phase 3c). So this did NOT rebuild that; it adds the missing accelerant on top of it: a header banner that files EVERY merchant group the pipeline gave a unanimous honest suggestion for, in one undoable tap, leaving only the genuinely-ambiguous groups for manual review. Engine-first: pure `isConfidentGroup`/`selectConfidentGroups`/`summarizeConfident` in group.ts (a group is confident iff `suggestedCategoryId !== null` — groupReviewRows only sets it when EVERY row agrees, never a fabricated guess; the SAME predicate drives the client button AND the server action so they cannot drift, the similarTransactionsWhere↔groupKey discipline). New server action `acceptAllConfident()` re-derives the confident set server-side from getTriageGroups(userId) (client list NEVER trusted), then loops the EXISTING tested `fileMerchantGroup` per group — per-group serializable-tx commit, rule mint/reuse for rule-eligible merchants, aggregates get exact-descriptor scope + no rule (#23) — collecting all correctionIds into ONE undo batch (the existing `undoCorrections` reverts the lot + removes only the rules it minted). PER-GROUP commits (not one giant tx): a drain is incremental — each group independently filed AND undoable; one failing group never rolls back the rest (catch-per-group), and a TOTAL wipeout throws a stable user-safe message (fail-loud, no raw-error leak). Client: banner shown only when `mode==='idle'` AND ≥2 confident (never mid-pick → never silently discards an in-progress recategorization; 1 confident group is just one swipe), optimistic drop-then-reconcile with the authoritative returned queue, focus handoff to the aria-live count on completion (SC 2.4.3), one undo entry. GOLDEN-SAFE by construction: the demo's 12 review groups are ALL ambiguous (Zelle payees / checks / Store Card → 0 confident) so the banner is provably inert and every golden is byte-identical; it acts only on an explicit user click. Gate (real, 2026-07-04): `bash scripts/verify.sh` → ✅ VERIFY GREEN, **1716 unit / 129 files** (+12: pure selection, drain files-confident/leaves-ambiguous, mint-vs-reuse, undo round-trip removing ONLY minted rules, ownership isolation, no-op, partial-failure-skips-and-requeues, total-failure-fail-loud, and a non-vacuous demo-0-confident golden lock), tsc/eslint/next build clean; read-only e2e green (banner absent on the demo + the existing gesture/filing/undo flow unregressed).): Chose to ADD an accelerant over the already-shipped #143 grouped bulk-apply rather than rebuild it (surgical/simplicity), and surfaced the stale-premise finding to the owner before building. Chose loop-the-tested-fileMer…
- **#163** (Phase 3+): Categorization-quality pass — leaf-precision defaults, bank-noise stripping, processor priors, coverage expansion, LLM Plaid parity. (a) KNOWN_MERCHANTS/generic defaults re-pointed from coarse parents to the precise leav…
- **#164** (Phase 3): Triage pending-wedge fix (the phase2-triage e2e stall, STATUS 2026-07-04): the triage inbox's busy state moved OFF `useTransition` onto an explicit `useState` flag, every awaited server action in the island is bounded by…
- **#165** (Phase ?): Transfer pair FILING + "a transfer is never in review" (owner pick: transfer-pairing for "credit card paid"). PREMISE RE-CHECKED before building (the #162 stale-premise lesson): pairing already EXISTED (detectTransfers d…
- **#166** (Phase post-5): **Seamlessness pass** (owner: "make it something users want over Simplifi/Mint — too many things don't work seamless"). Full-app audit first: production build + fresh seeded DB, scripted 17-page walk (0 console errors, <…
- **#167** (Phase post-5): **Reliable-mutation pattern app-wide + e2e golden isolation** (#166 NEXT items 0+1, resumed on "continue"). (0) manual-card-statement.spec.ts moved onto a THROWAWAY user (auth.spec signup pattern) — its $500 add→delete w…
- **#168** (Phase post-5): **Merchant-spend Ask intent** (#166/#167 top-queued NEXT item, resumed on "continue"): "how much did I spend at Costco?" now answers a per-merchant total instead of abstaining. New `merchant_spend` intent (intent.ts unio…
- **#169** (Phase post-5): **Triage accuracy metric recovers on undo** (#166/#168 open follow-up (e), resumed on "continue"). Filing a review row stamps its `CategoryPrediction.actualCategoryId` as ground truth (DECISIONS #37) so the /triage accur…
- **#170** (Phase post-5): **Reliable-mutation pass finished** (#166/#167) on the last four lower-traffic surfaces, resumed on "continue". Only ONE was a true stale-UI defect: **connect-simplefin** used useTransition + router.refresh() (the coin-f…
- **#171** (Phase post-5): **Connection-health / data-staleness surfaces** (Competitive-Gap plan Gap 1 §3–4; resumed on "continue" after the #166–#170 seamlessness thread finished — the FIRST increment executing the freshly-written docs/COMPETITIV…
- **#172** (Phase post-5): **Cash Flow Radar** (Competitive-Gap Gap 2 §1 = AI plan §1.2 rank #2, adjudicated **build-now**; the first Fable-lane increment per plan §3 — a NEW money-math engine). Pure engine first: `radar/burn.ts` (weekly nearest-r…
- **#173** (Phase post-5): **Notification delivery** (Competitive-Gap Gap 2 §2 = the proactive-layer stickiness build; consumes #172's dormant `pushWorthy` hook). Engine-first (LOOP #5): a PURE `engine/notify/select.ts` (`selectNotifications`) uni…
- **#174** (Phase post-5): **Weekly digest email** (Competitive-Gap Gap 2 §3 — the last Gap-2 increment, "the cheapest retention win": bring the user back without a new surface). Engine-first (LOOP #5) but mostly COMPOSITION, not new math: a PURE …
- **#175** (Phase post-5): **Gap 3 §1 — production-readiness backlog burn-down** (Competitive-Gap plan Gap 3 §1: the 2026-06-24 UX/prod-readiness audit's "DO NEXT" list). An explorer survey of the 7 listed items found 5 already done by prior sessi…
- **#176** (Phase post-5): **Guided first-run connect flow** (Competitive-Gap Gap 3 §3: "bank → confirm payment account → see your Cash-Needed number," measuring time-to-first-number against the existing <10s benchmark). Pure UI composition — no s…
- **#177** (Phase post-5): **AI-trust accuracy panel in Settings** (Competitive-Gap Gap 4 §2) — *recorded post-hoc*: the #177 session committed (978e03f) without writing this row; reconstructed minimally from STATUS.md #177, which holds the full a…
- **#178** (Phase post-5): **Glass-Box reconciled numbers** (Competitive-Gap Gap 4 §1 — the flagship trust-moat build: "tap a number → the rows it's made of, reconciled to the penny", scoped per the AI plan §2.1 adversarial rework to the two highe…
- **#179** (Phase post-5): **Per-account data freshness on /accounts** (Competitive-Gap Gap 1 §3 follow-up — the "per-account last-activity on /accounts" slice #171 explicitly deferred as "the next slice"; resumed on "continue" in the Opus/routine…
- **#180** (Phase post-5): **Holding provenance badge on /investments** (Competitive-Gap Gap 5, first item: "provenance tag on synced holdings — `source` column is in place"; resumed on "continue" in the Fable lane per the #179 handoff menu). Surf…
- **#181** (Phase post-5): **CI: run `scripts/verify.sh` in GitHub Actions on push + PR** (Competitive-Gap Gap 6 §1 — "run scripts/verify.sh in GitHub Actions on push; today it's on-demand local only"; resumed on "continue" per the #180 handoff, w…
- **#182** (Phase post-5): **Multi-device session invalidation + PII-free deletion record** (Competitive-Gap Gap 6 §3, the two items PRIVACY.md §Deletion listed as deferred "real-auth release" limitations). Resumed on "continue" in the Fable lane;…
- **#183** (Phase post-5): **Sync-failure surfacing — broken-connection reconnect alert** (Competitive-Gap Gap 1 §4, the one item the §2 reconciliation still listed as unbuilt-and-in-session-verifiable; resumed on "continue" in the Fable lane per …
- **#184** (Phase post-5): **Proactive-cron scheduling — wire notify + digest into `vercel.json`** (Competitive-Gap Gap 2 §2/§3, the one item the §2 reconciliation still listed as unblocked-and-in-session-verifiable after #183 finished Gap 1; resu…
- **#185** (Phase post-5): **Gap 3 §1 reconciliation — the "delete confirmations + skeletons" backlog is already built; budget-`Clear` is a consistent one-tap, not a gap** (Competitive-Gap Gap 3 §1, the item the §2 reconciliation still listed as u…
- **#186** (Phase post-5): **ROADMAP ALSO CONSIDER UX/a11y burn-down** (Opus/Sonnet lane per COMPETITIVE_GAP_PLAN §3; resumed on "push, then continue" after #185). A read-only audit found half the ALSO CONSIDER list was already built (#81/#89/#90/…
- **#187** (Phase post-5): **Mobile secondary-nav redesign — labelled More sheet** (Competitive-Gap Gap 3 §2 / ROADMAP #71; owner authorized "lets work on mobile-nav redesign"). Replaces the 8 unlabeled top-bar icons on phones with one labelled **…
- **#188** (Phase post-5): **Desktop header: Settings/Sign-out overlap fix** (owner report after #187). Root cause: `AppNav`'s `<nav>` gained `flex-1` for the phone More button, so on `sm+` the 13 labelled text links expanded into the header's Sig…
- **#189** (Phase post-5): **Prod error tracking — dormant-until-DSN Sentry envelope client** (Competitive-Gap Gap 6 §2). Chose a thin `lib/errors.ts` fetch client over `@sentry/nextjs`: mirrors email/push dormant pattern, avoids wrapping `next.co…
- **#190** (Phase post-5): **Bounded per-user threshold tuning + live prediction log** (TASKS 3.6 / STRATEGIC_AUDIT §4.2 loop 4). NEW pure engine `categorize/tuning.ts`: per-user Brier over USER-labeled `CategoryPrediction` rows → `AUTO_FLAGGED` o…
- **#191** (Phase post-5): **Wave 0.4 — live provider spot-checks + re-runnable SimpleFIN validator.** Ran `npm run plaid:validate` against the real Plaid sandbox (12 accounts/50 signed txns/1 statement, ✅ PASSED) → flipped `docs/PLAID_WALKTHROUGH…
- **#192** (Phase post-5): **Cross-provider duplicate-account guard (advisory).** The app has NO cross-provider dedup — Plaid/SimpleFIN/manual each mint their own `Account` row and `Transaction` dedup is `@@unique([accountId, providerRef])` (accou…
- **#195** (Phase post-5): **Return-moment "Since you were away" greeting (TASKS 1.1 / STRATEGIC_AUDIT idea 3).** Rewards a return after a >7-day gap with a short story instead of a backlog. NEW pure composer `engine/return-moment/build.ts` (`buil…
- **#196** (Phase post-5): **Owner decisions (2026-07-09 session close): (a) distribution is the immediate priority; (b) household/family mode IS in scope this year; (c) monetization is intended but deferred.** (a) Next session prioritizes **Wave …
- **#197** (Phase post-5): **Contextual Ask follow-up chips (TASKS 1.2).** After every non-unknown answer, show up to 3 follow-up question chips. Pure static `followUpQuestions(intent)` map in `engine/assistant/follow-ups.ts` — every chip is a ful…
- **#198** (Phase post-5): **Wave 0.3 partial — activate dormant prod env on the existing `reiforge/aimplifi` deploy** (owner: "connect all of those"; allowlist = `michael.lee.p@gmail.com`). Inventory showed Production already had `DATABASE_URL`/`…
- **#199** (Phase post-5): **Route-specific empty states (TASKS 1.5).** Zero-account `/coach`, `/goals`, `/calendar` no longer render the shared `EmptyDashboard` cash-needed welcome. Extracted `ConnectOnboardingPanel` (SimpleFIN + Plaid + CSV/manu…
- **#200** (Phase post-5): **Household architecture (TASKS 4.1 spike — decision doc + schema design ONLY, no product code): household entity + explicit membership + per-account owner-consented READ-ONLY sharing; NOT a tenant layer.** Full design i…
- **#201** (Phase post-5): **Owner decisions on the household design's three open questions (2026-07-10, answering HOUSEHOLD_ARCHITECTURE.md §6): (a) partner categorization on shared accounts — YES; (b) ONE joint household digest; (c) UI naming — …
- **#202** (Phase post-5): **Glass-Box shareable snapshot (TASKS 1.6).** Client-only redacted share from the open reconciled Cash-Needed `GlassBoxNumber` panel. Pure `redactTraceForShare` / `formatShareText` in `engine/glass-box/redact.ts` — amoun…
- **#203** (Phase post-5): **Sentry / `SENTRY_DSN` deferred** (owner, 2026-07-10). Wave 0.3 will not set a Sentry DSN for now: the app is first and foremost personal for family and friends; paid error-tracking is not worth the cost at this stage. …
- **#204** (Phase post-5): **Resend domain verified (owner, 2026-07-10).** `aimplifi.app` DNS on Vercel; Resend status Verified; owner confirmed Delivered test email to `michael.lee.p@gmail.com`. Wave 0.3 email path live (`RESEND_API_KEY` + defaul…
- **#205** (Phase post-5): **Savings-rate streaks + celebration copy (TASKS 1.4).** Pure `computeSavingsStreak(flows)` walks ascending `MonthlyFlow[]` backward; consecutive months with `savingsRateBps >= minRateBps` (default 0); null/below-floor b…
- **#206** (Phase post-5): **Value-receipts ledger (TASKS 1.3): append-only `ValueReceipt` table + pure `engine/receipts` + "What Aimplifi caught" /coach card + weekly-digest tally.** One row per proactive catch actually surfaced, with the amount …
- **#207** (Phase post-5): **Personalized triage alternatives (TASKS 1.7).** Soft-hints for swipe-left: pure `deriveCorrectionHints(txn, corrections)` uses the same signature + latest-wins + #44 sign guard as `deriveLearnedRules`, but threshold = …
- **#208** (Phase post-5): **UnknownQuestion ledger (TASKS 2.2).** Additive `UnknownQuestion` (`userId`, `scrubbedText`, `llmGuessKind?`, `resolvedIntent?`, `createdAt`) + pure `scrubQuestionText` (emails → `[email]`, currency → `[amount]`, remain…
- **#209** (Phase post-5): **EngagementEvent capture (TASKS 3.1).** Additive `EngagementEvent` (`userId`, `surface`, `verb`, `subjectKey`, `createdAt`) + pure closed-set validator (`engine/engagement/event.ts`). Thin `recordEngagementEvent` / `log…
- **#210** (Phase post-5): **Household MVP slice 1 — membership core (TASKS 4.2 §5 slice 1).** Shipped the §4.2 schema verbatim (3 additive tables + `Account.sharedToHousehold @default(false)`, inert until slice 2), pure membership engine (`engine…
- **#211** (Phase post-5): **Weekly self-audit Critic (TASKS 3.2).** Additive `SelfAuditSnapshot` (userId+weekStart unique) + pure `computeSelfAuditSnapshot` (bps rates; zero denom → 0). Server gathers: review = point-in-time triage-eligible needs…
- **#212** (Phase post-5): **Household MVP slice 2 — account sharing read path + consent action (TASKS 4.2 §5 slice 2).** Central authz helpers in `src/server/authz.ts` per §4.3: `partnerIdsOf` / `partnerSharedAccountsWhere` (null when no partners…
- **#213** (Phase post-5): **Household MVP slice 3 — shared transactions in the register (TASKS 4.2 §5 slice 3).** `getSharedTransactionsView()` is a SEPARATE query path from `getTransactions` (personal summary totals, merchant counts, and categor…
- **#214** (Phase S): S.1: scripts/ledger.ts (decision/regression/progress appenders + DECISIONS_INDEX.md generator) + package.json aliases (verify:e2e, verify:fast, ledger). Ran once for real: regenerated docs/DECISIONS_INDEX.md from all 209…
- **#215** (Phase TASKS 4.2): #215: Household MVP slice 4 (joint cash-needed, HOUSEHOLD_ARCHITECTURE §4.4). getSharedSnapshotSlice(partnerId) (src/server/household-finance.ts) mirrors DemoProvider.getFinanceSnapshot's query shapes exactly, scoped per…
- **#216** (Phase TASKS S): Docs de-dup pass D1-D8 (TASKS.md S.2, plan docs/SKILLS_PLAN.md §5). | D1: stripped hardcoded test counts from README/ROADMAP/COMPETITIVE_GAP_PLAN; added a one-status-home rule to CLAUDE.md pointing at docs/STATUS.md + do…
- **#217** (Phase TASKS S): S.3: scripts/docs-lint.ts (Pulse-leak/hardcoded-count/archive-banner/verify-phrasing checks over every tracked *.md file) + npm run docs:lint alias + a non-gating docs-lint step in .github/workflows/verify.yml (continue-…
- **#218** (Phase TASKS 4.2): Household MVP slice 5 (cards/calendar household scope + cross-app copy audit, HOUSEHOLD_ARCHITECTURE §5 slice 5). /cards and /calendar now thread the same 'mine'|'household' searchParam contract as /dashboard: getDashboa…
- **#219** (Phase TASKS 4.2): Household MVP slice 6 (partner categorization on shared accounts, HOUSEHOLD_ARCHITECTURE SS5 slice 6 / owner decision #201's SS6.1 boundary). New recategorizeSharedTransaction (src/server/household-actions.ts): any house…
- **#220** (Phase TASKS 4.2): Household MVP slice 7 (joint household digest, HOUSEHOLD_ARCHITECTURE SS5 slice 7 / owner decision #201(b)). A member with a LIVE partner now receives ONE household-scope weekly digest instead of the personal one: dues c…
- **#221** (Phase post-5): **Slice 8 full-surface household hostile critic (T1-T12): 3 fresh-context Fable critics (authz state machine / read-visibility+mutation boundary / money surfaces) found 0 P0, 7 P1, 10 P2 — all P1s fixed, all actionable P…
- **#222** (Phase TASKS 2.1): **Ask conversation frame — deterministic ellipsis resolution.** New pure `src/lib/engine/assistant/frame.ts`: `frameFromIntent(intent)` distils the previous turn's RESOLVED intent into a slot frame `{kind, timeframe, tar…
- **#223** (Phase TASKS 2.1): **Frame hostile-critic cycle 1 (fresh-context Fable): FAIL — 3 P1 + 4 P2, all fixed in-cycle.** The P1s were all the same disease — the frame answering a question the user did not ask: (1) a timeframe-FIRST fragment ("an…
- **#224** (Phase TASKS 2.1): **Frame hostile-critic cycle 2 (same critic, re-executed repros): PASS — 0 P0, 0 P1.** All 3 P1s and all 4 P2s from cycle 1 confirmed CLOSED by re-run repros; a 19-case false-abstain sweep found no legitimate ellipsis br…
- **#225** (Phase TASKS 2.3): **Learned vocabulary — the weekly mining loop (audit §4.2 loop 2).** New additive `VocabEntry` (per-user, `@@unique([userId, phrase])`, cascade) + pure `src/lib/engine/vocab/vocab.ts` (`normalizePhrase`, `matchVocab`, `m…
- **#226** (Phase TASKS 2.3): **Vocab hostile-critic cycle 1 (3 fresh-context Fable critics in parallel — routing/money, the learning loop, authz+privacy): FAIL — 0 P0, 5 P1, 11 P2. All P1s and every actionable P2 fixed in-cycle and regression-locked…
- **#227** (Phase TASKS 2.3): **Vocab hostile-critic cycle 2 (fresh context, every cycle-1 repro re-executed): all 11 cycle-1 findings CONFIRMED CLOSED — and 1 NEW P1, in the cycle-1 FIX itself.** The non-ASCII abstention guard tested the first white…
- **#228** (Phase TASKS 2.3 / 2.6): **Vocab critic cycles 3 and 4 — the 4-CYCLE CAP was reached on a PRE-EXISTING parser defect, and the remaining findings are ESCALATED (docs/STATUS.md §OPEN, TASKS 2.6).** The vocab slice itself PASSES: cycle 4's sweep fo…
- **#229** (Phase TASKS 2.6): **The spend_total inversion shipped: the sink now EARNS its answer via a positive licence, enforced in all three routes.** New shared primitive `unconsumedSpendObject` (intent.ts): every at/with/on/in object ANYWHERE in …
- **#230** (Phase TASKS 2.7): Ask timeframe follow-up + largest_purchases merchant scope shipped (STATUS §OPEN items 3-4 from the 2.6 escalation), built + criticized on Fable. (a) parseExplicitTimeframe learns bare years ('in 2025' → that calendar ye…
- **#231** (Phase TASKS 2.8): **Tier-1 synonym-inside-store-name (STATUS §OPEN item 2): ATTEMPTED, proven MERCHANT-DB-BLOCKED across three fresh-context Fable critic cycles, and REVERTED — nothing shipped; tree back at #230's b062e5a, verify GREEN.**…
- **#232** (Phase GLASSBOX slice 1): **Glass-Box row-sum trace engine shipped (GLASSBOX_PLAN slice 1): `src/lib/engine/assistant/trace.ts` — tap-a-number reconciliation for the 6 ROW-SUM intents, built on shared-predicate lockstep, two fresh-context Fable c…
- **#233** (Phase GLASSBOX slice 2a): **Glass-Box trace UI shipped (GLASSBOX_PLAN slice 2a): a row-sum answer's headline number is tappable → an inline reconciliation panel showing the exact rows behind it, penny-reconciled.** Architecture: the server comput…
- **#234** (Phase GLASSBOX slice 2b): **Glass-Box slice 2b shipped: per-FACT tappability + the one-tap correction chip (GLASSBOX_PLAN §2b).** (i) READ: `AssistantFact` gains builder-set `traceKey` (categoryId) + `cents` (the builder's OWN figure) — facts are…
- **#235** (Phase GLASSBOX slice 3): **Glass-Box slice 3 shipped: derivation-chain "formula + inputs" traces for net_worth / cash_needed / savings_rate (GLASSBOX_PLAN §Slice 3 — no fake row-sum).** New `DerivationTrace` variant on `AnswerTrace` (kind:'deriv…
- **#236** (Phase NUDGE slice 1 (AI §2.2)): **Smart Notification & Nudge Engine — slice 1 (pure engine) shipped: `src/lib/engine/nudge/{types,select}.ts` + `tests/unit/nudge-select.test.ts` (criteria 1–7).** A pure reshape+order over reminders/radar/cash-needed/op…
- **#237** (Phase NUDGE slice 2 (AI §2.2)): **Smart Notification & Nudge Engine — slice 2 (dashboard "Today" feed + dismissal store) shipped.** UI: `TodayFeedCard` (src/components/dashboard/today-feed-card.tsx + pure copy in today-feed-copy.ts), fed by the RSC das…
- **#238** (Phase WHY-THIS-CATEGORY slice 1 (AI §3.1)): **Why-This-Category — provenance ENGINE + PERSISTENCE shipped (no UI).** Surfaces the deterministic-vs-LLM category origin we already compute but discarded at every write. New pure `src/lib/engine/categorize/provenance.t…
- **#239** (Phase WHY-THIS-CATEGORY slice 2 (AI §3.1)): **Why-This-Category — provenance UI shipped: per-row badge on the register + one-tap "Confirm" for AI guesses (criteria 6–9).** `TxnView` gains a required `provenance: ProvenanceVerdict` computed ONCE server-side in `get…
- **#240** (Phase BALANCE-MOVE (AI §2.3)): **Balance-Move Explainer shipped — a grounded, descriptive one-liner on /trends "What changed".** Pure `engine/trends/balance-move.ts` reshapes the tested movers into atomic factors; `explainBalanceMove` forces `primaryD…
- **#241** (Phase MONEY-REVIEW (AI §2.4)): **Monthly Money Review shipped — the /coach recap is now a closed candidate-insight set with an optional key-gated LLM that only SELECTS and ORDERS ids.** Pure `engine/fi/money-review.ts`: `buildReviewCandidates` assembl…
- **#242** (Phase post-5): AI Trust Center & Audit Ledger (AI plan §3.2; owner-picked from the Wave 3 remainder after the stale STATUS pointer was corrected — §1.1's goal-type trilogy #125/#126/#131 and §1.2 (#172) were already complete). The adju…
- **#243** (Phase post-5): Demo bank-connect fence (#242 owner follow-up). The shared demo account (user-demo) could link a real bank (Plaid/SimpleFIN), landing one anonymous visitor's real financial data in the single row every other visitor logs…
- **#244** (Phase post-5): Demo manual-entry fence (#243 owner follow-up — the TYPED/UPLOADED leg). Owner confirmed the scope 2026-07-16: the demo is read-only for visitor-BROUGHT data; hands-on play with the SEEDED (fake) data remains an intended…
- **#245** (Phase post-5): Demo fence critic cycle (#244) — e2e migration, destroy fence, and the owner-accepted residual. Cycle-1 fresh-context Fable critic verified the four manual-entry fences clean but returned FAIL: (P1-1) five transactions.s…
- **#246** (Phase AI plan §3.4): Subscription Radar deterministic slice = forward renewal schedule ONLY (new pure engine upcomingRenewals over RecurringItem[] from summarizeRecurring: active expense series expanded from nextExpectedAt by cadence through…
- **#247** (Phase AI plan §3.3): **Doc Extractor v1 = text-only card-statement extractor, reshaped exactly per the plan's adversarial verdict (owner's "continue" after the #246 owner-gated fork; §3.5 is blocked on a nonexistent vision pipeline and every…
- **#248** (Phase AI plan §3.2): **AI Trust Center per-touchpoint track record — the one genuine §3.2 gap left after the feature shipped at #242.** Owner's "continue" at the #247 owner-gated fork; a code-vs-plan reconciliation found the AI plan §3.1–§3.…
- **#249** (Phase AI plan §3-Later #12): **Unusual Charge Radar v1 = the per-merchant median+MAD outlier half ONLY, shipped exactly per the plan's reshape verdict (owner's "continue" at the #248 owner-gated fork; income-pause/runway deferred — it needs FI-mutat…
- **#250** (Phase AI plan §Later #19): **Merchant Pattern Lens shipped exactly per the plan's reshape verdict — "ship the deterministic profile + templated narration; drop the generative-LLM framing" (owner's "continue" at the #249 fork; picked as the one rem…
- **#251** (Phase AI plan §Later #20): **Income-Pause Radar shipped = §20's ONE groundable signature (lapsed `isIncome` series + runway) as its own narrow engine, with the verdict's `projectedIncome = 0` mutation CONFIRMATION-GATED (owner's "continue" at the …
- **#252** (Phase AI plan §Later #11): **Money Signature shipped = §11 reshaped exactly per its adversarial verdict — hysteresis before any label change, stable axes decoupled from a responsive weather state, habit framing not personality (owner's "continue" …
- **#253** (Phase Owner request (accounts)): **Synced-account deletion: SimpleFIN accounts become deletable once the bank is DISCONNECTED — never while connected.** Owner hit the gap live: "there needs to be an easy way to delete an account… i'm trying to delete th…
- **#254** (Phase AI plan §Later #17 (streaks half)): **Habit Streaks — #17's groundable streaks shipped per the split verdict; the drift loop stays gated on the transfer-pair engine.** Owner's "continue" at the #253 fork; board reconciliation (lesson #26) found the plan's …
- **#255** (Phase #255 / AI plan §Later #13 slice 1): Scenario Studio slice 1 SHIPPED as the pure snapshot-coherence engine (src/lib/engine/scenario/scenario.ts): ONE canonical ScenarioBase/ScenarioState + knob-delta application (income/expense percent+absolute, extraDebtMo…
- **#256** (Phase #256 / owner request (Plaid disconnect + sandbox disclosure)): Owner hit two gaps live: Plaid Link rejected their real phone number, and /accounts offered no way to disconnect specific accounts. (1) SANDBOX DISCLOSURE: the phone rejection happens inside Plaid's own hosted Link UI — …
- **#257** (Phase #257 / owner request (forgot password)): Owner locked out of the deployed app ('we need to build a forgot password link'). Shipped the full reset flow, engine-first: pure engine/auth/reset.ts (32-byte base64url tokens — full-entropy URL secrets, NOT the human-t…
- **#259** (Phase #259 / 2026-07-21 agent-review follow-up slice 1): Applied the review's top candidates: docs C1–C5 (README's Plaid row now records the sandbox-verified truth — ROADMAP #1 DONE 2026-06-17 — replacing "not implemented"; seed-pinned measurements de-hardcoded to target + the…
- **#260** (Phase #260 / 2026-07-21 agent-review follow-up slice 2): Applied the review's remaining NON-owner-gated candidates: the whole B redundancy wave plus A5 and A6. **B2** — five LLM modules (assistant-llm, balance-move-llm, llm-categorize, llm-statement-extract, money-review-llm) …
- **#261** (Phase #261 / owner-reported password symptom + a secrets-in-git finding): **The owner's "it's just not remembering the password I entered earlier" is still NOT diagnosed, and this slice deliberately does not claim it is.** Two things were done instead. (1) **A precautionary RESTORATION of the …
- **#263** (Phase Wave M (mobile UI)): **Owner's /accounts mobile overflow — the reported "doesn't format correctly."** Owner sent two iOS-Safari screenshots (M.0 deliverable): /accounts Assets ($1,735,553.08) and Liabilities (−$997,970.24), money values + th…
- **#264** (Phase Wave M.2 (mobile tap-target floor)): **The finance controls were ~20px tall; even the shadcn Button primitive tops out at h-8/32px — all under the 44×44pt iOS / 48dp Android minimum.** Shipped ONE shared primitive: the `.tap-target` token in globals.css, sc…
- **#265** (Phase M.3 (#265)): Fixed the money-display overflow class with min-w-0 on the grid cell plus break-words on the value (never text-ellipsis/truncate, which would clip a digit): hero cash-needed-card due-date rows, plus the grid-cols-3 money…
- **#266** (Phase 4.6 spec (#266)): Speced cross-provider account reconciliation (docs/PROVIDER_RECONCILIATION_ARCHITECTURE.md) to answer the owner's SimpleFIN-to-Plaid migration question. Chosen design: keep BOTH account rows and add an additive AccountRe…
- **#268** (Phase Wave M.4 slice 1 (mobile visual foundation)): First slice of the owner's "make it beautiful" ask, kept deliberately restrained (the M.4 row forbids a big-bang restyle) and judged against REAL rendered before/after mobile screenshots at 393px, not imagination (rule 0…
- **#269** (Phase M.4): Extracted the 5 byte-identical dashboard link-card className strings into one shared token SURFACE_LINK_CARD_CLASS (src/components/finance/surface-card-styles.ts), applied to safe-to-spend / spending-insights / top-spend…
- **#270** (Phase #270 (Wave 4.6 slice 1)): Built detectReconciliationCandidates as a PURE directional layer over the #192 duplicate detector: a suspected cross-provider duplicate pair becomes a predecessor->successor reconciliation candidate using each row's hasL…
- **#271** (Phase Wave 4.6 slice 2 (#271)): Additive AccountReconciliation table (one table; NO Account column changes; predecessorAccountId @unique, successorAccountId not unique, NO Account FK) + Prisma-only confirm/undo core (src/server/reconciliation.ts, NextA…
- **#272** (Phase Wave 4.6 slice 3 (#272)): The assembler money boundary (R1/R2/R8), built + hostile-criticized on Fable, 2 critic cycles (cycle 1 FAIL: 2 P0 + 1 P1 + 5 P2/P3, ALL fixed; cycle 2 PASS + 1 new P2 also fixed). NEW pure engine src/lib/engine/account/r…
- **#273** (Phase Wave 4.6 slice 4 (#273)): Cash-needed cards + household follow-through + scheduled rows (R4/R5 + slice-3 critic F6). Built on Fable; hostile-reviewed via in-thread Maker/Checker + a choke-point proof (the two Fable subagent critics both hit a mon…
- **#274** (Phase Wave 4.6 slice 5 (#274)): The reconciliation UI + the F5 close-out (the LAST per-account money-display surface). Two halves. (1) MONEY: `getAccountsView` was the only Prisma-direct per-account balance surface still bypassing the reconcile boundar…
- **#275** (Phase 275): Wave 4.6 slice 6 — full-surface hostile critic over R1-R10: three parallel fresh-context Fable critics (money core + boundary-straddle lead target / lifecycle-authz / downstream surfaces + copy honesty). 8 P1s fixed same…
- **#276** (Phase #276): Wave M.3 close-out: fix the triage quick-pick grid (the one MEASURED defect among the deferred items) and CORRECT the brief's remaining items as non-issues rather than 'fixing' them. The quick-pick Buttons get `h-auto mi…
- **#277** (Phase #277): Owner-reported false all-clear on cards: fix it at BOTH ends and treat 'we cannot date this card' as a first-class, disclosed state rather than an absence. (1) ENGINE: CashNeededResult gains unknownDueDateCards — the car…
- **#278** (Phase #278): One-button 'Sync all accounts' on /accounts covering EVERY connected provider, plus per-connection sync for each Plaid bank and the pre-existing SimpleFIN button — and Plaid finally joins auto-sync-on-load, throttled to …
- **#279** (Phase #279): Hostile-critic hardening of the #278 sync work, run AFTER it shipped because the slice reached production without a critic pass: (1) scalar-validate the server-action itemId — a non-string reached the Prisma where clause…
- **#280** (Phase #289): TASKS L.4 — close the five #277-critic P2s, then unify undatable-card copy across every surface after a hostile-critic pass found them drifting. (1) Nudge feed: the empty 'Nothing needs you today.' all-clear now names th…
- **#281** (Phase Wave 4.3 (#290)): Plaid /investments/holdings/get synced to parity with the SimpleFIN path: new pure mapper src/lib/providers/plaid-holdings.ts (join holdings to securities by security_id) + PlaidProvider.syncHoldings and reconcilePlaidHo…
- **#282** (Phase owner-reported dup fix (#291)): Owner-reported /accounts duplicate-detection fixes (his + spouse aggregated in one account, so two Capital One connections; his Venture ····6271 and her Venture ····0966 were flagged a possible duplicate on the shared na…
- **#283** (Phase dup veto refinement (#292)): Refined the differing-last-4 duplicate veto (#291) after owner clarification + a Fable critic. A different last-4 means different CARDS, not necessarily different ACCOUNTS: one account can carry several cards (a spouse a…
- **#284** (Phase plaid freshness + connection row alignment (#293)): Owner-reported 2026-07-24 with screenshots: the /accounts page contradicted itself — every Plaid CONNECTION row read "last synced 2026-07-24" while the ACCOUNT rows under Liabilities read "Not synced yet" (Loan - 2927, Q…
- **#285** (Phase name-embedded last-4 as a POSITIVE match (#294)): A last-4 embedded in an account NAME is now a POSITIVE duplicate-match signal (never a veto). Owner screenshots 2026-07-24 showed three real duplicates double-counting ~965k of net worth; the detector caught only one. It…
- **#286** (Phase actionable duplicate warning (#295)): The duplicate warning card is now ACTIONABLE. Owner-reported 2026-07-24: the card said "Keep one and disconnect or delete the other" and offered NEITHER control — only "Not a duplicate — dismiss" — while both sides of hi…
- **#287** (Phase duplicate card distinguishes CONNECTIONS, not rows (#296)): Owner-reported 2026-07-24 with a screenshot, on the fix shipped hours earlier as #295: the duplicate card's two sides were BYTE-IDENTICAL — "Loan - 2927 (Plaid ····2927)" twice, with two "Disconnect U.S. Bank (Plaid ····…
- **#288** (Phase combined-accounts card groups by live account (#297)): Owner-reported 2026-07-24 (STATUS §Combined-accounts), the last open defect from his real /accounts screenshots: the "Combined accounts" card listed "Venture (Plaid ····6271)" TWICE, identically — same mask, same "histor…
- **#289** (Phase /cards tells same-named cards apart (#298)): Owner-reported 2026-07-24 from a live /cards screenshot: THREE cards named `CREDIT CARD` and TWO named `Venture`, each with its own amount due, and a headline reading "Do this first: pay Venture $9,250.93" while he holds…
- **#290** (Phase /cards discloses a card counted twice (#299)): The second half of TASKS L.6, from the owner's /cards screenshot of 2026-07-24: one real Chase card arriving through TWO LIVE Plaid connections emitted TWO full obligations, so the "Do this first" instruction and this cy…
- **#291** (Phase account identity: prevent the duplicate, don't detect it (design, TASKS L.10)): Prevent a same-account re-pull at link time (Plaid update mode + collision interception) instead of writing a second row and printing an advisory; keep two lookalike REAL accounts and make them tellable apart | DESIGN ON…
- **#292** (Phase L.10 slice 1 (#300)): Capture identity BEFORE building any identity logic: three nullable, additive columns — PlaidItem.institutionId (Plaid stable ins_* id), Account.subtype (the provider raw subtype, verbatim) and Account.persistentAccountI…
- **#293** (Phase L.10 slice 2 (#301)): Ship Plaid Link UPDATE MODE as a per-connection 'Add or fix accounts' control on /accounts — layer 1 of the account-identity design. linkTokenParams gains an update variant that sends access_token + update.account_select…
- **#294** (Phase L.11(A) savings-rate figure fix (#302)): The savings-rate card's multi-month figure is now a POOLED rate — new pure pooledSavingsRateBps (Sum income - Sum expenses)/Sum income over income-bearing months — replacing an arithmetic mean of monthly RATIOS. Display …
- **#295** (Phase Safe-to-Spend → guilt-free spending (owner decision, L.11C)): Reframe the Safe-to-Spend card as 'guilt-free spending' in the I Will Teach You to Be Rich (Ramit Sethi) sense: income minus fixed bills, minus THIS-CYCLE card obligations (from the cash-needed engine, not just detected …
- **#296** (Phase L.12(a+b)): Persist Plaid's own category (personal_finance_category) on Transaction as two nullable columns providerCategoryId + providerCategoryConfidenceBps, and surface it in the triage inbox as a labelled Plaid's-guess one-tap s…
- **#297** (Phase combine two live duplicate connections (#304)): Owner-reported 2026-07-24 with two screenshots and the question "What did you actually fix? I see the same accounts I posted earlier." He was right: #298/#299 taught /cards to TELL two same-named cards apart and to discl…
- **#298** (Phase collision interception on the fresh-Link front door (#307)): Owner, verbatim 2026-07-24: *"Why in the heck are you allowed to make 2 of the same accounts... when I try to link same account again, it just refreshes."* Layer 2 of docs/ACCOUNT_IDENTITY_ARCHITECTURE.md §4, wiring the …
- **#299** (Phase the two remaining duplicate-creating paths (L.17, #308)): Closes the two residuals both fresh-context critics of #307 raised, each REPRODUCED before it was fixed (executed; output in PROGRESS.md). **(a) A connection linked before the `institutionId` column was invisible to the …
- **#300** (Phase the six surfaces that rendered a duplicated card and said nothing (L.15, #309)): Closes the class opened by the L.8 sweep: /cards (#299) and the dashboard hero + reminders list (#306) disclosed a both-live duplicate; the calendar, the reminder email, the weekly digest, web push, the Ask cash-needed a…
- **#301** (Phase an account the bank stopped sharing: disclose, adjust nothing (L.14)): **The defect.** Plaid Link update mode ships with `account_selection_enabled`, so a user can untick an account. `upsertPlaidAccounts` only ever creates or updates, so nothing noticed: the row kept its last balance, kept …
- **#302** (Phase what a frozen balance may be used FOR (L.14 critic cycle 1)): Amends #301 in the one place it was wrong. #301 argued “keep counting, disclose” entirely over LIABILITIES — a stale card balance merely over-funds, while excluding a still-owed card risks a missed payment. Two fresh-con…
- **#303** (Phase the fact rides the money, and every surface states its own claim (L.18)): **The defect.** #301 disclosed a frozen account once, in the cash-needed engine's `assumptions`, and its comment claimed that reached "/cards, the dashboard hero, the calendar, the Ask answer and the weekly digest". Exac…
- **#304** (Phase a loan can go stale too, and a claim about an absence must name the right mechanism (L.19)): **The gap.** #303 made `frozenSince` ride every obligation, LOANS INCLUDED, and then built each surface's frozen list from cards alone. Two consequences, both live. **(1)** The all-clear — "You're all caught up" on the d…
- **#305** (Phase the narrowings that strip the fact, and the loan nobody could name (L.20)): **One disease, three faces.** `feedDroppedAt` rides the money out of the providers and is then dropped at a server-side NARROWING, so three surfaces printed figures from a frozen balance in silence. **(1) The Today feed.…
- **#306** (Phase the L.20 adversarial pass: three critics, one gate, and the narrowing nobody named (L.21)): **L.20 shipped verified but without a critic pass** — the owner redirected mid-slice — so this cycle is the pass it owed. Three fresh-context critics ran in parallel on `3139bfa` with different lenses (copy honesty; wiri…
- **#307** (Phase Safe-to-Spend becomes guilt-free spending (L.11(C), owner decision #295)): The Spending Plan is reframed in the I Will Teach You to Be Rich sense: leftToSpend = expected income − spending outside credit cards − upcoming bills (per remaining OCCURRENCE — a biweekly paycheck with two paydays left…
- **#308** (Phase the account name a person chooses, and the two places a name is read (L.7, owner-requested 2026-07-24)): Owner, verbatim: *"there should be a way to edit name of accounts myself. Similar to simplifi.com."* His screen carries three cards the feed calls `CREDIT CARD` and two called `Venture`; #296/#297/#298 each spent a build…
- **#309** (Phase a window has an edge, and money does not reset when it is crossed (L.11(D))): Owner-reported 2026-07-25 with three screenshots, verbatim: *"It's worse now."* His dashboard said $18,814.14 was needed by Wed, Aug 5 to pay all 7 cards and, directly beneath it, offered **$22,254.09 guilt-free to spend…
- **#310** (Phase L.9): Retirement registration is a cross-provider VETO; one stale row matching several live accounts is carried out, never offered; proven identity outranks every heuristic. (1) registrationsConflict: a Roth is never a Traditi…
- **#311** (Phase L.22): Guilt-free spending is the owner's formula, computed from patterns: income = the MEDIAN of up to the last 3 complete months over non-credit accounts (fallback: detected income series at a monthly rate; fallback: 0 with i…
- **#312** (Phase L.23): A detected ANNUAL bill reaches the money, for expenses only and only while it is still charging. `toScheduledTransactions` projected WEEKLY/BIWEEKLY/MONTHLY only, and `src/server/recurring.ts` is the ONLY writer of the S…
- **#313** (Phase L.24): Two new recurring cadences reach the money — QUARTERLY and SEMIANNUAL — under exactly the conditions L.23 established for ANNUAL, plus one new evidence bar of their own. `cadenceFromGap` recognized four windows (5-9, 12-…
- **#314** (Phase TASKS L.25 (owner-reported live "Fixed & recurring expenses $0.00", 2026-07-26)): **A bill paid from ANY cash account reaches the money; income keeps the payment account.** `toScheduledTransactions` filtered EVERY detected series to the single resolved payment account, while the same plan's income ter…
- **#316** (Phase L.28): **A sync that rewrites a figure tells the page to re-render, and it answers that by comparing STORED ROWS rather than by counting writers.** `AutoSync` runs a bank sync on every full page load and calls `router.refresh()…
- **#317** (Phase L.29): **A zero says which zero it is, and the four labels have one author.** On the guilt-free breakdown a $0.00 line was the same pixel whether the reader owned no credit cards, owned cards whose statements all fall past the …
- **#318** (Phase L.30): A broken fixed-expenses zero must NAME its cause: the reason a repeating series is not projected is recorded, not thrown away. `toScheduledTransactions` decided admission with two `.filter`s and discarded WHY, so "Fixed …
- **#319** (Phase L.30-critics): TWO fresh-context critics on L.30, both FAIL, and every P1 was executed before it was believed. Fixed: (1) the empty branch is DELETED, not narrowed — 'no repeating bills found yet' read `detected === 0` as proof of an e…
- **#320** (Phase O.1 (part 2)): **The tax columns, the control that writes them, and the file that reads them ship as ONE slice — and the demo account is fenced out of all three.** Part 1 built the pure engine and deliberately stopped before the column…
- **#321** (Phase O.4): **Sessions expire after 30 minutes of inactivity, rolling forward on every page load — and the window is an IDLE timeout, not an absolute cap, because Auth.js re-signs the token on every read.** The config carried no `se…
- **#322** (Phase O.3): **The streamed-Suspense reveal is frame-gated exactly once per document, and the harness supplies the frame the machine does not.** React's `$RC` never reveals inline: it queues the `[boundary, staging]` pair on `$RB` an…
- **#323** (Phase O.5): **A category figure links to its own rows, the link carries the WINDOW, and only ONE of the three "spending by category" surfaces was allowed to link — because only one of them reads the same rows the register does.** Th…
- **#324** (Phase O.5-critics): **Two fresh-context critics on O.5, both FAIL, and the P0 was mine: I verified the ENGINE and never read the INTAKE.** Critic A (money lens) executed a repro proving `/trends` movers do NOT reconcile with the register — …
- **#325** (Phase O.2 (landing)): **The register isolates what still needs a category — and the lock that blocked it was fixed rather than weakened, after the geometry was MEASURED instead of argued.** The feature was built a session earlier and held on …
- **#326** (Phase O.2-critics): **Fresh-context critic on O.2: FAIL, and the P1 was a number that could not keep its own promise.** Critic A (data-correctness lens) executed against a seeded database rather than reasoning, and found that `unclassifiedC…
- **#327** (Phase O.6 — one basis for "spending by category", and the two links it unblocks): **The gap, verified at source, not reported by a reader.** Three surfaces answered the same question three ways, and nobody could see it because the surfaces are never shown side by side. `/reports` (and Ask, which share…
- **#328** (Phase O.6 critic cycle — two critics, one P0 found twice, and a fix whose tests could not fail): **Both critics converged independently on the same P0**, which is the strongest available signal a finding is real. O.6 removed a merchant-table category fallback in `toPurchaseRows` on the theory that it labelled unfile…
- **#329** (Phase O.7): **An aggregate and a named row are two different questions, and one shared row builder made one of them wrong.** Ask answered “how much did I spend at Whole Foods this month” (`merchantSpend`) on a different basis than “…
- **#330** (Phase O.7-critics): **Two fresh-context critics, both FAIL, and they converged INDEPENDENTLY on the same P1 — the deleted predicate was doing a SECOND job nobody had named.** `isPurchaseRow` rejected the whole `Transfers & Other` group, and…
- **#331** (Phase Phase O.9): **Corrections now learn on the merchant CANONICAL as well as the descriptor signature — because the signature could not learn the owner's actual feed.** Owner, live: *"when categorizing, i've already inputed many and the…
- **#332** (Phase Phase O.9): **The proposal reaches the inbox as its own tier — and the shared demo row was fenced off the whole correction-learning path on the way.** `TriageGroupView` gained `proposedCategoryId` / `proposedCategoryName` / `proposa…
- **#333** (Phase Phase O.9): **The suggestion ladder reaches the REGISTER, per row, with a one-tap confirm (TASKS O.9d) — and the one tap is licensed by a rendered proposition, not a menu pick.** Owner, live, with register screenshots the day after …
- **#334** (Phase O.9e-critic): **The Fable pass on O.9a–c: FAIL on first reading, two P1s — and both were CLAIMS, not math.** P1-1: O.9c's "the shared demo stops learning" was false while `getThresholdTuning` stayed unfenced — the `labeledAt` gate sto…
- **#335** (O.8): **One question, one basis — and the discovery that sharing a basis is not sharing a SCOPE.** /trends and Ask answered "how much did I spend at merchant M this month" with different dollars ($65.00 vs $80.00, measured before any code changed); the naming half stays settled-only, the AMOUNT moves to the register basis, and O.8(b)'s proposed card-payment exclusion was refuted on both its stated grounds…
- **#336** (O.8-critics): **Two fresh-context critics, both FAIL, converging INDEPENDENTLY — and the parity lock was built so it could not have caught the P1.** Sharing a row BASIS is not sharing a merchant SCOPE: prefix matching makes "Costco Gas" answer $195.82 under the name "Costco" against /trends' $37.38; the lock used one merchant string, and the seed assertion had gone tautological…
- **#337** (O.12d): **The provider-category backfill — /transactions/get repairs the pre-L.12 nulls that silenced the inbox's "Plaid's guess" tier.** Pure planner, exact-match-or-counted-skip, one shared guess author with ingest (F4 sign guard included), per-row compare-and-set on both-null + matched amount, on-demand CRON_SECRET route only. Critics: the route shipped UNREACHABLE behind the middleware matcher (P0, executed repro, fixed + fail-old locked) and a raw NUL byte in plaid.ts made ripgrep treat the file as binary (P1, both critics; fixed + `source-hygiene.test.ts` gates all control bytes, catching a pre-existing 0x01 pair in coach.ts)…
