# Lessons index

Cross-session memory. One line per lesson: `- [title](file.md) — one-line hook`.
Convention lives in `LOOP_ENGINEERING.md` → "Lessons ledger": one lesson per file, one-line summary at
the top, corrections and confirmed approaches alike with the *why*, no duplicates of what the repo or
ledgers already record, update rather than duplicate, delete lessons proven wrong.
Hooks here are deliberately short (≤220 chars); the full incident write-up, evidence,
and rule live in each lesson file — open the file before acting on a hook.

- [Never guess — verify or say you're unsure; write user instructions for a first-timer](never-guess-and-write-for-a-first-timer.md) — a live Plaid env-var + reset support session broke the deployed app via abbreviated, guessed-at steps and unverified diagnoses; […]
- [Check a provider's PRODUCTION requirements before telling the owner "no setup needed"](check-provider-production-requirements-before-saying-no-setup.md) — "your integration is already built, ignore that checklist" was true of Plaid's tutorial and wrong about Plaid: unpublished Data Transparency Messaging made Link EXIT at institution-select for every OAuth bank; […]
- [Don't develop in cloud-synced folders](cloud-synced-folders.md) — OneDrive/Dropbox/iCloud sync holds file locks that make a cold `tsc`/`eslint`/`next build` fail once and pass on rerun, and forces LF→CRLF churn; keep the checkout on a plain local disk.
- [Generate code with Write/Edit, not shell heredocs](windows-codegen-via-shell.md) — bash→python heredocs mangled
 escapes in generated TS and `*/` inside comments broke builds; file tools are byte-exact.
- [Diagnose hangs at boundaries, not by correlation](diagnose-hangs-at-boundaries.md) — load-correlated evidence pattern-matched the triage stall to the SQLite flake twice; […]
- [Mutation forms use onSubmit, never useActionState](mutation-form-recipe.md) — React 19's form-action auto-reset silently reverts uncontrolled `<select>`s to their first option on the error return → a silent mis-file; […]
- [mobile-380 Playwright viewport-scaling flake](mobile-380-viewport-scaling-flake.md) — a ~11.8% viewport scaling mismatch could make fixed-bottom-nav clicks intercept. **CORRECTED #193:** the recurring "full e2e can't exit 0 here" was actually a DETERMINISTIC auth.spec strict-mode locator bug (#182's "Sign […]
- [CI (GitHub Actions Linux runner) e2e timing flake](ci-e2e-timing-flake.md) — a different `toHaveCount`/timeout assertion fails each rerun of the same unchanged commit (budget-targets, then phase2-triage), while unit tests and the untouched code stay green; […]
- [The local full-suite e2e flake is load-induced, and its symptom varies by page](e2e-dials-value-corruption-flake.md) — a *corrupted persisted value* (`ClimbingTravel`) looked like a real data-integrity bug until a full verify on **clean HEAD** failed too, on a different test (a 60s click timeout). […]
- [Second-person copy is a load-bearing invariant, not a style choice](second-person-copy-scope.md) — `reminderLine`'s "you'll pay $600 yourself" was true only because it had always rendered the reader's OWN card; […]
- [Widening a single-actor field to multi-actor requires re-auditing every consumer](multi-actor-field-scoping.md) — `Correction.userId` went from "always the owner" to "the acting user" (household slice 6, partner categorization); […]
- [The demo account is one shared row — anything that learns from a user's INPUT must fence it off](shared-demo-account-must-not-learn.md) — every anonymous visitor is the same `user-demo`, so any feature that accumulates what a visitor BRINGS — typed (learned vocab #226), a household seat (#210), or a CONNECTED real bank (#242 follow-up) — leaks one stranger […]
- [A guard must read the same input as the thing it guards](a-guard-must-read-what-it-guards.md) — the fix for "spend at 星巴克 → the all-spending total" tested the first token while the tokenizer skipped leading articles, so the identical bug survived one article away ("at **the** 星巴克"); […]
- [A self-improving loop leaks in its BACK half](self-improving-loops-leak-in-the-back-half.md) — the abstention gates survived three critics; every P1 was in the half nobody watches: an undo a background job can overwrite, a rule nothing checks once it starts answering (excluding your own output from your own eviden […]
- [A precision fix that fabricates $0 is worse than a safe category-superset](precision-fix-that-fabricates-is-worse-than-a-safe-superset.md) — TASKS 2.8 tried to route "at travel lodge"/"at 24 hour fitness" to the merchant instead of the category superset; […]
- [Copying a value verbatim is not copying its meaning](verbatim-value-not-verbatim-meaning.md) — the nudge feed's `centsAtStake` is carried byte-for-byte but MEANS a different thing per kind (the monthly INCREASE for price-increase, an ESTIMATED saving for insurance/negotiable, the user-action REMAINDER after autopa […]
- [A fence copied per call site will miss call sites](fence-by-construction-not-per-call-site.md) — #242's demo fence missed 2 of 5 LLM call sites the same session had just edited; make the guarded capability obtainable ONLY through a fencing constructor (`categorizeSuggestFor`) so the invariant holds by construction a […]
- [Context-carrying features are judged by what they ABSTAIN on](context-carrying-features-must-abstain.md) — the Ask conversation frame passed 29 happy-path tests and was still wrong three ways: every bug was a fragment it shouldn't have resolved (a dropped merchant, an ignored negation, the assistant's own vocabulary read as a […]
- [Fencing a shared-demo write path breaks every e2e that drove it as demo](fencing-a-write-path-breaks-the-tests-that-drove-it.md) — #244's fences shipped verify-green while five transactions.spec tests (demo-driven manual entry/CSV/recat setup) were red, because verify.sh skips Playwright without VERIFY_E2E=1; […]
- [A plan doc's verdict is authoring-time, not current build state](plan-verdicts-are-authoring-time-not-current-state.md) — at #248 the AI plan still read "build-later"/"needs-rework" for §3.1–§3.4 that were all shipped; a fresh explorer faithfully reported the stale verdicts and cost two passes rediscovering already-built code. […]
- ["Identical" copies are never identical — diff before extracting](dedup-must-diff-the-copies-first.md) — #260's redundancy wave found five `medianOfSorted` copies that disagreed on the even-count rounding (3 floor / 1 round / 1 raw, all feeding money) and five LLM provider blocks where three had drifted to a non-null-assert […]
- [Committed is not shipped — and an unpushed branch silently falsifies diagnoses](committed-is-not-shipped.md) — `main` sat 8 commits ahead of `origin/main` for four sessions (#257–#261), so the owner could not see a feature built the day before, AND the previous session's leading hypothesis blamed code (#258) that was never on the […]
- [An e2e spec run tests the last `next build`, not your edit](e2e-runs-a-stale-build.md) — Playwright starts `next start` and reuses an existing server locally, so a targeted spec run after a source edit silently tests compiled output from an earlier build; […]
- [A mobile overflow can be invisible to Chromium AND Playwright's WebKit](mobile-overflow-is-a-safari-truncation-quirk.md) — the owner's /accounts right-edge clipping (money + Delete off-screen on iOS Safari) reproduced in NEITHER Playwright engine at 360/393/430 with long synced names, 6/7-figure balances, or 150% text; […]
- [An empty set is not a fact about money](an-empty-set-is-not-a-fact-about-money.md) — `buildObligation` returned null for a card it couldn't date and the caller dropped it, so "we don't know" and "paid off" became the same value and EIGHT surfaces rendered the emptiness as "nothing is due" / "you're all c […]
- [A passive page-load gate cannot see what fits in the gutter](a-passive-gate-cannot-see-what-fits-in-the-gutter.md) — the M.1 overflow gate asserts document scrollWidth on passively-loaded routes, so it is structurally blind to controls behind a tap AND to any overflow smaller than the 16px shell gutter; […]
- [A new egress path invalidates claims written for the old ones](new-egress-means-auditing-every-live-claim.md) — #247's 4 critic P1s were all live CLAIMS falsified by a new LLM egress (privacy policy, scrubber promise, "every printed form" test title, uncovered rate-limit rule), not code bugs; […]
- [Prevention is not a remedy for the instance already on the user's screen](prevention-is-not-a-remedy.md) — four sessions shipped identity, disclosure, capture and prevention for the owner's duplicated Chase card and never removed it ("what did you actually fix?"); […]
- [An irreversible act may not be authorised by stored state — re-prove it against the source](irreversible-acts-need-live-proof.md) — L.10 layer 2 decided whether to hand a just-created Plaid connection back to Plaid by comparing against Account ROWS, which keep describing a connection after it can no longer reach them: re-linking a bank to REPAIR it d […]
- [A disclosure written for a page is false in an email](a-disclosure-written-for-a-page-is-false-in-an-email.md) — L.15 moved the duplicate-card disclosure onto six new surfaces, and every design decision came from one fact: copy qualifying a money figure carries an implicit claim about WHERE THE READER IS STANDING. […]
- [Ask the failure-direction question per ROLE, not per class of value](failure-direction-is-per-role-not-per-value.md) — L.14 argued "a frozen balance keeps counting, we just say so" entirely over LIABILITIES (a stale card merely over-funds), and two fresh-context critics independently produced the inverse: the PAYMENT account can itself b […]
- [A disclosure is several claims wearing one sentence](a-disclosure-is-several-claims-in-one-sentence.md) — L.18's partner bug was fixed THREE times because each pass corrected one clause (subject, then possessive, then remedy) while the rest went on asserting the old assumption: a qualifying sentence is a subject, a mechanism […]
- [A subagent's green is a hypothesis until you reproduce it](a-subagents-green-is-a-hypothesis.md) — three background agents (one e2e author, two critics) each produced a correct-looking result that was wrong about the tree: the e2e agent reported 5 passed against a `.next` that predated a copy edit made after it launch […]
- [A sweep bounded by the surfaces you already had in mind is not a sweep](the-narrowing-you-did-not-list.md) — L.20's thesis was "the fact is dropped at NARROWINGS" and its critic cycle found one it never listed: `ReturnMomentRadarInput`, a hand-built payload over a closed three-field type, dropped the frozen starting balance und […]
- [A borrowed total imports the other engine's window — share ROWS, not totals](a-borrowed-total-imports-its-window.md) — L.11(C) copied the cash-needed headline verbatim into a calendar-month budget and reserved one statement against two months' income; […]
- [Conflicting evidence is an absence, and a guess may not be offered where a proof is withheld](proof-outranks-guess-and-conflict-is-absence.md) — L.9's four critic cycles each found the SAME two rules one level deeper: resolution-on-conflict kept resolving wrong (within a name, name-vs-subtype, evidence-vs-absence — bank names embed the HOLDER'S name and Roth is a […]
- [A dead branch is a claim that something is handled — and a disclosure of a gap can entrench it](a-dead-branch-is-a-claim-that-something-is-handled.md) — L.22's ANNUAL `/12` rule had a passing test, an explaining docblock and a critic-approved disclosure, and NOTHING in production could hand it an annual row (the sole writer filtered the cadence out; […]
- [A narrowing belongs at the consumer that needs it — and widening it back is not symmetric across sign](a-shared-filter-is-not-symmetric-across-sign.md) — the plan subtracted expenses summed over ONE account from income summed over ALL of them ($21,117.48 vs $0.00 on the owner's live page); […]
- [Closing a gap shrinks the disclosure that described it](closing-a-gap-shrinks-the-disclosure-that-described-it.md) — L.24 taught the detector two new billing rhythms and deleted both from the "rhythms we don't recognize" list, leaving a shorter list that read as an EXHAUSTIVE one: seven IRREGULAR ranges still survive, the copy named fo […]
- [A driver-parsed timestamp is not the stored value](a-driver-parsed-timestamp-is-not-the-stored-value.md) — every `DateTime` here is Postgres `timestamp without time zone`, so node-pg re-reads it in the client's local zone and a probe from UTC−4 sees every production event **4 hours late**; […]
- [Three sessions of hypothesis, one query of evidence](three-sessions-of-hypothesis-one-query-of-evidence.md) — L.23/L.24/L.25 each shipped a real uncounted-bill fix aimed at the owner's "Fixed & recurring expenses $0.00", and none of them was the cause; […]
- [Ask the stored state what changed, not the writers](count-the-state-not-the-writers.md) — L.28's page-load auto-sync re-rendered only on `added`/`statementsWritten`, so the load that REPAIRED the owner's data (0→8 scheduled rows, $684.31/mo, during syncs reporting `added: 0`) re-painted its stale $0.00; […]
- [A zero is a claim — name WHICH zero, and give an unset one its control](a-zero-is-a-claim-and-must-name-which-zero.md) — four facts (no cards / all dated past the month / never set up / an upstream defect ate every bill) all rendered `− $0.00`, so the owner read the broken one four sessions running and could only say a number "looked wrong **2nd instance #417:** the register blamed its filters while printing the reason four lines above […]
- [A required argument makes a caller answer, never answer correctly — and an empty table is not an empty world](a-required-argument-makes-a-caller-answer-not-answer-correctly.md) — L.30 fixed a direction bug by making the direction REQUIRED, wrote "locked both ways", then passed the wrong value at the third caller in the same slice, because the discriminator was never a flag on the plan (`overspent […]
- [The failing response body was already on disk — and the framework's shipped code answered it](the-evidence-was-in-the-trace.md) — #287 sat "not diagnosed" for three sessions while Playwright's `retain-on-failure` trace held the exact HTML of the failing load (`resources/<sha1>.html`) plus DOM snapshots naming both copies; […]
- [A framework default is a decision you shipped](a-framework-default-is-a-decision-you-shipped.md) — Aimplifi kept sign-ins alive for **30 days** across browser-close and power-off because `auth.config.ts` set `session: {strategy:'jwt'}` and no `maxAge`; […]
- [A link on a money figure asserts that two engines agree](a-link-on-a-figure-asserts-two-engines-agree.md) — O.5 made category figures clickable and the href was never the hard part: "spending on groceries in July" is computed by THREE paths here whose predicates disagree, so a link is a *claim of equality*. […]
- [Deleting a predicate deletes every job it was doing — including the ones its name doesn't mention](deleting-a-predicate-deletes-every-job-it-had.md) — O.7 rightly moved `merchantSpend` onto the shared spending basis and gave away a pseudo-merchant guard nobody had written down, because it lived inside a category-group exclusion whose comment advertised only the other j […]
- [One question, one basis — and a documented invariant is what sets your scope](one-question-one-basis-and-the-invariant-sets-the-scope.md) — three surfaces summed "spending by category this month" over three different row sets and no reader could see it, because two surfaces only visibly disagree once something invites the comparison (a deep-link is that invi […]
- [Sharing a basis is not sharing a scope — and a task row's premise is a hypothesis](sharing-a-basis-is-not-sharing-a-scope.md) — O.8 unified WHICH ROWS COUNT across /trends and Ask and then claimed the figures agree, but agreement also needs WHICH ROWS BELONG TO THE MERCHANT: `merchantMatches`'s whole-word prefix makes "Costco Gas" sweep in "Costc […]
- [A typed key is a PATTERN, and every guard was written for an identity](a-typed-key-is-a-pattern-not-an-identity.md) — O.13a's keyword rules passed 46 engine locks and still failed two fresh-context critics with 2 P0 + 7 P1, all at the boundary where a substring met machinery built for an exact merchant: the only auto-file path with no # […]
- [A retry is not a second click — ask what the form does AFTER the failure](a-retry-is-not-a-second-click.md) — O.14b survived four sessions because "I click it again, it works" was read as *the same bytes verified on the second submit*, which is impossible against a deterministic scrypt check, so every session hunted a non-determ […]
- [Giving an existing column a new meaning is a data migration](a-new-meaning-for-an-old-column-is-a-migration.md) — O.13c encoded OR-groups into the column that already held the keywords and added `|` to the parser's separators, and every docblock called the codec "lossless BY CONSTRUCTION": true of values the NEW codec wrote, and not […]
- [A fix that cannot fail a test is a hypothesis](a-fix-that-cannot-fail-a-test-is-a-hypothesis.md) — O.13b took two hostile-critic cycles, and three of cycle 2's four P1s existed because a cycle-1 fix had been reviewed rather than LOCKED; […]
- ["Alive" is not "progressing"](alive-is-not-progressing.md) — asked if a 40-minute e2e gate was healthy, I checked that the Playwright PROCESSES existed and answered "nothing is wrong with the run itself"; […]
- [A page that lists what runs must read what runs — and a "narrow sibling" write ships the same dead end twice](a-page-that-lists-what-runs-must-read-what-runs.md) — /rules listed the rules it could RENDER while the categorizer loaded the rules it RUNS, so every "Always" rule filed money invisibly and the delete's WHERE excluded exactly the rules the page omitted; […]
- [Ownership is not a property of the row in front of you — and a refusal's REASON is a claim](ownership-is-not-a-property-of-the-row-in-front-of-you.md) — O.15 slice 7 fenced a new hand-set `status` to rows "no feed owns", but a split PIECE carries no `providerRef` (children cannot share the parent's under `@@unique([accountId, providerRef])`), so a piece of a BANK charge […]
- [The affordance already existed and was lossy](the-affordance-existed-and-was-lossy.md) — O.16's owner asked for "a way to go back" and there were already TWO "Back to transactions" links, both a bare `/transactions` that dropped his filter and page; […]
- [A written deletion promise is an architecture constraint, and it outranks the vendor in the task row](a-deletion-promise-picks-your-storage.md) — O.13h's row specified "Vercel Blob, private" and the retention policy's own §3 ("one cascading delete removes every associated row… nothing is retained after deletion") made object storage wrong: a bucket honours that on […]
- [One loader is not one reader — and I fixed the same bug on one surface while shipping it on another](one-loader-is-not-one-reader.md) — O.17's per-user category rename was designed around a single overlay loader and FOUR docblocks claimed it therefore reached "the register, triage, reports … by construction"; […]
- [A new money engine's risk is at its boundaries, not in its arithmetic](the-arithmetic-was-never-the-risk.md) — W.1's wealth-target planner was pinned against an independent closed-form annuity oracle and its compounding was never wrong once; […]
- [An answer is only as believable as its visible inputs](an-answer-is-only-as-believable-as-its-visible-inputs.md) — the owner called the wealth-target card "arbitrary savings for arbitrary time" and the arithmetic was correct AND self-consistent (both printed figures back-solve to the same ~$1.48M starting balance); […]
- [A repeated request is about the GESTURE, not the data](a-repeated-request-is-about-the-gesture.md) — the owner asked a third time for expandable rows while every one of those rows already linked to the transactions behind it, correctly and live; […]
- [A demonstrative is an undeclared parameter — and the author count in the task row is a guess](a-demonstrative-is-an-undeclared-parameter.md) — unifying four hand-rolled copies of one disclosure, the task row AND the filing docblock both said "three" (the fourth, in `answer.ts`, was found only by grepping the phrases rather than the identifier), and turning a no […]
- [A rate and its target must share a unit](a-rate-and-its-target-must-share-a-unit.md) — W.2's FI card compounded at the NOMINAL return toward a target built from today's spending; […]
- [A regenerator that cannot parse its input writes its own blindness out as the answer](a-regenerator-must-refuse-what-it-cannot-parse.md) — `ledger.ts reindex` rebuilt the decisions index from a file it could only half read, wrote 329 rows where 375 belonged, printed success, and its own generated header told the next reader to run it (a Cursor agent did, de […]
- [An identity good enough to COMPUTE with is not good enough to WRITE with](an-identity-safe-to-compute-with-is-not-safe-to-write-with.md) — C.24's loan-payment merchant set was safe inside the Fixed union because an invariant sat around it (excluded ⇔ unioned, so a wrong call cost nothing); […]
- [A fix on the reported surface is not a fix on the pattern](a-fix-on-the-reported-surface-is-not-a-fix-on-the-pattern.md) — C.12 found the radar's L.23 amount×date split unfixed in the sibling cash-needed engine and inherited by four surfaces ("$10,001.00 on Aug 10" for a $1.00 day), plus the same instruction-drifted-from-figure root in /cards' estimate promotion, /forecast's trailing caveat, and the radar's Clear verdict. When a defect is a SHAPE, the fix ends with a grep of the shape, not the symptom.
- [Unifying two figures can break a stronger invariant than the one it fixes](check-what-the-fix-breaks-before-what-it-fixes.md) — C.13's clamp was correct arithmetic, verify-green, and reverted: it left every /reports figure linking to an UNCLAMPED register ($120.00 clicked → $520.00 of rows). Rank the divergence you create against the one you remove before shipping.
- [A disclosure is inherited by every surface that inherits the rule — and each cycle's fix is the next cycle's defect](a-disclosure-is-inherited-by-every-surface-that-inherits-the-rule.md) — C.26 clamped "spent this month" to today and three critic cycles found ELEVEN P1s of which none was the arithmetic: a chart that blamed the reader's refunds for money the date rule removed, a page figure subtracted from two independently-floored sums that cancelled to silence, a label narrowed twice ("so far so far"); two cycles shipped copy nothing could assert because the repo had no render harness — so the slice built one, and it caught the next cycle's defect.
- [A control that replaces a HALF must be shown that half](a-control-that-replaces-a-half-must-be-told-which-half.md) — C.23 folded declared reserves into the fixed figure and the override form kept printing the whole suggestion beside its input, so locking the number shown counted the reserve twice; a figure has readers AND writers, and adding a term changes both.
- ["Add-only" bounds what you WRITE, not what your write MEANS downstream](add-only-bounds-what-you-write-not-what-it-means.md) — H.5's backfill only ever called `create` and two critics still found it destroying user data: adding rows moved a `MIN(date)` that a reconciliation claim window uses to DROP another account's rows, and an inherited "refresh" helper rewrote settled rows the moment a new row supplied a counterpart.
- [Proof is the command's own exit code and the FULL failure list — never a trimmed tail](proof-is-the-full-output.md) — one session, two manufactured verdicts: `verify | tail` reported tail's exit 0 over a FAILED gate, and a tail-trimmed failure list hid two deterministic failures, misdirecting a broken-suite diagnosis toward "flake"; capture $? from the command itself and read failure lists whole.
- [Deleting a surface deletes the claims it carried — and the engine that composes them stays green](deleting-a-surface-deletes-the-claims-it-carried.md) — #369 orphaned three Home cards and shipped verify-green; the L.20 disclosure naming a frozen mortgage survived only on an EMPTY feed, and /cards went on narrowing its all-clear by delegating to a card deleted five days earlier. […]
- [A red count is a floor, and the gate nobody reads is not a gate](a-red-count-is-a-floor-and-an-unread-gate-is-not-a-gate.md) — *(2026-08-08 addendum: once you issue `gh run rerun`, the branch is FROZEN — H.1(b)'s own docs-only push cancelled its slice's gate re-run mid-flight, and a cancelled run is an absence, never a pass.)* K.6 was filed as "the last two reds" and the file held three: a failure in a `mode:'serial'` spec aborts everything behind it, so "4 did not run" was read as 4 passes; meanwhile CI had been failing the same full gate on every push for days. RESOLVED by K.8 (gate clock pinned, ci-status.sh in rule 5) — whose critic found the divergence had a FIFTH axis (CI's Node 20 couldn't even load jsdom): when a gate splits from CI, enumerate every environment axis, not just the one you found. […]
- `a-refusal-learns-one-intention-and-walls-off-the-rest.md` — a guard that refuses is claiming to know WHY someone acted, inferred from what they did; the day a second purpose produces the same evidence it becomes unappealable. L.10's duplicate refusal destroyed every attempt to obtain Plaid's 730-day window, because the deeper connection necessarily returns the same accounts. Carry the intent, exempt the intent not the shape, and check the failure direction of trusting the user. Also: a route is not proven dead by measuring the state it was meant to change.
- [Hiding a surface makes you re-rank its claims — rank by CERTAINTY, not by which array sounds scariest](hiding-a-surface-reassigns-its-claims-by-certainty.md) — O.19 collapsed five /accounts cards behind one line, deliberately kept "the claim, not the machinery", and still shipped a P0: the money sentence went to the ADVISORY residue (the pairs left after every provable case is routed elsewhere) while the app's certain double-count got a procedural "can be combined" — executed at net worth $2,000.00 for $1,000.00 real, "twice" nowhere on the page. The slice's own new e2e asserted the weakened clause, ratifying it.
