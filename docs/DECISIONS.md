# Decision Log

Record every non-trivial decision made during the build: what, why, alternatives
considered. Append-only.

> Entries #1-#401 live in `docs/archive/DECISIONS_ARCHIVE_1_to_401.md`;
> #402-#484 live in `docs/archive/DECISIONS_ARCHIVE_402_to_484.md`
> (rotated 2026-08-28, same current-wave cut as PROGRESS/REGRESSION).
> #485-#723 live in `docs/archive/DECISIONS_ARCHIVE_485_to_723.md` (rotated 2026-09-11).
> Only entries #724 onward live here; append new entries as before — the numbering
> never resets, the archives hold the lower numbers.

## #724 - M.4 slice 2: shared page-chrome tokens, chrome restyle, brand-tinted dark theme (2026-09-10)
**Context.** The owner's standing beauty pass (Wave M.4, docs/MOBILE_UI_BRIEF.md: tokens before routes, no big-bang restyle). The working tree held the maker output, interrupted before gates: type scale and spacing had drifted (text-xl vs text-2xl page titles; some routes space-y-4), the shell chrome (header, nav, footer, demo banner, sign-in card) had no shared elevation language, and dark mode was a generic neutral slate.
**Decision.** Four shared tokens in src/components/finance/page-chrome.ts (PAGE_TITLE_CLASS, PAGE_LEAD_CLASS, PAGE_STACK_CLASS, PAGE_SECTION_LABEL_CLASS), unit-locked; every visible page h1 and lead migrated (including the new-user welcome card, the only h1 a zero-account user sees); the two byte-identical trends-view section labels migrated with it. Shell chrome restyle: sticky translucent header, pill desktop nav, mobile bottom-bar active indicator (shape, not color), demo banner as a rounded pill, sign-in card elevation, rounded-lg auth inputs, surface cards gain a 1-ring hairline. The .dark block re-tinted with slight brand-green chroma (oklch hue 165) - the app renders hard-coded dark, so this IS the product's identity; light :root untouched. All changes are className/CSS-variable only: no copy, figure, adjacency, testid, or logic change.
**Locked.** tests/unit/page-chrome.test.ts (token utilities); tests/e2e/m4-page-chrome.spec.ts (19 routes x 380+1440 no horizontal overflow; 30px desktop title scale; pill nav; demo-banner pill radius).
**Boundary repairs earned this session (own ledger rows).** A core.autocrlf=true re-smudge had rewritten 586 worktree files to CRLF (bash -n broke; a source-window test overflowed) - repaired byte-identically and pinned with .gitattributes `* text=auto eol=lf`. tests/unit/vercel-build.test.ts never passed on this machine: bash here is WSL (PATH/env do not cross the boundary as the test assumed; DATABASE_URL needs WSLENV) - made platform-aware, Linux path unchanged.
## #725 — CI repair: four red e2e specs + Needs-a-category aria-pressed (2026-09-10)
**Context.** CI `verify` run `34511385651` (slice 2) failed four mobile-380 e2e plus a flaky axe scan. Logs named the lines: `budget-clear-*` not found after set; reconnect alert missing "Reconnect it on the Accounts page"; wealth-target href still `/settings#money-dials`; `/transactions` axe `aria-allowed-attr` on `<a aria-pressed>`.
**Decision.** Retarget the specs to the shipped UI. Clear is inside BudgetRowTargetControl's editing form (#659) — open the row target first; after clear, lock the first-run hint and the gone row (a collapsed clear count-0 is a tautology). Failed-sync copy is `Reconnect it so your numbers stay current` with in-place SimpleFIN reconnect. Wealth-target assumptions link on /coach is `#coach-money-dials` (Settings `#money-dials` remains). The Needs-a-category chip stays an `<a>` so a click before hydration still filters; on-state uses `aria-current`, never `aria-pressed`.
**Locked.** `tests/e2e/budget-targets.spec.ts`, `tests/e2e/pwa-offline.spec.ts`, `tests/e2e/connection-health.spec.ts`, `tests/e2e/wealth-target.spec.ts`, `tests/e2e/transactions.spec.ts`; `test_regression__needs_category_chip_is_a_href_before_hydration`; `test_regression__household_can_change_a_budget_target_on_the_category_row` (Clear after `if (!editing)`).

## #726 — Household can edit plan figures from Home without leaving for Spending plan (2026-09-10)
**Context.** PlanFiguresForm (savings %, optional income/fixed locks) lived on Spending plan and Budgets. Home's Safe to Spend and cash-needed cards read those dials, but changing them required leaving — Safe to Spend only linked to Spending plan.
**Decision.** Mount PlanFiguresForm on /dashboard (`home-plan-figures`) beside Safe to Spend, ahead of Cash needed. Same writer (`updatePlanFigures`); already revalidates /dashboard. Demo read-only. No Settings remount. No PushOptIn / deepen / Coach Goals / category / CSV work.
**Locked.** `test_regression__household_can_edit_plan_figures_from_home_without_leaving_for_spending_plan`.

## #727 — Household can edit a card statement from Home Cash Needed without leaving for Cards (2026-09-10)
**Context.** Cash Needed on Home listed undated cards with balance and mostly said there was nothing to do except open Cards. Manual CREDIT cards already use CardStatementControl on Cards and Calendar (#710); Home still sent the owner away to add the statement that would put the card into the figure.
**Decision.** On /dashboard CashNeededCard, for viewer-owned manual CREDIT cards in the undated / not-included set, mount CardStatementControl (`home-cash-needed-statements` / `home-cash-needed-statement-{accountId}`). Same setManualCardStatement / clearManualCardStatement writers; card-actions already revalidates /dashboard. Partner cards stay display-only. Linked (non-manual) cards keep the honest “nothing to do” / wait-for-feed copy — no fake Add. Demo fenced. No PushOptIn. No Settings remount. No deepen / Coach Goals / category / CSV / Trust invent.
**Locked.** `test_regression__household_can_edit_card_statement_from_home_cash_needed_without_leaving_for_cards`.

## #728 — Household can edit a dated card statement from Home Cash Needed without leaving for Cards (2026-09-10)
**Context.** #727 mounted CardStatementControl on Home Cash Needed for undated / not-included manual cards. Dated dues still listed on the same card, but correcting an existing statement still required Cards or Calendar.
**Decision.** On /dashboard CashNeededCard, for viewer-owned manual CREDIT cards that appear in this-cycle due dates or upcoming estimated dues, mount CardStatementControl (`home-cash-needed-dated-statements` / same `home-cash-needed-statement-{accountId}`). Same writers; card-actions already revalidates /dashboard. Partner and linked cards stay display-only. Demo fenced. No PushOptIn. No Settings remount. No deepen / Sync / Coach Goals / category / CSV / Trust invent.
**Locked.** `test_regression__household_can_edit_dated_card_statement_from_home_cash_needed_without_leaving_for_cards`.

## #729 — Household can convert a repeating bill to a reserve from Recurring without leaving for Spending plan (2026-09-10)
**Context.** ConvertToReserveButton (createReserveFromSeries) lived on Spending plan Fixed composition and Settings Fixed costs. Recurring already edits bill name/amount/cadence and can mark Not a bill, but turning a convertible series into a reserve still required leaving.
**Decision.** On /recurring, for non-demo outflow rows whose billKey or merchantCanonical is convertible in the same `getSpendingPlan().fixedSetup` gate Spending plan uses, mount ConvertToReserveButton (`recurring-convert-to-reserve`) with that billKey. Same writer; already revalidates /recurring. Loan payments and income excluded. No Settings remount. No PushOptIn / deepen / Sync / Coach Goals / category create / CSV / Trust invent.
**Locked.** `test_regression__household_can_convert_bill_to_reserve_from_recurring_without_leaving_for_spending_plan`.
## #730 — Household can take a repeating bill off the plan from Recurring without leaving for Spending plan (2026-09-10)
**Context.** TakeBillOffPlanButton lived on Spending plan Fixed composition. Recurring already mounts Not a bill and now Convert (#729), but taking an on-plan repeating bill off the Fixed figure still required leaving.
**Decision.** On /recurring, for non-demo outflow rows whose billKey is on the Spending plan Fixed list (same gate as takeRepeatingBillOffPlan), mount TakeBillOffPlanButton (`recurring-take-off-plan`). Same writer; bill-rename-actions already revalidates /recurring. Loan payments excluded by the plan line gate. No Settings remount. No PushOptIn / deepen / Sync / Coach Goals / category / CSV / Trust invent.
**Locked.** `test_regression__household_can_take_bill_off_plan_from_recurring_without_leaving_for_spending_plan`.
## #731 — Household can put a bill back on the plan from Recurring without leaving for Spending plan (2026-09-10)
**Context.** PutBillBackOnPlanButton lived on Spending plan and Settings Fixed costs for `billsTakenOff`. Recurring already mounts take-off (#730) and Convert (#729), but restoring a taken-off bill still required leaving.
**Decision.** On /recurring, when non-demo and `getSpendingPlan().billsTakenOff` is non-empty, mount the same PutBillBackOnPlanButton list (`recurring-bills-taken-off` / `recurring-bill-taken-off-row`). Same writer; bill-rename-actions already revalidates /recurring. No Settings remount. No PushOptIn / deepen / Sync / Coach Goals / category / CSV / Trust invent.
**Locked.** `test_regression__household_can_put_bill_back_on_plan_from_recurring_without_leaving_for_spending_plan`.
## #732 — Household can download net worth export from Accounts without leaving for Investments (2026-09-10)
**Context.** Net worth CSV/PDF lived on Investments (#696) and Settings. Accounts already shows the net-worth headline and trend, but downloading still required leaving.
**Decision.** Mount the same `/api/export?format=net-worth-csv` and `net-worth-pdf` links on /accounts (`accounts-net-worth-export-card` / `export-net-worth-csv` / `export-net-worth-pdf`), after AccountsList. Same export routes. No Settings remount. No PushOptIn / deepen / Sync / Coach Goals / category / CSV parser / Trust invent.
**Locked.** `test_regression__household_can_download_net_worth_export_from_accounts_without_leaving_for_investments`.
## #733 — Household can download net worth export from Home without leaving for Investments (2026-09-10)
**Context.** Net worth CSV/PDF lived on Investments (#696), Settings, and Accounts (#732). Home already mounts NetWorthCard with the headline and trend, but downloading still required leaving.
**Decision.** Mount the same `/api/export?format=net-worth-csv` and `net-worth-pdf` links on /dashboard after NetWorthCard (`home-net-worth-export-card` / `export-net-worth-csv` / `export-net-worth-pdf`). Same export routes. No Settings remount. No PushOptIn / deepen / Sync / Coach Goals / category / CSV parser / Trust invent.
**Locked.** `test_regression__household_can_download_net_worth_export_from_home_without_leaving_for_investments`.

## #734 - M.4 slice 3: section-label token, lead-column cap, body-wash scrolls (2026-09-11)
**Context.** #724's critic left three residual P2s: seven near-twin section-label literals outside the token; PAGE_LEAD_CLASS max-w-2xl overreaching the max-w-md routes (triage, error, transactions/import, transactions/new); body gradient background-attachment: fixed (iOS Safari ignores it; forces a paint on every scroll frame).
**Decision.** The seven files render PAGE_SECTION_LABEL_CLASS (spend-class-panel's h3 keeps mb-2). PAGE_LEAD_CLASS caps at max-w-md - a ceiling, not a width - and PAGE_LEAD_WIDE_CLASS ('sm:max-w-2xl') re-opens the column on the wide routes (accounts, goals, rules, triage, ask). Body gradient uses background-attachment: local; the desktop pinned-wash behavior is intentionally gone on every platform. Two byte-visible micro-deltas accepted on /investments and /recurring: label icon gaps gap-1.5 to gap-2, aligning to the token the dashboard cards already use. Same-session repair before gates: spend-class-panel's mb-2 was silently dropped by the tokenization and restored.
**Locked.** tests/unit/section-label-tokens.test.ts (all seven files render the token; the near-twin literal is gone from them); tests/unit/page-chrome.test.ts (token utilities + PAGE_LEAD_WIDE_CLASS wired into all five wide consumers).

## #735 - TASKS.md ledger cut: eight closed waves rotated verbatim (2026-09-11)

**Context.** The 2026-09-11 rotation commit named TASKS.md (139.9 KB then) the next cut. Closed waves here hold no [x] rows — their rows already moved to the done-row archive on each close — only the wave preamble + empty table remain live.
**Decision.** Eight fully-closed sections moved verbatim to docs/archive/TASKS_DONE_ARCHIVE.md under a dated marker, per the D.3 closed-wave-preamble precedent: Wave O (owner feedback 2026-07-27 — O.1 notes/tax-tag/export verified shipped via the archive's own rows), O.16 (come back to where I was), O.12 (categorization-work measurement — its 16 pipe-lines are measurement tables, not task rows), O.18 (expandable rows), O.19 totals, Wave C (calculation audit), Wave G (gate integrity), O.19 accounts. Before rotating, each range was re-verified row-by-row (zero status-badged rows) and Wave O's feature claims were confirmed against the archive and #725 — the wave's three owner requests all shipped. 143 KB → 126.5 KB; remaining bulk is open rows' live status cells, not rotatable content.
**Locked.** docs-lint clean (240 files); ledger-decisions-index suite 20/20; no `TASKS.md#` anchored links exist in-repo (grep). Move done by scripts/rotate-tasks-2026-09-11.mjs (one-shot, range-guarded: refuses to run if a heading is not where the range says).

## #736 - M.4 slice 4: the residual near-twin labels onto the token (2026-09-11)

**Context.** #734's critic residual: ~13 near-twin section-label literals outside the 7-file slice-3 charter (triage/rules h3s, budgets/settings/spending-plan/household cards).
**Decision.** Fifteen sites across 8 files render PAGE_SECTION_LABEL_CLASS: budgets/triage/rules/settings pages (the "Your categories"/"Built-in categories" h3 pairs, each keeping its mb-2), settings household-card ("You're invited"/"Pending invites"), learned-phrases, finance household-sharing-card ("Shared with you"/"Share your accounts"/"Pending invites"), and the spending-plan hero pair ("Guilt-free to spend"/"Over plan") — 13 h3 + 2 p. The two hero <p>s carry `${PAGE_SECTION_LABEL_CLASS} justify-center` (the recurring-view precedent): the token is a flex row, and parent text-center cannot center a flex item — the unpatched first cut left-aligned the eyebrow beside the centered text-5xl amount on all three hero branches. Out of scope, named: 10px badge pills, 11px nav group headers, 10px list-group headers inside pickers, onboarding step-indicator brand caption (text-brand-500) — different jobs, not section labels. Critic's count correction: household-sharing-card carries 2 h3 sites, not 3; the third literal I first listed was the "Share your accounts" heading, migrated same-session.
**Locked.** tests/unit/section-label-tokens.test.ts widened 7 → 15 files + a hero-centering lock (both hero uses must carry justify-center); tests/e2e/m4-page-chrome.spec.ts gained a computed-style lock: budgets label weight 500 and, on spending-plan, hero justifyContent center + label/amount bounding-box centers within 1px. The e2e alignment lock failed-old (caught the stale pre-fix build reporting justifyContent normal before the gate rebuilt).

## #737 - Goal progress + pace: how far along, and does the pledge make the date (2026-09-11)

**Context.** Owner: "continue building this app out. make it extremely user friendly and insightful towards goals." Explorer map: the /goals card answered one question ("how many months at this pledge?" + FI delay) and never the two a saver asks - "how far along am I?" and "will I make my date?"; no % funded anywhere, no on-track/off-track against `targetDate`, editing a date never re-solved the monthly, Home was silent about goals. Both engines needed already existed: `goalFundingMonths` (the flat timeline) and `solveSavingsGoalByDate` (Ask's inverse planner).
**Decision.** New pure engine `src/lib/engine/goals/progress.ts` - `goalProgress()`: `remainingCents`, `fundedBps` (FLOORED and clamped 0..10000 so 99.99% never reads 100%), `monthsToFunded` via `goalFundingMonths`, `fundedByDate` via `addMonthsClamped`, and for a dated goal `targetMonths` + `requiredMonthlyCents` read from `solveSavingsGoalByDate` itself (safeToSpend not consulted), so the card's "$375/mo gets you there on time" and Ask's answer for the same goal are ONE figure. Pace = funded | date-passed | no-pledge | no-date | on-track | behind, with `monthsDelta` and `gapMonthlyCents`. **The deadline is month-granular:** the form stores a month pick as YYYY-MM-01, Ask resolves "by June 2027" to the month's END, every card prints "by Jun 2027" - so pace is judged against `monthWindow(monthKey(targetDate)).to`. Against the stored 1st, a form-set "Jun 2027" at $500/mo toward $6,000 (funded 2027-06-10) read "behind by 1 month" while the identical Ask-saved goal read on pace; GP-L pins all three days of June alike. Not a data migration: the column's meaning is unchanged, the consumer reads it at the display's granularity. Copy in `progress-copy.ts`: one sentence per pace (second person, no shame, the flat-pledge assumption named where a projection is made) + `goalsHeadline` for a set. UI: `GoalProgressBlock` (bar with role=progressbar + "N% funded" + pace badge; the bar is one color in every pace - the verdict is TEXT, shape-not-color) and `GoalPaceLine` on every savings card on /goals above the FI line; the debt-free cards untouched. Home gains `GoalsProgressCard` (`src/server/goals.ts` `getGoalProgressRows`: `kind: null` only, sorted by `GOAL_PACE_ATTENTION_RANK` then name; top 3 + "All goals (N more)"; renders null with no goals) on the same `coach.today` as /goals. No writers added; demo unchanged. Named next: Ask answering "am I on track for <goal>?" against stored rows; a behind-pace nudge kind; seeded demo goals.
**Locked.** `tests/unit/goal-progress.test.ts` (GP-A..GP-L hand-derived anchors in `tests/edge-cases/goal-progress-and-pace-decisions-737.md`; solver-consistency + minimality property over a grid: exactly requiredMonthly is on-track, one cent less is behind with gap 1c; copy per pace; headline; rank order). `tests/e2e/goal-progress.spec.ts` (throwaway user; no-date -> on-track -> 25% + 3 months ahead -> behind with "$375.00/mo gets you there on time - $75.00/mo more"; Home headline + row + link).
**Critic (fresh context): cycle 1 FAIL 1 P1 - fixed same-session.** P1-1: the verdict leaned on `savedCents`, a hand-typed field with no unset state (Ask-saved goals start at $0), and named only the pledge as its assumption: probed one month after an Ask save with saved never updated, the row read "Behind pace ... $545.46/mo" on Home at rank 0 with zero reader action - a coaching judgment whose basis was invisible (lesson: an answer is only as believable as its visible inputs; a zero is a claim). Fix: every projecting sentence now says "counting the $X you've marked saved", the behind sentence offers "update what you've saved and this recalculates" before the bigger pledge, and the Home row prints "$X marked saved of $Y" beside the percent (GP-M anchor + an every-projection-names-both-inputs lock). P2s fixed: "is here" -> "has passed" for an older target month; "1 of 3 goals need" -> "needs"; "you'll be funded" -> "has this funded"; Home name gets `title` and the badge wraps under it; the one tautological grid assertion replaced with an independent oracle. P2s accepted and named: `wholeMonthsUntil` caps at 1200 months so a >100-year date overstates the required monthly (the month input has no max); a regex-passing invalid stored date would split the two readers (unreachable through current writers).

## #738 - Ask answers on-track for a stored savings goal (2026-09-11)

**Context.** GL.1 (#737) put progress + pace on every savings card and on Home. Ask still only inverse-planned a NEW amount + date (`savings_goal_by_date`); a reader with three named goals could not ask about any of them.
**Decision.** New intent `goal_status` `{ nameQuery }`. Parser `goalStatusFromQuestion` (shared with `intentFromKind`) requires pace/status vocabulary and an extractable name, then commits only when `matchGoalName` against the reader's stored savings names is unique or ambiguous (`none` declines so later routes and the unknown/LLM path still run). Amount-bearing questions stay the inverse planner / wealth target; retirement/FI/debt-free and payoff/paydown/snowball words decline. Timeframe / unreadable date / `at <store>` is `unknown` only after a unique match, and only on the leftover tokens *outside* the stored name (`leftoverAfterGoalName`) — a goal named "June wedding" is askable; "Japan trip this month" still abstains. A nameless "am I on track for my savings goal?" is `unknown` — never the only or first goal. Matching is exact normalized, then unique whole-word containment; reverse containment only when leftover tokens are filler (`my` / `goal` / `fund`); "car" ≠ "card"; a 1-token stored name never reverse-matches; ambiguous and none are first-class. Savings rows only (`getGoalProgressRows` / `listSavingsGoalNames`, both `userId` + `kind: null`). Answer `detail` is byte-identical to `goalPaceSentence`; headline is `{stored name} — {GOAL_PACE_LABEL}`. No-match and ambiguous answers carry no figures. Spend synonyms inside a goal name ("trip", "vacation") are the goal, not a Travel-group total — `resolveSpendTarget` is not consulted. Frame does not swap names this slice. Demo read-only, no writers. No new money math: every figure is `goalProgress` (#737 / §Goal-progress-and-pace).
**Locked.** `tests/unit/goal-name-match.test.ts` (unique / none / ambiguous / prefix-not-whole-word / 1-token reverse / filler-only reverse / leftoverAfterGoalName). `tests/unit/assistant-goal-status.test.ts` (routing + abstention majority + stolen-route pins + dated-name goals + `intentFromKind` re-derives the name + detail === `goalPaceSentence` on GP-A). `tests/e2e/goal-status-ask.spec.ts` (throwaway user; card sentence === Ask answer).
**Critic (fresh context): cycles 1–4 FAIL, all blocking findings fixed same-session.** Cycle 1: 1 P0 + 3 P1 — mid-parser `unknown` deleted `debt_payoff` ("on track to pay off my car loan"); bare behind/progress claimed rent/bills; blocked leftover `"spending"` returned unknown instead of `null` (stole `spend_total`); 1-token reverse containment (`Fund` inside "emergency fund"). Cycle 2: 2 P0 + 3 P1 — noun spellings (`payoff`/`snowball`) still stole `debt_payoff`; reverse containment of a 2-token name inside "car loan payoff" printed wrong-goal dollars; `income` / `account_balance` / `safe_to_spend` / `cash_needed` lost questions they answered at HEAD; unmatched nouns claimed a no-match and hid the miner. Fix: commit `goal_status` only when `matchGoalName` is unique/ambiguous; server passes `listSavingsGoalNames` into the parser. Cycle 3: 1 P0 — timeframe/`at` unknown ran *before* match, so "spending last month" extracted a fake name and deleted 67 later routes. Cycle 4: 1 P1 — after unique match, timeframe still ran on the *whole question*, so a goal named "June wedding" / "Summer 2027 trip" / "Trip at sea" was identified then discarded. Fix: leftover-token window. No 5th critic (budget 4); residual P2s accepted: a stored name that is also an app concept (`Spending plan`) outranks that standing route; a single generic token (`fund`) can uniquely hit a multi-word goal (headline prints the stored name); `fundedPercent` is duplicated at the Ask boundary; `listSavingsGoalNames` runs on every Ask; two rows with the same normalized name produce a dead-end disambiguation; some natural phrasings (`am I behind on my Japan trip?`) stay unknown; demo has no seeded goals (GL.4).
## #739 - The goals wave closes: behind-pace nudge, seeded demo goals, the 1200-month horizon (2026-09-11)

**Context.** GL.1 (#737) shipped progress + pace; GL.2 (#738) shipped Ask against stored rows. Three dead ends remained open in TASKS Wave GL: the Today feed did not know a slipping goal existed (GL.3), the live demo /goals was the empty worked example (GL.4), and `wholeMonthsUntil` saturates at its 1200-month cap so a >100-year target date was planned against the cap while the card printed the reader's own far figure (GL.5, #737 critic P2).

**Decision.**
- **GL.5** — the horizon is a WRITER's rule, one predicate beside the cap: `isBeyondPlanningHorizon(today, target)` in `src/lib/engine/solve/debt-free-by-date.ts` (the target month's END — GP-L — vs today + 1201 months; the month-END basis because the form stores the 1st while every deadline judges the month's end), called by `updateGoalTargetDate` (inline field error naming the horizon month) AND both Ask save writers (`saveSavingsGoal`, `saveDebtFreeGoal` — the critic's P1-3: the savings solver only unreachables a PAST date, so a 2200 target would have persisted a plan computed at the saturated 1200 months). The month input carries a native `max` (viewer's local clock — advisory; the server's refusal wins, named in code) and the ceiling is disclosed in an always-describedby'd hint. `createGoal` takes no date — nothing to guard.
- **GL.4** — the demo seed carries two savings goals, byte-shape identical to `createGoal`'s rows (`kind: null`): Vacation Fund $2,400 / $800 / $200/mo, no date (no-date pace, funded Feb 2027, 33% floored) and New Car Fund $6,000 / $600 / $150/mo, Jun 2027 (BEHIND by 24 months: 36 to fund vs 12 to the month-END; required $450/mo, gap $300/mo). Dates derive from `--asOf`; names avoid every e2e-created demo goal and Ask-spec noun. Seed log counts goals.
- **GL.3** — new nudge kind `goal_behind_pace` (ACTION tier, dismissable, never pushed). Rows come from the caller's own `goalProgress` rows via `goalPaceNudgeRowsFrom` (only `isGoalPaceNudgeWorthy` paces: behind | date-passed) — the feed re-derives NOTHING about pace, so it cannot disagree with the card about who is slipping. `centsAtStake` is `gapMonthlyCents` verbatim (the EXTRA monthly needed, never the pledge; 0 for a passed date); `sortDate` is the target month's END (the deadline the verdict was judged on); the dismissal fact is `goal_behind_pace:<goalId>:<YYYY-MM>` — a moved date returns the row, an unchanged one stays gone. The detail line is the goal card's own `goalPaceSentence` rendered from a verbatim context group (`Proposal.goalNudge`) — one copy truth with /goals and Home (the GL.2 invariant). Title branches on pace (`is behind pace` | `'s date has passed`, the card badges' wording); the why-line names the fact on a passed date instead of printing a $0 remedy. Engagement closed set gains `nudge:goal_behind_pace`; demo dismissal stays session-only (existing fence).

**E2e ripples GL.4 caused (fixed same-session, critic P1-1).** The demo's planned-savings line is now GOAL-DERIVED $350.00: glass-box's L.29 which-zero lock moved to a throwaway fixture where unset-$0 binds by construction (inline income seeding, the conscious-buckets idiom); the demo sections pin "Planned savings (goals)" / $350.00 / no control; the demo guilt-free panel keeps its penny-match and loses the "nothing is invented" provenance clause (a reader-chosen figure now enters the identity — `trace.ts dataDerived`). Also honest: conscious-buckets' `legendCents['conscious-savings'] === 350_00`, and the budgets composition card keeps "not set" (savingsTargetBps stays null; goals are larger but the note only says so when a target exists).

**Locked.** `tests/unit/goal-date-horizon.test.ts` (writer refusals: updateGoalTargetDate Jul-2126 refused / Jun-2126 accepted / 2027-06 stored as the 1st; BOTH Ask saves refuse 2200-01-01 with nothing persisted; form carries `max` + the ceiling disclosure). `tests/unit/seed-goals.test.ts` (two kind-null goals; names avoid e2e-created ones; Vacation 8-months/33%-floored; New Car behind-24/gap-300; the card sentence from seeded fields). `tests/unit/goal-pace-nudge.test.ts` (engine: ACTION tier, verbatim gap/0, dismissal fact keys goal+month, sortDate = month END, dated-ACTION ordering; copy: byte-identical detail, both titles, both why-lines). `tests/e2e/goal-demo-and-nudge.spec.ts` (demo Home "1 of 2 goals needs a look" + behind-first row; /goals both cards with badges/percent/sentence; dashboard `nudge-goal_behind_pace` with the card sentence; throwaway user: 2126-08 server-refused inline, 3200-01 blocked client-side, 2027-06 saves, cleanup delete). Demo e2e ripples re-locked in glass-box.spec.ts + conscious-buckets.spec.ts.

**Critic (fresh context): cycle 1 FAIL 3 P1 — all fixed same-session; cycle 2 PASS (0 P0, 0 P1).** Cycle-1 P1s: demo plan e2e pinned the pre-seed $0 savings world; date-passed row titled "behind pace" with a "$0/mo more needed" why-line; Ask save writers could persist past-horizon dates. Residual P2s accepted: the client `max` uses the viewer's local clock while the server judges `businessToday` (they agree for a real signed-in user — the only writer; demo fenced; server wins), and the glass-box comment now correctly credits conscious-buckets for the demo $350 lock.

## #740 - One discretionary definition: the creep bar classifies with the register (O.20h) (2026-09-12)

**Context.** TASKS O.20h (owner-reported, live): "Discretionary" meant two different things on screen. `detectLifestyleCreep` counted a row by the CATEGORY's `discretionary` taxonomy flag, while the register and /budgets label the same row by its SPEND CLASS (`classifySpendClass` — the reader's per-row verdict, then the recurring-bill merchant guess, then the flag; #397 "the reader's verdict on THIS row wins"). A gym membership reading "Fixed · you set this" in the register was still counted in the /coach discretionary bar, and the O.20d panel listed that row under "discretionary purchases" beside a re-file link that could not move the number. The shipped mitigation was a disclosure sentence admitting the divergence (insights.ts `creepPanelBasis`, the F2 block).

**Measurement before the fix (the prescribed-remedy-is-a-hypothesis rule).** `scripts/audit-probes/o20h-creep-definition-arms.mts` (read-only, live Neon + seed) ran both rules per user BEFORE any code changed. Real user: 49 disagreement rows ($1,008 override-fixed + $253 override-guilt-free + $4,979 recurring-bill merchants + $0 uncategorized), bar baseline $17,342.29 → $17,013.68, spend growth 1.0% → −3.8%, RENDERED VERDICT UNCHANGED (Tracking income, not flagged either way — the definition change moves the bar and the panel, not this reader's verdict). Live demo DB: 15 rows (Netflix/Spotify/LA Fitness/YouTube — detected recurring series the register already labels Fixed) leave the bar, verdict unchanged. Demo seed at DEMO_TODAY: 0 disagreement rows — byte-identical, the unit lock's world does not move.

**Decision.** The detector selects rows on `classifySpendClass(...) === 'guilt-free'` (one definition, the register's own label): new trailing param `fixedMerchants` (empty-set default keeps every existing caller byte-identical — demo/seed ride the taxonomy flag exactly as before), `coach.ts` loads `getRecurringBillMerchantCanonicals(userId)` and carries `spendClassOverride` (already on the snapshot shape) into the engine; Ask and Money Review inherit through the same `getCoachData`. The income-branch refund disclosure keeps its filed-category key (every positive row is classifier `out-of-scope` by construction, so a spend-class key there would be dead). Copy: the F2 divergence disclosure is DELETED with the divergence it described (closing-a-gap-shrinks-the-disclosure rule: re-derived, not edited blind — the replacement is the positive admission rule naming the unified basis, which covers every classifier refusal by construction). Deliberately OUT of scope (recorded, not silent): `averageDiscretionaryCategorySpend` (the wealth-target cut proposals) keeps the category-grain taxonomy definition — that figure names CATEGORIES to cut, not rows, and its dial-aware copy is a different question from the per-row bar; named as the residual "second discretionary" in TASKS O.20h. Rows with NO stored category (raw null or the 'uncategorized' placeholder) leave the bar — the register shows them "No class yet" (critic cycle-1 P1-1: the engine passes the RAW row to the classifier, never the pipeline's read-time guess, so an unlabeled row can never count by a label the reader cannot see).

**Locked.** `tests/unit/insights.test.ts` describe "one discretionary definition" (all FAIL-OLD: with the engine fix stashed, 7 failed | 52 passed on the pre-fix engine — override-fixed leaves bar+panel with spend unmeasured; override-guilt-free on a non-discretionary category now counts, BOTH directions of the override; a detected recurring series' merchant classifies fixed (Planet Fitness engineered through `detectRecurring` itself, FAIL-OLD contrast pinned in-test); raw-null AND unresolvable rows leave the bar; NO-input defaults keep existing callers byte-identical on the INGESTED seed). `creepPanelBasis` tests rewritten (new basis sentence pins; the F2 admission inverted to assert the divergence sentence is GONE; refund-sentence reworded to the figure's own basis). The pure seed's rows arrive uncategorized (categoryId null — categories are applied at ingest), so the seed locks run the same categorize pass `prisma/seed.ts` Phase 2 writes before the engine. Probe committed as the measurement artifact.

**Critic (fresh context): cycle 1 FAIL 1 P1 + 6 P2 — all fixed same-session; cycle 2 PASS (0 P0, 0 P1; 3 P2 fixed in passing).** P1-1: the spend branch classified the RESOLVED category (the read-time pipeline fallback), so a NULL-category row the register badges "No class yet" still counted through the pipeline's guess — the exact contradiction shape the slice closes, surviving, while the new copy asserted register-parity. Fixed by passing the RAW row to `classifySpendClass` (a null reaches the classifier's own refusal, exactly as `getTransactions` hands it over); the pipeline fallback stays ONLY in the income branch's refund trigger (a different job: where a credit went, the F7 guard). The trigger itself was re-keyed to the figure's own basis (critic P2-4): it now fires when the SIGN-FLIPPED twin would count (or the filing is 'refund'), so a reader whose gym rows are marked Fixed gets no "discretionary category" sentence about a gym refund their bar does not carry; the sentence reworded accordingly. P2-1: the demo-world comments were false — the seeded demo DB writes 12 RecurringSeries and `coach.ts` loads them for the demo too (the override store is fenced, the series loader is not), so the LIVE demo bar moves by the demo register's own classification (measured by the committed probe's LIVE-SEED arm: 4 rows/month — Netflix/Spotify/LA Fitness/YouTube Premium, ~$78/mo lower, spend growth 25.5% -> 27.0%, verdict unchanged, "Spending is outpacing income" both arms). P2-2: the FAIL-OLD count restated from a clean stash-revert rerun (7 failed | 52 passed; the original "5" was a stale count). P2-3: the probe's prod-demo arm was gated on DEMO_TODAY in `.env.prod.tmp`, which is absent — the arm now runs at the wall clock with an explicit NOTE naming what it does and does not describe, and pointing at the authoritative demo measurements (the raw-seed arm + the live-seed arm run separately). P2-5: the e2e counted panel CONTAINERS, not rows — now counts `li` children. P2-6: the coach-creep-verdict comment named a merchant-tier mechanism that spec's world never exercises (SQL-seeded, no sync, no stored series) — corrected. Cycle 2 re-attacked the fixes and the whole slice (independently reproduced the gate, the 7|52 FAIL-OLD, and the demo figures from committed code) and returned 0 P0/P1; its 3 P2s: the stale "5 failed" count in REGRESSION_LEDGER + TASKS (corrected to 7|52), the demo figures citing a deleted one-off script (now re-derived by the committed probe's LIVE-SEED arm), and the probe census computing the NEW arm on the resolved category instead of the shipped raw-row rule (fixed; no recorded number was wrong — the bucket was $0 on this corpus).

