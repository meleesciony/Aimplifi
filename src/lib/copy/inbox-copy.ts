/**
 * Inbox (triage) reader-facing copy. Live 2026-09-01 the page promised
 * "Only genuinely ambiguous transactions land here — everything else is
 * filed automatically" next to Categorization accuracy 5.3%. The accuracy
 * figure is a real scored hit-rate over later labels; the promise is not.
 * These strings are the one author of that claim.
 */
export const INBOX_PAGE_SUBTITLE =
  'Transactions that still need a category. Accuracy below is how often auto-file matched a later label — not a promise that this list is small.';

export const INBOX_NAV_DESCRIPTION =
  'Transactions that still need a category.';

export const INBOX_EMPTY_TITLE = 'Transactions that still need a category land here';

export const INBOX_EMPTY_DESCRIPTION =
  'Once accounts are connected, this inbox collects transactions that still need a category decision.';

export const INBOX_EMPTY_FOOTNOTE =
  'Your data is private to your account. Connect once, then review transactions that still need a category.';
