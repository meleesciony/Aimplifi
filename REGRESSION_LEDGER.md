# REGRESSION_LEDGER.md

Per LOOP_ENGINEERING.md: every fix ships a regression test plus one line here
(`date | symptom | root cause | rule broken | locking test`). Append-only. Earlier
phase regressions live as named tests (tests/unit/critic*.test.ts) + docs/DECISIONS.md;
this ledger starts at the 2026-06-21 hardening session.

> Rows from 2026-06/2026-07 were moved verbatim to
> `docs/archive/REGRESSION_LEDGER_ARCHIVE_2026-06_to_2026-07.md` on 2026-08-04.
> Append new rows here as before.

| date | symptom | root cause | rule broken | locking test |
|---|---|---|---|---|
| 2026-08-03 | Owner: Discretionary dial and Return buttons looked sloppy — much larger than Details/Rules on the same row | Register dial was a native `<select>`; globals.css floors every select at 16px on touch (iOS zoom guard #140), so it outgrew the text-[10px] chips beside it; Return used text-sm | Row chrome shares one chip size; dial is a button menu, not a select; Return matches | e2e `txn-spend-class` asserts demo has no dial button/select; visual: same classes as Details/Rules |
| 2026-08-03 | Pending Hair Capital / Whole Foods showed "Not counted" with no Fixed/Discretionary dial — owner could not change class on the row he was looking at; pending sat under their authorization date among cleared rows instead of at the top like Mint/Simplifi | `classifySpendClass` reused `countsInFlows` (POSTED-only); register sort was date-only | Settlement status is not a spend-class axis (dial works on pending); pending pin to the top of Activity until cleared | `test_regression__pending_categorized_outflow_keeps_the_dial` in `tests/unit/spend-class.test.ts`; `test_regression__pending_stays_at_top_until_cleared` in `tests/unit/transactions-query.test.ts` |
| 2026-08-03 | Rules Return from Activity dumped the owner onto one prefilled transaction (Hair Capital) after he had been reviewing the list | `?from=` alone was treated as "he was on that row"; unfiltered Activity attached no `?back=` | Prefill ≠ place: always attach `?back=` (sentinel `_activity` when unfiltered); `via=row` only when he left detail — then primary Return is that row AND Activity is always offered | `tests/unit/register-return.test.ts` Activity sentinel; e2e `register-return` unfiltered→Activity + detail `via=row`→transaction + Activity |
| 2026-08-03 | Rules page had no Fixed/Discretionary THEN action and no reliable Return — owner lost his place in Activity after making a rule; wanted algorithmic guess (recurring→Fixed) with extra same-month charges left unstamped (utilities vary in amount) | Keyword rules stamped category/tax/rename only; return link was absent unless `?back=` carried a narrowed register view | Rule THEN actions that write money-adjacent state must preview then apply on one basis; extras in a billing period take no override (keep guessing); leaving Activity for Rules always offers Return to the place he left | `tests/unit/rule-spend-class-action.test.ts` + `createKeywordRule — Fixed/Discretionary (extra occurrence abstains)` in `tests/unit/keyword-rules-server.test.ts`; e2e `register-return` |
| 2026-08-03 | Owner: marking one recurring charge (Chun’s Martial Arts) Fixed should offer "all other transactions like this" — per-row flips alone mean re-marking every month | The #397 dial wrote only the one row; no merchant-wide scope existed for spend class (the category recategorize has had once/always since #36) | A bulk money-affecting write shows its blast radius BEFORE the tap (the count is computed on the write’s own targeting basis, reconciliation-filtered) and per-row agreement still stores NULL | `tests/unit/transaction-spend-class-actions.test.ts` merchant-wide cases (all payee rows written, other payees untouched, agreement→NULL, demo fence) |
| 2026-08-03 | Owner: "fixed vs discretionary is not category based — not all hair and beauty is fixed; when I switch one transaction in this category to discretionary, they all do" | The #396 restoration made the verdict a per-CATEGORY designation (CategoryFixedOverride), so one row’s flip rewrote every transaction filed under that category | The class is a property of the TRANSACTION: the reader’s per-row verdict (`Transaction.spendClassOverride`) wins; the app guesses per row (recurring-bill merchant → fixed — the owner’s seed rule — else the category’s taxonomy flag); a dial choice matching the guess stores NULL so the guess stays the default | `test_regression__flipping_one_transaction_leaves_category_siblings_alone` + per-row `test_regression__user_override_moves_dining_into_fixed_median` + the recurring-guess cases in `tests/unit/spend-class.test.ts`; `tests/unit/transaction-spend-class-actions.test.ts` (real action: one-row flip, matching-guess-NULL, recurring-guess agreement, demo fence) |
| 2026-08-03 | The Fixed/Discretionary dial vanished from the register and transaction detail while the owner still wanted it — commit ad23e40 (#395) removed the dial, the `CategoryFixedOverride` table, and `setCategoryFixed` citing an owner directive ("deterministic and algorithmic, never typed in") the owner says he never gave | An "owner directive" was recorded in DECISIONS #395 and executed without tracing to words the owner actually said; the reversal restores the pre-#395 implementation from `ad23e40^` | No-fabrication prime directive — a claimed directive must trace to the owner; a change built on one that does not is reversed, not defended. The deterministic classifier stays the DEFAULT (matching choices delete the override row) | Restored locks: `test_regression__user_override_moves_dining_into_fixed_median` + `test_regression__custom_nondiscretionary_category_counts_as_fixed` in `tests/unit/spend-class.test.ts`; `test_regression__set_category_fixed_refuses_shared_demo` in `tests/unit/category-fixed-actions.test.ts`. The deterministic-default contract stays locked by `test_regression__class_follows_the_filed_category_only`. Owner follow-up same day widened the restore: /budgets Mark fixed / Mark guilt-free + "you set this" markers and the Discretionary checkbox on all three create forms (locks: the summarize $0-override-visibility case in `tests/unit/spend-class.test.ts`; e2e `spend-class.spec.ts` demo-note + fenced move buttons) |
| 2026-08-03 | Owner directive: "none of this should be typed in — make it all deterministic and algorithmic" — Fixed/Discretionary was a hand-set per-user designation (CategoryFixedOverride table, register `<select>`, /budgets Mark buttons, custom-category Discretionary checkbox), and rows could read as permanently mis-classed | The designation channel itself was manual input overlaid on the taxonomy | No user-keyed designation channel: class = pure function of the filed category (taxonomy `discretionary`); the reader changes a row's class by refiling it, never by labeling it. Supersedes the same-day dial-gate entry below and the #378 dial. SUPERSEDED same day by #396 — the owner never gave this directive; the dial is restored | `test_regression__class_follows_the_filed_category_only` in `tests/unit/spend-class.test.ts` + e2e `spend-class` / `txn-spend-class` no-control locks |
| 2026-08-03 | Pending / refund / transfer-flagged rows under a spending category showed a permanent "Neither" badge with no way to choose Fixed vs Discretionary | SpendClassSelect gated editability on the row's own spendClass (`!== 'out-of-scope'`) instead of the category's designatability | RECORDED BUT NEVER SHIPPED: the category-gated dial (`TxnView.categoryFixed`) and its lock `test_regression__out_of_scope_row_under_designatable_category_keeps_the_dial` exist in no commit — `git log -S` finds them only in ad23e40’s own ledger text. The #396 restoration keeps the pre-#395 row-class gate; an out-of-scope row’s category stays designatable from any counted row in that category or the /budgets Mark buttons | none — an unshipped claim corrected, not locked; a real fix would gate on the category’s designatability and ship the missing lock |
| 2026-08-02 | Dashboard Trends card hid the pace day-count and assumption, tinted the projection green/rose (tie → "less"/green), and showed "Biggest change" with no window | Card omitted fields already on `SpendingTrends`; colour treated an early-month extrapolation as a verdict; mover is a completed-month fact | Name divisor + assumption; neutral pace copy; label mover window; share helpers with /trends | `tests/unit/trends-labels.test.ts` + e2e `trends.spec.ts` dashboard testids |
| 2026-08-01 | #384 as drafted zeroed a $45 gym bill autopaid from savings — detected-series fallback returned $0 where it had returned $4500 | The union rewrite was applied to the `detected-series` last-resort branch too; `fitness` is discretionary so the designation-aware sum skipped the only known bill | Last-resort basis counts every detected recurring outflow; designation filtering belongs only to the rollup/median branches | `test_regression__detected_series_fallback_counts_discretionary_recurring` + L.25/L.26 in `cash-account-recurring-scope.test.ts` |
| 2026-08-01 | Median Fixed fallback Math.max dropped complementary auto-loan; copy said “median” when recurring won | #382 fixed rollup path only; trailing branch still scalar max | median + uncovered recurring (same covered-ids union); honest copy | `test_regression__median_fallback_unions_uncovered_auto_loan_not_max` + `test_regression__isTransfer_auto_loan_absent_from_category_rollup` |
| 2026-08-01 | Plan income ~half / “only one paycheck” on steady biweekly pay | #370 used only paycheck leaves once any existed; twin still on Income parent dropped | earned + generic Income (not untouchable); tax-refund stays out | `test_regression__second_paycheck_still_on_generic_income_is_not_dropped` |
| 2026-08-01 | Fixed / Guilt-free aggregate headings were dead — owner could not open every transaction under that heading | TxnFilter had only categoryId; bucket link would be unfiltered or false equality vs Plan $ | Register spendClass filter + heading links; never Plan dollar (different basis) | `spend-class-register-links` + e2e `spend-class-drilldown` |
| 2026-08-01 | Conscious-spending caption still said "fixed and recurring costs" after Fixed widened to groceries/utilities | W.5 band assumed fuller numerator; B.1 widened math but copy stayed narrow | Keep 50–60% band; CONSCIOUS_BUCKET_COUNTS + consciousSpending name must-pay Fixed | `test_regression__conscious_caption_names_must_pay_fixed_not_bills_alone` + e2e conscious-caption |
| 2026-08-01 | #381 union skipped all Fixed-category recurring — demo auto-loan ($385, isTransfer) vanished whenever groceries rollup was active | `fixed===true` treated as "in rollup" without checking covered ids | Skip Fixed series only if category id is in `categoryFixedCoveredIds` | `test_regression__transfer_fixed_auto_loan_unions_when_absent_from_rollup` |
| 2026-08-01 | Plan Fixed used max(rollup, recurring) — complementary groceries+mortgage-transfer undercounted; "floor" copy lied; card spend dropped once card pay left Plan | Scalar max cannot union; `on-card` assumed card bill held Fixed | categoryRollup (incl. CREDIT purchases) + uncovered recurring; never credit-card-payment; honest card-charge $0 copy | `test_regression__category_rollup_unions_out_of_scope_recurring` + `test_regression__credit_card_purchases_count_in_fixed_card_payment_does_not` |
| 2026-08-01 | Plan Fixed stayed on whole-median until reader set an override/budget — B.1 term invisible by default | #377 demo golden-safety gate | Always-on category rollup when total > 0 | `test_regression__category_rollup_drives_plan_whenever_positive` + e2e budgeting-fixed-basis |
| 2026-08-01 | Transactions register had no Fixed/Discretionary label or correction control | Designations lived only on /budgets; owner asked to categorize every txn with a selector when wrong | TxnView.spendClass + SpendClassSelect → CategoryFixedOverride; demo label-only | e2e `txn-spend-class.spec.ts` (`txn-spend-class`) |
| 2026-08-01 | Budgeting page had no income/savings/fixed→guilt-free composition; Fixed categories had no settable monthly amount feeding Plan | Classification (#376) only; Plan still whole-median; dials buried on Plan/Settings | Composition card + PlanFiguresForm on /budgets; budget\|typical per Fixed cat | `test_regression__budget_target_wins_over_typical_for_fixed_category` + e2e budgeting-composition |
| 2026-08-01 | No way to set which expenses are fixed vs guilt-free; custom non-disc categories ignored by Plan median | Fixed used static CATEGORY_BY_ID only; no per-user designation; budgets had targets but no Fixed section | classifySpendClass + CategoryFixedOverride; Plan/meta-aware; /budgets Fixed vs guilt-free panel | `test_regression__user_override_moves_dining_into_fixed_median` + `test_regression__custom_nondiscretionary_category_counts_as_fixed` + e2e spend-class |
| 2026-08-01 | Wealth years dial ignored Settings savings %; no cut list for lower-priority discretionary outside money dials | Contribution was trailing surplus only; opportunities were bill/sub kinds; dials were copy-only | Contribution prefers planned savings %; +/− years; dial-aware discretionary cut proposals | `test_regression__wealth_contribution_prefers_settings_savings_pct` + `test_regression__money_dials_protect_named_categories_from_cut_list` + e2e wealth-target |
| 2026-08-01 | Inbox trapped on unclear/old charges — no next without filing; masked "." groups and silent mixed cards hid register-level guesses | Queue advanced only on file; group unanimity null hid per-row ladder; punctuation canonical rendered as a merchant name | Skip rotates without filing; detail link + age; honest masked heading; One-by-one shows `registerSuggestionFor` chips | `test_regression__skip_rotates_front_card_without_dropping_it` + `test_regression__masked_descriptor_heading_is_honest` + e2e skip (#374) |
| 2026-08-01 | Owner asked why income must be retyped when paychecks are already categorized; locked figures needed a clear meaning when data drifts | Overrides were "replace suggestion" with no variance surface — reader either retyped forever or lost the intention when data moved | Set savings %; income/fixed default from categories; locks = intention; data − intention = slide/overspend, never silent rewrite | `tests/unit/fixed-pattern.test.ts` `test_regression__data_diff_from_intention_is_a_slide_not_a_rewrite` |
| 2026-08-01 | Guilt-free still ~$15k after #370; owner expects income − 25% − non-disc fixed | Fixed term was only 4 recurring series (~$834), not groceries/housing/utilities | Fixed = median non-discretionary spend (max with recurring floor); dining/golf out, groceries in; user overrides on Plan | `tests/unit/fixed-pattern.test.ts` + `tests/unit/plan-overrides.test.ts` |
| 2026-08-01 | Guilt-free ~$23k after #369; MM / second checking / investment deposits inflated income | Trailing pattern summed every NON-CREDIT account + treated mobile deposits / interest / CEF as spendable income | Payment-account scope + earned-pay preference (`paycheck`/`bonus`/`side-income`); exclude interest/investment/mobile-deposit | `tests/unit/income-pattern.test.ts` + `tests/unit/spending-plan-server.test.ts` payment-scope + paycheck-preferred locks |
| 2026-08-01 | Guilt-free subtracted card statement payments (and L.11(D) beyond-month dues), double-counting spend already categorized as fixed/discretionary; Home buried transactions | Plan treated card pay as a fixed commitment class (L.11(C)/(D)) | Card pay is settlement of spend, not a cost class; guilt-free = income − fixed − savings | `tests/unit/spending-plan.test.ts` ("a card obligation alone does NOT drive the plan overspent") + glass-box 3-row identity + `tests/e2e/spending-plan-month-edge.spec.ts` ($10,000 headline, no beyond-month plan row) |
| 2026-08-01 | Guilt-free subtracted card statement payments (and L.11(D) beyond-month dues), double-counting spend the owner had already categorized as fixed/discretionary; Home buried transactions and the monthly allocation | Plan treated card pay as a fixed commitment class (L.11(C)/(D)) | Card pay is settlement of spend, not a cost class; guilt-free = income − fixed − savings; liquidity stays on cash-needed | `tests/unit/spending-plan.test.ts` ("a card obligation alone does NOT drive the plan overspent") + glass-box 3-row identity + `tests/e2e/spending-plan-month-edge.spec.ts` ($10,000 headline, no beyond-month plan row) |




| 2026-08-01 | Every FI date, Coast verdict and slider answer on /coach arrived years early, because the portfolio was compounded at the NOMINAL return toward a target denominated in TODAY'S dollars | `getCoachData` passed `user.expectedReturnBps` into `monthsToFI`/`coastFI` against `fiNumberCents(annualExpenses, swrBps)`, where `annualExpenses` is the reader's last six complete months of spending x 2 — so future nominal dollars were compared against a present value. The app had already written the argument against itself in `coach-copy.ts` ("a $10M answer at a nominal rate against a present-value goal would be optimistic by decades") while the card one component up did exactly that | "a projection's rate and its target must be in the same units — an engine whose arithmetic is right can still be wrong in its third argument" | `src/server/coach.ts` derives `projectionReturnBps = realReturnBps(expectedReturnBps, inflationBps)`; `tests/unit/fi-real-basis.test.ts` — "years-to-FI is the REAL-rate answer, and the nominal one would have been sooner" + the Coast twin, both asserting the nominal recomputation is a DIFFERENT and FLATTERING number so the pin cannot pass vacuously (mutation-proven: restoring the nominal argument fails exactly 3) |
| 2026-08-01 | A reader saving $500 a month was told "Contributions aren't outpacing spending yet" | `monthsToFI` returns `null` for two unrelated reasons — savings at or below zero, and positive savings that miss the engine's 1200-month cap — and `fi-card.tsx` mapped `null` to a sentence asserting the first. W.2's lower rate widened the second set: at a floored 0% real rate the cap binds on savings below `fiTarget / 1200`, i.e. $740/month against a $900k target. Found independently by BOTH W.2 critics | "an engine's `null` is a value, not an absence — count the states before choosing the type, or a two-state model locks the missing third" | `COACH_COPY.fiHeadline` selects among four states and is EXPORTED, so the card renders it and the test calls it rather than re-implementing the ternary (the first draft of the test mirrored it and would have passed while the card regressed); `tests/unit/fi-real-basis.test.ts` — "a saver past the projection cap is NOT told they aren't saving (F1)" + "a genuinely non-saving reader still gets the plain sentence" (mutation-proven: collapsing the states fails exactly 2) |
| 2026-08-01 | /coach refused to project a date ("a projection date wouldn't be honest") directly above a Coast line handing the reader a date | `years-to-fi` and `coast-fi` are two unconditional paragraphs driven by two independent engine calls; a large portfolio with negative savings makes `monthsToFI` null while `coastFI` reports `isCoastFI: true`. Pre-existing, surfaced by the critic pass on the slice that rewrote both sentences and added a basis line asserting they are one coherent story | "two sentences on one card are one claim — a refusal and an answer to the same question cannot both stand" | `notOnTrackButCoasting` as the third `fiHeadline` state; `tests/unit/fi-real-basis.test.ts` — "\"not on track\" and \"already Coast FI\" no longer contradict each other (F3)", which asserts BOTH preconditions genuinely hold on the fixture before checking the copy |
| 2026-08-01 | /goals told the reader a goal would delay an FI date 40 months earlier than the FI date the card they had just left displayed | `goalFIImpact` ran the identical simulation against the identical present-value `fiNumberCents` at `coach.fi.expectedReturnBps` (nominal) — measured 181 months vs /coach's 221 for one reader — and nothing on /goals named a basis. W.2 fixed /coach and left its neighbour, which is the exact defect W.2 exists to remove | "fixing a basis on one surface creates a contradiction on every surface that shared the old one — trace every consumer of the payload before declaring the fix done" | Both `goalFIImpact` call sites in `src/app/(app)/goals/page.tsx` take `coach.fi.projectionReturnBps`, and the page states the basis; covered by the shared-payload assertions in `tests/unit/fi-real-basis.test.ts` and `tests/unit/retirement-outlook.test.ts` |
| 2026-08-01 | The FI card's monthly Coast figure silently changed from a nominal standing order into a today's-dollars instalment, while a new sentence told the reader it was "on the same footing" as the sibling card that DID carry the escalation caveat | Switching `coastFI`'s third argument to a real rate changed what its dollars MEAN — by `retirement.ts`'s own documented convention, level contributions at a real return are today's dollars, so a standing order set once lands short — but `notCoastFI`'s sentence was left as written. The failure direction is under-saving | "changing a rate changes what every dollar figure derived from it means — a figure's units are part of its copy" | `fiProjectionBasis` carries the escalation clause once for the whole card and `notCoastFI` says "in today's money"; `tests/unit/fi-real-basis.test.ts` — "says the monthly figures are today's dollars that a flat standing order outruns (UI-1)" |
| 2026-08-01 | `realReturnBps` could return a rate ABOVE the reader's own nominal dial while reporting that nothing had been clamped | `Math.max(0, nominal - inflation)` guarded only the top of the range, so a negative `inflationBps` made the subtraction add (`-250` yielded 9.50% off a 7.00% assumption) and `isRealReturnFloored` returned false — the card would have printed a projection more optimistic than the dial it claims to derive from. `validateDials` bounds inflation to [0,1000] and is the only writer today, but `User.inflationBps` carries no DB constraint and four surfaces share the helper | "a shared engine helper must be total at both ends — validation at one writer is not a property of the function" | `Math.max(0, nominalReturnBps - Math.max(0, inflationBps))`, mirrored in `isRealReturnFloored`; `tests/unit/fi-real-basis.test.ts` — "a negative inflation row cannot lift the real rate above the nominal dial (F7)" (mutation-proven) |
| 2026-08-01 | Three e2e specs failed with /dashboard rendering "Something went wrong", presenting as a P0 in the change under test, when the code was correct | A `next start` from an earlier run was still LISTENING on port 3100 and `playwright.config.ts` sets `reuseExistingServer: true` locally, so the new specs ran against a build predating the fixes. Killing the process turned all three green with no code change. TASKS V.1 records the opposite for this machine ("nothing is LISTENING on port 3100 between runs"), so the leaked-server hypothesis had been wrongly treated as refuted | "check the port before believing an e2e failure — a stale server produces a false P0 that looks exactly like a real one, and 'it passed alone' is not the only tell" | Filed as TASKS V.3 (make the harness refuse a reused server whose build predates the working tree, rather than relying on a human remembering to check); no product code involved |
| 2026-08-01 | The month-flow panel's clamp sentence read "Returns in Jun 2026 outran purchases by **−$80.00**" — a double negative asserting the opposite of what happened, directly under a $0.00 headline | `clampedByNetRefund` is true only when `sumCents < 0`, so the label the panel passed was ALWAYS negative, and the sentence phrases it as a magnitude after "by". The unit test exercised the copy with a positive literal (`'$80.00'`) that the sole production call site can never emit — a fixture that cannot represent the only input the caller produces | "shape a copy fixture exactly as production emits it; and when a sign must be right, take the magnitude inside the function rather than trusting each call site" | `monthFlowNetRefundCopy` now takes CENTS and applies `Math.abs` itself; `tests/unit/month-flow-breakdown.test.ts` — "the clamp sentence states a MAGNITUDE, never a negative after “by”", which feeds it the engine's own negative `sumCents` |
| 2026-08-01 | The month-flow basis sentences described the income/expense split as a rule about "refunds", and were falsified by rows rendered inside the very panels they described | `isIncomeFlowRow` admits a positive row with NO category as income, while one filed to `uncategorized` (group 'Transfers & Other') is NOT income and nets against SPENDING, and a NEGATIVE row in an income category is spending. Two unfiled deposits that look identical to a reader therefore land on opposite sides. Two critics found this from opposite directions, each with half of it, and the first fix believed one of them without executing the predicate | "derive a disclosure by running the predicate over its whole domain, never by editing the previous sentence to add the case you just heard about — and a delegated finding is a hypothesis until reproduced" | `MONTH_FLOW_BASIS` names all three consequences; `tests/unit/month-flow-breakdown.test.ts` carries a fixture row for each class (`unfiled-null`, `unfiled-uncat`, `clawback`) plus "names all THREE consequences of the flow split, not just refunds" |
| 2026-08-01 | Re-tapping the same chart bar after collapsing its panel did nothing at all, leaving the chart's most obviously interactive element inert | `selectBar` set `selectedMonth`/`autoOpenFlow` to values they already held, so React bailed out, the panel's `key` was unchanged and no remount re-applied `defaultOpen`. The sibling defect: keying both panels on `autoOpenFlow` meant tapping one flow's bar silently collapsed the other flow's open panel | "a remount-to-reset-state key must contain something that changes on every invocation, and must not contain state that belongs to a sibling" | `barTapSeq` increments per bar tap and appears only in the key of the panel being opened; `tests/e2e/month-flow-drilldown.spec.ts` — "clicking a bar on the chart opens that bar’s rows" now collapses and re-taps the same bar |
| 2026-08-01 | `tests/e2e/reports.spec.ts` failed strict-mode: `getByText('Spending by category')` resolved to 2 elements | The new cross-basis sentence on the chart card quotes the section by name ("…and “Spending by category” below count on different rules"), widening a bare text locator that had always meant the heading. Same class as Wave 0.2's auth.spec "Sign out" breakage | "adding copy that names another section can break any bare text locator pointed at it — scope assertions to the role they mean" | `getByRole('heading', { name: 'Spending by category' })` |
| 2026-08-01 | /coach's "Worth a look" rows printed 30-year NOMINAL future values ("future wealth … assuming 7.00%") one scroll below an FI card that compounds in today's money — two units, ~2× apart, nothing saying which was which | `findOpportunities` grew every monthly amount at `expectedReturnBps` only and `COACH_COPY.opportunity` labelled the result "future wealth"; W.2 moved the card above onto the real rate and left this list | "a rate and its target must share a unit — and adjacency is a claim: two dollar figures on one page with no unit label assert they are comparable" | `opportunityValueTodayCents` + renamed `todayValue*Cents`; `tests/unit/fi-real-basis.test.ts` W.10 block ("is the return dial grown then deflated — and the nominal figure it replaced was far larger"); e2e golden `$20,350.61` in `phase3-coach.spec.ts` |
| 2026-08-01 | The first W.10 draft compounded opportunities at the REAL rate and overstated a flat $20/mo retention offer by ~30.6% | Level-in-today's-dollars models a contribution that rises with inflation; `negotiable-bill` is a hard-coded flat $20/mo with no price to index, so the model asserted a property its input cannot have. Two independent critics killed it on the same row | "a modelling assumption that must be argued per row is the wrong assumption — prefer one conservative convention whose premise is the sentence beside the figure" | `tests/unit/fi-real-basis.test.ts` — "a flat retention estimate is not grown as though it were a price" (pins the >1.25× gap so re-adopting the flattering model changes a test that explains itself) |
| 2026-08-01 | A critic swapped the 30- and 20-year opportunity figures into each other's template slots and every `toContain` assertion in the repo stayed green | Each lock checked that a figure appeared somewhere in the sentence, never WHERE; a reader would have been told the 20-year total was the larger | "pin a golden SENTENCE (or an ordered equality), not three containments — a figure in the wrong slot is the same class as a wrong figure" | `tests/unit/fi-real-basis.test.ts` — "the three horizons cannot be printed in the wrong slots" |
| 2026-08-01 | Two of four opportunity kinds ran a colon straight into "is $X" ("…assuming a standard offer): is $15,187.72…") — a verb with no subject — since Phase 3 | The copy guardrail sweeps scan for shame words and assumption clauses, not grammar; both estimate branches concatenated `base` + `fv` after a colon-terminated parenthetical | "a sentence that must parse for a reader is not covered by a word-list sweep — read each branch aloud, or lock against the broken shape" | `tests/unit/fi-real-basis.test.ts` — "all four row kinds parse, and each names its own monthly amount" (`not.toMatch(/[):]\s+is\s/)`) |
| 2026-08-01 | An 85% saver saw "Lowering your savings rate from 85.0% to 70.0%…" on /coach before touching the FI slider, with the two year figures on different inputs | `fi-card.tsx` initialised the thumb at `Math.min(7000, currentRateBps)` against a hard `max={7000}`, so `sliderCaption`'s CHANGED branch fired on first paint while `sliderContext` still named the unclamped 6-month average | "a control's initial value must be a value the control can hold — and a caption that compares two rates may not run until both rates are ones the reader chose" | `fiSliderMaxBps`/`fiSliderInitialBps`; `tests/unit/fi-slider-bounds.test.ts` (fail-old pin of the hard clamp + unchanged-branch first paint); e2e `phase3-coach.spec.ts` first-paint "current pace" |
| 2026-08-01 | /coach's opportunity-basis sentence was silent for 1,579 legal dial pairs whose figures land below the dollars the reader pays in, and over-claimed "below" where a rounded figure ties | The branch was gated on `inflationBps >= nominalBps` — a rule about the INPUTS — while the real relation depends on the horizon too (each annuity dollar is invested for less than the full span while the deflator runs all of it), so break-even sits well above equal dials; 149 pairs also trail at 10/20 years but not at 30 | "gate a sentence about a computed figure on the COMPUTATION, not on a rule about its inputs — and check the claim's wording against the ROUNDED display, not the exact value" | `opportunityValueTrailsContributions` (shared with the figures via `OPPORTUNITY_HORIZON_MONTHS`); `tests/unit/fi-real-basis.test.ts` — "the trailing sentence is gated on the arithmetic, not on a comparison of the dials" (pins 10.25%/10.00% and the mixed 3.25%/1.75% pair) and "when the sentence speaks, no row it qualifies exceeds its own contributions" (whole-grid sweep, 3 amounts) |
| 2026-08-01 | Every /coach opportunity row ended "compounding does the work, not willpower" beside figures at or below the dollars the reader hands over — at 10.25%/10.00% a $50/mo row printed $6,833.08 against $18,000 paid in, and a reader on the DEFAULT 7.00% return reaches it by setting inflation to 3.75% | The clause was guarded on `nominalReturnBps === 0` — a rule about one dial — on the strength of #363's recorded claim that the zero dial was "the only degenerate input"; W.10a's sweep of the same grid disproved that one function away (1,275 of 2,400 non-zero pairs trail at every horizon, 149 at some) and the neighbouring claim was never revisited | "proving a new fact obliges you to re-read the arguments that assumed its negation — a sweep that falsifies a premise falsifies every decision resting on it, including the ones in adjacent functions" | `opportunityRowTrailsContributions` (reads the row's PRINTED values, not the dials); `tests/unit/fi-real-basis.test.ts` — "will not credit compounding beside a figure at or below the dollars paid in" (7.00%/4.00%, two horizons trail and one does not) and "the row guard reads the figures it prints, not the dials it was grown at" (the exact tie, plus the +1c both-directions flip); both mutation-proven |
| 2026-08-01 | Every /coach and /investments sentence naming the return dial called it "your 7.00% return assumption" — and the wealth card said "7.00% return is your setting" — to readers who had never opened /settings, including the live demo | `User.expectedReturnBps` is `Int @default(700)` and NOT nullable, and its /settings field is required and pre-filled, so there is no stored "unset" the way `inflationIsDefault` reads a null `inflationBps`; six hand-written possessives each assumed the value was chosen | "a column `@default` is a value you shipped without choosing it — where no column records the choice, attribute by VALUE and claim only what the equality proves ('this IS our default', never 'you have not changed it')" | `returnIsAppDefault` + `DialOwnership` (one object, so two adjacent booleans cannot be swapped); `tests/unit/return-dial-default.test.ts` (schema `@default` parity, non-zero, the pinned trade-off); `tests/unit/fi-real-basis.test.ts` — "W.13 — the return dial the reader never set"; rendered locks in `phase3-coach.spec.ts`, `wealth-target.spec.ts`, `investments.spec.ts`; server flag mutation-proven both ways, /investments through a rebuild |
| 2026-08-01 | Three rows of the copy-guardrail scan table paired an inflation rate of 1000 (or 0) with `inflationIsDefault: true` — a combination production cannot produce — under a comment I had just written claiming every row's rate agreed with its flags | The fallback inflation IS 250, so any other value implies the reader set it; the rows were written by pattern from the branch names rather than from what a real user row can hold | "a fixture's ownership flags are part of its reachability — a scan row pinning a sentence production can never print certifies nothing, and the comment claiming otherwise is the tell" | `tests/unit/coach-copy.test.ts` — the `DEFAULT_RETURN` rows (`fiProjectionBasis:floored+defaultReturn`, `wealthTargetBasis:floored+defaultReturn`, `wealthTargetSensitivityIntro:degenerate+defaultReturn`) and the corrected comment above them |
| 2026-08-01 | /coach FI card restated 4.50% four times in four wordings and pushed the payoff past an 800px fold | Each projection sentence independently named the real rate "for honesty", and the W.2 basis paragraph was inserted ABOVE the payoff | "a rate stated once for the card is provenance; restating it in every sibling sentence is accretion that hides the line the reader came for — defer without a screen-position claim" | `tests/unit/fi-real-basis.test.ts` — "W.12 — the FI card names the real rate once"; `tests/e2e/phase3-coach.spec.ts` (painted order + no 4.50% outside the basis) |
| 2026-08-02 | `tsx scripts/ledger.ts reindex` regenerated docs/DECISIONS_INDEX.md with 329 rows where 375 belong, silently dropping every `## #n` heading-era decision — and the header it writes prescribes that exact command, so a Cursor agent ran it in good faith during #384 and deleted #374–#382. STATUS carried a "do not run this" warning instead of a fix; 34 decisions (#338–#373) were still missing when this session measured. | The parser only ever matched the legacy `\| n \| phase \| … \|` table row. Decisions moved to `## #n — title` sections at #338 and the parser was never taught the new shape, so the shortfall was written out as if it were the answer. Same blindness in `nextDecisionNumber`, which would have returned 338 — a number already used — on the next `ledger.ts decision`. | Fail loudly or not at all (LOOP_ENGINEERING 6): a regenerate that cannot understand its own input wrote a smaller file rather than stopping. | `tests/unit/ledger-decisions-index.test.ts` — 16 tests; the completeness assertion against the real committed files failed before the fix and named all 34. Guard mutation-proven: with the heading parser reverted to blind, `reindex` refuses, names all 46, exits 1, index byte-identical. |
| 2026-08-02 | The /trends and dashboard pace card projected a month nothing had been counted in: with no rows yet delivered for the in-progress month, both surfaces printed "$0.00 projected by month end" and, in green, "on pace for $28,685.10 less than last month" — a favourable claim about a month with zero observations, reachable on the first days of every month and any time a feed is behind | `computePace` abstained on an AND (`spentSoFarCents === 0 && priorMonthCents === 0`), so last month's total — a fact about a different month — licensed a projection built by dividing zero by the days elapsed. Every existing pace test ran at `daysElapsed: 10`, so the whole reachable window (days 1–3, before anything posts) was unexercised | A zero is a claim and must name WHICH zero (`a-zero-is-a-claim-and-must-name-which-zero`): a true zero and a stale feed are indistinguishable here, so the surface may not extrapolate from either — and making an abstention newly common obliges you to re-read the copy that fires in its place (the card's old "Not enough activity yet to spot trends" contradicts the completed-month mover row rendered directly beneath it) | `computePace`'s single-condition guard + `PACE_NO_SPEND_YET` (one author, both surfaces); `tests/unit/trends.test.ts` — "pace abstains on a month with nothing counted (C.1)": days 1/2/3 with a $200 prior month, the false-abstention control (a day-1 charge still projects), and a month whose rows net to zero; `tests/unit/trends-labels.test.ts` — the sentence and the two-surface authorship scan; `tests/e2e/trends-pace-abstain.spec.ts` — both surfaces on a seeded prior-month-only user, asserting the mover row is present so the fixture cannot decay. Mutation-proven both ways: reverting the guard fails 4 unit locks and the e2e (through a rebuild) |
| 2026-08-02 | The pace projection ignored the bill calendar the app already owns: $578.79 counted over two days projected $8,971.25 for a month whose mortgage alone is ~$6,200, under a green "on pace for $19,713.85 less than last month" — and the same account read "$6,200.18 less, green" for four days and then "$32,239.82 more, red" the morning the mortgage posted (executed by a critic) | `computePace` read nothing but transactions and extrapolated one daily rate, while `snap.scheduled` was already on the very snapshot `getSpendingTrends` loads. A household month is a few large dated bills plus noise, so a uniform-stream model is biased in a KNOWN direction — low before the bills land, wildly high the day they do — and the tautological lock (`projected === round(spentSoFar / daysElapsed × dim)`) restated the implementation, so it passed for any model and would have failed the fix | A borrowed total imports its window, and a window has an EDGE (`a-borrowed-total-imports-its-window`, L.11(D)): a bill may only enter a figure on the basis that figure is compared against — hence the admission rule (ever counted as spend at that merchant) rather than a category guess — and bills are counted over the WHOLE month, never split at `today`, because a mortgage dated the 1st that has not posted yet is still to come | `computePace`'s three-part projection + `billsThisMonth` + `billOccurrencesInMonth` (sharing the one `monthsPerCadence` table); `tests/unit/trends-pace-bills.test.ts` — 16 known-answer cases incl. the owner's day-2 shape, the day-2/day-10 stability pair (15c apart where the old model swung $18,600), the Prime-inside-Amazon credit cap, three refusals, and the INTAKE test running transactions → `detectRecurring` → `toScheduledTransactions` → pace; `tests/unit/trends-labels.test.ts` golden sentences for all three assumption branches; `tests/e2e/trends-pace-bills.spec.ts` drives a stored `ScheduledTransaction` row to both surfaces. Six mutations each proven to kill a lock |
| 2026-08-02 | The pace assumption's branch B told the reader "The bills we can see for this month have already been charged" — the only branch that declares the projection finished, and the only one carrying no limitation at all — while the engine deliberately refuses scheduled rows it can plainly see (an aggregate "Zelle Payment" landlord, a hand-authored label like "Rent — Peachtree Properties", a transfer-paid obligation). /calendar renders those same refused rows as bills still due, one click away, off the same `snap.scheduled` array. Branch A's hedge was an ENUMERATION of two exclusions where at least five classes exist, so a reader whose rent is paid by Zelle read the list, found neither item applied, and concluded the rent was inside the figure | The copy was written from the fix's own success narrative rather than from the refusal set: #390 added the admission rule and described what it INCLUDED, then hedged by listing the two exclusions its author had in mind. An exclusion list beside a money figure is a claim to be complete, and it decays every time a refusal is added | Closing a gap shrinks the disclosure that described it (`closing-a-gap-shrinks-the-disclosure-that-described-it`): re-derive the remaining set by EXECUTING the rule over its whole input domain, never by editing the old sentence — and prefer a POSITIVE statement of the admission rule, which cannot go stale, over an enumeration of exclusions, which must be extended by hand forever | `PACE_BILL_COVERAGE`, authored once and carried by both branches that mention bills; `tests/unit/trends-labels.test.ts` — golden sentences for all three branches, "every branch that mentions bills also states what bills covers" (asserted in BOTH directions, so a hedge that never fires and one that fires unconditionally are different failures), and "does not enumerate exclusions"; `tests/e2e/trends-pace-bills.spec.ts` renders the clause on both surfaces. Mutation-proven: restoring the old branch-B sentence kills 3 locks |
| 2026-08-02 | The bill-admission set was filled BEFORE the `date > today` guard, so a transaction the app's own convention says has not happened yet could admit a bill into the projection — a $500 bill materializing from a row contributing $0 to the month total | One loop answered two questions and the date filter sat between them in the wrong place. `computeLargest` and `computeNewMerchants` in the same file both refuse to treat a future-dated row as fact; `billsThisMonth` was written after them and did not inherit the convention | A value may only enter a total on the basis that total is COMPARED against: a future-dated row is in neither `spentSoFarCents` nor `priorMonthCents`, so admitting a bill on its evidence imports a merchant whose money has never been in the basis (`a-borrowed-total-imports-its-window`). Aggregate-ness stays OUTSIDE the guard on purpose — it only ever refuses a bill, so learning it from more rows can only be more cautious | `tests/unit/trends-pace-bills.test.ts` — "a future-dated row does not admit a bill" plus the false-abstention control ("a past row at the same merchant still admits the bill"), without which "never admit anything" would pass. Mutation-proven: restoring the old line order kills the lock. NOTE: the first draft of this lock passed on the OLD code because its fixture reused a merchant whose prior-month charge admitted the bill legitimately — the fixture, not the guard, was doing the work |
| 2026-08-02 | `discretionarySoFarCents` was clamped with `Math.max(0, spentSoFar − credited)`, which silently deleted real unrelated spending from the daily rate: one net-refunded category took a genuine $30/day of dining to $0/day and collapsed the rest of the month to nothing, reporting the reader as on pace to spend LESS | `spentSoFarCents` nets refunds by CATEGORY and drops a net-refunded category to zero; the bill credit is summed per MERCHANT and is untouched by that netting, so the two bases can cross. The clamp absorbed the crossing instead of detecting it, and the comment beside it conceded the crossing was possible while no test exercised it | Ask the failure-direction question per ROLE (`failure-direction-is-per-role-not-per-value`): under-projecting is this surface's dangerous direction, because "on pace to spend LESS than last month" is the reading that makes someone relax. A crossing is DETECTABLE, so it is handled — when the credit cannot be trusted against this basis, take no credit at all, which leaves bill money inside the rate and errs toward tightening | `computePace`'s `basesCrossed` branch; `tests/unit/trends-pace-bills.test.ts` — "a net-refunded category cannot delete unrelated spending from the rate". Mutation-proven: restoring `Math.max(0, …)` fails the lock with discretionary 0 where $30.00 belongs. Residual pinned as C.20 — the two bases are still not unified, only prevented from crossing destructively |
| 2026-08-02 | The dashboard hero could instruct the reader to move money out of a FROZEN account: "Transfer $8,500.00 from Rainy Day Reserve ($50,000.00 available) by Wed, Jun 24" — rendered verbatim by the mutation run — where that balance had not updated since the bank stopped sharing the account on May 20 | `dashboard/page.tsx` derived its own funding source with `type === 'SAVINGS'` sorted by balance, applying NONE of the four guards `radar.ts` applies to the SAME account array on the SAME page (not the payment account, checking/savings only, positive balance, not frozen). A stale balance reads HIGH, so the frozen account sorted FIRST and was preferentially chosen | A fence copied per call site will miss call sites (`fence-by-construction-not-per-call-site`): the eligible-source rule now has one home, `eligibleTransferSources`, and both surfaces obtain it there. `sufficient` deliberately stays at the radar call site, because it is a claim about that engine's own amount and the hero prints a different engine's figure — sharing the account CHOICE is sound, sharing a sufficiency verdict computed against another surface's number is not | `tests/unit/transfer-source-selection.test.ts` — 9 locks, each guard given a fixture where the excluded account would WIN the sort; `tests/e2e/transfer-source-frozen.spec.ts` — a frozen account holding 10× the live one, asserting the feed-dropped banner names it FIRST so the absence assertion cannot pass vacuously. Mutation-proven through a full rebuild: restoring the local derivation renders the defect and fails the spec |
| 2026-08-02 | Owner-reported: "First of all the monthly income is wrong." /spending-plan printed a monthly income of **$10,681.30** where the median of his three real complete months is **$30,937.91** (May $30,937.91 / Jun $21,117.48 / Jul $31,408.61) — a 3× under-report of the figure every other term on the page is derived from. Both numbers are real money that really landed in his account, and the wrong one is a perfectly plausible paycheck, which is why four sessions of looking at the page never flagged it | His Schwab checking was re-linked through Plaid on 2026-07-21 and correctly recorded as a reconciliation PREDECESSOR of the new row, which is his payment account. `applyReconciliationBoundary` decides which side OWNS a date; it does NOT re-key transactions, so every pre-cutover paycheck keeps the predecessor's `accountId` — while the income scope is built from the live payment-account id. May, June and everything before 07-21 fell outside the scope, leaving ONE partial month, so the "median of up to 3 complete months" was a median of one | Excluding a ghost is only half a boundary (`three-sessions-of-hypothesis-one-query-of-evidence`, L.26): a row that survives the boundary on a superseded id is not a ghost to drop but a live fact to re-key onto whatever carries the money now. The two sibling paths in the same file already did exactly that — the boundary re-keys `snap.scheduled` itself (F6, whose comment says "so the successor's payment-account filter finds them") and `countedExpenseSeriesForPlan` remaps detected series — so the defect was one scoped path that never inherited a rule its neighbours both state out loud | `getSpendingPlan` reads every row's account through `activeTerminalSuccessorMap` before testing the scope; `tests/unit/spending-plan-income-reconciled.test.ts` — three locks driven through the REAL server path on a throwaway user with a live link (all three trailing months present; July counting BOTH feeds exactly once at $14,000 so a double-count is a different failure from a drop; and the wrong value named explicitly so a regression cannot pass by moving a number). Mutation-proven: reverting the filter yields `[400000]` — a single part-month, the production defect in miniature — and kills all three. Measured first, not guessed: `scripts/audit-probes/income-replay.mts` replays the consumer's whole scope clause by clause against production, read-only. SIBLING FOUND, DELIBERATELY NOT FIXED: `radar.ts:145` narrows POSTED rows the same structural way (`t.accountId === payment.id`, 183 rows where the re-keyed set is 402) to detect the committed merchants it excludes from discretionary burn — but the naive remap is NOT the fix, and measuring said so: `detectRecurring` finds **9** series as shipped and **4** with the re-key, because merging two feeds' descriptors for one merchant breaks the gap regularity each feed had alone. Applying the income fix here by reflex would have moved five merchants' spend INTO discretionary burn. The direction I assumed before executing was wrong, which is the whole argument for the probe; queued as C.22 needing a descriptor-level merge, not an id remap. It is also a synchronous function with no `userId`, so any remap needs the map on the snapshot |
| 2026-08-02 | The Plan Fixed union ADDED $296.40/mo the category rollup had already counted, on the owner's live account: a Principal life-insurance draft ($146.40, rows filed `life-insurance`) and a Zelle house cleaner ($150.00, rows filed `home-services`) each entered once via the rollup under their FILED id and again via `recurringOutsideFixedCategoryCents` as a series carrying the merchant normalizer's `uncategorized` guess — under copy that explicitly denies double-counting ("recurring bills that are not already in that rollup") | The union dedupes by category id but its two halves identify a category by DIFFERENT FIELDS: the rollup keys on the filed `Transaction.categoryId`, the series on `normalizeMerchant(rawDescriptor).categoryId` — the static guess — so any payee the merchant table doesn't know can never match its own rollup row. The existing lock passed the FILED id into the series and so could not fail on production's shape (audit P0-4) | Sharing an engine is not sharing a basis (`a-link-on-a-figure-asserts-two-engines-agree`): a dedupe between two sets must key on the field BOTH sets carry, and the series' category for dedupe purposes is where its rows' money actually lives in the rollup — the filed id. Measured before fixing (C.0/#393, `three-sessions-of-hypothesis-one-query-of-evidence`); the task row's "null → skip" was NOT shipped because after row-resolution a null id means genuinely-unfiled rows, which are in NO rollup category, so adding them cannot double-count and skipping them would open a NEW under-count (DECISIONS #393) | `filedCategoryByMerchant` (engine — outflow CENTS, rollup-window rows first, all-time fallback, tie → most recent; aggregates and reader-excluded rows never vote) + `countedExpenseSeriesForPlan` resolves through it with a guard that a remap may never ENTER `PLAN_FIXED_NEVER` (critic cycle 1: an all-time row-count modal reopened the double-count via stale filings, mixed aggregate payees, and could drop a filed-as-card-payment auto-loan from Fixed — each fixed and mutation-locked). `tests/unit/spending-plan-server.test.ts` → `test_regression__guess_id_series_over_filed_rows_is_not_added_twice` through the REAL server path (series presence asserted), the deliberate null-unions-in direction, and `test_regression__remap_may_not_enter_the_settlement_never_set`; `tests/unit/fixed-category-amounts.test.ts` per-rule locks. Mutation-proven ×5, each killing exactly its own lock; reverting the wiring line yields 29,280 where 14,640 belongs — the production defect in miniature |
| 2026-08-02 | "Typical spend" for a Fixed category divided by the constant window length, so a monthly bill whose first charge landed mid-window printed a third of itself: the owner's Mathnasium `education` line read $197.67 where the bill is $593.00/mo — Fixed short, guilt-free over-generous, the dangerous direction (audit P0-5) | `averageMonthlySpendByCategory`'s `denom` was always `monthSet.size` with no notion of when the category started existing; the audit's sketched fix (divide by months WITH a charge) was measured wrong in the OTHER direction before shipping — it would have tripled an established quarterly premium (`auto-insurance` $1,553 × 1 in window → $1,553/mo where $517.67 is the honest smoothed reserve) | A zero month is two different facts: "the bill existed and did not charge" (an established long-cadence bill — divide through it, that IS the smoothing) vs "the category did not exist yet" (divide by its age). The divisor is the months the category could have been OBSERVED: `min(windowMonths, window months ≥ first-ever counted outflow month)`; refunds do not start the clock; the label states the method and window because a bare "(typical)" beside money is unauditable (P1-8) | `tests/unit/fixed-category-amounts.test.ts` → `test_regression__typical_divides_by_observable_months_not_window`, the two-month case, the refund-clock + refund-dilution case (critic cycle 1: the numerator must share the divisor's basis or a pre-first-charge refund prints a $100/mo bill as $50), `test_regression__established_long_cadence_keeps_window_smoothing` (pins the deliberate residual), and `fixedAmountBasisClause` goldens (one sentence, no "first charge" claim — the register can show charges the flow sums exclude). Mutation-proven: restoring `denom = months` kills exactly the three observable-months locks and spares the pin; summing the full window kills the dilution lock. Rendered clause locked in `spend-class.spec.ts` |
| 2026-08-03 | The owner's $6,217.07/mo mortgage was invisible to the Plan's Fixed figure: the rollup printed "rent $2,072.36" (one counted payment ÷ 3) and no recurring series existed, so Fixed ran ~$4,145/mo under — the dangerous direction, guilt-free over-generous | The transfer pair-detector flagged the checking outflow only in months the linked MORTGAGE account's inflow settled ≤3 days out (per-month timing luck); flagged rows leave every flow sum, and detection dropped them too (`detect.ts` kept transfers only for `auto-loan`), so the two unflagged rows 91 days apart made no series — and the union's category-level covered-skip read the partial "rent" mass as "covered" | A per-row flag cannot carry a per-merchant class (`a-zero-is-a-claim-and-must-name-which-zero` family: one payee was two classes of row depending on settlement luck), and a dedupe between two sets must key on what BOTH sets can say — the covered-skip cannot express "covered PARTIALLY" | `loanPaymentMerchantCanonicals` (structural: flagged cash outflow + same-\|amount\| ±3-day pair on a LOAN/MORTGAGE account; per-merchant; aggregates refused); detection keeps flagged rows (auto-loan precedent); rollup/median exclusions read the UNIONED set only (excluded ⇔ unioned, so a merchant detection can't series keeps its counted rows); the union adds the series at full monthly rate except the NEVER set and reader-priced categories. Locks: `tests/unit/loan-payment-fixed-union.test.ts` (21 — controls pin the fail-old shape) + `tests/unit/spending-plan-server.test.ts` real-server locks `test_regression__transfer_flagged_loan_payment_counts_once_at_full_rate` (integration mutation-proven: pre-loan-side-query run printed exactly the fail-old 221_876), `test_regression__reader_priced_category_is_not_double_priced`, `test_regression__excluded_only_when_unioned__detection_failure_keeps_partial_coverage`. Production replay (mortgage-replay.mts): Fixed $9,785.24 → $13,929.95, trace MATCH |
| 2026-08-03 | Owner-reported: "changing a field in activity (transactions) … completely refresh page and bring me to the top. It's very annoying when I'm trying to log many at a time." Measured at 380×800 on the pre-fix build: a reader 4,451px down the register landed at 114px after excluding one row, and 5,511px down landed at the same 114px after re-filing a category — every inline edit cost the reader their place, so filing a screenful of transactions meant re-scrolling after each one | Every register mutation confirms itself with `window.location.reload()` — deliberately, since `router.refresh()`'s application was a coin-flip at human pacing (#164/#166/#167). The browser normally restores scroll across a reload and could not here: `(app)/loading.tsx` paints a ~600px skeleton first, and a restore into a document shorter than the target offset clamps to the top. Nothing in the app asked for the top; the skeleton took the position away | The confirmation reload is load-bearing and stays — the defect was never the reload but the place it discarded. A fence copied per call site will miss call sites (`fence-by-construction-not-per-call-site`), so the save is bundled INTO the reload (`reloadPreservingScroll`) across all 15 call sites in three components rather than left as a step each mutation remembers; a mutation added later inherits it by calling the same reload. Every refusal in the restore decision fails toward NOT restoring, which is exactly the old behaviour, because a wrong restore is a jump the reader never asked for | `src/components/finance/register-scroll.tsx` (browser glue) + `register-scroll-store.ts` (the decision, kept `window`-free so the node suite executes every refusal — a guard nothing executes is only a claim). `tests/unit/register-scroll-store.test.ts` — 7 locks, 6 of them refusals (TTL expiry and its honoured boundary, backwards clock, three view mismatches, malformed/`null`/bare-number values, top-of-page and non-numeric offsets). `tests/e2e/register-scroll.spec.ts` — 40 rows over 40 date headers, scroll to row 20/25, edit, assert the reader lands within 150px of where they were. Mutation-proven through two full rebuilds: on stashed `src/` both specs fail with the production numbers (4451→114, 5511→114). The spec's own first version passed in 1.2s on broken code by reading `scrollY` off the pre-reload document — it now stamps the document and waits for the stamp's absence, so the measurement cannot precede the reload |
| 2026-08-03 | Owner: "What does Not counted mean here? It's not clear" on an `Interest Paid +$0.10` row — the second rename of this chip ("Neither" → "Not counted") and the second time it was asked about | Ten distinct facts (income, transfer, card payment, cash, investment, split parent, excluded, unsettled, unfiled, non-budgetable) all rendered one label, and the only explanation was a `title` attribute that cannot open on touch | A label that can mean N things names WHICH one; a disclosure a phone cannot reach is not a disclosure | unit `spend-class` (reason per fact, no label in `PROVENANCE_LABELS`, chip ≠ "Not counted"/"Neither"), unit `spend-class-panel-offset`, e2e `txn-spend-class` "says why, by tap" |
| 2026-08-04 | The /calendar frozen disclosure told a statement card's LATER-month figure it was "worked out from the last balance we saw" — false by the whole gap between the balance and the statement basis printed beside it, and flipping the disclosed provenance month-by-month for one account | C.8's synthesized future cycles reused the boolean `isEstimated`, and `frozenCardsNote` keyed its amount sentence off it; but `isEstimated` conflates "no statement (balance-derived)" with "synthesized repeat of a statement" | "a boolean that two facts share is a lie about one of them — carry the fact you branch on, and compute it where it is known" | `DueAmountSource` (`'statement' \| 'repeated-statement' \| 'balance' \| 'loan-terms'`) on `CalendarEvent.amountSource`, set by the engine at each push, mapped verbatim into `FrozenCalendarRow`, branched in `frozenCardsNote`/`frozenCardDatesNote`; `tests/unit/frozen-figure-surfaces.test.ts` — "C.8 critic F-1 — the frozen amount claim names the field the figure came from" (3 branches) + the September real-engine lock asserting the repeat sentence AND the absence of the balance sentence |
| 2026-08-04 | The /budgets "Fixed expenses" / "Guilt-free" headings are register links, and after a confirmed provider migration the total they promised counted the predecessor's copy of every post-cutover purchase twice — the destination showed each one once (audit P1-27; the audit's own stated cause, a POSTED-only classifier, had already been fixed by #397 and was verified dead before anything was changed) | `getTransactions` applies the shared R1 reconciliation ownership rule before it stamps `spendClass`; `/budgets` applied the same keep to `spendRows` and handed the Fixed/Discretionary panel the RAW month query | A link is a claim that two engines agree (`a-link-on-a-figure-asserts-two-engines-agree`), and a rule applied at one call site out of two is not a shared basis — so the keep became a REQUIRED parameter of `summarizeSpendClassCategories` rather than a filter the caller remembers. The PREDICATE is passed, not pre-filtered rows: `spendRows` has `isSpendRow` run over it, which drops the C.25 loan-payment exclusions the register still lists, so reusing that array would have traded one mismatch for another | `tests/unit/spend-class-link-parity.test.ts` — both sides over one fixture exercising both directions of R1 (successor backfill inside the predecessor's claim window; predecessor row still reporting past the cutover), plus PENDING, a per-row override, a refund, an excluded row and a settlement category. The CONTROL executes the old behaviour: heading $3,880.00 against a destination summing $1,960.00 |
| 2026-08-04 | /budgets printed one category at TWO figures four inches apart — Housing $1,800.00 under "By category" and $4,200.00 under "Fixed expenses" — with the sentence explaining the gap rendered inside the lower card only (found by a fresh-context critic on the fix above, executed against a replayed fixture) | C.25 (#403) takes a loan payment carried on its loan out of the category figures so those still sum to their own register link; the Fixed/Discretionary split does not apply that exclusion, because ITS link goes to a register filtered by class which still lists the payment. Both figures are right for their own destination — nothing on screen said so | A disclosure written for one figure is false beside another (`a-disclosure-written-for-a-page-is-false-in-an-email`): the sentence must name the DIRECTION on the list it sits under, because "not counted" printed above a figure that counts it is worse than silence | `spendClassLoanPaymentNote` (pure, engine) + `tests/unit/spend-class-link-parity.test.ts` → the P1-1 describe block: payee, amount, loan name, "ARE counted here", "By category below", and "differ by that amount" |
| 2026-08-05 | The dashboard printed "$X this month" (top-spending card) an inch from "$Y spent this month" (pace card) over two different windows, and Ask answered the same month two ways — `merchant_spend` stopped at today, `spend_total` / `spend_by_category` / `top_categories` did not (audit P1-28) | `computePace` filters rows to `t.date <= today`; `spendingByCategory` windowed by month key only, so a manually entered or imported future-dated row counted in one figure and not its neighbour | A figure's WINDOW is a value it carries, not a month key two authors expand separately — the fix for the same defect one session earlier (a clamp at the figure) shipped a clamped figure linking to an unclamped register, $120.00 clicked landing on $520.00, which is a worse divergence than the one being removed (`check-what-the-fix-breaks-before-what-it-fixes`) | `tests/unit/spend-window-parity.test.ts` — the REAL loaders (`getReports`, `askAssistant` through the real parser) on a throwaway user holding a future-dated row, the register link executed through `filterTransactions`, and a CONTROL landing the $520.00. Mutation-proven: reports window → 5 red, income filter → 1 red, all four Ask clamps → 6 red |
