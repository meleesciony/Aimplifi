# REGRESSION_LEDGER.md

Per LOOP_ENGINEERING.md: every fix ships a regression test plus one line here
(`date | symptom | root cause | rule broken | locking test`). Append-only. Earlier
phase regressions live as named tests (tests/unit/critic*.test.ts) + docs/DECISIONS.md;
this ledger starts at the 2026-06-21 hardening session.

> Rows from 2026-06/2026-07 were moved verbatim to
> `docs/archive/REGRESSION_LEDGER_ARCHIVE_2026-06_to_2026-07.md` on 2026-08-04.
> Append new rows here as before.

| date | symptom | root cause | rule broken | locking test |
|---|---|---|---|---|
| 2026-08-20 | O.20j: transfer-category rows (Venmo / AUTOMATIC PAYMENT / brokerage sweeps) stayed `isTransfer=false` so register vs spend gates disagreed and new siblings kept arriving unflagged | `detectTransfers` evidence was only normalizer-transfer descriptors OR ±3-day opposite-amount pairs; those descriptors normalize to uncategorized and often have no unique opposite leg | a guard must read what it guards — the filed `transfer` leaf is already the verdict; treat it as descriptor-equivalent evidence (one direction; never clear spend-category converse leak) | `tests/unit/transfer-pair-filing.test.ts` `test_regression__o20j_filed_transfer_category_flags_without_pair_or_descriptor`; `test_regression__o20j_automatic_payment_and_brokerage_sweep_filed_transfer_flag`; `test_regression__o20j_converse_leak_spend_category_not_touched_by_filed_leaf_rule` |
| 2026-08-20 | O.20j R6: largest "Overdraft Transfer from Brokerage -7383" ($7,792.97) filed Fees & Charges with isTransfer=false and still counted as spend after the #485 transfer-leaf gate | GENERIC `\bOVERDRAFT\b` → fees beat any transfer rule; isTransfer for this family came only from pair matching, and the unpaired largest amount had no opposite leg | a fence copied per call site will miss call sites — fix the normalizer the detector and categorize already share, not a third spend predicate | `tests/unit/insights.test.ts` `test_regression__o20j_r6_overdraft_transfer_from_brokerage_is_not_fees_spend`; `tests/unit/normalize.test.ts` `test_regression__o20j_r6_overdraft_transfer_beats_fees_keyword`; `tests/unit/transfer-pair-filing.test.ts` `test_regression__o20j_r6_unpaired_overdraft_transfer_descriptor_overturns_fees` |
| 2026-08-20 | O.20j: rows filed `categoryId=transfer` with `isTransfer=false` still inflated /reports chart, /coach savings rate + Money Review, and Ask income/expense (up to $10,051.27 in a measured month; 76 live rows) | `countsInFlows` keyed only off `isTransfer` + POSTED + split/exclude; `isSpendRow` already dropped the transfer leaf | a fence copied per call site will miss call sites — share the category gate on the flow predicate the chart/coach/Ask already share | `tests/unit/insights.test.ts` `test_regression__o20j_transfer_category_unflagged_does_not_count_in_flows`; `tests/unit/month-flow-breakdown.test.ts` `test_regression__o20j_transfer_category_unflagged_stays_out_of_month_flow_panels` |
| 2026-08-17 | W.4: "if I want to save up to 10 mil what do I need to do?" did not route; `parseTargetAmount` missed `mil` and "ten million", and a dateless save+amount fell through | W.1 shipped the engine and card, not the Ask path; `savings_goal_by_date` requires a date | a context-carrying parse is judged by what it abstains on; a compound "twenty five million" must not become $5M | `tests/unit/assistant-wealth-target.test.ts` `test_regression__w4_owner_question_routes_to_wealth_target` |
| 2026-08-17 | W.8: seven COACH_COPY keys sat in `KNOWN_UNSCANNED`, so the shame / assumption / ticker sweeps never saw their strings (four of them were already composed under a `nextAction:` label the completeness check could not count) | `ALL_STRINGS` labels are matched on `split(':')[0]`; the pin was allowed to hold the gap | a hand-maintained scan is only as good as the check that it is everything; the pin may only shrink | `tests/unit/coach-copy.test.ts` `test_regression__w8_every_coach_copy_key_is_scanned` |
| 2026-08-16 | O.17a: a category rename detached the money-dial marker, and coach cut proposals compared stored names to `categoryId` so a dial was never protected | `User.moneyDials` was free-text names matched at each reader; the write path was a textarea | a new meaning for an old column is a migration; key the preference by id, resolve leftover names only when unique | `tests/unit/o17a-money-dial-ids.test.ts` `test_regression__o17a_*` |
| 2026-08-16 | G.2: `.mts` audit probes were invisible to `tsc --noEmit`, so a wrong call could ship as a recorded dollar figure (O.20g keep-object; income-replay `.filter(countsInFlows)` bound the index as `excludedFlowIds`) | `tsconfig.json` include is `**/*.ts`; `.mts` never entered the compile set | a probe the compiler cannot see is not evidence | `tests/unit/g2-probes-compile-set.test.ts` `test_regression__g2_*` |
| 2026-08-15 | C.22: radar committed-merchant detection read only the re-linked payment account's short tail (183 vs 402); the income remap concatenated both feeds and took 9 series to 4 | `detectRecurring` groups by canonical and infers cadence from gaps; two feeds of one merchant become one irregular/multi-amount group | a sum remap is not a detection remap; detect each source, then union | `tests/unit/radar-committed.test.ts` `test_regression__c22_*` |
| 2026-08-15 | H.9: a mortgage/loan row showed a balance and no payment history; clicking it could not answer “what did I pay?” | payments post against checking/savings/cards; the register excludes LOAN/MORTGAGE; no reader-chosen payee link existed | an empty set is not a fact about money; never infer a payee from name similarity; the panel must use the register’s own merchant axis | `tests/unit/loan-payment-history.test.ts`; `account-detail-panel.test.tsx` H.9 ask / linked-empty / list / checking hidden; `account-payment-merchant-actions.test.ts` set/clear/ownership/type/demo/manual-no-merchantId; e2e `no-dead-ends` H.9 |
| 2026-08-15 | C.21: pace assumption branch C printed the same sentence for "no bills on the calendar" and "every calendar bill was refused", so the least-complete projection looked like a complete one | `paceAssumption` had no refused-count; `billsThisMonth` discarded the `!counted` / aggregate continues | a zero is a claim and must name which zero; the engine must be able to tell the two zeros apart before copy can | `tests/unit/trends-labels.test.ts` C.21: refused preamble + shared daily-rate tail vs empty calendar; 1 ≡ 3; no N / so / this month / as bills. `trends-pace-bills.test.ts`: counts + Zelle/demo compose + mixed stays A. e2e `trends.spec.ts` demo both surfaces |
| 2026-08-15 | U.14: the last-4 name-signal veto was inert for SimpleFIN (no mask column), so "lee" / "plan" proposed Combine for accounts that are not the same; the 2026-08-12 widening hid the genuine Roth 396/5351 offer | `masksDiffer` required both mask columns; SimpleFIN never fills one. Reading every advertised 2+ digit group treated Schwab's 3-digit id as a last-4 | a last-4 is 4 digits, not every number a feed prints; a year is not a last-4; a 2–3 digit SimpleFIN id is an absence | `tests/unit/account-duplicates.test.ts` U.14: LEE 4034 vs 4927 hidden; `(2021)` still flags; 396 vs 5351 and 529 …-01 still name-match. `account-reconciliation-candidates.test.ts` U.14: no LEE Combine; Roth still offered; 4-digit conflict does not promote a wrong sibling; no-last-4 pair stays ambiguous. e2e `reconcile.spec.ts` U.14 |
| 2026-08-15 | U.17: a dormant predecessor's last-used day was filed as a false "handover day" that should stop being released, or whose claim should extend to the cutover; copy then located a connection-change on that day | measured 0 dormant / 0 dragged of 25 effective links (16 coincident); both prescribed money fixes reintroduce a silent loss (A = U.13, B = F4 inverted); "changing connections" is false when last ≠ cutover | last-used stays released; copy locates the keep-rule, never a cause; Combined accounts prints no date; Combine's exception is a separate sentence | `tests/unit/reconcile-boundary.test.ts` U.17: 2025-03-15 / 2026-07-21 keeps both −$1,200.00, unique succ −$25.00, gap; `u16-handover-disclosure.test.ts` U.17 locator + refuses changeover; e2e `handover-day-disclosure` 7/7 |
| 2026-08-15 | U.7: a collision winner's recorded class was filed as a defect that should prefer the successor or drop the date | measured 0 class-disagreeing collisions; all 55 live snapshots still NULL, so both sides fall back to today's type which an effective link already matches; the prescribed fixes invert U.6 or understate a U.4 bucket | the class rides with the winning observation, the same way the cents do — do not re-sign by the successor or refuse the date | `tests/unit/reconcile-boundary.test.ts` U.7: CHECKING-recorded covering winner `+$5,000.00`; genuine CREDIT over CHECKING echo `−$4,800.00`; NULL+NULL both CREDIT stay liability either winner |
| 2026-08-15 | U.8: the account detail panel never rendered for CHECKING / SAVINGS / CREDIT, so a reclassified spending account's per-row explanation was unreachable | `accountRowDestination` sends those types to the register and the panel rendered only for `dest.kind === 'detail'` | spending rows keep the register as the primary click; a sibling Details control opens the same panel; the intro must not claim a spending account has no activity feed | `tests/unit/balance-history-view.test.ts` U.8: mortgage sentence byte-identical, checking/credit refuse "instead of an activity feed"; `account-detail-panel.test.tsx` checking render; e2e `no-dead-ends` U.8 sibling open/close + destinations (checking href, no Brokerage Details) |
| 2026-08-15 | U.10: a snapshot dated today was marked "counted" while `netWorthSeries` overwrites that bucket with live balances, so the panel named a figure the chart's today point does not use | the U.5 `countsInNetWorth` flag is the boundary verdict (account is in today's net worth) and was read as "this recording is the chart figure"; the live overwrite is intentional so today matches the headline | a recording dated today is not the live point — mark it with its own reason, never the combine note; do not make live yield; copy must not claim discarded dollars when cents match or promise "tomorrow" while today is pinned | `tests/unit/account-detail-reconciled.test.ts` U.10: today-row `replacedByLive` + chart constituent `−$1,500.00` not `−$1,000.00`; dropped today-row stays combine; `balance-history-view.test.ts` note has no "not from this recording" / "Tomorrow"; e2e Auto Loan panel `account-detail-replaced-by-live`; PDF heading `Trend` |
| 2026-08-14 | U.2: status and brand chrome were painted with raw `emerald-*` / `amber-*` classes, so a later palette change would leave stray hue literals | two semantic roles (brand chrome vs positive/warning status) shared one hue name; no token layer for classes (chart hexes were already unified) | a hue name is not a role — call sites use `brand` / `positive` / `warning`; values alias Tailwind emerald/amber so pixels do not move | `tests/unit/u2-semantic-color-tokens.test.ts`: no `emerald-N`/`amber-N` under `src/` except the token file; token file aliases brand/positive → emerald and warning → amber |
| 2026-08-14 | U.37 (opened by the U.12 critic): a covering predecessor's monthly echo still beat the live successor's real reading, and a later-cutover echo still beat an earlier-cutover genuine reading once both windows had closed (terminal had no row) | U.12 compared genuineness only after both sides shared covering-tier 0; the terminal is always tier 1 and closed-tier ranking stayed cutover-only | a repeat is not an observation — genuineness outranks tier; both-genuine falls through to U.9 so covering still beats terminal when both sides read | `tests/unit/reconcile-boundary.test.ts` U.37 block: covering echo pred loses to genuine succ / `netWorthCents === 500_000`; both-genuine covering pred still wins; closed genuine beats later-cutover echo; covering echo loses to closed genuine; terminal echo loses to closed genuine; equal-cutover genuine ancestor outranks echo mid; CREDIT −$5,000.00; lone echo kept |
| 2026-08-14 | U.12 (opened by the U.9 money critic): a quiet feed's carried-forward monthly echo could outrank another record's genuine balance for the same date — e.g. s1 dropped 2026-01-15 / cutover 2026-02-28 vs s2 live through June / cutover 2026-06-30, collision on 2026-01-31 counted s1's $4,000.00 repeat | U.9 ranked covering collisions by earliest cutover only; U.4 writes a snapshot every month including quiet feeds; `feedDroppedAt` travelled with the account and the ranker ignored it | a ranking that claims the tightest ownership window must not prefer a repeated last-reading over a real observation in the same covering tier; required `feedDroppedAt` on the snapshot-collision input (U.33); same predicate as the panel (`date > dropped`) | `tests/unit/reconcile-boundary.test.ts` U.12 block: named defect s2 / `netWorthCents === 500_000` (order-independent); both-genuine still s1; drop-date is a reading; lone echo kept; both-echoes fall to earliest cutover; equal-cutover; quiet ancestor in a chain; CREDIT sibling −$5,000.00 |
| 2026-08-14 | U.36 (opened by the U.34 critic): five composed Ask intents (`safe_to_spend` / `debt_free_by_date` / `savings_goal_by_date` / `retire_at_age` / `savings_rate`) fetched `getReconciliationBoundary` in `composeAnswer` and then paid it again inside `getSpendingPlan` or a second snapshot inside `getCoachData` — a discarded extra read of the same table | #466 fetched the composer boundary for every intent so a second spend case could not reintroduce a desync; the five delegates never use those views | a fetch the answer does not read is waste, not a shared snapshot; skip the unused composer boundary for the named delegates; keep the eager fetch for every kind that uses a view; do not grow an optional fallback (U.33) or put `terminalOf` on the snapshot (would re-litigate #466's account_balance fold) | `tests/unit/reconciliation-boundary-shared-read.test.ts` U.36 block: `safe_to_spend` findMany === 3, `savings_rate` === 2, `retire_at_age` === 4, `debt_free_by_date` === 4, `savings_goal_by_date` === 3, each with `kind` asserted; U.34 `spend_total` stays at 2 |
| 2026-08-14 | U.35 (opened by the U.34 critic): `/reports`, `/trends`, and `/coach` each already held a snapshot that had read the reconciliation link table for the keep, then fetched `getReconciliationHandoverKeys` independently — so a confirm/undo between those reads could disclose a changeover day the keep had not released, or stay silent on a day it had | #466 parked passing a pre-fetched boundary *into* `getFinanceSnapshot` as a provider-shaped change, and left the three pages on the two-read shape | keep and disclosure of one page must come from one link-table snapshot; emit the views the assembler already paid for rather than changing the provider to accept a boundary (an optional-in parameter is how a second read survives — U.33); an exported function with no caller is a false claim (deleted `getReconciliationHandoverKeys`) | `tests/unit/reconciliation-boundary-shared-read.test.ts` U.35 block: `getReports` / `getSpendingTrends` / `getCoachData` `findMany` count === 1 + `getReports` `breakdown.totalCents === 5300` AND `countedOnHandoverDays > 0` (critic P2-4: a June-only figure lock could go green without the keys); U.31 block: snapshot keys === boundary keys on the CREDIT-pair fixture; `tests/unit/reconcile-boundary.test.ts` assembler keys === `pred\|2026-06-30` + `succ\|2026-06-30` |
| 2026-08-14 | U.34 (opened by the U.33 critic): `getSpendingPlan` read the reconciliation link table twice for one rendered plan (income scope, then expense scope, with `detectRecurring` between them), and `askAssistant` fetched handover keys in four spend cases plus the links again for account_balance plus the keys a fifth time for the Glass-Box trace — so a confirm/undo landing between awaits could scope income against one set of links and expenses against another, or disclose a changeover day the trace did not mark | U.33 consolidated the persisted artifacts and stopped at the rendered leftovers; each call site independently asked for one view | a consolidation is finished when the READ COUNT for one artifact is one extra beyond the snapshot, not when the functions the row named have been converted; required parameters, not optional fallback fetches (U.33); an exported function with no caller is a false claim (deleted `activeTerminalSuccessorMap`) | `tests/unit/reconciliation-boundary-shared-read.test.ts` U.34 block: `getSpendingPlan` `findMany` count === 2 + numeric `leftToSpendCents`; `askAssistant` spend_total count === 2 + `headlineCents === 4200` |
| 2026-08-08 | A CSV import file containing the same line twice (two overlapping exports pasted together — the guides' own chunked-download flow) imported BOTH copies silently: a double-import corrupting every total | `planCsvDedupe`'s multiset difference is exactly right for store-vs-file, but a file-internal repeat with no stored row imported twice with zero disclosure — the multiset cannot know which line is the corrupt one | A double-import must be SURFACED, never silent: warn with the count, never block — the key cannot tell a genuine twin charge from a paste artifact | `repeatedRows` tests in `tests/unit/csv-import.test.ts` (pasted-overlap → 2, re-import → 0, file 3/store 1 → 2, sign-part-of-key, mixed-file); server flow-through + audit meta in `tests/unit/csv-import-dedupe.test.ts`; e2e `transactions.spec.ts:637` (warning renders on the doubled import, absent on the all-duplicate re-import) |
| 2026-08-03 | Pending Hair Capital / Whole Foods showed "Not counted" with no Fixed/Discretionary dial — owner could not change class on the row he was looking at; pending sat under their authorization date among cleared rows instead of at the top like Mint/Simplifi | `classifySpendClass` reused `countsInFlows` (POSTED-only); register sort was date-only | Settlement status is not a spend-class axis (dial works on pending); pending pin to the top of Activity until cleared | `test_regression__pending_categorized_outflow_keeps_the_dial` in `tests/unit/spend-class.test.ts`; `test_regression__pending_stays_at_top_until_cleared` in `tests/unit/transactions-query.test.ts` |
| 2026-08-03 | Rules Return from Activity dumped the owner onto one prefilled transaction (Hair Capital) after he had been reviewing the list | `?from=` alone was treated as "he was on that row"; unfiltered Activity attached no `?back=` | Prefill ≠ place: always attach `?back=` (sentinel `_activity` when unfiltered); `via=row` only when he left detail — then primary Return is that row AND Activity is always offered | `tests/unit/register-return.test.ts` Activity sentinel; e2e `register-return` unfiltered→Activity + detail `via=row`→transaction + Activity |
| 2026-08-03 | Rules page had no Fixed/Discretionary THEN action and no reliable Return — owner lost his place in Activity after making a rule; wanted algorithmic guess (recurring→Fixed) with extra same-month charges left unstamped (utilities vary in amount) | Keyword rules stamped category/tax/rename only; return link was absent unless `?back=` carried a narrowed register view | Rule THEN actions that write money-adjacent state must preview then apply on one basis; extras in a billing period take no override (keep guessing); leaving Activity for Rules always offers Return to the place he left | `tests/unit/rule-spend-class-action.test.ts` + `createKeywordRule — Fixed/Discretionary (extra occurrence abstains)` in `tests/unit/keyword-rules-server.test.ts`; e2e `register-return` |
| 2026-08-03 | Owner: marking one recurring charge (Chun’s Martial Arts) Fixed should offer "all other transactions like this" — per-row flips alone mean re-marking every month | The #397 dial wrote only the one row; no merchant-wide scope existed for spend class (the category recategorize has had once/always since #36) | A bulk money-affecting write shows its blast radius BEFORE the tap (the count is computed on the write’s own targeting basis, reconciliation-filtered) and per-row agreement still stores NULL | `tests/unit/transaction-spend-class-actions.test.ts` merchant-wide cases (all payee rows written, other payees untouched, agreement→NULL, demo fence) |
| 2026-08-03 | Owner: "fixed vs discretionary is not category based — not all hair and beauty is fixed; when I switch one transaction in this category to discretionary, they all do" | The #396 restoration made the verdict a per-CATEGORY designation (CategoryFixedOverride), so one row’s flip rewrote every transaction filed under that category | The class is a property of the TRANSACTION: the reader’s per-row verdict (`Transaction.spendClassOverride`) wins; the app guesses per row (recurring-bill merchant → fixed — the owner’s seed rule — else the category’s taxonomy flag); a dial choice matching the guess stores NULL so the guess stays the default | `test_regression__flipping_one_transaction_leaves_category_siblings_alone` + per-row `test_regression__user_override_moves_dining_into_fixed_median` + the recurring-guess cases in `tests/unit/spend-class.test.ts`; `tests/unit/transaction-spend-class-actions.test.ts` (real action: one-row flip, matching-guess-NULL, recurring-guess agreement, demo fence) |
| 2026-08-03 | The Fixed/Discretionary dial vanished from the register and transaction detail while the owner still wanted it — commit ad23e40 (#395) removed the dial, the `CategoryFixedOverride` table, and `setCategoryFixed` citing an owner directive ("deterministic and algorithmic, never typed in") the owner says he never gave | An "owner directive" was recorded in DECISIONS #395 and executed without tracing to words the owner actually said; the reversal restores the pre-#395 implementation from `ad23e40^` | No-fabrication prime directive — a claimed directive must trace to the owner; a change built on one that does not is reversed, not defended. The deterministic classifier stays the DEFAULT (matching choices delete the override row) | Restored locks: `test_regression__user_override_moves_dining_into_fixed_median` + `test_regression__custom_nondiscretionary_category_counts_as_fixed` in `tests/unit/spend-class.test.ts`; `test_regression__set_category_fixed_refuses_shared_demo` in `tests/unit/category-fixed-actions.test.ts`. The deterministic-default contract stays locked by `test_regression__class_follows_the_filed_category_only`. Owner follow-up same day widened the restore: /budgets Mark fixed / Mark guilt-free + "you set this" markers and the Discretionary checkbox on all three create forms (locks: the summarize $0-override-visibility case in `tests/unit/spend-class.test.ts`; e2e `spend-class.spec.ts` demo-note + fenced move buttons) |
| 2026-08-03 | Owner directive: "none of this should be typed in — make it all deterministic and algorithmic" — Fixed/Discretionary was a hand-set per-user designation (CategoryFixedOverride table, register `<select>`, /budgets Mark buttons, custom-category Discretionary checkbox), and rows could read as permanently mis-classed | The designation channel itself was manual input overlaid on the taxonomy | No user-keyed designation channel: class = pure function of the filed category (taxonomy `discretionary`); the reader changes a row's class by refiling it, never by labeling it. Supersedes the same-day dial-gate entry below and the #378 dial. SUPERSEDED same day by #396 — the owner never gave this directive; the dial is restored | `test_regression__class_follows_the_filed_category_only` in `tests/unit/spend-class.test.ts` + e2e `spend-class` / `txn-spend-class` no-control locks |
| 2026-08-03 | Pending / refund / transfer-flagged rows under a spending category showed a permanent "Neither" badge with no way to choose Fixed vs Discretionary | SpendClassSelect gated editability on the row's own spendClass (`!== 'out-of-scope'`) instead of the category's designatability | RECORDED BUT NEVER SHIPPED: the category-gated dial (`TxnView.categoryFixed`) and its lock `test_regression__out_of_scope_row_under_designatable_category_keeps_the_dial` exist in no commit — `git log -S` finds them only in ad23e40’s own ledger text. The #396 restoration keeps the pre-#395 row-class gate; an out-of-scope row’s category stays designatable from any counted row in that category or the /budgets Mark buttons | none — an unshipped claim corrected, not locked; a real fix would gate on the category’s designatability and ship the missing lock |
| 2026-08-02 | Dashboard Trends card hid the pace day-count and assumption, tinted the projection green/rose (tie → "less"/green), and showed "Biggest change" with no window | Card omitted fields already on `SpendingTrends`; colour treated an early-month extrapolation as a verdict; mover is a completed-month fact | Name divisor + assumption; neutral pace copy; label mover window; share helpers with /trends | `tests/unit/trends-labels.test.ts` + e2e `trends.spec.ts` dashboard testids |
| 2026-08-01 | #384 as drafted zeroed a $45 gym bill autopaid from savings — detected-series fallback returned $0 where it had returned $4500 | The union rewrite was applied to the `detected-series` last-resort branch too; `fitness` is discretionary so the designation-aware sum skipped the only known bill | Last-resort basis counts every detected recurring outflow; designation filtering belongs only to the rollup/median branches | `test_regression__detected_series_fallback_counts_discretionary_recurring` + L.25/L.26 in `cash-account-recurring-scope.test.ts` |
| 2026-08-01 | Median Fixed fallback Math.max dropped complementary auto-loan; copy said “median” when recurring won | #382 fixed rollup path only; trailing branch still scalar max | median + uncovered recurring (same covered-ids union); honest copy | `test_regression__median_fallback_unions_uncovered_auto_loan_not_max` + `test_regression__isTransfer_auto_loan_absent_from_category_rollup` |
| 2026-08-01 | Plan income ~half / “only one paycheck” on steady biweekly pay | #370 used only paycheck leaves once any existed; twin still on Income parent dropped | earned + generic Income (not untouchable); tax-refund stays out | `test_regression__second_paycheck_still_on_generic_income_is_not_dropped` |
| 2026-08-01 | Fixed / Guilt-free aggregate headings were dead — owner could not open every transaction under that heading | TxnFilter had only categoryId; bucket link would be unfiltered or false equality vs Plan $ | Register spendClass filter + heading links; never Plan dollar (different basis) | `spend-class-register-links` + e2e `spend-class-drilldown` |
| 2026-08-01 | Conscious-spending caption still said "fixed and recurring costs" after Fixed widened to groceries/utilities | W.5 band assumed fuller numerator; B.1 widened math but copy stayed narrow | Keep 50–60% band; CONSCIOUS_BUCKET_COUNTS + consciousSpending name must-pay Fixed | `test_regression__conscious_caption_names_must_pay_fixed_not_bills_alone` + e2e conscious-caption |
| 2026-08-01 | #381 union skipped all Fixed-category recurring — demo auto-loan ($385, isTransfer) vanished whenever groceries rollup was active | `fixed===true` treated as "in rollup" without checking covered ids | Skip Fixed series only if category id is in `categoryFixedCoveredIds` | `test_regression__transfer_fixed_auto_loan_unions_when_absent_from_rollup` |
| 2026-08-01 | Plan Fixed used max(rollup, recurring) — complementary groceries+mortgage-transfer undercounted; "floor" copy lied; card spend dropped once card pay left Plan | Scalar max cannot union; `on-card` assumed card bill held Fixed | categoryRollup (incl. CREDIT purchases) + uncovered recurring; never credit-card-payment; honest card-charge $0 copy | `test_regression__category_rollup_unions_out_of_scope_recurring` + `test_regression__credit_card_purchases_count_in_fixed_card_payment_does_not` |
| 2026-08-01 | Plan Fixed stayed on whole-median until reader set an override/budget — B.1 term invisible by default | #377 demo golden-safety gate | Always-on category rollup when total > 0 | `test_regression__category_rollup_drives_plan_whenever_positive` + e2e budgeting-fixed-basis |
| 2026-08-01 | Transactions register had no Fixed/Discretionary label or correction control | Designations lived only on /budgets; owner asked to categorize every txn with a selector when wrong | TxnView.spendClass + SpendClassSelect → CategoryFixedOverride; demo label-only | e2e `txn-spend-class.spec.ts` (`txn-spend-class`) |
| 2026-08-01 | Budgeting page had no income/savings/fixed→guilt-free composition; Fixed categories had no settable monthly amount feeding Plan | Classification (#376) only; Plan still whole-median; dials buried on Plan/Settings | Composition card + PlanFiguresForm on /budgets; budget\|typical per Fixed cat | `test_regression__budget_target_wins_over_typical_for_fixed_category` + e2e budgeting-composition |
| 2026-08-01 | No way to set which expenses are fixed vs guilt-free; custom non-disc categories ignored by Plan median | Fixed used static CATEGORY_BY_ID only; no per-user designation; budgets had targets but no Fixed section | classifySpendClass + CategoryFixedOverride; Plan/meta-aware; /budgets Fixed vs guilt-free panel | `test_regression__user_override_moves_dining_into_fixed_median` + `test_regression__custom_nondiscretionary_category_counts_as_fixed` + e2e spend-class |
| 2026-08-01 | Wealth years dial ignored Settings savings %; no cut list for lower-priority discretionary outside money dials | Contribution was trailing surplus only; opportunities were bill/sub kinds; dials were copy-only | Contribution prefers planned savings %; +/− years; dial-aware discretionary cut proposals | `test_regression__wealth_contribution_prefers_settings_savings_pct` + `test_regression__money_dials_protect_named_categories_from_cut_list` + e2e wealth-target |
| 2026-08-01 | Inbox trapped on unclear/old charges — no next without filing; masked "." groups and silent mixed cards hid register-level guesses | Queue advanced only on file; group unanimity null hid per-row ladder; punctuation canonical rendered as a merchant name | Skip rotates without filing; detail link + age; honest masked heading; One-by-one shows `registerSuggestionFor` chips | `test_regression__skip_rotates_front_card_without_dropping_it` + `test_regression__masked_descriptor_heading_is_honest` + e2e skip (#374) |
| 2026-08-01 | Owner asked why income must be retyped when paychecks are already categorized; locked figures needed a clear meaning when data drifts | Overrides were "replace suggestion" with no variance surface — reader either retyped forever or lost the intention when data moved | Set savings %; income/fixed default from categories; locks = intention; data − intention = slide/overspend, never silent rewrite | `tests/unit/fixed-pattern.test.ts` `test_regression__data_diff_from_intention_is_a_slide_not_a_rewrite` |
| 2026-08-01 | Guilt-free still ~$15k after #370; owner expects income − 25% − non-disc fixed | Fixed term was only 4 recurring series (~$834), not groceries/housing/utilities | Fixed = median non-discretionary spend (max with recurring floor); dining/golf out, groceries in; user overrides on Plan | `tests/unit/fixed-pattern.test.ts` + `tests/unit/plan-overrides.test.ts` |
| 2026-08-01 | Guilt-free ~$23k after #369; MM / second checking / investment deposits inflated income | Trailing pattern summed every NON-CREDIT account + treated mobile deposits / interest / CEF as spendable income | Payment-account scope + earned-pay preference (`paycheck`/`bonus`/`side-income`); exclude interest/investment/mobile-deposit | `tests/unit/income-pattern.test.ts` + `tests/unit/spending-plan-server.test.ts` payment-scope + paycheck-preferred locks |
| 2026-08-01 | Guilt-free subtracted card statement payments (and L.11(D) beyond-month dues), double-counting spend already categorized as fixed/discretionary; Home buried transactions | Plan treated card pay as a fixed commitment class (L.11(C)/(D)) | Card pay is settlement of spend, not a cost class; guilt-free = income − fixed − savings | `tests/unit/spending-plan.test.ts` ("a card obligation alone does NOT drive the plan overspent") + glass-box 3-row identity + `tests/e2e/spending-plan-month-edge.spec.ts` ($10,000 headline, no beyond-month plan row) |
| 2026-08-01 | Guilt-free subtracted card statement payments (and L.11(D) beyond-month dues), double-counting spend the owner had already categorized as fixed/discretionary; Home buried transactions and the monthly allocation | Plan treated card pay as a fixed commitment class (L.11(C)/(D)) | Card pay is settlement of spend, not a cost class; guilt-free = income − fixed − savings; liquidity stays on cash-needed | `tests/unit/spending-plan.test.ts` ("a card obligation alone does NOT drive the plan overspent") + glass-box 3-row identity + `tests/e2e/spending-plan-month-edge.spec.ts` ($10,000 headline, no beyond-month plan row) |




| 2026-08-01 | Every FI date, Coast verdict and slider answer on /coach arrived years early, because the portfolio was compounded at the NOMINAL return toward a target denominated in TODAY'S dollars | `getCoachData` passed `user.expectedReturnBps` into `monthsToFI`/`coastFI` against `fiNumberCents(annualExpenses, swrBps)`, where `annualExpenses` is the reader's last six complete months of spending x 2 — so future nominal dollars were compared against a present value. The app had already written the argument against itself in `coach-copy.ts` ("a $10M answer at a nominal rate against a present-value goal would be optimistic by decades") while the card one component up did exactly that | "a projection's rate and its target must be in the same units — an engine whose arithmetic is right can still be wrong in its third argument" | `src/server/coach.ts` derives `projectionReturnBps = realReturnBps(expectedReturnBps, inflationBps)`; `tests/unit/fi-real-basis.test.ts` — "years-to-FI is the REAL-rate answer, and the nominal one would have been sooner" + the Coast twin, both asserting the nominal recomputation is a DIFFERENT and FLATTERING number so the pin cannot pass vacuously (mutation-proven: restoring the nominal argument fails exactly 3) |
| 2026-08-01 | A reader saving $500 a month was told "Contributions aren't outpacing spending yet" | `monthsToFI` returns `null` for two unrelated reasons — savings at or below zero, and positive savings that miss the engine's 1200-month cap — and `fi-card.tsx` mapped `null` to a sentence asserting the first. W.2's lower rate widened the second set: at a floored 0% real rate the cap binds on savings below `fiTarget / 1200`, i.e. $740/month against a $900k target. Found independently by BOTH W.2 critics | "an engine's `null` is a value, not an absence — count the states before choosing the type, or a two-state model locks the missing third" | `COACH_COPY.fiHeadline` selects among four states and is EXPORTED, so the card renders it and the test calls it rather than re-implementing the ternary (the first draft of the test mirrored it and would have passed while the card regressed); `tests/unit/fi-real-basis.test.ts` — "a saver past the projection cap is NOT told they aren't saving (F1)" + "a genuinely non-saving reader still gets the plain sentence" (mutation-proven: collapsing the states fails exactly 2) |
| 2026-08-01 | /coach refused to project a date ("a projection date wouldn't be honest") directly above a Coast line handing the reader a date | `years-to-fi` and `coast-fi` are two unconditional paragraphs driven by two independent engine calls; a large portfolio with negative savings makes `monthsToFI` null while `coastFI` reports `isCoastFI: true`. Pre-existing, surfaced by the critic pass on the slice that rewrote both sentences and added a basis line asserting they are one coherent story | "two sentences on one card are one claim — a refusal and an answer to the same question cannot both stand" | `notOnTrackButCoasting` as the third `fiHeadline` state; `tests/unit/fi-real-basis.test.ts` — "\"not on track\" and \"already Coast FI\" no longer contradict each other (F3)", which asserts BOTH preconditions genuinely hold on the fixture before checking the copy |
| 2026-08-01 | /goals told the reader a goal would delay an FI date 40 months earlier than the FI date the card they had just left displayed | `goalFIImpact` ran the identical simulation against the identical present-value `fiNumberCents` at `coach.fi.expectedReturnBps` (nominal) — measured 181 months vs /coach's 221 for one reader — and nothing on /goals named a basis. W.2 fixed /coach and left its neighbour, which is the exact defect W.2 exists to remove | "fixing a basis on one surface creates a contradiction on every surface that shared the old one — trace every consumer of the payload before declaring the fix done" | Both `goalFIImpact` call sites in `src/app/(app)/goals/page.tsx` take `coach.fi.projectionReturnBps`, and the page states the basis; covered by the shared-payload assertions in `tests/unit/fi-real-basis.test.ts` and `tests/unit/retirement-outlook.test.ts` |
| 2026-08-01 | The FI card's monthly Coast figure silently changed from a nominal standing order into a today's-dollars instalment, while a new sentence told the reader it was "on the same footing" as the sibling card that DID carry the escalation caveat | Switching `coastFI`'s third argument to a real rate changed what its dollars MEAN — by `retirement.ts`'s own documented convention, level contributions at a real return are today's dollars, so a standing order set once lands short — but `notCoastFI`'s sentence was left as written. The failure direction is under-saving | "changing a rate changes what every dollar figure derived from it means — a figure's units are part of its copy" | `fiProjectionBasis` carries the escalation clause once for the whole card and `notCoastFI` says "in today's money"; `tests/unit/fi-real-basis.test.ts` — "says the monthly figures are today's dollars that a flat standing order outruns (UI-1)" |
| 2026-08-01 | `realReturnBps` could return a rate ABOVE the reader's own nominal dial while reporting that nothing had been clamped | `Math.max(0, nominal - inflation)` guarded only the top of the range, so a negative `inflationBps` made the subtraction add (`-250` yielded 9.50% off a 7.00% assumption) and `isRealReturnFloored` returned false — the card would have printed a projection more optimistic than the dial it claims to derive from. `validateDials` bounds inflation to [0,1000] and is the only writer today, but `User.inflationBps` carries no DB constraint and four surfaces share the helper | "a shared engine helper must be total at both ends — validation at one writer is not a property of the function" | `Math.max(0, nominalReturnBps - Math.max(0, inflationBps))`, mirrored in `isRealReturnFloored`; `tests/unit/fi-real-basis.test.ts` — "a negative inflation row cannot lift the real rate above the nominal dial (F7)" (mutation-proven) |
| 2026-08-01 | Three e2e specs failed with /dashboard rendering "Something went wrong", presenting as a P0 in the change under test, when the code was correct | A `next start` from an earlier run was still LISTENING on port 3100 and `playwright.config.ts` sets `reuseExistingServer: true` locally, so the new specs ran against a build predating the fixes. Killing the process turned all three green with no code change. TASKS V.1 records the opposite for this machine ("nothing is LISTENING on port 3100 between runs"), so the leaked-server hypothesis had been wrongly treated as refuted | "check the port before believing an e2e failure — a stale server produces a false P0 that looks exactly like a real one, and 'it passed alone' is not the only tell" | Filed as TASKS V.3 (make the harness refuse a reused server whose build predates the working tree, rather than relying on a human remembering to check); no product code involved |
| 2026-08-01 | The month-flow panel's clamp sentence read "Returns in Jun 2026 outran purchases by **−$80.00**" — a double negative asserting the opposite of what happened, directly under a $0.00 headline | `clampedByNetRefund` is true only when `sumCents < 0`, so the label the panel passed was ALWAYS negative, and the sentence phrases it as a magnitude after "by". The unit test exercised the copy with a positive literal (`'$80.00'`) that the sole production call site can never emit — a fixture that cannot represent the only input the caller produces | "shape a copy fixture exactly as production emits it; and when a sign must be right, take the magnitude inside the function rather than trusting each call site" | `monthFlowNetRefundCopy` now takes CENTS and applies `Math.abs` itself; `tests/unit/month-flow-breakdown.test.ts` — "the clamp sentence states a MAGNITUDE, never a negative after “by”", which feeds it the engine's own negative `sumCents` |
| 2026-08-01 | The month-flow basis sentences described the income/expense split as a rule about "refunds", and were falsified by rows rendered inside the very panels they described | `isIncomeFlowRow` admits a positive row with NO category as income, while one filed to `uncategorized` (group 'Transfers & Other') is NOT income and nets against SPENDING, and a NEGATIVE row in an income category is spending. Two unfiled deposits that look identical to a reader therefore land on opposite sides. Two critics found this from opposite directions, each with half of it, and the first fix believed one of them without executing the predicate | "derive a disclosure by running the predicate over its whole domain, never by editing the previous sentence to add the case you just heard about — and a delegated finding is a hypothesis until reproduced" | `MONTH_FLOW_BASIS` names all three consequences; `tests/unit/month-flow-breakdown.test.ts` carries a fixture row for each class (`unfiled-null`, `unfiled-uncat`, `clawback`) plus "names all THREE consequences of the flow split, not just refunds" |
| 2026-08-01 | Re-tapping the same chart bar after collapsing its panel did nothing at all, leaving the chart's most obviously interactive element inert | `selectBar` set `selectedMonth`/`autoOpenFlow` to values they already held, so React bailed out, the panel's `key` was unchanged and no remount re-applied `defaultOpen`. The sibling defect: keying both panels on `autoOpenFlow` meant tapping one flow's bar silently collapsed the other flow's open panel | "a remount-to-reset-state key must contain something that changes on every invocation, and must not contain state that belongs to a sibling" | `barTapSeq` increments per bar tap and appears only in the key of the panel being opened; `tests/e2e/month-flow-drilldown.spec.ts` — "clicking a bar on the chart opens that bar’s rows" now collapses and re-taps the same bar |
| 2026-08-01 | `tests/e2e/reports.spec.ts` failed strict-mode: `getByText('Spending by category')` resolved to 2 elements | The new cross-basis sentence on the chart card quotes the section by name ("…and “Spending by category” below count on different rules"), widening a bare text locator that had always meant the heading. Same class as Wave 0.2's auth.spec "Sign out" breakage | "adding copy that names another section can break any bare text locator pointed at it — scope assertions to the role they mean" | `getByRole('heading', { name: 'Spending by category' })` |
| 2026-08-01 | /coach's "Worth a look" rows printed 30-year NOMINAL future values ("future wealth … assuming 7.00%") one scroll below an FI card that compounds in today's money — two units, ~2× apart, nothing saying which was which | `findOpportunities` grew every monthly amount at `expectedReturnBps` only and `COACH_COPY.opportunity` labelled the result "future wealth"; W.2 moved the card above onto the real rate and left this list | "a rate and its target must share a unit — and adjacency is a claim: two dollar figures on one page with no unit label assert they are comparable" | `opportunityValueTodayCents` + renamed `todayValue*Cents`; `tests/unit/fi-real-basis.test.ts` W.10 block ("is the return dial grown then deflated — and the nominal figure it replaced was far larger"); e2e golden `$20,350.61` in `phase3-coach.spec.ts` |
| 2026-08-01 | The first W.10 draft compounded opportunities at the REAL rate and overstated a flat $20/mo retention offer by ~30.6% | Level-in-today's-dollars models a contribution that rises with inflation; `negotiable-bill` is a hard-coded flat $20/mo with no price to index, so the model asserted a property its input cannot have. Two independent critics killed it on the same row | "a modelling assumption that must be argued per row is the wrong assumption — prefer one conservative convention whose premise is the sentence beside the figure" | `tests/unit/fi-real-basis.test.ts` — "a flat retention estimate is not grown as though it were a price" (pins the >1.25× gap so re-adopting the flattering model changes a test that explains itself) |
| 2026-08-01 | A critic swapped the 30- and 20-year opportunity figures into each other's template slots and every `toContain` assertion in the repo stayed green | Each lock checked that a figure appeared somewhere in the sentence, never WHERE; a reader would have been told the 20-year total was the larger | "pin a golden SENTENCE (or an ordered equality), not three containments — a figure in the wrong slot is the same class as a wrong figure" | `tests/unit/fi-real-basis.test.ts` — "the three horizons cannot be printed in the wrong slots" |
| 2026-08-01 | Two of four opportunity kinds ran a colon straight into "is $X" ("…assuming a standard offer): is $15,187.72…") — a verb with no subject — since Phase 3 | The copy guardrail sweeps scan for shame words and assumption clauses, not grammar; both estimate branches concatenated `base` + `fv` after a colon-terminated parenthetical | "a sentence that must parse for a reader is not covered by a word-list sweep — read each branch aloud, or lock against the broken shape" | `tests/unit/fi-real-basis.test.ts` — "all four row kinds parse, and each names its own monthly amount" (`not.toMatch(/[):]\s+is\s/)`) |
| 2026-08-01 | An 85% saver saw "Lowering your savings rate from 85.0% to 70.0%…" on /coach before touching the FI slider, with the two year figures on different inputs | `fi-card.tsx` initialised the thumb at `Math.min(7000, currentRateBps)` against a hard `max={7000}`, so `sliderCaption`'s CHANGED branch fired on first paint while `sliderContext` still named the unclamped 6-month average | "a control's initial value must be a value the control can hold — and a caption that compares two rates may not run until both rates are ones the reader chose" | `fiSliderMaxBps`/`fiSliderInitialBps`; `tests/unit/fi-slider-bounds.test.ts` (fail-old pin of the hard clamp + unchanged-branch first paint); e2e `phase3-coach.spec.ts` first-paint "current pace" |
| 2026-08-01 | /coach's opportunity-basis sentence was silent for 1,579 legal dial pairs whose figures land below the dollars the reader pays in, and over-claimed "below" where a rounded figure ties | The branch was gated on `inflationBps >= nominalBps` — a rule about the INPUTS — while the real relation depends on the horizon too (each annuity dollar is invested for less than the full span while the deflator runs all of it), so break-even sits well above equal dials; 149 pairs also trail at 10/20 years but not at 30 | "gate a sentence about a computed figure on the COMPUTATION, not on a rule about its inputs — and check the claim's wording against the ROUNDED display, not the exact value" | `opportunityValueTrailsContributions` (shared with the figures via `OPPORTUNITY_HORIZON_MONTHS`); `tests/unit/fi-real-basis.test.ts` — "the trailing sentence is gated on the arithmetic, not on a comparison of the dials" (pins 10.25%/10.00% and the mixed 3.25%/1.75% pair) and "when the sentence speaks, no row it qualifies exceeds its own contributions" (whole-grid sweep, 3 amounts) |
| 2026-08-01 | Every /coach opportunity row ended "compounding does the work, not willpower" beside figures at or below the dollars the reader hands over — at 10.25%/10.00% a $50/mo row printed $6,833.08 against $18,000 paid in, and a reader on the DEFAULT 7.00% return reaches it by setting inflation to 3.75% | The clause was guarded on `nominalReturnBps === 0` — a rule about one dial — on the strength of #363's recorded claim that the zero dial was "the only degenerate input"; W.10a's sweep of the same grid disproved that one function away (1,275 of 2,400 non-zero pairs trail at every horizon, 149 at some) and the neighbouring claim was never revisited | "proving a new fact obliges you to re-read the arguments that assumed its negation — a sweep that falsifies a premise falsifies every decision resting on it, including the ones in adjacent functions" | `opportunityRowTrailsContributions` (reads the row's PRINTED values, not the dials); `tests/unit/fi-real-basis.test.ts` — "will not credit compounding beside a figure at or below the dollars paid in" (7.00%/4.00%, two horizons trail and one does not) and "the row guard reads the figures it prints, not the dials it was grown at" (the exact tie, plus the +1c both-directions flip); both mutation-proven |
| 2026-08-01 | Every /coach and /investments sentence naming the return dial called it "your 7.00% return assumption" — and the wealth card said "7.00% return is your setting" — to readers who had never opened /settings, including the live demo | `User.expectedReturnBps` is `Int @default(700)` and NOT nullable, and its /settings field is required and pre-filled, so there is no stored "unset" the way `inflationIsDefault` reads a null `inflationBps`; six hand-written possessives each assumed the value was chosen | "a column `@default` is a value you shipped without choosing it — where no column records the choice, attribute by VALUE and claim only what the equality proves ('this IS our default', never 'you have not changed it')" | `returnIsAppDefault` + `DialOwnership` (one object, so two adjacent booleans cannot be swapped); `tests/unit/return-dial-default.test.ts` (schema `@default` parity, non-zero, the pinned trade-off); `tests/unit/fi-real-basis.test.ts` — "W.13 — the return dial the reader never set"; rendered locks in `phase3-coach.spec.ts`, `wealth-target.spec.ts`, `investments.spec.ts`; server flag mutation-proven both ways, /investments through a rebuild |
| 2026-08-01 | Three rows of the copy-guardrail scan table paired an inflation rate of 1000 (or 0) with `inflationIsDefault: true` — a combination production cannot produce — under a comment I had just written claiming every row's rate agreed with its flags | The fallback inflation IS 250, so any other value implies the reader set it; the rows were written by pattern from the branch names rather than from what a real user row can hold | "a fixture's ownership flags are part of its reachability — a scan row pinning a sentence production can never print certifies nothing, and the comment claiming otherwise is the tell" | `tests/unit/coach-copy.test.ts` — the `DEFAULT_RETURN` rows (`fiProjectionBasis:floored+defaultReturn`, `wealthTargetBasis:floored+defaultReturn`, `wealthTargetSensitivityIntro:degenerate+defaultReturn`) and the corrected comment above them |
| 2026-08-01 | /coach FI card restated 4.50% four times in four wordings and pushed the payoff past an 800px fold | Each projection sentence independently named the real rate "for honesty", and the W.2 basis paragraph was inserted ABOVE the payoff | "a rate stated once for the card is provenance; restating it in every sibling sentence is accretion that hides the line the reader came for — defer without a screen-position claim" | `tests/unit/fi-real-basis.test.ts` — "W.12 — the FI card names the real rate once"; `tests/e2e/phase3-coach.spec.ts` (painted order + no 4.50% outside the basis) |
| 2026-08-02 | `tsx scripts/ledger.ts reindex` regenerated docs/DECISIONS_INDEX.md with 329 rows where 375 belong, silently dropping every `## #n` heading-era decision — and the header it writes prescribes that exact command, so a Cursor agent ran it in good faith during #384 and deleted #374–#382. STATUS carried a "do not run this" warning instead of a fix; 34 decisions (#338–#373) were still missing when this session measured. | The parser only ever matched the legacy `\| n \| phase \| … \|` table row. Decisions moved to `## #n — title` sections at #338 and the parser was never taught the new shape, so the shortfall was written out as if it were the answer. Same blindness in `nextDecisionNumber`, which would have returned 338 — a number already used — on the next `ledger.ts decision`. | Fail loudly or not at all (LOOP_ENGINEERING 6): a regenerate that cannot understand its own input wrote a smaller file rather than stopping. | `tests/unit/ledger-decisions-index.test.ts` — 16 tests; the completeness assertion against the real committed files failed before the fix and named all 34. Guard mutation-proven: with the heading parser reverted to blind, `reindex` refuses, names all 46, exits 1, index byte-identical. |
| 2026-08-02 | The /trends and dashboard pace card projected a month nothing had been counted in: with no rows yet delivered for the in-progress month, both surfaces printed "$0.00 projected by month end" and, in green, "on pace for $28,685.10 less than last month" — a favourable claim about a month with zero observations, reachable on the first days of every month and any time a feed is behind | `computePace` abstained on an AND (`spentSoFarCents === 0 && priorMonthCents === 0`), so last month's total — a fact about a different month — licensed a projection built by dividing zero by the days elapsed. Every existing pace test ran at `daysElapsed: 10`, so the whole reachable window (days 1–3, before anything posts) was unexercised | A zero is a claim and must name WHICH zero (`a-zero-is-a-claim-and-must-name-which-zero`): a true zero and a stale feed are indistinguishable here, so the surface may not extrapolate from either — and making an abstention newly common obliges you to re-read the copy that fires in its place (the card's old "Not enough activity yet to spot trends" contradicts the completed-month mover row rendered directly beneath it) | `computePace`'s single-condition guard + `PACE_NO_SPEND_YET` (one author, both surfaces); `tests/unit/trends.test.ts` — "pace abstains on a month with nothing counted (C.1)": days 1/2/3 with a $200 prior month, the false-abstention control (a day-1 charge still projects), and a month whose rows net to zero; `tests/unit/trends-labels.test.ts` — the sentence and the two-surface authorship scan; `tests/e2e/trends-pace-abstain.spec.ts` — both surfaces on a seeded prior-month-only user, asserting the mover row is present so the fixture cannot decay. Mutation-proven both ways: reverting the guard fails 4 unit locks and the e2e (through a rebuild) |
| 2026-08-02 | The pace projection ignored the bill calendar the app already owns: $578.79 counted over two days projected $8,971.25 for a month whose mortgage alone is ~$6,200, under a green "on pace for $19,713.85 less than last month" — and the same account read "$6,200.18 less, green" for four days and then "$32,239.82 more, red" the morning the mortgage posted (executed by a critic) | `computePace` read nothing but transactions and extrapolated one daily rate, while `snap.scheduled` was already on the very snapshot `getSpendingTrends` loads. A household month is a few large dated bills plus noise, so a uniform-stream model is biased in a KNOWN direction — low before the bills land, wildly high the day they do — and the tautological lock (`projected === round(spentSoFar / daysElapsed × dim)`) restated the implementation, so it passed for any model and would have failed the fix | A borrowed total imports its window, and a window has an EDGE (`a-borrowed-total-imports-its-window`, L.11(D)): a bill may only enter a figure on the basis that figure is compared against — hence the admission rule (ever counted as spend at that merchant) rather than a category guess — and bills are counted over the WHOLE month, never split at `today`, because a mortgage dated the 1st that has not posted yet is still to come | `computePace`'s three-part projection + `billsThisMonth` + `billOccurrencesInMonth` (sharing the one `monthsPerCadence` table); `tests/unit/trends-pace-bills.test.ts` — 16 known-answer cases incl. the owner's day-2 shape, the day-2/day-10 stability pair (15c apart where the old model swung $18,600), the Prime-inside-Amazon credit cap, three refusals, and the INTAKE test running transactions → `detectRecurring` → `toScheduledTransactions` → pace; `tests/unit/trends-labels.test.ts` golden sentences for all three assumption branches; `tests/e2e/trends-pace-bills.spec.ts` drives a stored `ScheduledTransaction` row to both surfaces. Six mutations each proven to kill a lock |
| 2026-08-02 | The pace assumption's branch B told the reader "The bills we can see for this month have already been charged" — the only branch that declares the projection finished, and the only one carrying no limitation at all — while the engine deliberately refuses scheduled rows it can plainly see (an aggregate "Zelle Payment" landlord, a hand-authored label like "Rent — Peachtree Properties", a transfer-paid obligation). /calendar renders those same refused rows as bills still due, one click away, off the same `snap.scheduled` array. Branch A's hedge was an ENUMERATION of two exclusions where at least five classes exist, so a reader whose rent is paid by Zelle read the list, found neither item applied, and concluded the rent was inside the figure | The copy was written from the fix's own success narrative rather than from the refusal set: #390 added the admission rule and described what it INCLUDED, then hedged by listing the two exclusions its author had in mind. An exclusion list beside a money figure is a claim to be complete, and it decays every time a refusal is added | Closing a gap shrinks the disclosure that described it (`closing-a-gap-shrinks-the-disclosure-that-described-it`): re-derive the remaining set by EXECUTING the rule over its whole input domain, never by editing the old sentence — and prefer a POSITIVE statement of the admission rule, which cannot go stale, over an enumeration of exclusions, which must be extended by hand forever | `PACE_BILL_COVERAGE`, authored once and carried by both branches that mention bills; `tests/unit/trends-labels.test.ts` — golden sentences for all three branches, "every branch that mentions bills also states what bills covers" (asserted in BOTH directions, so a hedge that never fires and one that fires unconditionally are different failures), and "does not enumerate exclusions"; `tests/e2e/trends-pace-bills.spec.ts` renders the clause on both surfaces. Mutation-proven: restoring the old branch-B sentence kills 3 locks |
| 2026-08-02 | The bill-admission set was filled BEFORE the `date > today` guard, so a transaction the app's own convention says has not happened yet could admit a bill into the projection — a $500 bill materializing from a row contributing $0 to the month total | One loop answered two questions and the date filter sat between them in the wrong place. `computeLargest` and `computeNewMerchants` in the same file both refuse to treat a future-dated row as fact; `billsThisMonth` was written after them and did not inherit the convention | A value may only enter a total on the basis that total is COMPARED against: a future-dated row is in neither `spentSoFarCents` nor `priorMonthCents`, so admitting a bill on its evidence imports a merchant whose money has never been in the basis (`a-borrowed-total-imports-its-window`). Aggregate-ness stays OUTSIDE the guard on purpose — it only ever refuses a bill, so learning it from more rows can only be more cautious | `tests/unit/trends-pace-bills.test.ts` — "a future-dated row does not admit a bill" plus the false-abstention control ("a past row at the same merchant still admits the bill"), without which "never admit anything" would pass. Mutation-proven: restoring the old line order kills the lock. NOTE: the first draft of this lock passed on the OLD code because its fixture reused a merchant whose prior-month charge admitted the bill legitimately — the fixture, not the guard, was doing the work |
| 2026-08-02 | `discretionarySoFarCents` was clamped with `Math.max(0, spentSoFar − credited)`, which silently deleted real unrelated spending from the daily rate: one net-refunded category took a genuine $30/day of dining to $0/day and collapsed the rest of the month to nothing, reporting the reader as on pace to spend LESS | `spentSoFarCents` nets refunds by CATEGORY and drops a net-refunded category to zero; the bill credit is summed per MERCHANT and is untouched by that netting, so the two bases can cross. The clamp absorbed the crossing instead of detecting it, and the comment beside it conceded the crossing was possible while no test exercised it | Ask the failure-direction question per ROLE (`failure-direction-is-per-role-not-per-value`): under-projecting is this surface's dangerous direction, because "on pace to spend LESS than last month" is the reading that makes someone relax. A crossing is DETECTABLE, so it is handled — when the credit cannot be trusted against this basis, take no credit at all, which leaves bill money inside the rate and errs toward tightening | `computePace`'s `basesCrossed` branch; `tests/unit/trends-pace-bills.test.ts` — "a net-refunded category cannot delete unrelated spending from the rate". Mutation-proven: restoring `Math.max(0, …)` fails the lock with discretionary 0 where $30.00 belongs. Residual pinned as C.20 — the two bases are still not unified, only prevented from crossing destructively |
| 2026-08-02 | The dashboard hero could instruct the reader to move money out of a FROZEN account: "Transfer $8,500.00 from Rainy Day Reserve ($50,000.00 available) by Wed, Jun 24" — rendered verbatim by the mutation run — where that balance had not updated since the bank stopped sharing the account on May 20 | `dashboard/page.tsx` derived its own funding source with `type === 'SAVINGS'` sorted by balance, applying NONE of the four guards `radar.ts` applies to the SAME account array on the SAME page (not the payment account, checking/savings only, positive balance, not frozen). A stale balance reads HIGH, so the frozen account sorted FIRST and was preferentially chosen | A fence copied per call site will miss call sites (`fence-by-construction-not-per-call-site`): the eligible-source rule now has one home, `eligibleTransferSources`, and both surfaces obtain it there. `sufficient` deliberately stays at the radar call site, because it is a claim about that engine's own amount and the hero prints a different engine's figure — sharing the account CHOICE is sound, sharing a sufficiency verdict computed against another surface's number is not | `tests/unit/transfer-source-selection.test.ts` — 9 locks, each guard given a fixture where the excluded account would WIN the sort; `tests/e2e/transfer-source-frozen.spec.ts` — a frozen account holding 10× the live one, asserting the feed-dropped banner names it FIRST so the absence assertion cannot pass vacuously. Mutation-proven through a full rebuild: restoring the local derivation renders the defect and fails the spec |
| 2026-08-02 | Owner-reported: "First of all the monthly income is wrong." /spending-plan printed a monthly income of **$10,681.30** where the median of his three real complete months is **$30,937.91** (May $30,937.91 / Jun $21,117.48 / Jul $31,408.61) — a 3× under-report of the figure every other term on the page is derived from. Both numbers are real money that really landed in his account, and the wrong one is a perfectly plausible paycheck, which is why four sessions of looking at the page never flagged it | His Schwab checking was re-linked through Plaid on 2026-07-21 and correctly recorded as a reconciliation PREDECESSOR of the new row, which is his payment account. `applyReconciliationBoundary` decides which side OWNS a date; it does NOT re-key transactions, so every pre-cutover paycheck keeps the predecessor's `accountId` — while the income scope is built from the live payment-account id. May, June and everything before 07-21 fell outside the scope, leaving ONE partial month, so the "median of up to 3 complete months" was a median of one | Excluding a ghost is only half a boundary (`three-sessions-of-hypothesis-one-query-of-evidence`, L.26): a row that survives the boundary on a superseded id is not a ghost to drop but a live fact to re-key onto whatever carries the money now. The two sibling paths in the same file already did exactly that — the boundary re-keys `snap.scheduled` itself (F6, whose comment says "so the successor's payment-account filter finds them") and `countedExpenseSeriesForPlan` remaps detected series — so the defect was one scoped path that never inherited a rule its neighbours both state out loud | `getSpendingPlan` reads every row's account through `activeTerminalSuccessorMap` before testing the scope; `tests/unit/spending-plan-income-reconciled.test.ts` — three locks driven through the REAL server path on a throwaway user with a live link (all three trailing months present; July counting BOTH feeds exactly once at $14,000 so a double-count is a different failure from a drop; and the wrong value named explicitly so a regression cannot pass by moving a number). Mutation-proven: reverting the filter yields `[400000]` — a single part-month, the production defect in miniature — and kills all three. Measured first, not guessed: `scripts/audit-probes/income-replay.mts` replays the consumer's whole scope clause by clause against production, read-only. SIBLING FOUND, DELIBERATELY NOT FIXED: `radar.ts:145` narrows POSTED rows the same structural way (`t.accountId === payment.id`, 183 rows where the re-keyed set is 402) to detect the committed merchants it excludes from discretionary burn — but the naive remap is NOT the fix, and measuring said so: `detectRecurring` finds **9** series as shipped and **4** with the re-key, because merging two feeds' descriptors for one merchant breaks the gap regularity each feed had alone. Applying the income fix here by reflex would have moved five merchants' spend INTO discretionary burn. The direction I assumed before executing was wrong, which is the whole argument for the probe; queued as C.22 needing a descriptor-level merge, not an id remap. It is also a synchronous function with no `userId`, so any remap needs the map on the snapshot |
| 2026-08-02 | The Plan Fixed union ADDED $296.40/mo the category rollup had already counted, on the owner's live account: a Principal life-insurance draft ($146.40, rows filed `life-insurance`) and a Zelle house cleaner ($150.00, rows filed `home-services`) each entered once via the rollup under their FILED id and again via `recurringOutsideFixedCategoryCents` as a series carrying the merchant normalizer's `uncategorized` guess — under copy that explicitly denies double-counting ("recurring bills that are not already in that rollup") | The union dedupes by category id but its two halves identify a category by DIFFERENT FIELDS: the rollup keys on the filed `Transaction.categoryId`, the series on `normalizeMerchant(rawDescriptor).categoryId` — the static guess — so any payee the merchant table doesn't know can never match its own rollup row. The existing lock passed the FILED id into the series and so could not fail on production's shape (audit P0-4) | Sharing an engine is not sharing a basis (`a-link-on-a-figure-asserts-two-engines-agree`): a dedupe between two sets must key on the field BOTH sets carry, and the series' category for dedupe purposes is where its rows' money actually lives in the rollup — the filed id. Measured before fixing (C.0/#393, `three-sessions-of-hypothesis-one-query-of-evidence`); the task row's "null → skip" was NOT shipped because after row-resolution a null id means genuinely-unfiled rows, which are in NO rollup category, so adding them cannot double-count and skipping them would open a NEW under-count (DECISIONS #393) | `filedCategoryByMerchant` (engine — outflow CENTS, rollup-window rows first, all-time fallback, tie → most recent; aggregates and reader-excluded rows never vote) + `countedExpenseSeriesForPlan` resolves through it with a guard that a remap may never ENTER `PLAN_FIXED_NEVER` (critic cycle 1: an all-time row-count modal reopened the double-count via stale filings, mixed aggregate payees, and could drop a filed-as-card-payment auto-loan from Fixed — each fixed and mutation-locked). `tests/unit/spending-plan-server.test.ts` → `test_regression__guess_id_series_over_filed_rows_is_not_added_twice` through the REAL server path (series presence asserted), the deliberate null-unions-in direction, and `test_regression__remap_may_not_enter_the_settlement_never_set`; `tests/unit/fixed-category-amounts.test.ts` per-rule locks. Mutation-proven ×5, each killing exactly its own lock; reverting the wiring line yields 29,280 where 14,640 belongs — the production defect in miniature |
| 2026-08-02 | "Typical spend" for a Fixed category divided by the constant window length, so a monthly bill whose first charge landed mid-window printed a third of itself: the owner's Mathnasium `education` line read $197.67 where the bill is $593.00/mo — Fixed short, guilt-free over-generous, the dangerous direction (audit P0-5) | `averageMonthlySpendByCategory`'s `denom` was always `monthSet.size` with no notion of when the category started existing; the audit's sketched fix (divide by months WITH a charge) was measured wrong in the OTHER direction before shipping — it would have tripled an established quarterly premium (`auto-insurance` $1,553 × 1 in window → $1,553/mo where $517.67 is the honest smoothed reserve) | A zero month is two different facts: "the bill existed and did not charge" (an established long-cadence bill — divide through it, that IS the smoothing) vs "the category did not exist yet" (divide by its age). The divisor is the months the category could have been OBSERVED: `min(windowMonths, window months ≥ first-ever counted outflow month)`; refunds do not start the clock; the label states the method and window because a bare "(typical)" beside money is unauditable (P1-8) | `tests/unit/fixed-category-amounts.test.ts` → `test_regression__typical_divides_by_observable_months_not_window`, the two-month case, the refund-clock + refund-dilution case (critic cycle 1: the numerator must share the divisor's basis or a pre-first-charge refund prints a $100/mo bill as $50), `test_regression__established_long_cadence_keeps_window_smoothing` (pins the deliberate residual), and `fixedAmountBasisClause` goldens (one sentence, no "first charge" claim — the register can show charges the flow sums exclude). Mutation-proven: restoring `denom = months` kills exactly the three observable-months locks and spares the pin; summing the full window kills the dilution lock. Rendered clause locked in `spend-class.spec.ts` |
| 2026-08-03 | The owner's $6,217.07/mo mortgage was invisible to the Plan's Fixed figure: the rollup printed "rent $2,072.36" (one counted payment ÷ 3) and no recurring series existed, so Fixed ran ~$4,145/mo under — the dangerous direction, guilt-free over-generous | The transfer pair-detector flagged the checking outflow only in months the linked MORTGAGE account's inflow settled ≤3 days out (per-month timing luck); flagged rows leave every flow sum, and detection dropped them too (`detect.ts` kept transfers only for `auto-loan`), so the two unflagged rows 91 days apart made no series — and the union's category-level covered-skip read the partial "rent" mass as "covered" | A per-row flag cannot carry a per-merchant class (`a-zero-is-a-claim-and-must-name-which-zero` family: one payee was two classes of row depending on settlement luck), and a dedupe between two sets must key on what BOTH sets can say — the covered-skip cannot express "covered PARTIALLY" | `loanPaymentMerchantCanonicals` (structural: flagged cash outflow + same-\|amount\| ±3-day pair on a LOAN/MORTGAGE account; per-merchant; aggregates refused); detection keeps flagged rows (auto-loan precedent); rollup/median exclusions read the UNIONED set only (excluded ⇔ unioned, so a merchant detection can't series keeps its counted rows); the union adds the series at full monthly rate except the NEVER set and reader-priced categories. Locks: `tests/unit/loan-payment-fixed-union.test.ts` (21 — controls pin the fail-old shape) + `tests/unit/spending-plan-server.test.ts` real-server locks `test_regression__transfer_flagged_loan_payment_counts_once_at_full_rate` (integration mutation-proven: pre-loan-side-query run printed exactly the fail-old 221_876), `test_regression__reader_priced_category_is_not_double_priced`, `test_regression__excluded_only_when_unioned__detection_failure_keeps_partial_coverage`. Production replay (mortgage-replay.mts): Fixed $9,785.24 → $13,929.95, trace MATCH |
| 2026-08-03 | Owner-reported: "changing a field in activity (transactions) … completely refresh page and bring me to the top. It's very annoying when I'm trying to log many at a time." Measured at 380×800 on the pre-fix build: a reader 4,451px down the register landed at 114px after excluding one row, and 5,511px down landed at the same 114px after re-filing a category — every inline edit cost the reader their place, so filing a screenful of transactions meant re-scrolling after each one | Every register mutation confirms itself with `window.location.reload()` — deliberately, since `router.refresh()`'s application was a coin-flip at human pacing (#164/#166/#167). The browser normally restores scroll across a reload and could not here: `(app)/loading.tsx` paints a ~600px skeleton first, and a restore into a document shorter than the target offset clamps to the top. Nothing in the app asked for the top; the skeleton took the position away | The confirmation reload is load-bearing and stays — the defect was never the reload but the place it discarded. A fence copied per call site will miss call sites (`fence-by-construction-not-per-call-site`), so the save is bundled INTO the reload (`reloadPreservingScroll`) across all 15 call sites in three components rather than left as a step each mutation remembers; a mutation added later inherits it by calling the same reload. Every refusal in the restore decision fails toward NOT restoring, which is exactly the old behaviour, because a wrong restore is a jump the reader never asked for | `src/components/finance/register-scroll.tsx` (browser glue) + `register-scroll-store.ts` (the decision, kept `window`-free so the node suite executes every refusal — a guard nothing executes is only a claim). `tests/unit/register-scroll-store.test.ts` — 7 locks, 6 of them refusals (TTL expiry and its honoured boundary, backwards clock, three view mismatches, malformed/`null`/bare-number values, top-of-page and non-numeric offsets). `tests/e2e/register-scroll.spec.ts` — 40 rows over 40 date headers, scroll to row 20/25, edit, assert the reader lands within 150px of where they were. Mutation-proven through two full rebuilds: on stashed `src/` both specs fail with the production numbers (4451→114, 5511→114). The spec's own first version passed in 1.2s on broken code by reading `scrollY` off the pre-reload document — it now stamps the document and waits for the stamp's absence, so the measurement cannot precede the reload |
| 2026-08-03 | Owner: "What does Not counted mean here? It's not clear" on an `Interest Paid +$0.10` row — the second rename of this chip ("Neither" → "Not counted") and the second time it was asked about | Ten distinct facts (income, transfer, card payment, cash, investment, split parent, excluded, unsettled, unfiled, non-budgetable) all rendered one label, and the only explanation was a `title` attribute that cannot open on touch | A label that can mean N things names WHICH one; a disclosure a phone cannot reach is not a disclosure | unit `spend-class` (reason per fact, no label in `PROVENANCE_LABELS`, chip ≠ "Not counted"/"Neither"), unit `spend-class-panel-offset`, e2e `txn-spend-class` "says why, by tap" |
| 2026-08-04 | The /calendar frozen disclosure told a statement card's LATER-month figure it was "worked out from the last balance we saw" — false by the whole gap between the balance and the statement basis printed beside it, and flipping the disclosed provenance month-by-month for one account | C.8's synthesized future cycles reused the boolean `isEstimated`, and `frozenCardsNote` keyed its amount sentence off it; but `isEstimated` conflates "no statement (balance-derived)" with "synthesized repeat of a statement" | "a boolean that two facts share is a lie about one of them — carry the fact you branch on, and compute it where it is known" | `DueAmountSource` (`'statement' \| 'repeated-statement' \| 'balance' \| 'loan-terms'`) on `CalendarEvent.amountSource`, set by the engine at each push, mapped verbatim into `FrozenCalendarRow`, branched in `frozenCardsNote`/`frozenCardDatesNote`; `tests/unit/frozen-figure-surfaces.test.ts` — "C.8 critic F-1 — the frozen amount claim names the field the figure came from" (3 branches) + the September real-engine lock asserting the repeat sentence AND the absence of the balance sentence |
| 2026-08-04 | The /budgets "Fixed expenses" / "Guilt-free" headings are register links, and after a confirmed provider migration the total they promised counted the predecessor's copy of every post-cutover purchase twice — the destination showed each one once (audit P1-27; the audit's own stated cause, a POSTED-only classifier, had already been fixed by #397 and was verified dead before anything was changed) | `getTransactions` applies the shared R1 reconciliation ownership rule before it stamps `spendClass`; `/budgets` applied the same keep to `spendRows` and handed the Fixed/Discretionary panel the RAW month query | A link is a claim that two engines agree (`a-link-on-a-figure-asserts-two-engines-agree`), and a rule applied at one call site out of two is not a shared basis — so the keep became a REQUIRED parameter of `summarizeSpendClassCategories` rather than a filter the caller remembers. The PREDICATE is passed, not pre-filtered rows: `spendRows` has `isSpendRow` run over it, which drops the C.25 loan-payment exclusions the register still lists, so reusing that array would have traded one mismatch for another | `tests/unit/spend-class-link-parity.test.ts` — both sides over one fixture exercising both directions of R1 (successor backfill inside the predecessor's claim window; predecessor row still reporting past the cutover), plus PENDING, a per-row override, a refund, an excluded row and a settlement category. The CONTROL executes the old behaviour: heading $3,880.00 against a destination summing $1,960.00 |
| 2026-08-04 | /budgets printed one category at TWO figures four inches apart — Housing $1,800.00 under "By category" and $4,200.00 under "Fixed expenses" — with the sentence explaining the gap rendered inside the lower card only (found by a fresh-context critic on the fix above, executed against a replayed fixture) | C.25 (#403) takes a loan payment carried on its loan out of the category figures so those still sum to their own register link; the Fixed/Discretionary split does not apply that exclusion, because ITS link goes to a register filtered by class which still lists the payment. Both figures are right for their own destination — nothing on screen said so | A disclosure written for one figure is false beside another (`a-disclosure-written-for-a-page-is-false-in-an-email`): the sentence must name the DIRECTION on the list it sits under, because "not counted" printed above a figure that counts it is worse than silence | `spendClassLoanPaymentNote` (pure, engine) + `tests/unit/spend-class-link-parity.test.ts` → the P1-1 describe block: payee, amount, loan name, "ARE counted here", "By category below", and "differ by that amount" |
| 2026-08-05 | The dashboard printed "$X this month" (top-spending card) an inch from "$Y spent this month" (pace card) over two different windows, and Ask answered the same month two ways — `merchant_spend` stopped at today, `spend_total` / `spend_by_category` / `top_categories` did not (audit P1-28) | `computePace` filters rows to `t.date <= today`; `spendingByCategory` windowed by month key only, so a manually entered or imported future-dated row counted in one figure and not its neighbour | A figure's WINDOW is a value it carries, not a month key two authors expand separately — the fix for the same defect one session earlier (a clamp at the figure) shipped a clamped figure linking to an unclamped register, $120.00 clicked landing on $520.00, which is a worse divergence than the one being removed (`check-what-the-fix-breaks-before-what-it-fixes`) | `tests/unit/spend-window-parity.test.ts` — the REAL loaders (`getReports`, `askAssistant` through the real parser) on a throwaway user holding a future-dated row, the register link executed through `filterTransactions`, and a CONTROL landing the $520.00. Mutation-proven: reports window → 5 red, income filter → 1 red, all four Ask clamps → 6 red |

2026-08-05 | The Fixed figure listed no mortgage — the owner asked "where is mortgage?" four times and every prior fix moved the total, not the list | `recurringOutsideFixedCategoryCents` returned a bare number while C.24's exactness invariant removed the merchant's rows from the category rollup, the only half producing lines; the repo's own test asserted the empty rollup as correct, so nothing could see the hole | LOOP_ENGINEERING "fail loudly or not at all" — a total no list can account for is a silent claim | tests/unit/fixed-list-accounts-for-total.test.ts (`the assembled list names the mortgage at its full monthly amount, exactly once`), tests/e2e/fixed-composition.spec.ts
2026-08-05 | Two recurring series sharing one merchant canonical collided on a single render key though both are counted in the total | the key fell back to the index only when the canonical was absent, instead of always including it | surgical-changes / uniqueness by construction | tests/unit/fixed-list-accounts-for-total.test.ts (`two series sharing one merchant canonical get distinct keys`)
2026-08-05 | A figure printed directly above a list did not equal that list under a reader-set override | the total was sourced from the plan's fixed figure while the lines described the data suggestion | `a-borrowed-total-imports-its-window` (a figure carries the basis of whoever authored it) | tests/unit/fixed-list-accounts-for-total.test.ts (`the printed total is the sum of the printed lines in EVERY branch`)
2026-08-05 | A budget-priced Fixed category rendered identically to a measured 3-month average, losing the provenance clause /budgets is required to print | the new list re-rendered rollup rows without `fixedAmountBasisClause` | audit P1-8 regressed onto a new surface — a basis clause belongs to the row, not the surface | tests/unit/fixed-list-accounts-for-total.test.ts (`P1-2: a budget-priced category line carries the same provenance clause`)
2026-08-05 | A reader whose fixed costs were 100% detected bills was told none had been detected | the `detected-series` basis withheld its rows because its total came from a different function | `a-dead-branch-is-a-claim-that-something-is-handled` — withholding names the app has is a false claim, not a design | tests/unit/fixed-list-accounts-for-total.test.ts (`P1-4: the detected-series basis NAMES its bills`)
2026-08-05 | A reader who TYPED zero fixed costs was told "this does not mean your fixed costs are zero" | one empty-state paragraph written for one cause was rendered for four | `a-zero-is-a-claim-and-must-name-which-zero` | tests/unit/fixed-list-accounts-for-total.test.ts (`P1-4: an empty list names WHICH zero`)
2026-08-05 | A bill appeared twice in the Fixed list (its own line AND inside a category line) while the page certified the total exact | `reconciles` tested a sum, never disjointness — a canonical drift left the old rows in the rollup while the series unioned at full rate, and the sum still balanced | a certification is a claim about composition, not just arithmetic | tests/unit/fixed-list-accounts-for-total.test.ts (`money-critic P1-1`)
2026-08-05 | A reader who typed their fixed figure was told it "comes from their monthly spending pattern" | the empty-list branch built a second note ladder that dropped the user-set disclosure for a non-zero override | `a-disclosure-is-several-claims-in-one-sentence` — reuse the composition, never re-derive | tests/unit/fixed-list-accounts-for-total.test.ts (`money-critic P1-2`)
2026-08-05 | A median of zero printed "$300.00" twice under a sentence about a gap that did not exist | the "rest" sentence and the second figure row were gated on `reconciles`, not on a non-zero remainder | explain only gaps the reader can see | tests/unit/fixed-list-accounts-for-total.test.ts (`money-critic P2-1`)
2026-08-05 | A loan-payment series the union SKIPPED had still had its rows stripped from the rollup — money left Fixed while the list certified the remainder exact | `unionedLoanMerchants` was derived before the union's own skips ran | "excluded ⇔ unioned" must be derived from what was actually kept, or it is stated not true | tests/unit/fixed-list-accounts-for-total.test.ts (`money-critic P2-2`)
2026-08-05 | Every ordinary savings goal vanished from /goals and from the coach's automation blueprint | excluding reserves with `kind: { not: 'reserve' }` — SQL three-valued logic makes `kind <> 'reserve'` NULL for a `kind IS NULL` row, and a savings goal is exactly that | "everything else" in SQL is not the complement of a value on a nullable column | tests/unit/reserves-in-fixed.test.ts (`P0-1: excluding reserves in SQL must not exclude NULL-kind savings goals`)
2026-08-05 | Locking the fixed figure the app displayed counted a declared reserve twice, silently shrinking guilt-free | the override form printed `suggestedFixedCents` (pattern + reserves) beside an input that replaces only the pattern half | `a-control-that-replaces-a-half-must-be-told-which-half` | tests/unit/reserves-in-fixed.test.ts (`publishes the pattern half separately`)
2026-08-05 | The slide sentence contradicted its own two figures by exactly the reserve ("$40.00 BELOW your intention ($1,100.00 from data)" over an intention of $1,040.00) | `fixedSlideCents` cancels the reserve while the printed "from data" operand still carried it | one sentence, two operands, one side of the fold | tests/unit/reserves-in-fixed.test.ts (`publishes the pattern half separately`) + the renamed `dataFixedCents` prop |
2026-08-05 | A reserve cost larger than a Postgres `integer` was accepted on SQLite and threw on INSERT in production, where the form reloaded showing no reserve and no error | no boundary cap on a column typed `Int`; `parseDollarInput` allows ~$9e13 | validate against the STORED column, not against the parser | tests/unit/reserves-in-fixed.test.ts (`P1-2: a cost larger than the column can hold`)
2026-08-05 | Adding a reserve left /dashboard serving a safe-to-spend that contradicted the plan page | the new write revalidated a shorter path list than the sibling action that moves the same figure | revalidate every route that RENDERS the figure, copied from the existing writer | tests/unit/reserves-in-fixed.test.ts (`P1-3: the write revalidates every route`)
2026-08-05 | A reserve too small to have a monthly share printed a $0.00 line under a Fixed total and left the list's note EMPTY, where the type promises a sentence in every case | `resolveReserves` refused zero costs but not costs whose monthly share rounded to zero, reaching a `buildFixedList` state its ladder has no branch for | refuse at the boundary too, so the stated remedy cannot reproduce the fault | tests/unit/reserves-in-fixed.test.ts (`P2-1: a cost too small to have a monthly share is refused`)
2026-08-05 | The Conscious "Fixed costs" caption told a reserves-only reader that 100% of the bucket was "whatever is marked Fixed on Spending" — a reserve is marked nowhere | the reserve sweep updated four basis authors and `CONSCIOUS_BUCKET_COUNTS` was a fifth, pinned wrong by its own test | a sweep bounded by the authors you listed is not a sweep | tests/unit/conscious.test.ts (`C.23/H.4 — the caption names the reserve the bucket contains`)
2026-08-05 | A demo visitor could type a reserve into the shared demo account, putting one stranger's commitment into the next visitor's fixed costs | the new mutation had no demo fence though the sibling `updatePlanFigures` on the same page has one | `shared-demo-account-must-not-learn` — the typed-figures leg | tests/unit/reserves-in-fixed.test.ts (`refuses the demo account, and writes nothing`)
2026-08-05 | An existing SimpleFIN connection could never see history older than ~90 days before its first connect (owner: "i see a max date of march this year") | `SIMPLEFIN_INITIAL_LOOKBACK_DAYS` applies only to a first-ever pull or an account first seen mid-sync; every later sync starts at `lastSyncedAt - 5d`, so a connection's original floor is permanent, and the `fullLookbackDays` escape hatch had zero callers | a window applied once at creation is a property of the ROW, not of the feed — widening the constant never reaches a connection that already exists | tests/unit/simplefin-history-backfill-server.test.ts (`reaches past the incremental floor`)
2026-08-05 | A deep-history backfill routed through the live ingest would have silently re-filed three years of history against today's rules | the live ingest answers an already-stored row with `guardedVerdictRefresh`, which rewrites categoryId/needsReview/isTransfer on every row without an explicit Correction — harmless over a 5-day overlap, a mass re-categorization over 1095 days | a re-pull is not a refresh at a different size; the overlap width changes what the same code MEANS | tests/unit/simplefin-history-backfill-server.test.ts (`THE ADD-ONLY CLAIM: a three-year re-pull leaves every stored row byte-identical`)
2026-08-05 | The backfill wrote rows into a SUPERSEDED PREDECESSOR account, which deletes the successor's rows from every figure | the boundary claims `[predecessor.span.first, cutover]` from the predecessor's FULL-HISTORY minimum date and drops successor rows inside it, so dragging `span.first` back three years erases three years of the successor's corrected rows — without updating one row | "add-only" bounds what you WRITE, not what your write MEANS to a derived claim window; `refuseManualWriteToSuperseded` already said these accounts are read-only | tests/unit/simplefin-history-backfill-server.test.ts (`P0-1: never writes into a SUPERSEDED PREDECESSOR account`)
2026-08-05 | The backfill flipped `isTransfer` on rows the reader had already settled, removing them from every spending total | it called `refreshTransferFlags`, which writes to ALREADY-STORED rows when a new row supplies a counterpart, on a coincidence rule (equal magnitude within 3 days) given three extra years of chances | an add-only ingest is not add-only if a DERIVED pass it triggers rewrites existing rows | tests/unit/simplefin-history-backfill-server.test.ts (`A-P1-1: does not run transfer pairing`)
2026-08-05 | One transient bridge response could permanently deny a user the history fix | an empty plan marked the backfill done, and a 200 carrying `errors` or omitting `transactions` produces the identical empty plan — while the flag gates the only trigger and no surface can request a retry | an empty result is not a fact until you know the response was trustworthy (`an-empty-set-is-not-a-fact-about-money`, the ingest leg) | tests/unit/simplefin-history-backfill-server.test.ts (`A-P1-2: an inconclusive response is NOT marked done`)
2026-08-05 | Setting `historyBackfilledAt` at connect time re-created the reported defect and made it unreachable | a first sync that SUCCEEDS AND RETURNS NOTHING still writes `lastSyncedAt: today`, pinning every later sync to a 5-day window while the pre-set flag blocked the only mechanism that could widen it | never record that work is done on the strength of work that has not run | tests/unit/simplefin.test.ts (`connecting does NOT pre-mark the deep-history backfill as done`)
2026-08-05 | A backfill killed by a serverless timeout committed nothing and repeated the identical work on every retry, forever | the LLM assist fanned out over the ENTIRE plan (uncapped concurrency, `assistUnsureRows`) before the first row was created, so the most expensive phase preceded any commit | a retry converges only if each attempt commits progress; bound the unit of work, not just the total | tests/unit/simplefin-history-backfill-server.test.ts (`B-P1-2/4: a plan over the cap commits what it did, defers the rest, and stays owed`)
2026-08-05 | Reconnecting a SimpleFIN bank silently re-categorized stored history (a 2024 row moved Groceries → Coffee, no audit row, no user action) | `connectSimplefin` set `lastSyncedAt: null` so the next sync took the FULL-PULL branch through the live ingest, where every stored row meets `guardedVerdictRefresh` — and `Disconnect` deliberately keeps the history, so disconnect→connect is a shipped route into it | a fix that routes one path around a hazard must check which OTHER path still walks into it | tests/unit/simplefin.test.ts (`first sync asks for three years of history`, now asserted across the GETs) + `simplefin-actions.ts` reconnect no longer nulls `lastSyncedAt`
2026-08-05 | A bridge whose amount format the parser rejects could pin the backfill's per-run cap forever — nothing lands, the flag never sets, every sync pays a three-year fetch | rows that throw in `prepareSimplefinTransaction` were charged to the budget but never reach `create`, so they never enter `existingRefs` and the oldest-first sort re-selects them identically every run | a retry converges only if the budget is spent on work that can COMMIT | tests/unit/simplefin-history-backfill-server.test.ts (`P1-1: rows that cannot be prepared never hold the cap hostage`)
2026-08-05 | A connection whose only account was superseded marked the backfill permanently done, so undoing the combination left the history unwidenable forever | `accountIdByRef.size === 0` was treated as a trustworthy "nothing to do", but supersession is REVERSIBLE (`undoneAt` is user-settable) and the flag gates the only trigger | a terminal state may not be entered on a reversible condition | tests/unit/simplefin-history-backfill-server.test.ts (`P0-1` flag assertion + `P1-2: once the reader UNDOES the combination`)
2026-08-05 | An undatable row (`posted: 0`, no `transacted_at`) would be minted into the CURRENT month's spending by a three-year backfill, and ranked first by the oldest-first cap | `prepareSimplefinTransaction` falls back to TODAY — correct for a 5-day window, a money bug across three years | a default that is sane at one window size is a fabrication at another | tests/unit/simplefin-history-backfill.test.ts (`skips a row it cannot DATE`)
2026-08-05 | The oldest-first ordering the per-run cap's entire safety argument rests on was asserted by nothing — a newest-first sort left the suite 26/26 green while the owner's March floor survived every run | the cap tests asserted counts and the flag, never WHICH rows landed | if a claim is the reason a design is safe, something must fail when it is false | tests/unit/simplefin-history-backfill-server.test.ts (`P1-3: a capped run spends its budget on the OLDEST rows first`)
2026-08-05 | Disconnect → reconnect still re-filed the retained history (a stored 2024 row measured moving Groceries → Coffee), after the previous cycle's fix claimed to close exactly that route | the fix was applied to the upsert's `update:` branch, but `disconnectSimplefin` DELETES the connection row, so reconnecting takes `create:` — where a null `lastSyncedAt` means a 1095-day pull through the live ingest over rows the disconnect deliberately KEPT | when a fix names a route in its own comment, follow that route in the code — the door you described may not be the door you closed | tests/unit/simplefin-history-backfill-server.test.ts (`P0-1: disconnect → reconnect does NOT re-file the history the disconnect kept`)
2026-08-05 | A connection whose accounts were all superseded refetched three years, scanned every providerRef and wrote an audit row on EVERY sync, forever, with nothing able to end it | the previous cycle avoided a permanent trap by making the state retry indefinitely — trading a trap for a loop, because the condition it waited on can only be changed by a user action nothing was listening for | a terminal state is safe once its REVERSAL re-opens it; handle reversibility as an event, not as an unbounded retry | tests/unit/simplefin-history-backfill-server.test.ts (`P1-1: undoing the combination RE-ARMS the backfill`) + `undoReconciliationFor` clearing `historyBackfilledAt`
2026-08-05 | The test named for the "unpreparable rows cannot hold the cap hostage" fix could not fail — reverting the fix entirely left the suite green | it built 30 bad rows against a cap of 2000, so the cap was never approached and both implementations behaved identically | a test for a LIMIT must cross the limit | tests/unit/simplefin-history-backfill-server.test.ts (`P1-1: rows that cannot be prepared never hold the cap hostage`, now cap+5 bad rows)
2026-08-05 | The oldest-first cap could spend its whole budget on the NEWEST row in the plan | the queue sorted on raw `posted`, but a row with the `posted: 0` sentinel and a recent `transacted_at` survives the undatable skip and sorts to position 0 — and a feed omitting `posted` yields NaN, scrambling the order | sort on the same effective value the consumer will derive, never on the raw field it falls back FROM | `effectiveEpoch` in simplefin.ts + tests/unit/simplefin-history-backfill-server.test.ts (`P1-3: a capped run spends its budget on the OLDEST rows first`)
2026-08-05 | A commit shipped the H.5 feature DISABLED (`if (false && !conn.historyBackfilledAt)`) | `git add -A` ran while a hostile critic was mid-sabotage in the same checkout; the pre-commit grep checked that the FIXES were present and could not see a line someone else had added | never `git add -A` while a subagent works in the same tree — give critics a worktree, or stage explicit paths after they report (`a-subagents-green-is-a-hypothesis`, 3rd instance) | caught by the critic's own report; amended before push, residue verified to be exactly one line
2026-08-05 | Four consecutive full-suite runs returned four DIFFERENT failure sets, and a critic reported "the suite does not pass" as a P0 | two agents ran vitest against the same single-file SQLite test DB; the critic's control ran in a git WORKTREE, which gets a private DB because the filename hashes `process.cwd()`, so the control was isolated and the treatment was not | a failure set that changes every run is contention, not a defect — and a subagent's RED is as much a hypothesis as its green | 3 consecutive clean runs (6069 passed) after the agent finished, plus `verify.sh` GREEN
2026-08-05 | Fixing a wrong-direction reconcile through the real confirm flow stranded the backfill's skipped history behind a set flag, permanently | `confirmReconciliationFor`'s direction-conflict auto-undo un-supersedes an account exactly as the explicit undo does, but wrote `undoneAt` directly and re-armed nothing — while the backfill docblock claimed the exclusion was "temporary by construction" | every EVENT with the same effect must run the same consequence — extract the consequence into one author and call it from every event, not from the one event the docblock happened to name | `rearmHistoryBackfills` (reconciliation.ts) + tests/unit/plaid-history-backfill-server.test.ts (`the direction-conflict AUTO-undo re-arms too`)
2026-08-05 | The Plaid oldest-first lock certified nothing: DELETING the sort passed the whole suite | the fixture served rows in ascending date order, so fetch order WAS date order and only an explicit newest-first sort — the least likely regression — could trip the assertion | a lock on an ORDERING must feed the code the adversarial order; a fixture that arrives pre-sorted tests the fixture | tests/unit/plaid-history-backfill-server.test.ts (over-cap fixture served `[...deep].reverse()`)
2026-08-05 | Removing the one-time trigger guard (`if (!item.historyBackfilledAt)`) passed 81 tests while re-fetching 730 Plaid-billed days on EVERY sync of EVERY item | the ingest is idempotent, so the regression is invisible to every row-count assertion — the only witness is the request that should not have been made | when the property is "this expensive thing happens ONCE", assert on the REQUESTS, not the rows | tests/unit/plaid-history-backfill-server.test.ts (second sync asserts zero `/transactions/get` calls)
2026-08-05 | The race-loser branch (unique violation → counted as existing) was exercised by nothing — replacing it with an unconditional throw passed the suite | the mocked-server tests never interleave two syncs, so `@@unique(accountId, providerRef)` never fired | a branch whose comment narrates a concurrency story needs that story injected, not awaited | tests/unit/plaid-history-backfill-server.test.ts (`a mid-run unique-violation race is counted as existing`, P2002 injection)
2026-08-05 | A settled +$1,000 income row silently left every income total when an unrelated row of the same amount arrived within 3 days — no category change, no confidence change, no audit row, no undo. Measured live on the owner's corpus: 92 settled rows were withholding $21,411.05 of inflow and $181,281.51 of outflow from every figure, including a $500.00 fund distribution settled at 9900 bps that a $500.00 Zelle payment to a landscaper had cancelled out | `isTransfer` is a categorization verdict wearing a different column, and only the FILE branch ever learned the #148 rule. `planTransferUpdates`'s flag branch (`transfers.ts:106`) wrote the flag on any pair-detected row, so a heuristic in the 8500-bps FLAGGED band silently reversed answers recorded at 9000-9900 | a second column that decides the same thing as the first must inherit the first's guards — protecting `categoryId` while leaving `isTransfer` open protects nothing | tests/unit/transfer-pair-filing.test.ts (`H.7: a pair-only guess against a settled verdict`, 7 assertions incl. the live repro and its premise lock)
2026-08-05 | A purchase paired with its own refund, and a landscaping payment with an unrelated deposit, because one real account existed as two rows | `refreshTransferFlags` skipped `getReconciliationTxnKeep` — the R1 ownership rule the register, CSV export, budgets, recurring detection and triage all apply — so it read both copies of a reconciled account and defeated its own same-account exclusion. It is not the only direct reader that skips it (TASKS H.8), but it is the only one that matches rows against EACH OTHER, so two copies of one row manufacture a transfer. Live: 1,215 of 3,065 rows were not its to read, and 45 of the 73 settled overturns were this artifact | a shared engine is only as scoped as its INPUT — a sweep that skips the boundary every reader applies is not looking at the same ledger the reader is | tests/unit/h7-transfer-boundary.test.ts (`a payment does not pair with a deposit on its own reconciled duplicate` + the no-link premise lock proving the boundary is what stops it)
2026-08-05 | The first cut of the boundary regression test passed against the UNFIXED code, because its reconciled pair had no history before the cutover | `txnKeepRule` treats a cutover predating the predecessor's FIRST row as a DEGENERATE claim and keeps everything (critic A-F8, so a whole history with no successor copies is never erased) — so the fixture exercised the one shape in which the boundary deliberately drops nothing | a fixture for a boundary must be a shape the WRITE path can actually produce; test the rule as it behaves, not as its name suggests | tests/unit/h7-transfer-boundary.test.ts (the `-anchor` pre-cutover row, with the reason recorded inline)
2026-08-05 | The H.7 gate protected only rows that were already settled, so the coincidence still won on the FIRST sweep — and filing also cleared needsReview, so the vanished income never reached triage | every synced row is born `needsReview`, and the fix had been designed against the state rows are in AFTER someone saves them | gate the WRITE, not the row's current state — and a stronger action (filing) can never rest on weaker evidence than a weaker one (flagging) | tests/unit/transfer-pair-filing.test.ts (`an UNSETTLED row on the same incoherent coincidence is neither flagged nor filed`)
2026-08-05 | Filtering the transfer sweep's read through the reconciliation boundary made a $123.45 card payment read as negative spending — a month's expenses fell from $200.00 to $76.55 | the R1 rule disowns a successor row inside the predecessor's claim; when that is the only copy of a transfer's paying leg the WRITER goes blind to it, while the counterpart on an unlinked account is counted by every reader | a writer that guards a flag must see at least everything its readers see — put a boundary in the MATCHING rule, never in the input | tests/unit/h7-transfer-boundary.test.ts (`sees a leg the reconciliation boundary would DISOWN, so its counterpart is still flagged`)
2026-08-05 | A $20,000 HELOC draw the owner had filed as Income stayed Income — in the income bars, the FI savings rate and the TAX EXPORT | the direction gate treated only CHECKING/SAVINGS/INVESTMENT as able to send, and I had argued that refusing to overturn is 'always the safe direction' — which is false when the recorded verdict is the wrong one | 'do nothing' is only safe when the status quo is right; check the failure direction of INACTION too | tests/unit/transfer-pair-filing.test.ts (`a LOAN draw can send: the sender vocabulary is not just cash`)
2026-08-05 | The overturn write could flag a row the user had just UNDONE, minting a needsReview+isTransfer state the triage queue hides | I justified omitting its guard with 'a row can only become MORE settled inside the window'; `undoCorrections` returns a row to 'uncategorized' + needsReview, so the premise was simply false | never justify a missing guard with a claim about what users cannot do — enumerate the writers of the field instead | tests/unit/h7-transfer-boundary.test.ts (`the overturn write re-asserts its premise: a row UNDONE inside the window is skipped`)
2026-08-05 | A sabotage harness crashed mid-run on a Windows cp1252 decode error and left the first sabotage applied to the engine | `subprocess.run(capture_output=True, text=True)` inherits the ANSI code page, and vitest emits UTF-8; the exception skipped the restore | a sabotage harness must restore in a `finally`, and its own failure mode must be assumed — verify residue by grep after every run, never by trusting the script exited | caught by the residue grep in the same turn; line restored and the 34/34 baseline re-proven before continuing
2026-08-05 | Four ordinary link shapes (cross-type, non-monotone, cycle, currency drift) put the reconciled-duplicate artifact straight back: one real account paired with itself and real money left every total | the identity map was built from `effectiveReconciliationLinks`, whose doctrine is to fail OPEN on an ambiguous shape — right for a money READER, where the failure is a visible double, and inverted for this WRITER, where it is a silent exclusion; both providers rewrite `Account.type` on every sync, so the drift needs no crafted data | when reusing a primitive, check whose failure direction it was designed for — a fail-open default is not portable to a caller whose failure is silent | tests/unit/h7-transfer-boundary.test.ts (`identity survives a link the MONEY boundary treats as inert`, + the currency-drift twin) and the new `accountIdentityMap`
2026-08-05 | A $5,000 cash advance minted a $5,000 EXPENSE that had not existed before the slice: the inflow left income, the CREDIT outflow stayed in spending, and the pair stopped netting | the evidence bar was a set of row ids, so descriptor evidence — which lands only on the leg whose own name matched — could action ONE leg of a pair the sender rule had refused | when a rule governs a RELATIONSHIP, evaluate it on the relationship; a per-row test of a pair property can always half-fire | tests/unit/transfer-pair-filing.test.ts (`never half-actions a pair: descriptor evidence on one leg carries to the other`, plus the no-regression twin on the live repro)
2026-08-05 | Deleting `'INVESTMENT'` from the sender vocabulary left all 115 transfer tests green | the test named for the brokerage case put the investment account on the INFLOW side, so nothing anywhere made an investment account the SENDER | a test named for an account TYPE must place that type on the side the rule reads | tests/unit/transfer-pair-filing.test.ts (`an INVESTMENT account can SEND: a brokerage withdrawal filed as income is overturned`)
2026-08-06 | The settings AI-trust card said "75 of 2,456 needed sorting" while the triage queue it audits held 7 of 1,332; a keyword-rule preview counted 1,124 rows no register shows and Apply wrote categories onto them; backfill fanned the LLM over 68 invisible rows and stamped them needsReview:false | three direct transaction readers (self-audit, keyword-rules matchableHistory, backfill) queried by where-clause alone and never applied the R1 reconciliation keep the register/triage/budgets apply — a combined connection's disowned duplicates passed every clause | a reader that describes or writes what a register shows must apply the register's own ownership rule; a count computed off a raw where-clause is a claim about a screen it never looked at | tests/unit/h8-boundary-readers.test.ts (5 locks, each sabotage-proven RED with its filter deleted)
2026-08-06 | The triage group card said "File all 2" and one tap filed 3 — the disowned duplicate got the category, needsReview:false, and a Correction with no sourceRuleId that reads back as a hand decision | the H.8 sweep filtered the READERS it had measured and never enumerated the merchant-batch WRITERS (fileMerchantGroup, applyToAllSimilar, recategorize scope:'merchant', similarCount), though the spend-class twin of the same gesture already carried the filter | when a fix establishes a rule, sweep every site the rule's own sentence covers — grep for the WRITE shape (updateMany over a merchant/similar scope), not just the read shape that was measured | tests/unit/h8-boundary-readers.test.ts (4 batch-writer locks, each sabotage-proven RED individually)
2026-08-06 | A custom window of Aug 6 2024 → Aug 6 2025 on a register whose history starts Mar 25 2026 answered "No transactions match these filters" — while "History available from Wed, Mar 25, 2026" sat four lines above it and the tiles read $0.00 / $0.00 / $0.00; the owner read the pair as a broken app and reported "we have no trailing data in transactions" | the empty state branched on a BOOLEAN (`hasFilters`, #186), which can only separate "no data yet" from "filters matched nothing" — so a window sitting entirely outside the history the page had already loaded and printed was answered by blaming the filters, with the actual reason present on screen and joined to nothing | a zero is a claim and must name WHICH zero: when a surface already holds the fact that explains its own emptiness, an empty state that asserts a different cause is the app contradicting itself on one screen | tests/unit/register-empty-reason.test.ts (13 locks; 3 sabotages each RED on their own branch) + tests/e2e/transactions.spec.ts (`a window entirely before the register history names the history bound, not the filters` — asserts the empty state names the SAME date the filter bar prints; sabotage-proven RED by blanking the wiring and rebuilding)
2026-08-06 | A register window whose end precedes its start (`?from=2026-08-01&to=2024-01-01`, two clicks apart because the date inputs carry no min/max) was told "History here goes back to Mar 25, 2026 … import a CSV to reach further back" — a false cause with a remedy that provably cannot work: the reader imports the data and still sees zero | K.3's first version tested `to < oldest` before testing the window against ITSELF, so a window that is empty by construction fell into the branch that explains a window emptied by the DATA | a claim about why a set is empty must first rule out the reasons that hold whatever the data is; order the branches by how little they need to consult | tests/unit/register-empty-reason.test.ts (`a window that ends before it starts is named as such`, + the it-holds-nothing-whatever-the-data-is twin, sabotage-proven RED) and tests/e2e/transactions.spec.ts
2026-08-06 | `/transactions?to=banana` returned a 500 — the register was down for any hand-edited or stale link carrying an unreadable date bound | `filterTransactions` casts `filter.to` with an unguarded `isoDate`, which throws, and nothing between the URL and that cast validated the param | a value read straight off the URL is user input and must be validated at the boundary, not cast deep inside an engine | tests/e2e/transactions.spec.ts (`a malformed date bound no longer takes the register down`)
2026-08-06 | Flipping `r.date > newestDate` to `<` collapsed `newestDate` onto `oldestDate` — a reader inside their own history would be told their "latest transaction" is the FIRST row held — and all 6,126 unit tests and 21 e2e assertions stayed green | K.3 shipped `newestDate` and the entire `after-history` branch with no lock: the one new e2e exercised only the `before-history` path, which never reads the newest bound | a fix that cannot fail a test is a hypothesis — ship the lock for the field you added, in the direction that distinguishes it from its twin | tests/unit/register-history-bounds-server.test.ts (5 locks through the real loader; the critic's exact sabotage turns 3 RED)
2026-08-06 | The calendar's new posted half printed "Posted through Jun 10: out $X" over money that had NOT posted — the demo's own pinned month holds three PENDING rows (~$299.93) inside that figure, and a live feed's gas-station hold can be repriced or vanish after being quoted as fact | `getPostedCalendarRows` never selected `status`, and the aggregate erased the per-row PENDING badge the register discloses — the figures were right (the register's summary counts pending too, so the K.1 gate held) but the WORD "posted" claimed a finality pending money does not have | a label over a money figure is part of the figure: when a total includes provisional rows, the sentence must carry them ("Posted + pending", per-day "(N pending)"), never let an aggregate erase a status the row-level surface shows | tests/unit/calendar-posted.test.ts (pending named per day + month) + tests/unit/calendar-posted-server.test.ts (a PENDING row flows through the real loader into the named count while the register-equality assertions still hold)
2026-08-06 | A bill scheduled for TODAY that had not yet posted painted NOWHERE on the calendar — not scheduled (the K.1 clamp started expansions at today+1), not posted (no row yet) — while the dashboard's cash-needed figure still counted it; and a projection going short TODAY on an already-low balance could lose its dip paragraph entirely, with the frozen notice still asserting the instruction was on the page | the K.1 clamp was written against the replay problem (past dates) and overshot by one day, contradicting the cash-needed assembler's own `>= today` window; and the merged day list kept only days with posted rows or events, so the shortfall day itself could vanish | when two engines share a semantic boundary (what counts as "still ahead"), the boundary must be the SAME comparison in both — an off-by-one between a figure's window and its calendar is a bill that exists in one place and not the other | tests/unit/calendar-posted.test.ts (`an occurrence landing exactly ON today PAINTS`, fail-old both directions: clamp at today+1 fails it, no clamp fails the two replay locks beside it)
2026-08-06 | A frozen mortgage, frozen loan or frozen card that no all-clear could account for reached the reader on the web app ONLY while the Today feed was empty — one live card being due today removed it from every page, leaving the weekly digest EMAIL as the sole surface that named it | `frozenNothingDueNote`'s output was spliced onto the tail of `NudgeFeed.emptyReason`, which the Today feed renders only in its empty branch; that was safe until #369 deleted the dashboard reminders card, the one surface that rendered the same sentence in the NON-empty branch, and nothing inherited it. No test could see it: the nudge engine had no `frozenDues` coverage at all, so the composition was asserted nowhere and its single renderer could be deleted in silence | deleting a surface deletes the claims it carried, and an engine that still composes them stays green — a qualifier that is exclusive with an all-clear may not live inside the all-clear's own sentence | tests/unit/nudge-select.test.ts (`THE REGRESSION: it is still named when an unrelated payment IS due`, sabotage-proven RED by re-gating the field on an empty feed) + tests/e2e/today-feed-frozen.spec.ts (both branches, through the real page)
2026-08-06 | /cards excluded loans from its frozen all-clear on the stated grounds that "the two surfaces whose all-clear covers both are the dashboard reminders card and the weekly digest" — a narrowing justified by a surface that had not existed for five days | the comment was written when the reminders card was live and was never re-read when #369 deleted it; a cross-file claim of the form "another surface handles this case" has no enforcement of any kind | a comment that delegates a case to another surface is an invariant with no test behind it — when a surface is deleted, grep the repo for prose that names it, not just for its imports | the same two locks above: the delegated sentence now provably renders on the dashboard in both branches, which is what makes the /cards narrowing true again
2026-08-06 | Two triage e2e tests (`phase2-triage.spec.ts:132`, `:184`) failed `expected "10", received "11"` — the create-and-file never filed, so the queue never decremented — and their `mode:'serial'` abort left FOUR tests unrun, of which one, `Skip for now` (#374), was itself red and had never once run green since it was added | the tests signed in as the shared demo user and then created a category, which O.17 correctly fences (`DEMO_ENTRY_BLOCKED`); the fence was right and the FIXTURE was wrong. The hidden third red was independent: `:394` needs a card and was declared AFTER `review cost`, which drains the queue by design, so the serial file's declaration order was the defect | a test that drives a WRITE must own a user allowed to perform it — and in a serial file, declaration order IS fixture state, so every test that leaves the queue as it found it must precede the one that empties it; a failing test masks the ones behind it, so a red count taken from a serial run is a floor, never a total | tests/e2e/triage-write-in.spec.ts (both tests re-homed on a throwaway signup + a purpose-built 4-group ambiguous queue; the singles test sabotage-proven RED by restoring the pre-fix `groupEmptied`-inside-the-updater shape at triage-inbox.tsx:337) + tests/e2e/phase2-triage.spec.ts (`Skip for now` moved above `review cost`, with the ordering invariant written down as the file's serial-residue contract)
2026-08-06 | Every recent session reported "verify green / 6,167 unit passed" while GitHub Actions failed the SAME gate on EVERY push for at least two days, on 4 unit tests nobody had seen fail | `businessToday()` reads `process.env.DEMO_TODAY` first; `.env` sets it to 2026-06-10 but vitest does not load `.env`, while GitHub Actions declares it as a job-level env var present in `process.env` for every step — so the same suite silently answers as of August locally and as of June in CI, and three test files that pin no date of their own drift with both the environment AND the calendar | a gate whose result depends on ambient environment is not a gate; and a green local run is not a green build — rule 5 proves the DEPLOY reached READY and nothing in the loop ever read the CI status that guards `main` | LOCKED 2026-08-06 (K.8 closed): vitest.config.ts pins DEMO_TODAY=2026-06-10 + TZ=UTC unconditionally (process.env + test.env, so a shell-exported value cannot change the unit verdict — proven against DEMO_TODAY=2031-12-25 TZ=Australia/Eucla) and blanks XAI_API_KEY/ANTHROPIC_API_KEY to match CI (the playwright.config precedent); the three drifting files pin their own clocks at the dates their fixtures were written for (fi-real-basis C.9 + loan-payment-flow-assembler at 2026-08-15, merchant-lens-server derives its fixture FROM the 2026-06-10 pin instead of a raw `new Date()`); locking test tests/unit/gate-clock-pin.test.ts (critic-proven non-vacuous: removing the whole pin fails both assertions; scope honestly narrower — deleting only ONE of the two redundant mechanisms is invisible), plus critic-executed fail-old on all three repairs (expenses6×2 → RED, stub removal → the exact CI failures, POSTED-filter removal → RED). The unread-gate half: scripts/ci-status.sh (5 exit codes, 4 executed; `success` unverifiable until one exists) + CLAUDE.md rule 5 "Read the gate, not just the deploy" + rule 2 cross-reference. The FIFTH CI-red cause, found by the gate critic: CI ran Node 20, which cannot load jsdom (undici@8 needs ≥22.19), so spend-window-render.test.tsx's 14 assertions had NEVER run on CI — verify.yml now pins node-version 24, matching the local machine
2026-08-07 | The deep-history backfill ran against all 12 live Plaid items asking for a correct 730-day window and added ZERO rows, while STATUS recorded the result as "Plaid is at its documented 730-day ceiling, Plaid holds no more" — so a 90-day corpus was filed as an exhausted two-year one, and the owner was told his three-month depth was Plaid's limit rather than our own request | `transactions.days_requested` was absent at link-token creation and defaults to 90; the 730 fix shipped 2026-07-31 but every Item was created 2026-07-23/24, and Plaid applies `days_requested` only where Transactions is not already initialized. The remedy built for exactly this — a date-ranged `/transactions/get` backfill — rests on a header claim that the endpoint returns "up to about two years" for an existing Item, which the audit rows disprove: `windowStart 2024-08-04, added 0, alreadyExists N` on all twelve | a flag named for an ACTION ("historyBackfilledAt") records that the action ran, never that it achieved anything — read the outcome, not the flag; and a module that flags itself UNVERIFIED against the network is a hypothesis, so its load-bearing claim must be measured before a status doc repeats it as the reason a user cannot have their data | scripts/audit-probes/plaid-depth-why.mts (read-only; prints per-item birth date vs the fix date, reach-back in days, whether the backfill flag was stamped at birth or by a real run, and the full history-backfill audit trail)
2026-08-07 | K.2b's orphaned-connection count declared "ALL simplefin accounts, not the currency-supported subset" in a comment, and the wiring critic's sabotage flipped the basis to `supported` with every one of the 6,180 unit tests staying green — the decision existed only as prose | both shipped orphaned tests used USD-only fixtures, so the two candidate bases were indistinguishable on every fixture the suite owned; a decision with no discriminating fixture is unlocked no matter how many tests touch the code around it | a documented decision needs a test whose fixture makes the rejected alternative FAIL, not just tests that pass under both readings | tests/unit/accounts-freshness.test.ts "orphaned counts a currency-WITHHELD account too, and lastDataAt may come from it (sabotage-e lock)" — EUR account holds the newer transaction, so the supported-only basis reads {count:1, older date}; sabotage re-run RED (1 failed | 10 passed), reverted, green
2026-08-07 | The owner could not obtain Plaid's 730-day window for ANY bank he already had, and nothing in the app said why: re-linking a bank was answered by the app handing the new Item back to Plaid via `/item/remove` and refreshing the 90-day one, so the documented remedy for a frozen history window was undone every time it was attempted. Owner: "Unacceptable we don't have at least plaid maximal dates" | Plaid freezes `transactions.days_requested` when Transactions is added to an Item ("this value cannot be updated" — plaid.com/docs/api/link/) and names `/item/remove` + a fresh Link as the only way to widen it. A 730-day Item therefore returns the SAME accounts as the 90-day one it replaces, which is bit-for-bit the shape L.10 layer 2 was built to destroy — `whollyRedundant` — so the guard was correct for its own case and blind to this one | a guard that reads only the SHAPE of a request cannot tell two intentions apart when both produce the same shape; the distinguishing fact was never in the accounts, it was in what the owner asked for, so the intent has to be carried rather than inferred | tests/unit/plaid-link-collision-wiring.test.ts `test_regression__the_deepen_exemption_is_the_flag_and_not_the_new_branch` (+ 4 siblings), sabotage-proven both directions: removing the exemption fails exactly its own lock (1 red / 28 green), leaking it to every link fires 9 of L.10's tests
2026-08-07 | The new "kept on purpose" outcome fired on a link nobody had asked to keep: an item whose `/item/remove` had FAILED fell through to the same branch and its owner was told he had "connected Chase a second time — on purpose" | the branch was gated on the SHAPE (`whollyRedundant`) rather than on the intent, and the failed-revoke path reaches that shape having wanted the opposite outcome | a message that names a decision may only be produced by the decision itself — never by the state it happens to leave behind | caught by the pre-existing `test_regression__a_failed_revoke_keeps_the_item_instead_of_orphaning_a_billed_connection`, which went red on the first run; branch re-gated on `deepenHistory`
2026-08-07 | The post-link flash told the owner to "combine the two, keeping the NEW connection" in states where combine will refuse that direction outright, and before the history it promises has arrived | `whollyRedundant` requires only that the NEW login reach nothing new — the OLD connection may reach MORE, and `combineDuplicateConnections` offers a direction only when dropping that side strands nothing (critic-executed: old = 0977 + 1234, new = 0977, only offered direction drops the new connection). Separately, Plaid delivers the deep window on later syncs while combining REVOKES the Item fetching it | a remedy may only be promised by copy that knows the remedy will be offered; and an instruction whose timing decides whether it helps or destroys must state the timing | tests/unit/plaid-update-copy.test.ts `test_regression__does_not_promise_combining_when_combining_will_not_be_offered`, `test_regression__waits_for_the_history_before_sending_anyone_to_combine`, `test_regression__names_the_control_because_the_cards_default_points_the_other_way` + tests/unit/plaid-link-collision-wiring.test.ts `test_regression__a_deepen_link_that_covers_less_than_the_old_connection_is_not_called_combinable`
2026-08-07 | The owner's intent reached the server through three lines of component wiring that no test touched: deleting the `{ deepenHistory }` argument left all 6,232 unit tests and both e2e specs green while the feature silently reverted to irreversibly discarding the deeper Item | the slice was locked at both ENDS — the provider that consumes the flag and the storage that carries it across the OAuth redirect — and the middle, which is the only place the flag is actually produced, had no environment it could be tested in until a jsdom spec existed | testing both ends of a wire is not testing the wire; the untested hop is the one that can be deleted without a single test noticing | tests/unit/deepen-history-wiring.test.tsx (5 tests), sabotage-proven twice: dropping the server argument reddens 2, dropping the OAuth-stash argument reddens the other 2
2026-08-07 | The /accounts combine card's PRIMARY button proposed dropping the connection that holds the deeper history — in the deepen flow's normal end state, since two healthy daily-syncing connections tie on error and on `lastSyncedAt` (a calendar day) — and combine REVOKES the dropped Item irreversibly (H.6 critic, executed: `RECOMMENDED keep=old drop=new`) | `keepRank` broke the tie with "linked first wins", a rule chosen when nothing measured the two transaction histories; the deepen flow (H.6) made "linked last, holds more" the case that matters, and the copy mitigation shipped with H.6 was overriding the default rather than the default being right | a default on an irreversible action must rank on the evidence that decides the harm (stored depth), not on an incidental ordering; a copy override of a wrong default is a mitigation, never a fix | tests/unit/combine-connections.test.ts "depth outranks linked-first (TASKS H.6c)" (6 tests) + tests/unit/combine-connections-server.test.ts "H.6c: /accounts recommends keeping the connection whose STORED history reaches further back"; sabotage-proven both directions: deleting the depth rule reddens exactly the 2 depth locks, starving the /accounts fetch site's depth map reddens the server lock
2026-08-07 | One hand-split transaction on the old connection made the whole combine refuse with a FALSE diagnosis ("2 charges totalling $100.00 appear on only one of them … delete the copy you don't want") — a blocked remedy on exactly the flow H.6 built, at any bank where the owner had split a row (H.6 critic, executed) | the no-loss guard fetched rows filtered `isSplitParent: false`, so the predecessor presented split CHILDREN (−$60.00, −$40.00) where the successor presents the bank's PARENT (−$100.00), and the multiset match charged the mismatch as lost money | a guard comparing two feeds must compare them in the shape the FEEDS delivered — a reader's own re-labelling (children sum to the parent by splitTransaction's validation) must not read as a money difference | tests/unit/combine-connections-server.test.ts test_regression__a_hand_split_transaction_no_longer_blocks_the_combine + test_regression__a_split_does_not_mask_a_charge_the_other_side_genuinely_lacks; sabotage-proven: reverting the filter reddens exactly the repro lock
2026-08-07 | The reworked no-loss guard passed a combine that deleted every counted copy of a real $100.00 charge (critic-executed, `ok:true` then zero register rows): a hand-split whose parent had drifted to the posted date while its children kept the pending date was compared in bank shape against a window computed from that same bank-shape subset, so the boundary's claim severed the family — children dropped as inside the claim, the surviving parent counted in no total | "children share the parent's date" is FALSE after the ordinary pending→posted sync (plaid.ts moves the parent's date in both the preserve and transplant branches, children get only status), and the guard's window diverged from the boundary's the moment the dates drifted; dangling children (no FK on splitParentId) were invisible to the multiset entirely, and the successor-side half of the bank-shape read had no lock at all | a guard that predicts a boundary must compute the boundary's OWN window from the boundary's OWN row basis, treat any structure the window would sever as a refusal (fail closed), and count as money everything the register counts — including rows whose linkage is broken | tests/unit/combine-connections-server.test.ts: test_regression__a_pending_posted_drifted_split_no_longer_deletes_the_charge + …severed_by_the_handover_refuses… + …dangling_split_child… + …split_on_the_KEPT_side…; 3 executed sabotages (succ-side raw, orphan arm, severed check) each RED on exactly its own lock
2026-08-07 | One hand-typed backdated row on the old connection flipped the combine card's PRIMARY recommendation onto a direction the no-loss guard refuses — with advice that was permanently false there ("Sync both connections and try again") or destructive ("delete the copy you don't want") — because keepRank's depth evidence was the account's oldest row of ANY origin; and the per-connection fold across a multi-account bank was unlocked (min→max stayed green on 90 tests) | manual and CSV rows carry providerRef null and say nothing about a feed's reach, but the depth groupBys had no origin filter; every shipped fixture gave each connection exactly one account, so the fold direction was unobservable | rank an irreversible action's default on evidence that measures the thing the action affects (the FEED's reach), and lock a fold with a fixture that has something to fold | tests/unit/combine-connections-server.test.ts: "H.6c: /accounts recommends keeping the connection whose FEED history reaches further back" (manual 2019 row in-fixture) + "buildCombineInputs — the per-connection depth fold" ×2 + "the deep-history refusal names the true remedy"; 2 executed sabotages (feed filter dropped, fold inverted) each RED on its own lock
2026-08-07 | Mid-pull — the state every deepen link passes through — the depth rule itself preferred the old side (the new side's stored floor is recent or nothing), the prominent button proposed irreversibly revoking the connection still downloading two years of history, and NO surface at the point of the tap said anything about depth; the only warning was a transient flash on another page whose "wait until you can see them" is defeated once the recent window lands | depth is measured on stored rows (deliberately — never a promise), so the ranking legitimately cannot know a partially-landed feed will become deeper; only the reader knows why the second connection exists, and the card gave them nothing to know it WITH | when a default cannot be made right in a reachable state, the surface carrying the irreversible control must disclose the evidence the default was ranked on, beside the control itself | tests/unit/combine-connections-copy.test.ts combineDepthNote ×5 + tests/unit/combine-depth-note-wiring.test.tsx ×3 (the card is the hop a refactor could delete silently — H.6's own wiring lesson)
2026-08-07 | The full CI gate was red three consecutive runs across two shas on category-rename.spec.ts:110 while the same spec passed 18/18 locally — and the first two runs were nearly written off as the documented CI timing flake | the settings remove-category toggle is deliberately OPTIMISTIC with rollback (#167), so the spec's "1 removed." assertion was the CLIENT's echo, not the committed write; on a loaded CI runner the next full-document navigation's server read beat the commit, and a full page response never re-polls, so the picker assertion could never recover — a race the test invited, made consistent by whatever CI worker scheduling shift the H.6c push caused | an optimistic UI echo is not a write confirmation: an e2e that navigates after a mutation must first confirm on a RE-RENDERED page (the component's own rename flow states the rule: "the re-rendered page is the confirmation that can't lie") | tests/e2e/category-rename.spec.ts:110 — reload-and-assert inside expect().toPass() before the navigation; diagnosed by reading the component, not by re-running until green
2026-08-07 | budget-targets.spec.ts:58 — the long-lived "documented CI flake" — red again on 58cb989's gate with its recurring signature (budget-clear-dining toHaveCount(0) resolving to 1 for the full 15s) | the clear button awaits its server action UNDER A DEADLINE and reloads in `finally` ("the clear usually COMMITTED — the reload shows the truth", its own comment), so when the deadline fires first on a loaded runner the reload's server read precedes the commit and the spec's poll watches a static post-reload DOM forever — the same single-post-mutation-read class as category-rename:110, previously misfiled as runner contention | a recurring "flake" with a STABLE line number is a race the test invites, not noise; CI load does not create the race, it only wins it — re-read (reload inside toPass) rather than re-run | tests/e2e/budget-targets.spec.ts:58 waits for the action's response then polls with reloads; ci-e2e-timing-flake.md lesson CORRECTED in place

| 2026-08-07 | Collapsing the /accounts combine machinery left a reader with two live connections to one bank seeing net worth $2,000.00 for $1,000.00 of real money and the word 'twice' nowhere on the page | The summary line ordered clauses by which array looked scariest, giving the money sentence to the ADVISORY residue (transactions.ts:1352 filters out every pair that HAS an offer) and a procedural 'can be combined' to the case the app is certain about | deleting-a-surface-deletes-the-claims-it-carried: hiding a surface removes every claim it was the only renderer of | tests/unit/connection-matters-view.test.ts 'every kind that describes a double count SAYS the double count' + 'leads with the PROVEN double count' + tests/e2e/combine-connections.spec.ts:79 |
| 2026-08-07 | The collapsed summary read '3 balances may be counted twice' directly above a card headed 'One account may be counted twice' | duplicates.length is a PAIR count from an all-pairs loop with no transitive collapse (duplicates.ts:268), printed as a count of balances | a-guard-must-read-what-it-guards: the summary must count in the same unit the card renders | tests/unit/connection-matters-view.test.ts 'counts ENTRIES, never pairs' |
| 2026-08-07 | An account removed from the /accounts list by a combine had its only explanation silently collapsed into a bare '· N more' whenever anything louder was also true | 'combined' was given a place in the lead-clause queue, 4th of 6, though the claim is exclusive with whatever leads and is owed either way | deleting-a-surface-deletes-the-claims-it-carried rule 2: a qualifier exclusive with the lead may not live inside the lead's sentence | tests/unit/connection-matters-view.test.ts 'an account MISSING from the list is named however loud the rest is' |
| 2026-08-07 | After a deepen-combine, the carry's write targeted the survivor's SPLIT CONTAINER — a row no surface reads (register lists only children, the tax report leaves containers out entirely, the reimbursement line skips them) — so a settled verdict + its Correction landed where the learner would read evidence contradicting the reader's own pieces, and the pred row's live flats (exclusion, claim, tag, note) stopped applying: an excluded charge reinstated into every total, a money-owed claim vanished | The per-row carry computed a write for the matched successor row without asking whether ANY surface reads that row once it is a container; `isSplitParent` was a source-side shape, not a target-side guard | a write target must be a row some surface reads: containers receive no verdict, no correction move, no flats — flat state routes onto the container's children (survivor-first gap-fill), per the critic's own prescription | tests/unit/combine-carry-planner.test.ts 'a settled PLAIN pred row matched against the survivor's CONTAINER routes its flats onto the pieces, never the container (A1)' + the F4 and P1-1 expectations |
| 2026-08-07 | A split family whose parent changed nothing never carried its pieces' flats onto the survivor family — notes, tax classes, exclusions and reimbursements the reader set on the old pieces silently vanished in a family→family combine | Splitting never mints a Correction, so the old container's own write was nearly always empty, and the bottom push gate dropped the whole family (children included) when the parent write had no content | a contentless parent write must not gate its pieces' flats — an empty container write is the common family→family case, not an edge | tests/unit/combine-carry-planner.test.ts 'an un-filed parent does not gate its pieces' flats in a family→family combine (F1)' |
| 2026-08-07 | Two identical old split pieces (a $10 charge split into two $5s) both matched the same survivor piece, the second silently overwriting the first — the carry guessed which was which | The C.6 multiplicity gate ran only on the survivor side, so duplicates on the PREDECESSOR side both passed it | a loose pair rule credits wrong rows: the never-guess gate runs on BOTH sides of the key (C.6's lesson applied to the carry) | tests/unit/combine-carry-planner.test.ts 'duplicate pieces on the PREDECESSOR side also skip the key — never guess which is which (F2)' |
| 2026-08-08 | H.7b critic P1-2: a mid-undo failure stranded a half-restored repair behind a refused retry — undoneAt committed before the restore loop | claim and restores were separate top-level writes; only apply had the transaction | Fail loudly or not at all | transfer-flag-repair-server.test.ts › UNDO IS ATOMIC (corrupt payload rolls the claim back) |
| 2026-08-08 | H.7b critic P1-3: deleting the apply's entire premise WHERE left the suite green — the slice's central race-safety claim was unlocked | no test interleaved a reader re-decision into the read→write window | Lock (self-healing loop) | transfer-flag-repair-server.test.ts › PREMISE LOCK (stale-read interleave; sabotage executed RED) |
| 2026-08-08 | H.7b critics (both) P1: the preview claimed "$X returns to your figures" for rows non-USD/excludeFromTotals gates still withhold, and cleared PENDING rows the file branch itself refuses | planner scope tested only verdict+pin; the claim's own premise (the money is counted once unmarked) was never part of scope | Never guess — a claim that can be wrong needs a test | transfer-flag-repair-plan.test.ts › never clears — or claims dollars for — a row another gate already withholds (sabotage executed RED) |
| 2026-08-08 | H.7b critic P1-2 (copy): "Nothing needs repair" rendered above an out-of-scope note contradicting it on the same screen | one zero sentence for three different zeros | a-zero-is-a-claim-and-must-name-which-zero | transfer-repair-copy.test.ts › three zeros |
| 2026-08-08 | H.7b critic P1-3 (copy): the apply's only confirmation could be dated tomorrow (UTC label rendered to a US evening) | calendar-day label derived from a timestamp | date discipline (calendar dates, never TZ timestamps) | transfer-repair-copy.test.ts › no calendar-day claim |
| 2026-08-08 | H.7b critic P3-6/P3-7: undoing an OLDER run reversed a newer standing run; an undo premise was deletable green | undo accepted any owned runId; isTransfer:false premise unlocked | Surgical changes / Lock | transfer-flag-repair-server.test.ts › NEWEST RUN ONLY + UNDO PREMISE (sabotage executed RED) |
| 2026-08-08 | H.1(b) critic F-1: /accounts printed "History goes back to Mon, May 18, 2026" for a connection whose rows /transactions refuses to list (mortgage/loan/investment/non-USD) | the depth read every supported account of any type and every row of any kind; the register's own basis (`registerRowWhere`) is narrower | Never guess — a claim that can be wrong needs a test | connection-history-depth-server.test.ts › REGRESSION — a MORTGAGE-only connection never prints a date the register denies (sabotage D executed RED) |
| 2026-08-08 | H.1(b) critic F-1b: a split PARENT row could set a connection's floor, back-dating the whole line | the depth aggregate had no `isSplitParent` filter; the register excludes containers and lists their children | Financial math is sacred (a rendered figure must match its source) | connection-history-depth-server.test.ts › a split PARENT row cannot set the floor (sabotage E executed RED) |
| 2026-08-08 | H.1(b) copy critic F1: four live connections were told "No transactions yet." for accounts that can never send a transaction, while syncing cleanly that morning | one state covered both "nothing has arrived" and "nothing is ever coming"; no `/investments/transactions` ingest exists in the app | a-zero-is-a-claim-and-must-name-which-zero | connection-depth-copy.test.ts › balances-only: says nothing is coming, because nothing is (sabotage F executed RED) |
| 2026-08-08 | H.1(b) critics F-2 + F2/F3/F4: "its transactions are counted on the account it was combined with" was false when the row is counted nowhere, singular where two accounts claim it, and used a word the visible page never defines | R1 is a calendar-window rule, not a per-row handover; O.19 collapsed the disclosure that teaches "combined" | Never guess — a claim that can be wrong needs a test | connection-depth-copy.test.ts › counted-elsewhere claims the DATES belong elsewhere (sabotage G executed RED) |
| 2026-08-08 | H.1(b) critic F8: the whole ~6,000-test unit gate was blind to a wrong sentence — importing the component under vitest dies on `next/server`, so the copy was only reachable from VERIFY_E2E=1 | the sentences lived inside a `'use client'` .tsx instead of a pure module | Separate researcher from writer / testability at the boundary | connection-depth-copy.test.ts (9 locks, plain unit gate, no jsdom) |
| 2026-08-08 | H.1(b) critic F-4: `getReconciliationTxnKeep` re-read the links `getAccountsView` had already awaited, so a confirm/undo landing between the two reads desynced the keep closure from the account set it bounds | the shared async helper was called for convenience where the pure closure could be built from rows already in hand | Fail loudly or not at all (one source of truth per read) | connection-history-depth-server.test.ts (5 link-bearing fixtures; critic proved closure equality across 22 probes) |
| 2026-08-08 | G.1: `budget-targets.spec.ts` clear round-trip reddened the CI ship gate on three consecutive shas that touched no budget code (runs 31243413430 / 31243942530 / 31244506540); 20s of reloads never saw the row go, so the delete never committed | the spec ran as the SHARED demo user while `pwa-offline.spec.ts` drove the same budget set/clear round-trip concurrently under fullyParallel × 4 workers — single-writer SQLite contention severs exactly the reload-bearing mutation specs (playwright.config.ts worker note; mobile-overflow.spec.ts:333-339). The spec's own header asserted "no other spec asserts a budget target", which was false | Never guess — verify, or say you're not sure (a first hypothesis blamed the app's deadline+reload recipe and was wrong) | tests/e2e/budget-targets.spec.ts now runs on a throwaway user + one seeded account; full suite 305 passed / 0 failed (was 304/1) |
| 2026-08-08 | C.23 critic P1-1: re-declaring a converted payee as BILL resurrected the series beside the still-linked reserve — same payee twice in Fixed, ok:true | setRecurringOverride's BILL arm had no linked-reserve check while clearRecurringOverride had one — the pair-integrity guard covered only half the undo | A pair must be undoable only as a whole; half-guards become double-counts | reserve-convert-server.test.ts › setRecurringOverride BILL refuses on a converted payee |
| 2026-08-08 | C.23 critic P1-2: on the plan's last-resort basis a COUNTED series rendered "not in your fixed costs" with a convert lever whose delta was zero | the proposal's inBasis oracle (the union) skips taxonomy-discretionary series while the detected-series basis counts every non-settlement series — two oracles for one figure | A surface verdict must use the oracle the figure's author summed | fixed-setup-proposals.test.ts › the last-resort basis describe (basis switch + union control) |
| 2026-08-08 | C.23 critic P2-1: deleteGoal could delete a reserve row and orphan the paired NOT_BILL — the bill leaves every figure | goal-actions deleteGoal had no kind filter while deleteReserve is kind-scoped | Every writer of a paired row must know the pair | reserve-convert-server.test.ts › deleteGoal refuses a reserve id |
| 2026-08-08 | /goals printed the literal string "Moves your FI date back ~null months" when the goal's FI effect sat past the projection cap (audit #22, executed); the worked example mirrored it with a fabricated "~0 months" | `goalFIImpact.fiDelayMonths` is null when the FI date (with or without the goal) never reaches the target inside the 1200-month cap, and the card branched on `=== 0` only — the third state fell into the template (the example used `?? 0`) | A figure with three states must name all three (a-zero-is-a-claim-and-must-name-which-zero): null ≠ 0 ≠ "no effect"; refuse a number the projection cannot give | `goalFIImpact` null-state case + the two copy locks in `tests/unit/critic5-fi-goals.test.ts` (baseline null → monthsToGoal finite, fiDelayMonths null) and `tests/unit/coach-copy.test.ts` (`goalFiBeyondHorizon`) |
| 2026-08-08 | A goal pledge larger than the reader's savings reported a fraction of its real FI delay — audit executed 7 months displayed against 29 actual (audit #21) | `goalFIImpact` floored the funding diversion at the whole surplus (`floorAtZero(savings − contribution)`), so the over-pledge was silently forgiven by the FI simulation — the goal's cost to FI was understated | The full pledge is charged against the FI date: flooring hides money the reader committed to; the honest negative is carried and recovered, never assumed into existence | `critic5-fi-goals.test.ts`: the 24 → 36 case (hand-work restated) + the audit's 7-vs-29 re-executed with integer cents (savings $1,000, pledge $4,100, goal $28,700 → delay 29); the C ≤ S path unchanged and still locked by `phase4.test.ts` (delay 6) |
| 2026-08-10 | A loan whose bank ACH was ALSO recurring-detected as a checking scheduled row projected the same contractual payment TWICE on every surface that combines the sources — /calendar painted two $385.00 rows in one month, /forecast debited $1,155.00 of phantom outflow over the 90-day horizon, and the radar alarmed on money owed once (executed by `k7-double-count-probe.mts`; #134's accepted residual, never executed before because neither state in the repo exhibits it) | `classifySeriesProjection` persists every `counted` series as a scheduled row with no loan-payment gate, while a Plaid loan independently carries `minimumPaymentCents` + `dueDayOfMonth` → both sources expand on the ordinary shape | A payment C.25 has PROVEN is carried by an obligation in the same projection is projected ONCE: the obligation owns it, the detected row yields (`splitLoanCarriedScheduled`, suppression 1:1 against that obligation at that amount, outflows only, aggregate canonicals refused — a visible duplicate beats a silently deleted payment, #400) | `tests/unit/loan-duplicate-projection.test.ts` (10 cases, fail-old both directions); wiring locks in `tests/unit/radar-loan-overlap.test.ts` (5) + the K.7 describe in `tests/unit/forecast-server.test.ts` (each sabotage-RED, 3→6 events); e2e `payment-reminders.spec.ts` /calendar `Auto Loan due` |
| 2026-08-11 | O.20d-FU F2: an INVESTMENT account carrying a −$5,000.00 margin balance rendered "The $0.00 is your current portfolio — the live balance of your investment accounts today" — the reader's balance misstated by $5,000.00, in the flattering direction | `buildRetirementInputs` floors the portfolio at 0 for the projection, and the age-currentAge panel printed that floored output as a measured statement about the accounts — the module's own stated rule ("a clamped output may not print its inputs") applied to the arithmetic but not to the sentence written about it | A clamped output may never be described as a measurement: the composer receives the unclamped value and names the clamp instead of the figure | `retirement-panel.test.ts` › re-review describe (margin-balance branch asserts the new sentence AND `not.toContain('The $0.00 is your current portfolio')`; empty-portfolio and frozen-feed branches locked alongside) |
| 2026-08-11 | O.20d-FU F1 (creep): the gross-vs-net disclosure was silent at its canonical case — a reader who returns a jacket and files it to the app's own "Refund" category got a still-gross bar and no explanation, on precisely the month the sentence exists to explain | the flag keyed on `meta.get(categoryId)?.discretionary`, and `refund` ships as `{group: 'Income', discretionary: false}` — the disclosure tested the category's taxonomy flag rather than the branch that decides whether the credit nets the figure | A disclosure fires on the condition it describes, not on a proxy for it; and it is not widened past that condition (a grocery return neither enters this bar nor is withheld from it) | `insights.test.ts` › "a return filed to the app's own Refund category flags too" + the non-discretionary control ("a return to a NON-discretionary category does not raise the flag") |
| 2026-08-11 | O.20d-FU F2 (panels): all four new drilldowns hardcoded `reconciles: true`, and two passed the headline as its own `sumCents`, so "these rows add up … matched to the penny" was a sentence verifying itself and BreakdownPanel's documented mismatch branch was unreachable at exactly the four newest row lists | the carry-out invariant genuinely holds in the engines, so the call sites asserted the conclusion instead of computing it — removing the guard that would catch a future display-side filter, cap, or row-level change | The fail-loud check is computed from the rows actually rendered, never asserted: `sumCents` reduces the row list, `reconciles` compares it to the figure | the four call sites now reduce + compare (`allocation-drilldown`, `forecast-view`, `net-worth-trend-drilldown`, `lifestyle-creep-chart`); covered by the existing penny-match e2e in `o20d-bars.spec.ts` |
| 2026-08-11 | O.20d-FU F6 (creep): a single loan payment dated in July printed "Loan payments carried on a loan account are excluded" on the FEBRUARY bar, where nothing was excluded | `CreepResult.loanPaymentsExcluded` is one window-wide boolean meaning "the caller handed me a non-empty set", passed to a per-month panel — and vacuous regardless, since `loan-payment` is `discretionary: false` and could never enter a discretionary figure | A per-month panel states only per-month facts; a disclosure that cannot move the figure it sits under is removed, not rescoped | `insights.test.ts` › "the loan-payment sentence is gone: it was window-wide, and vacuous for this figure" (asserts across both refund states) |
| 2026-08-11 | O.20g: a $2,000 merchandise return filed to `refund` counted as INCOME in the lifestyle-creep detector, lifting income growth ~33% and clearing the "Spending is outpacing income" flag — and the /coach copy printed "income grew ~33.3%", a raise that never happened, beside a savings-rate card reporting income unchanged | `detectLifestyleCreep`'s income accumulator added EVERY positive row, while `monthlyFlows` four functions up refuses exactly these (#166: "counting it as income would inflate income AND expenses") — two definitions of an income row on one page | The income series uses the app's ONE income predicate, `isIncomeFlowRow`. A refused positive is DROPPED, never netted into the bar: the bar is deliberately GROSS and its disclosure says so | `insights.test.ts` › "the reported P1: merchandise returns no longer count as income" + "FAIL-OLD, executed: returns big enough to clear the flag under the old rule no longer can" (returns in TWO second-half months — one cannot move a three-month median, and the first version of this lock passed against the old engine for exactly that reason) |
| 2026-08-11 | O.20g: the same gating made the owner's live figure WORSE — the first-half income median fell from $10,604.95 to $0.08, income growth read 70,470,525%, and because `flagged` is a difference the reader was told "no lifestyle drift detected" while their discretionary spending grew ~153% | growth is `(last - first) / first`, so a baseline near zero is not a measurement; `halfGrowth` returned the refusal 0 and nothing downstream could tell it from a genuinely flat income | `incomeMeasured` = the first-half income median is positive AND at least the first-half discretionary median — self-referential, no dollar threshold; `flagged` requires it, and the card gains a third verdict rather than rendering the refusal as an all-clear | `insights.test.ts` › "THE RULE: an income baseline below the discretionary spending it is compared against is refused" + "the same rule catches the divisor blow-up" + "FAIL-OLD, executed: the flag itself is gated, not merely the copy" |
| 2026-08-11 | O.20g critic: a COVERAGE rule (refuse when any window month carries no income row) was built first and was wrong in BOTH directions — 8 cents of monthly savings interest satisfied it while the reader's payroll account was unlinked (the card then asserted "income was flat"), and one missing month vetoed a correct figure for anyone paid ten months a year | a count of covered months was asked to stand in for "is this baseline a real measurement", which it cannot do: the median is already robust to one absent month, and presence of a row says nothing about its size | the rule is about the BASELINE, never attendance — see the entry above; the median's robustness is left intact | `insights.test.ts` › "a SINGLE missing income month does not refuse — the median was already robust to it" (asserts `flagged` stays TRUE) and the 8-cent fixture in "THE RULE…"; both directions also e2e-locked in `coach-creep-verdict.spec.ts` |
| 2026-08-11 | O.20g critic: adding the `watch-creep-not-comparable` review candidate silently DROPPED the Money Review's watch line for every reader in the new state — the recap rendered two rows instead of three and the "Personalized" badge fired because the floor was broken | `selectDeterministic`'s watch chain is a hardcoded `price-increase ?? creep ?? clear`; the new id was added to the closed set and to `buildReviewCandidates` (which also suppresses `watch-clear`) but not to the chain, so the role resolved to `undefined` and was filtered out — and `selectFromOrder` backfills from the same function, so the LLM path could not recover it either | the chain carries the new id; the module's byte-for-byte-with-`generateMoneyReview` contract is re-pinned across the new branch, and a test now asserts every watch id is PRODUCIBLE (the reverse of the closed-set check, without which a dead id is invisible) | `money-review.test.ts` › "O.20g — an unmeasurable window keeps its watch line, and it is not the all-clear" + the `creepNotComparable` row in the A2 byte-parity matrix + "every id in the frozen set is producible by some input" |
| 2026-08-11 | O.20g critic: `reviewCreepSpending` hard-coded "while income is flat" — a clause with no input behind it — and printed "is up ~-10.0%" when the figure fell, rendering beside a card that said income fell 50% | the sentence took the spend figure ALONE, and the fall case is reachable exactly when it renders, because `flagged` is a DIFFERENCE (income falling faster flags a spend figure that itself shrank). The slice fixed the card's sentence first and turned a consistent error into a visible contradiction | it takes the whole `CreepResult` and shares one `growthPhrase` helper with the card, so both sentences about the same two numbers use the same verb; a rounded zero (|bps| < 5, which `pct1` prints as "0.0%") reads "barely moved" rather than asserting a direction with no magnitude | `coach-copy.test.ts` › "the recap line about the SAME two figures cannot contradict the card" — added after an 11-mutation battery found this fix was the ONE with no binding lock |
| 2026-08-11 | O.20g critic: the refusal card's link was labelled "Check what the app counts as income" and pointed at `/transactions?type=income`, which would land the reader on the very February credit the sentence above calls "no income" | `matchesType(t, 'income')` is a SIGN filter (`!isTransfer && amountCents > 0`), not `isIncomeFlowRow` — the label asserted a definition its destination does not implement, and O.20g is precisely the change that made the two diverge | the label describes what the link OPENS ("See the money coming in on your activity") and the body carries the definition | `coach-copy.test.ts` › "the link label does not assert a definition the register does not implement" |
| 2026-08-11 | O.20g: `detectLifestyleCreep` THREW ("cents() requires a safe integer, got NaN") at `windowMonths <= 1` instead of refusing — a crash on an exported API introduced by the slice's own new baseline fields | the compared half is an EMPTY slice there and `median([])` is NaN; the code reasoned that `NaN > 0` is false and never reached that comparison, because `cents()` rejects NaN first | a non-finite median is collapsed to 0 ("no baseline") at the boundary, before it enters a typed money field | `insights.test.ts` › "the degenerate windowMonths=1 refuses on the NaN baseline" (and `critic3-fi-attacks.test.ts`'s pinned NaN-growth hazard still holds) |
| 2026-08-11 | O.20k: the shipped O.20g probe called its keep closure with a single object (`keep({accountId, date})`) where `reconciliationTxnKeepFilter` returns `(accountId, date) => boolean` — the object failed every keyed lookup and the closure returned `true` for every row, so all of DECISIONS #445's probe-computed magnitudes double-counted every reconciled predecessor/successor pair (second-half income median inflated from $32,485.70 to an implied $56,376.42; growth read 70,470,525% instead of 40,607,025.0%) | the call site passed the WRONG ARG SHAPE to a positional closure, and `.mts` probes are invisible to `tsc --noEmit`, so the type error could not surface at any gate — the same silent-no-op class the O.20a critic caught in its probe's first draft | a probe call site is verified against the callee's signature; probe files compile under a `--project` check before their output is trusted (the one-off `tsc --project` compile is the current check; making it a permanent gate is Wave G) | one-off `tsc --noEmit --project` over the probe (exit 0 after the fix, exit 2 with the missing-`currentBalanceCents` type error before) + the re-run against production whose numbers are internally consistent (growth ≡ f(first/second-half medians)); all nine boundary-using probes swept — every other call site already uses the two-arg form |
| 2026-08-11 | Owner: clicking his mortgage on /accounts landed on an EMPTY register — "No transactions match these filters", every control on its default, the dropdown reading "All accounts" while a filter was active | THREE claims false at once: (1) every non-investment account row linked to `/transactions?account=` while `registerRowWhere` scopes to SPENDING_ACCOUNT_TYPES, so 8 of 11 types linked to a page empty BY CONSTRUCTION; (2) the account dropdown was built from rows-present, so the active out-of-scope (or zero-row) filter was invisible — "the controls below say which" was false; (3) the empty state blamed "these filters", a remedy that cannot work when the SET is empty by type exclusion | A link is a claim its destination answers the click; a zero is a claim and must name WHICH zero; a filter with nothing in the bar is a dead end wearing a page | `tests/unit/account-row-destination.test.ts` (register links ⊆ the register's OWN type set, asserted against the import); `register-empty-reason.test.ts` account-axis block (not-here wins over every window branch; unknown; spending-null passthrough); `register-merchant-filter-render.test.tsx` select-truth + empty-state renders (incl. the currency and account-empty branches); `register-account-filter-server.test.ts` (authz: a stranger's id resolves 'unknown'/null, zero-row account in the dropdown); `account-detail-panel.test.tsx` (the REAL-user no-history shape); e2e `no-dead-ends.spec.ts` (loan row expands in place; a hand-added mortgage opens too; a zero-row card names its own zero; deep link names the account in the select + the empty state + a live way out) |
| 2026-08-12 | U.4 critic (data integrity): the net-worth delta beneath the headline on /dashboard and /accounts printed **−$251,200.00** (or **+$50,000.00 in green**, depending only on which account was linked first) for a user who added an account mid-month | `netWorthSeries` points can cover DIFFERENT account sets — U.4's writer claims a month with its first sync, so an account linked afterwards is absent from that month's point — and both surfaces simply subtracted the last two points. Pre-U.4 a live user had ONE point, so `prev` was null and the figure never rendered; the slice brought it into existence | a difference between two figures is a change in the thing measured only when both measure the same set; and a fix that makes a previously-unreachable surface reachable owns that surface's correctness | LOCKED: `netWorthDelta` (src/lib/engine/networth/panel.ts) compares the constituent id sets already carried out of the producing loop and returns `deltaCents: null` with a named reason when they differ; `tests/unit/networth-panel.test.ts` — joined / left / churn / like-for-like / gap cases (the suite previously held the account set constant in every case) |
| 2026-08-12 | U.4 critic (rendered claims): a mortgage whose bank stopped sharing it rendered a monthly column of identical dated balances headed "Recorded balance history", 40px below the amber note saying the bank stopped sharing it and the balance "has not changed since" | U.4 records EVERY account each month — correctly, since the trend's totals must keep counting a frozen balance (L.14) — so the list mixed balances the bank sent with the last one repeated, and nothing on the row said which was which. A flat MEASURED line reads as evidence the connection works | a disclosure elsewhere on the page cannot qualify a figure: the fact must ride the row that carries the money | LOCKED: `feedDroppedAt` travels in `AccountDetailView`; rows dated after it render "carried forward" plus a note naming the last date anything was read; `tests/unit/account-detail-panel.test.tsx` — "rows recorded after the feed went quiet are marked carried-forward, never printed as readings" + the live-feed negative case |
| 2026-08-12 | U.4 critic (rendered claims): the exported Net Worth PDF — the one artifact that leaves the app and is handed to a lender — headed its rows "Trend (month-end)" while every row of a live user's export is the day a balance was read | the heading was written when `prisma/seed.ts` was the only writer of snapshots and every row in existence WAS a month-end; U.4 changed the shape and the sweep stopped at the two on-screen sentences | closing a gap makes the copy that described the old world false, and the sweep must cover every surface that prints the figure — including the ones with no way to correct themselves afterwards | LOCKED: `src/lib/export.ts` heading is now "Trend (recorded balances)" (each row already carries its own date); the same slice's shared `NET_WORTH_TREND_BASIS` covers the two in-app surfaces, /accounts having previously had no basis sentence at all |
| 2026-08-12 | U.6: a feed reclassifying an account across the asset/liability line rewrote the sign of EVERY balance already recorded for it — one stored row of 1,000,000 rendering `$10,000.00` as CHECKING and `−$10,000.00` as CREDIT, on /dashboard, /accounts, the drilldown and the exported PDF, with nothing on any surface saying a past figure had moved | `netWorthSeries` signed each STORED snapshot with `isLiabilityType(account.type)` — the account's CURRENT, mutable type — and this repo states in its own code (`reconcile-boundary.ts`) that both providers rewrite `Account.type` on every ordinary sync. The detail panel had the identical defect on a second surface, painting every history row from the account's current class. Unreachable before U.4 (live users had no rows) and never on the demo (seeded types never change) | a stored observation must carry the fact it was observed under; the app's CLASSIFICATION of that fact stays a read-time rule with one author, so a correction to the rule still reaches history, but the fact itself may never be re-derived from mutable state | LOCKED: `BalanceSnapshot.accountType` (nullable — NULL means "recorded before the column existed" and is the ONLY case that falls back to the current type); `tests/unit/networth-series.test.ts` › "signs a stored row by the class it recorded" + "a reclassification moves no past point" + the null/`''` fallback + the negative-asset case; `tests/unit/account-detail-panel.test.tsx` › the per-row sign and its marker; `tests/unit/balance-history-server.test.ts` › "records the type the account had THEN"; `tests/unit/seed.test.ts` › every seeded row carries its class; e2e `no-dead-ends.spec.ts` at 380px |
| 2026-08-12 | U.6 critic (data integrity): the slice's own backfill of pre-U.6 rows stamped a class it could DISPROVE from its own table — asserting an account was a credit card in June and July while a row three lines down proved it was still a checking account in August — and printed success | it filled every NULL with the account's type TODAY, grouped only by `accountId`, never reading the rows that already carried a recorded class for the same account. The evidence to refuse was one query away and unused (the `a-regenerator-must-refuse-what-it-cannot-parse` shape) | NULL is "we don't know", and converting it into a value indistinguishable from an observation destroys the only evidence that would let the app ever say so — measured through the real engine as a **$40,000.00 swing**, because an un-backfilled NULL row SELF-HEALS when the misclassification is corrected and a stamped row never can | RESOLVED BY DELETION: `scripts/backfill-snapshot-account-type.ts` removed; the schema comment now records WHY these rows are never backfilled; `tests/unit/account-detail-panel.test.tsx` › "names the rows whose class was never recorded" (the panel says so instead) and "a row written before the column existed … claims nothing" |
| 2026-08-12 | U.6 critic (rendered claims): the account panel's new note asserted "Your net worth trend counts every balance the way it was recorded" directly above rows the trend counts by the account's CURRENT class, and called carried-forward rows balances that "were recorded" four lines under the note saying nothing had been read since a date | two independent overclaims in one paragraph: the absolute ignored the slice's own NULL fallback (the `every account` overclaim `netWorthPointBasis` was rewritten to avoid, re-introduced two files away), and the reclassified count and the carried-forward count were computed by filters that knew nothing about each other | a note that qualifies a figure inherits every claim in its sentence: an absolute must be true of the whole list it sits under, and a row the app admits it never read may not be described as one it read | LOCKED: `tests/unit/account-detail-panel.test.tsx` › "a carried-forward row is never called a balance that was READ" (asserts "One balance here was read", NOT "3 balances") and the note's `not.toContain('every balance')`; the never-recorded rows get their own note, raised only beside a known reclassification |
| 2026-08-12 | U.6 critic (money): `netWorthDelta` deleted a true **+$2,000.00** figure and printed a class-change refusal, because a paid-off card sitting at **$0.00** had been moved CREDIT → OTHER_ASSET by its feed | the new guard counted any constituent whose class differed, with no reference to its balance — but the term a class change distorts is exactly **2 × the PREVIOUS balance**, which at $0.00 is nothing. A false refusal is as much a defect as a false number, and paid-off cards and closed accounts are routine | a guard against a distorted figure must test whether the figure actually MOVES, not whether the condition that can distort it occurred | LOCKED: `tests/unit/networth-panel.test.ts` › "does NOT refuse over a $0.00 account that changed sides" (expects the real $2,000.00) and "still refuses when the moved account carried a real balance on the earlier point" |
| 2026-08-12 | U.9: two stale rows continued onto ONE live account (the shape the non-unique `successorAccountId` exists for) BOTH survived every date on or before both cutovers, so one real $5,000.00 savings account contributed $10,000.00 to the net-worth trend — while U.5's own panel note on the same screen said "so the same account is not counted twice" | `keepsSnapshot` decided collisions by walking `upstreamsOf`/`downstreamsOf` only. Siblings are neither to each other, so each was compared against the successor (which it correctly beat) and never against its twin. Every rule built from those two walks inherits the blind spot; the statements rule (A-F6) had already hit it and solved it locally, and nothing generalised that lesson to the other three | a link asserts "these two rows are the same real account" and that assertion is TRANSITIVE, so the unit of de-duplication is the connected COMPONENT, never the chain — a rule expressed as two directional walks silently excludes every node that is related but not ordered | LOCKED: component-scoped winner in `src/lib/engine/account/reconcile-boundary.ts` (exactly one row per (component, date), chosen only among rows that exist on that date); `tests/unit/reconcile-boundary.test.ts` › the "U.9 sibling predecessors" block (double-count, between-cutovers, lone-observation control, after-both control, order-independence, equal-cutover tiebreak); `tests/unit/account-detail-reconciled.test.ts` › the SIBLINGS block, real Prisma, asserting the TREND'S OWN constituents and the panel verdict agree |
| 2026-08-12 | U.9 critics (rendered claims, ×2 independent): the /accounts detail panel note was FOUR wrong claims in one sentence — "both sides recorded a balance on that date" where the reader's own counterpart had recorded nothing; a singular "another one" / "The combination" on the only panel that can render it, which is the live row and may continue SEVERAL old ones; "Aimplifi counts one of them", whose nearest antecedent is the uncounted balances in the same sentence, none of which is counted; and an unqualified "the same ACCOUNT is not counted twice" that certifies spending, which U.11 measures as still double | the sentence was written for a two-account pair and every clause inherited that scope. NOTE the causal correction: the "both sides" clause was ALREADY false for chains when U.5 shipped (the winner can be two links up) — U.9 widened the rule to whole components and added the sibling shape, but did not create that defect, and the first draft of this row wrongly said it did. The other three clauses had never been audited against a >2-account component at all | a disclosure inherits the scope of the rule it explains, and a qualifying sentence is several claims wearing one: widening a rule from "a pair" to "a component" falsifies every clause that names the pair, counts it, or points at it — and a claim may only certify the surface its rule actually covers, never the account's every figure | LOCKED: `uncountedBalancesNote` rewritten (states what DID happen, "at least one other account", "Your combined accounts are listed", "no balance is counted twice"); four assertions in `tests/unit/balance-history-view.test.ts` (absence of "both sides", of the singulars, of the unqualified account claim, and of the vacuous modal "can describe"), plus `tests/unit/account-detail-panel.test.tsx` and the `no-dead-ends.spec.ts` e2e updated to the narrowed claim |
| 2026-08-12 | U.9 critics (money + rendered, INDEPENDENTLY): with a chain whose two cutovers are EQUAL, the new component rule handed the date to the mid-chain account — whose ownership window `(cut..cut]` is empty — and which of the two won was decided by cuid sort order. Measured: the same data produced a $4,000.00 or a $9,000.00 trend point (a second repro moved $1,000.00) depending only on how two opaque ids happened to sort | the winner comparator ranked by tier then cutover then ACCOUNT ID, with no notion of chain position, while its own comment four lines above cited the half-open rule "A owns [..cutAB], B owns (cutAB..cutBC]". Equal cutovers are valid write-path data — the confirm action refuses only a strictly EARLIER downstream cutover, and `handoverDate` returns today for a successor with no transactions, so two same-day combines produce it. Every pre-existing chain fixture uses DISTINCT cutovers, so no test could see it, and the slice's own 210,120-case probe asserted only cardinality and order-independence, never WHICH row wins | a tiebreak inside a money rule is part of the rule: ranking by an identifier the user cannot see makes the figure nondeterministic across users with identical data. And an invariant probe that counts survivors without checking their identity is narrower than the claim it is quoted as proving | LOCKED: `depth` (links to the terminal) breaks an equal-cutover tie before account id — greater depth wins before a cutover, lesser after, id only for true siblings; `tests/unit/reconcile-boundary.test.ts` › "a CHAIN whose two cutovers are equal is decided by chain position, NEVER by account id" runs BOTH id orders, plus the post-cutover mirror |
| 2026-08-12 | U.9 critic (money, finding 3): a predecessor with TWO successors produced two survivors for one real account — the U.9 double-count through a different door | `chainMaps` builds `succOf` with `new Map(links.map(...))`, so the LAST edge wins and the other successor silently keys its own component. `predecessorAccountId @unique` makes the shape unreachable from the database, and the new component key's soundness rests on out-degree <= 1 — an invariant the engine borrowed from the schema while re-checking every other one (cycles, monotonicity) at read time | when a fix makes a borrowed invariant load-bearing for money, it stops being someone else's guarantee: guard it where you rely on it (`a-guard-must-read-what-it-guards`), with the failure direction the file already uses — inert, so everything counts fully | LOCKED: out-degree guard in `effectiveReconciliationLinks` (every link out of a forked predecessor drops); `tests/unit/reconcile-boundary.test.ts` › "a predecessor with TWO successors goes inert" asserts the empty effective set, the R8 exact-input-reference fast path, and that a well-formed sibling pair is untouched |
| 2026-08-12 | U.14: the duplicate detector proposed three distinct Schwab 529 plans as "the same account" as one Vanguard 401k on the single shared token "plan", and two different cardholders' Chase cards on "lee" — and nine such pairs had ALREADY been confirmed in the owner's production data, each asserting an identity between accounts that are not the same | `masksDiffer` — the veto that disqualifies the weak NAME signal when two rows carry different last-4s — read the `mask` COLUMN, and SimpleFIN never populates it. So `!!lo.mask && !!hi.mask` is false for every SimpleFIN→Plaid pair, and the veto was structurally inert across exactly the provider migration this whole feature exists for. The number was sitting in the NAME the whole time ("Charles Schwab US Rollover IRA ...191 (191)"), and `maskFromName` could not read it either: it parses only FOUR-digit groups, while SimpleFIN truncates to as few as two | a veto that reads one provider's field is not a veto — check the fact the way EVERY feed renders it, and correspond truncated identifiers by SUFFIX, never equality (Schwab "…383" and Plaid mask "7383" are one account; equality condemned a genuine $898,889.99 pair during this slice's own measurement). Scope stays where it was: the weak name signal only, so mask and identical-balance matches are untouched | LOCKED: `advertisedAccountNumbers` + `accountNumbersConflict` in `src/lib/engine/account/duplicates.ts`; `tests/unit/account-duplicates.test.ts` › the "U.14" blocks — 3 refusal locks proven fail-old by reverting the one line (529s, Roth-vs-Roth on "ira"/"roth", two cardholders on a surname), plus 5 must-not-change controls (suffix correspondence, absence-not-difference, identical-balance and last-4 signals surviving a conflict, product names like "529"/"401k" never read as account numbers). MEASURED before shipping in `scripts/audit-probes/u11k-which-veto-catches-which.mts`: 9 of 9 wrong pairs caught, 0 of 8 independently-judged-genuine links suppressed |
| 2026-08-12 | U.14 REVERTED same session: widening the `masksDiffer` veto to read account numbers out of NAMES (a) hid a genuine duplicate — `Roth IRA (2021)` parsed as account number "2021", so a real cross-provider double stopped being flagged, a SILENT double-count — and (b) turned a withheld L.9 ambiguity into a one-click Combine, because suppressing one candidate makes `detectReconciliationCandidates` see `list.length === 1` | the scope claim ("gates the weak name signal only") was TRUE of the boolean and false of the consequence: `duplicateSignals` also feeds the candidate SET, and a set's CARDINALITY is a branch elsewhere. Plus the widened parser read TWO digits where the `#292`/F1/F2-rejected `maskFromName` read four — strictly more misreadings than the rule that had been banned, with the comment forbidding it still three lines above | a predicate's blast radius is every CONSUMER of what it changes, not the lines that read the variable — when it feeds a collection, ask what reads the collection's SIZE. And evidence too dangerous to GATE on is not too dangerous to SHOW: the measurement was sound, its POSITION was wrong | LOCKED: `masksDiffer` restored to the mask column; the parser (year range excluded, prefix as well as suffix correspondence, whitespace after bullet masks) now feeds ONLY the advisory `link-audit.ts`; `tests/e2e/reconcile.spec.ts:237` (the test that failed in CI run 31627590689) 4/4; `tests/unit/account-duplicates.test.ts` U.14 blocks rebuilt after the critic's mutation test survived 5 of 8 regex mutations — every prefix alternative, both year-range edges, the truncation-prefix case and the space-delimited mask now bound individually |
| 2026-08-12 | U.13: a genuine +$2,086.40 "Deposit Mobile Banking" on the owner's live Investor Checking was silently dropped from the register, budgets, reports AND the tax export — dated exactly the cutover, reported only by the live Plaid feed, with no surviving row replacing it. Measured, not inferred: of 709 rows the shipped R1 rule dropped, 706 were true duplicates and this one was real money leaving every surface | `txnKeepRule` dropped every successor row inside the predecessor's claim `[first, min(cutover, last)]` **inclusive of the end**, whether or not the predecessor actually had that row. R1's stated invariant — "exactly one side owns each date, no overlap, no gap" — is unachievable at day granularity: a handover is an instant INSIDE a day, the retiring feed stops partway through while the live one covers all of it, and every business date here is a calendar date with no time. So neither side's silence that day proves anything, and BOTH whole-day awards lose money — measured at $2,086.40 (predecessor owns) versus $25,574.13 across 24 rows (successor owns, the direction the shape suggests) | the claim span is half-open at BOTH ends, `[first, claimEnd)`: no side owns the handover day, so neither side's rows are dropped for it. The cost is bounded and was measured too — 9 rows / $374.40 of VISIBLE duplication, which is the failure direction this engine already required of itself ("a visible, advisory-covered double, never a silent loss"). The task row's prescribed remedy (prove a counterpart by amount ±3 days) was refused on evidence: at ±1 day the corpus has ZERO mid-span losses, so it addresses a problem the data does not have, and it is not expressible in an `(accountId, date)` predicate with ~20 call sites, several applying it to windowed row sets that do not hold the claiming side's rows | LOCKED: one comparison in `src/lib/engine/account/reconcile-boundary.ts` `txnKeepRule` (`<= 0` → `< 0`); `tests/unit/reconcile-boundary.test.ts` › `test_regression__u13_handover_day_never_silently_drops_a_row` (the production shape with the real numbers — fail-old proven by reverting the comparison, the deposit absent entirely), plus the rewritten R1 union test which now locks NO GAP (every input date survives) and that the doubled-date set is exactly the handover day, catching any release that widens into the claim; `tests/unit/connection-history-depth-server.test.ts` › the U.13 handover-day depth lock. Sibling composition verified unbroken (`scripts/audit-probes/u13c-which-rows-moved.mts`: 1 of 10 boundary rows still correctly dropped, being strictly inside a sibling's claim) |
| 2026-08-12 | U.16: every spending surface counted U.13's deliberately-released handover day and said nothing, and the glass-box drilldown actively CERTIFIED it — two identical lines listed one above the other with "matched to the penny" printed beneath them, which reads as confirmation that both belong rather than as a statement about arithmetic. The tax export was the only artifact that disclosed it (U.13 fixed that one because the CSV leaves the app) | `BREAKDOWN_BASIS` stayed literally true — both rows ARE counted and the panel does list both — so nothing was false, and nothing said the one thing the reader needed. The released dates existed (`getReconciliationHandoverDates`, built for the tax export) but reached no panel, no page and no Ask answer. The repo had already solved the identical shape once for card payments (`cardDuplicateTraceBasis`, whose comment says outright "two rows for one card therefore read as CONFIRMATION"); there was simply no handover-day equivalent | a figure that can legitimately count one real transaction more than once must say so ON the surface that certifies it, and the disclosure must be scoped to the figure it qualifies — a count summed over rows the figure excludes is a NEW false claim wearing a fix's clothes. Where a fact is disclosed on a surface with no row list, it needs its own author: the panel sentence's "N rows here", "the figure above" and the reassurance about the tally are all false in an answer that prints one number | LOCKED: `breakdownHandoverDayCopy` + `handoverDayAnswerNote` + `onHandoverDay`/`countedOnHandoverDays` threaded through `category-breakdown.ts`, `month-flow-breakdown.ts`, `fi/insights.ts` (creep), `trends/trends.ts` (new merchants) and `reports/reports.ts` (per-category AND breakdown-wide), fed by `getReconciliationHandoverDates` from `server/reports.ts`, `server/trends.ts`, `server/coach.ts`, `server/assistant.ts` and `/budgets`; `tests/unit/u16-handover-disclosure.test.ts` (19 locks; the row-flag, the panel sentence and the Ask scoping each proven fail-old by sabotage) and `tests/e2e/handover-day-disclosure.spec.ts` (a seeded combined pair whose handover day carries a real duplicate — 3 rows not 4, exactly 2 marked, the sentence present; fail-old proven by blanking the marker and REBUILDING, since an e2e tests the last `next build`) |
| 2026-08-12 | U.16 critic (money, executed): Ask's `spend_total` qualified a $20.00 figure containing NO released row with "2 … fall on a day one of your combined accounts was changing connections" | the breakdown-wide `countedOnHandoverDays` was accumulated in the raw row pass, but `spendingByCategory` DROPS any category whose net is `<= 0` and `totalCents` sums only the survivors. A handover-day purchase more than cancelled by a refund therefore left the figure entirely while still being counted in the sentence beside it | a disclosure's count and the figure it qualifies must be summed from the SAME array — counting before a filter the figure applies after is the false-scope defect one level up from the one the slice was already guarding against | LOCKED: the count is now `byCategory.reduce(...)` over the surviving categories; `tests/unit/u16-handover-disclosure.test.ts` › `regression__u16_count_excludes_categories_the_figure_drops` (fail-old: the critic's own repro asserted the wrong number and now fails against the fix) |
| 2026-08-12 | U.16 critic (rendered claims + my own re-read): the sentence called every released row a "charge" and promised "Spending in Reports lists those rows and marks them" | (a) a REFUND can fall on a handover day too, and a duplicated refund pushes a spending figure DOWN — "charge" is false of it in both senses, and a reader would hunt for a purchase that is not there; (b) /reports' category table is ALWAYS the current month (`spentSoFarWindow`, and its only URL parameter sets the CHART range) while an Ask timeframe is whatever the reader said, so the pointer sent anyone asking about last month or last quarter to a page that could not show them the rows it named | a noun in a money sentence is a claim about direction as well as kind; and a disclosure may not promise a destination it has not checked can serve the reader's own period (`a-disclosure-written-for-a-page-is-false-in-an-email`) | LOCKED: "transaction" throughout both sentences (three sites, including "would lose transactions only one connection saw" and the tally clause's "two lines are one transaction"), the Reports pointer replaced with the always-true no-adjustment clause; `regression__u16_handover_noun_covers_a_refund` asserts `/charge/i` appears in NEITHER sentence, and `regression__u16_answer_note_promises_no_surface_it_cannot_deliver` asserts the pointer is gone |
| 2026-08-12 | U.16 critic (money, executed): Ask's `top_categories` answer printed "Total <period>: $X." — a figure a released handover day can inflate — with no disclosure at all, alongside a headline naming the top category's own amount | the slice disclosed `spend_total` and `spend_by_category` because the task row named them, and stopped there; `answerTopCategories` states a period total from the same breakdown and was never audited. The task row's own list was the boundary of the sweep, which is precisely the `the-narrowing-you-did-not-list` failure | a sweep bounded by the surfaces someone else listed is not a sweep — enumerate by "which figures can contain this?", not by the ticket's bullet points | LOCKED: `answerTopCategories` carries the same note, scoped to the total it prints; `tests/unit/u16-handover-disclosure.test.ts` › `regression__u16_top_categories_qualifies_the_total_it_prints`, which also asserts byte-identical silence for a reader with no combined accounts |
| 2026-08-12 | U.16 critic cycle 2 (money, executed): the disclosure marked and counted rows on accounts that are in NO combined pair. Measured: six grocery rows on the handover day, two of them from the pair, and the panel marked all six and announced "6 rows here fall on a day one of your combined accounts was changing connections" | every consumer tested `handoverDates.has(t.date)` — a bare DATE match. `reconciliationHandoverDates` returns unscoped `YYYY-MM-DD` strings, which is correct for its two ORIGINAL consumers (the tax CSV has no account column; cadence detection folds by component) and wrong for a per-row marker: a released day is an ordinary shopping day on every other account the reader owns. `ReportTxn` carried no `accountId`, so the engine could not scope it as written | a fact inherited from another feature's helper inherits that feature's SCOPE — before reusing a set, ask what it is a set OF. The marker is the affordance that lets a reader find the two identical lines; marking four unrelated rows destroys exactly the thing it was added for | LOCKED: `handoverKey(accountId, date)` + `reconciliationHandoverKeys` (both sides of each effective link at its own released date) in `reconcile-boundary.ts`, `getReconciliationHandoverKeys` in `server/reconciliation.ts`, `ReportTxn.accountId`, and `toTrendTxns` now carrying `accountId` (without it /trends marked nothing at all — a hand-built payload dropping the one field a new feature needs, `the-narrowing-you-did-not-list`); `tests/unit/u16-handover-disclosure.test.ts` › `regression__u16_marker_is_scoped_to_the_pair_not_the_date`, fail-old proven by reverting to a date-only match |
| 2026-08-12 | U.16 critic cycle 2 (money P0, executed): the slice asserted in a code comment that "a released day can only make a figure too high", and both sentences encoded that one direction. A RETURN both connections reported subtracts twice — measured: one $100 purchase and one real $30 return doubled renders "You spent $40.00" against a true $70.00, with the only sentence beside it saying the figure may be too high | the release is a rule about a DATE, not a sign, and `spendContributionCents` negates — so a duplicated refund pushes a spending figure DOWN. Nothing in the slice distinguished the two, and the tax export made the identical one-directional claim ("the only sentence in this file about a total being too HIGH") | a disclosure that names the WRONG direction is worse than silence: a reader auditing a suspiciously low number is actively steered away from the cause | LOCKED: both sentences and the tax export now say "too high when they are purchases, and too LOW when they are returns"; `regression__u16_states_both_directions` |
| 2026-08-12 | U.16 critic cycle 2 (both critics independently): Ask's own drilldown — the panel a reader opens by tapping "Tap to see the transactions behind this number", four lines under the slice's own new disclosure — listed the two identical rows unmarked under "✓ 3 transactions add up to $130.00" | `assistant/trace.ts` was untouched by the slice: `traceForKind` never received the handover set, its three spend traces called `spendingByCategory` without it, and `TraceRow` had no field to mark. The slice threaded the ANSWER path in `server/assistant.ts` and left the TRACE path four files over with its own un-fed call | when a slice adds a disclosure to a figure, every path that RE-DERIVES that figure needs it too — a trace is a second selector over the same rows, and it is the one that prints a green check | LOCKED: `TraceRow.onHandoverDay`, `handoverBasis()` on all three spend traces, `TraceInput.handoverKeys` fed from `server/assistant.ts`, and the `(connection changeover)` marker rendered in `ask-view.tsx` beside the cited row |
| 2026-08-12 | U.16 critic cycle 2 (rendered claims): the tally clause said "These rows still add up to the figure above" over a ONE-row panel, where `BreakdownPanel` deliberately prints "This amount is the whole figure." and suppresses the penny-match — a sentence asserting a tally the panel had just declined to state, in the plural, over one row. Its antecedent was also wrong at N < panel rows: "these rows" attaches to the marked subset, reading as "the marked rows alone sum to the figure" | the clause was gated on `reconciles` alone, which is true on a single-row panel; the panel's own one-row branch is a separate rule the engine did not know about | a clause about what a surface PRINTS must be gated on whether that surface prints it, not on whether the underlying fact is true | LOCKED: the parameter is renamed `statesATally` (`reconciles && rows.length > 1`) at all four panel call sites, the clause now names "The rows in this panel"; `regression__u16_tally_clause_needs_a_tally_on_screen` |
| 2026-08-12 | U.16 critic cycle 2 (rendered claims): `combineSuccessFlash`'s PARTIAL branch still promised "2 accounts now count once" — the exact sentence U.13 had requalified in the success branch eight lines above, in the same function | U.13 corrected the branch it was looking at. The partial branch is the one a reader reaches when something already went wrong, so it is the worse place to over-promise | when a rule is requalified, every branch of the function that states it is in scope — a fix applied to the branch you were reading is a fix half-applied | LOCKED: the partial branch now uses the success branch's wording ("one balance instead of two"); `tests/unit/combine-connections-copy.test.ts` 23/23 |
| 2026-08-12 | U.23: the transactions CSV — the file U.19 had just certified as disclosing the one deliberate double — double-counted EVERY split and shipped rows in currencies the app cannot convert into a column of dollars. Measured against a real database: a $100.00 purchase split 60/40 plus one −€99.00 row exported as 4 rows summing −$299.00 where the register shows 2 rows summing −$100.00 | the route built its OWN where-clause (`account: { userId, type: { in: SPENDING } }`) three lines above a comment claiming "the exported ledger must match the in-app register". It was missing `isSplitParent: false`, so the split PARENT — the row the schema calls "excluded from ALL sums" — exported beside the children carrying the real amounts; and missing the `#135` currency guard, so rows the register withholds for having no exchange rate shipped unlabelled. Pre-existing: the clause predates U.19 and that slice's diff never touched it — but U.19 then added a disclosure asserting the changeover double was the disclosed one, while the larger, silent double rode the same file | a claim of parity is a claim about an EXPRESSION, not an intention: the only way a second surface matches the first is to run the first's own clause. And withholding is not the end of the obligation — a file that leaves the app is the last place a silent omission is affordable, so the guard's complement must be disclosed IN the artifact, by an author scoped to it (the sibling notes say a FIGURE excludes accounts; only this one says the transactions are not in the file) | LOCKED: `where: registerRowWhere(userId)` in `src/app/api/export/route.ts`; `getWithheldRegisterAccountSummary` in `src/server/transactions.ts`, built by destructuring and NEGATING `registerAccountWhere`'s own currency clause so withhold and disclosure cannot drift, scoped to spending accounts that actually hold an exportable row (a euro BROKERAGE is out of this file for reasons unrelated to currency — the U.16 panels scope lesson); `withheldExportNote` in `currency.ts` (a fourth author, every singular/plural/opaque-token branch locked); `transactionsToCsv`'s second parameter REQUIRED, never optional-defaulting-to-silence, on the U.19 precedent. `tests/unit/u23-export-register-parity.test.ts` 17/17 — the critic's repro rebuilt against a real Prisma DB, with parity asserted BY CONSTRUCTION against `getTransactions` rather than a hardcoded row count; both halves proven fail-old by sabotage (old clause → 5 red, the exact 4-rows/−$299.00 shape; suppressed note → 4 red). `tests/e2e/transaction-detail.spec.ts` extends the existing UI split with the export: the container's −212.40 must be absent and the column must sum to the two purchases the test itself entered (the old clause summed −443.45) |
| 2026-08-13 | U.26 (opened by the U.23 money critic, MEASURED against a real database): the transactions CSV exported one $100.00 purchase, one $1,200.00 row the reader had marked "not my spending" and one $2,000.00 transfer as **3 rows summing −$3,300.00**, while the register's own `TxnSummary` over those same three rows reported **$100.00 of money out** and `excludedCount: 1` on screen — a 33× disagreement. U.25, from the same critic pass: the file's first line is its header, so it stated its basis nowhere, while U.23's currency note named ONE of the four reasons it is incomplete and only for the rare reader who owns a non-USD account | ROW-SET parity was intact (U.23 made the route run `registerRowWhere`), so this was never a parity defect: the file carried no column for `excludeFromTotals` or `isTransfer` and emitted no note, and those are exactly the two gates `summarizeTransactions` uses to keep a row out of the in/out/net tiles (`query.ts:415-419`). So the one act the file exists for — summing the amount column — could not reproduce ANY figure in the app, and nothing in the file said why. The reader had set one of the two flags BY HAND and could see the badge on screen; the export dropped the fact on the way out | a file that leaves the app must carry the reasons its own numbers differ from the app's, because the app is not there to be asked. A shared row set is not a shared basis: parity of WHICH ROWS is only half of it, and the other half — which rows COUNT — has to travel as data on the row, not as knowledge the reader is assumed to still have | LOCKED: `excluded_from_totals` and `transfer` appended as UNCONDITIONAL columns (appended, never inserted — a reader's saved script indexes this file by position); both REQUIRED on `ExportTxn` on the U.19/U.23 precedent, read straight off the Prisma row in `route.ts` so nothing re-derives which rows count; `excludedTransferCsvNote` assembled from the flags actually PRESENT (the `a-disclosure-gated-to-the-loudest-branch` rule applied forwards: an AND-gate over both flags reddens 5 locks), direction-free and promising no equality; `BASIS_CSV_NOTE` unconditional, stating a RULE with no enumeration in it; the note padding derived from the header so schema and padding cannot drift. `tests/unit/u25-u26-export-basis-and-flags.test.ts` 19/19 incl. the critic's measurement rebuilt against a real Prisma DB and every row's mark asserted BY CONSTRUCTION against `getTransactions`; `tests/e2e/action-menu.spec.ts` drives the exclusion through the real action menu and measures the file's unmarked rows against the register's own tile ($85.00 → $45.00); FIVE sabotage proofs run and reverted (columns unmarked → 2 red; basis note suppressed → 10 red; note AND-gated → 5 red; direction clause restored → 2 red; the whole pre-critic first draft of both notes restored → 9 red). TWO fresh-context critics then found ZERO defects in the columns, the arithmetic, the padding or the gate logic (financial correctness 9/10, structural integrity 10/10 across all 16 note branches with an adversarial account name) and SIX in the COPY, two of them executed independently by both: the basis note asserted omissions the reader may not have (false at 2 accounts of 2, and false for the demo's own 847-row file); "left out of the spending, income and net totals it shows" was false live on the demo, where an auto-loan ACH marked `transfer,yes` is printed by /spending-plan as "CarMax Auto Finance $385.00/mo" inside a $3,096.72 Fixed figure; the transfer clause promised a counterpart row that need not exist (`isTransfer` is descriptor evidence, so a reader who never added their car loan owns no other account); "Account balances count every row either way" is false for a hand-entered row; and the excluded clause was not sign-neutral. Each fixed and locked by a named regression test — the lesson being that a disclosure can pass every structural test, a full verify and a green CI gate while asserting five things the app does not do |
| 2026-08-13 | U.27 (opened by the U.23 rendered-claims critic): DECISIONS #141 standardized the currency-withhold family on "not in U.S. dollars", and four more authors — `household-copy.ts` (x2), `keyword-rules.ts`, a `money.ts` docblock — had independently drifted to the informal "US dollars"/"US dollar". Separately, `formatWithheldCurrencies` rendered a mixed printable+opaque currency list as "EUR and others", which parses as "EUR, and other ACCOUNTS" beside sentences that already talk about accounts | the standard lived in the STRING #141 shipped, not in a shared constant every caller is forced through, so nothing stopped a sixth author from writing the same fact in the app's spoken register instead of its copy register. The ambiguous phrase was a scope bug in miniature: "others" with no noun inherits whatever noun is nearest, and the nearest noun on every rendered sentence was "accounts", not "currencies" | a family's copy standard, once shipped, needs an enforcement mechanism that outlives the slice that set it — a docblock only informs an author who reads it first. And a list-completion word ("others", "and more", "etc.") is a noun-less pointer; state the noun it stands for or the reader's own sentence supplies one for you | LOCKED: `tests/unit/u27-currency-copy-drift.test.ts` — a repo-wide sweep over every `src/**/*.{ts,tsx}` file for the bare `\bUS dollars?\b` pattern (word-boundaried so "U.S. dollars" itself can never match), so the NEXT drift fails a test naming what it did rather than waiting for a seventh critic; plus direct assertions that `withheldBannerCopy` and `withheldExportNote` both render "and other currencies", never bare "and others". `tests/unit/currency.test.ts`'s two "EUR and others" locks updated to the new phrase. Fail-old proven for both by direct sabotage (household-copy.ts reverted → 1 red; currency.ts reverted → 3 red across two files) |
| 2026-08-13 | U.24 (opened by the U.19–U.22 critic, P2-3): /calendar summed BOTH copies of a released handover day into its day tiles and month totals — one real $50.00 purchase reported by a retiring feed and the live one that replaced it prints $100.00 of money out — and the page contained no handover vocabulary at all. Every sibling spending surface had been disclosing this since U.16; this was the last silent one. Then, inside the fix itself, two fresh-context critics INDEPENDENTLY found the new day marker asserting "counted on both connections' records" as a flat fact, four lines under this slice's own comment saying it deliberately makes no such claim | the fact was dropped at a NARROWING: `PostedTxnLike` is a lean row shape carrying "exactly what the shared summarize needs", and `TotalableTxn.onHandoverDay` was OPTIONAL with a docblock naming that very shape as its justification — so `summarizeTransactions` read an absent value as "not released" and returned 0 forever, with the silence documented as a design decision rather than showing up as a gap. The marker defect had its own root: the handover keys are minted per link from the cutover date alone, so a released day on which only ONE connection reported anything is marked too, and the draft turned a fact about a DATE into a claim about DUPLICATION | a fact that must survive a narrowing has to be REQUIRED on the narrow type, not optional on the shared one — optionality moves the question from the compiler to the next author, and three slices in a row answered it wrong by not being asked. And a disclosure's MARKER is bound by the same honesty rule as its sentence: if the sentence states the doubling conditionally because it is not knowable from the dates, a marker that states it flatly is the same falsehood with fewer words — which is why a spec asserting a testid is VISIBLE, without reading its text, cannot gate copy | LOCKED: `PostedTxnLike.onHandoverDay` REQUIRED (tsc enumerated every builder — exactly one in production, one test helper), resolved in `getPostedCalendarRows` where `accountId` lives so the unit stays the (account, day) PAIR; `countedOnHandoverDays` on the day and the month read off the SAME `summarizeTransactions` calls the figures are summed from; `handoverDayAmountsNote` reused (no seventh author) and moved into `CardDescription` above the projected Expected line, whose figures hold no released row and were stealing the "these amounts" antecedent; marker restated to the unconditionally-true keep. 8 unit tests in `calendar-posted.test.ts` + 2 in `calendar-posted-server.test.ts` (a real reconciled pair with a THIRD account holding a row on the same date, plus calendar-vs-register count parity — the K.1 gate extended); `handover-day-disclosure.spec.ts` gains a /calendar test asserting the marker's TEXT and the note's DOM ORDER, and the no-combined-accounts control gains its /calendar half. FOUR sabotage proofs: engine count zeroed → 3 red; server flag forced false → 2 red; scoping regressed to bare-date → 2 red (both the scoping control and the parity gate); note suppressed on a REBUILT server → e2e red |
| 2026-08-13 | U.33 (opened by the U.31 critic): two loaders still read the reconciliation link table more than once per artifact — `getTaxExport` twice (concurrently: `taxRows` fetching the keep for itself while `getReconciliationHandoverDates` re-fetched the same links), and `refreshRecurringForUser` **four** times, not the two the row named: the keep, the released dates, `activeTerminalSuccessorMap` feeding the same `collapseHandoverDuplicates` call, and `activeTerminalSuccessorMap` AGAIN fifty lines later where `new Set(terminalOf.keys())` decides the account scope for the rows this function WRITES | U.31 consolidated the keep and the (account, day) keys and stopped there, leaving `getReconciliationHandoverDates` holding the last hand-rolled copy of the links + accounts + spans triple — the same three queries written out a third time — and never touching the terminal map at all. So a confirm or an undo committing between two awaits could collapse duplicates against one set of links while deciding account scope against another, and in `refreshRecurringForUser` that disagreement is not rendered, it is persisted as `RecurringSeries` + `ScheduledTransaction` rows feeding forecast, the calendar, the spending plan and the Cash-Needed Engine | a consolidation is finished when the READ COUNT is one, not when the functions the row named have been converted — and the count in a row is the count someone noticed, so re-measure it before trusting the scope (the second consecutive slice in this family whose row under-counted itself, after #463). Corollary: an optional parameter that falls back to its own fetch is how the second read survives a consolidation unnoticed, so `taxRows` takes the keep as REQUIRED and lets the compiler enumerate the callers | LOCKED: `getReconciliationBoundary` widened to four views (`keepsReconciled`, `handoverKeys`, `handoverDates`, `terminalOf`) from ONE read, all four the same derivation from the same inputs, all four empty/constant-true on the no-links fast path; `getReconciliationHandoverDates` routed through the shared fetch. `tests/unit/reconciliation-boundary-shared-read.test.ts` extended: equivalence against both standalone functions, dates-vs-keys proven DIFFERENT sets (1 date, 2 keys — a bare date is not a key and a key is not a bare date), all-four-empty fast path, and — the actual regression lock, because equivalence would pass against four separate reads too — a `vi.spyOn(prisma.accountReconciliation, 'findMany')` COUNT asserting `getTaxExport` and `refreshRecurringForUser` each read the table exactly once, the tax case additionally asserting the export really assembled ($125.00). Both counts proven fail-old by sabotage: the pre-U.33 tax pair reddened at "expected 1, got 2", the restored second terminal read reddened at "expected 1, got 2" |
| 2026-08-15 | C.20: a healthy-category bill stayed inside the daily rate whenever a dropped-category bill rode next to it — #391's crossing guard took NO credit once any merchant raw sum exceeded the month total, so $80 of electricity plus $30 of dining projected as if both were discretionary ($330.00 vs $170.00) | `creditedCents` summed per MERCHANT off raw rows while `spentSoFarCents` is the sum of surviving category nets; detecting the crossing is not sharing a basis | a crossing guard is not a shared basis — attribute through the same nets the total already dropped, and do not couple still-due to the reduced credit (a posted bill whose category was netted out has landed) | LOCKED: `tests/unit/trends-pace-bills.test.ts` › `test_regression__c20_credit_uses_surviving_category_nets` (discretionary $30, projected $170.00); `test_regression__c20_partial_category_refund_credits_the_surviving_net`; `test_regression__c20_exclusive_category_is_credited_before_contested`; P1-2 fixture now also locks `billsStillDueCents === 0` |
| 2026-08-15 | C.20 copy: branch B said matched bills were "already counted", which claims they sit in the month total — false of a posted bill whose category was net-refunded, a month this slice newly puts on that branch | "counted" was true when credit and still-due shared one landed number, so subtracting it implied the money was in `spentSoFar` | a noun in a money sentence is a claim about which total the fact lives in; still-due knows "posted", not "in the month total" | LOCKED: `tests/unit/trends-labels.test.ts` branch B; C.20 fixture asserts `already posted` and forbids `already counted` |
