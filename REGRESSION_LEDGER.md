# REGRESSION_LEDGER.md

Per LOOP_ENGINEERING.md: every fix ships a regression test plus one line here
(`date | symptom | root cause | rule broken | locking test`). Append-only. Earlier
phase regressions live as named tests (tests/unit/critic*.test.ts) + docs/DECISIONS.md;
this ledger starts at the 2026-06-21 hardening session.

> Rows from 2026-06/2026-07 were moved verbatim to
> `docs/archive/REGRESSION_LEDGER_ARCHIVE_2026-06_to_2026-07.md` on 2026-08-04, and
> rows from 2026-08-01 through 2026-08-17 to
> `docs/archive/REGRESSION_LEDGER_ARCHIVE_2026-08-01_to_2026-08-17.md` on 2026-08-27.
> Rows 2026-08-20 through 2026-09-02 live in `docs/archive/REGRESSION_LEDGER_ARCHIVE_2026-08-20_to_2026-09-02.md`
> (rotated 2026-09-11), and rows 2026-09-03 through 2026-09-11 in
> `docs/archive/REGRESSION_LEDGER_ARCHIVE_2026-09-03_to_2026-09-11.md` (rotated 2026-09-17).
> Only rows from 2026-09-12 onward live here. Append new rows here as before.

| date | symptom | root cause | rule broken | locking test |
|---|---|---|---|---|
| 2026-09-21 | Live cash-needed was 14px; Plan/Spending were synonyms; Home Today restated the hero; Inbox vs “17”; Coach/catalog/sign-in dumped work | Card `size=sm` + UA button font beat `MONEY_DISPLAY_CLASS`; labels named the wrong jobs; Today used the full feed | a stage number uses the display token; one job per noun; Today does not restate the stage | `tests/unit/home-today.test.ts`; `tests/unit/ux-a-grade-locks.test.ts`; `tests/unit/page-chrome.test.ts` (`amountClassName` + CardTitle skip); `tests/e2e/home-stage.spec.ts` (amount ≥28px); `tests/e2e/today-feed.spec.ts` (no `nudge-payment_due`) |
| 2026-09-21 | Tap-open of a Home/Coach chapter could snap shut on a leftover click; Enter-then-click-to-close could be swallowed for 500ms | first click-open did not arm `holdOpen`; the 500ms timer was the only way a real close got through | a leftover activation must not close a just-opened chapter; a new pointerdown after ready is a real close | `tests/unit/coach-chapters.test.ts` / `home-chapters.test.ts` (`clearHoldOpen`, `onPointerDown`); `tests/e2e/home-chapters.spec.ts` / `coach-chapters.spec.ts` (tap-open then `HTMLElement.click()` stays `open &&` card; first-open Enter then Playwright click closes) |
| 2026-09-20 | First-open Enter or a UA-first leftover click snapped a just-opened Home/Coach chapter shut | `readyRef` made the leftover click skip `preventDefault`, so the same gesture native-toggled closed | a first-open gesture must leave the chapter open with its body already there — swallow the leftover activation | `tests/unit/coach-chapters.test.ts` / `home-chapters.test.ts` (`holdOpenRef`, `armHoldOpen`); `tests/e2e/coach-chapters.spec.ts` / `home-chapters.spec.ts` (first-open Enter `open && fi-card` / `open && net-worth-amount`; `el.open = true` then trusted `summary.click()` stays open, next `click()` closes) |
| 2026-09-20 | A UA that opened a Home/Coach chapter before React's click painted an empty shell; `toggle` attached after first paint | `toggle` is a later task; `useEffect` listeners miss the first frame; `el.open = false` in `toggle` crashed the error boundary | a disclosure that is open must already hold its body — mount in the `open` mutation microtask, before paint | `tests/unit/coach-chapters.test.ts` / `home-chapters.test.ts` (`flushSync`, `MutationObserver`, `readyRef`); `tests/e2e/coach-chapters.spec.ts` / `home-chapters.spec.ts` (`el.open = true` then one microtask is `open && money-rules-card` / `open && home-plan-figures`; Enter after remount) |
| 2026-09-20 | Space/Enter could open a Home/Coach chapter as an empty shell if the UA toggled before click | click `preventDefault` did not run on keydown; `el.open = false` inside `toggle` crashed the app error boundary | a disclosure that is open must already hold its body; keyboard activation is the same gesture as click | `tests/unit/coach-chapters.test.ts` / `home-chapters.test.ts` (`onKeyDown`, `mountedRef`, `!el.open \|\| mountedRef.current`); `tests/e2e/coach-chapters.spec.ts` (Space `open && fi-card`; Habits `el.open = true` mounts `money-rules-card`); `tests/e2e/home-chapters.spec.ts` (Space `open && net-worth-amount`; Adjust `el.open = true` mounts `home-plan-figures`) |
| 2026-09-19 | First chapter open painted an empty shell, then the cards; `#coach-money-dials` hid the land ring | `el.open = true` ran before React committed children; landmark wrappers used `focus:outline-none` | a disclosure that is open must already hold its body; a skip-target must show where focus landed | `tests/unit/coach-chapters.test.ts` / `home-chapters.test.ts` (`pendingOpen`, `preventDefault`; landmark tags have no `focus:outline-none`); `tests/e2e/coach-chapters.spec.ts` (`open && fi-card`; money-dials `outlineStyle !== none`); `tests/e2e/home-chapters.spec.ts` (`open && net-worth-amount`) |
| 2026-09-19 | Closed Home/Coach chapters still committed every card into first HTML; after unmount, `#coach-money-dials` was a dead jump | `<details>` hid cards but left them in the tree; then mount-on-open only matched the chapter id | unread bodies stay unmounted until first open; a nested product hash must open its parent | `tests/unit/coach-chapters.test.ts` / `home-chapters.test.ts` (`{mounted ? children : null}`; Habits/Trajectory `landmarks`); `tests/e2e/coach-chapters.spec.ts` (`fi-card` / `money-rules-card` not attached; `/coach#coach-money-dials` + `wealth-target-dials-link`); `tests/e2e/home-chapters.spec.ts` (`net-worth-amount` / `home-plan-figures` not attached) |
| 2026-09-19 | Home had no jump to Adjust/Picture; a hash tap opened a chapter but left focus on the link; leads were a `<p>` inside `<summary>` | Coach got a chapter nav in #757; Home chapters did not; reveal only set `open` | a folded chapter needs a jump after the daily loop; a skip-target must take focus; summary phrasing stays phrasing | `tests/unit/home-chapters.test.ts`; `tests/e2e/home-chapters.spec.ts` (nav + hash focus; painted `listStyleType`); `tests/e2e/coach-chapters.spec.ts` |
| 2026-09-19 | Coach chapters hid the open/close triangle; a second Trajectory tap did nothing; Trajectory/Habits leads talked about page structure | `#757` hid the native marker and only listened for `hashchange`; leads described IA | a closed chapter must look openable; a jump to a chapter already in the hash must still open it; coaching copy is about the reader's money | `tests/unit/coach-chapters.test.ts`; `tests/e2e/coach-chapters.spec.ts` (Habits closed; same-hash Trajectory re-open); `tests/e2e/home-chapters.spec.ts` (`#home-picture`); `tests/e2e/desktop-header.spec.ts` Plan/Trends |
| 2026-09-19 | Home below cash-needed/guilt-free was still a feature dump; critic cycle 1 hid Welcome back and the $6,950 radar cover inside closed chapters | every Home card painted open; then the first wrap put one-shot greeting + alarm radar behind `<details>` | one job per surface: daily loop stays open; Adjust/Picture/Setup start closed; a safety or one-visit card may not sit inside a closed chapter | `tests/unit/home-chapters.test.ts`; `tests/e2e/home-chapters.spec.ts` (380 loop + radar visible; Picture/Adjust closed until click) |
| 2026-09-19 | CI `bec0b32e` run 35424012696: 380 Home guilt-free finished past 800px on Linux chrome; `/trends` `getByText('What changed')` hit two nodes | cash-needed due list + forecast/cards links sat open above guilt-free; Trends sidebar description started with the page's own h2 | the fold pair must fit 800px with the due list closed; a page heading string may not also lead a nav description | `tests/e2e/home-stage.spec.ts` (dues closed + guilt ≤800); `tests/e2e/phase1-cash-needed.spec.ts`; `tests/unit/nav-destinations.test.ts` Trends `movers` / not `what changed`; `tests/e2e/mobile-nav.spec.ts` `Category movers`; `tests/e2e/trends.spec.ts` |
| 2026-09-19 | Desktop nav wrapped 19 pills; Home put the plan form between heroes; Coach was a 7,076px unlabeled feed; critic cycle 1/2: TOC≠body, 19 described (then bare) rows, 380 Home led with guilt-free, More had no focus trap | chrome was a sitemap and a dump, not a stage + three jobs | one job per surface: a sidebar groups destinations; Home leads with cash-needed; Coach chapters start closed except This month | `tests/unit/desktop-sidebar.test.ts`, `tests/unit/coach-chapters.test.ts`, `tests/unit/home-plan-figures.test.ts`, `tests/e2e/home-stage.spec.ts`, `tests/e2e/coach-chapters.spec.ts` (380 height <140 until jump), `tests/e2e/phase1-cash-needed.spec.ts` (guilt ≤800), `tests/e2e/mobile-nav.spec.ts` (search focused) |
| 2026-09-19 | A padded vs clean `plaidItemId` looked like two connections, so same-item copies folded on last-4 and a genuine transfer vanished; padded accounts also disappeared from combine | #754/#755 trimmed the live-map join; `sameIngestConnection`, `accountsOf`, and the `/accounts` filters still used raw `===` | a padded item id that trims to the same key is the same ingest — last-4 must not fold two rows on one connection | `tests/unit/transfer-pair-identity.test.ts` `test_regression__o20j_padded_same_plaid_item_same_mask_are_two_accounts`, `_padded_same_item_same_mask_checkings_still_pair`, `_empty_plaid_item_id_does_not_fold_on_last4`, `_accounts_of_and_accounts_card_use_trimmed_item_equality`; `tests/unit/combine-connections.test.ts` `_padded_account_item_id_still_offers_the_chase_combine`. FAIL-OLD no helper: **8 failed \| 80 skipped**; critic **6 failed \| 82 passed** |
| 2026-09-18 | A padded stored `PlaidItem.itemId` missed the trimmed lookup and inherited the Account stamp, so a present-null live item looked identified | #754 trimmed the lookup key only; map construction used raw `i.itemId` | a live item key that exists after trim is live on both sides of the join — last-known is only for a missing / whitespace-only key | `tests/unit/transfer-pair-identity.test.ts` `test_regression__o20j_padded_map_key_does_not_inherit_the_stamp`, `_transfer_combine_and_accounts_build_the_map_with_trimmed_keys`; `tests/unit/combine-connections-server.test.ts` `test_regression__o20j_padded_map_key_does_not_inherit_the_account_stamp`. FAIL-OLD untrimmed helper: **3 failed \| 113 skipped** |
| 2026-09-18 | A padded `plaidItemId` (`" item-a"`) missed `Map.has` and inherited the Account stamp, so a present-null live item looked identified on id and name | `resolveLiveInstitutionField` looked up the raw `plaidItemId` without trimming | a live item key that exists after trim is live — last-known is only for a missing / whitespace-only key | `tests/unit/transfer-pair-identity.test.ts` `test_regression__o20j_padded_plaid_item_id_does_not_inherit_the_stamp`; `tests/unit/combine-connections-server.test.ts` `test_regression__o20j_padded_plaid_item_id_does_not_inherit_the_account_stamp`. FAIL-OLD untrimmed `has`: **3 failed \| 108 skipped** |
| 2026-09-18 | Combine and `/accounts` still inlined `item?.institution ?? stamp`, so a present-null live PlaidItem inherited the account name stamp; `getAccountsView` offered an irreversible continue (`provenIdentity: true`) while the live bank is unknown | two identity callers did not use a Map.has name join; `??` treats present-null like a missing key | a live Plaid side that lacks a bank name is unproven on the identity ladder's both-null name fallback — last-known is only for disconnect (key absent) | `tests/unit/combine-connections-server.test.ts` `test_regression__o20j_present_null_item_name_does_not_inherit_the_account_stamp`, `test_regression__o20j_present_null_live_name_does_not_inherit_a_stamp_and_offer_a_continue`; `tests/unit/transfer-pair-identity.test.ts` `_combine_and_accounts_call_the_name_join_not_the_inline_fallthrough`. FAIL-OLD `??`: **3 failed \| 1 passed \| 103 skipped** |
| 2026-09-18 | Combine and `/accounts` still inlined `item?.institutionId ?? stamp`, so a present-null live PlaidItem inherited the account stamp; `getAccountsView` offered a Chase combine and `buildCombineInputs` returned `ins_stale` while the transfer writer fail-closed | two identity callers did not use `resolveLiveInstitutionId`; `??` treats present-null like a missing key | a live Plaid side that lacks `ins_*` is unproven on every surface that decides identity, not only the transfer writer | `tests/unit/combine-connections-server.test.ts` `test_regression__o20j_present_null_item_does_not_inherit_the_account_stamp`, `a present-null item does not inherit a matching stamp and still names the missing bank ID`; `tests/unit/transfer-pair-identity.test.ts` `_combine_and_accounts_call_the_same_join_not_the_inline_fallthrough`. FAIL-OLD `??`: **3 failed \| 95 skipped** |
| 2026-09-18 | A live pre-backfill PlaidItem with `institutionId` null inherited `Account.institutionId` via `??`, so two copies with matching stale stamps folded on last-4 and a dining purchase vs a filed TRAVEL CREDIT was treated as one card (`overturned: 0`) | `resolveLiveInstitutionId` used `Map.get` + `??`; a present null is indistinguishable from a missing key | last-4 is not a bank; a live Plaid side that lacks `ins_*` is unproven — last-known is only for disconnect (key absent) | `tests/unit/transfer-pair-identity.test.ts` `a present live-null item does not fall through to the stamp`, `test_regression__o20j_live_null_item_ignores_a_stale_matching_stamp`; `tests/unit/transfer-pair-filing.test.ts` `test_regression__o20j_live_null_item_stale_stamp_still_overturns_a_purchase`. FAIL-OLD `??`: **3 failed \| 84 passed** |
| 2026-09-18 | Live CREDIT last-4 0977 (stamp NULL, PlaidItem `ins_56`) would refuse the identity fold if the join were stamp-only, while the filing lock stayed green because it stamped the account and never created a PlaidItem | the 0977 fixture used `Account.institutionId` as a stand-in for a join the test never ran | a fixture must live at the same join as production (the live-shape lesson); a stamp-only lock cannot witness an item-over-stamp fold | `tests/unit/transfer-pair-identity.test.ts` `test_regression__o20j_live_0977_join_is_item_ins_over_a_null_stamp`, `_stamp_only_0977_does_not_stand_in_for_the_join`, `_transfer_refresh_calls_the_join_not_the_stamp`; `tests/unit/transfer-pair-filing.test.ts` `_unconfirmed_duplicate_card_copies_do_not_overturn_a_purchase` (PlaidItem rows + null stamps). FAIL-OLD stamp-only: **2 failed \| 82 passed** |
| 2026-09-17 | Two Plaid items with no `ins_*` (pre-backfill) folded on last-4 alone, so Chase vs Ally with the same last-4 became one account and a $2,000 transfer vanished; Plaid(null) also folded onto SimpleFIN/manual on last-4 (`flagIds: []`) | `institutionsConflict` treated a missing id as "not a difference"; the first cut only vetoed Plaid–Plaid | last-4 is not a bank; an unproven Plaid side is unproven against ANY counterpart | `tests/unit/transfer-pair-identity.test.ts` `test_regression__o20j_plaid_plaid_missing_institution_ids_do_not_fold_on_last4`, `_unproven_plaid_does_not_fold_onto_a_non_plaid_last4`, `_same_last4_transfer_still_flags`, `_unproven_plaid_plus_simplefin_same_last4_transfer_still_flags`; `tests/unit/transfer-pair-filing.test.ts` `_plaid_plaid_missing_institution_ids_still_overturn_a_purchase`. FAIL-OLD: **5 failed \| 72 passed**. Probe `scripts/audit-probes/o20j-0977-institution-ids.mts` (live 0977 = `ins_56`) |
| 2026-09-17 | After save, the /goals debt-free card printed "Suggested: about $X/mo" / "On track … no extra needed" over the solver's extra, computed from the same frozen balances #746 had just named on the total, with nothing said; a typed monthly would have made a wider total-stamp claim false | `Goal.frozenAtSave` described the TOTAL only; `updateGoalMonthly` / `clearGoalMonthly` never recorded that the monthly had become the reader's | a stamp names only the figure it still describes; a second fact needs a second column, not a wider sentence | `tests/unit/saved-debt-goal-frozen.test.ts` (both stamps at save; extra stamp cleared on a CHANGED monthly and on clear, kept on `$500.00` no-op; extra-only / combined builder goldens); `tests/unit/goal-debt-free-fields.test.ts` (`extraCents: extra`); `tests/e2e/debt-plan-frozen.spec.ts`. FAIL-OLD extra-stamp writes stripped: **2 failed \| 15 passed** |
| 2026-09-17 | Critic cycles 1–3 on the same slice: extra-only on-track said "its on-track claim" (nearest noun = the bank); extra>0 said "the extra it suggests" (same binding); extra/on-track arms reused "includes the last balance" for figures that are not sums | figure labels borrowed a pronoun from a clause whose subject is the bank, then reused the total's verb ("includes") on a monthly extra | a caveat may not attribute a figure to the bank; the verb must be true of every subject it names — a derived extra is "worked out from" a balance, it does not contain one | `tests/unit/saved-debt-goal-frozen.test.ts` extra-only extra>0 / extra=0 full `toBe` literals + `not.toContain(' it suggests')` / `not.toContain('its ')`; combined arms "were both worked out from"; e2e pin `worked out from`. FAIL-OLD `includes` on extra-only extra>0 → 1 failed |
| 2026-09-17 | /coach "Your next dollar" and Ask's next-dollar answer said "Next extra dollar: <loan> (12.00% APR)" — an instruction naming one debt to send money to — over a balance the bank had stopped sharing, with nothing said; a frozen past-due card was ranked first on a "past due" from a statement the bank stopped confirming | `src/server/coach.ts` mapped `snap.accounts` → loans without `feedDroppedAt`, and cash-needed `CardObligation`s (which carry `frozenSince`) → pastDueCards without it; `NextDollarDebt` had no slot for the fact | a server-side narrowing may not strip a data-integrity fact before the copy that names the row (the #305/#742 disease); a required field at the boundary, not an optional one | `tests/unit/frozen-next-dollar.test.ts` describe `getCoachData — the fact rides the money through the real read path` (frozen past-due card + frozen 12% loan, both stamps arrive; live sibling arrives `null`), + builder / selector / COACH_COPY / Ask locks; `tests/unit/next-dollar-card-render.test.tsx`; `tests/e2e/next-dollar-frozen.spec.ts`. FAIL-OLD vs `9892cd44` coach.ts: **2 failed \| 23 passed** |
| 2026-09-17 | Critic P2-1/P2-2 on the same slice: an empty-string `frozenSince` painted "Your bank stopped sharing Personal Loan on Tue, undefined 0, 0"; on the investing branch the note named the losing loan BEFORE the why had introduced it | `!= null` admitted `''` into `formatISODate`; the note sat between headline and why on every branch, but only the why names the loan on `invest` | a stamp that is not a date renders nothing; a caveat may not precede the first mention of its subject — one position on every branch | `tests/unit/frozen-next-dollar.test.ts` "an empty-string stamp is not a date → null"; `tests/unit/next-dollar-card-render.test.tsx` "investing branch: the losing loan is introduced by the why BEFORE the note names it" + the why → note → skipped order lock; Ask detail order lock |
| 2026-09-17 | O.20j identity (new-budget critic cycle 1): Chase checking vs Ally checking with the same last-4 folded into one account and a $2,000 transfer vanished; purchase/refund filing depended on unordered `findMany` order; cycle-3 `$237.08` / 8-flag claim was treated as this function's live result | mask COLUMN only, no institution/currency/registration veto; group iteration followed Map insertion; a wider union's measurement was copied forward | money identity may not be wider than the advisory detector's hard prereqs; a money map may not depend on row order; do not ship a stale live number | `tests/unit/transfer-pair-identity.test.ts` `test_regression__o20j_different_institutions_same_last4_are_two_accounts`, `_different_currency_same_last4_are_two_accounts`, `_roth_and_traditional_same_last4_are_two_accounts`, `_0977_still_folds_when_both_plaid_copies_share_an_institution`, `_missing_account_record_fails_closed`, `_identity_does_not_depend_on_account_order`, `_chase_ally_same_last4_transfer_still_flags`, `_purchase_refund_does_not_depend_on_account_order`. FAIL-OLD: `HEAD` function → **12 failed \| 22 passed**. **Not on `main`.** |
| 2026-09-17 | O.20j identity: a confirmed H.7 predecessor (null-mask SimpleFIN terminal) smuggled two accounts on ONE Plaid item, or a dismissed pair, into one identity — a genuine $2,500 transfer then refused to flag or file, so the outflow counted as spend and the inflow as income | the veto walked the (type, mask) group while the union unifies `root()`; confirmed terminals sit outside the group | a guard must read what it guards — evaluate the veto over the component `root()` would produce | `tests/unit/transfer-pair-identity.test.ts` `test_regression__o20j_confirmed_bridge_does_not_defeat_a_dismissal`, `_does_not_fold_same_item_accounts`, `_two_mask_groups_bridged_by_confirmed_stay_two_accounts`, `_confirmed_bridge_does_not_equate_cross_type`, `_confirmed_bridge_does_not_unfile_a_same_item_transfer`, `_confirmed_two_mask_groups_do_not_unfile_a_dismissed_transfer`. FAIL-OLD: pre-component-veto function → **6 failed \| 20 passed** |
| 2026-09-16 | O.20j identity union (unshipped, branch `o20j-mask-column-identity`): a third same-mask copy, a DB-fault empty dismissal set, or two accounts on one Plaid item could fold distinct accounts and un-flag a genuine transfer | HIGH detector / edge-level skip / fail-OPEN empty set used as money identity; same-connection skip was an edge `continue` | a fence copied per call site; an empty set is not a fact about money; a guard must read what it guards | `tests/unit/transfer-pair-identity.test.ts` `test_regression__o20j_third_copy_does_not_defeat_a_dismissal`, `_unavailable_dismissals_do_not_fold`, `_same_plaid_item_same_mask_are_two_accounts`, `_third_copy_does_not_fold_same_item_accounts`, `_third_copy_does_not_unfile_a_same_item_transfer`. **Not on `main`.** Cycle-4 P1 (confirmed-map back door) still open — DECISIONS #743 |
| 2026-09-14 | Two unfiled deposits that look identical to a reader landed on opposite sides of one figure: a positive with a raw-null category counted as INCOME, while the same positive in the `'uncategorized'` placeholder store NETTED the month's spending down — so a $10,000 brokerage funding silently REDUCED spending, and `MONTH_FLOW_BASIS` had to name the contradiction instead of the app having one rule. The CARD side had the mirror defect: `isSpendRow` admitted that inflow and let it NET a category bucket down, so the chart called the row income and the card deleted real spending for it | `isIncomeFlowRow` tested `!t.categoryId` alone; the `'uncategorized'` leaf lives in group `Transfers & Other`, so it failed both that branch and the Income-group test. Measured live before the fix (probe): nil-store positives 0 rows corpus-wide, placeholder store 1 row / $10,000.00 (a brokerage transfer) — the null branch was a pure-engine convenience, never a live population | one fact, one rule: the raw null and the placeholder are the same "nobody labelled this row", the app already applies the sign rule to an unfiled OUTFLOW, and BOTH surfaces must agree — a card that deletes spending for a row the chart calls income is the dangerous direction | `tests/unit/insights.test.ts` describe `test_regression__o20c: ONE definition of an unidentified inflow`; `tests/unit/month-flow-breakdown.test.ts` (the `unfiled-uncat` fixture flipped to the income panel + the deleted basis clause asserted GONE); `tests/unit/category-breakdown.test.ts` (`x8`/`x9` never listed + the new `BREAKDOWN_BASIS` clause); probe `scripts/audit-probes/o20c-unidentified-inflow.mts`. FAIL-OLD proven by stash-revert: 6 failed \| 80 passed across insights + month-flow-breakdown pre-fix |
| 2026-09-14 | O.20c's card-side clause silently emptied a loan-payment answer: `merchantSpend` returned count 0 where the answer must keep a matching refund, so the loan-exclusion clause lost its figure | the fixture's refund row was UNFILED (`categoryId: null`), which under the unified rule is income rather than a netting refund — the fixture no longer expressed the case it was written for | a fixture must express the case under test: a refund nets only when the reader FILED it to a spend category (the documented refund convention), so the O.18e-FU3 fixture files its refund | `tests/unit/loan-payment-flow-exclusion.test.ts` "refunds-only branch: the clause says 'not in these figures', never 'not as spending'" (filed refund, count 1 restored); unfiled credits are covered by the O.20c locks |
| 2026-09-14 | CI e2e `trends-new-merchant-panel.spec.ts` red after the O.20c copy widening: the spec pinned `BREAKDOWN_BASIS` verbatim and the sentence had gained a clause | the widened constant is correct (the predicate gained a drop, and a sentence claiming a COMPLETE enumeration must name it), but a pin on a disclosed sentence is an assertion about the shipped copy, and it must be re-pointed, never weakened | a spec that pins a user-facing constant verbatim must be updated with the copy it describes — the copy is the product, the pin is its witness | `tests/e2e/trends-new-merchant-panel.spec.ts` (the basis assertion now carries `an inflow nobody has filed yet`); the two live-deploy check scripts keep their prefix assertions, which the new copy preserves |
| 2026-09-12 | A row the register labels "Fixed · you set this" was still counted in the /coach discretionary bar (and listed in its panel beside a re-file link that could not move the number) — "discretionary" meant two different things on screen | `detectLifestyleCreep` keyed the spend branch on the CATEGORY's taxonomy flag (and, in the first cut, on the pipeline's read-time guess for null-category rows) while register//budgets label rows by `classifySpendClass` (override → recurring-bill guess → flag) on the raw row | one question, one basis: the bar, the badge, and the budget list must name one definition — on the register's own input | `tests/unit/insights.test.ts` describe "one discretionary definition" (FAIL-OLD proven by stash-revert: 7 failed \| 52 passed pre-fix); probe `scripts/audit-probes/o20h-creep-definition-arms.mts` |
| 2026-09-16 | /goals' Debt Freedom planner printed "Debt-free by Jan 2034" + a total interest, and both Ask debt answers printed a payoff month or an extra-per-month, over a balance the bank had stopped sharing (`feedDroppedAt` set), with nothing said — one save-button away from a persisted goal target | `loadDebtAccounts` mapped each liability to the engine's `DebtInput` and dropped `feedDroppedAt` on the way — the server-side NARROWING DECISIONS #305 diagnosed on three other surfaces and named this path as the one left open | a data-integrity fact must reach the surface that prints the figure it qualifies; the read path's type carries it (`DebtAccount extends DebtInput`), the pure engine never reads it | `tests/unit/frozen-debt-plan.test.ts` (read path via Prisma, engine byte-identity, both Ask answers speak/abstain on golden literals); `tests/unit/debt-freedom-planner-render.test.tsx` (jsdom render, note precedes save, abstention = absence); `tests/e2e/debt-plan-frozen.spec.ts` 2/2. FAIL-OLD: pre-slice component swapped in → 1 failed \| 1 passed; critic's mutation probe 6 of 18 red |
| 2026-09-16 | The first cut's loan sentence said the real balance "may be lower than the one used here" — false for a LOAN this codebase itself produces: `plaid-map.ts` folds Plaid's revolving `line of credit` / `home equity` subtypes into `LOAN` (a draw pushes the real balance UP), and an amortising loan accrues interest and can carry a missed payment above the frozen figure; the understating direction is the dangerous one (critic P1-1) | a direction was asserted from the account's NAME ("a loan only goes down") rather than from the set of rows the type actually admits | a claim about a kind is checked against every row the mapper puts in that kind, not the archetype; where no direction holds, claim none (the `frozenLoanNote` precedent) | `tests/unit/frozen-debt-plan.test.ts` "a loan: NO direction" (pins the sentence + `not.toMatch(/only be lower\|may be lower/)`) and "several loans"; mixed note says the remedy once ("ONE remedy" test) |
| 2026-09-17 | A debt-free goal saved from the planner or Ask printed "$23,300.00 of debt" on /goals forever after, with nothing said, when a debt behind that total carried `feedDroppedAt` at the save — the #305 PDF shape (a durable artifact built from a frozen figure) one save button later; #742's critic P2-6 | `saveDebtFreeGoal` persisted the solver's `totalBalanceCents` and DROPPED the fact its input rows carried; `Goal` has no `createdAt`, so the card could not judge it at render time either — a re-resolve against today's `feedDroppedAt` would over-claim for a debt that froze after the save | a fact about the moment a figure was computed is recorded WITH the figure (additive `Goal.frozenAtSave`), never re-derived from later state; null = not recorded, renders nothing | `tests/unit/saved-debt-goal-frozen.test.ts` (real-Prisma writer stamps the save day on a frozen card / null on nothing frozen / null on a frozen $0 card; builder golden + no-direction/no-bank/no-remedy negatives; null/undefined/'' → null), `tests/unit/goal-debt-free-fields.test.ts` (page wiring), `tests/e2e/debt-plan-frozen.spec.ts` (saved card carries the note / abstention twin carries none). FAIL-OLD pre-slice writer: **2 failed \| 8 passed**; pre-slice page vs the wiring lock: **1 failed** |
| 2026-09-17 | Critic P2-2/P2-3 on the same slice: a mutation that read `goal.targetDate` instead of `goal.frozenAtSave` on the /goals card survived the whole unit suite (e2e-only lock); and tapping "Save target" on the pre-filled, UNCHANGED total cleared the stamp, silencing a note that was still true (the quiet direction) | the page wiring had no unit-tier lock; `updateGoalTarget` nulled the stamp unconditionally on any successful write rather than on a CHANGED total | the stamp describes the total — it moves only when the total does; and a wiring the unit tier cannot reach gets the repo's source-extent lock | `tests/unit/goal-debt-free-fields.test.ts` (`frozenSavedDebtGoalNote(goal.frozenAtSave)` + testid in the debt block, sliced to its real extent); `tests/unit/saved-debt-goal-frozen.test.ts` "re-saving the pre-filled, UNCHANGED total keeps the stamp" (unconditional clear → **1 failed \| 10 passed**) |
