# REGRESSION_LEDGER.md

Per LOOP_ENGINEERING.md: every fix ships a regression test plus one line here
(`date | symptom | root cause | rule broken | locking test`). Append-only. Earlier
phase regressions live as named tests (tests/unit/critic*.test.ts) + docs/DECISIONS.md;
this ledger starts at the 2026-06-21 hardening session.

> Rows from 2026-06/2026-07 were moved verbatim to
> `docs/archive/REGRESSION_LEDGER_ARCHIVE_2026-06_to_2026-07.md` on 2026-08-04, and
> rows from 2026-08-01 through 2026-08-17 to
> `docs/archive/REGRESSION_LEDGER_ARCHIVE_2026-08-01_to_2026-08-17.md` on 2026-08-27.
> Only the current wave (2026-08-20 onward) lives here. Append new rows here as before.

| date | symptom | root cause | rule broken | locking test |
|---|---|---|---|---|
| 2026-09-01 | Activity still showed TST* / SQ * as the payee | stripPayeeNoise skipped processor prefixes that ingest already drops | payee cleanup is general, not one household | `test_regression__tst_and_sq_prefixes_are_not_the_payee` |
| 2026-09-01 | Sign-in rejects a correct password with surrounding whitespace | authorize verified raw bytes only; trim-verifies was log-only | login trust: autofill whitespace is not a wrong password | `test_regression__sign_in_accepts_password_with_surrounding_whitespace` |
| 2026-09-01 | Import sold CSV as a Simplifi standup matcher | page/helper named Simplifi CSV and Simplifi-wins-during-standup | classify-any-source is the product, not one household export | `test_regression__csv_import_copy_is_any_source_not_simplifi_standup` |
| 2026-09-01 | Direct Starbucks filed Food Delivery (or DD *STARBUCKS filed coffee) | DD * stripped to STARBUCKS then coffee; no delivery-channel exception | Starbucks is Food Delivery only through a delivery app; direct is coffee | `test_regression__direct_starbucks_is_coffee_not_food_delivery` + `test_regression__doordash_starbucks_is_food_delivery` |
| 2026-09-01 | Cursor Usage Jul and Moonshot Ai filed Shopping | unknown merchant fell through to Plaid GENERAL_MERCHANDISE shopping rescue | a known software payee must not be rescued into shopping | `test_regression__cursor_usage_and_moonshot_file_software_not_shopping` |
| 2026-09-01 | Activity showed Purchase Trn / Www. / Linkagnt leftovers as payee names | cleanDescriptor title-cased the raw string; joined canonical kept the dirt | a name that still contains the bank token is not a payee | `test_regression__purchase_trn_and_www_are_not_the_payee` |
| 2026-09-01 | Inbox promised only ambiguous rows land here next to 5.3% accuracy | page/nav/empty claimed auto-file quality; accuracy is a scored later-label hit-rate | a disclosure is several claims — do not promise the queue is the leftover few when the same page scores 5.3% | `test_regression__inbox_copy_does_not_promise_only_ambiguous_land_here` |
| 2026-09-01 | Home "You're covered" sat next to radar "Investor Checking dips below $0 in 30 days" / -$34k lowest | cash-needed is this-cycle cards only; radar is 90-day committed (loans + estimated dues); all-clear omitted the window | a disclosure is several claims — an all-clear must name the window it actually walked | `test_regression__covered_alert_names_this_cycle_not_a_90_day_all_clear` |
| 2026-09-01 | Today feed "Payment due $X by DATE" named no account (live: eight unlabeled rows; cash-needed above had Bonvoy Amex ····2001) | paymentProposal dropped accountName and hardcoded merchant null; copy had nothing to print | a list of obligations without the name the source already has is not a control | `test_regression__payment_due_copy_names_the_account` |
| 2026-09-01 | Dashboard "Over plan by $3,085.33" / "guilt-free is $0 this month" next to "$0.00 this month" | over-plan is trailing complete-month pattern remainder; copy imported a this-month spend window | a disclosure is several claims — do not label a different window as this-month overspend | `test_regression__overplan_copy_does_not_claim_this_month_spend` |
| 2026-09-01 | Needs a category chip on /transactions did not filter (live: click left 2679 rows, aria-pressed false; ?unclassified=1 already showed 15) | button onClick + router.push dropped the first click before hydration (#167) | a first-interaction toggle that only exists as JS is not a control | `test_regression__needs_category_chip_is_a_href_before_hydration` |
| 2026-09-01 | Simplifi CSV import left matching Plaid rows on Aimplifi's category (Gusto stayed Fast Food) | duplicates skipped with no recategorize; Restaurants did not resolve to dining | Simplifi wins classification during standup - export category must file the existing id, never a new leaf | `test_regression__simplifi_csv_recategorizes_duplicate_existing_row` + `test_regression__simplifi_restaurants_csv_files_dining_not_a_new_leaf` |
| 2026-08-31 | Settings radios said "this year" for contribution room while the column stored no tax year, so next January the checked label and ranking would be a live false claim | indexical copy on a yearless status | a disclosure is several claims — "this year" is a tax-year claim the engine does not have | `test_regression__tax_room_copy_does_not_bind_a_tax_year` |
| 2026-08-31 | A garbage or omitted tax-room column could invent remaining and re-rank extra dollars | fail-open parse | a typed key is a pattern — closed set only; else unknown | `test_regression__garbage_tax_room_column_is_unknown_never_remaining` |
| 2026-08-31 | Remaining contribution room could have ranked above a high-APR revolving card | treating "has room" as free money | remaining is a wrapper on investing, not a contracted rate — only after revolving / match / runway / high-APR installment | `next-dollar.test.ts` N20 |
| 2026-08-31 | Demo Coach read could have used a dirty tax-room column if the write fence were bypassed | read force untested while the seed column is null | a fence copied per call site will miss call sites — lock the read leg with a dirty column | `test_regression__demo_tax_room_read_is_forced_unknown_even_if_column_is_dirty` |
| 2026-08-29 | Assumptions said "then match if known" after the reader stored captured/none, which never take that rung | "if known" dropped the engine predicate (`uncaptured` only) | a disclosure is several claims — "known" is not "insert the match rung" | `test_regression__assumptions_name_uncaptured_match_not_if_known` |
| 2026-08-29 | Investing ladder still claimed "we don't yet know whether you have a match" after Settings stored a status | generic #515 string was not a function of the new column | a new egress path invalidates claims written for the old ones | `test_regression__known_match_drops_the_dont_know_ladder_clause` |
| 2026-08-29 | Uncaptured why implied the feed observed a match ("rungs we can see") | copy was written when match was always unknown | copy that describes a surface the app does not render is fabrication | `test_regression__uncaptured_why_names_settings_not_a_feed` |
| 2026-08-29 | A garbage or omitted employer-match column could invent uncaptured and re-rank extra dollars | fail-open parse | a typed key is a pattern — closed set only; else unknown | `test_regression__garbage_employer_match_column_is_unknown_never_uncaptured` |
| 2026-08-29 | Captured match could have ranked above a thin runway | treating "known" as uncaptured | match is a rung, not a rate — only `uncaptured` wins | `test_regression__captured_match_does_not_win_over_thin_runway` |
| 2026-08-28 | Raising Auth.js `session.maxAge` to 30 days so "remember me" can persist overnight would also keep an *unchecked* session valid for 30 days — the #321 shared-machine defect | Auth.js mints JWT `exp` and cookie `Expires` from one `maxAge` on every session read; it cannot vary per sign-in | a framework default is a decision you shipped — the ceiling is the remember window; the default 30-minute idle is `applySessionLifetime` in the jwt callback, not a deleted assertion | `test_regression__default_session_dies_at_30_minutes_idle` + `test_regression__edge_jwt_drops_a_default_token_idle_past_30_minutes` |
| 2026-08-28 | An unchecked remember box could still mint a 30-day session if the form omitted the field and Auth.js treated missing as true | HTML checkboxes submit nothing when unchecked | an omitted field is a default-off, never an implicit yes — `isRememberRequested` is false for null/empty/`false`; the action always forwards `'true'` or `'false'` | `test_regression__unchecked_remember_box_never_forwards_true` |
| 2026-08-28 | Checking remember-me and shutting the lid overnight still landed on sign-in | the 30-minute idle window applied to every device with no opt-in | the owner asked for a remember control at login — grant the 30-day idle only when the box is checked | `test_regression__remember_session_survives_overnight_and_dies_at_30_days` + `test_regression__checked_remember_box_is_forwarded_to_authjs` |
| 2026-08-28 | Standing session reads (INDEX + TASKS + EDGE_CASES dump) cost ~49k tokens before any product file opened | INDEX hooks were one line but 300–800 chars; EDGE_CASES was a 242 KB monolith; closed TASKS waves kept empty preambles | token efficiency is principle 10 — an index that costs a dump defeats its purpose | `tests/unit/standing-reads.test.ts` |
| 2026-08-28 | "You have $X still unassigned" read as remaining cash on a leftover that is monthly capacity (income − fixed − savings; discretionary spend this month is not subtracted) | first-draft C6 copy used possession + "still unassigned" for a capacity figure | a disclosure is several claims — "You have" and "still unassigned" are remaining-cash claims the engine does not support | `test_regression__assign_to_zero_line_is_pinned_byte_exact` + the cash-claim ban in `assign-to-zero.test.ts` |
| 2026-08-28 | Assign-to-zero line certified leftovers the /budgets strip already discloses as too large (uncounted bills inflate guilt-free; frozen/excluded-card notes) | picker was a sign check only and never read the notes on the same card | a disclosure gated to the loudest branch misses the reachable one — silence that certifies a too-big leftover is P1; a louder leftover sentence without the hedge is louder | `test_regression__assign_to_zero_refuses_when_uncounted_fixed_inflates_leftover` + `test_regression__assign_to_zero_refuses_when_card_notes_make_leftover_direction_unknown` |
| 2026-08-28 | `parseCents` on the leftover sentence returned NaN ($1,803.28 expected) | the helper strips to digits-and-dots, so trailing periods ("remainder." / "verdict.") become extra decimals | a typed key is a pattern, not an identity — parseCents was written for a short money string, not a sentence | `conscious-buckets.spec.ts` matches `formatCents` of the guilt-free legend, never parseCents of the sentence |
| 2026-08-28 | Changing DECISIONS_INDEX to `#n — title → file` would make `parseIndexNumbers` return [] against the still-old index, so `droppedNumbers` is always empty and `reindex` can silently delete | the anti-deletion lock only sees numbers the *current* parser can read off today's index | a format change of a derived artifact must parse BOTH shapes until the first regenerate, or the refusal guard is neutered | `tests/unit/ledger-decisions-index.test.ts` "reads the pre-D.2 bold-prose lines and the D.2 one-line form" |
| 2026-08-26 | Swapping the two `label:` entries in the form's `PRESET_COPY` renders the Education chip as "Giving" and keeps every unit test, both e2e specs and the production probe green | every existing check read the name *input*, which `applyPreset` fills from the engine registry — nothing read the chip's own text | a chip is two claims (what it says and what it does) — assert the rendered label, not only the fill | `education-goal-preset.spec.ts` + `giving-goal-preset.spec.ts` exact `toHaveText`; `p21-live-deploy-check.mjs` label checks (EDGE_CASES EP6) |
| 2026-08-26 | An Education hint reusing Giving's "a lens, not a grade" would name a /reports figure the app never renders | the clause was written for the giving YTD lens (#520); education spend has no lens | copy that describes a surface the app does not render is fabrication — a shipped sentence is a claim | `test_regression__education_goal_preset_does_not_claim_a_reports_lens` |
| 2026-08-26 | An education envelope could have been filed against a student loan the debt planner already owns, so both surfaces would count it | `student-loan` is a spend/debt leaf, not a savings target | one question, one basis — name the loan to exclude it, never to fill it | `test_regression__education_goal_preset_names_the_student_loan_as_a_debt` |
| 2026-08-26 | A 529/ESA/tax-advantaged or scholarship sentence would be account advice the app has no surface or facts for | the canon names vehicles; Aimplifi recommends no account | educational not advisory — name the envelope, refuse the vehicle | `test_regression__education_goal_preset_gives_no_account_or_tax_advice` |
| 2026-08-26 | Ordering education against retirement ("before"/"after"/"prioritize") would ship a ranking the app never performed | the books disagree with each other on this order | a preset is a name the reader asked for, not a plan the app ranked | `test_regression__education_goal_preset_does_not_rank_against_retirement` |
| 2026-08-26 | Growing the preset registry could silently edit the Giving hint the #521 production probe greps, breaking the deploy proof with no local signal | the shipped live probe asserts four exact phrases that nothing local pinned | a shipped proof is state — a later slice may not quietly move what it reads | `test_regression__adding_a_preset_does_not_change_the_giving_one` (EP5) |
| 2026-08-26 | Giving-preset e2e `[data-testid^="goal-"]` + "Giving" also matched the chip, hint, and form, so create+delete could not lock | the new chip/hint testids start with `goal-` and contain the preset name | a locator copied from a unique name is a pattern, not an identity — scope to the list and the heading | `tests/e2e/giving-goal-preset.spec.ts` (goals-list + heading exact Giving) |
| 2026-08-26 | Intro said "Start from a preset" with one chip | catalog copy shipped ahead of a second preset | a disclosure is several claims — do not name a catalog the page does not have | `test_regression__giving_goal_preset_intro_does_not_claim_a_catalog` |
| 2026-08-26 | A Giving preset that invented a target or a 10% band would have been a recommended amount | the leftover was a starting name, not a tithe | a zero is a claim — a preset is a name, never an amount | `test_regression__giving_goal_preset_does_not_invent_an_amount` |
| 2026-08-26 | Empty Giving YTD title said "Gifts and donations already on file" while the body said none were filed | empty CardTitle reused a speaking-state subtitle | a zero is a claim — the empty title must not assert filing the empty body then denies | `test_regression__giving_ytd_empty_title_does_not_claim_already_on_file` |
| 2026-08-26 | Empty Giving YTD said "No gifts or donations are filed" when a custom Giving-group category still had spend | the two-leaf filter is correct; the empty sentence described the books, not the two leaves | a disclosure is several claims — name the leaves in the negative, do not deny giving the figure refused to count | `test_regression__giving_ytd_empty_does_not_deny_custom_giving_group_spend` |
| 2026-08-26 | Speaking copy lectured that extra cash "pays little" in checking and should be parked in a HYSA | liquid is checking+savings and we do not collect an APY | a disclosure is several claims — name the surplus, do not invent a yield or a parking place | `test_regression__idle_cash_names_only_this_liquid_and_does_not_nudge` |
| 2026-08-26 | Idle copy said "at or under" a $18,000 cushion for $18,000.01 and "about 7 months … at or under a 6-month cushion" | the far gate widened idle to include past-cushion / not-far liquid; the sentence still used the at-or-under clause | scoping the number does not scope the sentence — name past-but-not-far as past, not under | `test_regression__idle_cash_past_cushion_not_far_does_not_say_at_or_under` |
| 2026-08-26 | One cent past a 6-month cushion printed the surplus sentence | the leftover was cash that *far* exceeds runway | a rounding nick is not "far" — speak only at ≥ one extra month of expenses | `test_regression__idle_cash_one_cent_past_cushion_is_idle_not_far` |
| 2026-08-26 | Card title said "Cash past a 6-month cushion" on idle and empty | the title is rendered on every branch | a title is a claim — the lens name must not assert the surplus the engine refused | `test_regression__idle_cash_title_is_a_lens_not_a_surplus_claim` |
| 2026-08-26 | A $0 expense average would have printed a $0 six-month cushion and called cash "past" it | unknown expenses are not a fact about a cushion | an empty set is not a fact about money — idle is not a surplus claim | `test_regression__idle_cash_zero_expenses_is_idle_not_a_surplus_claim` |
| 2026-08-26 | A HYSA / invest-the-difference sentence would have been advice wearing a lens | we do not collect a savings APY and Aimplifi never moves money | second-person copy is a load-bearing invariant — name the surplus, refuse the nudge | `test_regression__idle_cash_names_only_this_liquid_and_does_not_nudge` |
| 2026-08-26 | Negative checking+savings could have been phrased as a shortfall this note invented | this lens only speaks a surplus; Room for error already names a negative runway | a zero is a claim — do not mint a shortfall on a card that only names extra | `test_regression__idle_cash_negative_liquid_is_not_a_shortfall_sentence` |
| 2026-08-26 | Putting idle-cash on /accounts would replay the #518 add-asset crash | `getCoachData` throws with zero accounts | a new read must not take down a page its throw was written for a different empty | `test_regression__idle_cash_does_not_block_accounts_list_on_coach` |
| 2026-08-26 | A second expense window would disagree with the runway figure | idle cash uses the same last-N average runway already divides by | one question, one basis — name the runway expense window | `test_regression__idle_cash_names_runway_expense_window` |
| 2026-08-25 | CI /accounts `add-asset-btn` timed out after PAW shipped | `getCoachData` throws "No accounts found" for a zero-account throwaway, and /accounts is that first-run surface | a new read must not take down a page its throw was written for a different empty | `test_regression__paw_lens_does_not_block_accounts_list_on_coach` |
| 2026-08-25 | An unset age on the expected-NW lens could have printed "you're under" against a fabricated $0 expected | age 0 / missing income must not produce an expected figure | an empty set is not a fact about money — idle is not a savings claim | `test_regression__paw_lens_zero_age_is_idle_not_a_savings_claim` |
| 2026-08-25 | A PAW/UAW label would grade the reader against Stanley & Danko's UAW | the book names under-accumulators; Aimplifi does not | second-person copy is a load-bearing invariant — bands are above/near/short-of, never a grade | `test_regression__paw_lens_names_only_this_income_and_does_not_nudge` |
| 2026-08-25 | A second yearly-income definition would disagree with the FI card | PAW uses pretax annual income; we already have a monthly average | one question, one basis — name the FI-card window × 12 | `test_regression__paw_lens_names_fi_card_income_window` |
| 2026-08-25 | Mortgage extra-principal treated the cash-due minimum as pure P&I with no escrow/add-on sentence | `minimumPaymentCents` is the payment due (Plaid next_monthly_payment), and the walk applies all of it after interest | a disclosure is several claims — name that the minimum is cash due and add-ons are not split out | `test_regression__mortgage_early_payoff_names_cash_due_is_not_split_escrow` |
| 2026-08-25 | A $0.00 mortgage and "never had one" shared "no mortgage on file" | picker dropped `balanceCents <= 0` into `none` | an empty set is not a fact about a row that exists — paid-off is its own sentence | `pickMortgageForEarlyPayoff` paid-off lock; `mortgage-early-payoff-render.test.tsx` paid-off |
| 2026-08-25 | Reports YTD interest/fees sentence listed all four fee leaves after the dollars, so an interest-only year read as having ATM and late fees | fact and scan-set were one em-dash clause | a disclosure is several claims — name only the leaves in the dollars; the scan set is a separate "counts" sentence | `test_regression__interest_fees_ytd_names_only_the_leaves_in_the_dollars` |
| 2026-08-25 | CI `category-drilldown` "tapping the category BAR" timed out after the YTD tile was inserted between the chart and the category list | the new card pushed the first bar behind the 380px bottom nav; `mouse.click` does not scroll | a new card on a hit-tested page must not split a pair the hit-test already assumed were adjacent | `tests/e2e/category-drilldown.spec.ts` "Reports: tapping the category BAR opens that category" |
| 2026-08-25 | fee-drag printed a today's-money "cost" below dollars leaked with no W.10a clause when inflation outran growth | new caller of `opportunityValueTodayCents` inherited the shortfall and not the sentence that already explains it on the opportunity list | a new caller of a primitive inherits every disclosure that primitive already earned — trails-contributions is the assumptions working, not a silent cheaper cost | `test_regression__p15_fee_drag_trails_contributions_names_the_assumptions` |
| 2026-08-25 | fee-drag named the two dials but not the $118.33/mo leak or grow-then-deflate, so a reader reconstructing annual deposits missed the printed figure by ~8% | "at 7% and 2.5%" collapsed the W.10 recipe the opportunity list already states | one primitive, one mechanism — name the monthly leak and grow-then-deflate, never a blended rate that could be the FI card's real walk | `test_regression__p15_fee_drag_names_monthly_leak_and_grow_then_deflate` |
| 2026-08-25 | income-lever copy said lifestyle does not grow, while the engine only saves rate% of the raise and leaves the FI number put | first-cut basis named a lifestyle-frozen what-if the cents do not compute | a disclosure is several claims — name the hybrid (rate-share extra, FI number unchanged), never the walk you did not run | `test_regression__p14_income_lever_does_not_claim_lifestyle_frozen` |
| 2026-08-25 | income-lever result said "your current X% rate" after the savings slider on the same card had been dragged to a different pace | "current" pointed at the window average, not the live thumb | one card, one word — the rate the raise uses is the N-month average; do not call it current beside a what-if slider | `test_regression__p14_income_lever_names_window_average_not_current` |
| 2026-08-24 | life-energy existed per purchase but not per discretionary category over time; first cut said "each category" while topN dropped most hours, hid an unlabeled spark, and printed a median trend with no basis | W.6(c) not built; then copy over-claimed completeness and left the curve inaccessible | YMOYL fulfillment is category×months in hours; a truncation must be named; a trend figure states its half-window assumption (creep parity); headline hours must equal the strip | `fulfillment.test.ts` F1–F8; `coach-fulfillment.test.ts`; F7 truncated-subtitle lock; `tests/e2e/phase3-coach.spec.ts` fulfillment-card |
| 2026-08-24 | FI card had behavioral volatility copy but no quantitative drawdown line — reader could not see what a 30% drop does to the FI date | W.6(d) not built; only `volatilityPrice` existed | reasonable > rational needs a path illustration, not only a fee metaphor | `drawdown.test.ts` D1–D6; `coach-drawdown.test.ts`; `tests/e2e/phase3-coach.spec.ts` fi-drawdown |
| 2026-08-24 | Ask "How much should I pay off my cards before I can invest?" answered the extra-dollar ranking (no this-cycle dollars) | `\bshould i\b` + `\bbefore\b` was the ranking proxy and also cash-needed's modal; `or` short-circuited quantity/purpose; `fi_status` stole purpose-adjunct `retire` | context-carrying features must abstain — constituency of `or` vs purpose, not `should I`; a quantity stem is not ranking; retirement inside `before I can` is not the FI date | `test_regression__w6b_should_i_is_not_the_before_ranking_proxy`; `test_regression__w6b_quantity_or_is_not_the_ranking`; `test_regression__w6b_purpose_or_is_not_the_ranking`; `test_regression__w6b_ranking_or_before_purpose_stays_ranking`; `test_regression__w6b_before_im_able_is_purpose_not_contrast`; `tests/e2e/ask.spec.ts` P1 cash-needed headline |
| 2026-08-24 | Ask "Should I pay off debt or invest?" stole "When will I be debt-free so I can invest?" (and "pay off my loan if I keep investing") into the extra-dollar ranking | `asksNextDollar` treated any co-occurrence of invest + debt vocab as the ranking | context-carrying features must abstain — contrast (`or`/`vs`/`instead of`/`rather than`, or `before` only in a `should I` frame) is the ranking; purpose-clause co-occurrence stays debt_payoff / cash_needed | `test_regression__w6b_invest_cooccurrence_is_not_the_ranking`; `test_regression__w6b_instead_of_and_before_are_contrast`; `test_regression__w6b_before_i_can_is_not_contrast` |
| 2026-08-24 | a loan with no APR was ranked as 0% and the invest sentence said "No installment debt is on file" while the loan row existed | `aprBps ?? 0` invented a rate the feed did not send; copy gated "named loan" on `aprBps > 0` | no fabrication — unknown is skipped (`loan_apr`), known 0% is named, empty is "with a known APR" | `test_regression__w6b_unknown_loan_apr_is_skipped_not_invented_as_zero`; copy honesty block in `next-dollar.test.ts` |
| 2026-08-24 | /coach printed the same cut list as Ask with no "what happens if I do" — Ask had the FI + radar sentences, the card that owns the list did not | #506/#507 scoped the counterfactuals to Ask only, so two surfaces answering one question computed (or omitted) the walk independently | one question, one basis — the walk lives in `getCoachData({ cutImpact: true })`; copy's one author owns the honest null; dashboard/digest must not pay the walks (opt-in, same shape as `orderReview`) | `tests/unit/coach-cut-impact.test.ts` (flag-off nulls; payload equals hand-called `cutCounterfactual` on the card basis; demo $78.87 / $23,661.00; `test_regression__p1_coach_cut_fi_sentence_comes_from_the_one_author`; `test_regression__p1_coach_does_not_invent_a_radar_dip_on_the_demo`); `tests/e2e/phase3-coach.spec.ts` FI present + radar absent; `tests/e2e/auth.spec.ts` empty-coach testids 0 |
| 2026-08-24 | Ask "what should I cut?" could name the FI move (#506) but not whether the 90-day checking dip actually moves — and a $20/mo estimate applied raw to a weekly $15 series would have cancelled ~13 hits and fabricated "the dip goes away" | Opportunity has no series id so the harness matches by merchant canonical; `negotiable-bill` is calendar-monthly $20 while unused/delta/15% are per-occurrence; first-cut copy said "stop hitting" for reduce-walks too | engine before UI — a dip movement must be the same `radarFromSnapshot` walk, spoken only if dip date or rounded cover improves (duplicate-pair precedent); a monthly estimate must be scaled onto cadence before it can cancel a series; the honest null and the assumption live in the copy's one author | `tests/unit/radar-cut-counterfactual.test.ts` (weekly $15+$20 → −1038 not dropped; unused weekly still cancels; cancel-vs-scaled `computeRadar` dipDisappear lock; demo seed `moved === false`; checking-row harness moves); `test_regression__p1_cut_does_not_invent_a_radar_dip_on_the_demo_seed`; `test_regression__p1_cut_radar_movement_comes_from_the_engine`; `coach-copy.test.ts` cutRadarCounterfactual ALL_STRINGS + "stop hitting" refusal; ask.spec + live-check demo absence of `90-day cash-flow walk` |
| 2026-08-24 | Ask "what should I cut?" listed the opportunities but could not say what acting on them does — the owner's "what happens if I do" had no computed answer | the first P.1 slice (#494) shipped the list with no re-projection; the counterfactual engine did not exist | engine before UI — a money movement must be computed by the same walk at the same basis (`projectionReturnBps`, W.2), never asserted; one merchant one saving (unused + price-increase rows double-count without the per-merchant MAX); the honest null lives beside the sentence (nothing moves ⇒ no sentence) | `tests/unit/fi-cut-counterfactual.test.ts` (hand-computed 0% anchors 400→367/$30,000, savings-only non-vacuity, honest nulls, monotonicity sweep, dedupe order-independence, demo wiring lock vs `getCoachData`); `tests/unit/assistant-what-to-cut.test.ts` `test_regression__p1_cut_fi_movement_comes_from_the_engine`; `tests/unit/coach-copy.test.ts` cutCounterfactual ALL_STRINGS rows + the null + span-phrasing locks |
| 2026-08-24 | The P.1 e2e asserted the movement sentence's assumptions clause with the capitalization of an earlier draft ("Same return…") and failed the full gate 3/3 while every unit test was green | the critic-cycle copy edit moved the clause behind a semicolon ("…; same return…") after the e2e was written; vitest does not typecheck or case-check a Playwright string | the gate re-runs on the final tree after EVERY fix edge — the e2e that locked the pre-fix copy was re-run only because the full gate re-ran; unit green is not e2e green | `tests/e2e/ask.spec.ts` P.1 test now matches the shipped copy (`/same return and inflation assumptions as Coach/`) and passed in the green gate |
| 2026-08-21 | Plan P1.2's stay-wealthy sentence always claimed cards clear, N-month cushion, and spending tracks income, while `/coach` already had those as three separate engines that can disagree | no composed row existed; a template that lists three facts is a fabrication when any signal is absent | one question, one basis — compose the existing engines, do not originate a trio claim; a zero is a claim (`!flagged` is also the incomparable window); a disclosure written for a page ("this card"/"below") is false in Ask; a context-carrying parse is judged by what it abstains on (named store/category/amount/date); single-signal questions stay their routes | `tests/unit/staying-wealthy.test.ts` `test_regression__stay_wealthy_does_not_claim_all_three_when_one_is_absent`; `tests/unit/assistant-stay-wealthy.test.ts` `test_regression__stay_wealthy_am_i_staying_wealthy_routes_to_coach`; `test_regression__stay_wealthy_answer_agrees_with_coach_row`; `test_regression__stay_wealthy_copy_does_not_claim_this_card_or_below`; `test_regression__stay_wealthy_date_window_abstains` |
| 2026-08-21 | Ask "how are my spending buckets?" was unknown while `/budgets` already printed the Sethi split; the June 24 coach-principles plan still said PLAN ONLY | no standing buckets intent existed; plan verdicts were authoring-time | one question, one basis — Ask must use the same engine as the dedicated surface (#75); a context-carrying parse is judged by what it abstains on (named store/category/amount; a date other than this month); a disclosure written for a page ("this card"/"below") is false in Ask; a plan verdict is authoring-time | `tests/unit/assistant-conscious-spending.test.ts` `test_regression__conscious_spending_how_are_my_spending_buckets_routes_to_strip`; `test_regression__conscious_spending_answer_agrees_with_budgets_caption`; `test_regression__conscious_spending_copy_does_not_claim_this_card_or_below`; `test_regression__conscious_spending_other_date_window_abstains` |
| 2026-08-21 | Ask "how many months of runway do I have?" / "do I have an emergency fund?" fell through to unknown while `/coach` already printed months of expenses in cash | no standing runway intent existed; emergency-fund words were only a savings-goal / wealth-target phrase | one question, one basis — Ask must use the same engine as the dedicated surface (#75); a context-carrying parse is judged by what it abstains on (named store/category/amount/date); a disclosure written for a page ("this card"/"below") is false in Ask; "run out of money" is radar, a dated/amount emergency-fund goal is a planner, not this card | `tests/unit/assistant-runway.test.ts` `test_regression__runway_how_many_months_routes_to_coach`; `test_regression__runway_answer_agrees_with_coach_runway_card`; `test_regression__runway_copy_does_not_claim_this_card_or_below`; `test_regression__runway_date_window_abstains` |
| 2026-08-21 | Ask "is my lifestyle creeping?" fell through to unknown, and "is my spending outpacing my income" answered this-month income, while `/coach` already printed the discretionary-vs-income verdict | no standing creep intent existed; `\bmy income\b` poached the comparison question; a stem `\boutpac\b` does not match "outpacing" | one question, one basis — Ask must use the same engine as the dedicated surface (#75); a context-carrying parse is judged by what it abstains on (named store/category/amount/date); a disclosure written for a page ("this card"/"below") is false in Ask; a prefix regex with a word-boundary after the stem does not match inflections | `tests/unit/assistant-lifestyle-creep.test.ts` `test_regression__lifestyle_creep_is_my_lifestyle_creeping_routes_to_coach`; `test_regression__lifestyle_creep_answer_agrees_with_coach_creep_card`; `test_regression__lifestyle_creep_copy_does_not_claim_this_card_or_below`; `test_regression__lifestyle_creep_date_window_abstains` |
| 2026-08-21 | Ask "when can I retire?" (no age) fell through to unknown while `/coach` already printed months-to-FI | `retire_at_age` required a named age and no standing FI intent existed; a retirement question with no age was unknown | one question, one basis — Ask must use the same engine as the dedicated surface (#75); a context-carrying parse is judged by what it abstains on (named age/amount/date); a disclosure written for a page ("this card"/"below") is false in Ask | `tests/unit/assistant-fi-status.test.ts` `test_regression__fi_status_when_can_i_retire_routes_to_coach_fi`; `test_regression__fi_status_answer_agrees_with_coach_fi_headline_math`; `test_regression__fi_status_copy_does_not_claim_this_card_or_below`; `test_regression__fi_status_date_window_abstains` |
| 2026-08-20 | Ask had no answer for "what should I cut?" — the owner's named differentiator — while `/coach` already ranked the same opportunities | Ask intents covered subscriptions and spend, never the coach opportunities list; a cut question fell through to unknown or (if scoped) a different money route | one question, one basis — Ask must use the same engine as the dedicated surface (#75); a context-carrying parse is judged by what it abstains on (named store/category/amount/date); do not invent an FI-date movement | `tests/unit/assistant-what-to-cut.test.ts` `test_regression__p1_what_should_i_cut_routes_to_opportunities`; `test_regression__p1_cut_answer_agrees_with_coach_opportunities`; `test_regression__p1_cut_does_not_invent_fi_movement` **[SUPERSEDED 2026-08-24 by #506: the counterfactual shipped, so the lock is now `test_regression__p1_cut_fi_movement_comes_from_the_engine` — movement iff the engine reports it — plus the two honest-silence locks]** |
| 2026-08-20 | "What should I cut last month" answered the standing Coach opportunities list | `whatToCutFromQuestion` treated only `parseTargetDate` / unresolved shapes as dates; `last month` is an explicit timeframe, not a goal deadline, so the unwindowed list shipped as the answer | a context-carrying parse is judged by what it abstains on — a calendar window is a different question from a standing ranking | `tests/unit/assistant-what-to-cut.test.ts` `test_regression__p1_cut_with_a_date_window_abstains` |
| 2026-08-21 | Ask/Coach ranked a money-dial merchant as a cut (Fitness unused gym) | `findOpportunities` never received Settings dial ids; W.6(a) `categoryMatchesMoneyDial` only gated wealth-target proposals | Sethi: hunt savings everywhere else — a dial is spending that buys happiness, not a cut candidate; Coach and Ask must share one list (#75) | `tests/unit/insights.test.ts` `test_regression__w6a_opportunities_skip_money_dial_categories`; `tests/unit/assistant-what-to-cut.test.ts` `test_regression__w6a_ask_cut_list_omits_a_money_dial_merchant` |
| 2026-08-20 | Draft #12 hardcoded 40% as the household savings goal on the conscious band and coach dashed line | a personal Settings target was written as `CONSCIOUS_TARGET_BPS.savings = [4000,4000]` and a 4000-bps chart line | a rate and its target must share a unit — the target is `User.savingsTargetBps`; 40% is one reader's dial, not a product constant | `tests/unit/conscious.test.ts` `test_regression__conscious_savings_band_follows_settings_not_a_hardcoded_40`; `test_regression__conscious_caption_names_settings_dial_not_a_baked_in_40` |
| 2026-08-20 | Header Sign out used `<form action={doSignOut}>` (live HTML `action=""`); session could clear on the server while the client never navigated to /sign-in | same #164/#166/#167 / #489 leftover in signed-in chrome; progressive enhancement dead on a document POST | mutation forms use onSubmit, never bare form-action — Sign out was the last always-on leftover of that class in the app chrome | `tests/unit/sign-out.test.ts` `test_regression__sign_out_uses_redirect_false_and_returns_ok`; `test_regression__sign_out_propagates_auth_failure`; `test_regression__sign_out_uses_client_onsubmit_not_form_action` |
| 2026-08-20 | Owner hand-file to Transfer left `isTransfer=false` so register filter / triage inbox / recurring still treated the row as spend until next sync | `applyCategory` (and twin file paths) updated `categoryId` but never stamped `isTransfer`; only manual entry and the detector (#487) set the flag | a fence copied per call site will miss call sites — owner filing the Transfer leaf is the same verdict as a persisted leaf; stamp immediately; do not invent a silent clear (H.7b #428 remains the only `isTransfer:false` writer) | `tests/unit/apply-category-transfer-stamp.test.ts` `test_regression__o20j_apply_category_transfer_stamps_is_transfer`; `test_regression__o20j_file_off_transfer_does_not_clear_is_transfer`; twin locks for applyToAllSimilar / recategorize merchant / fileMerchantGroup |
| 2026-08-20 | Ask Costco Gas answered $195.82 at Costco while /trends Costco Gas = $37.38 | merchantMatches bidirectional whole-word prefix swept sibling warehouses; largest-magnitude then named Costco | sharing a row basis is not sharing a merchant scope — Ask must use exact store identity like the register (#250) | tests/unit/o8-merchant-basis-parity.test.ts test_regression__o10a_costco_gas_is_not_costco; tests/unit/assistant-intent.test.ts test_regression__o10a_costco_gas_is_not_hijacked_by_fuel_synonym |
| 2026-08-20 | Live "Explore the demo" CTA stayed on /sign-in with zero auth cookies on a document POST; HTML had `action=""` | `<form action={demoSignIn}>` progressive enhancement dead (same #164/#166/#167 family); demo provider itself was fine | mutation forms use onSubmit, never bare form-action — public CTA was the leftover anti-pattern | `tests/unit/demo-sign-in.test.ts` `test_regression__demo_cta_uses_redirect_false_and_returns_ok`; `test_regression__demo_cta_propagates_auth_failure`; `test_regression__demo_cta_uses_client_onsubmit_not_form_action` |
| 2026-08-20 | Live demo: Ask “will I run out in 90 days?” printed Everyday Checking **$12,495** / lowest **$3,400** while Cash flow radar projected **−$6,943.99** / move **$6,950** (cash-needed cycle transfer **$1,050**) | Ask `forecast` intent used recurring-only `getCashFlowForecast` for run-out vocabulary; fine print omitted card payments so it looked like the radar question | one question, one basis — Ask must use the same engine as the dedicated surface for that question (#75); do not invent a fourth projection | `tests/unit/ask-runout-radar-agreement.test.ts` `test_regression__ask_runout_routes_to_cash_flow_radar_not_forecast`; `test_regression__ask_runout_agrees_with_radar_lowest_and_cover`; `test_regression__thin_forecast_and_cycle_cash_needed_stay_different_questions` |
| 2026-08-20 | O.20j: transfer-category rows (Venmo / AUTOMATIC PAYMENT / brokerage sweeps) stayed `isTransfer=false` so register vs spend gates disagreed and new siblings kept arriving unflagged | `detectTransfers` evidence was only normalizer-transfer descriptors OR ±3-day opposite-amount pairs; those descriptors normalize to uncategorized and often have no unique opposite leg | a guard must read what it guards — the filed `transfer` leaf is already the verdict; treat it as descriptor-equivalent evidence (one direction; never clear spend-category converse leak) | `tests/unit/transfer-pair-filing.test.ts` `test_regression__o20j_filed_transfer_category_flags_without_pair_or_descriptor`; `test_regression__o20j_automatic_payment_and_brokerage_sweep_filed_transfer_flag`; `test_regression__o20j_converse_leak_spend_category_not_touched_by_filed_leaf_rule`; `tests/unit/transfer-flag-repair-plan.test.ts` `test_regression__o20j_filed_transfer_is_endorsed_by_repair_not_declined_out_of_scope` |
| 2026-08-20 | O.20j R6: largest "Overdraft Transfer from Brokerage -7383" ($7,792.97) filed Fees & Charges with isTransfer=false and still counted as spend after the #485 transfer-leaf gate | GENERIC `\bOVERDRAFT\b` → fees beat any transfer rule; isTransfer for this family came only from pair matching, and the unpaired largest amount had no opposite leg | a fence copied per call site will miss call sites — fix the normalizer the detector and categorize already share, not a third spend predicate | `tests/unit/insights.test.ts` `test_regression__o20j_r6_overdraft_transfer_from_brokerage_is_not_fees_spend`; `tests/unit/normalize.test.ts` `test_regression__o20j_r6_overdraft_transfer_beats_fees_keyword`; `tests/unit/transfer-pair-filing.test.ts` `test_regression__o20j_r6_unpaired_overdraft_transfer_descriptor_overturns_fees` |
| 2026-08-20 | O.20j: rows filed `categoryId=transfer` with `isTransfer=false` still inflated /reports chart, /coach savings rate + Money Review, and Ask income/expense (up to $10,051.27 in a measured month; 76 live rows) | `countsInFlows` keyed only off `isTransfer` + POSTED + split/exclude; `isSpendRow` already dropped the transfer leaf | a fence copied per call site will miss call sites — share the category gate on the flow predicate the chart/coach/Ask already share | `tests/unit/insights.test.ts` `test_regression__o20j_transfer_category_unflagged_does_not_count_in_flows`; `tests/unit/month-flow-breakdown.test.ts` `test_regression__o20j_transfer_category_unflagged_stays_out_of_month_flow_panels` |
| 2026-08-27 | CI red on a docs-only ledger rotation: ledger-decisions-index.test reported 390 decisions "dropped" after DECISIONS #1-#401 moved to docs/archive verbatim | the DECISIONS.md <-> DECISIONS_INDEX.md parity test and scripts/ledger.ts reindex/next-number read ONLY the live file; the rotation made the archive invisible to them | a ledger move is not done until every tool that reads the ledger reads its new shape | tests/unit/ledger-decisions-index.test.ts now asserts the live+archive UNION (plus an archive-glob-nonempty guard); scripts/ledger.ts decisionSources() reads docs/archive/DECISIONS_ARCHIVE_*.md |
