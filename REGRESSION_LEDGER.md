# REGRESSION_LEDGER.md

Per LOOP_ENGINEERING.md: every fix ships a regression test plus one line here
(`date | symptom | root cause | rule broken | locking test`). Append-only. Earlier
phase regressions live as named tests (tests/unit/critic*.test.ts) + docs/DECISIONS.md;
this ledger starts at the 2026-06-21 hardening session.

| date | symptom | root cause | rule broken | locking test |
|---|---|---|---|---|
| 2026-06-21 | Manual card statement set/clear timed out under parallel test load | interactive `prisma.$transaction(async tx…)` holds SQLite's single writer lock across app round-trips | "fail loudly / verify under real load" — the timeout only surfaced in the full parallel suite | `tests/unit/card-actions.test.ts` (array-form `$transaction`; green in the full suite) |
| 2026-06-21 | Dashboard listed an estimated card payment TWICE | both callers spread `[...result.cards, ...result.upcoming]` but the engine's `cards` already includes `upcoming` (a subset) | "know your data shapes before composing them" | `tests/unit/reminders.test.ts` (dedup test) + `tests/e2e/payment-reminders.spec.ts` (no-duplicate-card assertion) |
| 2026-06-21 | Reminder email/card said "you'll pay $Y yourself", hiding the autopay portion under a larger headline | the partial-autopay (top-up) case wasn't disclosed; `PaymentReminder` carried no autopay portion | coaching guardrail "every projection states its assumptions inline" | `tests/unit/reminders.test.ts` (top-up known-answer fixture) |
| 2026-06-21 | `rateLimitDurable` let a CONCURRENT burst bypass the limit entirely (50/50 at limit 8) — the brute-force/export throttle did nothing under the conditions it exists for | the reset branch did `upsert{count:1}` + `return true` unconditionally, never reading a post-write count (TOCTOU) | "a security control must hold under its own threat model (concurrency)" | `tests/unit/rate-limit-durable.test.ts` (12-call `Promise.all` burst → exactly 4 allowed) |
| 2026-06-21 | `RateLimit` table grew unboundedly — one permanent row per attacker-guessed `signin:<email>`, no prune, no index (CWE-770, unauthenticated) | no reclamation path + attacker-controlled key space | "bound any attacker-reachable allocation" | `tests/unit/rate-limit-durable.test.ts` (`pruneExpiredRateLimits` deletes expired, keeps live) + `@@index([resetAt])` |
| 2026-06-21 | Two concurrent `splitTransaction` calls could double-split a row (children created twice → doubled in every aggregate) | `isSplitParent` was read BEFORE the transaction; both racers passed the pre-read | "guard data-integrity invariants inside the atomic boundary, not before it" | `tests/unit/split-race.test.ts` (parallel splits → exactly one set of children; verified to fail on pre-fix code) |
| 2026-06-21 | "Always" racing "Undo" could orphan a priority-100 rule (undo deletes a rule a concurrent re-apply had reassigned to a different correction) | `undoCorrections` deleted the rule by `id` alone, ignoring whether it still belonged to this correction's lineage | "scope a destructive op to the lineage it owns (conditional-claim)" | `tests/unit/undo-orphan-rule.test.ts` (reassigned-lineage rule survives the undo; normal delete still works) |
| 2026-06-21 | Email-keyed sign-in throttle let an attacker who knows a victim's address lock that account out (targeted-account DoS) | the only throttle dimension was `signin:<email>` checked BEFORE auth, so it blocked the victim's correct password too | "a throttle must not penalize the legitimate (correct-credential) user" | `tests/unit/rate-limit-durable.test.ts` (per-IP cap before auth + per-account-FAIL after; a correct password succeeds even after the fail budget is spent) |
| 2026-06-21 | Two concurrent undos of the SAME correction wrote a duplicate inverse correction | `undoCorrections` checked `alreadyUndone` with a pre-read OUTSIDE the transaction (TOCTOU) | "make idempotency a DB invariant (unique), not a read-then-write window" | `tests/unit/undo-orphan-rule.test.ts` (two concurrent undos → exactly one inverse; `undoesId @unique`) |
| 2026-06-21 | The Plaid webhook (DB-touching, sync-triggering) verified no signature — anyone reaching it in Plaid mode could trigger a sync | the `Plaid-Verification` JWT check was never wired (only an interim 404-unless-Plaid gate) | "verify a webhook's signature before any side effect; pin the alg to stop confusion" | `tests/unit/plaid-webhook.test.ts` (ES256 + body-hash + freshness; rejects wrong-key/tampered/stale) + `plaid-webhook-route.test.ts` (401 unsigned) |
| 2026-06-21 | "DUKE ENERGY EPAY" (a utility bill) was auto-filed as a TRANSFER → silently dropped from spend, understating expenses | the generic `\bEPAY\b` transfer pattern fired on utility e-payments, not just card payments | "a too-broad transfer signal erases real spending; require context before excluding from spend" | `tests/unit/utility-epay.test.ts` (utility EPAY → utilities + not-a-transfer; card payments unaffected) — found by `npm run eval:categorize` |
| 2026-07-16 | The shared demo account (`user-demo`) could CONNECT a real bank (Plaid/SimpleFIN) — one anonymous visitor's real financial data landing in the row every other visitor logs into | the connect/ingest server actions never fenced the demo user (the #242 Trust-Center fence covered LLM egress, not the connection write path) — same shared-account leak class as household #210 / vocab #226 | "anything that stores a user's own INPUT under the shared demo row must fence the demo user" | `tests/unit/connect-demo-fence.test.ts` (all four ingest actions refuse demo with zero provider calls, on a KEYED deployment; a real user passes the fence) |
| 2026-07-16 | A demo-owned bank connection created before the fence could keep syncing via the scheduled cron sweep / Plaid webhook (the bug's residual) | the cron swept ALL users incl. demo, and the webhook synced whatever userId its item pointed to — neither excluded demo | "close a privacy fix's remediation path too, not just the forward write" | `tests/unit/cron-sync-demo-skip.test.ts` (demo excluded from the sweep, no `sync.cron` audit row) + webhook demo-skip (by-construction, shares the tested `isDemoUser`) |
| 2026-06-23 | New `/trends` page failed WCAG AA — a dark-mode percent label sat at 4.42:1 (below the 4.5:1 floor) | an `opacity-80` on the mover %-change span dimmed `rose-400` text under the contrast minimum | a11y "WCAG AA holds on every new surface" | `tests/e2e/trends.spec.ts` (axe wcag2a/2aa/21a/21aa scan on /trends) |
| 2026-06-23 | Trends could surface an aggregate pseudo-merchant in new-merchants/largest; the unit test MASKED it by stubbing `aggregateMerchant` instead of deriving it from `normalizeMerchant` | the engine's aggregate exclusion was correct but only ever tested with a hard-coded flag — never the real server path — and a docstring over-claimed "Store Card Purchase" as aggregate (it is intentionally a real, rule-eligible merchant here) | "test the integrated path, not a stubbed boundary; keep docs consistent with behavior" | `tests/unit/trends.test.ts` (integrated normalize→engine: genuine Zelle/Check aggregates excluded; Store Card — a real merchant in this codebase — kept) |
| 2026-06-24 | SimpleFIN connect+sync test flaked ("expected 0 to be 2") in the full parallel suite, not in isolation | the suite shares one rollback-journal SQLite dev.db across worker processes; under load a writer is starved by readers past busy_timeout → SQLITE_BUSY, swallowed by connectSimplefin's credential-safe catch as added:0 | "determinism / fail loudly — the verify gate must not flake; a swallowed infra error must not masquerade as a wrong value" | tests/unit/db-wal.test.ts (asserts WAL on, set by tests/setup/wal-global-setup.ts) + tests/unit/simplefin.test.ts (now asserts r.error is undefined; mechanism proven fail-before/pass-after) |
| 2026-06-25 | New debt payoff engine threw `cents() requires a safe integer` on a negative-amortization input (minimum below monthly interest) — the balance compounded past `Number.MAX_SAFE_INTEGER` before the 1200-month cap, exploding inside `roundHalfAwayFromZero` | the amortization loop only terminated on payoff or the month cap; it had no guard for divergence, so an unpayable debt grew unboundedly | "fail loudly or not at all — a non-terminating/diverging numeric loop must detect non-progress, not overflow into a thrown formatter" | `tests/unit/payoff.test.ts` ("reports never (null) on negative amortization") — constant-budget guard breaks when a month yields no net balance reduction, returning `monthsToDebtFree: null` |
| 2026-06-27 | Unit SimpleFIN connect flaked "expected 0 to be 2"; e2e phase2-triage throughput test timed out — both under load, only in the full run | the unit + e2e SQLite DB lived at the repo root under OneDrive; the sync client holds external OS locks on .db/-wal/-shm that WAL + busy_timeout + single-file serialization (all in-process) cannot wait out -> SQLITE_BUSY (DECISIONS #120) | "determinism — the verify gate must not flake on an external/infra cause; an in-process mitigation cannot fix an external lock" | tests/unit/test-db-location.test.ts (asserts the live DB is under os.tmpdir(), not the repo) + the tests/setup/test-db.ts relocation. UNIT flake FIXED (suite green + fast, reliably across many runs). E2E reduced but RESIDUAL: under load DIFFERENT correct tests (triage throughput, transactions register-search) still wall-clock-timeout because the next start server / .next / app tree is still on OneDrive — complete fix = relocate the working copy off the synced tree (CLAUDE.md); recorded at STATUS #16/#17 + the #120 section. |
| 2026-06-27 | e2e `transactions.spec.ts:145` (inline recat) flaked intermittently off OneDrive — after recat, the row sometimes still showed 'Dining Out' within the 5s window | the positive assert ran on the WHOLE ROW (`toContainText('Groceries')`) and matched the in-flight 'File as Groceries?' confirm prompt → passed before persistence, so the negative `not.toContainText('Dining Out')` raced `router.refresh()` on its default 5s budget (asymmetric vs the positive's 20s) | "assert on the element the test actually means (the chip), not text that includes transient sibling UI; give both sides the same generous budget" | tests/e2e/transactions.spec.ts:145 now asserts on `category-chip` (sibling-prompt-immune) with 20s on both assertions — app behavior verified correct (commit awaits recategorize then router.refresh); 4/4 full-suite runs green post-fix (DECISIONS #121) |
| 2026-06-28 | `parseTargetAmount` (new, #126) truncated any ungrouped 4+ digit `$` amount to its first 3 digits — "save $20000 by 2028" parsed as $200, a 100×-wrong money figure the user never stated, shown as their own goal and durably persisted on Save | the integer regex `\d{1,3}(?:,\d{3})*|\d+` tries the comma-grouped branch first; for "20000" `\d{1,3}` matched "200", `(?:,\d{3})*` matched ZERO groups, and the trailing tokens are all optional, so the overall match SUCCEEDED without ever backtracking to the `\d+` branch (only the `$` rule lacked a required trailing anchor that would force the retry) | cardinal "the AI never originates a number; financial math is sacred" — a silently-corrupted, never-stated money figure presented as the user's own | `tests/unit/assistant-savings-goal-by-date.test.ts` (`hostile-critic #126 regression locks` → "$20000"=2000000, "$5000"=500000, "$1234"=123400, plus the grouped/suffix/≤3-digit forms still correct); fix = require ≥1 comma-group (`\d{1,3}(?:,\d{3})+|\d+`) so ungrouped numbers fall through to `\d+`. Found by the hostile critic before ship (wf_3de855be). |
| 2026-06-28 | LIVE SimpleFIN ingest: an OVERDRAWN deposit account was stored as a POSITIVE asset and a positive-principal loan / keyword-less liability as a CHECKING asset — both INVERT that account's net-worth sign on the owner's real connected accounts | `mapSimplefinAccount` did `Math.abs(balance)` on EVERY account (discarding the overdraft sign) and `inferAccountType` defaulted keyword-less names to CHECKING, rescued to a liability only by a NEGATIVE balance — so a loan reporting positive principal escaped the rescue | "financial math is sacred — a balance's net-worth sign must be correct for every account state, on every provider" | `tests/unit/simplefin-map.test.ts` (sign-correctness: overdraft stores −, owed card +|owed|, positive-principal servicer loan typed LOAN with +|owed|, each hand-verified through `netWorthCents`) + the broadened `inferAccountType`. Fix: signed balance for assets, `\|owed\|` for liabilities (SimpleFIN gives no liability sign convention); liability-keyword branch before the generic "credit" rule. Found by the live-ingest contract audit (wf_6eade83c, DECISIONS #127). |
| 2026-06-28 | LIVE Plaid ingest: every connected Plaid card carried `aprBps` null/0, so the debt-payoff + cash-needed engines computed ZERO interest on the owner's real cards — corrupting the just-shipped debt-free-by-date + cash-needed figures | Plaid's `aprs[]` was never mapped onto `Account.aprBps` (the account mapper doesn't see liabilities data; the liabilities loop upserted a Statement but ignored the APR) | "a tested mapper must carry EVERY field the downstream money engine depends on; a missing rate silently zeroes interest" | `tests/unit/plaid-map.test.ts` (`pickPlaidAprBps`: purchase-APR 24.99→2499 bps no float drift, highest-non-special fallback, null when none) + wired into `/liabilities/get` to set `Account.aprBps` even with no generated statement. Found by the live-ingest contract audit (wf_6eade83c, DECISIONS #127). |
| 2026-06-28 | LIVE SimpleFIN ingest: a PENDING transaction that never posted lingered forever (the cash-needed engine sums pending → overstated "cash needed" + spend), and a pending that re-posted under a NEW id double-counted (stale pending + new posted both present) | SimpleFIN sends a stateless per-window snapshot with NO `removed[]`, and the spec permits a pending txn's id to change when it posts; nothing ever deleted a pending the feed stopped reporting | "reconcile by ABSENCE when the feed has no removed-list; a deleteMany on real money rows must be fenced by status/ownership/window guards" | `tests/unit/simplefin-pending-reconcile.test.ts` (11 tests, proven fail-before/pass-after) locks the in-window reconcile (dropped pending deleted; same-id post updates in place; new-id post leaves exactly one row) + every safety rail (POSTED / manual null-ref / out-of-window / split-parent preserved). Fix = `reconcilePendingTransactions` two-pass after Pass-2 (DECISIONS #128, backlog #4). |
| 2026-06-28 | A SimpleFIN pending "hold" (hotel/car-rental) that stayed pending past the 5-day incremental overlap then dropped/re-posted was UNRECONCILABLE — simultaneously absent from the feed AND below the reconcile's date>=startDate floor → lingered forever / double-counted on a new-id re-post | the pending-reconcile window was welded to the narrow incremental FETCH window (startDate = lastSyncedAt-5d), so a multi-day hold fell out of both once startDate advanced past its date | "a reconcile-by-absence must cover the full lifetime of the thing it reconciles, not just the fetch overlap; add an age-threshold backstop for what drifts past the window" | `tests/unit/simplefin-pending-reconcile.test.ts` "ages out a feed-owned pending older than the max hold…" + "does NOT age out a long hold the snapshot still reports". Fix = pass-2 AGE-OUT: delete feed-owned PENDING older than PENDING_MAX_AGE_DAYS=32 excluding the corroborated set. Found by hostile critic wf_35ef0562 (P1-1), DECISIONS #128. |
| 2026-06-28 | An untrusted SimpleFIN response with `transactions: null` (not merely an omitted field) threw `TypeError: null is not iterable` and ABORTED the whole multi-account sync (no txns ingested, lastSyncedAt not advanced → recurs every sync) | the new omitted-field guard used `=== undefined`, a REGRESSION from the prior null-safe `?? []`; `null !== undefined` slipped past into `for (const txn of acct.transactions)` | "guard untrusted-feed fields against null AND undefined; one bad account must never abort the whole sync" | `tests/unit/simplefin-pending-reconcile.test.ts` "a feed sending `transactions: null` does NOT throw/abort the sync". Fix = `if (!acct.transactions) return;` (falsy catches null+undefined; empty [] is truthy so it still reconciles). Found by hostile critic wf_35ef0562 (P1-2), DECISIONS #128. |
| 2026-06-28 | LIVE SimpleFIN holdings: a low-price / high-quantity brokerage lot rendered $0 (or ~2× wrong) in the /investments breakdown — a penny lot (1,000,000 sh @ $0.01) reconstructed to $0.00 and VANISHED; a sub-dollar 10,000-sh / $50 lot showed $100 | #124 stored ONLY a per-share priceCents=round(market_value÷shares) and the engine recomputed marketValue=round(quantity×priceCents); for sub-cent-per-share lots the per-share rounds to 0/1¢ so the reconstruction loses or doubles the feed's authoritative TOTAL | "a tested mapper must carry the SOURCE's authoritative value (the feed's TOTAL market_value), not a lossy per-share reconstruction" | `tests/unit/simplefin-holdings.test.ts` (PENNY→1¢; SUB $50 not $100; VOO $100 exact not 9999) + `simplefin-holdings-sync.test.ts` (low-price lot end-to-end through getInvestments) + `investments.test.ts` (valuePosition authoritative path). Fix = persist `Holding.marketValueCents` (TOTAL), engine uses it verbatim; null for manual→derive (DECISIONS #129, backlog #5). |
| 2026-06-28 | LIVE SimpleFIN holdings (Postgres prod only): a single position with TOTAL market_value > $21,474,836.47 would overflow the new `marketValueCents` Int (32-bit) column; the write error is swallowed by the reconcile's per-row try/catch and the position SILENTLY VANISHES from /investments (invisible on 64-bit SQLite CI) | the mapper bounded persisted cents only by Number.isSafeInteger (~$90T), far above the Postgres INTEGER ceiling (2,147,483,647¢) the column actually stores | "every persisted value must fit the column it is stored in; an out-of-range value must fail VISIBLY (skip+count), not silently (swallowed overflow)" | `tests/unit/simplefin-holdings.test.ts` "totals bounded to the DB Int ceiling" (over-ceiling skipped+counted; exactly 2,147,483,647 kept, +1¢ skipped; rest-of-feed kept). Fix = MAX_DB_CENTS bound on priceCents/costBasisCents/marketValueCents in the mapper ok-check. Found by hostile critic wf_844918ca (P1-1), DECISIONS #129. |
| 2026-06-28 | LIVE Plaid ingest: an INVESTMENT or LOAN account's balance FROZE at link time — net worth went stale and never recovered after the initial link, because every later sync left the balance untouched | `PlaidProvider.syncTransactions` refreshed balances ONLY for accounts ECHOED in the `/transactions/sync` `accounts` array (depository/credit with transaction activity); investment/loan carry no Transactions product, so they were re-fetched ONLY at link (`exchangePublicToken` → `syncAccountsForItem`), never on the recurring sync | "the ledger must reflect the institution's CURRENT balance each sync; a frozen balance silently misstates net worth" | `tests/unit/plaid-balance-refresh.test.ts` (first mocked-Plaid-server integration: a frozen INVESTMENT+LOAN balance is refreshed each sync; proven fail-before/pass-after) — fix = call `syncAccountsForItem` (`/accounts/get`, all accounts) once per item per sync, best-effort + audited (DECISIONS #130, backlog #6). Found by the live-ingest contract audit (wf_6eade83c, #127). |
| 2026-06-28 | LIVE Plaid ingest: a `/accounts/get` reporting a null `balances.current` would OVERWRITE a real investment/loan balance with $0 on EVERY sync, silently cratering net worth until a later non-null sync self-heals | the mapper did `plaidSignedDollarsToCents(current ?? 0)` and `upsertPlaidAccounts` wrote `currentBalanceCents` unconditionally; once backlog #6 made balances refresh every sync, that latent null-handling shortcut on a documented-nullable field became active money corruption on the net-worth-critical, non-nullable column | "financial math is sacred — null means UNKNOWN, never $0; never silently overwrite a real balance with a default" | `tests/unit/plaid-map.test.ts` (null `current` → null, not 0) + `tests/unit/plaid-balance-refresh.test.ts` (a null `current` on resync PRESERVES the stored balance) — fix = map null→null and OMIT `currentBalanceCents` from the UPDATE when null (Prisma preserves last-known-good; CREATE falls back to 0); also closes the same hole on the depository/credit echo path (shared mapper). Found by hostile critic wf_25be9884 (P1), DECISIONS #130. |
| 2026-06-29 | Ask retire-at-age: the natural inflections "retiring"/"retired" ("I'm retiring at 65") did NOT route to the planner in the zero-key demo — they fell through to unknown (or, with "afford", to safe_to_spend giving a wrong answer); only the bare "retire" worked | the routing gate `/\bretire(ment)?\b/` and parseTargetAge's first regex were anchored on the literal `\bretire` — "retiring" has no "retire" substring (retir-i) and "retired" lacks the trailing word boundary, and the LLM fallback couldn't rescue them (key absent in demo; intentFromKind re-derives the age via the same parseTargetAge → null) | "match the natural inflections of a feature's own vocabulary deterministically; don't rely on the optional LLM to cover a phrasing the rule should" | `tests/unit/assistant-retire-at-age.test.ts` ("covers the natural inflections retiring / retired"; "the inflections route deterministically"). Fix = broaden both to `retir(?:e|es|ed|ing|ement)` (kept in lockstep). Found by hostile critic wf_c5d22775 (P1→P2 on verify), DECISIONS #131. |
| 2026-06-29 | Ask retire-at-age: "can I retire at <plan-through age>?" (e.g. retire at 95 with endAge 95) gave a vacuous savable "on-track" answer whose Save button the server ALWAYS rejected — the answer and the save validator disagreed | the solver's after-end guard was STRICT (`targetRetirementAge > endAge`), so age == endAge (0 retirement years → projectRetirement never withdraws → trivially "sustained") escaped it and produced a reachable/on-track result with a save action, while `saveRetirementAge` rejects retirement >= plan-through | "an answer must not offer an action the server will reject; mirror the save validator's boundary in the solver" | `tests/unit/retire-at-age.test.ts` (RA-AFTEREND: `targetAge == endAge` → unreachable 'age-after-end'). Fix = `>=` (age == endAge is after-end), so no Save is offered + honest "at or past your plan-through age" copy. Found by hostile critic wf_c5d22775 (P2), DECISIONS #131. |
| 2026-06-29 | LIVE Plaid ingest: a statement CREDIT (a NEGATIVE `last_statement_balance` — the holder overpaid / has a credit) was abs()'d into an amount OWED, so a card on which nothing is owed would DEMAND that cash in the cash-needed headline | `mapPlaidLiabilityToStatement` mapped the balance through `plaidDollarsToPositiveCents` (abs) instead of a sign-preserving conversion, discarding the credit sign before the engine's `floorAtZero` could neutralize it | "financial math is sacred — a statement balance's sign must survive to the engine; a credit owes nothing and must never be inflated into a demand" | `tests/unit/plaid-map.test.ts` ("preserves a NEGATIVE last_statement_balance…" + the mapper→cash-needed e2e "a statement CREDIT … demands $0, not the abs() amount"). Fix = `plaidSignedDollarsToCents`; the engine's floorAtZero on remainingDue/minimumDue then yields $0. Found by the live-ingest contract audit (#127 P2), confirmed clean by hostile critic wf_edd3d8f3, DECISIONS #132. |
| 2026-06-29 | LIVE Plaid ingest: a card whose `minimum_payment_amount` Plaid reported as null (or literally 0) on a positive statement balance showed a $0 MINIMUM, understating the minimum-path cash needed below the engine's own no-statement estimate | the mapper did `minimum_payment_amount ?? 0` (and trusted a provided 0 verbatim), so a missing/zero minimum collapsed to $0 instead of a usable estimate | "financial math is sacred — never understate the conservative cash-needed amount; a missing/zero minimum is not a $0 minimum" | `tests/unit/plaid-map.test.ts` ("estimates a missing minimum (1% of balance)…", "$35 floor", "treats a reported ZERO minimum … like a missing one", + the MINIMUM-scenario e2e "demands the estimated minimum (not $0)"). Fix = a usable minimum is >0; otherwise estimate via the reused `estimateMinimumPayment` (max $35 / 1% of balance) when balance>0, else $0. #127 P2 + hostile critic wf_edd3d8f3 P2, DECISIONS #132. |
| 2026-06-29 | LIVE SimpleFIN ingest: a NON-EMPTY brokerage feed whose positions ALL fail to map (a format glitch / all-unsupported types) WIPED the owner's entire synced /investments breakdown — mapSimplefinHoldings returned [] (all skipped) and reconcileSimplefinHoldings deleted every source='simplefin' row, mistaking it for a sell-all | the INVESTMENT branch reconciled whenever `acct.holdings !== undefined`, and reconcile treats an empty mapped set as 'sold everything' (deleteMany on the whole account); nothing distinguished 'feed reported zero positions' from 'feed reported positions we could not map' | "a provider sync must never lose user data on a partial/glitchy feed; only an EXPLICITLY empty feed is a real sell-all — reconcile-to-empty needs the RAW feed empty, not just the mapped result" | `tests/unit/simplefin-holdings-sync.test.ts` ("does NOT wipe synced holdings when a NON-EMPTY feed maps to zero positions"). Fix = reconcile only when `holdings.length > 0 || acct.holdings.length === 0`; otherwise leave rows intact (skipped). Found by the #127 audit P2, DECISIONS #133. |
| 2026-06-29 | LIVE SimpleFIN ingest: an untrusted feed sending a NON-ARRAY `holdings` (e.g. `null`, not merely omitted) ABORTED the whole sync ("null is not iterable" inside mapSimplefinHoldings), and a `holdings: ""` would have wiped the breakdown via `.length===0` | the holdings guard tested `acct.holdings !== undefined`, so null/non-array values passed through to iteration; mapSimplefinHoldings's `= []` default only fires for undefined, not null | "harden every untrusted-feed field against null AND non-array shapes (the #128 transactions:null lesson applied to holdings); Array.isArray is the single guard that covers undefined/null/non-array" | `tests/unit/simplefin-holdings-sync.test.ts` ("leaves synced holdings intact (and does NOT abort) when the feed sends a non-array holdings (null)"). Fix = `if (Array.isArray(acct.holdings))`. Found by hostile critic wf_8a9d99dc (P2), DECISIONS #133. |
| 2026-06-30 | LIVE Plaid ingest: a linked mortgage/student loan carried ONLY a balance — its APR stayed 0 (debt-payoff mis-computed) and its monthly payment + due date were invisible everywhere — because `syncLiabilities` read ONLY `liabilities.credit[]`, never `mortgage[]`/`student[]` | the `/liabilities/get` response was typed `{ credit?: PlaidCreditLiability[] }` and the loop iterated only credit; mortgage/student arrays were unmodeled, so a loan's rate/payment/due-day were never written to the Account | "ingest every liability product the engines model; a loan that exists only as a balance is a silent data gap on real money" | `tests/unit/plaid-loan-liabilities-sync.test.ts` ("populates a mortgage and a student loan with APR + fixed monthly payment + due day"). Fix = widen the response type + sibling mortgage/student loops applying `mapPlaidMortgageToLoanFields`/`mapPlaidStudentToLoanFields`. #127 audit's biggest remaining item, DECISIONS #134. |
| 2026-06-30 | LIVE Plaid ingest: a deferment-state loan (or any sync missing a field) would have BLANKED a known APR/payment/due-day to null had the loan UPDATE written every field unconditionally | preserve-on-null was not yet applied to the new loan-field writes (the #130 lesson, here pre-empted) | "preserve last-known-good on a null field — a missing value is 'unknown this fetch', never an instruction to erase a real one" | `tests/unit/plaid-loan-liabilities-sync.test.ts` ("preserves the last-known APR/payment/due-day when Plaid reports them null"). Fix = `applyLoanFields` builds the UPDATE data from only the non-null mapped fields (skips the write entirely when all null). DECISIONS #134. |
| 2026-06-30 | Demo: once the seed Auto Loan surfaced as a first-class loan-due obligation, its hand-authored `sched-autoloan` checking outflow would have DOUBLE-DISPLAYED the payment on the calendar (a generic outflow on Jul 5 AND a loan-due badge on Jul 2) and double-counted it in totalOut | the seed modeled the loan payment TWICE — as the LOAN account (acct-autoloan) AND as a duplicate ScheduledTransaction on checking (the pre-#134 stand-in that was the only way the loan reached cash-needed) | "model a real-world item ONCE; a stand-in added to fake a surface must be removed when the real surface lands, or it duplicates" | `tests/unit/phase4.test.ts` ("the stand-in scheduled outflow is gone (no double-display)" + "shows the loan as a badged loan-due event … counted as outflow + reminder"). Fix = delete `sched-autoloan` from the seed; the loan-obligation engine now drives the surface. Demo has no shortfall → cash-needed headline byte-identical. DECISIONS #134. |
| 2026-06-30 | LIVE ingest: a non-USD / zero-decimal (EUR/JPY) feed balance was summed into net worth at a fabricated 1:1 (the app does no FX) — a EUR account added its balance as USD | neither mapper read the feed's currency (SimpleFIN `acct.currency` present-but-unread; Plaid `iso_currency_code` not even modeled), so every account was treated as USD | "the AI/ingest never fabricates a fact — withhold what we can't denominate, never invent a USD figure" | `tests/unit/currency-guard.test.ts` ("excludes non-USD accounts from the snapshot and its net worth"). Fix = persist `Account.currency`; `DemoProvider.getFinanceSnapshot` filters accounts by `isSupportedCurrency`. #127 audit #3/#10, DECISIONS #135. |
| 2026-06-30 | Currency guard: `getInvestments` rolled a non-USD brokerage's holdings into the USD "Portfolio value" at 1:1, disagreeing with net worth + /coach on the same page | getInvestments reads INVESTMENT accounts DIRECTLY (not via the filtered snapshot) and rolled every account's holdings into the overall total with no currency filter | "apply the guard at EVERY account-scoped read, not just the snapshot — a direct read bypasses the source filter" | `tests/unit/currency-guard.test.ts` ("excludes a non-USD brokerage from the /investments roll-up"). Fix = select `currency` + `.filter(isSupportedCurrency)` before the roll-up. Hostile critic wf_74fc0808 (P1-A), DECISIONS #135. |
| 2026-06-30 | Currency guard: an all-non-USD user skipped the first-run empty state (gates counted RAW rows) then hit `resolvePaymentAccount`'s throw on the now-empty filtered snapshot → 6+ pages error-boundary + a 500 from the export route | the ~15 `prisma.account.count({where:{userId}})===0` gates counted every account while the snapshot returned only supported ones — the invariant "count>0 ⟹ snap.accounts non-empty" broke | "a gate must agree with the source it guards; filtering one without the other is a latent crash" | `tests/unit/currency-guard.test.ts` ("counts ONLY supported accounts for the first-run gate"). Fix = every gate adds `OR:[{currency:null},{currency:'USD'}]`. Hostile critic wf_74fc0808 (P1-B), DECISIONS #135. |
| 2026-06-30 | Currency guard: a withheld non-USD account's TRANSACTIONS still summed at 1:1 in reports/trends/coach/spending-plan/assistant/register/inbox/**/budgets**/recurring-refresh — the account net worth denied existed still drove spending totals + a foreign subscription persisted a scheduled row on the USD payment account | the snapshot filtered only `accounts`; the Transaction model has no currency, so every direct/derived transaction read needed the account-relation predicate and several lacked it | "withhold a withheld account's CHILD rows too — filtering the balance but not the transactions is a split-brain the same class of leak" | `tests/unit/currency-guard.test.ts` ("excludes a withheld account TRANSACTIONS from the snapshot"; "excludes withheld-account rows from the triage review count"; "the direct supported-account predicate … excludes foreign rows"). Fix = filter `snap.transactions`/`scheduled`/`balanceSnapshots` by supported ids + the `OR` predicate on getTransactions/triage/budgets/refreshRecurringForUser. Hostile critic wf_74fc0808 (P1-C) + confirmation wf_bda5c45a (budgets + recurring), DECISIONS #135. |
| 2026-06-30 | Currency guard: `resolvePlaidCurrency('','BTC')` returned null (= assumed USD) — a blank iso shadowed a populated crypto code, summing the account at 1:1 | `canonicalizeCurrency(iso ?? unofficial)` — `??` only falls through on null/undefined, so an empty-string iso kept '' and canonicalized to null | "a defensive parser must fail SAFE on malformed feed data, never fail open to the corrupting default" | `tests/unit/currency.test.ts` ("does NOT let a blank/whitespace iso shadow a populated unofficial code"). Fix = `canonicalizeCurrency(iso) ?? canonicalizeCurrency(unofficial)`. Hostile critic wf_74fc0808 (P2), DECISIONS #135. |
| 2026-07-01 | Manual add-transaction filed under one of the user's own CUSTOM categories ("Golf") always threw `Unknown category "<cuid>"` — the form offered the category (getVisibleCategories) but the entry could never save | prepareManualTransaction's defense-in-depth re-check validated the explicit id against the system-only static CATEGORY_BY_ID, unaware of the per-user custom rows the server action had ALREADY verified via assertOwnedCategory | "two validators of one write must agree on the valid set — a defense-in-depth re-check STRICTER than the source of truth turns valid input into an outage" | `tests/unit/manual-custom-category.test.ts` (test_regression__manual_txn_custom_category, driven through the REAL createManualTransaction — proven fail-before/pass-after) + 3 engine cases in transactions-manual.test.ts. Fix = `extraValidCategoryIds` param (default empty = unchanged); the action passes the one assertOwnedCategory-verified id. DECISIONS #136. |
| 2026-07-02 | Resync silently REVERTED "just once" triage corrections: SimpleFIN's 5-day overlap re-sends recent rows every sync and the upsert's `update` rewrote categoryId/needsReview/confidenceBps from the fresh pipeline verdict (Plaid identically for `modified` rows) — a corrected unknown-merchant txn went BACK into the review queue with the correction row orphaned | both sync update paths persisted the full pipeline verdict unconditionally; the only durable artifact ingest consults is a CategorizationRule, and "just once" deliberately creates none — so nothing marked the row as user-decided | "a user decision outranks the pipeline — a sync refresh may update what the BANK knows (date/amount/status/descriptor), never what the USER decided (category verdict)" | `tests/unit/sync-preserves-corrections.test.ts` (both providers, real sync paths + real applyCategory, proven fail-before/pass-after; untouched-row control locks the guard's scope). Fix = correction-scoped verdict-field strip in plaid.ts update + simplefin.ts upsert-update. PULSE_CATEGORIZATION_FIX Phase 3d, CATEGORIZATION_DIAGNOSIS item 4. |
| 2026-07-02 | fileMerchantGroup with a MERCHANTLESS anchor (manual/CSV rows) mass-filed EVERY null-merchant review row under one category — `{merchantId: null}` is a real Prisma IS-NULL filter, so one tap on a "Local Coffee" card also filed the hardware store and everything else unmatched | similarTransactionsWhere built merchant scope from a nullable id without a null guard, and the group card was keyed per-canonical while the action scoped per-IS-NULL — consent and effect diverged | "a nullable id is never a scope — null must select NOTHING or a defensively narrower set, and a card's count must equal exactly what its action mutates" | tests/unit/triage-groups.test.ts ("P0 lock: a MERCHANTLESS anchor files ONLY its exact-descriptor rows"); mechanically reproduced pre-fix by 3 independent checker lenses (wf_908cf9a8, file:line traces in the workflow output). Fix = null-merchant → exact-descriptor scope in similarTransactionsWhere + groupKey `raw:` so count ≡ scope. Phase-3 checker cycle 1. |
| 2026-07-02 | Checker cycle-1 P1 batch on the rebuild: (a) sync guard was check-then-act — a concurrent group-file could be silently overwritten by the in-flight sync verdict; (b) fileMerchantGroup fetched targets outside its transaction — double-fire recorded stale-fromCategoryId corrections (undo would revert the OTHER session) + duplicate equal-priority rules; (c) Plaid pending→posted id churn orphaned corrections (the 3d guard only covered surviving ids); (d) split parents/undone rows mishandled by the guard predicate; (e) singles-mode leaked onto the next merchant card; (f) empty-state undo double-tap | the 3d/3b increments guarded the common paths but not the concurrent, id-churn, and container edges — races and identity churn need the same compare-and-set discipline as backfill.ts | "every guard needs its concurrency story: check+act in one write transaction, preconditions re-asserted inside the write, identity churn transplants state, containers are never resurrected" | sync-preserves-corrections.test.ts (+3: undo-edge, split-parent, pending→posted transplant), triage-groups.test.ts (+2: P0 scope, double-file/rule-dedupe); checker wf_908cf9a8 mechanical pre-fix reproductions on file. Fixes: atomic guard txns with fresh in-tx reads, isTransfer joins the preserved verdict, fetch-in-tx + needsReview re-assert + rule dedupe in fileMerchantGroup, pending_transaction_id transplant (Plaid), UI mode/undo/error hardening. |
| 2026-07-02 | Checker cycle-2 on the rebuild (20/20 confirmed): (a) P0 — a split PENDING parent posting under a new Plaid id was deleted with `isSplitParent` dropped: children dangled (no FK) AND a new full-amount row DOUBLE-COUNTED the charge; removed[] likewise orphaned children of canceled split charges, and a same-id amount drift kept a stale split whose parts no longer summed; (b) P1 — every "atomic" check-then-act guard (both sync guards, the transplant, fileMerchantGroup, recategorize) rested on SQLite write serialization; production Postgres runs READ COMMITTED, where a concurrent commit between in-tx read and write clobbered decisions, recorded stale-fromCategoryId corrections, or minted duplicate rules — and the transplant even read its predecessor OUTSIDE the tx; (c) P1 — the singles-leak fix set `groupEmptied` inside the setGroups updater and read it synchronously: React skips eager updater evaluation whenever the fiber has pending updates (deterministic on the write-in path), so the reset silently no-oped; (d) P2s — merchantless file scope matched same-descriptor rows on a DIFFERENT (m:) card; SimpleFIN findFirst+create reopened the CQ-2 create/create race (Plaid same shape); removed[] applied per page defeated the transplant when the remove landed a page early; the rule dedupe ignored the five condition columns; recategorize never deduped at all | cycle-1 shipped correct SQLite-local semantics but never asked "on which isolation level does this argument rest?", side-effected a React updater, and left identity/partition edges (split containers, page ordering, card-vs-scope) uncovered | "an atomicity argument must name its isolation level and hold on the DEPLOYED database, not the test one; a React updater is pure — derive, don't side-effect; a card's partition and its action's scope must be THE SAME function" | serializable-tx.test.ts (helper contract: level requested, P2034-only bounded retry, fail-loud); sync-preserves-corrections.test.ts +6 (transplant carries split / drift dissolves ×2 providers / removed cascades children / early-page remove buffered / preserved-split children post) — proven fail-old/pass-new by stash-run: 8 red at proof time, 9 on the final tree (the preserved-split-children lock was added after the stash run; cycle-3 checker independently re-measured 9 — count corrected per its finding); triage-groups.test.ts +4 (raw:-vs-m: partition, same-canonical separation [prophylactic gate lock — passes old code by design], conditional-rule dedupe, recategorize dedupe — 3 proven fail-old); phase2-triage.spec.ts singles-leak write-in e2e (fail-old by mechanism inspection only — React eager-bailout timing). Fixes: serializableTx (SERIALIZABLE + bounded P2034 retry, probed OK on better-sqlite3) wraps all five guard sites; predecessor read moved in-tx; transplant carries/dissolves splits; removed[] buffered per item + child cascade; P2002→guarded-update fallback both providers; scope pins merchantId (merchantless only); ensureUnconditionalRule shared mint. PG-race closure is isolation-level reasoning, NOT reproduced on SQLite (see STATUS honesty note). DECISIONS #146. |
| 2026-07-02 | Checker cycle-3 on the cycle-2 fixes (16/16 confirmed, 1 refuted): (a) P0 — SimpleFIN pending→posted NEW-ID churn made a stale pending split IMMORTAL (reconcile excluded split parents in BOTH passes; children shielded by providerRef not-null) → children + the freshly-ingested posted row both counted, double-counting the charge PERMANENTLY in cash-needed and every projection (the DECISIONS #128 "self-healing ≤32d" rationale was provably false for this class); (b) P1 — all three dissolve sites inherited the raw pipeline verdict, so a user rule / confident merchant match auto-filed a dissolved split SILENTLY (no triage card) — mechanically probed: NETFLIX drift dissolved to needsReview:false; (c) P1 — applyCategory (the HIGHEST-traffic single-row writer: singles fileRow + recategorize scope:'one') was a fully unguarded check-then-act — not even one transaction — recording stale fromCategoryId whose undo reverts the other writer, and a crash mid-sequence persisted a correction never applied; (d) P1 — an unconditional rule to a DIFFERENT category was never retired, and the equal-priority stable-sort tie-break made the STALE rule win every future ingest (probed: recategorize coffee→dining still filed 'coffee'); (e) P1 gate — stripping serializableTx from every guard site left the whole suite GREEN (empirically sed-proven by the checker); (f) P2s — removed[]-cascade's own read ran outside its tx; applyCategory(always) + makeRuleFromCorrection still raw-minted duplicate rules; deleted-in-window rows threw P2025 aborting the whole SimpleFIN pass-2 loop; 'rule.create' audit events logged for REUSED rules; ledger row miscounts | the cycle-2 increment applied its own new discipline unevenly: the invariant said "every churn path" but only Plaid's linked churn was covered; "dissolve to review" was implemented as "dissolve to pipeline verdict"; the guard-site inventory missed the sixth writer; the mint helper deduped same-category only; and the isolation fix shipped with no wiring lock | "an invariant claimed for EVERY path must be enumerated against every provider's churn modes, not the one that exposes a link; a dissolved user decision re-decides UNCONDITIONALLY — no inherited confidence; a guard-site inventory includes every writer of the guarded rows; a fix without a lock on its own wiring is one refactor from silent reversion" | serializable-wiring.test.ts (spy: 4 triage actions route through serializableTx + provider source pin ≥3 sites & zero interactive $transaction — the checker's own sed experiment now goes red); sync-preserves-corrections.test.ts +4 (sf_new_id_churn_dissolves_stale_split, aged_out_split_dissolves, dissolve_forces_review_despite_rule ×2 providers); triage-groups.test.ts +2 (stale_rule_wins_recategorize, always_stacks_duplicates incl. makeRuleFromCorrection reuse). ALL 9 proven fail-old by stash-run (9 red pre-fix / green post). Fixes: reconcile dissolves stale/aged pending split parents WITH children (read-in-tx, both passes); dissolve forces needsReview:true + confidenceBps:null at all 3 sites (rule still supplies the SUGGESTION); applyCategory + makeRuleFromCorrection rebuilt as serializableTx with fresh in-tx reads through ensureUnconditionalRule; the helper now SUPERSEDES other-category unconditional rules (retire-on-changed-mind) and its four surfaces share it; removed[]-cascade read moved in-tx; guardedVerdictRefresh skips deleted-in-window rows; audit gated rule.create/rule.reuse on minted. DECISIONS #147. |
| 2026-07-02 | Cycle-4 FINAL confirmation (1 P1 + 8 P2, owner-authorized cycle-5 fixes): (a) P1 — the cycle-3 "dissolve forces review" fix survived exactly ONE sync: the dissolved row carried no durable marker, was representationally identical to an UNDONE row, and the (correct) cycle-1 undone-takes-fresh-verdict rule let the user's own merchant rule silently auto-file the drifted charge one cron interval later (empirically probed twice); (b) reconcile dissolve fired on FALSE staleness — a garbled feed row (parse-skip) or one transiently-absent snapshot read as absence and destroyed a still-real user split in-window; (c) the Plaid same-id dissolve, the P2025 skip, and the audit gating were fixed-but-unlocked (revert-stays-green proven); the wiring pin was comment-satisfiable, blind to non-async interactive callbacks, and absent for triage-actions; a superseded rule left becameRuleId dangling ("success" reporting a dead rule); the reconcile Safety docstring still asserted the pre-#147 invariant | a forced state that must OUTLIVE the process needs a DURABLE representation — in-band flag combinations collide with real states; and "absence" signals from an untrusted feed must be corroborated at the RAW layer, not the post-parse layer | "state that must survive re-derivation gets a column, not a shape; corroborate at the rawest layer you received; every fix ships the lock that makes its own revert red" | Transaction.reviewPinned (schema #148) + preserve-predicate + churn-carry + clear-on-file, locked multi-sync at ALL THREE dissolve sites + churn (sync-preserves +2 new, 3 extended); raw-id corroboration (simplefin) + garbled-row lock; age-out-only split sweep (owner call #27) with the reconcile lock rewritten to the 3-state contract (corroborated kept / in-window survives / age-out dissolves, no orphans); makeRuleFromCorrection live-rule check + lock; wiring pin hardened (non-comment count, any-shape ban, triage-actions allowlist=4); deleted-in-window + audit-provenance locks. 8 locks fail-old-proven by stash-run. DECISIONS #148. |
| 2026-07-02 | Cycle-5 scoped confirmation (4/4 confirmed): the /triage BACKFILL button was a seventh needsReview:false writer blind to reviewPinned — one tap re-ran the user's own rule over a dissolve-pinned row, silently auto-filed it, and left a contradictory pinned-but-filed row that no surface could ever clear (not in triage; preserve predicates then froze it; a later id churn popped it BACK into review); the in-window sweep could DELETE a pinned PENDING row on one flaky absence and re-create it next sync on the rule verdict (pin laundering); the wiring pin's comment-stripping missed trailing and block-interior comments | the pin inventory was built from the SYNC writers; bulk system actions (backfill) and delete+recreate paths are writers too — "every writer of the guarded rows" means every path that can produce or destroy the guarded state, not just the ones in the diff under review | "a guard's writer inventory is complete only when it covers producers, mutators, AND destroyers of the guarded state — enumerate by querying for the field's absence, not by memory" | sync-preserves-corrections.test.ts +2 (backfill_respects_pin, sweep_launders_pin — both fail-old-proven by stash-run); fixes: reviewPinned:false in backfill's select + CAS re-assert; reviewPinned:false in the in-window sweep where; stripComments hardened (block + trailing). STATUS cycle-5-confirmation note; owner authorization (fix round + scoped confirmation) now fully spent. |
| 2026-07-03 | The SimpleFIN holding-currency gate refinement (#156) widened the #133 breakdown-preservation guarantee: `|| withheldNonUsd > 0` opened the reconcile whenever ANY row read non-USD, so a MIXED non-empty feed (one foreign lot withheld + one un-mappable glitch row → zero USD positions) reconciled with an empty synced set and PRUNED still-held USD rows that #133 was written to preserve | the new all-foreign-prunes clause reasoned only about "we interpreted the feed" and forgot the un-mappable-glitch axis — a garbled feed whose garble co-occurs with one foreign row must NOT count as a clean interpretation | "when a guard opens on a NEW positive signal, re-check every axis the OLD guard protected — an all-foreign reconcile must exclude the case where un-mappability (skipped>0) is also present, or it silently regresses the sibling guarantee" | `tests/unit/simplefin-holdings-sync.test.ts` ("does NOT prune held holdings on a mixed non-empty feed (one foreign + one un-mappable → 0 USD)") — proven fail-before/pass-after by reverting the qualifier (coarse gate → removed:2, count 0; fixed → removed:0, count 2 preserved). Fix = `(withheldNonUsd > 0 && skipped === 0)` qualifier at simplefin.ts ~475. DECISIONS #156, Checker P2. |
| 2026-07-04 | Passive learn-from-corrections (#161) over-generalized across FOUR hostile-critic cycles: a learned rule minted from >=2 corrections on ONE payee blanket-filed UNRELATED payees. c1 — numeric-payee aggregates (one "ZELLE -> rent" filing EVERY Zelle) because the payee-less detector was an enumeration of channel words, defeated by a phone/account number in the payee slot; c1 — an unguarded canonical branch + no match-time sign guard; c2 — same class deeper (SEND/MONEY/BANKING wording; an HMSHOST "Airport Dining" bucket canonical firing a THIRD unrelated outlet); c3 — payee-less generic MECHANISM labels (DIRECT DEBIT / POINT OF SALE / SERVICE CHARGE / LOAN PAYMENT) passing the distinguishing-token guard; c4 — bare payment-FREQUENCY / card-ENTRY labels ("AUTOMATIC PAYMENT <date>", SCHEDULED/REGULAR/PERIODIC/GENERAL PAYMENT, PIN PURCHASE) with NO number to separate billers, so biller-A's learned category transplanted onto an unrelated biller-B (reproduced end-to-end: a car-loan outflow auto-filed 'rent') | an enumeration / word-list approach to detecting a payee-less residue is fundamentally incomplete (the module header now names it "an unwinnable whack-a-mole"); the durable fix is STRUCTURAL — an identity-preserving signature that KEEPS account/phone/check numbers so two distinct payees can never collapse to one key regardless of wording — plus, for the residual payee-less-AND-number-less bare-label class that genuinely has no identity token, an AI-badge backstop (auto-file at 8500 with needsReview:false) that bounds any missed label to a VISIBLE correctable guess rather than a silent misfile | "over-generalization safety must rest on a STRUCTURAL invariant (distinct payees carry distinct identity tokens -> distinct keys), never on the completeness of a denylist; where a class genuinely has no identity token (bare payee-less labels), bound the failure SEVERITY with a visible-correctable backstop instead of trusting the list to be complete" | tests/unit/learn.test.ts "hostile-critic regressions" block — P0 numeric-payee never blanket-files across ZELLE/ONLINE BANKING/WEB PAY/ACH PPD wordings; P1 payee-less verbatim refused; P0 store-card canonical; P1 HMSHOST outlets; P1(c3) generic mechanism labels refuse while GEORGIA POWER learns; P1(c4) "AUTOMATIC PAYMENT" never blankets an unrelated car loan + 8 sibling bare labels refuse + a branded "AUTOMATIC PAYMENT ALLY AUTO" still learns; match-time sign guard — plus the computeDescriptorSignature/hasDistinguishingToken structural canaries and tests/unit/learn-loader.test.ts (real recategorize -> loadUserRules -> categorize chain: one correction not enough, the second mints a rule). Each canary is fail-old by construction (an empty NOISE_TOKENS or the removed canonical mode reproduces it). DECISIONS #161. |
| 2026-07-04 | "Accept all confident" bulk drain (#162): the per-group graceful-partial + fail-loud contract — the whole reason acceptAllConfident loops fileMerchantGroup per group rather than aborting on the first failure — shipped UNTESTED (hostile-critic coverage P1, self-downgraded to P2: shipped code correct, latent regression risk). Mutating the per-group `catch` to rethrow (abort the drain on the first bad group), or dropping the failed group from the returned queue, passed all original tests | the feature's central branch was locked only on the all-succeed and zero-confident paths; the catch block + "a failed group reappears in the fresh queue" + the total-failure throw were never exercised, so a refactor could silently turn graceful-partial into abort-on-first | "test the branch that IS the design: if a feature loops-with-per-item-catch specifically to survive a partial failure, the partial-failure and total-failure paths are the load-bearing tests, not the happy path" | tests/unit/accept-all-confident.test.ts — "partial failure: an unfileable confident group is SKIPPED, the rest still file... no throw" (a rule pointing a merchant at ANOTHER user's custom category makes the group confident-but-unfileable: getTriageGroups suggests it, fileMerchantGroup's assertOwnedCategory rejects it → merchantsFiled=1, no throw, failed group still queued + in res.groups) and "total failure: every confident group unfileable → throws (fail-loud), nothing filed"; plus the golden lock "the demo seed has ZERO confident groups" (non-vacuous: asserts groups>0 AND confident==0). Fail-old by construction (rethrow-on-catch reddens partial; a no-op-on-total-failure reddens total). DECISIONS #162, Checker coverage P1→P2. |
| 2026-07-04 | Categorization-quality pass (#163) hostile-critic cycle 1 — 3 P1 proven by execution: (P1-1) five new Income-group payout KNOWN entries (Stripe/Buildium/Concur/Dasher/Rent-Payout) were sign-blind at SILENT 9600 — a Stripe balance debit, a tenant PAYING rent through Buildium, or a Concur/Gusto/ADP fee auto-filed as INCOME with no badge (inflates income AND erases spend — the income-side analog of the F4 transfer-erasure class); (P1-2) the DECISIONS claim "sign guards generalized to the Income GROUP (backfill + LLM assist)" was FALSE for the assist — the categorize-assist edit had silently failed to land (a rejected Edit went unnoticed), so LLM-rescued inflow paychecks were still refused (literal-'income' check predating the income split) AND the newly-Plaid-wired assist could file an OUTFLOW the LLM called 'interest-income'; (P1-3 family) new generic tokens reintroduced the exact location/name-word collision class the same pass fixed for UNIVERSITY: bare CARTER (JIMMY CARTER BLVD → kids), bare CHURCH (CAVA FALLS CHURCH VA → charity), bare HOA (PHO HOA → hoa), bare FIDELITY (FIDELITY NATIONAL TITLE $1.5k escrow → investment), bare PROGRESSIVE (PROGRESSIVE LEASING → auto-insurance), GOODWILL INDUSTRIES #NN register purchases → charity, prefix-open CARDMEMBER SERV erasing an INTEREST CHARGE as transfer | (1) the merchant-default path was the ONLY tier without a #44 sign check — every guard audit had inventoried hints/learned-rules/backfill but not the default path the new Income-group entries ride; (2) a multi-edit turn treated a failed tool call as landed — the claim was written from intent, not from a re-read; (3) token vetting checked the brand's OWN forms but not the token's OTHER American-English senses (people/places/unrelated companies) | "every tier that can COMMIT a category carries its own sign check — new Income-group mappings are exactly the rows that find the unguarded tier; a claim about an edit is only true after re-reading the file; a new \b token ships only after asking what ELSE that word names (cities, roads, banks, restaurant chains)" | tests/unit/categorize-precision.test.ts 'hostile-critic cycle-1 locks' (20 cases): 7 outflow-income descriptors → review never silent income + the inflow direction still files; 9 collision descriptors never auto-file to the greedy leaf + the qualified forms still resolve; GOODWILL register-vs-donation split; CARDMEMBER SERV payment-vs-interest-charge. Fixes: pipeline.ts merchant-default outflow→Income-group guard (also closes the pre-existing GUSTO-FEE/INTEREST-REVERSAL 8500 class); categorize-assist.ts Income-GROUP guard BOTH directions (re-applied after Read, verified in-file); tokens tightened (CARTER'S/apostrophe-or-store-number, denominational CHURCH phrases, HOA dues-context, FIDELITY INVEST*, PROGRESSIVE+insurance-token, GOODWILL register forms → clothing, CARDMEMBER SERV+payment-token); the critic's probes added to the NOVEL corpus as 14 adversarial traps so the benchmark can't go green on happy paths alone. DECISIONS #163. |
| 2026-07-05 | The phase2-triage e2e stall (STATUS 2026-07-04, "a server action that neither errors nor returns"): one spec per full-file run timed out at 60s on a triage button stuck disabled-while-pending; it roamed between specs and trees and was blamed on the SQLite writer. Client net-event + server entry/exit probes proved otherwise: the action committed in ~5ms and the response even net-finished — but Next had aborted EARLIER superseded action streams (net::ERR_ABORTED), their router flight-data application never resolved, and React's transition-lane ENTANGLEMENT kept `useTransition.pending` true forever, disabling every button. Two masked failures surfaced the moment the stall was fixed: the singles-mode test deterministically failed (the write-in test net-files the demo's ONLY multi-row group before it) and the #162 banner lock failed (it ran AFTER the review-cost test drained the whole queue) | (1) a pending flag derived from useTransition entangles with the Next router's lanes — any wedged lane anywhere freezes it, so a state-owning island must own its busy flag; (2) an always-failing test MASKS every defect behind its failure point — the tests that "did not run" for weeks hid two real ordering bugs; (3) diagnosing by correlation (load, sequence length) pattern-matched to the KNOWN SQLite flake — only boundary probes (client send/headers/body-fin × server entry/exit) split the layers honestly | "a shared pending flag is only as reliable as every lane it entangles with; when a chronic failure is fixed, re-run everything BEHIND it before declaring the tail healthy; diagnose hangs by instrumenting the boundaries, not by correlating with load" | tests/unit/action-deadline.test.ts (test_regression__triage_pending_stall_bounded + 4 siblings, fail-old-proven: a never-settling promise times the suite out on the old unbounded await); tests/unit/llm-categorize.test.ts test_regression__llm_fetch_hang_bounded (fail-old-proven by stash-run: old code times out at the 20s test budget); fixes: explicit useState busy flag + withDeadline at all four dispatch sites + refreshTriageQueue re-sync recovery (triage-inbox.tsx), 7s AbortController on llm-categorize fetches, hermetic e2e keys, SERIAL-RESIDUE CONTRACT ordering in phase2-triage.spec.ts. DECISIONS #164. |
| 2026-07-05 | "CREDIT CARD PAID" (and any pair whose descriptor the normalizer doesn't know) sat WEDGED in the triage queue forever: cross-account pair detection set only isTransfer:true, so the row was excluded from every sum yet kept needsReview:true + a wrong guessed category — the user had to hand-file it (the #161 learned-rule workaround exists because of this). Plus 2 critic-cycle-1 P1s in the fix itself: undoing a transfer-flagged filing restored a row the queue guard instantly HID and the next sync re-filed over the user's explicit re-decision (F1); the "apply to N similar" batch scope kept counting/mutating hidden wedge rows the queue no longer shows, breaking the #23/#44 count-equals-mutation invariant (F2) | pair detection's persistence was add-flag-only (updateMany {isTransfer:true}) while categorize() only clears needsReview on the DESCRIPTOR path — no code path ever resolved a pair-only row's review state; the cycle-1 P1s came from adding a visibility guard without auditing every surface that writes needsReview:true (undo) or reads it bare (batch scope) | "when a detector's verdict is persisted, persist the WHOLE verdict (flag + category + review state), not one bit of it; when you hide rows from a queue, audit every producer (restore paths) and consumer (counts, batch scopes) of the hidden state for agreement — the no-drift invariant spans ALL of them" | tests/unit/transfer-pair-filing.test.ts (15: pure plan incl. pending/currency/pin/heal + integration: wedged pair filed at 8500 flagged-band + out of queue/badge/singles, idempotent rerun, user-resolved never clobbered, pin wins and surfaces, EUR/pending flagged-not-filed, batch scope counts 1-not-2 while register still reaches transfers, undo pins + resurfaces + next sync files 0, backfill leaves a pipeline-confident wedged NETFLIX row untouched — fail-old by construction); tests/unit/categorize-assist.test.ts transfer-refusal both directions. DECISIONS #165. |
| 2026-07-05 | #165 cycle-2 checker P1 (caught pre-commit): refreshTransferFlags' filing updateMany used a bare `id IN (fileIds)` where — none of the read guards re-asserted in the write — so a user decision landing inside the read→write window (which contains an awaited ensureCategories call) was silently clobbered back to 'transfer', or an undo-PIN raced into the unclearable pinned-but-filed wedge | the plan was computed from a snapshot and the applier trusted it: the same read-guard-vs-write-where drift the backfill cycle-5 P1 mandated re-assertion for — a guard that exists only in the read is not a guard | "every write that acts on a planned snapshot re-asserts the plan's guards in its own where-clause; a returned count is the guarded write's real count, never the plan's length" | tests/unit/transfer-refresh-race.test.ts (test_regression__transfer_filing_read_write_race, 2 variants: mid-window user file → decision stands + flag still lands; mid-window undo-pin → no filing over the pin; deterministic via a mocked ensureCategories performing the racing action inside the window; fail-old by construction). DECISIONS #165 cycle-2. |
| 2026-07-05 | #166 seamlessness audit P0: since #163's leaf taxonomy, a REAL user's payroll (PAYROLL / DIRECT DEP / GUSTO / ADP → 'paycheck') was classified by `monthlyFlows` as a *refund netted against expenses* — the income test was the LITERAL id `'income'`, predating the leaves — so production income read $0 and savings rate / FI number / coach / Money Review / assistant income answers were all garbage; every golden stayed green because the demo's own payroll matches a merchant-specific rule mapping to the old 'income' id. Same stale-id class in `isBudgetable`: 'Paycheck' was the budget-target picker's DEFAULT option | an id-literal check written before a taxonomy split silently misclassifies every new leaf; the demo seed's happy path (merchant-specific rule → old id) masked the generic path real users hit | "when a category taxonomy splits, grep every `=== '<old-id>'` comparison the same day — and test the GENERIC classification path with descriptors the seed does NOT contain, because goldens only witness the seed's path" | tests/unit/insights.test.ts test_regression__monthly-flows-income-leaves (paycheck salary = income at 8667 bps + an EVERY-Income-leaf canary + netting/custom-category preservation; 4 fail pre-fix, stash-run proven) and tests/unit/budgets.test.ts test_regression__budgetable-income-leaves (every Income leaf excluded + credit-card-payment excluded, custom kept). Fix = group-aware `isIncomeCategoryId()` in categories.ts used by monthlyFlows + isBudgetable. DECISIONS #166. |
| 2026-07-05 | #166 audit P1 (three agents converged): client-side navigation was randomly SWALLOWED app-wide on Next 15.5.x — RSC fetch returns 200, router never commits, no error: calendar month arrows dead for any TARGET month with events (deterministic, scripts/audit-probes/calendar-month-nav.ts 5/7 fail), /transactions filters snapping back / dead pagination + Import links (agent probes 4/4 first-action failures), budgets/goals mutations succeeding server-side with a stale page. Previously misread as the "phase4:13 / load-correlated e2e flake" (the #164 lesson repeating). Buffering the response (route-interception) cured it → upstream client flight-application bug, unaffected by prefetch={false}, NOT fixed by 15.5.20 | an upstream framework defect presenting as nondeterministic per-page flakes pattern-matches to "environment"; only a deterministic minimal repro (fixed payload = calendar month) plus a buffered-vs-streamed differential split framework from app | "when the same impossible symptom (server 200 + client no-op) roams across unrelated surfaces, suspect the shared framework layer and build a DETERMINISTIC repro before attributing to load; a Heisenbug that vanishes under interception is a streaming-consumption bug" | Fix = Next 15.5.19 → 16.2.10 (major, verify-gated): cal-probe6 7/7 COMMITTED, transactions first-action probe 8/8, FULL e2e 75/75 green on the upgrade. Locked by the existing phase4:13 calendar next-month e2e + transactions filter specs, which now pass deterministically. DECISIONS #166. |
| 2026-07-05 | #166 mutation-reliability arc (budgets/goals forms): (1) invalid money input ("abc", and even the natural "$500"/"1,000") THREW from createGoal/setBudget through a plain form action to the app error boundary — a typo cost the whole page + all typed input; (2) once converted to useActionState, React 19's auto-reset wiped the fields on the very validation failure (critic P1) and the budget select snapped to the first option so a retyped amount could write a target to the WRONG category; (3) the deeper roulette: post-action page application (form-action pipeline AND router.refresh(), transition-wrapped or not, SW present or absent, prefetch storm removed or not) silently failed ~50% per action in plain-paced probes on Next 15.5.19 AND 16.2.10 — mutations committed server-side while the page stayed stale and useActionState's pending could wedge forever (the #164 class beyond triage) | (a) validation at a form boundary must return data, never throw; (b) useActionState/form-action couples three behaviors (auto-reset, lane-entangled pending, response-embedded re-render) that each independently break UX when the framework misbehaves; (c) e2e green ≠ healthy — Pixel-5-paced specs outran the race for months while the STATUS #16/#17 "flakes" were this defect | "for infrequent mutations, confirmation must be structurally unable to lie: own busy flag (useState) + direct action call + withDeadline + full reload on success (the re-rendered page IS the confirmation); reserve reconciliation-from-returned-data (#164 triage) for high-frequency surfaces; and treat 'works in e2e, flaky by hand' as a pacing-sensitive race, not an environment flake" | Witness: scripts/audit-probes/budget-mutation.ts 5/5 consecutive full set→clear→set runs deterministic (was ~50%), scripts/audit-probes/budget-invalid-input.ts (inline error + BOTH fields preserved + no crash), scripts/audit-probes/goal-budget-mutation.ts (goal add/delete/abc all healed); unit: money.test.ts parseDollarInput (lenient + comma-grouping rejection), budgets.test.ts, goal/budget action tests via custom-category-lifecycle. E2E pwa-offline.spec.ts rewritten to drive a server action UNDER A CONTROLLING SW. Fixes: parseDollarInput; result-returning createGoal/setBudget; GoalForm/BudgetTargetForm/ClearBudgetButton/DeleteGoalButton on the busy+deadline(8s, form-deadline.ts)+reload pattern, MoneyDialsForm on the same minus the success-reload (inline confirmation from own awaited state; its useActionState confirmation failed identically in the gate); sw.js v3 (no fetch handler — interception amplified the abort class for zero offline value); nav prefetch={false} (12-link re-prefetch storm on every revalidatePath). DECISIONS #166. |
| 2026-07-05 | #167 app-wide reliable-mutation pass: the five surfaces still on useTransition + router.refresh() (register recategorize, accounts manual add/value/delete/statement, backfill, settings custom-category CRUD, settings visibility toggles) all inherited the #166 ~50%-loss application race — witnessed live: scripts/audit-probes/recategorize-mutation.ts lost 0/2 rounds at plain pacing on the old wiring (chip stayed stale after "Just this once") and transactions.spec.ts:145 failed in the full run + 1/2 solo the same session; backfill's router.refresh() could never update TriageInbox's client-held groups at all, so "Auto-filed N" sat beside the undrained rows | router.refresh()'s RSC application is a coin-flip at human pacing (#164/#166 root cause) and refresh cannot reach client-held state regardless | "every infrequent mutation confirms via the recipe: own useState busy + withDeadline + reload on success (flash.ts carries text reports across the reload); a message about server state must come from the same render that proves it" | Witness: recategorize probe 0/2 → 2/2, accounts + backfill + budget + first-action probes all PASS on the converted build; FULL e2e 75/75; critic P1 (post-reload pre-hydration click drops in the throwaway-user spec) fixed with state-aware click-and-verify retries, re-witnessed 3/3 on the exact failing mix; tests/unit/flash.test.ts (5) + tests/e2e/backfill.spec.ts new. DECISIONS #167. |
| 2026-07-07 | The /triage categorization-accuracy card never recovered after an undo: filing a review row stamps CategoryPrediction.actualCategoryId (ground truth, #37), so a mis-filed ambiguous group scores as a miss and drops the displayed accuracy — but undoCorrections restored the transaction to review + removed the minted rule while leaving actualCategoryId SET, so getCategorizationAccuracy (counts predictions WHERE actualCategoryId is not null) kept scoring a decision the user retracted and accuracy stayed dropped forever (#166/#168 open follow-up (e)) | undo persisted only PART of the retraction: it reverted transaction state (categoryId/needsReview/confidence) + rule lineage but NOT the accuracy label the SAME filing wrote — the file path had four actualCategoryId writes and the undo path had zero, an asymmetry no test covered (accuracy-score.test.ts is engine-only) | "when a mutation writes to N tables its undo reverts all N — a filing that stamps a ground-truth label must un-stamp it on undo; the checkable invariant: a needsReview row carries no confirmed label (file<->undo symmetric); and the correct undo semantics is NULL, not prior-label-restore, because the row returns to undecided" | tests/unit/accuracy-undo.test.ts (2, real applyCategory->undoCorrections against throwaway data: a MISS filed then undone -> transaction restored to 'shopping'/review AND actualCategoryId null AND getCategorizationAccuracy n back to 0; a HIT undone also un-counted, guarding a null-only-on-miss fix that would inflate accuracy). Fail-old PROVEN by stash-run (2/2 fail without the fix; the un-nulled sample even leaks into the sibling test's count). undoSplit needs no counterpart — split never labels. DECISIONS #169. |
| 2026-07-07 | #170: manual add-transaction was a plain `<form action>` whose server action THREW on reachable form input (a non-numeric or non-positive amount — the amount box is free text) straight to the app error boundary, losing the page + all typed input on a typo; the first fix attempt (useActionState) then hit React 19's auto-reset silently reverting the account `<select>` to the first option, so a corrected retry could file the transaction to the WRONG account | (a) validation at a form boundary must RETURN data, never throw; (b) a `<form action={fn}>` auto-resets after every dispatch and the account revert is a silent MIS-FILE (not mere annoyance), so this app's proven recipe is a plain onSubmit (never triggers the reset) calling the action directly, NOT useActionState — the #166 finding, re-confirmed here by an e2e that failed on the useActionState attempt at `expect(account).toHaveValue(chosen)` | "manual-entry validation returns an inline error (never the error boundary) AND writes no row on the bad path; the correction flow preserves the user's entries incl. the chosen account — a mutation form uses onSubmit + own busy + withDeadline + navigate/reload on success, never useActionState" | tests/unit/manual-txn-validation.test.ts (3: abc/0/-5 → {ok:false} + no row; fail-old PROVEN by defeating the try/catch → the engine throw propagates, 3/3 fail) + tests/e2e/transactions.spec.ts:145 (picks a non-default account, bad amount → inline error, no navigation, account+descriptor+amount all preserved; fail-old WITNESSED live on the useActionState attempt — expect(account).toHaveValue(chosen) failed the full gate). DECISIONS #170. |
| 2026-07-09 | desktop Settings overlapped Sign out after #187 | AppNav flex-1 expanded 13 text links into Sign-out sibling | LOOP #3 surgical | tests/e2e/desktop-header.spec.ts (#188) |
| 2026-07-09 | Gap 6 §2: no prod error visibility; risk of always-on third-party or PII leak if bolted on carelessly | n/a (new feature) — dormant-until-DSN invariant must hold like email/push | "a monitoring control must be zero-network without credentials and must scrub PII before send" | tests/unit/errors.test.ts (no DSN → no fetch; scrub strips emails/secret keys; invalid DSN → no fetch) |
| 2026-07-09 | #177's accuracy panel and #190's threshold tuning had NO live data source: every ingest path (Plaid/SimpleFIN/CSV/manual) created transactions without CategoryPrediction rows, so filing actions' labeling updateMany no-op'd for real users — "No data yet" forever, tuning permanently at insufficient-samples (demo seed masked it) | prediction logging was only ever implemented in prisma/seed.ts; the live write sites were never wired | LOOP prime directive (a shipped metric that can never receive data reports a capability that does not exist) | tests/unit/ingest-prediction-log.test.ts (4: SimpleFIN sync→log→label→tuning counts it; abstention logged but excluded; manual pipeline row logged / explicit pick NOT logged; CSV mixed rows — fail-old: findUniqueOrThrow on the prediction row throws without the #190 ingest wiring) |
| 2026-07-09 | Full `VERIFY_E2E=1` never exited 0 on this Windows machine — blamed on the "mobile-380 viewport flake" across #183/#186/#187 | MISATTRIBUTION: the real failure was DETERMINISTIC — #182 added a "Sign out of all devices" button (`revoke-sessions-submit`) on /settings whose accessible name contains "Sign out"; `auth.spec.ts` ends its nav loop on /settings then did a bare `getByRole('button',{name:'Sign out'})` → strict-mode violation (2 elements), every run. The viewport flake did not reproduce (2 full runs, 0 interception failures). | LOOP #6 fail-loudly (a red gate was accepted as a known env flake instead of diagnosed; the feature landed in #182 without updating the spec's now-ambiguous locator) | tests/e2e/auth.spec.ts:8 (locator scoped to `getByTestId('sign-out-form')`; the old bare locator re-fails with the strict-mode violation while both buttons are present) |
| 2026-07-10 | Zero-account /coach /goals /calendar showed dashboard "Welcome to Aimplifi" cash-needed framing | Shared EmptyDashboard was the early-return for every cash-engine route | retention: route should explain its own payoff | tests/e2e/auth.spec.ts (coach-empty/goals-empty/calendar-empty + route h1 + onboard-manual; empty-dashboard absent on those paths) |
| 2026-07-10 | Glass-Box share would leak card names if it screenshotted the live panel | Share must redact before clipboard/PNG; no server upload | PRIVACY: no live data leaves the client for marketing share | tests/unit/glass-box-redact.test.ts + tests/e2e/glass-box.spec.ts (share target has Card N, not Platinum/Sapphire/Freedom) |
| 2026-07-10 | No habit/streak celebration on savings-rate hero metric | MonthlyFlow series existed but no streak engine or copy | retention: small-win on hero metric | tests/unit/savings-streak.test.ts + coach-copy ALL_STRINGS + phase3-coach e2e (streak or PB visible) |
| 2026-07-10 | Triage swipe-left alternatives ignored Correction history | suggestAlternatives was fully generic | retention: one re-file should teach the next swipe | tests/unit/suggest-alternatives.test.ts (golden empty + rent hint + payee CANARY + conflict/undo/sign) |
| 2026-07-10 | Partner custom category vocabulary would leak into coach/reports/trends/recurring/triage if getCategoryMeta were widened for shared register rows (HOUSEHOLD critic F3) | shared-row category names need the owner's custom labels, but getCategoryMeta is the shared resolver behind six personal surfaces | �4.5 scope-not-copy: never widen getCategoryMeta; resolve shared labels via id-scoped lookup only | tests/unit/household-shared-txns.test.ts (categoryNamesByIds loads only requested ids; getCategoryMeta(viewer) lacks partner customs; shared view shows Partner Golf, not Secret Therapy) |
| 2026-07-10 | scripts/ledger.ts progress command silently ate the blank line after PROGRESS.md's title and wrote a stray bare-CR mid-file | PROGRESS.md is checked out CRLF on Windows even though its git blob is LF-only; the blank-line check compared a split line against the empty string, but a CRLF file split on bare \n leaves that line as a lone \r, not an empty string, so the check never matched | LOOP_ENGINEERING rule 6 (fail loudly, not silently) and CLAUDE.md's no-CRLF-churn caution -- a formatting script must not depend on an assumed line ending | manually reproduced against the real CRLF PROGRESS.md (git checkout -- PROGRESS.md to revert the bad write, then re-ran after switching every split/join in ledger.ts to detectEol()/\r?\n -- verified byte-exact CRLF preserved via cat -A; no dedicated vitest since the script has no test suite yet |
| 2026-07-11 | Household 'household'-scope cash-needed could silently fund from (and reveal the balance of) a partner's shared checking account | resolvePaymentAccount's CHECKING/first-account fallback re-ran inside cashNeededFromSnapshot against the MERGED snapshot, not the viewer's own pre-merge accounts, whenever the viewer had no checking/savings account of their own | HOUSEHOLD_ARCHITECTURE SS4.4 'payment account stays the viewer's own' + LOOP fail-loudly (a wrong funding source is a silent P0, not a coincidental-correct output) | tests/unit/household-cash-needed.test.ts describe('regression: household scope never funds from a partner's shared checking (critic P0)') -- fail-old PROVEN by temporarily removing the paymentAccountIdOverride call, restored after confirming the failure |
| 2026-07-11 | Transaction search typed before hydration was silently swallowed: the box blanked itself, the URL never gained `?q=`, and the user stayed on the unfiltered register with no error | `txn-search` was a CONTROLLED input (`value={search}` from `useState` + a `useEffect` resync); a Playwright/user `fill` landing before hydration attached `onChange` never reached React state, so the first render reset the DOM box to `''` and `commit()` pushed `/transactions` — the same URL, so the navigation never committed | LOOP fail-loudly (a dropped query must not look like an empty result set) + the repo's own mutation-form lesson: never let React state that hydration can clobber be the source of truth for what the user typed | `tests/e2e/transactions.spec.ts` `regression #216: a search typed before hydration still commits` (`goto` with `waitUntil:'commit'` forces the race) — fail-old PROVEN on mobile-380 before the fix, 17/17 green after |
| 2026-07-11 | CI-only failure: self-audit-server.test.ts "upserts rates ... unknownAttempts" expected 1 got 0 on push e1fca4a, deterministic on rerun | CI pins DEMO_TODAY=2026-06-10 at the job level (businessToday overrides *every* user, by design, DECISIONS #58), but the test seeded UnknownQuestion/NotificationSent/EngagementEvent rows with implicit now() timestamps and then queried them by a [weekStart,weekStart+7) window built from that pinned date; local `vitest run` never loads .env so DEMO_TODAY is unset there and now() happens to coincide with real-clock weekStart, masking the bug | CI-vs-local env divergence must not be papered over by wall-clock coincidence in seeded test data | tests/unit/self-audit-server.test.ts -- explicit `inWeek` timestamp on the three seeded rows; fail-old PROVEN via `DEMO_TODAY=2026-06-10 npx vitest run tests/unit/self-audit-server.test.ts` before the fix (1 failed), green after |

| 2026-07-12 | Dashboard payment-reminders card and /cards breakdown at household scope billed the reader for a PARTNER's card ('you pay $200', 'You must pay', 'Do this first: pay ...', 'keep the cash in place') — the false money claim class the slice-7 digest fix cured in email only | household scope was bolted onto components whose copy assumes everything on screen is the viewer's own; no owner map reached the reminders card at all, and the cards map covered CREDIT only | second-person-copy invariant (docs/lessons/second-person-copy-scope.md): a money string's implied subject must match the account's owner | tests/unit/household-copy.test.ts 'every in-app partner-owned string passes the partner-due ban' + accountOwnerLabel all-types widening locked in tests/unit/household-cash-needed.test.ts |
| 2026-07-12 | Household headline 'Needs $600.00 in Owner Checking' counted a partner's autopay draft as money the viewer must position in their OWN account — autopay mechanically drafts the partner's account, so the attribution was a wrong claim on the flagship surface | the headline copy hardcoded the viewer's funding-account name regardless of scope | CLAUDE.md 'every projection states its assumptions inline' — the joint total is needed ACROSS the household, not in one member's checking | HOUSEHOLD_COPY.headlineAcrossHousehold + extended scopeAssumptions autopay sentence, scanned by tests/unit/household-copy.test.ts; attribution decision recorded in DECISIONS #221 |
| 2026-07-12 | Joint digest claimed 'No accounts are shared in ... yet' while listing the partner's shared LOAN due in the same email — a false disclosure in a money email | the 'anything shared?' branch keyed on the SPENDING-only movement.accountCount; a shared loan is counted for dues but was invisible to the branch | no-fabrication / disclosure honesty: a disclosure string must not contradict the email it appears in | tests/unit/digest.test.ts 'F-4: a loan-only share never claims no accounts are shared' (fail-old) + household-digest.test.ts sharedAccountCount=3 vs movement.accountCount=2 |
| 2026-07-12 | Same real joint bank account connected by BOTH partners (any provider mix) silently double-counted in every household figure — dues, digest movement, merged balances — with the digest mailing the doubled number in two contradictory voices | two provider connections mint two Account rows with different ids: the merge's disjoint-by-id guard cannot see it, and the personal #192 detector's input is correctly pinned to the owned set so it can never see the cross-owner pair | T9(b); inherited slice-7 critic F5, routed to slice 8 | tests/unit/account-duplicates.test.ts detectHouseholdDuplicateAccounts suite (incl. relaxed same-provider skip) + household-cash-needed.test.ts disclosure integrations + the DOCUMENTED-not-fixed fail-old lock (merged snapshot still contains both twins) + digest duplicatePairCount locks |
| 2026-07-12 | A partner's non-USD shared account vanished from household dashboard/cards/calendar with zero trace (the digest already disclosed the same withhold), and its orphan autopay/statement/cardPayment rows traveled into the merge | getSharedSnapshotSlice filtered accounts/transactions/scheduled by the currency guard but not the other three row families, and returned no withheld count for interactive surfaces | DECISIONS #135 'counted and disclosed, never silent' | tests/unit/household-cash-needed.test.ts 'F-6: the withheld EUR share is counted for disclosure and absent from the merged figures' + householdWithheldCount T6 parity assertions |
| 2026-07-12 | A household merge-guard throw (drift/duplicate-id) cost the member their ENTIRE weekly digest — Money Review and own dues included — with no retry until next week | the cron's per-user catch wrapped the whole digest assembly, so a guard defending the HOUSEHOLD number also killed the personal content | fail loudly, but scope the blast radius: the personal digest makes no household claim and is always honest to send | degrade-to-personal path in api/cron/digest/route.ts (dues read + context succeed or degrade TOGETHER, so household-scope reminders can never render without owner labels), audited as digest.household.degraded |
| 2026-07-12 | T8 (export contains only the exporter's own rows) had NO locking test — the architecture promised 'extend existing export test with a household fixture' and every other T-invariant got its lock except this one | export code was correctly scoped but nothing prevented a future 'include shared accounts in your export' widening from shipping silently | HOUSEHOLD_ARCHITECTURE §4.6 T8 (slice-8 critic B-1, the pass's only P1 in charter B) | tests/unit/export-route.test.ts 'T8 household scope' — the fixture PROVES the shared row is visible to the widened read scope, then asserts the CSV excludes it |
| 2026-07-12 | T10's locking test asserted the survivor's promotion but never the VISIBILITY half (no shared account was seeded on the deleted user), and a household whose last member ran deleteMyData left a ghost Household row with live redeemable invites | the visibility half held only by the untested delete cascade; deleteMyData lacked leaveHousehold's opportunistic memberless-household reap | T10/T11 (slice-8 critic A-F1/A-F2) | tests/unit/household-actions.test.ts T10 test now seeds a shared account on the deleted owner and asserts the survivor's widened read returns zero of it; deleteMyData reap added in src/server/account-actions.ts |
| 2026-07-12 | Ask conversation frame: "how much did I spend on groceries this month?" then "and this month at costco?" answered a confident GROCERIES figure — the merchant the user explicitly named was silently dropped | `merchantFragment` only looked for a merchant at the START of the fragment, so a timeframe-first fragment hit the parser's stop-words on token 1, yielded no merchant, and fell through to the timeframe-only swap carrying the OLD category | "the AI never originates a fact" / no confident-wrong money answer — the frame answered a question the user did not ask | `tests/unit/assistant-frame.test.ts` → "P1-1: a timeframe-first fragment keeps the merchant it names" |
| 2026-07-12 | Ask conversation frame: the follow-up "restaurants not groceries" answered GROCERIES — the category the user had just rejected | `resolveSpendTarget` ran on the whole fragment and its synonym table is first-match-wins (groceries precedes dining); negation was never modelled | "fail loudly or not at all" — an unrepresentable request (exclusion) was silently coerced into its opposite | `tests/unit/assistant-frame.test.ts` → "P1-2: a NEGATED fragment abstains" |
| 2026-07-12 | Ask conversation frame: "what about income?" answered "No spending at Income this month." — the assistant's own intent vocabulary became a merchant probe, and it STOLE the question from the LLM classifier that routes it correctly | any ≤3-token noun that wasn't a category synonym was accepted as a bare merchant name; the frame also suppressed the UnknownQuestion write, so the miss left no telemetry | rule 6 "fail loudly or not at all" + rule 5 (a deterministic guard existed but wasn't written) | `tests/unit/assistant-frame.test.ts` → "P1-3: the assistant's own vocabulary is not a merchant" (+ `frame:<kind>` ledger tagging in `src/server/assistant.ts`) |
| 2026-07-12 | Ask conversation frame: "why so much on dining?" / "should I cut back on dining?" answered a bare dining figure — a question about causation or advice got a number that answers neither | the sentence-word guard was applied only on the merchant path, never on the category path | coaching guardrail (educational, answers the question asked) | `tests/unit/assistant-frame.test.ts` → "P2-4: a WHY / SHOULD / comparison question is not a slot swap" |
| 2026-07-12 | Ask conversation frame: a carried "this month" window kept its deictic label across a month boundary — a June window headlined "this month" on July 1 | `frame.timeframe` was carried verbatim; the label is deictic but the window is absolute | "financial math is sacred" — a true figure under a false window name is a false statement | `tests/unit/assistant-frame.test.ts` → "P2-7: a carried deictic window is re-labelled across a month boundary" |
| 2026-07-12 | `validateIntent` — the declared trust boundary for a client-echoed frame — accepted month `2026-13`, an unbounded label, and a 100,000-char merchant, all of which then flowed into an answer headline | the month regex was `\d{2}` and neither label nor merchant had a length bound; the field had never before crossed a client boundary | "validate at system boundaries" (the intent became untrusted input the moment the client echoed it back) | `tests/unit/assistant-frame.test.ts` → "P2-6: validateIntent bounds the fields a client-echoed frame can carry" + "rejects a broken window on the MONTH alone" |
| 2026-07-12 | Ask conversation frame, critic cycle 2: a client-echoed frame could label the TRAVEL group "Groceries" — a TRUE figure under a FALSE name in the money headline ("You spent $840.00 on Groceries this month") — and carry a 331-char label into that copy | `isSpendTarget` validated the target's id/group but accepted `label` as any string; the label had never before crossed a client boundary, so nothing derived it | "validate at system boundaries" + financial-copy integrity (a correct number under the wrong name is a wrong claim) | `tests/unit/assistant-frame.test.ts` → "P2-B: a target label is DERIVED from its identity, never trusted from the client" (+ `canonicalTargetLabel` in intent.ts) |
| 2026-07-12 | Ask conversation frame, critic cycle 2: "and last month at least?" / "what about at work?" manufactured the merchants "least" and "work" from a stray "at"; conversely "and at Save Mart?" wrongly ABSTAINED because "save" sat in the question-word guard | the merchant preposition search matched any "at", and the question-word set carried words that are also ordinary store-name words while adding nothing the interrogatives didn't already catch | rule 2 (simplicity — a guard list doing work it didn't need to) + no confident-wrong answers | `tests/unit/assistant-frame.test.ts` → "P2-A: a stray 'at' … does not manufacture a merchant" + "P3-C: a store whose name contains an ordinary word still resolves" |
| 2026-07-12 | Ask conversation frame, critic cycle 2: a carried TRAILING window ("the last 3 months", Apr–Jun) kept that name after today left it — in September it still read "the last 3 months" | `relabelForToday` fixed only single-month deictic labels and bailed on multi-month windows | "financial math is sacred" — a true window under a false name is a false statement | `tests/unit/assistant-frame.test.ts` → "P3-D: a carried TRAILING window is re-named once today leaves it" |
| 2026-07-12 | Learned vocabulary (#226 P1): the SHARED demo account learned — one anonymous visitor's typed question ("can melissa and i afford the clinic") would be mined into a rule and rendered on the NEXT visitor's /settings, under copy promising it is shared with no one | every demo visitor is the same `user-demo` row, and no vocab path excluded it; the scrub masks emails/amounts/digits, not names or clinics | privacy: anything that accumulates a user's own INPUT and shows it back must opt the shared demo account out (the #210 household precedent) | `tests/unit/vocab-server.test.ts` → `test_regression__vocab_demo_user_never_learns` |
| 2026-07-12 | Learned vocabulary (#226 P1): a user's "Forget this" landing while the weekly miner was mid-run was silently REVERTED by the miner's stale write — and the rejection lives nowhere else, so it was lost for good; the entry resumed serving and self-promoted | the miner reads → decides → writes, and its `updateMany` filter had no tombstone guard, unlike the sibling `retireVocabEntry` | audit §4 constitution (b): every adaptation is undoable — an undo that a background job can overwrite is not an undo | `tests/unit/vocab-server.test.ts` → `test_regression__vocab_miner_clobbers_tombstone` |
| 2026-07-12 | Learned vocabulary (#226 P1): a served entry became unmonitorable and immortal — it short-circuits the LLM, so no independent resolution of that phrase could ever be ledgered again; held-out evidence froze, flagged→active promoted on the entry's OWN serves, and "one disagreement retires it" was unreachable | the self-confirmation guard (`vocab:` rows are never evidence) is correct, but nothing replaced the independent signal it excluded | audit §4 constitution (d) promotion on HELD-OUT validation + (e) automatic reversion on regression | `tests/unit/vocab-server.test.ts` → `test_regression__vocab_weekly_independent_recheck` (3 cases: disagree → retire, agree → keep, no key → no change) |
| 2026-07-12 | Ask parser (#226 P1, PRE-EXISTING): "how much did I spend at 星巴克 last month" answered the user's ALL-SPENDING TOTAL with no hedge, and "with Zürich Café" silently became merchant "zrich" | both #166 abstain guards are ASCII-only — `extractMerchantPhrase` strips every non-`[a-z0-9'&.-]` char and the `on <object>` guard matches `[a-z0-9]+` — so a non-Latin store name tokenized to nothing and fell through to `spend_total` | the cardinal sin: a TRUE FIGURE UNDER A FALSE QUESTION | `tests/unit/assistant-intent.test.ts` → `test_regression__spend_at_non_ascii_merchant` (+ the end-to-end case in `tests/unit/vocab-ask.test.ts`) |
| 2026-07-12 | Learned vocabulary (#226 P2): a rule mined from "can I pay off my car by 2027" (a DATE) answered "can I pay off my car by 65" (an AGE) — the `[num]` scrub collapses both to one key, and `intentFromKind` silently degrades `debt_free_by_date` → `debt_payoff` when no date parses, so the user's deadline question got a different question's answer and NOTHING had read the actual words | the phrase key masks exactly the spans the answer path re-derives — but a kind that degrades to ANOTHER kind is not a re-derivation | no confident-wrong answers: a kind swap means these are not the words the rule was mined from | `tests/unit/vocab-ask.test.ts` → `test_regression__vocab_kind_must_round_trip` |
| 2026-07-12 | Ask parser (#226 cycle 2, P1 — a bug in the cycle-1 FIX): "how much did I spend at **the** 星巴克 last month" still answered the ALL-SPENDING TOTAL; and "at the café zurich" still became merchant "caf zurich" | the new guard tested the first whitespace token after the preposition, but `extractMerchantPhrase` skips leading articles (`the/a/an/my`) and reads up to 4 tokens — guard and tokenizer disagreed about where the merchant starts, so the original harm was one article away | the cardinal sin (a true figure under a false question), reintroduced by a partial fix — a guard must walk the SAME input its subject walks | `tests/unit/assistant-intent.test.ts` → `test_regression__unreadable_merchant_behind_an_article` (+ shared `merchantTokenStream` / `spendObjectUnreadable`) |
| 2026-07-12 | Ask parser (#226 cycle 2, P2 — regression caused by the cycle-1 fix): "how much did I spend at mcdonald’s" (the iOS keyboard's default curly apostrophe) began returning `unknown`, silently breaking every phone-typed possessive store name, with no LLM rescue possible (`merchant_spend` is not in the classifier's closed set) | the non-ASCII abstention treated a smart quote as unreadable text, though it carries no meaning the tokenizer would drop | rule 3 (surgical changes — a safety guard must not take working input with it) | `tests/unit/assistant-intent.test.ts` → `test_regression__smart_apostrophe_merchant_still_resolves` |
| 2026-07-12 | Ask parser (#226 cycle 4, P0): a store written in glyphs the tokenizer DELETES — "at ⓒⓞⓢⓣⓒⓞ", "at 🅲🅾🆂🆃🅲🅾", "on 🍕" — answered the user's ALL-SPENDING TOTAL, unhedged | the cycle-3 predicate was narrowed to non-ASCII LETTERS (so emoji beside a name wouldn't cause a false abstain), which said nothing about an object made ENTIRELY of symbols: it stripped to nothing and fell through the sink | the cardinal sin; and the wrong question — not "is there a symbol?" but "did the object survive being read?" | `tests/unit/assistant-intent.test.ts` → `test_regression__object_that_strips_to_nothing` |
| 2026-07-12 | Ask parser (#226 cycle 4, P0): DECOMPOSED "café zurich" (NFD) answered ALL COFFEE-SHOP spending for a question about ONE STORE, while the composed spelling of the identical question abstained | `resolveSpendTarget` ran BEFORE the readability guard, and a combining mark is not a word char, so `\bcafe\b` matched "cafe"+U+0301 | two byte sequences the user cannot tell apart must not route differently; readability is a precondition of routing, not a fallback | `tests/unit/assistant-intent.test.ts` → `test_regression__nfd_accents_cannot_reach_the_category_route` (+ NFC in `normalize()`, guard moved above `resolveSpendTarget`) |
| 2026-07-12 | Ask parser (#226 cycle 4, P1): a FRONTED unreadable object ("At 星巴克, how much did I spend?"), a sentence break, or a zero-width space glued to the preposition all skipped every guard and answered the all-spending total; and `intentFromKind` let the LLM re-answer the very question the parser had just abstained on, as `spend_total` | every guard was a filter scoped to verb-then-preposition, while `spend_total` was the family's unconditional SINK — inheriting the answer instead of earning it | the sink must earn its answer: three cycles of hardening filters each moved the bug one syntactic inch | `tests/unit/assistant-intent.test.ts` → `test_regression__fronted_and_split_unreadable_objects` (+ the `containsUnreadableName(q)` licence before `spend_total` and inside `intentFromKind`) |
| 2026-07-12 | Ask conversation frame (#226 cycles 3–4, P1): "what about at café zurich?" answered a confident-wrong "No spending at caf zurich", and "what about at 🍕 last month?" silently DROPPED the unreadable store and answered the CARRIED one — the previous shop's total under a question about a different shop | the frame consumed the parser's merchant TOKENIZER but never its readability GUARD | a slot the fragment names but we cannot read must abstain the whole fragment, never vanish | `tests/unit/assistant-frame.test.ts` → `test_regression__frame_abstains_on_unreadable_names` |
| 2026-07-12 | Learned vocabulary (#226 cycle 2, P2): the weekly re-check retired a serving rule SILENTLY — indistinguishable at rest from the user's own "Not what I meant", and invisible in the audit trail | `auditServableEntries` wrote no audit row, while `forgetLearnedPhrase` writes `vocab.retired` | audit §4 constitution (a): every adaptation is visible — which must include un-learning | `tests/unit/vocab-server.test.ts` → "retires a SERVING entry the independent resolver now disagrees with, and says so" (asserts the `vocab.retired.recheck` row + its meta) |
| 2026-07-12 | Learned vocabulary (#226 P2): "how much do I owe 田中 for rent share" and "…owe 房东 for rent share" — different payees — collapsed to ONE key and would share one learned rule; and a fast-clocked instance's supporting row could be recounted as HELD-OUT evidence for the rule it helped create | normalization deletes every non-`[a-z0-9[]]` char (an undocumented wildcard mid-key); and the held-out boundary was the miner's wall clock rather than the data | validate at boundaries + "held-out means held out" (a gate that can validate itself is not a gate) | `tests/unit/vocab.test.ts` → `test_regression__vocab_non_ascii_key_collapse`, `test_regression__vocab_heldout_boundary_is_data_not_clock`, `test_regression__vocab_entry_cap` |
| 2026-07-12 | Ask parser (TASKS 2.6, the #226 inversion): a fronted ASCII object — 'At Costco, how much did I spend?' — answered the user's ENTIRE spending, unhedged, for a question about one store; the LLM/vocab routes could re-answer the same question via intentFromKind | every guard in front of spend_total was a filter scoped to verb-then-preposition order, and the sink inherited the answer instead of earning it | the cardinal sin (a true figure under a false question); the sink must earn its answer | tests/unit/assistant-spend-licence.test.ts → test_regression__fronted_ascii_object_reaches_spend_total (+ unconsumedSpendObject shared by parser sink, frame, and intentFromKind) |
| 2026-07-12 | Ask parser (TASKS 2.6 critic cycle 1, F1/F2 P0 — a bug in THIS slice's first licence): 'at Best Buy / Top Golf / All Saints / 5 Guys / 76, how much did I spend?' still took the all-spending total through every route at once | the licence scan let the FIRST consumed-class token (idiom/total/numeric/timeframe cue) license the WHOLE object — one licensed word became a licence for the words behind it | the cardinal sin, reintroduced by the fix; every word of the object must be consumed before the total is licensed | tests/unit/assistant-spend-licence.test.ts → test_regression__one_licensed_word_is_not_a_licence |
| 2026-07-12 | Ask frame (TASKS 2.6): 'what about with amex in june?' / 'income in june?' silently DROPPED the refused object and answered the CARRIED question's June window — a spend total answering an income question; and 'what about on 🍕 last month?' deleted the pizza and answered the carried category (critic cycle 1 F3, P1: the unreadable guard scanned only at/with while the bare-merchant path accepts every leading preposition) | a guard-refused merchant candidate returned plain null, indistinguishable from 'no object named', so the timeframe-only swap fired | a refused object is a different question — abstain, never substitute | tests/unit/assistant-frame.test.ts → test_regression__refused_object_falls_to_the_timeframe_swap + test_regression__unreadable_object_behind_other_prepositions |
| 2026-07-12 | Ask parser (TASKS 2.6): 'at home depot' / 'at homegoods' / 'at home and garden' answered the Home GROUP — rent + mortgage inside a figure for one retailer (#111-era bare-substring group fallback); 'at - costco' answered merchant '- costco'; 'at... costco' fell to the all-spending total | tier-3 group fallback used q.includes() with no boundaries and no extension check; the merchant extractor required whitespace immediately after the preposition and kept punctuation-only tokens as name words | surgical validation at the weakest-signal tier; punctuation is filler, not a name | tests/unit/assistant-spend-licence.test.ts → test_regression__home_depot_is_not_the_home_group + test_regression__punctuation_after_the_preposition |
| 2026-07-12 | Ask (TASKS 2.6): a custom category named 'Café' was UNREACHABLE via its own name (JS \\b has no boundary after é, and the unreadable-object abstain fired first); and the first carve-out tested PREFIX equality, so 'at café in 星巴克 town' matched 'café' and silently dropped the unreadable store (critic cycle 2, NEW-2) | ASCII word boundaries on Unicode names + equality tested on a stop-word-truncated prefix (containment in disguise) | the user's own vocabulary must be readable; equality means the whole object, tail included | tests/unit/assistant-spend-licence.test.ts → test_regression__non_ascii_custom_category_unreachable + test_regression__carve_out_prefix_is_not_equality; tests/unit/assistant-frame.test.ts → test_regression__non_ascii_custom_category_in_a_fragment |
| 2026-07-12 | Ask parser (TASKS 2.6 critic cycle 2, NEW-1 P2): 'at Do It Best, how much did I spend?' — a real ~1,400-store chain spelled entirely in licence-recognized tokens — took the all-spending total in fronted order | 'do'/'did'-class auxiliaries were unconditionally question machinery, so 'do it best' read as a question tail | question machinery must be shaped like a question — an auxiliary counts only ahead of another question word | tests/unit/assistant-spend-licence.test.ts → test_regression__store_made_entirely_of_question_words |
| 2026-07-14 | Ask parser (TASKS 2.7): 'how much did I spend on groceries in 2025' answered the unhedged THIS-MONTH Groceries figure; 'since 2024' / 'between 2024 and 2025' the this-month total; 'since march' a March-only window for a March-through-today question — true figures under different windows, live in production shapes | parseTimeframe silently defaults to this-month when parseExplicitTimeframe recognizes nothing, and only the spend_total sink was licence-protected — bare years, 'since', ranges and numeric dates were simply not in the grammar | the cardinal sin (a true figure under a false question); a default window must not answer a dated question | tests/unit/assistant-timeframe-dates.test.ts → 'REGRESSION: since 2024 / since march no longer answer a different window' + the routes/abstention suites (unresolvedDateShape enforced in parser, intentFromKind, and frame) |
| 2026-07-14 | Ask parser (TASKS 2.7): 'what was my biggest purchase at costco?' answered the GLOBAL biggest purchase — merchant silently dropped (STATUS §OPEN item 3); 'biggest costco purchase' / 'biggest bank charges' ranked everything; a window swap after a scoped largest would have widened it back to global | largest_purchases had no merchant field and its route read no object; the frame carried no merchant for it | scope discipline: a route must read its object or abstain — never answer the unscoped superset | tests/unit/assistant-largest-scope.test.ts (parser/intentFromKind/frame/validateIntent/answer matrix, abstentions in the majority) |
| 2026-07-14 | Ask parser (TASKS 2.7 critic cycle 1, F1/F7 P1): 'biggest purchase at the moment' → merchant 'moment' → 'No purchases at Moment this month.'; verb-order 'spend at the end of last month' → merchant 'end of' — factually false money claims minted from licence idiom vocabulary | extractMerchantPhrase stops only at timeframe/total cues, so LICENSED_SPEND_OBJECTS words became store names; the new largestScope led-branch was built on the same unguarded extractor | a guard must read the same input as the thing it guards — the licence's idiom set existed and no merchant extractor consulted it | tests/unit/assistant-largest-scope.test.ts → test_regression__licensed_idioms_became_merchants; tests/unit/assistant-spend-licence.test.ts → test_regression__licensed_idioms_became_merchants_in_verb_order (shared isLicensedIdiomPhrase) |
| 2026-07-14 | Ask parser + frame (TASKS 2.7 critic cycle 1, F3/F4 P1): 'in march 2027' / 'since march 2027' resolved a FUTURE (or wrong since-discarded) window through the month loop's year fallback — 'No income recorded December 2030.' as a definitive past-tense claim; and the frame silently DROPPED an unresolvable date ('what about groceries in 2027?' answered LAST MONTH's groceries) | the month loop had no future guard so unresolvedDateShape saw a resolved window; the frame never consulted the new guard at all — a timeframe-carrying route the 'every route' claim missed | no route answers what another refused; future windows are not windows | tests/unit/assistant-timeframe-dates.test.ts → test_regression__month_name_beside_a_future_year_escaped_every_refusal + test_regression__the_frame_silently_dropped_an_unresolvable_date |
| 2026-07-14 | Ask parser (TASKS 2.7 critic cycle 2, N-1 P2): 'biggest purchase from my checking account' → merchant 'checking account' → 'No purchases at Checking Account this month.' — the #168 payment-source class, re-opened by the new 'from' anchor | NON_MERCHANT_SPEND_OBJECTS covered card/venmo/amex vocabulary but not account self-reference (checking/savings/bank/account) | #168: how you paid is not where you spent | tests/unit/assistant-largest-scope.test.ts → test_regression__account_words_became_merchants |
| 2026-07-15 | Glass-Box trace (slice 1 critic cycle 1, F1 P1): a caller omitting category meta traced a custom-category group answer at $40.00 "reconciled to the penny" against the server's $160.00 headline, custom rows labeled "Uncategorized" — a wrong number stamped reconciled, in the feature that exists to kill exactly that | `TraceInput.meta` was optional and silently defaulted to the static CATEGORY_BY_ID while every server answer is built with `mergeCategoryMeta(custom)` — an optional parameter whose omission degrades correctness is a trap | the cardinal sin (a confident wrong figure); lockstep means SAME inputs, not just same code | tests/unit/assistant-trace.test.ts → 'F1 — custom category meta' (meta now a required field — omission is a tsc error) |
| 2026-07-15 | Glass-Box trace (slice 1 critic cycle 1, F2 P1): a row syncing between answer-time and tap-time made the drawer show a DIFFERENT figure than the one tapped, green-checked — internally consistent, externally a lie | `reconciled` only compared the trace against its own recomputation; nothing tied it to the number the user actually tapped, and AssistantAnswer carries only formatted strings | fail loudly or not at all — a reconciliation that never sees the tapped figure cannot fail on its behalf | tests/unit/assistant-trace.test.ts → 'F2 — expectedHeadlineCents: input drift between answer and tap fails reconciliation' |
| 2026-07-15 | Glass-Box trace UI (slice 2a critic cycle 1, P1-1): tapping a `top_categories` headline ("top spending: Groceries at $412") opened a panel green-checking "60 transactions add up to $412.00" and listing every top-N category's rows under the ✓ — a reconciliation endorsing a count/sum drawn from categories NOT behind the tapped figure | the panel folded `rowCount` and rendered groups over ALL `trace.groups`, but for top_categories `sumCents`/`headlineCents` is the TOP category alone (the engine carries all top-N in `groups`) | Fail loudly / no fabrication — a ✓ beside a number it can't stand behind | tests/unit/assistant-trace-view.test.ts (reconciledView: top_categories → groups null, 1 row summing to 12100 not 28600) + tests/e2e/ask.spec.ts top-categories case (ask-trace-group count 0) |
| 2026-07-15 | Glass-Box correction chip (slice 2b critic cycle 1, F1 P2): a recompute failure AFTER the committed category write rendered as "Couldn't update that category — please try again" — a false failure beside a now-stale green-checked panel, with the undo handle never delivered | `correctFromAsk` awaited `composeAnswer` inside the same success path as the committed `applyCategory`; one client `catch` conflated "write failed" with "write committed, refresh failed" | Fail loudly or not at all — a committed money write must never masquerade as a failure | tests/unit/ask-correction-action.test.ts → 'a recompute failure AFTER the committed write is disclosed' (fault-injected snapshot read; asserts answer null AND the write durable) |
| 2026-07-15 | Glass-Box correction chip (slice 2b critic cycle 2, F1/F2 P2): after a disclosed stale state the headline/fact taps could reopen a green-check panel over PRE-WRITE rows (one tap after "couldn't be refreshed"); and a new ask racing an in-flight correction let whichever transition landed last render under the wrong caption | tappability never consulted `correctionState`; `run()` guarded `pending` but not `correcting` (the guard every sibling action already carried) | never offer a reconciliation the panel can't stand behind; surgical-consistency (guards exist everywhere or nowhere) | ask-view.tsx `staleAnswer` gate + `run()` correcting guard; behavior pinned by the e2e render-only chip spec + 17/17 ask e2e (UI-state fix — no unit seam; documented here for the pattern) |
| 2026-07-15 | Glass-Box derivation trace (slice 3 critic cycle 1, F6 P2): a user with ZERO accounts got a tappable "$0.00" net worth opening two empty columns and "$0.00 − $0.00 = $0.00" — an honest-but-hollow reconciliation that reads as a glitch in the feature built to earn trust | `traceNetWorthDerivation([], 0)` legitimately reconciles (empty sum = 0 = net), and nothing required a formula to have any inputs before offering the tap — cash_needed's gate had the rows-length guard, net_worth's didn't | simplicity-first asymmetry (a guard exists on one arm and not its sibling); never offer an explanation with nothing to explain | tests/unit/assistant-derivation.test.ts → 'no accounts → the builder withholds the tap entirely' + tests/unit/assistant-derivation-view.test.ts → 'net_worth: no lines at all → null' |
| 2026-07-15 | Glass-Box derivation gate (slice 3 critic cycle 2, P2-3): a tampered payload with FRACTIONAL cents (x+0.5 / y−0.5 across two lines) passed every sum equality and rendered malformed money strings under a green ✓ — and on the savings arm `cents()` would THROW inside the component instead of declining | `derivationView` checked `Number.isSafeInteger` only where a crash forced it (savings_rate's `cents()` call), not as the gate's own contract — integers were assumed, not verified, on the net_worth/cash_needed arms | fail loudly or not at all — a gate whose stated contract is "never render what doesn't verify" must verify its own number domain | tests/unit/assistant-derivation-view.test.ts → 'any kind: a fractional-cents line → null even when the sums still match' |
| 2026-07-15 | Nudge engine (slice 1, Fable critic cycle 1 P1-2): the push-lockstep test used daysUntil ∈ {1,2,20} and never touched the window EDGE, so a `<=`→`<` mutation at the boundary would push a daysUntil=3 actionable due (notify/select emits) while the feed tiers it ACTION — push escalates, feed buries a material warning, suite green. Root cause: coverage gap at the one integer where the twin predicates can drift. Rule broken: LOOP_ENGINEERING "a test that doesn't cover the behavior is flagged as such" + Maker/Checker on the core engine. Locking test: `nudge · criterion 5 · lockstep holds at the window boundary (daysUntil=%i)` sweeps 0..5 asserting pushEmits(key) ⇔ feedTier==='critical'; mutation-verified fails-old (daysUntil=3: push=true tier=action) / passes-new. |
| 2026-07-15 | Nudge feed UI (slice 2, Fable critic cycle 1 P1-1): the "Today" card rendered `centsAtStake` with a single semantic ("Now $X/mo", "$X/mo"), but that field means a DIFFERENT thing per opportunity kind (fi/insights.ts: the monthly INCREASE delta for price-increase; an ESTIMATED ~15% saving for insurance-reshop; a flat $20 estimate for negotiable-bill) — so a Netflix $15.49→$17.99 increase rendered "Now $2.50/mo", a false price claim; the two estimate kinds disclosed nothing | one display template assumed centsAtStake was always "the monthly cost"; the engine's verbatim-copy is intact, the UI boundary attached the wrong semantic | no fabrication / #221 false-money-copy class + coaching guardrail (every estimate states its assumption inline) | tests/e2e/today-feed.spec.ts → price-increase row shows "Up $2.50/mo" and NOT "Now $" |
| 2026-07-15 | Nudge feed UI (slice 2, Fable critic cycle 1 P1-2): a LOAN/MORTGAGE payment_due proposal (data.reminders includes loanObligations) rendered as "Card payment due" — a false statement about the obligation type, and on the top-ranked CRITICAL slot for a mortgage due within days | `describe()` hardcoded "Card payment due"; the Proposal drops obligationType, so the card had no card/loan discriminant and defaulted to "card" | no fabrication — never assert a fact (card vs loan) the data doesn't carry | tests/e2e/today-feed.spec.ts → today-feed card never contains "Card payment due"; payment_due title is the neutral "Payment due" |
| 2026-07-15 | Nudge feed store (slice 2, Fable critic cycle 1 P1-3): `dismissNudge` was an authenticated write path with no rate limit, no key-length cap, and an unbounded read — a scripted `dismissNudge(randomUUID())` loop could bloat NudgeDismissal without bound (repo rule: every request path uses rateLimitDurable) | the new action skipped the repo's standard write-path hygiene | fail-safe boundaries — validate at system boundaries; bound attacker-controlled key spaces (NotificationSent OPS-1 precedent) | tests/unit/nudge-dismissal.test.ts → over-length (201-char) key rejected with no write, 200-char boundary accepted; rateLimitDurable(`nudge-dismiss:<user>`, 40, 60s) on the action |
| 2026-07-15 | Nudge feed UI (slice 2, Fable critic cycle 2 N1 P1): a partial-autopay card (autopay covers part, a top-up remains) rendered "Payment due — $500" where $500 = userActionCents (the remainder after autopay), while the reminders card on the SAME dashboard showed "$600 due · autopay covers $100, you pay $500" — the two cards disagreed on the headline figure for one obligation | centsAtStake (=userActionCents) was displayed as if it were the statement total; the Proposal carried no autopay context to disclose the split | no fabrication / #221 false-money-copy — the feed can never disagree with the dashboard | tests/unit/nudge-select.test.ts (payment_due carries autopayCents verbatim; ZERO for non-payment) + tests/unit/nudge-feed-copy.test.ts (partial-autopay shows "$500 to pay · autopay covers $100", never the summed $600) |
| 2026-07-15 | Nudge feed engine (slice 2, Fable critic cycle 2 N2 P1): cash_needed_shortfall hardcoded isEstimated:false, so an estimate-driven shortfall (new user, no generated statement → the whole cycle projected from estimated obligations) presented "About $2,000 short" with no estimate disclosure — while the sibling payment_due said "(estimated statement)" and dip carried includesEstimatedDues | the flag was a literal false, not derived from the source's own per-card isEstimated flags | every projection states its assumptions inline (coaching guardrail); no undisclosed estimate | tests/unit/nudge-select.test.ts (isEstimated = perDueDate.some(cards estimated): all-real→false, any-estimate→true) + tests/unit/nudge-feed-copy.test.ts (inline "(estimated)" marker on shortfall + dip) |
| 2026-07-16 | Why-This-Category (slice 1, Fable critic cycle 1 P0-1): an LLM pick at confidence 1.0 → `Math.round(1*10000)`=10000, which collides with the "user-dictated" sentinel — dropped by `logCategoryPredictions`' `<10000` filter (no prediction row) and then read by the resolver as "You set this". A pure model guess shown as a human fact, and the LLM's most confident predictions silently excluded from the Brier/tuning log. | `parseLlmCategory` had no cap; 10000 was reachable from the one boundary that must never produce it | no fabrication (the AI never originates a fact) — 10000 is reserved for user dictation | `tests/unit/categorize-llm.test.ts` (conf 1.0 and 0.99996 → 9900) + `tests/unit/prediction-source.test.ts` composed prepare→assist→create→log (a max-confidence LLM row persists source 'llm' → ai-guess, NOT dropped→user-set) |
| 2026-07-16 | Why-This-Category (slice 1, Fable critic cycle 1 P1-3): the create-only `CategoryPrediction` records the FIRST verdict, but `transaction.categoryId` moves afterward (backfill LLM re-file, sync verdict refresh, household-partner correction at 9900 w/o labeledAt) — so the stored `source` could describe a SUPERSEDED category, and slice-2 badges would name a false origin (e.g. "AI guess" on a category a partner set). | the resolver had no input expressing "predicted category ≠ current category" | no fabrication — never surface an origin for a category that origin didn't produce | `tests/unit/categorize-provenance.test.ts` (predicted≠current → not-recorded for EVERY source; partner-corrected llm row does not beg to re-confirm; sweep asserts ai-guess ⇒ categoryMatch) |
| 2026-07-16 | Why-This-Category (slice 1, Fable critic cycle 1 P1-2): the CSV import correlated each row's provenance to its inserted id by `createManyAndReturn` ARRAY INDEX — an ordering SQLite/Prisma do not contractually guarantee (SQLite RETURNING order is documented undefined) — so a batch could pin an 'llm' source to a merchant-default row and vice-versa. | positional correlation across a DB write with no ordering contract; the slice is what made ordering load-bearing (the pre-existing code read all fields from the same returned row) | no fabrication — provenance must bind to its own row by KEY | pre-assigned `id: randomUUID()` per CSV row → the prediction log reads source off the same `data` array by key (no positional correlation); `tests/unit/categorize-assist.test.ts` pins per-row source across a mixed batch (untouched keeps its source, overlaid → 'llm') |
| 2026-07-16 | Why-This-Category (slice 2, Fable critic P2-1): the demo AI-guess fixture selected the most-recent auto-filed row and, finding no unknown-merchant candidate, silently fell through to a KNOWN brand — then overwrote its `source` to 'llm', so the register showed "AI guess — needs your OK" on a name-brand merchant's obvious, deterministically-filed category. A provenance the real pipeline can't produce (merchant-default always beats the LLM overlay) — a fabricated origin in the safe direction, but still the exact dishonesty this feature exists to prevent. | source-flip-only assumed an honest 'llm' relabel existed among real-category rows; it doesn't — every real-category demo row is a known merchant, and the fall-through masked it | no fabrication (a model never originates a fact) — never present a deterministic origin as an AI guess | `tests/unit/prediction-source.test.ts` seed contract: EXACTLY ONE demo row is ai-guess, it is source 'llm' + unlabeled + predicted===current, AND its merchant has NO real default (null or 'uncategorized') — pinning the fixture to an ambiguous merchant the LLM overlay legitimately resolves, never a known brand. The seed now PROMOTES an uncategorized unknown-merchant review row (the authentic overlay path) rather than relabeling a settled row, and fails loudly if none exists. |
| 2026-07-16 | Balance-Move LLM prose could show a fabricated/swapped money figure (free-prose validator defeated 20/20 by a fresh critic) | validating free-form model prose for money-truth is unwinnable; the model authored figures | cardinal rule "the LLM never originates a fact" | `tests/unit/balance-move.test.ts` (atomic-template grammar: model authors STRUCTURE only, engine substitutes every figure — reorder/literal/relational all rejected) |
| 2026-07-16 | A grammar-legal template could REORDER placeholders so a figure bound to the wrong category ("{second}, {primary_delta}" → "Gas, up $240.00") | placeholder presence was checked but not order; adjacency is the binding | cardinal rule (mis-bound figure) | `tests/unit/balance-move.test.ts` ("reordered template … missing-primary/placeholder-order"; atomic fused label+figure) |
| 2026-07-16 | Relational connectives ("shifted from {primary} to {second}") asserted a false inter-category FLOW the engine never computed | the connective whitelist admitted flow/comparison words | descriptive-not-causal guardrail | `tests/unit/balance-move.test.ts` ("relational template … non-connective:shifted/compared"; connectives pruned to purely additive; window intro baked into the {window} atom) |
| 2026-07-16 | A benign custom category ("Spare Change") silently suppressed the whole explainer forever (word "change" hit the foreign-category scan) | scanning user labels with model-output rules; the scan was vestigial under the atomic grammar | "fail closed for safety, not against benign user data" | `tests/unit/balance-move.test.ts` ("does NOT suppress a benign custom-category name"; foreign-category scan removed) |
| 2026-07-16 | A digit-bearing finance label ("401k Contributions") false-suppressed the surface via stray-number | the factor's own label tokens weren't masked before the digit check | same fail-closed-against-benign-data class | `tests/unit/balance-move.test.ts` ("does NOT suppress a benign digit-bearing finance label"; label tokens masked before stray-number) |
| 2026-07-16 | Money Review §2.4 (Fable critic cycle 1, P1-1): the review-ordering LLM call fired on EVERY `getCoachData` caller — dashboard, goals, investments, assistant (×2), and the per-user digest cron loop — adding up to 7s serial latency and egressing merchants/cents/rates to a third-party model for surfaces that never render the recap (the cron discarded the result per user per run) | the LLM await was unconditional in the shared data assembler; "the page load is the natural bound" assumed one caller — the assembler had six | token discipline / privacy: no model call (and no data egress) on a path that doesn't render the model's output | `getCoachData(userId, opts?)` gates the call on `opts.orderReview`, passed ONLY by /coach/page.tsx; every other caller compiles the deterministic floor with zero network. Locked by the A4 plan criterion + cycle-2 critic re-verification of all six call sites |
| 2026-07-16 | Money Review §2.4 (Fable critic cycle 1, P1-2): a valid-vocabulary LLM reply naming an id absent from the built candidates (`["action-transfer"]` with no pending transfer) produced an EMPTY recap under a "Personalized" badge — a bad model reply was load-bearing for whether the recap existed | `selectFromOrder` filtered to the model's listed ids and never backfilled; in-vocabulary-but-absent ids passed `parseReviewOrder` (non-null → "personalized") yet matched nothing | "the LLM is bounded polish, never load-bearing" — a model reply may reorder, never delete | `selectFromOrder` rewritten one-per-role with deterministic-floor backfill: every role the floor shows is always present; `test_regression` in `money-review.test.ts` ("can never shrink the recap below the floor (P1-2)") pins empty-array and absent-id replies to the exact floor output |
| 2026-07-16 | §3.2 design risk: a categorize/intent audit row could have persisted RAW model output (or the user's question), letting model-authored text reach the ledger renderer and one user's words reach an audit surface | logging at the wrong layer — before the closed-set validator — would capture unvalidated strings | cardinal rule (no model-authored fact/text persists) + shared-demo privacy | `tests/unit/ai-audit-sink.test.ts` ("invalid reply … the raw reply is never persisted"; move_draft "EMPTY meta"; intent meta = validated kind only) |
| 2026-07-16 | §3.2 design risk: an audit-write fault (DB down) breaking categorization/routing/recap — punishing the user's action to keep bookkeeping perfect | awaited sink without a fire-wall would propagate a prisma throw into the answer path | fail loudly or not at all (recording is subordinate to answering) | `test_regression__ai_sink_fault_never_breaks_value_path` (ai-audit-sink.test.ts) + recorder try/catch |
| 2026-07-16 | §3.2 design risk: the shared demo account accumulating an AI trail (one visitor's activity shown to the next) on a keyed deployment | every anonymous visitor is the same user-demo row (the #210/#226 lesson class) | shared-demo-account-must-not-learn | `test_regression__demo_account_never_records_ai_trail` (ai-audit-recorder.test.ts) + honest empty-state copy locked by trust.spec.ts |
| 2026-07-16 | Demo visitors could TYPE/UPLOAD their real figures into the shared `user-demo` row (manual account balance, hand-entered transaction with raw descriptor, pasted CSV statement, brokerage holding) — visible to every later visitor; the #243 critic named this the open typed/uploaded leg of the bank-connect leak class | the four manual-entry actions had no demo fence; only the CONNECTED leg was fenced in #243 | shared-demo-account-must-not-learn (#210/#226/#243 class) | `tests/unit/manual-entry-demo-fence.test.ts` (`test_regression__demo_cannot_type_or_upload_real_data`: all four actions refuse demo with `DEMO_ENTRY_BLOCKED` before any DB/provider touch, row-count deltas zero, fetch spy proves no egress on a keyed deployment; a real user passes the fence) |
| 2026-07-16 | Any demo visitor could type the confirm phrase and irreversibly wipe the shared demo account for everyone (demo sign-in then FK-crashed until reseed), or bump the shared session epoch signing every concurrent visitor out (#244 critic P1-3, pre-existing) | deleteMyData/revokeOtherSessions had no demo fence and the settings UI rendered the destructive controls for demo | demo is mandatory & first-class (rule 4) + shared-demo-account leak class | `tests/unit/demo-destroy-fence.test.ts` (`test_regression__demo_cannot_destroy_the_shared_account`: both actions throw for demo with row/epoch unchanged and no signOut; real user hits the normal gate / real revoke works) + account-deletion.spec.ts (demo sees notes, controls absent) |
| 2026-07-21 | Merchant lens (#250 critic F1): the cadence line rendered the SIGNED recurring amount — "Looks recurring: monthly, typically −$1,800.00" directly under "Typically $1,800.00 a charge" on the same card, one register tap away in the demo (rent/CarMax/Geico/Netflix) | detectRecurring's typicalAmountCents is signed (negative for expense series); lens-copy formatted it raw, and the C5 unit fixture used a positive value production never emits for expenses | financial math is sacred (a self-contradicting money surface) + rendered-copy lock lesson | `merchant-lens-copy.test.ts` C5 rewritten to the production negative fixture (pins "typically $17.99", asserts sign-independence and no "-$"); `merchant-lens-server.test.ts` asserts the composed line end-to-end |
| 2026-07-21 | Merchant lens (#250 critic F2): the lens fed PENDING rows into detectRecurring while /recurring is POSTED-only — an ordinary 1–3-day pending charge at a subscription merchant made the lens claim a different "typically" than /recurring AND manufactured a phantom price-change record | the lens composition mapped register rows (which include PENDING) without the status filter getRecurring applies | the slice's own cross-surface-agreement claim; no wrong figure may depend on unposted data | `merchant-lens-server.test.ts` (`PENDING rows never reach the cadence line`): 6 posted + 1 larger pending → line pins the posted typical, pending amount asserted absent |
| 2026-07-21 | Merchant lens (#250 critic F3): `isAggregateCanonical` was exact-case, so case-variant stale rows ('ZELLE PAYMENT') could fabricate a "pattern" over heterogeneous Zelle payees — and the same casing fragility sat under the rule-read and anomaly-radar guards | the guard compared canonicals case-sensitively while every merchant compare around it is case-insensitive | defense in depth must not hinge on casing | `merchant-profile.test.ts` L3b (case-variant aggregate → null both query casings); guard itself now lowercases both sides for ALL callers |
| 2026-07-21 | Income-pause (#251 critic F1, executed repro): a routine provider row-removal (occurrences 4→3, below the alarm floor) was treated as "resumption" — the user's confirmation row was DELETED and +$380 phantom income re-projected into cash-needed as arriving today, with no feed row disclosing any of it | "resumed" was inferred from ¬lapsed (absence of the full alarm-gate lapse), so every GATE failure looked like a fresh deposit; alarm gates and standing consent are different questions | no fabrication (a projection claimed money no deposit revived) + the back-half lesson (a rule nothing checks once it starts acting) | `income-pause.test.ts` P14a–d (`confirmedPauseState`: gate-failure stays paused, only date-fresh deposits resume, vanished series inert) + `income-pause-server.test.ts` 2b (`#251 critic F1 (regression)`: row-removal → exclusion holds, consent survives, against the real refreshRecurringForUser) |
| 2026-07-21 | Income-pause (#251 critic F2): the confirmed pause's "Why am I seeing this?" disclosure said "Autopay covers this — nothing to do" — a false money claim on the exact row disclosing an active projection mutation, which has no autopay and DOES have something to do (Undo) | the card rendered a tier-generic TIER_RULE map; HANDLED means "autopay" for payment rows but "confirmed state" for income_pause, and no per-kind branch existed | false-money-copy class (#221/#249): one display template cannot describe two semantics | `nudge-feed-copy.test.ts` (`#251 critic F2`): `tierRule` (now in the testable copy module) never says autopay for a handled income_pause, keeps it for real autopay rows |
| 2026-07-21 | Money-signature card said "Saving is a steady habit … has held" while its own inline facts showed 5/12 months saved (share 4166 bps, inside the variable band) — the hysteresis-lagged label rendered as a present-tense claim | The card branched on the confirmed label alone; the latest month's banded raw (a contrary run underway) never reached the copy | Loop closure / no-fabrication (a rendered claim must be true of the facts beside it) — #221/#250 false-copy class | test_regression__signature-lag-contrary + test_regression__signature-lag-contrary-steadiness (engine `latestContrary`) + test_regression__signature-shifting-copy (lag-honest variants) |
| 2026-07-21 | "a positive savings rate in 12 of your last 12 full months" was false when no-income months sat inside the window — the 12 eligible months spanned 14 calendar months | The eligible window SKIPS null-rate months but the copy said "your last N full months" unqualified | Same false-copy class (the forming line already carried "with income"; the labeled lines dropped it) | test_regression__signature-with-income-qualifier |

| 2026-07-21 | A Plaid synced-account delete judged its resurrection guard from a PRE-transaction account read: a concurrent re-link/sync stamping plaidItemId to a live item in the gap deleted a row that item's next sync resurrects (history lost) — #256 critic P1-1, executed repro | deleteDisconnectedSyncedAccountFor read the account's linkage outside prisma.$transaction and passed the stale snapshot to syncedDeleteBlockReason; only the connection/item lists were read inside (the #253 F2 fix was half-applied to the new discriminating field) | Maker/Checker + #253 F2 precedent: every input a destructive guard judges must be read inside the same transaction that commits the destruction | test_regression__plaid-linkage-read-inside-tx (tests/unit/account-delete-server.test.ts) |
| 2026-07-21 | /triage's Backfill ("Re-run categorizer") button was unreachable for every brand-new signup: backfill.spec timed out for 60s waiting for `backfill-run`, and a real first-run user had no way to re-run categorization at all — found by the first FULL e2e run after #259, not by #259's own targeted spec runs | #259 (agent-review A3) gave /triage a zero-account first-run empty that replaces the ENTIRE page, toolbar included; the spec's premise "a fresh signup has an empty review pile" silently became "a fresh signup has no Backfill button" | Surgical-change discipline: adding a route-level gate changes what EXISTS on the route for a whole user class — sweep the specs that enter that route in that state, in the same slice (the #244 fence lesson, one level up: a page gate is a fence too) | tests/e2e/backfill.spec.ts — now provisions one manual asset past the gate, and asserts `triage-first-run-empty` has count 0 BEFORE clicking, so a future gate move fails with the cause instead of a button timeout |

| 2026-07-21 | Owner: "it's just not remembering the password I entered earlier" — NOT diagnosed. The verified sub-fact: with the #258 viewer open, the form submits with the password field at `type="text"`, so the DOM a browser password manager inspects at submit time is not the DOM it inspected before #258 existed | #258's `PasswordInput` flips `type` and never flips it back; nothing restored it at submit | Rule 0 — restore a known-good state before diagnosing, and do not claim a cause you have not checked (the browser-behaviour half stays UNVERIFIED; only the DOM half is executed) | tests/e2e/auth.spec.ts — "a visible password is hidden again before the form submits" (toggle to text → submit → `type` is `password`, `aria-pressed` is `false`) |

| 2026-07-22 | #264 (M.2): finance list controls and the shadcn Button primitive rendered under the 44×44pt iOS / 48dp Android tap-target minimum (~20px hand-rolled buttons; Button base h-8/32px) — "a 20px Delete next to a 20px Edit" is the owner's "doesn't work right on my phone" | No layout gate ever asserted control SIZE (the whole e2e suite rendered one width and only checked existence); the Button primitive itself floors at 32px so "uses shadcn Button" was not a proxy for ≥44px | Wave M.2: one shared `.tap-target` primitive (min-height 44px, `@media (pointer: coarse)`) on the Button base + every hand-rolled control; min-height only so it never widens a row into M.3's horizontal overflow | tests/e2e/tap-targets.spec.ts — asserts boundingBox().height ≥ 44 for a shadcn Button (demo-sign-in, locks the primitive bump) and hand-rolled /accounts add buttons (locks the utility), under the touch-emulated mobile-380 project |
| 2026-07-22 | Owner iOS-Safari screenshots (2026-07-21): /accounts Assets + Liabilities rows overflow the right edge — money values and the Delete control clipped by the viewport (e.g. "$16,728.7", "$25,374.10 De"), a WRONG figure. NOT reproduced in Playwright Chromium OR WebKit 26.4 at 360/393/430 with long synced names, 6/7-figure balances, or 150% text — real iOS Safari's stricter flexbox `min-width:auto` is the leading cause (its child `<Link>` lacked min-w-0, so a long synced name never truncated) | `LinkedRow`'s flex chain (`<li>`→`<Link>`→name flex) had min-w-0 only on the name-side div; lenient engines shrink a min-width:auto flex item anyway, real iOS Safari does not, so the long name pushed balance+Delete off-screen. Also hardened the net-worth summary line to flex-wrap | Mobile UI never tested for layout: the whole e2e suite rendered one Chromium width (380) and NO test asserted the page fits; /accounts had no axe scan either (docs/MOBILE_UI_BRIEF.md) | tests/e2e/mobile-overflow.spec.ts (scrollWidth<=clientWidth at 360/393/430 on Chromium + a NEW mobile-webkit project, incl. a seeded long-name SYNCED LinkedRow reproducing the owner's data) — a regression GUARD: green now, but does not fail-old in Playwright's lenient engines; owner on-device confirmation is the final check |
| 2026-07-22 | At a 7-figure value the money figures in the `grid-cols-3` summary strips (transaction summary, forecast milestones, recurring "coming up") overflow their own ~100px cell and overlap the neighbouring figure — a wrong figure; the hero `cash-needed-card` `grid-cols-[auto_1fr_auto]` due-date rows could also push the page past the viewport edge with long concatenated card names (#265, Wave M.3) | Tailwind `grid-cols-3` compiles to `repeat(3, minmax(0,1fr))`, so those tracks already shrink and the PAGE never overflows — but an unbreakable 7-figure dollar amount overflows its OWN cell without `break-words`; the hero card uses a bare `1fr` (= `minmax(auto,1fr)`) whose min-content floor a long card-name string forces wide | cardinal sin: never silently clip or overlap a money figure — and no per-element overflow gate existed for the money grids (the page-level gate can't see in-cell overlap) | tests/e2e/mobile-overflow.spec.ts — `/transactions with a 7-figure summary` asserts the money value's own scrollWidth ≤ clientWidth at 360/393/430 (fail-old PROVEN by reverting the txn-list fix, rebuilding, and observing the failure), plus a demo structural sweep extending the page-level gate to /dashboard, /transactions, /forecast, /recurring || 2026-07-22 | #272 (Wave 4.6 slice 3, critic F1 P0 CONFIRMED): with a reconciliation active and NO designated payment account, every funding-account fallback (`resolvePaymentAccount`, the forecast anchor) picked the zeroed PREDECESSOR (it sorts first by creation order) — cash-needed fabricated an 80 000¢ shortfall while the user's real checking held $2,500 | The boundary engine remapped only a non-null stored `paymentAccountId`; the fallback tiers in finance.ts/forecast.ts scanned `snap.accounts` unaware of superseded rows | Maker blind spot: designed for the stored-id path, never enumerated the FALLBACK resolution paths the null case takes (the exact onboarding-nudge state) | tests/unit/reconcile-boundary-assembler.test.ts — "F1: fallback funding-account resolution skips the zeroed predecessor" (resolvePaymentAccount + getCashFlowForecast through the real assembler, paymentAccountId null) |
| 2026-07-22 | #272 (Wave 4.6 slice 3, critic F2 P0 CONFIRMED): successor transactions dated ≤ cutover but BEFORE the predecessor's first row (Plaid's 24-month backfill vs a 90-day SimpleFIN window) were dropped with no counterpart — real money removed from every snapshot engine, unfixable by cutover choice (cutover is bounded ≥ predecessor's first txn) | First-draft rule "successor keeps only date > cutover" assumed the predecessor's history covers everything before the cutover; a successor can reach further back | The half-open line was tested only at the boundary date, never at the far END of the window — the abstention/majority lesson applied to dates, not just link shapes | tests/unit/reconcile-boundary.test.ts — "F2: the successor's deeper backfill BEFORE the predecessor's first transaction is never dropped" + assembler R1 test now seeds a 2024 backfill row |
| 2026-07-22 | #272 (Wave 4.6 slice 3, critic F3 P1 CONFIRMED): predecessor balance snapshots dated after the cutover (real observed month-ends while still live; cutover defaults to last TXN date) were dropped while the successor had no snapshots there — a fabricated ~70% dip in the dashboard net-worth trend | Balance snapshots were split by the TRANSACTION half-open rule by analogy; snapshots are STOCKS — a lone observation is a correct single contribution, only same-date collisions double-count | Applied a flow-dedup rule to a stock series without re-deriving the invariant for the data type | tests/unit/reconcile-boundary.test.ts — "F3: a snapshot with no same-date counterpart is ALWAYS kept" (netWorthSeries composed, 240 000¢ point where the draft produced 0) + exact-date collision winner test |
| 2026-07-22 | #272 (Wave 4.6 slice 3, cycle-2 critic F9 P2 CONFIRMED): a chain with a downstream cutover EARLIER than an upstream one (A→B cut 06-30, B→C cut 03-31) double-counted every date in (03-31, 06-30] where the oldest and newest generation both have data — the same real transaction and snapshot surviving twice behind a "reconciled" label | The engine composes DIRECT links only (deliberate — transitive claims would make the read path graph analysis), and confirm never related a new link's cutover to its chain neighbours | The read-time inertness matrix covered link SHAPES (missing/self/cross-type/cycle) but not link PARAMETER relationships across a chain | tests/unit/reconcile-boundary-assembler.test.ts — F9 half of "does NOT undo a legitimate chain link": re-confirming downstream P→S with a cutover before upstream Q→P's is refused; on-boundary re-confirm passes |
| 2026-07-22 | #273 (Wave 4.6 slice 4, Opus red-team CLAIM2 P1 CONFIRMED): a reconciled CREDIT card whose LIVE successor is on the estimate path (fresh Plaid reconnect, no statement generated yet) lost the predecessor's real CURRENT statement — the owed amount demoted to the successor's next-cycle estimate and, because another card had a real statement, dropped OUT of the cash-needed headline (`cycleObligations = real ? real : estimated`) and the 5-day reminder window ($2000 owed rendered as $0 due this cycle) | slice-4 first draft FULL-DROPPED predecessor statements on the theory "the successor's live balance captures the debt" — true only on the STATEMENT path; on the estimate path the engine buckets estimated obligations into `upcoming` and excludes them from the headline whenever any real statement exists | financial math is sacred (a money under-count) + verbatim-meaning lesson (a rule derived for one path, never re-derived for the estimate path) | tests/unit/reconcile-boundary.test.ts "R4 (CLAIM 2): a predecessor statement NEWER than the successor's is RE-KEYED onto the successor" + "with NO successor statement, ALL predecessor statements re-key"; tests/unit/reconcile-slice4.test.ts "CLAIM 2: a live successor on the ESTIMATE path inherits the predecessor's current statement" (headline 210 000¢ incl. the re-keyed $2000, isEstimated false) |
| 2026-07-22 | #273 (Wave 4.6 slice 4, Opus red-team CLAIM7 P2 CONFIRMED): the household superseded-predecessor exclusion checked ONLY successor existence, not the assembler's full effectiveness rule — so a crafted USD→EUR reconciliation (confirm had no currency guard; the EUR successor is currency-withheld ⇒ the link is INERT in the owner's personal view, USD predecessor counts fully) marked the USD predecessor superseded in the household, vanishing the partner's real USD shared account from every household surface while the owner still saw it | activeSupersededPredecessorIds hand-rolled a successor-exists check instead of reusing effectiveReconciliationLinks; the boundary GUARDS cross-type/cycle/withheld shapes at read time (a-guard-must-read-what-it-guards) but the household helper did not, so the two disagreed on exactly those inputs | a-guard-must-read-what-it-guards: the household exclusion must read the SAME effectiveness the boundary does | reconciliation.ts helper now reuses effectiveReconciliationLinks on the currency-supported set (exact parity) + a confirm-time currency guard refusing cross-currency at the source; tests/unit/reconcile-slice4.test.ts "CLAIM 7: activeSupersededPredecessorIds supersedes the valid predecessor but NOT the inert cross-currency one" + "confirmReconciliationFor refuses a cross-currency link" |
| 2026-07-22 | #273 (Wave 4.6 slice 4, Opus red-team CLAIM5 P2 CONFIRMED): the shared register HIDES the owner's superseded reconciliation predecessor rows (getSharedTransactionsView excludes them), but the slice-6 recategorizeSharedTransaction WRITE-guard hand-rolled the same shared predicate WITHOUT the exclusion — so a partner could recategorize a transaction on an account they are never shown (a read/write asymmetry landing on the owner's triage state) | the R5 exclusion was applied per-call-site to 5 READ surfaces; the 6th site (a WRITE guard sharing the identical predicate) was missed — the exact fence-copied-per-call-site failure the sanctioned-sites comment itself cites | fence-by-construction-not-per-call-site: a guarded predicate copied per site misses sites; the one integration test must drive every site | household-actions.ts recategorize guard now subtracts activeSupersededPredecessorIds (matches getSharedTransactionsView); tests/unit/reconcile-slice4.test.ts "CLAIM 5: recategorizeSharedTransaction refuses a row on a superseded predecessor" (refused, category unchanged) |
| 2026-07-22 | #274 (Wave 4.6 slice 5, F5 CONFIRMED-live latent bug closed): `getAccountsView` (the /accounts page, its subtotals, and its net-worth trend) read balances Prisma-direct and NEVER applied the reconcile boundary — so once a link existed the reconciled predecessor's balance still counted, DOUBLING net worth on /accounts while the dashboard + assistant (snapshot-fed) already showed it at $0.00. The two surfaces contradicted each other on the same number. Safe until this slice ONLY because no UI could create a link | slice 3 applied the boundary in `getFinanceSnapshot`, but `getAccountsView` is a SEPARATE Prisma-direct read path (richer per-account view) that the snapshot fix never touched — the exact "widening/second read path" class | financial math is sacred (a doubled net worth) + a-guard-must-read-what-it-guards (the last per-account money surface must apply the SAME boundary the dashboard does) | `getAccountsView` now runs the SAME `applyReconciliationBoundary` engine (accounts+snapshots+links; txns/statements/scheduled empty — the account/snapshot outputs depend only on the former, so balances + trend match the dashboard by construction); tests/unit/reconcile-accounts-view.test.ts (R2 predecessor zeroed / R6 warning suppressed / R3 both-live still warns / R9 undo restores) + tests/e2e/reconcile.spec.ts (seed twin → $4,900 → combine → $2,500 → undo → $4,900) |
| 2026-07-22 | Reconciled chain A→B→C: the terminal successor's deep backfill double-counted history inside the ORIGINAL predecessor's claim (one $50 purchase counted twice); A↔C same-date snapshots both survived, doubling the net-worth trend | keepsTxn/keepsSnapshot composed DIRECT links only — EDGE_CASES residual (e) claimed only non-monotone chains could double, which was false for monotone chains with deep backfill | Money integrity (no double-count, R1/R2) | reconcile-boundary.test.ts 'A-F1: the terminal successor is excluded by the GRAND-predecessor claim' + 'A-F4: a grand-pair same-date snapshot collision keeps exactly one copy' |
| 2026-07-22 | /transactions register showed BOTH providers' copies of every claim-span transaction — rows and summary totals up to 80% inflated, contradicting the dashboard on the same screenload; same class in CSV export, budgets month spend, triage queue/badge, recurring re-detection | getTransactions (and the four siblings) read Prisma directly with no boundary — the slice-5 'every per-account money surface' closure missed the transaction-list class | Money integrity / spec §10.5 same-deploy rule | reconcile-surfaces.test.ts 'linked: rows AND summary count each real transaction once' + reconcile.spec.ts 'slice 6: the register agrees with the dashboard' |
| 2026-07-22 | A manual or CSV row written to a reconciled predecessor dated after its cutover persisted but was dropped from every sum — money the user entered that no figure reflected | createManualTransaction/importTransactionsCsv validated ownership only; the boundary assumes a predecessor's post-cutover dates are covered by the successor's feed, false for hand-typed rows | Never a dropped figure (§2) | reconcile-surfaces.test.ts 'refuses the predecessor, allows the successor' (+ pickers exclude superseded preds) |
| 2026-07-22 | Assistant answered 'how much is in my old checking' with the boundary-zeroed $0.00 ghost, and type queries counted one real account as two | answerAccountBalance had no superseded-awareness — snap.accounts carries the zeroed predecessor row by design (history joins) | No false money claims | reconcile-surfaces.test.ts 'C-5 — assistant account-balance folds superseded predecessors' |
| 2026-07-22 | With A→B confirmed, the A→C candidate was still offered and one tap silently RE-TARGETED the confirmed link via the predecessor upsert slot; two same-bank PlaidItems' rows never warned as duplicates | candidate filter keyed on the exact pair only, not the predecessor's link membership; #192's blanket same-provider skip ignored that each PlaidItem mints its own providerRefs | User-confirmed + user-visible (constitution); advisory coverage (R3) | reconcile-surfaces.test.ts 'C-8' + 'C-10' blocks |
| 2026-07-22 | At 360px the triage quick-pick grid rendered a category name outside its own cell: the Button base is `whitespace-nowrap shrink-0`, and a grid item's min-width:auto floors the track at min-content, so "Household & Home" needed 108px in the ~102px `grid-cols-3` track and painted over its neighbour/gutter (measured; a longer user-created category name would run off the edge entirely) | The M.1 overflow gate only loads routes PASSIVELY, so every control behind a tap (the triage picker panel) was never measured; and the page-level scrollWidth assertion cannot see this class at all, because the ~5px bleed lands in the 16px shell gutter without widening the document | Never silently clip a label on a control that files money (MOBILE_UI_BRIEF / M.3 "a half-visible figure is a wrong figure"), plus the per-element measurement rule the #265 money-grid fix established | tests/e2e/mobile-overflow.spec.ts "triage quick-pick category names fit their own cells at every phone width" — per-button scrollWidth<=clientWidth at 360/393/430 in BOTH mobile-380 and mobile-webkit, on a throwaway user with its own seeded triage row; fail-old executed against pre-fix code (108>102) |
| 2026-07-23 | The dashboard claimed "No card payments are due this cycle." while real Plaid-linked Chase/Capital One cards carried balances, /cards told the owner "No credit cards yet. Connect the bank that issues your card" (they had), and the Ask assistant + weekly digest email + payment-reminders card all gave the same false all-clear | TWO independent root causes. (a) ENGINE->UI: buildObligation returns null for a card with no statement AND no cycle days ('nothing knowable about this card'); the caller discarded that null, so an undatable card was indistinguishable from a paid-off one and six surfaces rendered the resulting EMPTY set as a positive money claim. A Plaid card hits this by construction — syncLiabilities writes a Statement or nothing, and the only writer of cycleCloseDayOfMonth was the user's own manual-card form. (b) DATA: syncLiabilities — the only writer of card statements/due dates/minimums — had exactly ONE production caller (linkPlaidAccount, inside a swallowing try/catch) and no cron; and the nightly sweep resolves through getProvider(), a no-op unless DATA_PROVIDER==='plaid'. So due dates were fetched once at link, best-effort, and never refreshed | Financial math is sacred + never state a figure the data doesn't support (#221 false-money-copy class): absence of evidence was rendered as evidence of absence, on the number the whole dashboard is built around. Also the 'grep every surface fed by the widened data class' lesson — the first pass fixed 2 of 8 surfaces | tests/unit/cash-needed-unknown-due.test.ts (engine: unknownDueDateCards carried out, excluded from every total, empty for datable/paid-off/no-card cases; plus the counter-lock that a due day ALONE never fabricates a date), tests/unit/undated-cards-copy.test.ts (assistant + digest, empty AND mixed branches), tests/unit/plaid-sync-sweep.test.ts (liabilities swept on a schedule, survive a transaction failure, silent total failure reported as failed, demo never swept), tests/e2e/card-unknown-due.spec.ts (seeded undatable card: dashboard + /cards tell the truth) |
| 2026-07-23 | A Plaid-linked account could not be synced at all from the app: /accounts printed 'last synced <days ago>' beside a Disconnect button and no sync control, and nothing in the UI could move it | syncSimplefinNow + auto-sync-on-load existed for SimpleFIN since #91, but the Plaid equivalent was never built: Plaid's only ingest was the one-shot pull inside linkPlaidAccount, and the nightly cron resolves through getProvider(), a documented no-op unless DATA_PROVIDER==='plaid'. Two providers, two completely different behaviours for the same user action | Fail loudly or not at all — a connection that silently stops updating while still showing a 'last synced' label reports success it isn't delivering; plus the parity rule that a capability offered for one provider is expected for the other | tests/unit/plaid-sync-now.test.ts (both halves run, liabilities survive a transaction failure, both-fail is the only failure, demo fenced, no-items refused, per-item scoping + another user's itemId refused) and tests/unit/sync-all-accounts.test.ts (both providers attempted, only the ones the user has, partial success reported as success with the failure named, a throwing provider contained, all-fail is a failure, 'No new transactions' stated) |
| 2026-07-24 | Four P1s on the ALREADY-DEPLOYED sync slice (#278): a non-string itemId reached the Prisma where clause verbatim so a filter object matched every item and turned the per-bank Sync into an all-banks sweep; the outer catch returned a raw Prisma error (absolute server paths, four lines of source, model shape, raw userId) which the UI rendered in a role=alert; a THROWN transaction pull left added:undefined which the summary reported as 'No new transactions' — a green all-clear to a user whose bank login had expired; and a per-request-BILLED endpoint had no server-side rate limit, its only brake a per-tab sessionStorage stamp that a fresh tab or a reload loop resets for free | The slice shipped WITHOUT a hostile-critic pass despite touching authz and money ingest; each defect is the class the rubric exists to catch, and three of the four are the same 'surface asserts what the data does not support' failure as #277 earlier the same day | Financial-copy honesty (#221 class) for the false all-clear; the repo's stated rule 'every request path uses rateLimitDurable' for the missing limit; and validate-at-the-boundary (#271 scalar validation) for the untyped server-action argument | tests/unit/plaid-sync-now.test.ts (non-string and blank itemId refused with no provider call; no internal detail in any error; transactionsFailed flagged on a thrown half and NOT on a genuine zero; limiter consulted with a per-user key and its refusal honoured) and tests/unit/sync-all-accounts.test.ts (summary never says 'No new transactions' after a failed pull, names absent card-statement data, still reports a genuine zero, honours a limiter refusal) |
