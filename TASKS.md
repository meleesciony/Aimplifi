# TASKS.md — Prioritized build queue with model routing

> Completed (`[x]`) rows were moved verbatim to `docs/archive/TASKS_DONE_ARCHIVE.md`
> on 2026-08-04 so this queue stays loadable. Open/partial/blocked rows are untouched;
> when a task is done, move its row there instead of leaving it here.

*Derived from `docs/STRATEGIC_AUDIT_2026-07-09.md` (read it for the "why" behind every task).
Every task obeys AGENTS.md / CLAUDE.md / LOOP_ENGINEERING.md: engine-first, integer cents,
`bash scripts/verify.sh` green before done, hostile critic on money/security/data-integrity,
regression ledger on every fix. Update the Status column in place; record decisions in
`docs/DECISIONS.md`.*

## Routing policy (token efficiency is a core principle)

> **SUPERSEDED 2026-08-08 — read `docs/COMPETITIVE_GAP_PLAN.md` §3 for the live policy.** The
> routing was recut on an "absolute need" bar: **DeepSeek V4 Flash** is now the default maker
> (~80% of sessions — feature slices, UI, refactors, e2e authoring, mechanical slices, routine
> non-money critic cycles); **Opus 5** `xhigh`/`max` only for the four absolute-need lanes (new
> money-math engines, architecture/authz redesigns, EVERY rule-3 hostile-critic pass including
> slices Flash built, multi-hour autonomous builds); **Fable 5** only as the second-opinion
> critic. The per-row Owner/Agent cells below still carry the pre-recut model names — treat them
> as the *tier* the work needs, and resolve the actual model through §3.

Per `docs/COMPETITIVE_GAP_PLAN.md` §3. Pricing (per MTok in/out): Fable 5 $10/$50 · **Opus 5
$5/$25** · Opus 4.8 $5/$25 · Sonnet $3/$15 · Haiku 4.5 $1/$5. Output tokens dominate agentic
sessions. **UPDATED 2026-07-24: Claude Opus 5 shipped and replaces Opus 4.8 everywhere below; it
carries the full effort ladder including a new `max` tier, and Anthropic positions it as frontier
intelligence at half Fable's price with its biggest gains in deep reasoning, long-horizon agentic
work and bug-finding. Route money-math + critic work to Opus 5 `xhigh` (or `max` for the hardest),
and keep Fable 5 for a differently-trained second opinion on expensive clean bills of health.**

- **Fable 5 (effort high/xhigh)** — ONLY: new money-math/routing semantics, architecture
  decisions, hostile-critic passes on money/security/data-integrity. Costs 2× Opus; spend it
  exactly where a silent bug is most expensive.
- **Opus 4.8 (effort medium default, high for engines)** — the daily driver: feature slices,
  server wiring, refactors, routine critic cycles. ~80% of Claude sessions.
- **Sonnet (or Opus at effort: medium)** — well-specified mechanical slices, copy variants,
  test scaffolds, docs.
- **Haiku `explorer` subagent** — ALL heavy reads/exploration, on every task, regardless of the
  main model. Never paste whole files into a Fable/Opus context.
- **Cursor / Grok 4.5** — UI polish, route-specific empty states, e2e authoring from existing
  recipes (`docs/lessons/mutation-form-recipe.md`), chip/copy plumbing, test scaffolds. Binds
  to AGENTS.md like every agent. **Never** on money-math engines, categorization routing,
  authz, or critic passes — Claude owns those; Grok's output there still costs a Fable review,
  which erases the savings.

**Session hygiene (all models):** explorer-first for anything >2 files; state in ledgers, not
scrollback; one task = one session where possible; `high/xhigh` effort only for engine/critic
work. Est. budget = rough output-token ceiling for the session (LOOP_ENGINEERING rule 7); at
the ceiling without a verified result, write findings to PROGRESS.md and stop.

Status legend: `[ ]` open · `[~]` in progress · `[x]` done (verify green) · `[!]` blocked.

---

## Wave 0 — Ship what's built (this week; ops-bound, highest leverage in the repo)

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| 0.3 | Deploy: Vercel + Neon, set `DATABASE_URL`, `AUTH_SECRET`, `DATA_ENCRYPTION_KEY`, `SIGNUP_ALLOWLIST`, `CRON_SECRET`, `RESEND_API_KEY`, `VAPID_*`, `SENTRY_DSN` per docs/DEPLOY.md. Verify crons fire. | Human (keys) + Opus (guided walkthrough) | low | 10k | **[~] PARTIAL 2026-07-10 (#198/#203/#204).** Core + allowlist/cron/VAPID/Resend live; Resend domain **owner-verified** + test Delivered (#204). Sentry deferred (#203). Cron *fire* still UNVERIFIED. **Do not seed.** |
| 0.6 | Neon scheduled backups per BACKUP_AND_RECOVERY.md. | Human (ops) | — | — | [!] BLOCKED (owner-only): Neon dashboard/ops, no `NEON_API_KEY`/`psql` in this env. Neon already keeps managed backups (BACKUP_AND_RECOVERY.md); this is the owner setting PITR retention / a scheduled dump. |
| 0.7 | **Plaid institution registration (owner-reported 2026-07-22).** Plaid returns on item creation: *"You must register your application with this institution before you can create items for it. Charles Schwab and PNC require additional registration steps; for other institutions, if Production access was recently granted, registration may still be in process and can take up to 24 hours to complete. View registration status and any required next steps at https://dashboard.plaid.com/activity/status/oauth-institutions."* Owner action: open that Plaid dashboard link, complete the per-institution registration (Schwab + PNC need extra steps), and wait out the ≤24h Production-access propagation for the rest; then re-run a Plaid sandbox/production spot-check (`plaid:validate`) to confirm item creation succeeds. App stays on demo/SimpleFIN meanwhile (no code change — this is a Plaid-dashboard registration gate, not an app bug). **Message = Plaid's `INSTITUTION_REGISTRATION_REQUIRED` (verified via Plaid docs this session): cleared by finishing the app/company profile + Security Questionnaire, then Plaid-side per-institution registration (1–2 days; up to ~6 weeks for Charles Schwab).** After registration clears, OAuth banks (Chase/BofA/Schwab/PNC/…) ALSO need `PLAID_REDIRECT_URI` set + an exact-match "Allowed redirect URIs" entry in the dashboard (see `.env.example` + `plaidEnv`/`linkTokenParams`). Do NOT flip `DATA_PROVIDER=plaid` on Vercel until a real link succeeds — a bad prod cred throws app-wide (never-guess lesson). | Human (Plaid dashboard) | — | — | **[!] BLOCKED (owner-only):** owner **submitted the per-institution registration request 2026-07-22** (this session) — now awaiting Plaid's ≤24h Production propagation (longer for Schwab/PNC). Next owner step: check the OAuth-institutions status page; on "registered", set `PLAID_REDIRECT_URI` + re-run `plaid:validate`. Nothing in the app to fix. |

## Wave 1 — The return loop (30 days; retention)

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|

## Wave 2 — Ask grows a memory (90 days)

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| 2.4 | Per-user vocab in the LLM classifier prompt (top merchants + custom categories injected into the closed-set prompt; closed set unchanged). | Sonnet | low | 20k | [ ] |
| 2.5 | Evaluate local Qwen (OpenAI-compatible endpoint) as the runtime intent/label picker per COMPETITIVE_GAP_PLAN §3 — provider addition behind existing validation. | Opus 4.8 | medium | 60k | [ ] |

## Wave 3 — The self-improving layer (90 days, follows audit §4)

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| 3.3 | Adaptive dashboard order: derive card weights from EngagementEvent at read time (learn.ts idiom — recompute-from-scratch, no ratchet); chronically-ignored cards collapse behind "More". Critical alerts never demotable. | Opus 4.8 build; Cursor/Grok UI | medium | 80k | [ ] |
| 3.4 | Tenure/tone coach-copy variants: pre-written COACH_COPY variants (guardrail-test-scanned) selected by tenure bucket + dismissal history. Runtime picks *which approved sentence*, never what it says. | Sonnet (variants) + Opus (selector) | medium | 60k | [ ] |
| 3.5 | Notification cadence adaptation: repeatedly-dismissed-without-action alert kinds demote a level (never below critical floor); acted-on kinds hold. Extends notify/select.ts quiet rules. **MERGED 2026-07-15 → slice 3 of docs/NUDGE_PLAN.md (AI plan §2.2); do not build standalone — it needs the nudge feed's tier floor + behavioral data first.** | Opus 4.8; **Fable 5 critic** (alert suppression = safety surface) | high | 60k + 30k | [ ] see NUDGE_PLAN.md |
| 3.7 | Learned-rule recency decay in learn.ts (stale intent fades; undo already handles reversal). | Opus 4.8 | medium | 40k | [ ] |

## Wave 4 — Structural bets (6 months)

> **Household mode GREEN-LIT (owner decision #196, 2026-07-09):** family/household is
> in scope this year — the Wave-4 owner-decision gate is satisfied. Schedule 4.1 (the
> architecture spike + decision doc) sooner rather than later; authz retrofit cost only
> grows. Monetization is intended-but-later (household is the natural paid tier).


| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| 4.4 | Crisis-mode coach framing (multi-collision radar state → triage-order copy; guardrails extended, not bypassed). | Opus 4.8 + Sonnet copy | medium | 50k | [ ] |
| 4.5 | Widen the allowlist deliberately; watch Wave-3.2 metrics as the first real cohort lands. | Human | — | — | [ ] |

## Wave L — Live-account correctness (owner-reported from the real deployment, 2026-07-23)

> These came from the owner USING the deployed app with real linked banks, not from a plan.
> That is the highest-signal source in the repo: each one was a live falsehood or a missing
> control that no green suite had caught.

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|





| L.10 | **Account identity: stop creating the duplicate instead of detecting it.** Owner instruction 2026-07-24: *"It's one thing to allow duplicate accounts from 2 different people (husband and wife sharing a card with diff numbers) and a total other to allow 2 separate pulls from same account. If user links same account (exactly) twice in plaid, app should treat it as a refresh… if I check select all accounts, app should know that the ones in app already are just a refresh."* Design doc: **`docs/ACCOUNT_IDENTITY_ARCHITECTURE.md`** (read it first — it holds the verified code facts, the Plaid facts fetched this session, the identity ladder, and invariants D1–D9). **Why the app cannot do this today (all verified):** a Plaid account row is keyed on `(userId, provider, providerRef)` where providerRef is Plaid's `account_id` (`plaid.ts:356-358`), and Plaid's `account_id` is **not stable across Items** — so the same card re-linked through a second Link session becomes a brand-new row *by construction*; the app stores an institution NAME but no `institution_id`, so it cannot even tell that a bank is already connected; and there is no `subtype` column, so a Roth and a Traditional are both just `INVESTMENT`. **The design, four layers:** (1) route by intent — a per-connection "Add or fix accounts" control opening Link in **update mode** with `update.account_selection_enabled`, which Plaid documents as the remedy and which makes the "select all" case structurally incapable of duplicating (existing accounts return their existing `account_id`s and take the update branch, i.e. a true refresh); (2) collision interception after the exchange and before any row is written — same institution + a proven account match ⇒ discard the redundant Item and route to update mode, with a "different login — keep both" escape; (3) a real remedy for the duplicates that already exist (combine a both-live pair by disconnecting the losing side, then the shipped `confirmReconciliation` path — R3 honoured, history kept), which closes the gap L.6 recorded and L.8 inherits; (4) everything unproven stays advisory, cross-provider stays user-confirmed forever. **The line that separates the owner's two cases:** within one provider+institution a differing last-4 is a hard veto (that is what protects the spouse's card), while ACROSS providers it must never veto (L.9, owner-confirmed) — same comparison, opposite meaning, so it is scoped, never shared; and an identical balance never authorises an automatic action, because "one account, two cards" is a fact only the cardholder knows. **Schema (additive, nullable, demo byte-identical):** `PlaidItem.institutionId`, `Account.subtype` (also answers L.9's open decision (a)), `Account.persistentAccountId`. Check `git diff origin/main..main --stat -- prisma/` before the push (CLAUDE.md rule 5). 5 slices; slices 1–2 are Opus, 3–5 are **Fable** (money-visible structure + the R3 direction guard). **UNVERIFIED:** no live Plaid connection in this environment — update mode and `/item/get` are documented, not exercised. | Opus (slices 1–2); **Fable 5** (3–5) build + hostile critic | high | 120k | **[~] slice 1 done 2026-07-24 (#300, DECISIONS #292).** The three columns exist and are captured: `PlaidItem.institutionId`, `Account.subtype`, `Account.persistentAccountId` — all nullable and additive, written by the existing mapper/upsert and backfilled by the existing `syncInstitutions` sweep, so rows linked before them populate on the next ordinary sync (no re-link, no migration). Both Account columns are PRESERVE-ON-NULL: Plaid supplies `persistent_account_id` only at Tokenized-Account-Number institutions, so a response omitting it says nothing about the account and must never erase what is stored. The sweep now buys the id and the name as two separate calls — a named item never re-buys its name (Plaid bills per request), and a failed name lookup keeps the id it was resolved from, on both the sweep and the link path. Nothing reads any of it yet: no figure, route or copy changed. Gate: `bash scripts/verify.sh` GREEN — tsc 0 / eslint 0 / **3785 unit / 254 files** / build clean; FAIL-OLD proven (13 assertions fail against the stashed pre-change source). **Slice 2 done 2026-07-24 (#301, DECISIONS #293)** — Plaid Link **update mode**: every connection on /accounts offers **Add or fix accounts**, which reopens the connection that already exists instead of creating a second one. `linkTokenParams` gains an update variant (sends `access_token` + `update.account_selection_enabled`, structurally OMITS `products` — Plaid rejects them alongside an access token); `createUpdateLinkToken` mints against the item's own decrypted token with ownership enforced in the core; the update-mode public token is never exchanged. **Three fresh-context critics: 2 P0 + 6 P1, all fixed cycle 1 + regression-locked (4 ledger entries).** The P0 was structural — the update/new discriminator lived in a second localStorage key written at mint time, while the connect front door on the same page pre-mints on mount and opens without writing, so in the worst ordering a COMPLETED new-bank link was discarded unexchanged and the user was redirected as though it had worked; fixed by shape (one atomic record, stamped by whoever opens Link). Two P1s were pre-existing and fixed because the new copy depends on them: a per-item sync failure was invisible to callers (so the EXISTING Sync button said "no new transactions" when a bank had refused), and `removeItem` threw away the identity in the `/accounts/get` response it had already paid for, leaving every disconnected row permanently identity-less. All user-facing sentences live in `plaid-update-copy.ts` with their own tests. Gate: `bash scripts/verify.sh` GREEN — tsc 0 / eslint 0 / **3814 unit / 256 files** / build clean. **UNVERIFIED against live Plaid** (no connection here; request shapes read from plaid.com/docs 2026-07-24, flow exercised against a mocked server, and the Link window itself cannot be browser-tested). Residual deferred to **L.14**. **Slices 3(ladder)+4+5 done 2026-07-24 (#304, DECISIONS #297)** — reordered ahead of collision interception because the owner was looking at a duplicate the app could not fix ("What did you actually fix? I see the same accounts I posted earlier"). Shipped: the pure **identity ladder** (§5) with the null-is-unknown and never-a-balance rules, an abstain when only one side carries an `ins_*` id, and a disqualifying unknown subtype for retirement pairs; the **connection-level planner** that offers a direction only when dropping it strands nothing and every dropped row matches EXACTLY ONE surviving row; and the **combine action** (SERIALIZABLE claim -> revoke -> reconcile) with a no-loss proof that refuses any split which would drop a charge the other side does not hold. Three parallel fresh-context critics: 3 P0 + 6 P1 + 9 P2, all fixed + 8 REGRESSION_LEDGER entries. Additive schema: `Account.institutionId` + `Account.institutionName`, stamped at disconnect. **Slice 3's other half DONE 2026-07-24 (#307, DECISIONS #298) — collision interception on the fresh-Link front door.** `exchangePublicToken` now decides BEFORE it writes anything: it resolves the new item's institution, asks the user's other connections at that bank what they can reach RIGHT NOW over the wire, runs the ladder, and — only when every account the new login reaches is already reachable — hands the new Item back to Plaid (`/item/remove`), persists nothing, and refreshes what they already had. A partial overlap keeps both and discloses. Two fresh-context critics: **2 P0 + 5 P1 + 6 P2, all fixed**; both P0s shared one cause — the first version authorised an irreversible revoke from Account ROWS, which keep describing a connection after it can no longer reach them, so a re-link that REPAIRED an expired bank destroyed the working credential and kept the dead one. Layer 1 (#301) still makes the common path structurally incapable of duplicating; this is the backstop for the front door. Residuals recorded as **L.16** (the D7 prompt) and **L.17** (pre-#300 null `institutionId`; concurrent links). **UNVERIFIED against live Plaid** (no credentials here). Remaining in this row: nothing — L.10 is closed. |


| L.12 | **"We added plaid to all accounts. Why do I still have 321 inbox items… Simplifi and mint never had this problem."** + follow-up 2026-07-24: *"Showing you categorization issues. Many of these are self explanatory and simplifi deals with 90% of them well. Ours is awful by comparison."* Screenshot: the inbox groups by merchant (140 merchants left), and **"Goose Pond Bar Grille" (8 txns) shows "Suggestion: none yet — pick once for all 8"** — an obvious dining merchant with NO suggestion. **ROOT CAUSE VERIFIED THIS SESSION (all read directly, not inferred):** (1) **Plaid's own category is captured but NEVER PERSISTED.** `mapPlaidPersonalFinanceCategory` (`plaid-map.ts:546`) maps Plaid's `personal_finance_category` into our taxonomy and `prepareIngestedTransaction` uses it AT INGEST to rescue a row (`plaid-map.ts:608`, `pipeline.ts:262`) — but there is no column on `Transaction` storing the hint (confirmed: `grep providerCategoryHint prisma/schema.prisma` is empty). (2) **The triage inbox recomputes the suggestion from scratch WITHOUT the hint.** `getTriageGroups` calls `categorize(rawDescriptor, amount, date, accountId, rules, {flaggedBps})` with no provider hint (`triage.ts:~219`), then `suggestedCategoryId = out.categoryId==='uncategorized' ? null : …` — so Plaid's guess is discarded for the suggestion, and any row PFC didn't auto-file at ingest shows "none yet". (3) **The deterministic ruleset is thin and buggy.** `KNOWN_MERCHANTS` ~221 exact patterns + ~25 generic keyword rules (`normalize.ts`); the dining rule is `/\b(RESTAURANT|GRILL|…|BAR ?& ?GRILL|BBQ)\b/i` — `\bGRILL\b` does NOT match **"Grille"** (no word boundary before the "e"), and "Bar Grille" has no "&", so this merchant matches nothing. So the 140 are rows where Plaid's PFC came back LOW/UNKNOWN (Plaid isn't confident on small/local merchants) AND our ruleset missed — and even Plaid's low-confidence guess is never shown as a starting suggestion. **The fix, engine-first, in slices:** (a) **persist the provider category hint** (additive nullable `Transaction.providerCategoryId` + `providerCategoryConfidenceBps`, written by `prepareIngestedTransaction`) — schema change, check the prisma diff before push; (b) **surface it in the inbox as a one-tap pre-filled suggestion even below the auto-file bar** — Simplifi's "90%" is mostly the bank's own category shown as a confirmable default, not a silent auto-file; keep auto-FILE at the current confidence bar but let SUGGEST use the stored hint at lower confidence, disclosed as "Plaid's guess"; (c) fix the `\bGRILL\b`→"Grille" boundary + widen generic rules (whack-a-mole, lowest priority); (d) optionally auto-file at PFC MEDIUM with the visible AI badge + undo to actually shrink the queue, measured on HIS corpus via `eval:categorize`. **The honesty constraint stays**: anything auto-filed keeps the why-this-category trail and one-tap undo; a shown suggestion is labelled as a guess, never a silent decision. **Categorization routing + a money-adjacent surface ⇒ hostile-critic pass** (Opus + fresh-context critic while owner is out of Fable). This is the owner's loudest competitive complaint — high priority. **OWNER CHOSE THIS AS THE NEXT BUILD (2026-07-24).** Start slice (a) persist the provider category hint + (b) surface it as a one-tap inbox suggestion; measure the before/after auto-suggest coverage on his corpus. | Opus 4.8 build; fresh-context critic (categorization routing) | high | 120k | **[~] slices (a)+(b) DONE 2026-07-24 (#303, DECISIONS #296).** Plaid's `personal_finance_category` is now PERSISTED (two nullable `Transaction` cols `providerCategoryId` + `providerCategoryConfidenceBps`, written only by the Plaid path — demo/SimpleFIN/CSV/manual stay null) and surfaced in the triage inbox as a labelled **"Plaid's guess"** one-tap suggestion, but ONLY as a FALLBACK when our own pipeline has no suggestion. New `mapPlaidProviderCategoryGuess` (superset of the unchanged auto-file hint, captures LOW at 4000 bps) sharing a `resolvePfcCategoryId` core; group-level fallback is unanimity-gated + aggregate-suppressed + kept OUT of `isConfidentGroup`/Accept-all-confident/swipe-right (never bulk-filed). Auto-FILE behavior byte-identical (LOW 4000 < tuned clamp floor 6500 — can never auto-file). Fresh-context critic: 1 P1 + 2 P3, all fixed + locked — **P1 = the surfaced guess lacked the #44/F4 sign guard** (an OUTFLOW tagged INCOME would one-tap-book spend as income); gated in `prepareIngestedTransaction` (outflow never Income; inflow→spend refund kept). Gate: `bash scripts/verify.sh` GREEN — tsc 0 / eslint 0 / **3846 unit / 257 files** / build clean; new e2e `triage-provider-suggestion.spec.ts` PASSES. **UNVERIFIED against live Plaid** (mocked providers + real Prisma). **Deferred: (c)** ruleset `\bGRILL\b`→"Grille" boundary + generic-rule widening; **(d)** auto-file at PFC MEDIUM with an AI badge — both lower-priority whack-a-mole, measured on his corpus via `eval:categorize`. **Coverage measurement:** demonstrated deterministically in unit tests (a review group with a Plaid guess flips from "none yet" to a one-tap); the owner's LIVE before/after is UNVERIFIED here (no Plaid creds). |

| L.13 | **"Also the previous items regarding vanguard."** Owner-reported 2026-07-24 as a continuation of an earlier report. **Recorded as UNRESOLVED-REFERENCE, deliberately: there is no open Vanguard item anywhere in this repo.** Searched this session across `docs/`, `TASKS.md`, `PROGRESS.md` and `REGRESSION_LEDGER.md` — the only Vanguard mention is `DECISIONS #61`, a *closed* SimpleFIN-era fix in which a Vanguard retirement plan was classified as CHECKING and `inferAccountType` was extended to route retirement/529/annuity names to INVESTMENT. That may or may not be what he means. **Do not invent a Vanguard bug to match this line, and do not assume #61 regressed.** First action: ask him which items — ideally a /accounts or /investments screenshot showing the Vanguard rows — then reproduce. If it IS the classifier, note that Vanguard now arrives via Plaid rather than SimpleFIN, so the relevant path is `mapPlaidAccountType` (`plaid-map.ts`), not `inferAccountType`, and the new `Account.subtype` column (L.10 slice 1) now carries Plaid's own subtype verbatim, which is better evidence than the name. | Human (screenshot) + Opus 4.8 | — | 30k | [ ] |

| L.14 | **A deselected account freezes, keeps counting, and still reads as freshly synced.** Found by two fresh-context critics during L.10 slice 2 (2026-07-24) and left DELIBERATELY unfixed there, because the honest fix is money-visible and needs its own slice. Verified chain: Plaid Link update mode ships with `account_selection_enabled`, so a user can untick an account; `upsertPlaidAccounts` only ever creates or updates, and nothing prunes a row the feed stops returning (contrast `syncHoldings`, which does prune); so the row keeps its last balance and keeps counting toward net worth, cash-needed and /cards. It also keeps LOOKING current, because a Plaid row's freshness is graded from its bank's `lastSyncedAt` (#293) and that bank is still syncing. And it cannot be deleted while its bank is connected (`syncedDeleteBlockReason` → "Disconnect the bank first"). Slice 2 shipped a disclosure in the success flash naming the re-tick as the remedy — a transient message is NOT a fix for a permanently wrong figure. **Decisions this needs:** (a) how a feed-dropped row is REPRESENTED — a nullable `feedDroppedAt` on Account is the obvious additive shape, set when an item's `/accounts/get` omits a row it previously returned (and only on a CLEAN response, per the #290 prune precedent: a malformed or truncated payload must never mass-drop); (b) whether such a row is excluded from totals or disclosed and kept — #192/#221/#299 all say DISCLOSE rather than silently adjust, and the same logic applies here since only the user knows whether the account still exists; (c) the per-row freshness label must stop inheriting the bank's sync date once dropped; (d) whether Delete should be permitted for a dropped row without disconnecting the whole bank. Money-visible ⇒ Fable build + hostile critic. | **Fable 5** build + hostile critic | high | 80k | **[~] BUILT + CRITIC CYCLE 1 (2026-07-25). Both P0s and 4 of 6 P1s closed; 4 P1s recorded OPEN as L.18 — this row does NOT claim a pass.** Shipped: additive `Account.feedDroppedAt` stamped by a pure `reconcileFeedPresence` from a complete `/accounts/get` census ONLY (never the `/transactions/sync` echo, which carries only accounts with transaction activity and would have frozen every quiet loan, card and brokerage on the first sync after deploy); never on an empty or unreadable list; never re-stamped; cleared when the account returns. **Stance: DISCLOSE, ADJUST NOTHING** for totals — excluding a still-owed unticked card would stop the app telling someone to pay a bill they still owe. New `not_shared` freshness level graded from the DROP; Delete permitted (the standing refusal’s premise is false for a row the feed no longer sends); disclosure on /accounts and the dashboard. **Two fresh-context critics, run in parallel, both broke it; every fix below is locked by an executed test.** P0-1: the banner quoted a real balance as "still counted" for a reconciliation PREDECESSOR the boundary had already zeroed and /accounts had hidden — now excluded via the existing `activeSupersededPredecessorIds`. P0-2: Cash Flow Radar offered a FROZEN balance as money to move, sorted FIRST because it sorts by size — the one surface that issues a move-this-much instruction; frozen accounts are now withheld as sources and the withholding is stated. **F-1, the deepest finding, reached independently by both critics: the stance had been argued over LIABILITIES only.** The PAYMENT account can itself be frozen, and there the direction inverts — a balance frozen high reports shortfall $0 and no transfer recommendation while the real account cannot cover the autopay; cash-needed now discloses that its projection rests on a balance that stopped updating, without adjusting it. F-2: the holdings sweep PRUNED a dropped brokerage’s positions (absence read as "sold everything"), so /investments lost $50k while net worth kept it — making the shipped sentence false; prune now skipped for a dropped account. F-3: a partner’s frozen account counted in household figures unannounced — now a count in the banner (never names or amounts, which exceed the sharing consent). F-4: the remedy named "Add or fix accounts", a control that ceases to exist once the bank is disconnected — liveness is now a REQUIRED argument. Also fixed: a garbled-but-200 `/accounts/get` threw out of the LINK path; a tautological e2e assertion; a comment claiming email coverage for a channel never wired. Gate: verify.sh GREEN — tsc 0 / eslint 0 / **4148 unit / 272 files** / build clean; new e2e 2/2; FAIL-OLD proven (7 assertions). Residuals: pre-#256 unstamped rows out of scope, SimpleFIN unwired — both fail toward the pre-existing behaviour, never toward a false drop. |

| L.16 | **A collision prompt with a remembered "keep both", which layer 2 shipped without (invariant D7 → D7a).** Layer 2 (#307) decides and acts inside the exchange, so there is no prompt: a connection is discarded only when every account it reaches is already reachable through another connection that answered over the wire in the same request. That structural escape covers the ordinary different-login case, and deliberately does NOT cover two: a second login whose account set is **entirely** shared (a spouse who can see only the joint account), and a tier-A collision where two genuinely different accounts share last-4, type, subtype and currency at one bank. Both end with a connection handed back to Plaid and no way to say "no, keep both" — recoverable only by disconnecting the kept connection and linking again, which leaves an orphaned Account row. **What this needs:** a way to hold the exchanged item across a user round-trip WITHOUT writing rows the app would then have to unwind (invariant D6), a persisted per-(user, institution) "keep both" decision so the answer is not re-asked and the door is not re-walled, and copy for a prompt that names the accounts it believes are duplicates. Both fresh-context critics of #307 raised this; it is recorded, not dropped. | **Fable 5** (irreversible action + new persisted user state) | high | 80k | [ ] |
| L.19 | **The five surfaces L.18 named open** (docs/STATUS.md §STILL OPEN, 2026-07-25), ranked by money consequence: (1) **/calendar**, the sharpest, because it prints a dated amount to pay — `result.cards` (estimates included) and `loanObligations`, both of which already carry `frozenSince`, and it already renders the L.15 duplicate disclosure so the placement exists; (2) the dashboard **Today-feed nudges**, built from the frozen funding balance above the fold (mitigated — the two cards directly below them disclose it — and the `Proposal` shape is a closed field set, so wiring it is a real change); (3) the **PDF/CSV export**, whose footer affirmatively asserts "Balances reflect the data source at export time" over a frozen row, in a durable artifact handed to a lender; (4) **a frozen LOAN can never reach an all-clear qualifier** — both the digest's and the reminders card's frozen lists are cards-only and `frozenNothingDueNote` says "of the cards here", while a loan's stored due day is exactly the field the bank stopped confirming; (5) **/investments** + the **debt-payoff path** (figures only; `DebtInput` needs a type widened, so it is the drop candidate if the slice grows). Reuse the L.18 primitive in `feed-dropped-view.ts` rather than re-inventing it (the L.15 lesson), and prove fail-old in BOTH directions per the L.18 lesson — silencing the builders AND making them speak unconditionally. **Money-display copy over a data-integrity fact ⇒ Fable build + hostile critic.** | **Fable 5** build + hostile critic | high | 80k | **[~] SLICE A DONE 2026-07-25 (#311, DECISIONS #304) — surfaces (1) and (4); (2), (3), (5) remain open.** Taken together because they share ONE primitive: the set of frozen DUES a surface prints, which L.18 built from cards alone. `FrozenNothingDueRow.kind` is now REQUIRED — a card's gap is a **statement** that could not reach us, a loan issues none and its gap is a payment amount and a due day that stopped being confirmed, so the two claims cannot share a sentence. The dashboard and the digest had each hand-rolled the row list near-identically, which is how one gap opened in two places; `frozenNothingDueRows` builds it once. /calendar resolves against the due events the displayed MONTH actually paints (a quiet month is silent) and counts ACCOUNTS, not events. **A pre-existing P1 fixed en route:** `frozenLoanNote` hardcoded "Your bank" + "Check it with your lender before paying" and was ALREADY being called on a partner's shared loan — L.18's critic P1-1 closed exactly this for cards and left the loan branch. **Two fresh-context critics (copy honesty; wiring) → FAIL: 3 P1 + 3 P2 + 2 P3, all fixed + locked.** P1-1: the page prints one instruction no due row accounts for — the "transfer $X by DATE to stay covered" line, projected from the funding balance — and with every card and loan live but that balance frozen the first cut disclosed NOTHING (the quiet, expensive direction: frozen HIGH shows no dip at all). P1-2: on a calendar the DATE is the product, and `buildObligation` clamps a passed due date to today, so the grid can paint a date no bank sent and no new statement can move it. P1-3: the dashboard dropped the frozen qualifier whenever an undatable card was also present, while the digest — same rows, same builder — printed both. **A fix of mine that a test caught before it shipped:** deduplicating the repeated remedy deleted the reader's OWN remedy, because the tail differs per owner and their claim comes first. **Two of my own comments corrected by reading the engine:** the dedupe's stated justification (one obligation per statement) is false — `computeCashNeeded` pushes exactly one per card — and the multi-row all-clear said "of the cards here" on four surfaces that render no list. Gate: `bash scripts/verify.sh` GREEN — tsc 0 / eslint 0 / **4250 unit across 273 files** / build clean; new e2e `calendar-frozen.spec.ts` **6/6** incl. both abstentions; fail-old proven BOTH directions (**23** silenced, **9** unconditional); **no schema change**. NEW residual found by the critic and recorded rather than closed: an UNDATABLE frozen loan (no stored due day) still reaches no all-clear — cards carry that case out via `unknownDueDateCards`, loans have no analogue, and the fix needs `feedDroppedAt` threaded into the account payload that surface (3) also needs, so they belong in one slice. |

| L.30 | **A broken fixed-expenses zero still read like a true one** — the sharpest residual of L.29 (docs/STATUS.md §OPEN after L.29 #1). `(none counted)` was honest but could not separate "you have no repeating bills", "your bills are charged to a credit card", "your series have lapsed" and "the projection dropped them" (the L.26 signature). A count of `RecurringSeries` rows could not do it — a subscription charged to a card is legitimately detected and legitimately unprojected — so the check had to re-run the account-scope predicate and report the rows it discarded and why. | **Fable 5** build + hostile critic | medium | 60k | **[~] BUILT + SHIPPED 2026-07-27 (DECISIONS #318, STATUS §L.30) — CRITIC PASS OWED.** The reason a series is not projected is now RECORDED rather than discarded: pure `classifySeriesProjection` returns a closed reason set, `toScheduledTransactions` is implemented in terms of it (so a row and its reason cannot disagree by construction), `refreshRecurringForUser` runs ONE pass for both, and the reason persists to an additive nullable `RecurringSeries.projectionStatus` (null = "not recorded", never readable as counted). Read back as a `FixedSeriesCensus` on `SpendingPlanDisclosures` via one groupBy folded into the existing `Promise.all`. Six label branches ordered by FAILURE DIRECTION — the alarm (`N bills found, none counted here` + /recurring) and no-cash-account (+ /accounts) before the two correct absences (`all charged to a card`, `none still charging`, no control) and the now-provable empty case (`no repeating bills found yet`); L.29's `(none counted)` survives as the fallback for a mixed or unrecorded census. Plus `uncountedFixedNote` for the case NO label reaches — an UNDERSTATED non-zero figure — on /spending-plan's basis list, Ask's qualifiers and /budgets' buckets strip. **Two of my own defects caught before shipping:** the direction was backwards on Ask's overspent branch (which renders the OVERAGE, where a missing bill makes the figure BIGGER — `headline` is now required and locked both ways), and a comment claimed an auto-loan ACH was "already held by the linked loan" when `SpendingPlanInput` has no loan term at all. Gate: `VERIFY_E2E=1 bash scripts/verify.sh` — tsc 0 / eslint 0 / **286 files / 4535 tests** / build clean; full e2e **207/207 serialized** (the parallel run flaked twice on `duplicate-connections.spec.ts:164` with the #287 strict-mode DOM-duplication signature; 12/12 alone). Additive schema only. **CRITIC CYCLE 1 DONE (DECISIONS #319) — both critics FAIL, 3 P1 + 3 P2 + 3 P3, all executed and all fixed.** Two converged independently on the same two defects. (1) The "no repeating bills found yet" branch is **deleted, not narrowed**: `detected` counts STORED rows, a series is stored only if its merchant has a `Merchant` row, and manual-add/CSV-import never create one — so both critics executed a reader who had TYPED IN a bill and was told none was found while /recurring listed it. An empty table is not an empty world. (2) "All charged to a card" now requires the card term to be ACTING — an undated card's obligation is excluded entirely, so both lines read $0.00 while the fixed line said the card line held it. (3) **I reintroduced the direction bug I had just fixed, one caller later**: /budgets renders `leftToSpendCents` itself and got Ask's `overspent ? 'overage'` rule, telling an overspent reader a number was bigger than a figure that gets smaller — a required argument makes a caller answer, never answer correctly, and the discriminator is which figure the SURFACE prints. (4) the alarm prints `uncounted`, not `detected` (label and note disagreed by two bills on one page); (5) the note takes a required `lineName` (/budgets has no fixed-expenses line, only a "Fixed costs" bucket); (6) a cards-only reader is no longer sent to link a checking account by a control that cannot move the figure; (7) `derivedProjectionDigest` now selects `projectionStatus`, or L.28's defect returns on the first post-deploy sync. Re-gate: tsc 0 / eslint 0 / **286 files / 4535 tests** / build clean; full e2e **207/207 serialized**. Residuals ranked in docs/STATUS.md §OPEN after L.30 — sharpest is the ROOT cause of (1): a merchant-less series is invisible to the whole reason ledger, so the alarm has zero coverage for a hand-entering reader. |

## Wave O — Owner feedback 2026-07-27 (LIVE REQUESTS — highest priority)

Three requests arrived mid-session while L.30 was in flight. Verbatim, in the order sent:

1. *"Can we create notes like simplifi to remember what a transaction is?"*
2. *"And for fields that may have tax implications (medical expenses, child care, etc…) so easy to
   export that data during tax time"*
3. *"Please make it easier to see unclassified items in activity"*

Read together, (1) and (2) are ONE slice with two halves — a free-text note per transaction, and a
structured tax flag whose whole point is a tax-time export — because the note is prose a human reads
and the tax flag is a field a report GROUPS BY, and building the note alone would invite tax data to
be typed into it where no export can ever find it. (3) is separate and cheap.

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|


**SURVEYED 2026-07-27 (read-only; every claim below re-verified first-hand — the explorer's report cited a path that does not exist, so treat its remaining citations as unchecked).** The data class is **three** surfaces that print a per-category total, not six, and all three already hold a `categoryId`, so no new identifier is needed: `top-spending-card.tsx` (`breakdown.byCategory`, `key={c.categoryId}`), `reports-view.tsx` (same shape, top 12) and `budgets/page.tsx` (`summarizeBudgets` rows, `key={row.categoryId}`). `trends-view.tsx` shows category MOVERS (a change, not a total) and would need a different window if linked at all; `ask-view.tsx` and `recurring-view.tsx` print no category total and are out of scope. **What reconciles already:** the register query at `src/server/transactions.ts:112-113` applies the same account scope and the same split-parent exclusion as the engines (`account.type in SPENDING_ACCOUNT_TYPES`, `currency null|USD`, `isSplitParent: false`), and `src/lib/engine/transactions/query.ts:118` matches `categoryId` exactly — including `'uncategorized'`, which is a real category row (`categories.ts:141`), not a null, so it IS linkable (reports already sends that one row to /triage instead, `reports-view.tsx:116` — the only drill-down idiom in the repo today). **The two things that would make a linked page disagree with the figure clicked, both to be handled in the build, not discovered during it:** (1) **the window** — all three sources sum the CURRENT CALENDAR MONTH (`server/reports.ts` slices `today.slice(0,7)`; `budgets/page.tsx:34,48` filters `date startsWith month`), while the register defaults to ALL history, so the link must carry `from`/`to` for the month or it lands on a strictly larger number (L.31: a borrowed total imports its window); (2) **transfers** — the engines drop them outright (`reports.ts:45`, `trends.ts:122`: `if (t.isSplitParent || t.isTransfer) return false`), and the register's SUMMARY also drops them (`query.ts:173`) but its ROW LIST at the default `type=all` does NOT (`query.ts:97-101`), so a transfer row carrying that category would be visible in a list whose own total excludes it — the link should carry `type=expense` so the rows and the total describe the same basis. **Therefore the reconciliation test is the deliverable's centre**: assert the register summary at the linked URL equals the figure rendered on the source card, for at least one category, on all three surfaces. | 



## Wave O.16 — Come back to where I was (owner request 2026-07-30, LIVE)

Owner, verbatim, mid-session while O.15 slice 7 was in flight: *"Notate this for future session. Can you
add away to go back to what we were doing after let's say changing a rule? Right now I have to click
activity again and needs category"* ("add away" = "add a way"; **Activity** is the register's nav label,
`src/components/app-nav.tsx`; **"Needs a category"** is the register filter option,
`src/components/finance/transaction-filters.tsx:129`).

The complaint is about LOST PLACE, not about rules. He is working a filtered queue — Activity filtered to
"Needs a category" — opens a row's rule lever, saves, and is left wherever the rule flow ends, with the
filter gone. Getting back is two deliberate clicks he should not have to make, once per transaction, which
is exactly the friction that makes a triage session feel endless.

**Verified this session (facts, not hypotheses), enough to scope but NOT enough to build:**
`/rules` already receives `?from=<transactionId>` (`src/app/(app)/rules/page.tsx:37-49`, shipped by
O.13b) — so the rule page knows WHICH ROW he came from, but nothing carries WHERE HE WAS: the register's
filter state lives in its own `searchParams` (`src/app/(app)/transactions/page.tsx:48-53`), and no return
URL, no back link, and no post-save redirect to the originating view exists on the rules page.

**NOT yet measured — do these first, do not assume:** (1) the full set of doors into `/rules` (register
row menu, detail view, More sheet, the `/transactions` header link) and which of them a reader is most
often standing in front of; (2) whether the same lost-place friction exists after the OTHER row actions
(category change, split, tax tag, recurring), which would make this a shared return-context concern rather
than a rules-only one; (3) whether the register restores scroll position and page number, not just the
filter, since returning to row 1 of page 3 is still a lost place.

**Design cautions for whoever builds it.** A return target arriving as a URL parameter is
attacker-controllable, so it must be validated as a same-origin internal path, never redirected to
verbatim. The register already treats unknown filter values as "no filter"
(`transactions/page.tsx:48`), so a malformed return URL must degrade to the plain register rather than
error. And a "Back to…" affordance that names a view the reader did not come from is a false claim about
his own history — if the origin is unknown, say nothing rather than guess.

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|

## Wave O.14 — "the login is buggy" (owner report 2026-07-29, LIVE)

Owner, verbatim: *"Also the login is buggy. When I click login, it sometimes says wrong pw. I click it
again, it works."* Full investigation trail in `docs/STATUS.md` §the login "wrong password" report.

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| O.14b | **Name the cause of the owner's intermittent failure.** Refuted so far, each by execution or source: `MissingCSRF` (impossible — `next-auth/lib/actions.js` passes `skipCSRFCheck`), non-deterministic verification (scrypt, per-password salt, no env/clock), and the pre-hydration controlled-input class (the inputs are uncontrolled). **Both blocking owner facts ARRIVED 2026-07-29 and the space collapsed.** (1) The sentence is **"Invalid email or password."**, not "Enter your email and password." — so the password field did NOT arrive empty, which kills every "the value was cleared" mechanism including the reveal toggle's `type` flip. (2) The discriminator fired **`reason=bad-hash`** (prod log `2026-07-29T03:21:42Z`): the address matched a stored user exactly and the password bytes were non-empty and wrong. (3) NEW measurement: React resets the uncontrolled form after a rejection, emptying BOTH fields, so "I click it again" was never a second click on the same values — it was a fresh LastPass fill. With verification deterministic, **the first fill and the second fill delivered different bytes.** Three candidates remain, each with its own fix site: surrounding whitespace, the email mis-filled into the password field, or a STALE vault entry never updated after the #257 reset. A second-generation enum now separates all three on the next occurrence (`bad-hash+trim-verifies` \| `bad-hash+equals-email` \| `bad-hash` \| `no-password-set`). | **Fable 5** | medium | 40k | **[~] NARROWED 2026-07-29** — discriminator v2 + email-retention shipped (verify green, 217 e2e). Open on ONE owner-only fact: does the LastPass entry for aimplifi.app match the working password, and is there more than one entry? |
| O.14c | **If the vault entry is stale, fix the RESET flow, not the sign-in field.** `/reset-password` (#257) bumps `sessionEpoch`, does not sign the user in, and links to /sign-in — so nothing ever offers the browser/password manager the NEW credential to save, and the next visit autofills the old one. Gated on O.14b's owner answer; the fix is to let the credential be updated at the moment it changes. | **Opus 4.8** | small | 25k | **[ ] GATED on O.14b** |

## Wave O.13 — Simplifi parity is the FLOOR: the reader needs a lever (owner mandate 2026-07-29, LIVE)

Owner, verbatim, three messages with six Simplifi screenshots: *"Categorization and features for
categorization are extremely lacking compared to simplifi… You have the ability to change things like
'contains tjmax'. Because the card number and other numbers always change. This aids in future pain."* ·
*"Your goals are to make the app at least as good as simplifi. Currently we can't even solve the
transaction list. Rest of features also pale in comparison."* · *"Parity is the baseline. We were supposed
to make this smarter and more useful in every way, building in principles from personal finance books,
finding ways to cut unnecessary spending etc…"*

**The field-level gap list lives in `docs/SIMPLIFI_PARITY.md`** (matrix re-derived from the schema and the
components on 2026-07-29, not copied from an older plan): **10 of 16** Simplifi capabilities are missing
outright. The through-line is not sixteen features — it is that Simplifi gives the reader **a place to
stand on one transaction** (a detail view) and **an editable instruction the app executes** (a rule),
while Aimplifi has invested almost everything in inference (normalizer → learner → proposal → provider
guess) and given the reader no lever. When the inference is wrong his only recourse is to re-file the row,
which IS the "trailing categorization" complaint of Wave O.12.

**Governing failure direction for the whole wave:** a rule the reader typed executes without asking again,
so every ambiguity resolves toward matching too LITTLE (the row stays in review, where he sees it) rather
than too much (a silent mis-file). Preview the match count BEFORE saving, never after.

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| O.13a | **Keyword (`contains`) match keys — the owner's named ask.** A rule whose key is TYPED by the reader, not derived: every keyword must appear literally (case-insensitive) in the raw statement text, so deleting the volatile chips from `tjmaxx 0181 0966` leaves `contains tjmaxx`, which holds forever. **MEASURED CORRECTION (2026-07-29, before building): his own tjmaxx example ALREADY works here** — both `tjmaxx 0181 0966` and `TJMAXX 0499 1122` normalize to the known merchant `Tjmaxx` and auto-file as clothing without review. The live defect is elsewhere in the same screenshot: `Tst*mirko Pasta Buckhead` → canonical `Mirko Pasta Buckhead`, auto-filed dining, while `MIRKO PASTA` → canonical `Mirko Pasta`, uncategorized and in review — one restaurant, two canonicals, two categories, two review states, which NO derived key can span and one typed keyword spans exactly. That pair is the locking test. Pure engine (`keyword-rule.ts`: parse/encode/match/specificity) + `RuleLike.matchKeywords` + additive `CategorizationRule.matchKeywords` column + creation from a transaction with a live match-count preview + its own priority band above merchant rules (the reader typed this key; it is the most explicit instruction in the system) with a deterministic specificity tie-break. **Non-negotiable guard:** a keyword rule carries `merchantCanonical: null` ("any merchant" in `ruleMatches`), so an EMPTY key must match NOTHING — the `toRuleLike` orphan trap one level over. Deliberately permits what `learn.ts` refuses (aggregates, bucket labels): a derived key may not target Venmo, a typed one may, because the reader named it and can delete it. **Categorization routing on money-displaying surfaces ⇒ hostile critic.** | **Fable 5** | high | 90k | **[~] IN PROGRESS 2026-07-29 — ENGINE + STORAGE + SERVER LIVE, UI NOT BUILT (so the owner cannot use it yet).** Shipped: `keyword-rule.ts` (parse/encode/match/specificity + `KEYWORD_RULE_PRIORITY` 110), `RuleLike.matchKeywords` + one `ruleMatches` clause, additive nullable `CategorizationRule.matchKeywords`, `toRuleLike` decoding with the declared-but-empty REFUSAL, a specificity tie-break that leaves merchant-rule ordering byte-identical, and `server/keyword-rules.ts` (preview / create / list / delete) whose preview count is asserted equal to the set the rule then files. 40 locks across 3 files, the file-everything guard mutation-proven. **DONE 2026-07-29 — the builder SHIPPED and is live at `/rules`** (plus a Rules button in the register header): type the words, see the count + unfiled/already-filed split + money-in/out split + five real descriptors + a wrong-side-of-zero warning BEFORE the rule exists, then create with or without applying to history (each re-filed row writes a Correction, so it undoes like any filing), and a rules list with delete that does NOT un-categorize what it already filed. Three defects the e2e caught that no unit test could: the rule saved while the list still said "you have no rules" (the action revalidated /transactions and /triage but not the page the reader stands on), the new header link overflowed 380px in BOTH engines, and icon-only left it with no accessible name at phone width (two WCAG sweeps). Gate: VERIFY_E2E=1 green — **304 files / 4825 tests, 216 e2e**. **HOSTILE CRITIC CYCLE 1 COMPLETE — two fresh-context critics, both FAIL, 2 P0 + 7 P1, all fixed and fail-old locked (9 REGRESSION_LEDGER rows, DECISIONS #339).** The two P0s: a typed rule was the only auto-file path with NO sign check (an outflow filed as income erased real spending from reports while the flows engine still counted it), and the apply set was `{account:{userId}}` with none of the five exclusions its siblings carry (it overwrote split children, transfers, pinned rows and off-register populations). P1s: undo left the rule alive so the next backfill re-filed everything reverted; no demo fence (a visitor's typed words rendered to the next visitor, while the rule could never fire); three strings claimed an empty rule "matches everything" when the engine makes it match nothing; the builder pointed at bank text the register never shows (widened by this same session's brand work); the history rewrite defaulted ON with no undo anywhere; and the list contradicted its own success message. Also fixed: `serializableTx` + in-transaction re-read, `ensureCategories`, refusal of `transfer`/`uncategorized` targets, and a 3-character minimum key. |
| O.13b | **A transaction DETAIL view — the acute complaint ("we can't even solve the transaction list").** Today there is no `/transactions/[id]` and no drawer: the register edits through inline popovers and SPLIT is unreachable from it (split exists only in the triage inbox). One surface carrying the whole field set: payee, amount, account, date, category, status, split, note, the existing tax-class control, the O.11 flags when they land, the statement **provenance line** ("appears on your … statement as `<raw descriptor>` on …" — the text a keyword rule matches against, which is what makes rules teachable), and **"Create rule from this transaction"** pre-filled with O.13a chips. | Opus 5 | high | 100k | **[~] FIRST SLICE SHIPPED 2026-07-30 — the RULE half, on owner escalation.** Owner, mid-session: *"From transaction page, whenever clicking a transaction, should have rules pull up so you can change specifically for that transaction. This should work for prior and forward transactions. Having to remember which transaction and how to populate them exactly as written is too cumbersome."* Shipped: a `Rule…` link on every register row → `/rules?from=<id>`, the statement **provenance line**, and the key **pre-filled as chips from that row's own `rawDescriptor`** with its current category pre-selected. Two measured defects behind his complaint: `/rules` accepted NO prefill of any kind, and the register never rendered `rawDescriptor` (it shows the normalizer's cleaned-up name, a gap O.13i widened). The prefill deliberately KEEPS volatile tokens (flagged amber, not deleted) — the wave's failure direction is match-too-little, so widening stays the reader's gesture. Pure `rule-prefill.ts` + `getRuleSourceTransaction` (userId-scoped, with an `excludedReason` mirroring `matchableWhere`). Gate: VERIFY GREEN, **221 e2e**; new `tests/unit/rule-prefill.test.ts` (10) + `tests/e2e/rule-from-transaction.spec.ts` (prefill → count 1 → delete the store number → count 2 → both prior visits re-filed → a LATER visit files itself; bogus `?from=` falls back to the blank builder). **SECOND SLICE SHIPPED 2026-07-30 — the detail VIEW itself.** `/transactions/[id]`, reached by a `Details` link on every register row, carrying payee, amount, account, date, status, category, note, the tax control, the provenance badge, the statement **provenance line**, and — for the first time from the register — **SPLIT**, whose engine and action had lived only in the triage inbox since Phase 3c (a filed row could never be split). A split container renders its pieces plus Undo, the one place undo can live since the register hides the container. Every control calls the SAME action the register/inbox already called; the register's `suggestionForRow` mapper was hoisted to module scope so both surfaces compute the ladder + provenance badge from one code path, and the "why no rule here" sentence is asked of `getRuleSourceTransaction` rather than re-derived. Merchant-wide filing is deliberately NOT offered (the register's "apply to N" count comes from the reconciliation-filtered set it already holds; a second count here could differ) — this page files one row and sends durable instructions to `/rules`, which previews its own count. **The gate caught a defect the feature itself would never have revealed:** this is the app's FIRST dynamic route, and `sync-revalidate.test.ts` — the tripwire the O.12 critic left for exactly this day — failed, because `revalidatePath(p)` on a `[param]` path marks NOTHING, so the entry would have looked like coverage while nothing marked the route at all (the route is ƒ Dynamic, so this is list integrity rather than a live stale-money fix). Fixed with the type-aware `'page'` form; tripwire replaced by a stronger spy-based lock, mutation-proven. **HOSTILE CRITIC CYCLE 1 — two fresh-context critics, both FAIL: 8 P1 + several P2, all fixed and ledger-locked (6 REGRESSION_LEDGER rows).** They converged INDEPENDENTLY on the split dead-end. The two that mattered most: undo split was LOSSY (the claim nulled the parent's categoryId and undoSplit could not restore it, so undoing a split of an already-filed row discarded the reader's own filing — invisible until this slice, since split had only ever been reachable from the inbox where rows are unfiled), and the split was PERMANENT once he navigated away (a container is hidden from register AND inbox, so its undo lived at a URL nothing linked to; `splitParentId` was already returned and never rendered). Also: two false sentences — "Appears on your … statement as …" for MANUAL/CSV rows, which this slice's own e2e created the account to prove, and a rule promise of retroactive filing the default path does not do; a tax tag on a split container that saved and reached nothing (the export drops those rows); thrown refusals that would render as an opaque Next digest in production; no role=alert above a 380px fold; a silent deadline reload. Mutation-proven: reverting the split claim fails exactly the category-restore lock. **CYCLE 2 — FAIL again: 4 more P1, all fixed (5 further ledger rows + a new lesson file).** A third fresh context re-executed every cycle-1 finding instead of reading the comments claiming them closed; six were confirmed by mutation, four were not. The sharpest: the cycle-1 fix for the silent timed-out write was **INERT** — `page.tsx` imported its query-param constant from the `'use client'` view, and a client module's exports are client-REFERENCE stubs on the server, so the banner never rendered while tsc, eslint, the build and all 225 e2e stayed green (proved by an A/B build differing in one token). Also: a hand-typed row on a bank-LINKED account was still attributed to the bank (the fix had been scoped to `account.provider` when the row already knew via `providerRef` — and my own test had locked that bug in); splitting a TAX-TAGGED row silently destroyed the deduction, measured 21240 -> 0, warned only after the money moved; and the correct reconciliation-boundary fix had NO test (deleting the guard left all 9 green). Plus a note save clearing a container's tax tag, a sticky `unconfirmed` flag, two money-reasoning comments elsewhere falsified by the split change, and four per-user loaders fetched then discarded for filed rows. **The riskiest edit came back clean, by execution:** a container retaining its category cannot double-count — reports 21240 -> 21240 (household 21240 -> 1240, shopping -> 20000), trends `spentSoFarCents` 21240 both sides, register category filter returns only the child, spending-plan/dashboard/Ask byte-identical. Gate after all fixes: **✅ VERIFY GREEN**. New `tests/e2e/transaction-detail.spec.ts` asserts the raw bank text is on screen, the category edit persists (waiting on the server-rendered "You set this" badge, not the pre-reload DOM), the split's pieces render, the container leaves the register (or $212.40 counts twice) and **money-out is unchanged to the cent**. Residual recorded: a bogus id renders the branded not-found screen with HTTP **200**, not 404 — the `(app)` layout has already streamed by the time `notFound()` resolves. **STILL OPEN for the full O.13b:** payee edit on the row, user-settable status (O.13g), tags (O.11d), exclude-from (O.11a), attachments (O.13h), mark-as-recurring (O.13f). | 
| O.13e | **Category parity: hierarchy, type, tax flag.** Custom categories have a parent group and a discretionary flag; Simplifi has three levels, an explicit Expense/Income type (ours is inferred from the group NAME), and a per-CATEGORY "tax related" flag feeding a tax report (ours is per-transaction `taxClass`). Decide which of these are real gaps and which are our deliberate design before building — a third level multiplies every category picker in the app. | Opus 5 | medium | 70k | **[~] DECISION MADE 2026-07-30 (DECISIONS #345, Fable). The three sub-gaps got three different verdicts, each measured by execution, not read off a plan.** **(a) Three-level hierarchy — REFUSED, deliberate.** `Category.parentId` exists in the schema and has ZERO readers/writers outside generated Prisma, so hierarchy is not half-built; six picker surfaces plus every group-by in reports/trends/budgets/spending-plan render the 2-level shape, and Simplifi's third level (Auto & Transport → Registration → Registration Fees) is already representable here as a leaf under a group. Reopens only on an owner request naming a distinction two levels cannot express. **(b) Explicit Expense/Income type — REAL, DEFERRED, and it is not a UI toggle.** "Is this category income?" is answered in **14 places**, only 2 through the shared `isIncomeCategoryId`; the other twelve are inline `group === 'Income'`, and the readers split across TWO maps (`reports.ts`/`trends.ts` use the per-user merged meta, the other twelve the static custom-blind `CATEGORY_BY_ID`). They agree solely because `NON_CUSTOM_GROUPS` bars a custom from the Income group. Admitting custom income categories turns `pipeline.ts`'s three #44 sign guards from a documented exemption into a live P0 shape — `keywordRuleSignOk` would pass an OUTFLOW into an income category, which `isSpendRow` drops from reports/trends/budgets while `monthlyFlows` still counts it, two surfaces disagreeing by the amount with no review. Prerequisite: thread per-user meta into `pipeline.ts` and collapse all 14 onto one custom-aware basis. Against that cost, the taxonomy already ships 11 income leaves, so the marginal capability is small — hence deferred, not cancelled. **(c) Per-category tax flag — REAL, and re-routed to O.13a/row-2 rule then-actions.** Write-time stamping is the correct semantics (a read-time fallback would silently re-tag history whenever a category is edited, changing totals a reader may have handed to a preparer; write-time can only under-tag, which is visible), but there is NO single category-write choke point — `applyCategory`, `recategorize`, keyword-rule apply, backfill and both providers each write `categoryId` independently, so a per-call-site stamp is the fence-by-call-site anti-pattern. The instruction belongs on the already-fenced rule machinery, which has counted apply-to-existing and undo. Code delivered this session: the understated rationale on `NON_CUSTOM_GROUPS` (named 1 of 14 dependents) and on `isIncomeCategoryId` ("THIS is THE income test") corrected in place, so nobody relaxes the exclusion believing two call sites care. **UPDATE 2026-07-30: (c) SHIPPED as O.15 slice 6 / DECISIONS #346** — `CategorizationRule.setTaxClass`, a write-time stamp on the one fenced path, with a preview count, three documented exclusions (sign-refused, hand-filed, excluded-from-totals) and two independent guards. Two fresh-context Fable critics, both FAIL first reading (1 P0 + 7 P1), all fixed and ledger-locked. (a) stays refused and (b) stays deferred. |

## Wave P — Beyond parity: the half the owner actually bought (opened 2026-07-29)

Owner: *"Parity is the baseline. We were supposed to make this smarter and more useful in every way,
building in principles from personal finance books, finding ways to cut unnecessary spending etc…"*

The differentiator layer is largely SPECIFIED already — `docs/COACH_PRINCIPLES_PLAN.md` (the
personal-finance canon), `docs/NUDGE_PLAN.md`, `docs/AI_DIFFERENTIATION_PLAN.md`,
`docs/COMPETITIVE_GAP_PLAN.md` — and those plans own their own slices. What the owner named that has NO
task row anywhere is below. Read the plan docs against `git log` before building (lesson:
plan-verdicts-are-authoring-time).

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| P.1 | **"What should I cut?" as a standing answer, not a nudge.** The app detects price increases and negotiables reactively; it never answers the question the owner asked for. A cut is a DECISION WITH A COUNTERFACTUAL, so it must run the projection engines that already exist (radar, spending-plan, FI) over a PROPOSED change and report what actually moves — "drop these two subscriptions → your July dip disappears and FI moves 3 weeks" — with the honest null when nothing moves. Requires: a candidate finder (recurring + discretionary + duplicate-service + dormant-subscription classes), a re-projection harness, and copy that never shames (COACH guardrails). **Money instruction ⇒ hostile critic.** | **Fable 5** | xhigh | 150k | [ ] |
| P.2 | **Audit which canon principles are actually WIRED.** `COACH_PRINCIPLES_PLAN.md` predates several shipped waves; a plan verdict is authoring-time. Reconcile the plan against the code and STATUS, then re-scope. Cheap, and it stops P.1 from re-deriving what exists. | Haiku explorer + Opus 5 | small | 30k | [ ] |

## Wave O.12 — Why the categorization work looks like nothing happened (owner report 2026-07-29, LIVE)

Owner, verbatim: *"I don't see changes from the last day of work"* and *"when I categorize something, it
should also categorize the same going forward. It seems like they are only doing trailing
categorization"*.

> **2026-07-29, SECOND PASS — the first diagnosis below was measured on the wrong set and all three of
> its task rows are RETRACTED.** The original probe counted `needsReview = true` joined to `Account`
> with **no account-type filter**, but all three inbox entry points already scope to
> `SPENDING_ACCOUNT_TYPES` (`triage.ts:102`, `:260`, `:368`). It measured a set the owner's screen never
> renders. Superseded by the replay in `scripts/audit-probes/o12-what-the-inbox-actually-holds.ts`, which
> reproduces `getTriageGroups` statement for statement over his live rows. Retained here only so nobody
> re-derives the retracted numbers. Original text:
>
> *"548 unfiled rows, 16 with a proposal, 374 (68%) on INVESTMENT accounts burying the rest; the review
> queue is the one place that does not scope to `SPENDING_ACCOUNT_TYPES`."* — **false**: the queue does
> scope, the 374 securities rows were never in it, and the true queue is 173 rows / 89 merchant groups.

**Two hypotheses REFUTED in the first pass and still refuted — recorded so nobody re-chases them:** the
deploy was fine (the live domain serves the newest commit), and the register chip is not being suppressed
by its `currentCategoryId !== 'uncategorized'` gate. Also verified in code: learned rules DO apply going
forward — `loadUserRules` (`rules.ts:139`) appends them and every ingest path loads through it — so
"trailing only" is a symptom, not the mechanism.

**WHAT THE OWNER'S INBOX ACTUALLY HOLDS** (shipped `getTriageGroups` replayed over production):

| measurement | value |
|---|---|
| `needsReview` rows, every account type (what the first pass measured) | 548 |
| …after the queue's own `type in CHECKING/SAVINGS/CREDIT` | **173** (−375) |
| …after the currency and transfer guards | 173 (−0) |
| merchant **groups** — the number the nav badge prints | **89** |
| groups offered our own ruleset suggestion | **0** |
| groups offered Plaid's guess | **0** |
| groups offered an O.9 proposal | **7** |
| groups offering the reader **nothing at all** | **82** |

**WHY those 82 are silent** — bucketed by the probe, not guessed:

| reason | groups |
|---|---|
| aggregate pseudo-merchant (Venmo ×33 rows, Cash App, checks) — never ruleable by design | 10 |
| **merchant never corrected before — no history to learn from** | **72** |
| ≥2 corrections but below the bar / conflicting (the retracted O.12b class) | **0** |

**ROOT CAUSE — the tier built for exactly this population is dead on his data.** The 72 never-corrected
merchants are local one-offs (`La Mei Zi`, `Proof Of The Pudding`, `Sf Over Georgia Food`, `Creek Gc`); a
merchant learner cannot help on a first sighting, which is precisely why L.12 added Plaid's own
`personal_finance_category` as a labelled "Plaid's guess" fallback. Measured coverage of that column on
his spending rows:

| provider | rows | carrying a provider guess |
|---|---|---|
| plaid | 1,312 | **33 (2.5%)** — every one dated **2026-07-23 or later** |
| simplefin | 1,272 | 0 (correct — SimpleFIN sends no category) |

The mapping is not broken: the 33 rows map to sensible leaves (shopping, fast-food, coffee, dining,
groceries, fuel…). **L.12 shipped in `57e3576` on 2026-07-24, ingest has written the column ever since,
and (at diagnosis time) no backfill existed** — `providerCategoryId` then had exactly one writer,
`plaid.ts:1163`. Plaid's `/transactions/sync` never re-sends a row it has already
delivered, so those 1,279 nulls were **permanent** without an explicit repair — which O.12d below now
provides (the backfill is the deliberate second writer of the two provider columns). 97 of the 173 rows in his
queue are Plaid rows older than 2026-07-23, so the guess tier is silent on every one of them.

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|

## Wave O.11 — Reimbursable, excluded, and the Mint/Simplifi field set (owner request 2026-07-29, LIVE)

Owner, verbatim: *"Can we add reimbursable and exclude from budgets and all other mint and simplifi
fields? That way I don't have expenses that are work related. Similar to business related items as well"*

**ALREADY SHIPPED, verified at source before scoping this — do not rebuild:** a per-transaction free-text
`note` (O.1a); a `taxClass` drawer whose closed set already includes **`business`** ("Business expense",
`src/lib/engine/tax/classes.ts:56`) plus the six personal itemized classes, with a tax-year export;
splits (`isSplitParent`); a reviewed state (`needsReview` / `reviewPinned`); receipts; recurring
detection. **CORRECTION 2026-07-29 (fresh schema inventory, O.13h):** "receipts" here means the
`ValueReceipt` proactive-catch ledger (TASKS 1.3), NOT file/receipt ATTACHMENTS, which have never existed
— no column, no upload, no storage. And the "reviewed state" is the app's own queue state, not a flag the
reader can set (Simplifi's `Reviewed` toggle), which is O.11a's job. The genuine gaps against Mint/Simplifi are **an exclude flag**, **reimbursable**, and
**free-form tags**. Today `taxClass: 'business'` TAGS a row for the tax export and changes no figure
anywhere — which is exactly the gap the owner is describing.

**THE DESIGN DECISION, settled here so no slice has to re-litigate it: reimbursable is NOT excluded.**
The money left the account. A $800 flight you will expense has genuinely reduced what you can cover this
week, so a reimbursable row must stay in every CASH figure (cash-needed, spending plan, cash-flow radar,
account balances) and leave only the BEHAVIOUR figures (category totals, budget remainders, trends
movers, guilt-free spending). This is L.14's figure-vs-instruction axis, and the failure directions are
not symmetric: dropping it from a cash instruction tells the reader they have money they do not have.
`excluded` and `reimbursable` are therefore two flags, not one — same behaviour-figure exclusion, but
`reimbursable` additionally means "this is coming back", which is what makes the reimbursement INFLOW a
matching problem rather than income.

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| O.11a | **The two flags, engine-first.** Additive nullable/default-false columns on `Transaction` (`excludedFromSpending`, `reimbursable`), and ONE shared predicate the behaviour surfaces read — extending the `isSpendRow` family rather than adding a per-caller filter, so tsc enumerates the call sites (the O.7/O.8 mechanism). Behaviour surfaces exclude; cash surfaces do not. Every surface that starts hiding money must say so in its basis line (L.29), and a zero caused by an exclusion is a different zero from a true zero (L.30). | **Fable 5** | high | 80k | **[~] SUPERSEDED IN SUBSTANCE — verify before building.** Discovered 2026-07-31 during O.13h: both columns this row specifies ALREADY EXIST and are wired. `excludeFromTotals` and `reimbursement` shipped in **O.15 slice 2**, read through ONE shared predicate (`src/lib/engine/transactions/exclude.ts`), set from the row action menu and the detail view, badged in the register — i.e. O.11a and O.11b as written are largely built. What this row still describes that may be open: the `reimbursable`-vs-`excluded` SEPARATION argued in the wave header (slice 2 shipped one exclusion flag plus an informational refund tracker that changes no figure), and the cash-vs-behaviour surface split. Re-derive against the code before scoping — a plan verdict is authoring-time. |
| O.11b | **The UI: set them where you already are.** Register row + transaction detail + triage inbox, alongside the existing note and tax-class controls, so one row is tagged in one gesture. Include a visible marker on an excluded row in the register (an excluded row that looks identical to a counted one is how a reader loses trust in the total). | Opus 5 | medium | 60k | [ ] |
| O.11c | **The reimbursement round trip.** When the money comes back it lands as an inflow — probably `reimbursement` (an Income-group leaf that already exists, `categories.ts:31`). Counting it as income while the expense was excluded inflates income; counting both is a wash that never nets. Decide and lock: match the inflow to its outflow, or exclude both and disclose. **Money-visible ⇒ hostile critic.** | **Fable 5** | high | 70k | [ ] |
| O.11d | **Free-form tags** (the remaining Mint/Simplifi field). A many-to-many user-defined label set with filtering and a tag total. Deliberately LAST: it is the biggest schema change and the least load-bearing, and the owner's stated need (work expenses out of personal spending) is fully served by O.11a-c. | Opus 5 | medium | 70k | [ ] |

## Wave O.10 — Merchant scope, and the pair window (opened 2026-07-29 by the O.8 critics)

Both O.8 critics converged independently on the first item, which is the strongest available signal a
finding is real. None of these is owner-reported; all are verified by execution.

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| O.10a | **Ask answers a merchant question with a DIFFERENT merchant's money, under that other merchant's name.** `merchantMatches` (`answer.ts:733`) matches a bidirectional whole-word prefix, so a question about "Costco Gas" also sweeps every "Costco" warehouse row, and the display name is then chosen by largest matched magnitude — so the answer reads **"You spent $195.82 at Costco"** to someone who asked about Costco Gas. On the demo seed /trends prints $37.38 for the same merchant and month. The repo already holds both halves of the contradiction: `normalize.test.ts:93` asserts Costco Gas and Costco warehouse are DISTINCT merchants, while `assistant-merchant-spend.test.ts:33` documents the prefix match as intended. The prefix families are enumerable from the shipped merchant table (Costco/Costco Gas, Amazon/Amazon Prime/Amazon Web Services, Uber/Uber Eats). This is Ask's name resolution, not the money rule — O.8a could not close it from the trends side and pinned it instead (`o8-merchant-basis-parity.test.ts` asserts the divergence so closing it means changing a test that explains itself). **Money-visible Ask answers ⇒ hostile critic.** Decide whether a prefix query means the family or the exact store, and make the ANSWER name what it summed. | **Fable 5** / Opus 5 | medium | 60k | [ ] |
| O.10b | **A held-card payment can straddle the transfer pair window and become permanent phantom spending.** `detectTransfers` (`transfers.ts:41-58`) pairs opposite amounts within ±3 CALENDAR DAYS. Executed: checking pays on 06-28, the card posts its credit on 07-03 → only the CARD side is flagged (its descriptor is transfer-like), the checking-side payment counts as June spending on top of the charges it settles ($300 of charges + a $500 payment = $800.00), and the credit can never repay it because the next month's net-refund rule drops it. O.8(b) declined to fix this by excluding the `credit-card-payment` category, because that would delete the only trace of money leaving for a reader paying a card this app cannot see (L.14 — the direction that makes an instruction too generous). The fix belongs HERE instead: pair a `credit-card-payment` row against a held CREDIT account over a wider window, which repairs the double count without deleting anything. Zero live instances today (probe: every such row is already flagged), so this is latent, not burning. **Money-visible ⇒ hostile critic.** | Opus 5 | medium | 50k | [ ] |
| O.10c | **`merchantSpend`'s top-5 facts truncate refunds first.** `answerMerchantSpend` slices `items` (sorted contribution-desc) to 5, and a refund is always a negative contribution — so with ≥5 purchase rows the visible list is all purchases while the headline is NET. Disclosed in the detail clause ("less $X returned") and fully cited in the trace, so the figure is honest and only the VISIBLE rows are biased. Carried from O.8(c). Changing which rows a money answer shows needs its own copy pass. | Sonnet / Opus 5 | small | 30k | [ ] |
| O.10d | **Residuals recorded by the O.8 critics, none blocking.** (i) An unpaired card payment would render as "Credit Card Payment" spending beneath /budgets' "transfers excluded" label and Ask's "transfers and income are excluded" — acceptable only while that population is empty; if O.10b makes it nonempty, the label is a false claim on two live surfaces. (ii) `NewMerchant.firstDate` is computed and documented but never rendered (pre-existing dead output). (iii) The new-merchant amount applies `<= today` while /reports does not, so a future-dated manual row counts there and not here — conservative, and the card no longer claims parity with /reports. | Sonnet | small | 20k | [ ] |

## Wave O.9 — Categorization intelligence (owner feedback 2026-07-29, LIVE)

Owner, verbatim: *"categorization issues. when categorizing, i've already inputed many and the
system still doesn't recognize that the others are the same, perhaps by small differences in how
it's notated. it needs to be smarter. also for venmo and other checks that are repetitive in value,
there should be a guess at category and ask for confirmation...any time you can help categorize,
propose it and ask for confirmation. It's very dumb right now."*

Three requests in one message: (a) learning that survives notation drift, (b) a guess on repetitive
Venmo/check amounts, (c) propose-and-confirm everywhere.

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| O.9f | **An e2e for the proposal flow.** The unit + integration locks are mutation-proven, but no browser test drives a reader through seeing a proposal and confirming it. Cannot use the shared demo account (O.9c fences it), so the spec must sign up a real account and file two rows first — the #226 idiom (`ask.spec` drives a real signed-up account through the actual miner). | Opus 5 / Sonnet | medium | 50k | [ ] |

## Wave M — Mobile UI (owner request 2026-07-21)

Owner, verbatim: *"The mobile platform on my phone doesn't format correctly in the
accounts section and other sections. Please review mobile ui and make it more
functional and beautiful than simplifi, mint, and the best financial apps."*

**Read `docs/MOBILE_UI_BRIEF.md` before starting** — it holds the verified evidence,
the coverage gap that let this through, and the one question the owner must answer
first. Do not begin M.2+ before the screenshot arrives (CLAUDE.md rule 0: never
redesign a screen you have not seen).

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| M.0 | **Owner input (blocking for the layout half):** screenshots of /accounts and any other broken section, plus phone model, browser, and whether iOS Larger Text / Display Zoom is on. Named as blocking because "doesn't format correctly" has several readings (overflow, cramping, clipping under the tab bar, text scaling) with different fixes. | Human | — | — | **[~] Screenshots received 2026-07-22 (#263):** /accounts Assets + Liabilities on **iOS Safari**, ~430px (Pro Max-class, image 1290px @3×). Confirmed reading: **right-edge overflow** — money values + Delete clipped, truncation off. STILL OPEN (only if the fix doesn't fully resolve): iOS **Larger Text / Display Zoom** setting. |
| M.4 | **The beauty half — visual direction.** Load the `frontend-design` skill first. There is no `tailwind.config.ts`: this is Tailwind v4 with oklch tokens already in `globals.css:112-181` on the shadcn neutral base, so the gap is a **type scale and spacing rhythm**, not colour infrastructure — extend the token system, don't replace it. Slice 1 also revisits the app shell's 12px mobile side padding (`px-3` in `(app)/layout.tsx:46`), which shifts every route at once and so needs the full suite re-run. Then route by route, starting /dashboard and /accounts. 19 authenticated routes exist, so this is MULTI-SESSION: one or two per slice, never a big-bang restyle. Money copy must not change meaning — any figure or label edit inherits the #221 false-copy rules and the #250 verbatim-meaning rules, and any NEW adjacency of two money figures needs a rendered-copy test. | Opus 4.8 (+ Cursor/Grok for per-route polish) | high | 100k+/slice | **[~] slice 1 done 2026-07-22 (#268).** Foundation: shell mobile gutter 12→16px (`px-4`, header `-mx-4`, global — full suite re-run), page vertical rhythm `space-y-4→5` on /dashboard + /accounts, stronger page-title scale (/accounts `text-xl→text-2xl tracking-tight`), and firmer hero numerals (`font-semibold tracking-tight` on the cash-needed + net-worth figures — money TEXT byte-identical, only weight/tracking). Judged against real before/after mobile screenshots at 393px (rule 0). Deliberately restrained (no big-bang restyle); NO new CSS tokens invented — the Tailwind v4 scale + oklch vars already suffice, so authoring a parallel system would violate surgical-changes; `frontend-design` skill was NOT available in this env (noted). No money/authz/routing → no Fable critic. Gate: `VERIFY_E2E=1 bash scripts/verify.sh` → VERIFY GREEN, 3352 unit / 230 files, 157 e2e (overflow gate green in BOTH engines at 360/393/430 — 16px gutter added zero overflow). **Foundation follow-up done 2026-07-22 (#269):** the hand-rolled `TrackedActedLink` link cards were **5** (not 4), each carrying the *full* byte-identical string `block rounded-2xl border bg-card p-4 shadow-sm transition hover:border-foreground/20 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring/50` — extracted into one shared token `SURFACE_LINK_CARD_CLASS` (`src/components/finance/surface-card-styles.ts`, mirroring the #260 `AUTH_INPUT_CLASS` idiom) and applied to safe-to-spend / spending-insights / top-spending / recurring-summary / ask-aimplifi. Byte-identical ⇒ appearance provably unchanged (no rule-0 screenshot needed); the other `TrackedActedLink` consumers (onboarding-nudge → shadcn `Card`; connection-alerts-card → different inner CTA) are deliberately out of scope. Locked by `tests/unit/surface-card-styles.test.ts` (surface trio + keyboard focus ring survive future token edits — the one new risk centralisation adds). Gate: verify GREEN, **3354 unit / 231 files**, `playwright test mobile-overflow` 10/10 both engines. No money/authz/routing → no Fable critic. **Remaining slices (multi-session):** route-by-route restyle beyond /dashboard + /accounts. **Still owner-eyeball-gated:** the owner should eyeball the #268 before/after direction (and provide screenshots of the next routes — rule 0) before the subjective per-route restyle proceeds. |

**Already verified sound — do not "fix" these:** the viewport meta is correct
(`width=device-width, initialScale=1, viewportFit=cover`, and zoom is NOT disabled);
iOS safe-area handling exists (`globals.css` `env(safe-area-inset-bottom)` helpers +
the fixed bottom nav's own padding); there are zero raw `<table>` elements repo-wide.
The problem is content-level layout and control sizing, not the shell.

## Wave S — Skills, scripts & docs hygiene (see docs/SKILLS_PLAN.md for full specs)

Cheap-model wave: removes repeated AI work from every future session. Do S-tasks between
feature waves; none blocks Wave 0.

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| S.4 | Skills K3 `mutation-form`, K4 `e2e-recipe`, K5 `schema-change` (.claude/skills/) + their .cursor/rules mirrors. Extraction from lessons/precedents. | Sonnet; Opus reviews K5 golden-safety wording | medium | 60k | [ ] |
| S.5 | Skills K1 `hostile-critic` + K6 `session-close` — the biggest per-session token savers. | Opus 4.8 | medium | 60k | [ ] |
| S.6 | `scripts/scaffold-engine.ts` + skill K2 `new-engine`. | Sonnet | medium | 40k | [ ] |
| S.7 | Skills K7 `categorization-tuning` + K8 `deploy-runbook`. | Sonnet | low | 30k | [ ] |
| S.8 | Define `.claude/agents/explorer.md` (haiku, read-only) + `.claude/agents/critic.md` in-repo so the routing policy works on every machine/CI. | Sonnet | low | 20k | [ ] |


## Wave V — The verify gate's own trustworthiness (opened 2026-07-31)

Filed as its own wave, not under Wave M, deliberately: the failing specs are not mobile
(`merchant-lens`, `phase4-features`, `action-menu`, `mobile-overflow`), and the subject is the
Definition of Done itself. CLAUDE.md rule 2 makes `bash scripts/verify.sh` exiting 0 the gate for
**every** wave, so a gate that fails once per full run on a tree whose product code is sound taxes
every future slice — and worse, it trains the reader of these ledgers to wave failures through as
"the known flake", which is exactly how a real regression ships.

**The signature, recorded across two sessions and two different trees:** a full e2e run fails
exactly ONE or TWO tests; a DIFFERENT spec each run with zero overlap between runs; every failure
is the same mechanism (a click, then `waitForURL` never completing); and every one passes alone in
0.9–2.3s. Sightings so far: slice 6's tree, THREE full serialized (`--workers=1`) runs, one
rotating failure each (PROGRESS.md:7305); slice 7's tree, runs 1 and 2 (2 fails then 1 fail);
against ONE clean-HEAD run that was fully green. **The class therefore predates slice 7** — it is
visible on a tree that does not contain it — so "the 4 new data-creating e2e tests caused it" is
ruled out as the sole cause, though it may still be additive load.

**Candidate mechanism, NOT yet proven:** the e2e harness is one shared SQLite file with
`fullyParallel: true` and `workers: 4` (`playwright.config.ts:30-38`), and that config's own comment
already records this exact failure mode — write contention severing "server-action confirmation
streams to flunk the reload-bearing mutation specs (solo-green every time)". The awkward fact that
must be explained before anyone believes it: the slice-6 sightings were at `--workers=1`, where
that mechanism should not apply. Do not skip that contradiction.

**Preserved evidence** (copy it aside again before any re-run — a re-run destroys the
reproduction): `/tmp/slice7-e2e-artifacts/` holds run 1's `verify.log` plus per-failure trace
directories for all three specs, including the webkit `error-context.md` showing the page still on
`/transactions` with the action menu closed.

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| V.1 | **Settle the rotating e2e failure: contention, environment, or a real defect.** Order, cheapest first: (a) N full runs on one unchanged tree — the SAME spec failing every time is a bug, a rotating set is not; (b) the same N on clean HEAD, since a clean-HEAD failure closes the question outright; (c) vary ONE harness knob at a time (`workers`, `reuseExistingServer`, cold vs warm build) and record which one moves the rate — a rate is the measurement here, not a pass/fail; (d) instrument the stall rather than guess at it — the failure is always a navigation that never completes, so capture what the server was doing at that moment instead of inferring it. Land the outcome as either a harness fix with the rate re-measured, or a named, evidenced product defect. Do NOT close this by relaxing a timeout or retrying the spec: that hides the signal the gate exists to give. | Opus 5 | high | 80k | **[ ] OPEN — opened 2026-07-31 from the slice-7 gate.** | **NEW EVIDENCE 2026-07-31 (O.13h session, six full runs): the failure is CONNECTION-LEVEL, and it is not a leaked server.** One failure was not an assertion at all — `apiRequestContext.get: read ECONNRESET` on `GET /api/export` — which fits the whole signature (a navigation that never completes; every victim green alone in 1–3s; the set rotating). Measured: **nothing is LISTENING on port 3100 between runs**, refuting the leaked-`next start` hypothesis from `alive-is-not-progressing.md` on this machine, while sockets in `TIME_WAIT` on 127.0.0.1:3100 accumulated across the session — **754 after one run, 1,179 after the next** — with the failure count rising alongside (1 → 3 → 2 → 2). This fits the fact V.1 says must be explained first: it needs no concurrency, so the `--workers=1` sightings are consistent, and V.0's "a machine restart made it green" is consistent too, since a restart clears TIME_WAIT. **A correlation, NOT a proven cause** — 1,179 is far below the ~16k Windows ephemeral-port range, so exhaustion is not demonstrated. Cheapest next step, per (c): record TIME_WAIT before/after each run against the failure count, and move ONE knob — an idle machine, then `TcpTimedWaitDelay`/`MaxUserPort` — measuring the RATE, not a pass/fail.
| V.3 | **A leaked `next start` on port 3100 served an OLD build and produced a false P0 — and V.1's recorded evidence says this cannot happen on this machine.** Found 2026-08-01 during the W.2 slice (DECISIONS #361). Three e2e specs failed with /dashboard rendering its "Something went wrong" error boundary, which reads exactly like a P0 in the change under test; `netstat` showed **PID 22716 LISTENING on 0.0.0.0:3100**, and `playwright.config.ts` sets `reuseExistingServer: true` locally, so the new specs ran against a build predating the fixes. `taskkill` on that PID turned all three green with **no code change**. This matters twice over: (a) it is the `e2e-runs-a-stale-build` lesson recurring with a worse presentation (an error boundary, not a copy assertion), and (b) it **contradicts the V.1 note** that "nothing is LISTENING on port 3100 between runs" — the leaked-server hypothesis from `alive-is-not-progressing.md` was recorded as refuted on this machine and is not. Do NOT fold this into V.1's rotating-failure investigation without re-measuring: a stale server is a DIFFERENT mechanism from the rotating single-spec failure (deterministic, whole-suite, error-boundary) and conflating them is how V.1's evidence got polluted the first time. Fix should be structural — have the harness refuse a reused server whose build is older than the working tree, or set `reuseExistingServer: false` and pay the startup — rather than relying on a human remembering to check the port. | Opus 5 | small | 30k | **[ ] OPEN — opened 2026-08-01** |
| V.4 | **`phase2-triage.spec.ts` "singles mode never leaks onto the next card" fails DETERMINISTICALLY, and it is not the V.1 flake.** Found 2026-08-01 during the W.2 slice. It fails alone at `--workers=1` in ~27s, on the assertion `expect(inbox).toHaveAttribute('data-remaining', String(before - 1))` (`:179`) — not a `waitForURL` stall — and it **fails identically on clean HEAD** (verified by `git stash`, so it is not W.2's). That rules it out of the V.1 class on both diagnostic criteria the wave itself names: V.1's signature is a rotating victim that passes alone, and this one is a fixed victim that fails alone. Someone has been reading it as "the known flake", which is precisely the failure mode V.1's preamble warns about ("it trains the reader of these ledgers to wave failures through"). **This is very likely a REAL PRODUCT REGRESSION, not a test defect, and it is already on production** (it reproduces on clean HEAD = `251452b` = the deployed commit). The assertion that fails is the LAST one that matters: after filing the final row of a singles group through the WRITE-IN (`triage-add-category` → `new-category-submit`), `triage-inbox[data-remaining]` reads **11 when it should read 10** — the group emptied but the queue never advanced. That is precisely the cycle-2 P1 behaviour this spec exists to lock (`createAndFile` dispatches `setExtraCategories`/`setNewCatName` BEFORE `onPick→fileRow→removeRowLocally`, and the reset used to no-op when React deferred the updater), so the most likely reading is that the original fix has been undone or outrun by a later change to that ordering. Reader-facing symptom: create a new category from the last row of a group and the group appears not to file. **Do NOT fix this inside another wave's slice** — it is a state-machine change in the categorization path and wants its own diagnosis plus a critic. Start from the trace in `test-results/phase2-triage-singles-mode-*` and `git bisect` over the spec (it runs in under a minute). | Opus 5 | medium | 40k | **[x] CLOSED 2026-08-08 — the regression is gone; K.6 (2026-08-06, `4a612c7`) already rebuilt it, and this row was never updated.** K.6 moved both write-in tests to `tests/e2e/triage-write-in.spec.ts` (the O.17 demo fence made them un-runnable as demo; the fixture moved, the fence untouched) and its commit message names the product fix with sabotage proof: *"restoring the pre-fix cycle-2 P1 shape (groupEmptied mutated inside the setGroups updater, triage-inbox.tsx:337) turns the singles test RED at exactly its named assertion."* The moved test `singles mode never leaks onto the next card` asserts the exact V.4 scenario — write-in on the LAST single row → `data-remaining` advances AND `triage-singles` unmounts. **Re-verified on the current HEAD (`68c3f7f`, fresh build) this session: `npx playwright test tests/e2e/triage-write-in.spec.ts` → 2 passed (3.4s + 6.0s, 51.2s wall)** — so the row's "LIVE on production" premise is dead on the current tree, which also contains every later slice (H.7/H.8/H.1b/H.2).** |
| V.2 | **Blocked on the owner: authenticate `gh` (or supply `GH_TOKEN`) so CI can arbitrate.** `.github/workflows/verify.yml` runs the gate on a Linux runner — different hardware, different timing, `reuseExistingServer: false` — which is the documented arbiter for exactly this disagreement, and the one piece of evidence a local machine cannot produce. Unauthenticated `api.github.com` returns 404 on this private repo, so no session can read a CI result today. | Human | — | — | **[ ] OPEN — blocking (b)/(c) above being cross-checked off this machine.** |

## Standing rules for whoever picks up a task

1. Read `AGENTS.md` → `LOOP_ENGINEERING.md` → `CLAUDE.md` → `docs/lessons/INDEX.md` first, every session.
2. One task per session; explorer (Haiku) for all reconnaissance; budget above is the output ceiling — at the ceiling, checkpoint to PROGRESS.md and stop.
3. Anything touching money display, routing, authz, or alert suppression ends with a hostile-critic pass on the model marked in its row — no exceptions for Cursor/Grok work.
4. Additive schema only (golden/demo byte-identical); every adaptation user-visible and undoable per audit §4 constitution.
5. Done = verify green + tests + ledger entries + Status flipped here in the same commit.

## O.17 residuals (opened 2026-07-31 from the built-in-category rename slice, DECISIONS #350)

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| O.17a | **Key money dials by category ID, not free text.** `User.moneyDials` is a typed string list matched against category NAMES (`budgets/page.tsx`, `trends/page.tsx`), and the settings field suggests built-in names ("Travel, Dining Out, Hobbies"). A rename therefore detaches the marker from the category it was set on. O.17 mitigates it by matching either the new or the built-in name, which keeps existing dials working but is not the fix: two dials can still collide, and a dial can mark a category the reader never chose. The real change is a picker over category ids with a migration for the stored strings. | Opus 4.8 | medium | 40k | **[ ] OPEN** |

## Wave W — Coach intelligence, wealth targets, clickable charts (owner request 2026-07-31, LIVE)

Owner, 2026-07-31: *"We need to add more utility / features and insight into coach section of
app. Use principles from the best personal finance books. Intelligence is our differentiating
point compared to other apps. Also need to add a function in retirement planning or goal
planning that incorporates % savings, investment return, and other helpful features. Spending
and all other bars and graphs should be clickable. What exactly do you consider fixed costs?
For instance if I want to save up to 10 mil from my current investments and savings, what do I
need to do?"*

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| W.3 | **No Recharts chart in the app is clickable.** All four (`reports-view` BarChart, `forecast-view` AreaChart, `net-worth-card` AreaChart, `accounts-list` AreaChart) have zero `onClick`; only category/merchant TEXT figures link. The owner asked for "spending and all other bars and graphs" — but "all" is the wrong scope and the refusal is the deliverable: a /reports month bar IS a set of rows and should drill through (`categoryMonthRegisterHref` already exists with its refusal rules); a net-worth point is a BALANCE and the forecast is a PROJECTION — neither is a set of transactions, so a link there would assert an equality that is false (O.5's "a link on a figure asserts two engines agree"). Build the reports bars; state the refusal for the other three in the UI rather than leaving them looking broken. | Opus 4.8 | medium | 50k | **[ ] OPEN** |
| W.4 | **Route a wealth target through Ask.** The owner phrased his question in words ("if I want to save up to 10 mil … what do I need to do?"), which is the `plan-in-words` shape the three sibling solvers already have (`intent.ts` parse + `llm.intentFromKind` + `server/assistant.ts` case + an `answer*` composer). W.1 shipped the engine and the card but NOT the Ask route, so the question as he typed it still does not route. Needs an amount parser ("10 mil", "$10M", "ten million") with the abstention tests as the majority (see `context-carrying-features-must-abstain`). | Opus 4.8 | medium | 60k | **[ ] OPEN** |
| W.6 | **Coach intelligence expansion, ranked by what the canon actually says.** /coach already carries 12 cards. The gaps against SPEC.md §DIFFERENTIATOR #3 that are NOT yet built: (a) **money-dial-aware guilt-free framing** — Sethi's "spend lavishly on what you love, cut mercilessly elsewhere" exists as a rules list but nothing ranks cuts AGAINST the reader's stated dials; (b) **the marginal-dollar question** — given a dollar, does it go to debt, the match, the emergency fund or the taxable account, answered from the reader's own rates (the one piece of ordering advice the canon agrees on); (c) **YMOYL's fulfillment curve** — life-energy exists per purchase but not per CATEGORY over time; (d) **Housel's "reasonable > rational"** — a volatility/drawdown line on the FI projection, i.e. what a 30% drop does to the date, which W.1's sensitivity table does for returns but nothing does for sequence. Each is its own slice; (b) is the highest-value and needs rate data the app does not yet collect. | **Fable 5** | large | 120k+ | **[~] PARTIAL 2026-08-01 (#375)** — (a) SHIPPED on the wealth-target card: dial-aware discretionary cut proposals + years dial uses Settings/Plan savings % as contribution. (b)(c)(d) still OPEN. |
| W.8 | **Seven COACH_COPY keys have never been scanned by the copy guardrails.** The new completeness test in `coach-copy.test.ts` (added with W.1a) asserts every function-valued key of `COACH_COPY` has an `ALL_STRINGS` row, and found `reviewNextAction`, `reviewPersonalizedBadge`, `nextActionCancelSub`, `nextActionTransfer`, `nextActionAutomate`, `digestNothingDueWithUndated` and `digestUndatedAlongsideDues` outside it — so the shame sweep, the projection-assumption sweep and the ticker sweep have never seen any of them. They are pinned in a `KNOWN_UNSCANNED` list that may only shrink, and the pin itself is asserted so a registered key must leave it. Each needs representative args chosen with the same care as the rest of the table (several take a `PendingTransfer` with a `frozenFunding` disclosure), and registering them may surface real copy defects — which is the point. **Money-visible copy ⇒ critic if any string changes.** | Opus 4.8 | small | 40k | **[ ] OPEN** |
| N.2 | **The two spending pages are still named "Plan" and "Spending", and only their descriptions tell them apart.** N.1 deliberately renamed nothing: `/spending-plan` is labelled **Plan** and `/budgets` is labelled **Spending**, which is backwards from what most readers would guess (the page called "Spending" is the one with the targets; the page called "Plan" is the one with the guilt-free figure). Descriptions now carry the distinction, and that may be enough — check before acting. If it is not, the fix is a rename, which means: a label change in `src/lib/nav/destinations.ts`, a decision about whether the ROUTE changes too (`/budgets` is bookmarkable and appears in `categoryRegisterHref` targets and five e2e specs), and a redirect if it does. Do not rename the route without the redirect. Evidence first: this is a taste call and the owner is the only one who can settle it. | Opus 4.8 | small | 30k | **[ ] OPEN** |

## Wave O.18 — expandable rows (owner request 2026-07-31, LIVE)

Owner, with a /budgets screenshot: *"I've asked you many times to make rows expandable so I can see
what exactly system is classifying spending as. Not just the stuff in the photo but every table.
You haven't done it."*

The category LINKS shipped the day before (`21b6b20`, deployed) were not the ask. A link leaves the
page and answers one category at a time; "is this bucket right?" gets answered by scanning several
buckets, which only works if the answer opens in place. Both affordances now ship: the panel shows
what is inside, and its footer still offers the register, which is where a row gets re-filed.

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| O.18c | **/recurring rows need their own panel, because their figure is not a sum.** A series row prints `typicalAmountCents` — a median, not a total — so listing its sightings under the "these rows add up to exactly the figure above" contract would be false. The right panel here lists the CHARGES the detector saw and says plainly that the amount above is the typical one, not their total; it also wants the detector's own reasoning (cadence, occurrence count, amount plateaus), which is the real answer to "what is the system classifying as a bill". `RecurringSeriesResult` carries `occurrences` as a COUNT only, so this needs the rows carried out of the detector. | Opus 4.8 | medium | 60k | **[x] DONE 2026-08-10 (DECISIONS #439, verify green, shipped).** `RecurringSeriesResult.occurrenceRows` (required, oldest-first, signed cents + descriptor) built in `buildSeries` — the one constructor both detected and declared series funnel through — and the row expands (BreakdownPanel idiom) into the charges the detector saw under the INVERSE of the glass-box contract: the engine-composed first sentence embeds the row's RENDERED figure and says it is the typical, not the total of N charges ("The $17.99 above is this payee's most recent charge — the typical amount, not the total of 14 charges in the series"); no total rendered, no `reconciles` line (the figure is a median — a sum sentence would be false). Declared bills say "the rhythm is yours, not detected from your history"; the price-change sentence fires only for detected plateaus; income reads "deposit"/"amount". 8 unit + 1 e2e (axe AA after expand) locked; full gate + CI record in STATUS.md §O.18c. |
| O.18e | **/trends' other two lists are merchant-scoped, and one of them is a different basis.** "Biggest purchases" rows are single settled charges whose name links to the merchant-filtered register across ALL history — nothing to expand, though the row arguably wants its own `/transactions/[id]`. "New this month" rows ARE an aggregate (a merchant's month) and could carry a panel, but not this one: `buildCategoryBreakdowns` keys on category, and that card's amount applies a `<= today` guard /reports does not, so it is a THIRD basis whose panel would have to state it. Needs a merchant-keyed sibling builder, not a new argument on this one. | Opus 4.8 | small | 35k | **[x] DONE 2026-08-10 (DECISIONS #440, verify green, shipped).** `NewMerchant.rows` (required, oldest-first, signed cents, pending marked) + `futureDatedCents` carried out of the SAME pass 2 loop that sums the figure (the #439 carry-out construction), shaped by the sibling `merchantBreakdownRow` (label fallback, `${merchantKey}:${index}:${date}` keys, rawDescriptor null-when-equal-or-whitespace). The panel runs the GLASS-BOX contract (rows add up to the figure — "matched to the penny", computed in the view), and `newMerchantPanelBasis` (engine-composed, non-empty tuple, RENDERED figure/date embedded) names the in-progress window — "…in Jun '26 through Wed, Jun 10, 2026" — with the C.26 not-counted-yet sentence firing only when future-dated money exists. Premise corrected mid-slice (critic F1): NOT a third basis vs /reports — /reports stops at today too since C.26; the contrast is the complete-month movers beside this in-progress aggregate. Register footer reuses the row's merchant link with a label naming what it opens (critic F4). 15 unit + 1 e2e (axe AA) locked; full gate + critic + CI record in STATUS.md §O.18e. |
| O.18e-FU | **Critic F3 (accepted, documented): a C.25 flow-excluded loan payment can appear in the O.18e panel as "spending" while the pace card says "loan payments are not spending."** Executed scenario: a new auto loan — the lender is absent from the prior 6 months, so its payments become new-merchant rows the panel lists (pass 2 never takes `excludedFlowIds`; the figure behavior is pre-existing register-basis). The Opus critic's direction: copy, not money — the C.25 sentence is overbroad — but it is shared by FIVE surfaces (dashboard, coach, budgets, reports, trends) with their own locks, so the fix is its own slice. Either scope the C.25 sentence per-surface ("not counted in this figure") or exclude the flow set from pass 2 (money-visible — needs an Opus decision), then re-lock. | Opus 5 | small | 20k | **[x] DONE 2026-08-10 (DECISIONS #441, verify green, shipped).** Copy route, per the critic's own direction — the money behavior is untouched. One composer `loanPaymentBasisSentence(fact, scope)` in src/server/loan-payment-basis.ts, five scopes ('pace-figure' /trends, 'figures' /coach, 'cards' /dashboard, 'this-list' /budgets, 'page-figures' /reports with the escrow-change boundary), amount rendered once; all five surfaces call it (the /dashboard and /budgets hand-rolled copies deleted; the /reports client component imports the pure composer). The register-basis divergence is locked as DELIBERATE (trends-f3-regression, with anti-vacuity) so a future money-route change to pass 2 is a conscious figure change. 8 unit/render tests locked (5-scope verbatim contract + F3 negative + jsdom render proving the scoped sentence coexists with the panel listing the payment); full VERIFY_E2E=1 gate + CI + live record in STATUS.md §O.18e-FU. **Opus re-review (fresh context): PASS on first pass — 0 P0 / 0 P1**; its one wording finding on the shipped surfaces (P2-2 — the /dashboard "not in these cards" scope falsified by a literal reading of the same-page recent-transactions card, which lists the payment row) was EXECUTED — the scope now reads "the figures on these cards" (lock updated); the other three P2s accepted + recorded: P2-1 (all-excluded silence on /reports + /trends — queued as O.18e-FU2), P2-3 (the /ask answers' "counted on the loan, not as spending" — a sixth surface outside F3's five — queued as O.18e-FU3), P2-4 (the /coach reimbursement card's copy contradicting the figures sentence when a reader marks a loan payment "awaiting reimbursement" — contrived, pre-existing, recorded). |

| O.18e-FU2 | **The O.18e-FU re-review's P2-1 (accepted, documented): /reports and /trends gate the scoped C.25 sentence behind the figure it describes, so a reader whose cash account's ONLY activity is the excluded payment (a dedicated mortgage/house account — the population C.25 exists for) gets a non-empty exclusion set and NO sentence — the page instead prints "No spending counted yet this month" / "No income or spending recorded in the last 6 months" (the surfaces' own contract "silence means nothing did" is violated; something did move).** /coach, /dashboard and /budgets render the sentence unconditionally — only the two client-gated surfaces share the hole (reports-view.tsx:307/412 `hasFlows`, trends-view.tsx:315 non-null-pace branch vs the empty branch at 330-340). The naive fix (gate on `loanPaymentExclusions.length` instead of the figure branch) is FALSE COPY: the scoped sentence says "not in this pace figure", and in the abstain state there IS no pace figure — a claim about an absent figure is the same class of falsehood this slice removed. A truthful fix needs figureless-state copy designed for the empty branch, its own render decision + verbatim locks, and a fixture that reaches the state. | Opus 5 (money-visible copy design) | small | 20k | **[x] DONE 2026-08-10 (verify green, shipped).** Copy route, per the row's own prescription — the money behavior is untouched. The composer gains its 7th scope, `'figureless'` — "Payments to {payee} at {amount}/mo are counted on {loanName} instead." — positive facts only, "instead" qualifying the empty claim it sits under, never a claim about the absent figure (the naive gate-on-array fix would have been exactly that), no escrow boundary (it explains figure behavior the abstain state doesn't render; a different-amount payment flips the state to the with-figure branch where it exists). BOTH empty branches render it under the empty claim when the exclusion set is non-empty (testids `reports-loan-payment-basis-empty` / `trends-loan-payment-basis-empty`); single-ternary disjointness means the two sentences can never co-render. Locks: the Record-golden forces the new scope's verbatim (F3 negative + amount loops extend over Object.keys automatically); `o18e-fu2-render.test.tsx` reaches the state on both surfaces (pace null / all-zero months + non-empty exclusions), with CONTROL fixtures (empty + no exclusions → silent — an empty exclusion set is the only honest silence) and disjointness guards in BOTH directions (critic P2-2: a with-figure render never prints the figureless sentence, and vice versa). **Opus critic (fresh context): PASS — 0 P0 / 0 P1**, both P2s EXECUTED (docblock scope count — six said, seven in the union; the mirror disjointness guard); one P2 ACCEPTED (exclusion facts are snapshot-wide, so the sentence can render beside a payment predating the window — true as a standing disclosure, the with-figure branches share the property by documented design). The row's Opus-5 build recommendation was not followed: the slice is copy + render decisions + locks (Flash-shaped per COMPETITIVE_GAP_PLAN §3, money untouched), with the required Opus critic pass in-session. Gate: tsc 0 / eslint 0 / 6,626 unit + 1 skipped / build clean; record in STATUS.md §O.18e-FU2. |
| O.18e-FU3 | **The O.18e-FU re-review's P2-3 (accepted, documented): the /ask answers still carry the universal at one remove — "counted on the loan, not as spending" (answer.ts:989, plus the branch details at 1037 and 1058).** The slice's commit message claims "the universal removed" — true for F3's five surfaces; Ask is a SIXTH surface outside F3's scope. The answer's figure drops the rows and the sentence qualifies its own answer (consistency, not correctness — not false of the answer), but its source link opens the activity list that shows the payment: the F3 shape at one remove. Pre-existing (C.25 P1-C). Mechanical copy + any Ask locks. | Flash | small | 10k | **[x] DONE 2026-08-10 (DECISIONS #442, verify green, shipped).** FOUR sites, not the three the record cited — the fourth, which the record missed, was the hand-rolled universal the /ask server append printed (`src/server/assistant.ts`, five intents: spend_total / spend_by_category / top_categories / income / savings_rate) — its own sixth copy of the old tail. The engine's three sites now scope to the answer's own figures: the appended clause says "counted on the loan, not in these figures." (true wherever it renders — all four call sites have visible figures) and the count-0 branches end "— counted on the loan instead." (deliberately NOT "not in these figures": those answers render no figure, and a claim about an absent figure is the FU2-class falsehood; "instead" points back at the headline, which scopes the claim to this month). The server append now calls the composer with a NEW scope, 'answer' — "not in this answer.", which holds in both the with-figures and the abstain ("No spending recorded this month" + appended) states; the Record-locked golden forces its verbatim and the Object.keys-derived F3 negative covers it without a test edit. 2 new + 1 strengthened tests (both branches' exact strings locked, singular + plural, `not.toContain("not as spending")` negatives); repo-wide grep: zero runtime "not as spending" remains. Gate + CI + live in STATUS.md §O.18e-FU3. |

| O.18g | **The conscious-buckets e2e cannot reach its savings else-branch.** The branch where a working savings figure carries NO adjacent control is unreachable on the demo dataset, so it is unit-locked only and the e2e passes vacuously over it. Either seed a throwaway user where it provably binds (the pattern `trends-caps.spec.ts` and `reports-total-reconciles.spec.ts` already use) or delete the branch if it turns out no real dataset reaches it — an unreachable branch is a claim that something is handled. Carried forward from O.18f, which did not close it. | Sonnet / Opus 5 | small | 20k | **[x] DONE 2026-08-10 (DECISIONS #443, verify green, shipped).** The branch is KEPT — it is the L.29 "Set a savings target" control for real unset-$0 users (plan-row-labels.ts:579-587) — and the e2e now binds it: the demo test pins the provable $0 state (seed creates no target/goals; the dial is demo-fenced, settings-actions.ts:44) with a binding control assertion, and a throwaway user (the trends-caps / reports-total idiom) seeds a checking account + two complete POSTED paycheck months ($5,000.00 × 2 under the pinned DEMO_TODAY=2026-06-10) + `savingsTargetBps=2000` → pattern $5,000.00 → $1,000.00 planned savings, where the anti-vacuity chain (strip visible, savings > $0, panel penny-match, reconciled clause) must prove the working figure binds before the `toHaveCount(0)` control assertion. Spec 2/2 green on first run (25.5s; the new test 2.9s); gate + CI + live in STATUS.md §O.18g. |

## Wave O.20 — "Every single bar" (owner report 2026-08-01, LIVE)

Owner, 2026-08-01: *"and again, that's not even correct...i said every single bar and collection
of categories needs to be immediately available...esentially you are filtering
transactions...why is this so hard, you're not recreating wheel...mint and simplify both do
this."*

The honest accounting: twelve surfaces already drilled into transactions, and the CHART did not.
W.3 filed the Recharts charts as a refusal and **its enumeration was itself incomplete** — four
files named, seven exist, plus six hand-rolled bar surfaces. Scope by whether a bar IS a set of
transactions: where it is, it opens them; where it is not (balances, projections, holdings) it
owes a panel of what it IS made of, which is still not a refusal.

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| O.20a | **/reports prints one month's spending twice on two bases.** The bars are posted-only (`countsInFlows`); the "Spending by category" card counts pending (`isSpendRow`). Measured at $299.93 on the demo. Each panel states its basis and a new sentence names the DIRECTION of the gap without claiming a mechanism (a critic falsified the pending explanation in both directions — at least five rules separate them). Unifying the bases, or giving the card the chart's basis, is a money decision of its own. **Money-visible ⇒ critic.** | **Fable 5** | medium | 60k | **[ ] OPEN** |
| O.20b | **The /reports payload now carries six months of transaction rows instead of one.** `monthFlows` ships the rows behind all twelve bars so panels reconcile without a second query — the property the whole feature rests on. ~6× this route's RSC payload, unmeasured against a heavy real account. Measure first; only then decide whether a per-bar fetch (which would break the same-array guarantee) is worth it, or whether trimming `rawDescriptor` from chart panels is enough. | Opus 5 | small | 30k | **[ ] OPEN** |
| O.20c | **Two unfiled deposits that look identical land on opposite sides.** `isIncomeFlowRow` counts a positive row with NO category as income, while one filed to `uncategorized` (group 'Transfers & Other') nets against SPENDING. Pre-existing; O.20's panels make it visible and both basis sentences now name it. The real fix is deciding what an unidentified inflow IS, once, and it moves live figures. **Money-visible ⇒ critic.** | **Fable 5** | medium | 60k | **[ ] OPEN** |
| O.20d | **The bars still not drillable.** /coach's hand-rolled discretionary strip; /trends "New this month" (O.18e); /recurring rows (O.18c). Plus the three that are NOT transaction sets — net worth, forecast, investments allocation — which want a panel naming their constituents (accounts, projected items, holdings) rather than a transaction filter. The owner said "every single bar", so a refusal is only honest if the panel says what the bar is instead. | Opus 5 | medium | 80k | **[x] DONE 2026-08-11 (verify green, shipped).** The five remaining inert bars/charts are now real controls: the creep strip (bar → the month's discretionary purchases), the net-worth trend (point chip → the account constituents), the forecast balance line (day chip → the scheduled flows that move it), the investments allocation bar (segment → the accounts holding that symbol), and the retirement year bars (a projection refuses honestly — its panel says a projection has no rows; the age-currentAge bar is the LIVE portfolio and says so, never "no transactions or holdings make it up"). Every panel is fed the ROWS the engine carried out of the same loop that summed the figure (Σ rows === figure by construction; the "matched to the penny" markers are real checks), with engine-composed basis sentences (O.18c rule), a rowNoun contract (account rows are never called transactions), an onToggle sync (the panel's inner Hide clears the bar's expanded state), and the gross-vs-net refund disclosure. Engine: networth/series carries constituents; insights carries creep rows + hasDiscretionaryRefunds; forecast/investments/retirement basis composers. Gate: local verify GREEN (tsc 0 / eslint 0 / unit 6,650 / e2e 330 passed zero flaky); Opus critic round 1 FAIL 2 P1 (both copy-honesty) → fixed → round 2 PASS zero P0/P1. Full record in STATUS.md §O.20d. **⚠ That critic pass was run INSIDE the same DeepSeek V4 Flash session that built the slice, which CLAUDE.md forbids ("Flash never self-certifies these"). Re-reviewed 2026-08-11 by three fresh-context Opus 5 critics: the money math HOLDS (carry-out real in all three engines, integer cents intact) and every recorded number reproduces, but 10 P1s in copy honesty + tap targets — 7 fixed in O.20d-FU, the rest queued as O.20f/g/h. See STATUS.md §O.20d-FU and DECISIONS #444.** |
| O.20e | **The production demo has no investment holdings, so the allocation bar can't be demonstrated live.** The seed's five demo holdings postdate the production demo's seeding (SEED_SPEC doesn't document holdings at all — a spec gap too); the live /investments renders the honest `investments-empty` state. The additive, guarded fix is READY (`scripts/seed-demo-holdings.ts`, DECISIONS #79; production URL in `.env.prod.tmp`) but it writes to the live Neon demo — the O.20d ship slice's permission scope did not cover it. Owner decision: run it yourself (`npm run seed:demo-holdings` with the prod URL), or approve a session to do it. Tiny job, zero code. | — | tiny | 5k | **[ ] OPEN — needs owner OK for a production demo-data write** |
| O.20f | **The O.20d controls are below the repo's own tap-target floor — two of the three fresh-context critics found it independently, on different surfaces.** Making the bars controls is what created the violation: an allocation segment's BUTTON is the painted bar (`allocation-drilldown.tsx` — `h-2.5` = 10px tall, width = the position's weight), so the demo's NVDA segment is a **34×10px** target at 380px, and a portfolio priced at 0 (`addHolding` accepts `priceCents === 0`) renders every segment `width: 0%` — **no allocation panel is openable by touch at all**. The retirement strip is worse by count: 56 bars (up to 103 at `currentAge 18 / endAge 120`) share ~316px ⇒ **≈4.7px each**, and a mis-tap returns the WRONG refusal on the WRONG bar. WCAG 2.2 AA **2.5.8** requires 24×24 and the repo's own `.tap-target` token (`globals.css`, gated by `tests/e2e/tap-targets.spec.ts`) sets 44px; none of the five new controls carries it. The e2e missed it because Playwright's `.click()` hits an element's center at any size, and the mobile-380 gate measures two hand-picked controls. Fix: make the allocation LEGEND entries the buttons (they already carry symbol + swatch + index) keeping the bar as the picture; give the retirement strip `overflow-x-auto` with a 24px minimum bar width; then extend `tap-targets.spec.ts` to enumerate all five O.20d controls so the gate can see the next one. **Carry these same-file P2s in the same pass, all from the O.20d-FU critics:** (a) the percent label's own stated rule ("a sub-0.5% position must not announce 0%") is only pushed one decimal — a $3.00 position in a $1,000,000.00 portfolio renders "AAPL 0.0%"; floor it to "<0.1"; (b) "whole percent when exact" is decided by IEEE754 residue, so an exact 50/29/14/7 split renders "50% · 29.0% · 14% · 7.0%" — decide on the integer side (`Math.round(v*10)`); (c) net-worth constituent rows arrive in database order, so the "Apr 30" and "Today" panels list the same accounts in different sequences — sort by name or signed balance; (d) the panel's inner Hide unmounts the panel and drops focus to `<body>` at all four call sites — hold a ref to the opening control and restore focus; (e) `key={c.accountId}` collides on the two-same-account-snapshots input the engine's own new test blesses; (f) the chip strip is unbounded (18 pills today on a 380px dashboard, one per snapshot ever) — cap to the chart's useful window with the live point always last; (g) the month-end basis sentence in `networth/panel.ts` documents itself as safe because "every snapshot writer today writes month-ends", which is FALSE of the writer it names — `seed/build.ts` writes the `back === 0` snapshot dated `asOf`. What actually protects it is an unrelated coincidence (`businessToday()` returns `DEFAULT_AS_OF` for the demo, so that row is always overwritten by the live point). Break the coincidence with a documented invocation — `npx prisma db seed -- --asOf 2026-05-15` while `DEMO_TODAY` stays `2026-06-10` — and a "May 15" chip opens a panel reading "the sum of every account's month-end balance on May 15, 2026". Either derive the wording from the date (`isMonthEnd(date) ? … : "balance on"`) or drop the redundant `back === 0` snapshot. | DeepSeek V4 Flash (UI); no money math | medium | 50k | **[ ] OPEN — P1 (WCAG AA, in the Definition of Done) + 6 P2** |
| O.20g | **The creep detector counts merchandise returns as income, and can silence the very warning it exists to raise.** `insights.ts` adds EVERY positive row to the creep income series, which `monthlyFlows` explicitly refuses on the same page (#166: "counting it as income would inflate income AND expenses"). Failure traced by the critic: a reader with flat $6,000 income and 12% discretionary growth is flagged "Spending is outpacing income"; add one $2,000 furniture return filed to `refund` in the median month of the second half and `incomeGrowth` jumps to ~33%, the flag clears, and /coach prints **"income grew ~33.3%" — a raise that never happened**, while the SavingsRateCard beside it reports income unchanged. Pre-existing, but O.20d's new basis sentence now states the treatment to the reader, so the copy is honest about behaviour the rest of the app calls a bug. Fix: gate the income accumulator with the predicate that already exists — `isIncomeFlowRow` — and let non-income positives net `discSpend` or be dropped. **Moves a live figure and re-baselines the demo golden ⇒ money-visible, critic required.** | **Opus 5 build; Fable 5 critic** | medium | 60k | **[x] DONE 2026-08-11 (Fable 5, DECISIONS #445).** Shipped, and the row's own prescription was FALSIFIED by measurement before it was followed: a read-only production probe showed the gating ALONE made the owner's live figure worse — 67 positives ($38,619.68) left the income series, the first-half median fell from **$10,604.95 to $0.08**, and income growth went from 592% to **70,470,525%**, which silenced the flag while his discretionary spending grew ~153%. Two things shipped together: (1) the income series uses `isIncomeFlowRow` (a refused positive is DROPPED, never netted — the bar stays GROSS, which is what the O.20d-FU disclosure describes); (2) `incomeMeasured`/`spendMeasured` name the refusal that `halfGrowth`'s 0 used to hide, `flagged` requires both, and the card gains a THIRD verdict ("Can't compare yet") composed in the engine with its body and link, because every consumer read `flagged` as a binary and rendered "no lifestyle drift detected" over an income the app cannot see. THE RULE is self-referential — the first-half income median must be positive and at least the first-half discretionary median — with no dollar threshold; a COVERAGE rule (any month lacking an income row) was built first and **rejected after two critics broke it from opposite sides** (8¢ of savings interest satisfies a month count; one missing month vetoes a correct figure for anyone paid ten months a year). **Three fresh-context critics, all FAIL on pass 1: 2 P0 + 6 P1, every finding executed, all fixed.** The sharpest was that the slice's original locks DID NOT BIND — a one-month refund fixture cannot move a three-month median, so both "fail-old" tests passed against the old engine; every lock was rebuilt and the set re-proven by an 11-mutation battery (all 11 red). Also fixed en route: a dropped Money Review watch line, a recap sentence hard-coding "while income is flat", a link label asserting a definition its destination does not implement, and a NaN crash at `windowMonths=1`. **The demo verdict does NOT move** (measured on the seed at DEMO_TODAY: flagged, spend +25.5%, income exactly flat) — contrary to this row's "re-baselines the demo golden". Residuals: **O.20i**. |
| O.20i | **Two residuals of O.20g's income-baseline rule, both narrow, both deliberate.** (a) **Both baselines tiny.** `incomeMeasured` refuses when the first-half income median is below the first-half discretionary median, so a tiny income beside real spending is caught — but a reader whose discretionary baseline is ALSO near zero (income $0.08 against $0.05 of discretionary spending) satisfies `income >= spend` and the growth ratio still explodes. Vanishingly narrow and not present on the live corpus, but it is the shape the rule does not cover, so it is named rather than left to be rediscovered. (b) **A genuinely low-income reader is refused by design.** Someone living off a portfolio the app cannot see — real income $500/mo against $2,000/mo of discretionary spending — now gets "Can't compare yet" rather than a verdict. That is the intended, honest direction (the app cannot see what funds the spending, so it must not adjudicate "outpacing"), but it IS a live copy change on an unbounded population and deserves to be measured once real users exist. **Do not reach for a magnitude threshold for (a) without measuring:** this repo does not ship a magic constant into money-visible copy on a corpus of one. Re-measure both with `scripts/audit-probes/o20g-creep-income-refunds.mts`, which calls the shipped engine and prints both baselines plus the rendered verdict per real user. | **Fable 5** (money-visible copy over a baseline rule) | medium | 40k | **[ ] OPEN — documented residuals, instrument in the repo** |
| O.20h | **"Discretionary" means two different things and the two now contradict each other on screen.** The creep bar counts by the CATEGORY's `discretionary` taxonomy flag; the register and /budgets use the Fixed/Discretionary **spend class**, which honours the recurring-bill guess and the reader's own override (#397: "the reader's verdict on THIS row wins"). So a detected gym membership reads **"Fixed · you set this"** in the register and is still counted in the /coach discretionary bar — and O.20d's panel now LISTS that row under "discretionary purchases", beside a footer link inviting the reader to re-file it, which cannot move the number. Shipped mitigation (this slice): the panel states the divergence outright. The real fix is one definition — pass `spendClassOverride`/`fixedMerchants` into `detectLifestyleCreep` and select on `classifySpendClass(...) === 'guilt-free'`. **Moves a live figure on three surfaces ⇒ money-visible, critic required.** | **Opus 5 build; Fable 5 critic** | medium | 60k | **[ ] OPEN — P1 (mitigated by disclosure, not resolved)** |

Owner, 2026-08-01: *"fixed expenses are things that aren't discretionary...utilities, groceries,
and other items that we set in the budget section that we weren't built....you can help build it
and make it smart and suggest what things look like fixed. Guilt free spend is like going to
movies, going to amusement parks, going skiing, etc...in fact build a budgeting sedction so
someone can input fixed (with your suggestions), income you have calculated, the savings goal %
you have and that's how you calculate discretionary or guilt free spending."*

**The identity he describes already exists** — `computeSpendingPlan` sets
`leftToSpend = patternIncome − fixedExpenses − cardObligations − obligationsBeyondMonth −
plannedSavings`, and `mapToConsciousBuckets` re-partitions it so the three buckets sum to income
by construction. The savings-% dial (`User.savingsTargetBps`) exists in Settings, and income is
already computed. **The broken term is `fixed`**: it counts only DETECTED recurring series (≥3
occurrences, ≤2 amount plateaus, a recognised cadence) plus card payments, so groceries, fuel and
any variable utility fall into guilt-free — exactly W.5's finding, now confirmed by the owner.
Every category already carries a `discretionary` flag, which is the app's suggestion for free.

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|

## Wave O.19 — "These numbers do not add up to July monthly total" (owner report 2026-07-31, LIVE)

Owner, with two /reports screenshots (header "Jul · $28,253.04 total", eleven visible rows
summing to $19,312.25): *"Notate this. These numbers do not add up to July monthly total: 28k
and change. Make it make sense."*

Measured cause, same session: `reports-view.tsx:44` renders `byCategory.slice(0, 12)` under a
header printing `totalCents`, which the engine computes over the WHOLE array
(`engine/reports/reports.ts:101`) — a silent top-12 cap beside an uncapped total, the exact
"no silent caps" class. O.18's per-row expanders sharpened the contradiction: each row now
proves itself to the penny while the page-level identity is silently ~$8.9k short. The
dashboard Top Spending card is the same disease at `slice(0, 4)` beside "`totalCents` this
month" (`top-spending-card.tsx:44,58`). **Sweep CORRECTED by the O.19 hostile critic (my
first pass wrote "Ask's 'biggest' phrasings print no adjacent total" — false, verified by
reading):** `answerTopCategories` (answer.ts:441-458) prints `Total <period>: $X` beside a
capped fact list, and `answerSpendTotal` (answer.ts:381-398) headlines the whole-month total
over `byCategory.slice(0, 3)` facts with no count or remainder — the same shape the owner
photographed, mitigated only by the headline's tap-through trace. /budgets is uncapped;
/trends caps under completeness-implying headers but prints no adjacent total. Filed as
O.19b/O.19c below.

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|

## Wave U — Full user-experience cohesion pass (owner request 2026-07-31)

Owner, verbatim: *"App is not very cohesive. Eventually we need to do a full user experience
pass."* Filed as its own wave because it is a PLANNING deliverable first, not a slice: an
inventory of the app's surfaces against a single set of interaction idioms (which figures tap,
which rows expand, which pages answer which question — the N.1 menu descriptions are the seed),
then a ranked fix list. Do not start it inside another slice; it needs its own session and an
owner review of the plan before any code moves.

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| U.1 | **UX cohesion audit → ranked plan.** Inventory every route's idioms (tap targets, expanders, disclosure style, empty states, copy voice), name the inconsistencies with screenshots, rank by reader confusion, and propose the unifying rules as a doc the owner approves before implementation waves begin. | Fable 5 | large | 100k | **[ ] OPEN — owner said "eventually"; queue after the LIVE waves** |
| U.2 | **Semantic status-color tokens → migrate the emerald/amber class literals.** ~172 `emerald-*`/`amber-*` Tailwind class occurrences across 51 files under `src/` encode two semantic roles by raw hue: BRAND accent (nav active state, logo, focus rings — consistently `emerald-500`) and STATUS (positive money `emerald-600`/`dark:emerald-400`, warnings `amber-700`/`dark:amber-400`). Chart hexes are already unified in `src/lib/ui/chart-colors.ts` (2026-08-11) — this slice does the same for classes: define `brand`, `positive`, `warning` scale aliases in `globals.css` `@theme` (values identical to today's emerald/amber, so zero visual change), then migrate ALL call sites in one slice — a partial migration splits the vocabulary and is worse than none. Mechanical; values must be byte-identical per shade (do NOT re-judge shades per site — see the AA-contrast comments, e.g. `retirement-outlook-card.tsx` amber-700). Verify: full gate + grep proves zero remaining `emerald-`/`amber-` literals outside the token definitions. | DeepSeek V4 Flash | medium | 60k | **[ ] OPEN — opened 2026-08-11 (cohesion pass); safe to run any time, no owner gate** |

## Wave C — Calculation audit (owner report 2026-08-02, LIVE)

Owner, verbatim: *"do a multi agent adversarial review of every displayed calculation in this app.
For instance the trends makes no sense. 'on pace for 19,713.85 less than last month' how? we've
spent 578.79 on the first day of the month… 8971.25 makes no sense since our mortgage is ~6200.
That's just one of many. user experience also seems quite clunky in areas."*

**Full findings: `docs/CALC_AUDIT_2026-08-02.md`** — six parallel read-only hostile critics
(trends/pace · spending-plan+Fixed · money instructions · long-horizon projections · cross-surface
parity · transaction/rules UX). 8 P0, 29 P1, ~20 P2, plus 7 UX P1. Read that doc before starting
any row here; each row below is a pointer, not the evidence.

Both owner numbers resolved exactly: `57879 / 2 days * 31 = 897125c` and July actual
`$28,685.10`. Seven of the eight P0s were reproduced in the main thread against source, not taken
on a subagent's word (`a-subagents-green-is-a-hypothesis`).

**START HERE: C.1 + C.3 + C.2 as one /trends slice.** Owner's reply to the audit was *"doesn't
seem like it's fixed at all"* — the review shipped a document and his number is unchanged. The
next session writes CODE; the analysis is done and lives in the audit doc. Then C.7, then C.0 →
C.4/C.5, then C.6 in its own session.

**Grok 4.5 scope in this wave (owner asked 2026-08-02).** Per the routing policy above, Grok may
take **C.3**, **C.16** (excluding F5's confirm-step and F7's split-container fence — both are
data-integrity), the **wiring half of C.15** once the return encoder exists, and the **copy-only
P2s in C.17**. Grok may NOT take C.1, C.2, C.4, C.5, C.6, C.7, C.9, C.11, C.12, C.13, C.14's
`goalFIImpact` floor, C.15's encoder (a URL-construction boundary), or C.17's float-rounding fix.
Note for expectation-setting: **C.3 does not change the $8,971.25** — it adds the day count, the
assumption and the horizon and removes the false green. The figure itself moves only in C.2.

**C.0 gates the Fixed cluster ONLY** — it is a read-only database measurement that produces
nothing the owner can see, so it is not the place to start. Per
`three-sessions-of-hypothesis-one-query-of-evidence`, three prior sessions each shipped a
plausible Fixed-figure fix without measuring and none was the cause. C.4/C.5 move Fixed in the
OPPOSITE direction from the pace defect the owner reported; do not guess which is live on his
account.

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| C.13 | **Parity: the Fixed/Guilt-free heading link promises rows the destination cannot show.** The panel sums pending in; `?spendClass=` filters on a classifier requiring POSTED (`spend-class.ts:70` → `insights.ts:48`). $49.93 on the shipped demo. The e2e lock asserts URL params and row classes, never a total. Also: two "spent this month" figures ~200px apart on the dashboard over different windows (`<= today` on one, not the other), same split inside Ask. | Opus 5 | medium | 60k | **[x] P1-27 DONE 2026-08-04 (#409, verify green, critic-cycled). P1-28 SPLIT OUT to C.26 — read that row before re-opening this one.** The audit's stated cause for P1-27 was already dead: #397 admits PENDING into `classifySpendClass` (module doc, `spend-class.ts:7-15`), so the $49.93 is gone. The parity claim survived through a mechanism the audit did not name — the panel summed the raw month query while the register applies the shared R1 reconciliation keep, so a confirmed provider migration double-counted every post-cutover purchase in the heading the reader clicks. `summarizeSpendClassCategories` now takes `keepsReconciled` as a REQUIRED parameter. Critic P1: the page prints one category twice (C.25 excludes a loan payment from By-category so that figure matches ITS link; the split keeps it so ITS link matches) — `spendClassLoanPaymentNote` now says so beside both. P1-28 was BUILT, critic-FAILED and REVERTED the same session — do not rebuild it from the audit line alone. |
| C.14 | **`/goals` renders the literal string "null".** `goals/page.tsx:145-147` branches on `=== 0` only, so the third state falls into the template: "Moves your FI date back ~null months". `:71` makes the mirror error with `?? 0`. Same slice: `goalFIImpact` floors a goal contribution at the whole surplus, reporting 7 months of FI delay where 29 is true. | Opus 5 | small | 30k | **[x] DONE 2026-08-08 (DECISIONS #432, verify green, shipped).** Both audit findings (#21/#22) fixed and locked: (1) the null state — `fiDelayMonths` null when the FI date sits past the 1200-month cap — gets the coach's beyond-horizon refusal (`COACH_COPY.goalFiBeyondHorizon`) on the real cards AND the worked example (the `?? 0` mirror); (2) the pledge is charged in FULL — the `floorAtZero` on the funding diversion is gone, so a pledge beyond the surplus reports its real FI delay (audit's 7 vs 29 re-executed with integer cents and locked: savings $1,000, pledge $4,100, goal $28,700 → delay 29, hand-verified); the C ≤ S path is byte-identical (floorAtZero is the identity there — every existing lock passed unchanged), only the C > S lock moved 24 → 36 with its hand-work restated. Gate + CI record in STATUS.md. |
| C.15 | **UX: the return affordance exists and returns to the wrong place.** `decodeRegisterReturn` is rooted at the `REGISTER_PATH` literal (`links.ts:28,543`), so a TRANSACTION destination is structurally inexpressible — `/rules` always lands on the register, and on an unfiltered register renders no return link at all. Add a sibling encoder rooted at a `/transactions/<id>` literal (same "path is a literal, only the query comes from the caller" construction, `the-affordance-existed-and-was-lossy`). Same slice: the detail view's own split-parent link is bare (F2), and four entry points (triage, dashboard recents, breakdown expanders) hand the reader a dump (F3). **Do not add a third back link.** | Opus 5 | medium | 60k | **[x] DONE 2026-08-08 (DECISIONS #433, verify green, shipped).** Sibling encoders root a transaction destination (`decodeTransactionReturn`, id admitted only through `TRANSACTION_ID_PATTERN` — both cuid and `txn-00001` shapes verified against `dev.db`, path-escape-free) and a named page (`PAGE_RETURNS` closed table + `decodeNamedPageReturn`) in the same "path is a literal" construction; the three encodings are mutually disjoint so decode order cannot launder. F2: the split-parent link now carries the CHILD's forwardable context (the reader's place survives the second hop — never the parent's id). F3: triage ×2, dashboard recents, and the breakdown expanders (host derived as the INVERSE of PAGE_RETURNS, page query carried) hand the detail page a named token. `forwardableBack` gates every second-hop forward (decode-first, else the Activity sentinel) and `withForwardedReturn` is the sibling encoder; the raw `?back=` is passed down as a server prop after the first gate caught the O.16 e2e shipping `_activity` through a pre-hydration click. No third back link. Unit (41 new) + e2e (3 new) locks; gate + CI record in STATUS.md. |
| C.16 | **UX: move Fixed/Discretionary into the action menu that already exists.** The popup menu the owner asked for is already on every row and the detail view with ten actions incl. rules; spend class is the one verb missing (`actions.ts:389-400`) and is instead an always-on `<select>` on all 50 rows that performs a one-gesture, CATEGORY-WIDE write disclosed only in a `title` tooltip — while the category picker one row above is deliberately two-step. Same slice: the "Neither" tooltip names 3 causes for a predicate with 7 (F6), a split container gets a live control an inch below copy saying it is in no total (F7), and the register cannot say whether a class is our guess or the reader's own setting though `/budgets` renders exactly that (F8). | Opus 5 | medium | 70k | **[x] DONE 2026-08-08 (DECISIONS #434, verify green, shipped).** The always-on register dial is deleted — the row renders an inert badge (`txn-spend-class`), and the class write lives in the one action menu as "Change spending class…" (pick step + bulk-scope step for N-from-payee, no-op rule on picking the current class; engine order matches the wire: demo fence FIRST, then out-of-scope). The detail page's For-your-Plan block stays read-only (e2e locks `select` count 0). F7's root cause fixed: `classifySpendClass` now receives `isSplitParent` and the register never loads containers. F8: `spendClassReaderSet` is the provenance marker — only reader-authored classes stamp the override, and "you set this" renders exactly when it is true. Demo fence is single-sourced (`SPEND_CLASS_BLOCKED_OUT_OF_SCOPE` / `DEMO_ENTRY_BLOCKED` imported by engine AND server action). Critic (Opus 5 xhigh): **PASS, zero P0/P1**; four P2s recorded for the record. Gate + CI record in STATUS.md. |
| C.18 | **Every remaining chart is inert — sweep them (owner, 2026-08-02: *"again, make all charts and summaries, expandable"*).** #389 did /coach's savings-rate chart (the one he named) and /reports' income-vs-spending chart already expanded since O.20. Still INERT, in the order a reader meets them: **net-worth trend** (`net-worth-card.tsx`, /dashboard + /accounts — an 18-month AreaChart; a point expands into the account balances that moved, NOT transactions, and the honest panel is a balance delta, not a sum of rows); **cash-flow forecast** (`forecast-view.tsx` — a day's balance is a running projection, so the rows behind a point are that day's scheduled items, and the panel must not claim they sum to the balance); **retirement outlook** (`retirement-outlook-card.tsx`, /investments — a projected year has NO rows at all and must refuse rather than invent a panel); **life energy** (`life-energy-card.tsx`); **cards breakdown** (`cards-breakdown.tsx`). Rule for each, from `a-repeated-request-is-about-the-gesture`: build the panel from the very array the surface summed, pass the RENDERED figure so `reconciles` can fail, and where a figure genuinely has no rows behind it, say so instead of manufacturing a panel. | Opus 5 | large | 90k | **[x] DONE 2026-08-09 (verify green, shipped).** The demo-fence wave closed the last visitor-personalization write family on the shared demo row (money dials, budget targets, the unknown-question ledger, the audit cron skip — server refusals + a fence note, unit-locked in `shared-demo-fences.test.ts`); both e2e specs that drove those writes as demo migrated to throwaway users. The false "precached by the service worker" /offline shell deleted (middleware + sw-register docblocks corrected); every app description leads with the mission (sign-in tagline, metadata, privacy-policy intro); the privacy policy names the one PII-free salted hash that outlives deletion; the coach creep-card title stopped being the clickable claim; the register caption names its pending-included basis; the cash-needed forecast link says "90-day recurring forecast"; /goals and /investments carry the same frozen-portfolio qualifier /coach prints; forecast.ts anchors via the shared `resolvePaymentAccount` (D1); the "Aim·plifi" brand drift fixed. Gate: local verify GREEN (tsc 0 / eslint 0 / 6,546 unit + 1 skipped / 396 files / build clean / e2e 319); CI chain on the code + docs records: 31332192499 (category-rename:110, pre-existing class, isolation-proven 30/30), 31332778256 (budget-targets:61 + register-return:119 + transactions:910, the documented 4-worker timing-flake class), 31333436087 (mobile-overflow:386 — the CI failure named /forecast @360 overflow, checked as a potential real regression and isolation-proven 7/7 on this exact tree), **31334105296 SUCCESS — gate GREEN**. Live deploy marker-proven (deliberately-wealthier sign-in, privacy hash, /offline 307). Full record in STATUS.md §C.18. |
| C.19 | **Fixed/Discretionary must be per TRANSACTION, with the category-wide setting demoted to an explicit rule (owner, 2026-08-02).** Verbatim: *"i should also be able to select for each 'business services' which are discretionary or fixed. in fact make each transaction independent unless user makes rule such as all business services are fixed"*. This REVERSES `DECISIONS #378`, which chose category-wide deliberately ("not a second per-txn flag that could disagree with Plan") — record the reversal, do not let it lapse. The affordance already looks per-row and is not: `spend-class-select.tsx` sits on every register row and writes `setCategoryFixed(categoryId)`, disclosing "applies to the whole category" only in a `title` tooltip (`the-affordance-existed-and-was-lossy`). Shape: additive nullable `Transaction.spendClassOverride`, resolution row → category rule → taxonomy suggestion. **The money half is the real work:** every Fixed FIGURE is currently derived at CATEGORY level (`fixed-category-amounts.ts`, `plan.ts:483-493`, `spend-class.ts:107`), so once rows inside one category can disagree, those sums must be re-derived from ROWS by resolved class or the plan and the register will print different Fixed totals. Overlaps C.4/C.5 and C.16 — read all three before starting; do NOT ship the column without moving the sums. **REFINED by the owner 2026-08-02 (2nd report, "I've asked you to fix this before"), and the refinement changes the DEFAULT, not just the override:** verbatim — *"Basically most things that are fixed are recurring. Mortgage, credit card membership dues, car insurance, etc… some things in business services are absolutely fixed. We can't live without internet. Etc…. A good start for fixed would be recurring."* So the suggested class must derive from **RECURRENCE**, not from `CategoryMeta.discretionary`: resolution becomes row override → explicit category rule → **is this merchant a detected/declared recurring series** → taxonomy fallback. This is why the owner's example splits inside one category — xAI recurs monthly, DeepSeek is metered ad-hoc usage, and `categories.ts:167` cannot see the difference because `classifySpendClass` reads only `t.categoryId`. The machinery already exists and is already user-correctable per merchant: `detectRecurring` → `RecurringSeriesResult.merchantCanonical`/`cadence`/`isSubscription`, plus `RecurringOverride` (BILL \| NOT_BILL, `@@unique([userId, merchantCanonical])`). **Sequencing finding (verified, do not re-derive):** the hopeful reading — that recurrence-as-basis dissolves C.4 by collapsing the two halves — is FALSE. `plan.ts:476-494` unions a categoryId-keyed rollup with a merchant-keyed series set, deduping via `rollupCategoryIds.has(id)`; once recurrence DEFINES fixed, every series falls inside the fixed set and that null-categoryId leak (line 486 guard fails → line 492 adds) becomes load-bearing instead of incidental. C.4 gets WORSE, not better. **Therefore C.0 → C.4 → C.5 must land before this row**, or the owner's own rule ships the $12,400-for-a-$6,200-mortgage defect into the default path. **THIRD owner message, same session — Fixed has a THIRD source the app has no concept for:** verbatim — *"other items that aren't explicitly listed as expenditures or fixed are fixed based upon money being reserved every month for home repair, those are fixed. The way I personally categorize yearly membership dues is I divide by 12 and put that cash aside."* So the owner's Fixed = **committed monthly outflow**, from three sources: (1) a recurring bill that leaves this month; (2) a long-cadence bill SMOOTHED to monthly — the app already does exactly this via `monthlyRateCents` + `LONG_CADENCE_WORDS`, so the owner's ÷12 method and the engine's already agree; (3) **a monthly RESERVE against a known future expense (home repair) with no bill and no series behind it** — a sinking fund. Nothing models (3) today. **This collides head-on with a decision the owner set ONE DAY EARLIER** (`plan.ts:446`, "Settlement / savings — never a Plan Fixed cost class (owner 2026-08-01)"; `FIXED_PATTERN_EXCLUDE_CATEGORY_IDS` drops `investment`). Resolve it as a DISTINCTION, not a reversal — a sinking fund pre-funds a known EXPENSE (deferred bill ⇒ Fixed); savings/investing builds wealth (⇒ stays out of Fixed, keeps feeding the savings dial). Likely carrier: a third `Goal.kind` (`null` = savings, `'debt_free'`, + `'reserve'`), whose `monthlyContributionCents` routes to Fixed instead of the dial. **DOUBLE-COUNT HAZARD, verified, must be handled in the same slice:** `plannedSavingsCents` is documented at `plan.ts:332-337` as *"the LARGER of goal contributions and the savings-% target … A floor, never a sum"* — so a reserve goal moved into Fixed while still inside that max is counted TWICE (once as Fixed, once inside the savings floor). Reserve goals must leave the max as they enter Fixed. | Fable 5 | large | 150k | **[x] DONE 2026-08-09 — both residuals closed: (1) via C.23's reserve half (#412); (2) by live measurement, this session — see the close note at the end of this cell. (Re-scoped 2026-08-05 #411: most of the row above was already live — READ note follows.)** DECISIONS #397 (2026-08-03, one day AFTER this row was written) shipped the per-transaction ladder it asks for: `Transaction.spendClassOverride` exists, `classifySpendClass` resolves row override → recurring-bill merchant → taxonomy (recurrence-first, exactly the owner's refinement), `CategorizationRule.setSpendClass` stamps the explicit category rule, and `resolveFixedCategoryAmounts` already pre-filters to fixed-CLASSIFIED rows so a mixed category contributes only its fixed share. Verified first-hand this session, not taken from a subagent. **What #411 then closed:** the Fixed figure's composition was unsayable — the union contributed a bare number, so the mortgage was in the total and in no list (H.3, now DONE). **What GENUINELY REMAINS in this row:** (1) the reserve/sinking-fund third source — the owner's *"money being reserved every month for home repair"* and yearly dues ÷ 12 — which has no model at all and is C.23/H.4's `Goal.kind = 'reserve'`, carrying the `plannedSavingsCents` double-count hazard (`plan.ts:332-337`, a floor/max, so a reserve must LEAVE the max as it enters Fixed); (2) a possible P1 spotted while reading and NOT fixed here — `resolveFixedCategoryAmounts` lets a whole-category BUDGET TARGET replace the fixed-classified typical (`fixed-category-amounts.ts:362,367`), so a mixed category the reader priced enters Fixed at its ENTIRE allowance including its discretionary share; unmeasured on live data, do C.0-style measurement before assuming direction. **CLOSED BY MEASUREMENT 2026-08-09 (`scripts/audit-probes/c19-fixed-budget-replacement.mts`, run against the live corpus, read-only): the ENTIRE database holds ZERO Budget rows — the real user 0, the demo user 0, no seed rows — so `budgetByCategory` is always empty, `budgetCents` is always null, and the replacement at `fixed-category-amounts.ts:367` cannot fire anywhere; measured delta $0.00. No direction to assume, no fix warranted; the mechanism stays documented as latent (the only writer, `setBudget`, is demo-fenced — the mechanism arms only if the owner sets a budget target on a mixed category, and the probe is the re-measurement instrument).** |
| C.20 | **The pace credit and the month total do not share a basis — they are only stopped from crossing.** `spentSoFarCents` comes from `spendingByCategory`, which nets refunds by CATEGORY and drops a net-refunded category to zero; `creditedCents` sums per MERCHANT off raw rows. #391 detects the crossing and takes no credit (erring toward over-projecting, the safe direction here) but the two bases are still different questions. Real fix: attribute the bill credit through the same netting the month total uses, so the subtraction is exact rather than merely safe. Locked today by `tests/unit/trends-pace-bills.test.ts` → "a net-refunded category cannot delete unrelated spending from the rate", which pins the CURRENT behaviour — closing this means changing a test that explains itself. | Fable 5 | medium | 50k | **[ ] OPEN — from the C.2 critic cycle (#391), P1-2 residual** |
| C.21 | **The pace assumption's branch C conflates two different zeros.** It fires when NO bill was admitted, and says nothing about bills at all — so "you have no bills" and "we matched none of the bills you do have" render identically, which is the exact shape `a-zero-is-a-claim-and-must-name-which-zero` was written for. The reader most affected is the one whose bills are ALL refused (an aggregate payee, a hand-authored label), i.e. the reader whose projection is least complete. Needs a refused-count fact out of `billsThisMonth` (the `expected` entries that failed `counted`/`aggregate`), then a branch that names the count and what it means — not a copy tweak, because the engine currently cannot tell the two zeros apart. | Opus 5 | small | 40k | **[ ] OPEN — from the C.2 critic cycle (#391)** |
| C.17 | **P2 sweep.** Pace divides in float before `roundHalfAwayFromZero` (140 one-cent violations of the stated rule — fix is `(spentSoFar * dim) / daysElapsed`); `baselineLabel` prints a contiguous range for a gapped set and contradicts the balance-move sentence 40px away; a mover row prints a net-refund-clamped $0.00 as a fact and sorts on it; the runway card prints "-2.3 months of expenses in cash"; `toFIInputs` still pairs a nominal return with a present-value target (no caller — a trap, not a live claim); /forecast's frozen note uses `payment.name` where the figure uses `accountLabel`. Full list in the audit doc's P2 section. | Sonnet | medium | 50k | **[x] DONE 2026-08-09 (DECISIONS #435, verify green, shipped).** The pace divides by real fractional elapsed days (integer cents preserved, display days integer); the cash-needed headline pairs the total with the FIRST effective due while `byDate` stays the horizon; gapped baseline labels say "N months through <first>"; a mover's netted-zero abstains and sorts netted; the discretionary denominator counts distinct data months; `toFIInputs` deleted (mixed nominal+pv base, no caller); negative runway is named on every coach surface (the today-feed negative branch was dead — the nudge clamps to null — and removed); the frozen forecast note uses `accountLabel`; the streak floor is exclusive of zero with "under 0.1%" banding and full-history personal best; merchant lens and calendar strings name their bases/scope; the minimum-interest note names the covered set (incl. a paid-in-full cycle); the wealth-target header correction shipped. Hostile critic (Opus 5) FAIL → one P1 + four P2s, all closed: the recap now takes the SAME full-history `SavingsStreakResult` as the card (one helper, one basis — no slice re-derivation), `isPersonalBest` gained a positivity gate (no "-0.0%" least-bad month), the /trends clock comment states the truth, and the two copy fixes above. Gate record (13 attempts, 2 real defects found — hidden lint from the `toFIInputs` deletion, and the critic's P1) in STATUS.md; CI + live checks recorded there. |
| C.22 | **The radar's committed-merchant detection reads a re-linked payment account's short tail** (found while fixing the income defect, 2026-08-02; measured, not assumed). `radar.ts:145` narrows POSTED rows with `t.accountId === payment.id` to feed `detectRecurring`, whose output is the COMMITTED set excluded from discretionary burn. After the owner's 2026-07-21 re-link that scope holds **183** rows where the whole account's history is **402**. **The naive remap is not the fix and the probe proved it:** re-keying predecessor rows onto the successor takes detection from **9 series to 4**, because one merchant arriving under two feeds' descriptors loses the gap regularity each feed had alone — so a reflex application of the income fix would move five merchants' spend INTO burn and overstate it. The real fix is at the descriptor/merchant level (merge the two feeds' canonicals for one payee before detection), and it needs a decision about which side's descriptor wins. Also structural: `computeRadarFromSnapshot` is synchronous with no `userId`, so any remap must ride on the snapshot rather than a fresh query. Re-run `scripts/audit-probes/income-replay.mts` (its C.22 block) after any change. | Fable 5 | medium | 60k | **[ ] OPEN — sibling of the income fix; direction NOT yet established** |
| C.23 | **A Fixed-costs settings section, with RESERVES as a first-class kind (owner-requested 2026-08-02: *"there should be a settings section for this"*).** The owner illustrated the idea with an old personal budget workbook and was explicit that it is **an illustration of the CONCEPT, not a specification** — *"just an example from 4-5 years ago… our life is vastly different now and our priorities are different. It's to give you an idea how to properly do this."* **Do not rebuild that sheet.** Its line items, amounts, merchants and totals are stale and are deliberately NOT recorded here; only the model carries forward. **The model:** committed monthly money divides three ways — (1) **bills paid as they fall due** (what the app calls Fixed today); (2) **reserves — money set aside monthly so the cash is there when the item comes due**, in the owner's words *"cash I set aside every month for those items, so when that time comes, cash is accounted for"*; (3) **savings / wealth**, which stays out of Fixed. The 2026-08-01 rule at `plan.ts:446` is about that THIRD bucket and stands — C.19's "distinction, not reversal" rests on what the owner SAID a reserve is (quoted above), NOT on anything read out of the workbook. He subsequently warned that its numbers AND its formulas are unreliable placeholders — *"don't even rely on the numbers… last time I opened it I was typing in examples"* — so **no figure, formula, total or layout in that file is evidence for anything, and none is cited here.** **Reserves cover two cases and both must work:** a bill on a longer-than-monthly cadence — the app already smooths these, `monthlyRateCents` divides annual by 12 and quarterly by 3, the same method the owner applies by hand — and **a reserve with no bill behind it at all** (home repair, car repair, gifts), which is discoverable from nothing and so must be declarable. **Shape:** a settings page that IS the app's Fixed basis, not a second one (C.19's warning); one row per line = name, monthly amount, kind (`bill` | `reserve`); a reserve line stores true cost + cadence and the APP does the division, never the reader; surface one "move this much to reserves this month" figure and let the reader name the account that holds it. **Propose lines from `detectRecurring` / `RecurringSeries`** (cadence + typical amount are already available) so the reader confirms and edits rather than typing a list, then adds the reserve lines no transaction implies. Likely carrier for the no-bill case: a third `Goal.kind = 'reserve'` per C.19, whose double-count hazard (`plannedSavingsCents` is a floor/max, `plan.ts:332-337`) applies unchanged. **Gated behind C.0 → C.4 → C.5 → C.19** — these amounts land in the Fixed total. Money-visible + new user-entered data ⇒ hostile critic. | Fable 5 | large | 130k | **[~] BUILT + CRITIC CYCLE 1 DONE 2026-08-05 (#412) — the RESERVE half of C.23 is shipped; the guided settings half is not.** What landed: a reserve is a `Goal` row with `kind='reserve'` and a new additive nullable `Goal.cadence`, storing the TRUE COST once per rhythm — the app divides (`resolveReserves` -> `monthlyRateCents`), the reader never does, which is exactly the owner's ÷12 by hand. It is FIXED, not savings, as a distinction rather than a reversal: a reserve pre-funds a known expense, savings builds a balance, and `plan.ts:446` stands untouched. Reserves fold INTO `suggestedFixedCents` and `fixedExpensesCents` (twelve call sites already render that figure; a fourth term would have understated Fixed on eleven of them), appear as their own `kind: 'reserve'` lines in the C.19/H.3 Fixed list, and are declared on /spending-plan beside the list they move. New `fixedBasis = 'reserves-only'`. **H.4 criterion (2) — the double-count — is closed twice:** a reserve stores a null contribution AND the loader filters `kind !== 'reserve'` explicitly; the test locks the second via a row carrying both, because the first is only a data convention. An unrecognised stored cadence is REFUSED, never passed to `monthlyRateCents`'s identity default (a `'YEARLY'` would have entered the plan at 12x), and refusals are named on the page with their own remove control. **Three fresh-context critics, all FAIL — 1 P0, 5 P1, 4 P2, all executed and all fixed + 8 ledger entries.** The P0 broke a feature this slice does not touch: `kind: { not: 'reserve' }` reads as "everything else" and is not — `kind <> 'reserve'` is NULL for a `kind IS NULL` row, so every ordinary savings goal vanished from /goals and from the coach blueprint. The sharpest P1 was found before the critics: the override form printed the reserve-INCLUSIVE suggestion beside an input that replaces only the pattern half, so locking the number the app displayed counted the reserve twice (new lesson `a-control-that-replaces-a-half-must-be-told-which-half`). Gate: `bash scripts/verify.sh` GREEN — tsc 0 / eslint 0 / **6036 unit / 365 files** / build clean; e2e `fixed-composition.spec.ts` 3/3 (H.4's own test drives the real form); fail-old proven by mutation in four places. Schema: additive nullable `Goal.cadence` only. **STILL OPEN in C.23:** the settings section that PROPOSES lines from `detectRecurring`/`RecurringSeries` for the reader to confirm and edit rather than type, and the single "move this much to reserves this month" figure with a named holding account. The reserve MODEL is done; the guided setup is not. **[x] GUIDED HALF DONE 2026-08-08 (DECISIONS #431, verify green, critic-cycled, shipped) — the STILL OPEN text above is what shipped:** /settings' Fixed-costs section PROPOSES each counted recurring series with its union verdict (in basis / covered / discretionary / budget-priced) and the ONE lever — "turn this into a monthly reserve" — on the divided cadences (QUARTERLY/SEMIANNUAL/ANNUAL, never MONTHLY), storing true cost + cadence so the app divides and the swap is EXACT (in-basis −row +reserve-at-same-rate = the figure does not move a cent; out-of-basis +rate exactly). Convert writes reserve + NOT_BILL override as a PAIR in one serializable transaction; undo is the whole pair, both halves guarded (hasLinkedReserve shared by set/clear). "Move this much to reserves this month" is the plan's own reduce; the holding account is a NAME (`User.reserveHoldingAccountId`, PAYMENT_ACCOUNT_TYPES, demo-fenced), never a transfer. One-authority: `proposeFixedSetup` re-runs the SAME builders the plan sums, and the inBasis oracle switches with the plan's own `fixedBasis` (detected-series → `recurringPlanExpenseRows`). Critic (fresh-context Opus): 2 P1 + 1 P2 + 3 P3, both P1s + the P2 executed and locked, P3s recorded. Gate: verify GREEN — tsc 0 / eslint 0 / **6,437 unit + 1 skipped / 392 files** / build clean; e2e `fixed-setup.spec.ts` 5/5. Schema: additive nullable `User.reserveHoldingAccountId` + `Goal.merchantCanonical` (convert pair link). |
| C.26 | **One window for "spent this month" — and the register link must follow the figure (audit P1-28; BUILT AND REVERTED 2026-08-04, read this whole row before restarting).** The defect is real and confirmed: `computePace` filters rows to `<= today` (`trends.ts:571`) and `spendingByCategory` does not, so `TopSpendingCard` ("this month") and the Trends card ("spent this month") sit one card apart on the dashboard over two windows; nothing rejects a future date on manual entry or CSV import (`manual.ts:73`). Ask carries the same split: `merchantSpend` has applied the clamp since O.7 **with the argument already written out** (`answer.ts:849-855`) while `spend_total` / `spend_by_category` / `top_categories` do not. **The obvious fix — a `spentAsOf` clamp on the array in `getReports` + the three Ask intents — was implemented, verify-green, and then killed by a fresh-context critic with 5 P1s. All five were EXECUTED, not argued; they are the spec for the real slice:** **(1)** the clamped /reports figure links to an UNCLAMPED register — `categoryMonthRegisterHref` derives `to` from `monthWindow(month)` (last day of the month) and `query.ts` applies no `<= today` — measured $120.00 clicked, landing on $520.00 of rows. That breaks the O.5/O.6 link invariant, which this repo treats as more serious than the divergence being fixed, so **the link window must come from the same author as the figure's window.** **(2)** /budgets stays unclamped (it is an allowance tracker; omitting a row loosens a limit) and links to the SAME href, so /budgets would agree with the register and /reports would not — two pages, two numbers, one category. Either clamp /budgets too (money-visible: it moves "left to spend" in the generous direction) or accept and DOCUMENT the split; do not leave it undecided. **(3)** `monthlyFlows` in `getReports` takes the clamped array, so INCOME clamps on /reports (measured 0 vs 50000) while Ask's `income` intent and `traceIncome` stay unclamped — the same defect one axis over. **(4)** the shared Glass-Box basis sentence (`category-breakdown.ts:160-162`) is an exhaustive-sounding enumeration of what is left out, rendered by BOTH /reports and /budgets — it cannot state the clamp while one of its two surfaces does not apply it. **(5)** nothing locked the two production surfaces: mutation-proven — removing the clamp from `server/reports.ts`, and from all three `server/assistant.ts` intents, left **59 and 45 tests green** respectively, because the parity test re-derived the call-site expression instead of calling `getReports`/`buildAnswer`, and the two tests that DO call `getReports` run against a demo seed with no future-dated row. A real lock needs a seeded future row through the server loaders. **Also P2:** the helper had no runtime guard on `asOf`, and its failure direction on a malformed value is "you spent $0.00". | Fable 5 | large | 110k | **[x] DONE 2026-08-05 (#410, verify green, critic-cycled). All five reverted-attempt findings above were treated as the spec and all five are closed:** (1) the link window is no longer a second author — `SpendWindow` travels to `categoryWindowRegisterHref`, and after critic cycle 1 the /reports href is BUILT BY `getReports` so the view names no window at all; (2) /budgets keeps the whole month, decided and documented (an allowance a later-dated charge already consumed; clamping would loosen the limit) — each page equals its OWN register; (3) income clamps with spending on both /reports and Ask; (4) the shared basis sentence was not stretched — the clamp got its own amount-gated sentence in both panel families plus a page-level one; (5) real locks in `tests/unit/spend-window-parity.test.ts` through `getReports` and `askAssistant`, mutation-proven in both cycles. **Critic cycle 1 FAILed with 6 P1s — all executed, all fixed:** the view could still reintroduce the $120→$520 defect with the suite green (hrefs moved to the loader), the new disclosure was asserted nowhere (basis composition moved into the engine), the chart panel blamed the reader's refunds for money the clamp removed and could print "No posted spending" over $400 of posted spending (`buildMonthFlowBreakdowns` takes `asOf` itself and carries `notCountedYetCents`; `windowLabelSoFar` narrows the label every sentence interpolates), a category the clamp emptied disclosed nothing (page-level total), and Ask's basis omitted the newest exclusion. |

## Wave H — Full transaction history + Fixed must show mortgage and reserves (owner request 2026-08-04, LIVE)

Owner, verbatim: *"I want data pulled as long back as possible...Ideally 3-4 years of
transactions. Also where is mortgage? Fixed expense list must include mortgage and various
savings for longer term expenditures (yearly membership dues, home repair, etc...)."*

**Facts verified this session — read before starting any row:**

- **History:** `PLAID_DAYS_REQUESTED = 730` (`plaid.ts:187`, applied at `:279`) is **Plaid's
  documented API maximum** — Plaid cannot serve more than 2 years, so "3-4 years" cannot come
  from Plaid alone. The deep-history backfill for already-linked items shipped 2026-08-04
  (commit `18b6ad6`, `PROGRESS.md:32-66`) but has **no row in TASKS.md or STATUS.md**, and its
  effect on the owner's live items is **UNVERIFIED** (never claim it worked without querying).
  SimpleFIN already pulls 1095 days (`simplefin.ts:56`). Years 3-4 are reachable only via the
  existing CSV importer (`engine/transactions/csv-import.ts`, `/transactions/import`).
- **Mortgage:** modeled end-to-end (`MORTGAGE` type `plaid-map.ts:25`, liabilities sweep
  `plaid.ts:1875`, category `rent` = "Rent & Mortgage" `categories.ts:70`), and C.24/C.25
  settled its FLOW classification — but the **Fixed list** is still detected-series/category
  derived, which is exactly the defect C.19 fixes. This message is the owner's **FOURTH**
  report of it, and it adds a hard acceptance criterion (H.3). **C.19's gate C.0 → C.4 → C.5
  is now CLEARED** (all three archived done 2026-08-04): C.19 is unblocked and is the next
  LIVE slice.
- **Reserves:** "various savings for longer term expenditures (yearly membership dues, home
  repair)" is C.23's `reserve` kind, restated — including the owner's ÷12 method the engine
  already mirrors via `monthlyRateCents`. No scope change; C.23 stays gated behind C.19 only.

**SAME-DAY UPDATE (owner: *"why aren't they showing, i see a max date of march this year"*) —
diagnosis verified in code this session; three mechanisms, ranked:**

1. **SimpleFIN is the probable live cause.** The 1095-day window applies ONLY to a
   connection's first-ever pull or an account first seen mid-sync (`simplefin.ts:433-439`);
   an EXISTING connection always syncs from `lastSyncedAt − 5d`, and the `fullLookbackDays`
   escape hatch has **zero callers** (grepped `src/` this session). A connection first pulled
   under the old 90-day default keeps its original floor forever — ~90 days before first
   connect lands at March. **No backfill for existing SimpleFIN connections exists** → H.5.
2. **The Plaid backfill has not had an automatic chance to run.** It fires on an item's sync
   success path; the only automatic sync is the cron at 11:00 UTC (`vercel.json`), and the
   deploy landed 15:37 EDT 2026-08-04 — AFTER that day's cron. First chance: a manual Sync,
   or the next morning's cron. H.1's live check decides whether it then actually ran.
3. **Even when it runs, 730d on a pre-existing Plaid item is not guaranteed:** Plaid only
   ingested ~90d at init for items linked before `days_requested=730` (`plaid.ts:263-267`'s
   own doc citation), and `/transactions/get` returns what Plaid holds. The audit rows
   (`plaid.item.history-backfill*`) record `added` — if ≈0, the only full route is a fresh
   Link → H.6.

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| H.5 | **SimpleFIN one-time deep-history backfill for EXISTING connections (the probable live "March" cause).** Mirror the Plaid shape from `18b6ad6`: additive nullable `SimpleFinConnection.historyBackfilledAt`; on the connection's sync success path, if unset, one forced pull via the existing `opts.fullLookbackDays = SIMPLEFIN_INITIAL_LOOKBACK_DAYS` parameter (`simplefin.ts:385` — it exists and has no callers today) through the same ingest; audit-logged, add-only, a failure never reads as a sync failure (the `plaid.ts:1580-1603` stance). Institution caps what actually returns — say so on the surface, don't promise 3 years. **Verify the dedupe holds at 1095d scale before shipping**: the incremental path assumes a 5-day overlap; a full re-pull overlaps EVERYTHING already stored, and duplicate rows corrupt every total ⇒ hostile critic on the add-only boundary. | Opus 5 | medium | 60k | **[x] DONE 2026-08-05 (#413, critic-cycled) — premise CONFIRMED before building.** `fullLookbackDays` existed with zero callers; the 1095d window reaches only a first-ever pull or an account first seen mid-sync, so an existing connection is pinned to its original floor forever. Built as an ADD-ONLY path rather than by calling that parameter: routing a 1095d pull through the live ingest would answer every already-stored row with `guardedVerdictRefresh` and silently re-file three years of history against today's rules. A pure planner (`simplefin-history-backfill.ts`) emits only genuinely-new rows; the writer only ever `create`s. **Two fresh-context critics returned FAIL (1 P0 + 11 P1); all executed.** P0: the backfill wrote into a SUPERSEDED PREDECESSOR, whose full-history `span.first` is a reconciliation claim edge — three added years would have DELETED three years of the successor's corrected rows from every figure without updating one row. P1s: `refreshTransferFlags` (dropped — it rewrites settled rows); an empty plan marking done on an untrustworthy response (now `inconclusive`, and `data.errors` is read at last); the connect-time flag (removed — it re-created the reported defect and made it unreachable; a reconnect now CLEARS it); an unbounded LLM fan-out over the whole plan before any commit (now chunked 250 / capped 2000 per run, oldest-first, `markDone` only when the plan is consumed); and `rateLimitDurable` on `syncSimplefinNow`. **Dedupe verified AT SCALE** per this row's gate (`tests/unit/simplefin-history-backfill-scale.test.ts`, `H5_SCALE_PROBE=1`): 3000 rows over ~1090 days converge in 2 capped runs at 1.55ms/row; a forced full re-plan over an entirely-stored history adds 0, duplicates 0, drifts 0 columns. Surface: none built — `transaction-filters.tsx` already prints "History available from <date>" from the OLDEST ACTUAL ROW, so it states what came back and moves on its own (verified present, not rebuilt). **Cycle 4 = the HARD CAP: 3 P1 left OPEN and written to `docs/STATUS.md` per CLAUDE.md §6 — (a) three un-supersede paths that don't re-arm the backfill, (b) reconnect-after-disconnect can lose a pending row older than the 5-day overlap, (c) the PLAID backfill already on `main` has the same superseded-predecessor defect this slice rated P0 (its own slice). Cycle 4's P0 'the suite does not pass' did NOT reproduce — 3 consecutive clean runs + verify GREEN; its control ran in a worktree, which gets a private test DB.** |
| H.1 | **Verify + surface how far history actually goes.** (a) Against the live DB: for each `PlaidItem`, confirm `historyBackfilledAt` is set and `min(Transaction.date)` sits near `today − 730d` (or the account's opening, whichever is later); same check for SimpleFIN connections at 1095d. Paste real query output — per rule 1, no green without it. If any item never backfilled, run/fix the trigger path (`plaid.ts:1585-1603`). (b) Surface it: an "earliest data: <date>" line per connection (accounts or settings) so the owner can SEE the depth instead of asking. (c) Ledger hygiene: give `18b6ad6` its missing STATUS row and record the 730-day Plaid ceiling in DECISIONS so "pull more history" isn't re-litigated. | Opus 5 | medium | 50k | **[~] (a) MEASURED 2026-08-05 (#414 probe, real output in PROGRESS):** all 12 PlaidItems `historyBackfilledAt=2026-08-04`, every audit row `added: 0` (all `alreadyExists`) — the backfill ran and Plaid held nothing older; live depth plaid `[2026-04-24..2026-08-05]` n=1381, simplefin `[2026-03-25..2026-07-21]` n=1684. **The owner's SimpleFinConnection row NO LONGER EXISTS** (disconnected; history kept as superseded predecessors), so the SimpleFIN 1095d check runs only on a reconnect. **(b) DONE 2026-08-08 (DECISIONS #429, critic-cycled) + (c) NEEDED NOTHING** — #421 already records Plaid's 730-day ceiling in DECISIONS beside `plaid.ts:189`, and `18b6ad6` got its STATUS rows via #413/#414. (b) re-measured live first (the corpus MOVED: 58 accounts / 4,493 rows / 3,278 owned / 27 links, and the register floor is now **2024-08-11** — a Chase item with 1,395 rows, `backfill=2026-08-07`, i.e. the deepen route worked). That is also why a global line is the wrong answer: twelve of thirteen connections still start July 2026, so one date set by the deepest account reads as a claim about ALL history. Built engine-first (`connectionHistoryDepth`, pure) and read through `getReconciliationTxnKeep` rather than a raw `groupBy _min` — **seven connections carry a raw-vs-owned delta of 84–91 days**, so the naive aggregate would have put /accounts three months adrift of the register on one screenload (the H.8 defect one surface on). Three states because two would lie: the probe's Q3 hit is live (an Amex item **holds 7 rows and owns 0**), so `counted-elsewhere` gets its own sentence, never a date and never a false "no transactions yet". No remedy tail (K.2b's critic executed that false-tail defect one surface over) and no new date format (the register's own `formatISODate(_, 'long')`). A cross-provider `distinct`+`orderBy` ordering assumption was removed before shipping in favour of an explicit MIN. **CRITIC CYCLE 1 — two fresh critics (data integrity; copy/UX), BOTH FAIL: 6 P1 + 5 P2 + 5 P3, every finding executed, all six P1s fixed and locked.** Sharpest: (a) the slice's own headline claim was false — the depth read rows the REGISTER refuses to list (mortgage/loan/investment/non-USD/split-parent), executed as a card reading "History goes back to Mon, May 18, 2026" while /transactions returned zero rows and did not offer that account in its dropdown; the basis is now `registerRowWhere` itself, shared not restated. (b) ALL FOUR connections rendering "No transactions yet." were 100% never-transactional accounts (U.S. Bank ×2 LOAN, Vanguard ×4 INVESTMENT, Schwab ×2 IRA, Truist ×1 MORTGAGE — 9 of 9), every one synced cleanly that morning: 31% of the owner's connections told to wait for what is never coming. Five states now, each because a smaller set forced a lie; the `counted-elsewhere` sentence was rewritten after both critics (R1 is a calendar-WINDOW rule so "counted on the other account" is false when the row is counted nowhere; three executed shapes have TWO claimants; and "combined" is a word the visible page never defines since O.19 collapsed the disclosure that teaches it). Copy moved to a pure module — while it lived in the `.tsx`, importing it under vitest died on `next/server`, so the whole ~6,000-test gate was blind to a wrong sentence. SimpleFIN gained the same line (43% of the owner's accounts, and DEEPER than seven of the eight Plaid connections that print a date). **Live re-measure with the shipped rule (`h1b-depth-states.mts`): 9 reaches / 4 balances-only / 1 counted-elsewhere, ZERO false "No transactions yet."** SimpleFIN renders in the ORPHANED branch too — the owner's connection row is deleted (#421), so a line wired only into the connected branch would answer for everyone except him (regression-locked). Gate: verify GREEN — tsc 0 / eslint 0 / 6,381 unit + 1 skipped / 388 files / build clean; connection e2e 18/18; full e2e 304 passed / 1 pre-existing documented flake (`budget-targets.spec.ts:20`, green on re-run); four sabotages each RED on its own locks; no schema change. **Remaining in Wave H: H.2 (guided CSV backfill).** |
| H.2 | **Years 3-4 of history: guided CSV backfill.** Plaid cannot serve >730d (H.1's recorded ceiling), so the only route to 3-4 years is bank-exported CSV through the existing importer. Make it a first-class path: per-institution export instructions on `/transactions/import`; dedupe against provider-synced rows across the overlap window — **verify the importer's current dedupe behavior first, don't assume it exists**; a post-import "history now reaches <date>" confirmation reusing H.1(b)'s surface. Imported rows must flow through the same categorization + recurring detection as synced rows (deeper history should IMPROVE cadence detection for annual/semiannual series feeding Fixed). Money-adjacent (double-imported rows corrupt every total) ⇒ hostile critic on the dedupe. | Opus 5 | medium | 70k | **[x] DONE 2026-08-08 (#430, critic-cycled)** — the importer is a first-class backfill path: web-verified per-institution export guides on /transactions/import (7 cards + generic), overlap dedupe as a multiset difference on (date, signed amount) with the R1-filtered match set, post-import "history now reaches" depth confirmation on the register's own basis, and imported rows through the same categorization + recurring pipeline. Critic (Opus 5): 1 P1 + 6 P2 — all executed: file-internal repeats surfaced as a repeatedRows warning (never silent), picker + action fenced to SPENDING_ACCOUNT_TYPES/USD, two-read serializableTx check-then-act, amber contrast, docblock honesty; P2-1 (sync-vs-import mirror dedupe) and P2-6 (depth floor query shape) tracked in STATUS.md. Verify GREEN 6,411 unit / build clean; e2e 27/27 on transactions.spec.ts at 4 workers incl. the new warning spec. |
| H.3 | **Acceptance criterion on C.19: the mortgage row is visibly IN the Fixed list.** Not a new slice — an added e2e lock C.19 must ship with: on owner-shaped data (a ~$6,217.07 payment against a `MORTGAGE`-linked account, transfer-paired in SOME months per the C.24/C.25 history), the Fixed list renders a mortgage line at its full monthly amount, exactly once, in EVERY month. A row that C.25's four gates exclude from flow sums may NOT thereby vanish from Fixed (`classifySpendClass` dropping transfer rows is the known leak — C.25's status cell names it). Under C.19's recurrence-first resolution the mortgage qualifies twice over (detected series + `MORTGAGE` liability with `minimumPaymentCents`); if it still fails to render, that is a P0, not a tuning issue. | Fable 5 | — (inside C.19's 150k) | — | **[x] DONE 2026-08-05 (#411, critic-cycled) — but NOT as an e2e inside C.19; as its own slice, because C.19's premise was stale.** #397 had already shipped the per-row ladder C.19 asked for. The real defect was that the mortgage was in the Fixed FIGURE (C.24) and in no LIST: the union returned a bare number and C.24's exactness invariant had removed the merchant's rows from the category rollup, the only half producing lines — `loan-payment-fixed-union.test.ts:206` asserts that empty rollup as CORRECT, so nothing in the suite could see it. Fixed structurally: the union emits the rows it sums, `recurringOutsideFixedCategoryCents`/`recurringPlanExpenseCents` are implemented in terms of them, `buildFixedList` certifies or refuses, and /spending-plan (which itemized nothing) renders it. `tests/e2e/fixed-composition.spec.ts` is H.3's lock on owner-shaped seed data: the $6,217.07 line renders exactly once, chipped `loan payment` (a label only a union row can produce), in a month where the transfer pair fired AND one where it did not; a second test adds the figures read OFF THE DOM and asserts they equal the printed total. No figure moves; no schema change. Copy critic FAIL → 4 P1 + 2 P2 fixed. |
| H.4 | **Reserves land in Fixed = C.23, re-confirmed 2026-08-04.** Owner restated the model verbatim (yearly dues ÷ 12 set aside monthly; home-repair reserve with no bill behind it). No scope change to C.23 — queue it immediately after C.19 lands. Add two e2e locks to C.23's definition of done: (1) a declared reserve line (true cost + ANNUAL cadence) appears in the Fixed list at cost/12 alongside the mortgage and detected bills; (2) a reserve contribution LEAVES the `plannedSavingsCents` max as it enters Fixed (the double-count hazard, `plan.ts:332-337`) — assert the plan identity still sums to income to the cent. | Fable 5 | — (inside C.23's 130k) | — | **[x] DONE 2026-08-05 (#412, critic-cycled) — both e2e locks executed.** (1) A declared ANNUAL reserve renders in the Fixed list at cost/12 beside the mortgage, with the rendered lines still summing to the rendered total (`tests/e2e/fixed-composition.spec.ts`, the H.4 test, driven through the real form). (2) A reserve LEAVES the `plannedSavingsCents` max as it enters Fixed, and the plan identity `income − fixed − savings = leftToSpend` holds to the cent on all five bases (`tests/unit/reserves-in-fixed.test.ts`, through the real server loader). See C.23's row for what remains there. |
| H.7 | **The transfer sweep pair-flips `isTransfer` on SETTLED rows — decide when a pair-only detection may rewrite a settled verdict (found by the #414 critic, EXECUTED).** `planTransferUpdates`'s flag branch (`transfers.ts:106`) has no settled-row guard: any new row supplying a ±3-day same-\|amount\| cross-account counterpart flips a settled row's `isTransfer`, `needsReview: false`, no audit — critic repro: a settled +$1,000 income row left every income total when a backfilled two-year-old coincidental outflow arrived. This is a STANDING defect of the shared sweep on every sync source, not a backfill property: **H.5's "dropped the refresh on the backfill" was only a one-sync deferral** (the next cron sweep does the identical rewrite), and both deep-history backfills enlarge the sweep's input by up to 2000 old rows per run. The flip is NOT uniformly wrong — a backfill-revealed GENUINE counterpart flipping an old "expense" to a transfer corrects double-counted flows (that is the sweep's job when one bank posts late) — so the fix is a measured semantics decision (age gate? review-state gate? confidence floor?), on the owner's corpus, C.6-lesson territory (a loose pair rule once credited 11 refunds as payments). Own slice, own critic. | **Fable 5** build + hostile critic | medium | 60k | **[~] BUILT + LIVE-MEASURED 2026-08-05 (DECISIONS #415, STATUS §H.7) — CRITIC PASS OWED.** The semantics: `isTransfer` is a categorization verdict wearing a different column, so the flag branch inherits the protection the FILE branch has had since #148 — a pair-only guess may SUPPLY a verdict, never silently REVERSE one. **Measured before deciding** (4 read-only probes replaying the REAL engine over 3,065 production rows): **92** settled rows were withholding **$21,411.05** inflow / **$181,281.51** outflow under a non-transfer category; **73** stood on nothing but a pair — 45 duplicate-account artifacts, 12 brokerage funding, 9 card/loan payments, 7 pure coincidence. The critic's repro was live AND bidirectional: a $500.00 "CEF I CEF IV PPD" distribution settled at 9900 bps cancelled against a $500.00 Zelle payment to a landscaper, so a real income row and a real expense row both vanished. **Two causes, two scopes, deliberately not merged:** (1) the sweep was the ONLY transaction read surface in the app skipping `getReconciliationTxnKeep`, so it read both copies of a reconciled account (1,215 of 3,065 rows were not its to read) and paired rows against their own duplicates — defeating the same-account exclusion `transfers.ts` already declares; applying the app's own keep-rule removes **53 of the 73**, and it is exact because the link exists only on a user confirm. (2) A settled substantive row is overturned only by a **directionally coherent** pair (the SENDING leg must be an account money can leave) — a credit-line outflow is a purchase, which is what the remaining false overturns had in common; this one is a heuristic with real counterexamples (balance transfer, cash advance) so it is scoped to the overturn decision alone, where its failure mode is inaction rather than a rewrite. **Rejected after measuring, not assumed:** an AGE gate (refuses exactly the corrections a deep-history backfill exists to make) and a CONFIDENCE gate (useless — genuine and false both sit at 9000-9900 bps). **CRITIC CYCLE 1 DONE — both critics FAIL (1 P0, 5 P1, 4 P2, 1 P3), all fixed and locked; the mechanism changed twice.** The P0: the gate never fired on the shape a row ARRIVES in (every synced row is born `needsReview`), so the coincidence still won on the first sweep — and filing is the heavier write, since it also clears `needsReview` and the vanished income never reached triage. The evidence bar is now ONE bar over every write; an unevidenced pair gets no action at all, because flagging without filing recreates the pre-#165 wedge. The sharpest P1: filtering the READ through `getReconciliationTxnKeep` blinds the WRITER to a leg its readers still count (executed: a $123.45 card payment read as negative spending, a month's expenses $200.00 -> $76.55), so the boundary moved out of the input and into the MATCHING rule — rows carry their confirmed account identity and two rows on one real account never pair. Also fixed: LOAN/MORTGAGE can send (a $20,000 HELOC draw filed as Income was staying Income, including in the tax export); the overturn write re-asserts its premise (`undoCorrections` falsifies 'can only become more settled'); one exported constant feeds both the engine predicate and its SQL mirror; one pair+normalizer walk instead of two (+86% measured); `overturned` returned separately and counted toward `derivedChanged`. Re-gate: verify GREEN — tsc 0 / eslint 0 / **6096 unit / 369 files** / build clean; five sabotages executed, all RED, all restored with residue checks. Live re-measure with the shipped code: 114 flags + **39** overturns justified from scratch, two residual false ones ($0.07 interest rows). Unrepaired set: **53 rows / $29,848.84**. **CYCLE 2 DONE — FAIL, 2 P1 + 3 P2 + 3 P3, both P1s fixed and locked.** (a) The identity map inherited `effectiveReconciliationLinks`' fail-OPEN default, so four ordinary inert-link shapes — including the feed-driven type drift both providers cause on any sync — put the duplicate-pairing artifact straight back; new sibling `accountIdentityMap` reads every live link with only the cycle guard (measured live: 26 vs 26, no inert links today, so this is protection not repair). (b) The evidence bar was per-ROW, so a pair could be half-actioned: a $5,000 cash advance whose inflow descriptor was transfer-known left income while the CREDIT outflow stayed in spending — **a $5,000 expense that had not existed before this slice**. Descriptor evidence now carries across a matched pair, and the live repro provably stays refused. Also: `'INVESTMENT'` as a SENDER was entirely unlocked, and two of the file write's four re-assertions were deletable with the suite green. Re-gate: verify GREEN — **6103 unit / 369 files**; six new sabotages all RED. **Cycles used: 2 of 4.** Remaining in STATUS §STILL OPEN after H.7 — sharpest is the owner decision on repairing the 53 rows / $29,848.84, which needs a way to CLEAR `isTransfer` since nothing in the app can do that today. Gate: `bash scripts/verify.sh` GREEN — tsc 0 / eslint 0 / **6090 unit / 369 files** / build clean; **no schema change, no prisma diff, no UI surface**; three sabotages executed, each failing its own lock, each restored and residue-checked. **OPEN residuals in STATUS §STILL OPEN after H.7:** the $0.07 case; a settled CASH outflow still overturnable by a coincidental cash inflow (unmeasured after the boundary); **the 45 flags already written are NOT repaired** (add-only by construction — un-flagging is itself a silent rewrite on figures the owner has seen, so it needs an owner-visible decision); degenerate reconciliation claims keep both copies (A-F8); and the hostile-critic cycle itself. |
| H.7b | **Repair the transfer flags the shipped rule declines — the owner-triggered, undoable pass STATUS §after-H.7 residual 1 prescribed.** The 53 rows / $29,848.84 the pre-H.7 coincidence rule flagged (4 income-categorised) were still withheld from every total because flags are add-only. Built engine-first: `planTransferFlagRepair` replays the SHIPPED `planTransferUpdates` from scratch over the sweep's own read (one rule ⇒ a cleared row cannot bounce back), scope = settled substantive + un-pinned + POSTED + USD-counted + not reader-excluded; preview → owner tap on /settings → recorded `TransferFlagRepairRun` (additive schema) → atomic newest-run-only undo that skips reader-re-decided rows. Also the app's only `isTransfer: false` write path (residual 4). | **Fable 5** build + 2 fresh critics | medium | 60k | **[x] DONE 2026-08-08 (DECISIONS #428, critic-cycled: 2 fresh critics, both FAIL — 6 P1 + 5 P2 + 9 P3, all executed, all but one fixed same cycle + 6 ledger rows + 3 sabotages re-run RED).** Sharpest finding: a GENUINE card-sourced cash advance would be actively un-flagged and the sweep can never re-fix it — caution shipped before the button, per-row selection recorded OPEN in STATUS §after-H.7b (zero such rows live). Gate: verify GREEN — tsc 0 / eslint 0 / 6,345 unit + 1 skipped / 385 files / build clean; e2e 4/4 incl. the full apply→undo round trip. Schema: additive `TransferFlagRepairRun` only. |
| H.8 | **Five other direct transaction readers also skip the reconciliation boundary — measure whether any of them prints a figure.** Found while correcting an overstated claim in H.7 (I had written that the transfer sweep was the ONLY reader skipping `getReconciliationTxnKeep`; it is not). Verified by grepping every direct `prisma.transaction.{findMany,groupBy,aggregate,count}` in `src/`: **`backfill.ts:82`, `rules.ts:95`, `keyword-rules.ts:261`, `self-audit.ts:61-62` and `household-digest.ts:97`** read transactions without the keep filter. **The sharpest one was found by the H.7 cycle-1 critic and is not in that list: `spending-plan.ts:198`** reads LOAN/MORTGAGE inflows with no boundary and no superseded-account exclusion, then feeds them to `loanPaymentMerchantCanonicals` — which runs the SAME ±3-day same-|amount| pair rule H.7 just had to fix, so it carries the identical duplicate-pairing hazard, and it is joined against `snap.transactions`, which IS boundaried (a half-boundaried join). Its own sibling `demo.ts:120-133` DOES exclude superseded loan accounts, so the two disagree. Start there, and reuse H.7's answer: identity in the matching rule, never a filter on the input, while the register, CSV export, budgets, recurring detection and triage all apply it. **Do not assume these are defects** — they are different in kind from the sweep and each needs its own check: `rules.ts` reads by explicit id from Correction rows (a windowed boundary may be irrelevant there), `backfill.ts` targets only unresolved rows (`isTransfer: false` + `needsReview`/uncategorized) so its exposure is wasted work and re-categorising invisible rows rather than a wrong figure, `self-audit.ts` COUNTS (a diagnostic surface that could still contradict the register it audits), and `household-digest.ts` prints money to a partner, which is the one most likely to matter. **The measurement, before any code:** for the owner's live corpus (26 active links, 1,215 rows the boundary does not own), run each reader's own `where` clause with and without `getReconciliationTxnKeep` and report the delta — rows, and where applicable dollars. Only the readers whose delta reaches a rendered number get a fix. H.7 is the precedent for why this matters, and also for why the fix is not automatic: the sweep was the only one that MATCHES ROWS AGAINST EACH OTHER, so a duplicate manufactured a transfer there, whereas elsewhere the worst case is a doubled or double-counted row. | Opus 5 (measure) + **Fable 5** if a money figure moves | medium | 50k | **[x] DONE 2026-08-06 (#416, critic cycle 1 P1 fixed same cycle) — measured live first, and the verdicts followed this row's own rule.** Probe (`h8-boundary-readers.mts`, read-only, 26 active links): **[1] spending-plan loan inflows CLEAN** (delta 0, merchant sets byte-identical — the ±3d pair-rule hazard does not fire on this corpus, nothing touched); **[2] household digest n/a** (no household; superseded exclusion verified present); **[3] self-audit FIXED** (rendered 75/2456 vs the register's 7/1332 — a live contradiction on /settings); **[4] keyword rules FIXED** (preview counted 1,124 invisible rows / $271,467.59 AND Apply wrote to them; one filter in `matchableHistory` covers preview + all four writes, `/rules?from=` names the reason for a disowned row); **[5] backfill FIXED** (68 of 75 scanned were invisible — 10× LLM fan-out, `needsReview:false` stamps an undo would resurrect pre-filed); **[6] corrections DELIBERATELY UNFILTERED** (H.7 P1-3: evidence must not be blinded; 146/827 sit on disowned rows, classification probe committed but unrun — STATUS residual 2). **The fresh-context critic's P1 (executed): the MERCHANT-BATCH writers were missed** — the keep-filtered group card said "File all 2" and one tap filed 3, minting a hand-decision-shaped Correction on the invisible row; fixed at `fileMerchantGroup` + `applyToAllSimilar` + `recategorize scope:'merchant'` + triage `similarCount` (the #397 spend-class twin already carried the filter). 10 locks; **seven** executed sabotages each turning exactly its own lock RED; verify GREEN twice (6108 unit / 370 files, then the re-gate); no schema change. Residuals in STATUS §STILL OPEN after H.8. |
| H.6 | **Plaid: if H.1's audit rows show `added ≈ 0`, the 730-day route for a pre-existing item is a fresh Link — build the guided, owner-approved path.** Keep the old item's rows (add-only history is already stored), link a NEW item with `days_requested=730`, dedupe the overlap — **providerRef differs across items, so the dedupe must key on account identity + date + amount, and the C.6 lesson applies (a loose pair rule credited 11 refunds as payments)** — then retire the old item. Destroying a credential is irreversible and therefore the owner's click, never automatic (`plaid.ts:266-269` records this stance): the UI proposes and explains, the owner decides per institution. | Opus 5 | large | 90k | **[x] DONE 2026-08-07 (DECISIONS #424, verify green, 1 critic cycle: 0 P0 / 4 P1 — 3 fixed, 1 split to H.6c).** The blocker was NOT the missing flow — it was that the app DISCARDED the deeper link: a 730-day Item necessarily returns the same accounts, so `decideAndPersistItem` handed it back to Plaid as wholly redundant. One explicit owner intent (`deepenHistory`), carried from a new /accounts door through the OAuth round-trip, exempts that link and nothing else; the ordinary front door still refuses (2 sabotages: removing the exemption kills its own lock, leaking it fires 9). No dedupe engine was needed after all — `combineDuplicateConnections` + `applyReconciliationBoundary` already keep the successor's deeper rows (`reconcile-boundary.ts:17-23`). Locks: 5 provider + 5 storage + 5 component-wiring (2 sabotages) + 4 copy + 2 e2e. |
| H.6c | **The /accounts combine card's PRIMARY button drops the connection that holds the history — proven by execution, and it is the DEFAULT case, not an edge.** Found by the H.6 fresh-context critic. `keepRank` (`combine-connections.ts:192-201`) breaks ties with "linked first wins", and rules 1–2 (no sync error; most recent `lastSyncedAt`) TIE in the normal case because `lastSyncedAt` is a calendar day and both connections sync the same day. `CombineConnectionsCard` renders `p.recommended` first with `variant="default"`, so the prominent button proposes keeping the OLD 90-day connection and dropping the new 730-day one — and combine REVOKES the dropped Item, irreversibly, most likely before Plaid's background historical pull has finished. Critic's executed output: `RECOMMENDED keep=old drop=new`. **Mitigated but not fixed in H.6:** the flash now names the ordinal to keep and what the other choice costs (locked), i.e. the copy overrides a wrong default rather than the default being right. **Fix:** add a rule to `keepRank`, ahead of the linked-first tie-break, preferring the connection whose earliest transaction is older — needs `CombineConnectionItem`/`CombineItemRow` to carry a per-item earliest-txn date, threaded through `buildCombineInputs`'s three fetch sites. Money-adjacent planner change on an irreversible action ⇒ its own critic cycle. | **Fable 5** | small | 40k | **[x] DONE 2026-08-07 (DECISIONS #425 + addendum, critic-cycled: 2 fresh critics, 1 P0 + 5 P1 + 3 P2, all executed and all fixed same cycle).** `keepRank` rule 3: FEED depth (`providerRef` non-null — one hand-typed backdated row must not flip an irreversible default, critic-executed) ahead of linked-first; null = no evidence, so mid-pull the old side keeps the default and the card now carries `combineDepthNote` beside EACH direction saying what a tap would drop (jsdom-locked — the card was the untested hop). Depth threaded as a per-account map folded per-connection in `buildCombineInputs` (fold locked with a 2-account fixture after the critic inverted it green); both fetch sites locked by sabotage. The deep-history refusal now names the true remedy ("combine the other way round") instead of "sync and try again"/"delete the copy". See STATUS §H.6c+H.6b(b) for the gate. |
| H.6b | **Combining after a deepen stops applying the reader's hand-filed work — and a split makes it refuse outright.** Two executed critic findings, both pre-existing in combine and both first made REACHABLE by H.6. (a) `handoverDate` clamps the cutover to the predecessor's FIRST transaction whenever the successor reaches further back — the defining property of a successful deepen — so the old side keeps one day and everything it recorded after that (categoryId, note, taxClass, excludeFromTotals, split structure) stops counting in favour of the new connection's untouched copies; `combineDuplicateConnectionsFor` carries across only `AutopayConfig` + `displayName`. No money moves and nothing is deleted. Disclosed in H.6 rather than promised away (`deepen-history-caveat`, e2e-locked); the fix is to carry reader-owned fields onto same-date/same-amount successor rows. (b) `rowsLostToTheSplit` is fed rows filtered `isSplitParent: false`, so the predecessor presents split CHILDREN (−6000, −4000) where the successor presents the parent (−10000) — the guard then refuses the whole combine with a FALSE diagnosis ("2 charges totalling $100.00 appear on only one of them … delete the copy you don't want"). Executed. Critic's named fix: read `{ OR: [{ isSplitParent: true }, { splitParentId: null }] }` for the multiset so a parent stands in for its children; at minimum, name splits in the refusal. Refuses safely (`ok:false`, nothing destroyed) — a blocked remedy, not data loss. | **Fable 5** | small | 45k | **[x] DONE 2026-08-07 ((b) DECISIONS #425 + addendum; (a) DECISIONS #427, critic-cycled to the hard cap).** (b) The guard reads rows AS THE BANK DELIVERED THEM (parents + unsplit + DANGLING children; never children with a live parent), computes the boundary's own window (`[predFirst, min(cutover, predLast)]` over ALL rows), and refuses outright when the window would SEVER a split family (the critic's P0: the pending→posted sync drifts a parent's date from its children's, and the first bank-shape cut deleted a counted $100.00 behind ok:true — now conserved-or-refused, locked both directions on both sides). (a) `planReaderFieldCarry` now moves the reader's hand-filed work onto same-date/same-amount successor rows: Corrections MOVE never copy, the survivor's own reader values always win, split families carry whole (stale → durable review), both-split carries route piece flats by unique match, and a container (read nowhere) never receives a verdict, a correction move or flats — flat state routes onto its children (Finding A, critic cycle 4 = the cap). 4 critic cycles, every finding executed and locked (F1-F4, NEW-1/2, A1, P1-1/2/3); the deepen door's amber caveat now promises the carry, not everything. Gate: verify GREEN — 6,303 unit + 1 skipped / 381 files; deepen-history e2e 2/2; no schema change. |

## Wave G — Gate integrity (the ship gate must mean something)

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| G.1 | **`budget-targets.spec.ts:20` reddens the CI ship gate across unrelated slices, and the documented remedy did NOT hold.** Evidence, not inference: it failed the gate for H.7b (attempt 1, `gh run rerun --failed` → success), failed a full LOCAL run during H.1(b), and failed H.1(b)'s CI gate on run **31243413430** for `3fe37f6` — a commit that touches no budget file at all (verified with `--stat`). Signature is stable: `expect(locator).toHaveCount(expected) failed … 20 × locator resolved to 1 element`. **What makes this worth its own slice rather than another re-run:** `docs/lessons/ci-e2e-timing-flake.md` prescribes "confirm on a re-rendered page (reload inside `toPass`)", that remedy is ALREADY APPLIED to this spec's clear step (`budget-targets.spec.ts:63-71` — `waitForResponse`, then reload-inside-`toPass`), and it still failed. So either the failing assertion is a DIFFERENT `toHaveCount` in the same test (line 34's `budget-no-targets-hint` → 0, or line 52's `budget-row-dining` → 1 — Playwright reports the test's declaration line, so the run log alone cannot say which), or the remedy is insufficient under CI load. **First deliverable is identification, not a fix:** re-run the failed job with `--debug`, or add a per-assertion marker, and name the exact line before changing anything. Then fix the cause. **Do not weaken or delete the assertion to go green** — it guards the upsert-yields-ONE-row invariant on a money surface (#37/#186). Every slice currently pays this as a false red on its ship gate, which is precisely how K.8 found CI had been ignored for days. | Opus 5 | small | 40k | **[x] FIXED 2026-08-08 (verify green, full e2e 305/0 — was 304/1). IDENTIFIED first (H.1(b) read the gate twice and pulled the failure block).** **The assertion is named:** locator `getByTestId('budget-clear-dining')`, **expected 0, received 1**, at `budget-targets.spec.ts:70-71` — i.e. the CLEAR step's `toPass`, the one that ALREADY carries the lesson's remedy. Full 20s `toPass` exhausted, every inner poll seeing 1 element (`Timeout 20000ms exceeded while waiting on the predicate`). Deterministic in CI on **two** shas (runs 31243413430 / 31243942530), green locally. **What that rules out:** a stale read cannot survive twenty full-document reloads over 20 seconds — so the delete never committed, and this is NOT the "single post-mutation read" class the lesson describes. **A first root-cause hypothesis was WRONG and is recorded as wrong rather than quietly dropped.** It read the never-committing delete as `ClearBudgetButton`'s `finally`-reload cancelling its own in-flight action, which would have made this a money-surface PRODUCT defect across every form on the deadline+reload recipe. Kept looking instead of fixing, and the repo had already diagnosed this family: `playwright.config.ts`'s worker note and `mobile-overflow.spec.ts:333-339` both record that concurrent demo sessions on the single-writer SQLite e2e DB sever exactly the reload-bearing mutation specs' confirmation streams — *"pwa-offline's budget-clear round-trip flaked exactly this way"*. **The contention was real and this spec's own header denied it:** it claimed "no other spec asserts a budget target" while `pwa-offline.spec.ts:44-49` drives the same set/clear round-trip on the demo user concurrently under `fullyParallel` × 4 workers. Different categories, so the ROWS never collided — the collision was for the writer. **FIX: the spec runs on a throwaway user** (plus one seeded account, since /budgets renders first-run onboarding until one exists), which removes it from that contention and makes its end-of-test cleanup moot. **Not one assertion weakened** — the upsert-yields-ONE-row invariant (#37/#186), the WCAG scan and the clear round-trip are all user-agnostic, and budget targets are display-only so no golden value moves. **Adjacent risk recorded, deliberately NOT changed:** `pwa-offline.spec.ts` still drives its budget set/clear round-trip as the shared demo user, so it remains in the contention pool and is the same spec the config note already names as having flaked this way. Left alone because it is an OFFLINE/service-worker spec whose point is caching the demo's real seeded pages — moving it to a bare throwaway user could hollow out what it tests, which is a bigger decision than this row. It is more tolerant than budget-targets was (15s round-trip timeouts vs a 2s inner poll), so it is a watch item, not a known break. | 

## Wave O.19 — /accounts de-clutter: hide the combine machinery (owner request 2026-08-07, LIVE)

> **Renumbered 2026-08-07.** Filed as "O.18" when it was queued, but Wave O.18 (expandable rows,
> 2026-07-31) already owns that id and its rows O.18c/O.18e/O.18g are cited in STATUS — two waves
> answering to one number would make every future citation ambiguous. Same row, new id.

Owner, verbatim, after closing H.6c: *"Can we get rid of all the combine accounts on accounts
page. Looks like a beta website. Perhaps do[n']t delete that if we ever need to come back to it.
Maybe hide it for now. It's ugly. Save this for next slice after I /clear"* — and, same
exchange, the depth decision that de-prioritizes the machinery's biggest use case: *"We don't
need more than 2 years. I just want parity with what's out there."*

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| O.19 | **Collapse the combine/duplicate machinery on /accounts behind something unobtrusive — HIDE, never delete (owner-explicit).** What renders there today from this family, all in `accounts-list.tsx`: `CombineConnectionsCard` (offers AND per-pair blocked-reason blocks), the #192 advisory duplicate warnings, reconciliation candidate cards, the "Combined accounts" disclosure card (with Undo), the H.6 deepen door + its two explainer paragraphs, and the K.2b reconnect door. **The likely clutter driver for HIS screen:** 13 connections across 7 institutions ⇒ many same-bank lookalike pairs, and every pair that produces no offer renders its own explanation block (the "an empty set is not a fact" rule) — correct one at a time, a wall all at once. **Constraints, each earned by a shipped lesson:** (a) hiding the CARD must not hide the GUARD — `suppressCombineProposals` runs in both the view and the action, and the action's server-side validation must stay byte-identical regardless of UI visibility (a-guard-must-read-what-it-guards); (b) the deepen flow's CLOSING STEP is combine (H.6/H.6c) — a fully hidden card orphans that remedy, so collapse behind a small disclosure ("N duplicate-connection matters" or similar) rather than remove, and consider auto-expanding while a deepen-shaped pair exists (two live same-bank connections where one holds deeper feed history — the data is already on `CombineDirection`); (c) the blocked-reason conclusions must stay REACHABLE (behind the tap), not deleted — rendering nothing is how the owner once concluded nothing had shipped ("Not there"); (d) four e2e specs drive these cards (`combine-connections`, `duplicate-connections`, `deepen-history`, `combined-accounts`) and will need to open the disclosure first — update them WITH the slice, don't let the full gate go red silently (K.5/K.8 lesson); (e) this is the owner's broader "looks like a beta website" signal — while in the file, prefer removing visual noise over adding chrome, but scope stays /accounts + this card family. | Opus 4.8 (UI slice, no money math) + copy tests | medium | 40k | **[x] DONE 2026-08-07 (DECISIONS #426, critic-cycled: 2 fresh critics, 1 P0 + 4 P1 + 4 P2, all executed and all fixed same cycle).** Five cards behind one collapsed `Account cleanup` disclosure; nothing unmounted, no server file touched. The collapsed line carries the CLAIM (a balance counted twice; an account folded out of the list) chosen by evidence strength, and 15 sentences across the page, /cards, /calendar, the dashboard and two emails now name the section they point at (heading moved to `src/lib/engine/account/account-cleanup.ts` — `src/lib` cannot import `src/components`). Critic P0: the money sentence had gone to the ADVISORY residue while the PROVEN case got a procedural "can be combined" — executed at net worth $2,000.00 for $1,000.00 real, "twice" nowhere on the page, and this slices own e2e had ratified it. Gate: verify GREEN (tsc 0 / eslint 0 / 6,274 unit + 1 skipped / 381 files / build clean), family e2e 22/22 serialized, one sabotage reddening 4 locks. No schema change. Residuals in STATUS §O.19. |

## Wave K — /calendar only ever looks forward (owner report 2026-08-06, LIVE)

Owner, verbatim: *"Calendar makes no sense. I have forward data but not trailing?"*

**Diagnosed in code the same session — the report is correct, and it is by construction, not
one clamp.** Three independent mechanisms, each verified by reading the lines:

1. **There is no posted-transaction source at all.** `CalendarEvent.kind` is only
   `inflow | outflow | card-due | loan-due` (`engine/calendar/build.ts:22-25`), fed by scheduled
   cadences plus card/loan obligations. The page has `snap.transactions` in hand and never passes
   them (`calendar/page.tsx:96-106`).
2. **Scheduled rows step forward only, from wherever their anchor sits.** `expandScheduled` anchors
   on `ScheduledTransaction.nextDate` and iterates `i = 0,1,2…` forward (`build.ts:98-108`); its own
   comment says "A row dated in the past also steps forward now." So the calendar's earliest content
   is each series' own anchor — **not** `today` — and it repeats to whatever month is requested, with
   no far horizon.
3. **Dues on or before today are explicitly skipped** — the "stale anchor" guard at `build.ts:187`
   (cards) and `:234` (loans), `if (compareDates(effective, today) <= 0) continue`.

**CORRECTION, same session, from the owner's own observation** (*"I can scroll forward in dates and
all data populates into 2027. No data pre May"*): the first version of this row generalized #2 into
"the already-passed days of the CURRENT month are empty too" — that is WRONG, and his screen is the
evidence. A series anchored in the past paints past months, which is exactly why May–July are
populated. What is true is narrower and worse: **the entire past half of this calendar is
projections replayed backward from a scheduled anchor, and the entire future half is projections
replayed forward, to 2027 and beyond, with no horizon — none of it is a posted transaction, because
mechanism #1 means the page never reads one.** So "no data pre May" is the oldest scheduled anchor,
not the oldest thing that happened to him.

**The second, larger half of the report is a DATA fact, not a calendar fact.** Measured live
2026-08-05 (#414, output in PROGRESS): the oldest transaction across all 12 Plaid items is
**2026-04-24**. Plaid's documented maximum is **730 days** (re-verified against plaid.com/docs/api/link
on 2026-08-06, `days_requested` min 1 / max 730 / default 90), items linked before we asked for 730
only ever ingested Plaid's 90-day default, and the one-time backfill ran on all 12 items and got
`added: 0` — Plaid holds nothing older. **Three trailing years is therefore unreachable through
Plaid by construction.** The two routes that do reach it: a SimpleFIN connection, whose first-ever
pull asks for `SIMPLEFIN_INITIAL_LOOKBACK_DAYS = 1095` (`simplefin.ts:63`, three years to the day) —
the owner's SimpleFIN connection is currently DISCONNECTED (#414) — and per-bank CSV export through
the existing importer (H.2).

Pinned as intended behaviour by `tests/unit/calendar-due-expansion.test.ts:209` — *"a month BEFORE
the due month paints nothing (no backfill of history we never held)"*. So this is a **product
decision to revisit**, not a regression to fix; C.8 (#404) is the most recent slice in the area.

**Two things are defects on ANY of the directions below**, because they mislead independently of
what a past day should hold: (a) `/calendar` ships a "Previous month" arrow (`page.tsx:203`, `:230`)
that can only ever render an empty grid — an affordance with no reachable payload; (b) a past month
prints *"No scheduled activity this month"* (`page.tsx:308-312`), which asserts nothing happened
when the truth is that this surface never looks back — the "a zero is a claim, name WHICH zero"
lesson, on a page whose back-arrow invites exactly that reading.

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| K.1 | **A past day on /calendar shows what actually posted — ANSWERED by the owner 2026-08-06** (*"We want 3 trailing year data"*), which selects shape (i) over the two alternatives offered (bills-with-outcomes, and forward-only-made-honest). The calendar gains its first posted-transaction source: real income/spend on the dates they posted, behind today; obligations ahead of it. **These are money figures on a new surface, so they take the register's basis and the reconciliation boundary or the calendar becomes the next reader that disagrees with the register** (H.8's rule, and it is now SIX readers deep — do not add a seventh). Two things close with it: the "Previous month" arrow stops being able to land on a grid that only ever held projections, and the "No scheduled activity this month" copy stops asserting a zero it cannot see. **Also in scope because the owner's sentence is about trust, not just presence:** the forward half must stop reading like data — a 2027 cell is a scheduled series replayed 17 times, and only the card/loan dues currently carry `(est.)`. | **Fable 5** (money on a new surface) | medium | 70k | **[x] DONE 2026-08-06 (DECISIONS #419).** Days ≤ today paint recorded activity through the register's OWN math — one where-clause (`registerRowWhere`), one reconciliation keep, one `summarizeTransactions` (signature widened to `TotalableTxn`), so the gate holds by construction AND is locked three ways: function-level, real-loader (register-equality with hand-verified values), and DOM-to-DOM in e2e (a day's painted figure vs the register's summary after following the day's own link). Each posted day links to `/transactions?from=D&to=D`. Scheduled replays start AT today (the assembler's own window — the first cut's today+1 was critic-caught); zeros name WHICH kind with the bound inside the reason (K.3's discriminated-union rule); floor + trailing-lag edges named. **Two fresh-context critics, both FAIL: 4 P1 + 12 P2 → all P1s + 8 P2s fixed and locked** (pending money named "Posted + pending" everywhere — the demo's own pinned month holds three pending rows; before-history no longer denies the CSV remedy the register offers; the shortfall day always renders; "Expected:" with already-due counted out loud; transfer/excluded-only days explain their zero). Residuals recorded in STATUS §K.1: banner "money-out total above" ambiguity (two critic-cycled modules), frozen-feed thinning of posted days (L.19 family), 2× full-history loads per view (ROADMAP #8). Gate: verify — tsc 0 / eslint 0 / **6,161 unit + 1 skipped / 374 files** / build clean; calendar-posted e2e 3/3, calendar-frozen 6/6; full suite serialized 283/297 with all 14 non-passes PROVEN pre-existing on a stashed tree (3 repaired here → the rest are **K.5**). Depth unchanged by design — that is K.2. |
| K.3 | **The register blames the filters for a window that sits outside its own history (owner screenshot 2026-08-06).** Custom window Aug 6 2024 → Aug 6 2025, history starting Mar 25 2026: the page printed "History available from Wed, Mar 25, 2026", then $0.00/$0.00/$0.00, then "No transactions match these filters" — the reason present on screen and joined to nothing, which is how the owner concluded the app was broken rather than that the data was missing. | **Fable 5** | small | 25k | **[x] DONE 2026-08-06 (#417).** `registerEmptyReason` (pure, 4 kinds) computed from the SAME bounds the filter bar prints, so the two sentences cannot disagree — the e2e reads the printed date off the DOM and requires the empty state to contain it. Two new kinds only; #186's answers are preserved verbatim, including the empty-register-with-a-filter case. Also caught in-session and fixed before shipping: the raw `from`/`to` are `''` when unset and `?to=banana` is reachable, so a bare `isoDate()` would have thrown the whole route — an unparseable bound is now ABSENT. **CRITIC CYCLE 1 — FAIL, 4 P1 + 5 P2 + 2 P3, all four P1s executed and locked:** the CSV remedy is refused for the demo user (every anonymous visitor on production) and the slice's own e2e drove that path; the zero was named in a box BELOW the four `$0.00` figures the owner actually reported, not on the count line; an inverted window (`from` after `to`, two clicks apart — the date inputs carry no min/max) was told the history bound and offered an import that cannot help a window empty by construction (new `inverted-window` kind, decided without consulting the data); and `newestDate` + the whole `after-history` branch had NO lock — the critic's `>`→`<` sabotage collapsed both bounds and stayed green across 6,126 tests. It also disproved a claim this slice had written down: `?to=banana` was **already a 500** (`filterTransactions` casts with an unguarded `isoDate` first), so the tolerant parse was a dead branch whose comment denied a live defect — now fixed at the boundary, closing that 500. Re-gate: verify GREEN (**6137 unit / 372 files**), transactions.spec **24/24**, seven executed sabotages each RED on its own lock. **Does not make the data deeper — that is K.2.** |
| K.4 | **The owner's exact pair survives one filter away (opened by the K.3 critic, F10).** K.3 decides against the register's GLOBAL bounds, so a reader narrowed to a card connected last month who picks "Last year" gets a `to` inside the global span, falls through to `filters`, and sees "No transactions match these filters" sitting under "History available from Wed, Mar 25, 2026" — the identical broken pair K.3 exists to remove, one dropdown later. **The fix is not to narrow one of them:** the printed filter-bar line and the empty-state bounds are the SAME two values by construction, which is the property K.3's e2e locks, so moving the empty state to a filtered scope while the line stays global would re-create the disagreement. Both move together, or neither does — and a per-filter bound also changes what "History available from" means on every filtered arrival (`categoryRegisterHref` sends readers here pre-filtered by category AND month, so filtered arrival is the designed path, not an edge). Decide the scope question first, then build. | **Fable 5** | small | 35k | **[x] DONE 2026-08-09 (DECISIONS #436, verify green).** Scope decided by execution, not inspection: the bound is narrowed by the SET-DEFINING axes only — account, category, unclassified (`scopedDateBounds` in `query.ts`, wired into `getTransactions` replacing the global scan) — and BOTH surfaces (the filter-bar line and the empty-state reason inputs) receive the same scoped value, so K.3's pair-equality holds by construction. Match axes (type, class, search, merchant, reimbursement, the window) never move the line. Rejected: narrowing one surface (re-breaks the pair), scoping by all axes (the line flip-flops on every toggle), patching only the branch (both sentences stay true-but-not-about-the-view). Soundness: the scoped set is a superset of every further-narrowed subset, so its oldest is a lower bound on any of them — the window branches stay honest. Unit: 8 new locks (scopedDateBounds ×7, incl. the F10 depth shape + empty-scoped nulls; registerEmptyReason F10-shape — with the scoped bound, before-history fires where the global bound could not). E2E: throwaway user, two accounts of different depth (2024-08-11 vs 2026-07-01); unfiltered prints the global bound; account-narrowed + "Last year" prints the card's OWN bound in BOTH surfaces (`txn-empty-before-history`, never "No transactions match") — a state the unscoped code cannot reach, and the demo seed cannot produce (uniform account depth). Gate: local verify GREEN — 320/320 e2e, 6,554 unit + 1 skipped. The row's Fable-5 build recommendation was not followed: the slice is UI + a pure helper + e2e (Flash-shaped per COMPETITIVE_GAP_PLAN §3), the decision is non-money, and the in-session critic pass covered the scope logic + branch soundness. |
| K.5 | **Ten e2e specs assert a dashboard that no longer exists — pre-existing, PROVEN, currently reddening every full-suite run.** Found by K.1's full-suite gate (2026-08-06), the first full e2e run since 2026-08-01: commit `2e3bf72` (#369 "Home polish") removed the recurring-total and payment-reminders cards from Home and reordered it around the IWT loop, and its session never ran the full suite — so auth:82 (`dashboard-recurring-empty`), recurring:15, payment-reminders:14, dashboard-duplicate-disclosure:92+154, frozen-figure-surfaces:92, card-unknown-due:226, phase2-triage:132, and today-feed-frozen:220+238 fail deterministically (identical set on a stashed pre-K.1 tree, serialized — not flake). A separate 3 in transaction-status.spec broke the same way from O.16's `?back=` param and were repaired in-session (#419). **The fix is NOT a mechanical regex pass:** each assertion must be re-pointed at wherever #369 moved the surface (reminders/frozen disclosures now live in the Today feed and /cards), which means reading that commit's intent first — some assertions may now belong on different routes, and some (duplicate-disclosure's "hero and reminders name the card the same way") guard cross-surface consistency that must be re-established on the NEW layout, not deleted. Until this closes, `VERIFY_E2E=1` cannot go green, which weakens every slice's gate — highest-priority test-debt in the repo. | Opus 5 | medium | 60k | **[~] 8 of 10 CLOSED 2026-08-06 — and the row's own premise was half wrong.** All ten reproduce deterministically (serialized, fresh build), but only EIGHT are #369: the two `phase2-triage` failures are O.17's demo fence on `createCustomCategory` refusing the shared demo user, which both tests drive — no dashboard dependency at all, and the second was hidden behind the first by serial abort. Split out as **K.6**. **The eight were not all test debt.** `PaymentRemindersCard`, `RecurringSummaryCard` and `AskAimplifiCard` are ORPHANED — no render site anywhere — so nothing "moved"; each assertion had to be judged on what claim it guarded. Seven re-pointed to the surface that now carries the claim (/cards for the duplicate disclosure and the undated section, /calendar for named card+loan dues, the Today feed for the frozen note and the undated-card fence, `dashboard-recent-empty` for the sparse-card invariant); one deleted with a note in its place, because Home deliberately no longer claims a recurring total. **The eighth uncovered a live regression #369 shipped:** `frozenNothingDueNote` — the L.19/L.20 sentence naming a frozen card, frozen loan or undatable frozen mortgage — was spliced onto `NudgeFeed.emptyReason`, which the Today feed renders ONLY when empty. The reminders card had carried it in the non-empty branch; with that card gone, one live card being due removed a frozen mortgage from every page in the app, leaving the weekly digest EMAIL as the only surface that named it — the exact gap all three L.20 critics found independently, reintroduced by deleting a renderer rather than editing an engine. Fixed by promoting it to its own `NudgeFeed.frozenDueNote` field rendered unconditionally, exactly as its sibling half `fundingFrozen` already was; 5 new engine locks (the engine had ZERO `frozenDues` coverage, which is why this was invisible), the regression one sabotage-proven RED by re-gating the field on an empty feed. Also corrected: `cards-breakdown.tsx`'s comment narrowing its all-clear to cards "because the dashboard reminders card and the weekly digest cover both" — a delegation to a surface deleted five days earlier. Two REGRESSION_LEDGER rows; new lesson `deleting-a-surface-deletes-the-claims-it-carried.md`. Gate: tsc 0 / eslint 0 / **6,166 unit + 1 skipped / 374 files** / build clean; the 8 re-pointed specs 34/34 serialized. Residual recorded, not fixed: `/recurring` lost its ONLY nav-level link (it lived on the orphaned summary card) and is now reachable only from a transaction detail view and /spending-plan. |
| K.6 | **Two triage e2e tests drive a write path the demo user is fenced out of — the third recurrence of a known lesson.** Split out of K.5, which had attributed them to #369; they have no dashboard dependency. `phase2-triage.spec.ts:132` (singles-mode leak, cycle-2 P1) and `:184` (write-in creates + files the whole group, #136) both sign in as the shared demo user and then create a category, and `createCustomCategory` returns `DEMO_ENTRY_BLOCKED` for that user — O.17's closure of the typed leg of `shared-demo-account-must-not-learn`. The fence is CORRECT and must not be relaxed: a category name typed into the demo is a name the next anonymous visitor reads in every picker and every report. So the row is never filed, `data-remaining` never decrements, and `:132` fails on `expected "10", received "11"`; `:184` is currently masked behind it by `mode:'serial'`. **The fix is a fixture, not an assertion change:** these two tests need a NON-demo user holding a multi-row needs-review group, which a throwaway signup does not have (it lands on `empty-dashboard`). Options to weigh first, in this order: drive the rows in through the existing CSV importer as a throwaway user (which K.3 established is permitted for non-demo), or seed them directly via better-sqlite3 as `card-unknown-due.spec` and `today-feed-frozen.spec` already do. Do NOT move the coverage to a unit test — the defect under test is a React dispatch ORDERING (`createAndFile` fires `setExtraCategories`/`setNewCatName` before `onPick→fileRow→removeRowLocally`), which is exactly what a component-free test cannot see. Note for whoever takes it: this is the third time a shipped fence left the full suite red unnoticed (#244's transactions.spec, K.3's CSV remedy, now this), because `VERIFY_E2E=1` is opt-in — worth pairing with a cheap guard that greps specs for demo-driven writes. | Opus 5 | small | 40k | **[x] DONE 2026-08-06 — and the row undercounted its own scope: the file held THREE reds, not two.** Every claim executed, not inspected. Full-file serial run: `:132` fails (`expected "10", received "11"`) and **4 tests did not run** — `:184` was masked, and so were `:314`, `:385`, `:394`, so "the true red count is 2" was a hypothesis over four unrun tests. `--grep-invert :132` → `:184` fails identically at :223. `--grep-invert` both → **`:394` "Skip for now" (#374) FAILS**: `triage-card` not found, queue EMPTY, because it is declared AFTER `:314` (review cost), which drains the queue by design. Run alone, `:394` PASSES — so its defect was pure DECLARATION ORDER and it had never once run green in a full-file run since #374 added it; the two fenced tests always aborted the serial file first. **The fence was not relaxed.** The two write-in tests moved to `tests/e2e/triage-write-in.spec.ts`, each with its own throwaway signup and a 4-group seeded queue (3-row top group + three 1-row groups, all with `providerCategoryId` NULL so all three suggestion paths — pipeline, provider, proposal — are null and the top card is honestly ambiguous, which `:184` needs to reach the picker at all). Precedent and proof for the descriptor: `triage-provider-suggestion.spec.ts` already establishes `GOOSE POND BAR GRILLE` as unknown to our pipeline. The fixture asserts its own premise on arrival (`data-remaining` = 4, `triage-no-suggestion` visible), so a ruleset that learns one of these merchants fails loudly at the top instead of deep inside a flow. `phase2-triage.spec.ts` keeps its demo queue and gains a rewritten serial-residue contract — one ordering invariant (every test that leaves the queue as it found it comes first; the one test that drains it comes last) replacing a per-test hand-off written around the two departed tests. **Sabotage-proven:** restoring the pre-fix cycle-2 P1 shape (the `groupEmptied` flag mutated INSIDE the `setGroups` updater, `triage-inbox.tsx:337`) turns the singles test RED at exactly its named assertion — `triage-singles` count 1, i.e. the mode leaking onto the next card — then reverted and rebuilt. All 7 tests across both files pass serialized. **The row's headline claim is FALSE and is now disproven, not assumed: closing this does NOT make `VERIFY_E2E=1` green.** CI (`.github/workflows/verify.yml`, which already runs `VERIFY_E2E=1` on every push) has failed on EVERY push for at least two days on **4 UNIT tests that pass locally** — `fi-real-basis` ×2, `loan-payment-flow-assembler` ×1, `merchant-lens-server` ×1 — plus the documented `budget-targets` CI timing flake. Root cause of the split established: `vitest.config.ts` pins NO timezone, local is `America/New_York` (UTC−4) and CI is UTC, so the unit gate's "6,167 passed" is a claim about the maintainer's timezone, not about the code. Split out as **K.8**. Also assessed and REJECTED: the row's suggested "cheap guard that greps specs for demo-driven writes" — a spec file mixes demo and throwaway users per test, so a file-level grep is the wrong granularity (it flagged `transactions.spec.ts`, which already uses throwaway users for its write-ins and even carries an explicit demo-fence test at `:529`). The real guard already exists, already runs, and is already red: nobody was reading CI. |
| K.7 | **The demo's Auto Loan reaches /calendar as a DETECTED SERIES, never as a `loan-due` — so #134's "loans share the dues surface with cards" is currently unassertable anywhere.** Found while re-pointing K.5's `payment-reminders.spec` assertion, and found only because the first re-point PASSED for the wrong reason (`toContainText('Auto Loan')` matched the series row; it would have stayed green if loan dues were deleted outright). **Verified on production 2026-08-06 across five months** (Jun/Jul/Sep/Oct/Nov 2026): every month paints `Auto loan — CarMax` with the `scheduled` badge — an `outflow` event — and NO month paints the `${accountName} due` label with the `due` badge that `calendar/build.ts:224,250` emits for a `LoanObligation`. **This contradicts the seed's own stated design:** `src/lib/seed/build.ts:550` deliberately removed the hand-authored auto-loan scheduled row because "the loan account (acct-autoloan: minimumPaymentCents 38500, dueDayOfMonth 5) now drives a first-class loan-due obligation on the calendar + reminders (#134) — a duplicate scheduled row here would double-display it." The account exists with those exact terms, $385.00 on the 5th is exactly what renders, and it renders as the row the seed says should not exist. **Two candidate causes, decide by execution not inspection:** (a) `selectLoanObligations` yields nothing for the demo loan (check what it requires beyond `minimumPaymentCents` + `dueDayOfMonth` — an `effectiveDueDate` it cannot compute would explain it) and the recurring DETECTOR has since learned the payment from transaction history, standing in silently; or (b) the obligation exists and `/calendar` is not receiving it. **Note the double-display risk cuts both ways:** if (a) is fixed without removing the detected series, the demo gains TWO $385.00 rows on the 5th — precisely what the seed comment was written to prevent — so the fix must decide which source owns a loan payment, not just switch one on. Loan dues are money-with-a-date on a surface the owner reads, so: **Fable build + hostile critic.** | **Fable 5** | small | 40k | **[x] DONE 2026-08-10 (DECISIONS #437, verify green).** The diagnosis (PROGRESS 2026-08-09) executed the real defect: a fresh seed is CORRECT (obligation paints, no detected series — `k7-loan-due-probe` swept Jul 2026→Mar 2027, one loan-due/mo at $385), so the gap was never the demo — it was the DOUBLE COUNT: on the ordinary shape a loan obligation AND the recurring-detected ACH that pays it both projected the same payment ($1,155 phantom outflow/90d, `k7-double-count-probe`). **The obligation owns the payment; a C.25-proven detected row yields.** One pure engine (`src/lib/engine/loans/duplicate-projection.ts`, `splitLoanCarriedScheduled`) consumed by all three surfaces — /calendar (split before `withOwner`, the suffix would change the canonical), /forecast and /radar (split before `expandScheduled`, obligation flows appended after) — suppression 1:1, capped, never free-standing: a row drops only when a C.25 fact names a loan account in THIS call's obligation list at THIS row's amount, and at most as many rows as facts cover the (canonical|amount) key (one fact = one carried payment/mo). Radar's `loanOverlap` re-derived: discloses only a SURVIVING outflow matching an obligation amount ("counted twice" when unproven, silent when proven). **Hostile critic (Opus, fresh context) — FAIL, 2 executed P1s, both fixed + sabotage-proven:** F1 the rule was INERT on the real chain (C.25 mints `CarMax Auto Finance` from the raw ACH pattern; re-deriving it title-cases to `Carmax Auto Finance` — both sides now normalize before keying); F2 over-suppression (a shared canonical — Nelnet — with one undatable loan lost BOTH rows; the cap fixes it). F3: the /calendar wiring line was unwitnessed → page-render test (jsdom, mocked auth+getCashNeeded, real prisma): proven row absent, unproven duplicate VISIBLE (#400 direction), sabotage-proven red. Gate: tsc 0 / eslint 0 / **6,575 unit + 1 skipped / 398 files** / build clean; e2e payment-reminders + forecast + calendar-posted **9/9** on a fresh build — including the `Auto Loan due` July lock this row said could not exist. The loan half of #134 now has coverage on three surfaces. Residual (F4, recorded not fixed): the radar disclosure fires on amount-equality alone, hedged, over-hedge-safe. Owner-only leftover: production's stale demo dataset still carries the detected series (no obligation) until a reseed — TASKS 0.3. |
| K.2 | **Three trailing years of DATA — the half of the owner's ask that no calendar change can satisfy.** Not a new investigation: H.1(a) measured the live corpus (oldest row 2026-04-24, all 12 items backfilled, `added: 0`), and Plaid's 730-day ceiling is re-verified. Route A = **reconnect SimpleFIN**, whose first-ever pull asks `SIMPLEFIN_INITIAL_LOOKBACK_DAYS` (`simplefin.ts:63`) — owner action, one click, and it is the ONLY route that reaches multi-year for a bank that supports it. Route B = **H.2's guided CSV import** per institution, the only route for banks SimpleFIN cannot reach. Route C = **H.6's fresh Plaid Link** caps at 730 days and therefore CANNOT satisfy "3 years" alone — say so rather than letting it be re-proposed. Deliverable: tell the owner which of his institutions each route can serve, before he spends clicks on the wrong one. **RE-MEASURED LIVE 2026-08-06** (`h1-connection-depth.mts`, re-run this session, not quoted): 56 accounts / 3,087 rows / 1,872 after the R1 keep; register floor **2026-03-25**; **13** Plaid items (not 12) across Chase ×3, Capital One ×2, Charles Schwab ×2, American Express ×2, Vanguard, Truist, U.S. Bank + 1 unlabeled, ALL `backfill=2026-08-04`, oldest Plaid row anywhere **2026-04-24** ⇒ Route C is dead for depth, confirmed twice. **The finding that reorders this row: the SimpleFIN bucket is `NO-CONNECTION-ROW` — 25 accounts, 1,684 rows, frozen [2026-03-25..2026-07-21].** The `SimpleFinConnection` row is DELETED, so Route A is not "a wider ask", it is *the connection does not exist*; H.5's backfill has had nothing to run against for ~16 days. #421 widened the ask to 1460d so that reconnect reaches all of 2023 rather than stopping at 2023-08-07. **Two sub-items now split out:** (a) the probe `scripts/audit-probes/k2-institution-routes.mts` (written this session, per-institution names for the 25 SimpleFIN accounts) was **BLOCKED by the permission classifier** and never ran — the route table is at Plaid-institution granularity only, and the SimpleFIN 25 are unnamed; re-run it with approval before writing per-bank instructions. (b) **the accounts page never says the connection is GONE** — with no connection row, freshness falls back to newest-txn-date and renders "No new data in 16 days — you may need to reconnect" (`health.ts:82`, `accounts-list.tsx:1317`) while `feedDroppedAt` stays null because the only writer is an active sync; a deleted connection is indistinguishable from a merely-stale one, and the reconnect entry point (`simplefin-connect-btn`) reads as "+ Connect a bank", i.e. as first-time setup. Verified by a read-only trace, NOT by driving the UI — confirm before building. | Opus 5 | medium | 50k | **[~] (a)+(b) DONE 2026-08-07 (DECISIONS #423, STATUS §K.2b, critic-cycled ×2, 0 P0).** (a) probe RAN: SimpleFIN 25 = Amex 2/CapOne 5/Schwab 10/Chase 4/Vanguard 4 — one reconnect serves five of seven institutions; Truist + U.S. Bank are Plaid-only ⇒ CSV is their only multi-year route; plus one ORPHANED Plaid card (264 rows, item removed), which (b) now names. (b) freshness level `disconnected` claimed only on PROOF (no simplefin conn row / dangling plaid itemId; null linkage never "removed" — sabotage-locked); /accounts front door over orphaned accounts is a RECONNECT naming the stranded count, never first-time setup; per-row remedy tail deliberately ABSENT (false for plaid re-links + superseded predecessors — critic-executed); orphaned excludes superseded predecessors. Gate: verify exit 0 — 6181/376 files; connection-health e2e 4/4 on a fresh build; fail-old 8 assertions; hostile-env 60/60. **Remaining: the owner reconnect (the only thing that moves data) + (c) H.2 per-bank CSV instructions; residuals in STATUS §K.2b (dashboard hedge, H.5 gap-fills-last).** |
| K.8 | **The unit gate answers a different question locally than it does in CI — and CI has been RED on every push for at least two days while every session reported "verify green".** Found while disproving K.6's headline claim. `.github/workflows/verify.yml` already runs `VERIFY_E2E=1 bash scripts/verify.sh` on every push and PR; grouped over the last 100 runs the API returns (2026-08-02 → 2026-08-06) the tally is **50 `failure`, 49 `cancelled`, 0 `success`** — not one green CI run in the entire window, and `Skip for now` (#374, landed 2026-08-01) has therefore never once been observed passing in a full-file run. The latest run fails **4 unit tests that pass locally** — `fi-real-basis` ×2, `loan-payment-flow-assembler` ×1, `merchant-lens-server` ×1 — so the "6,167 passed / 374 files" every recent PROGRESS entry reports is true of the maintainer's machine and false of a clean checkout. **Root cause REPRODUCED locally, not inferred:** `businessToday()` (`src/lib/business-today.ts:33`) gives `process.env.DEMO_TODAY` top precedence; `.env` sets `DEMO_TODAY=2026-06-10`, but **vitest does not load `.env`**, so locally these tests fall through to `realClockToday()` (2026-08-06) while GitHub Actions declares `DEMO_TODAY: "2026-06-10"` as a JOB-LEVEL env var that IS in `process.env` for every step. None of the three files pins the date itself. `DEMO_TODAY=2026-06-10 npx vitest run <the 3 files>` reproduces all four failures byte-identically (`expected 1 to be 3`; `your last 1 full month × 12` where the test wants `your last 3 full months × 4`; `length of 4 but got 2`; `$50.00` where the test wants `$60.00 in all`). **A first-pass diagnosis blaming the timezone was rejected and must not be re-proposed:** `currentMonth = today.slice(0,7)`, so a UTC-vs-EDT one-day shift cannot change a month count. **The fix is not to edit four assertions until they are green.** These are three tests that silently depend on the ambient wall clock, so their verdicts drift with the calendar as well as with the environment — the real deliverable is a DETERMINISTIC clock for the unit gate (pin it in `vitest.config.ts`, or require every test that reads `businessToday()` to pin its own), and then repairing whatever that surfaces. It touches money-math expectations (the FI number, loan payment flows, merchant totals), so: build + hostile critic. **Also in scope, and arguably the larger finding: nothing in the loop reads CI.** Rule 5 proves a deploy reached `READY`; nothing proves the gate that guards `main` is green, which is why three shipped fences (#244, K.3, K.6) each left the suite red unnoticed. A grep-based spec guard was assessed and REJECTED under K.6 — a spec file mixes demo and throwaway users per test, so file-level grep is the wrong granularity and produced a false positive on `transactions.spec.ts`. | **Fable 5** (money-math expectations + a gate-integrity decision) | medium | 50k | **[x] DONE 2026-08-06 (DECISIONS #422; STATUS §K.8 RESOLVED).** Clock pinned at the gate (`vitest.config.ts`: DEMO_TODAY + TZ=UTC + blanked LLM keys, unconditional, hostile-shell-proven); the three files pin their fixture-consistent dates, NO money expectation edited (critic-executed fail-old on all three); tripwire `gate-clock-pin.test.ts`; `scripts/ci-status.sh` + CLAUDE.md rule 5 "Read the gate" + rule 2 cross-ref. Gate critic's P0 found the FIFTH CI-red cause this row's four could not see: **CI ran Node 20, jsdom (undici@8 ≥22.19) could not load, so the C.26 render harness's 14 assertions had NEVER run on CI** — verify.yml now pins node 24; `engines` field rejected (Vercel runtime blast radius). Money critic PASS 0 P0/P1/P2 (7 sabotages); gate critic 1 P0 + 1 P1 + 4 P2 + 5 P3, all fixed/accepted-in-place. Verify GREEN, 6,169+1 / 376 files. CI conclusion for the closing push recorded in STATUS when read. |
| K.9 | **The register applied a filter it never showed — owner report 2026-08-07 ("still not showing up").** 0 transactions, `$0.00` ×3, "No transactions match these filters", every visible control on its default, and "History available from Wed, Mar 25, 2026" four lines above. The **Clear** link was the discriminator (it renders on the same predicate as the "filtered slice" copy), so a filter was on and nothing on the page held it: `?merchant=` was the only axis in `hasFilters` with no control in the bar — exact-matched on the display name, set from a dozen link surfaces, so an unmatched name returns zero forever and reads as lost data. Gap already written down and queued in `links.ts`; K.3's history-bound line is what made the screenshot decidable without a DB read. | **Fable 5** | small | 30k | **[x] DONE 2026-08-07.** Merchant chip (names the string matched, clears in one tap); `registerEmptyReason` gains a `merchant` kind below the window branches carrying `withOtherFilters`, blank/whitespace OFF as the query engine reads it; empty state «No transactions here match "X"» + a LINK out. Locks are the RULE not the instance: a table-driven render test over ALL TEN axes of `hasFilters` — on ⇒ readable in the bar ⇒ clearable — so the next invisible filter fails a test instead of a phone. Sabotage: chip deleted → 5 RED. Verify GREEN (tsc 0 / eslint 0 / 6,211 unit + 1 skipped / 378 files / build clean) + new e2e green on a real build. Third instance appended to `a-zero-is-a-claim-and-must-name-which-zero.md`. **Open behind it (not this row's claim):** WHICH link produced the empty set — the chip now prints it; candidate is `/recurring` grouping by the normalized descriptor while the register displays `Merchant.canonical`, and `no-dead-ends.spec.ts` checks hrefs without ever navigating. |

| K.10 | **The e2e harness's 4-worker stall class, mechanism-proven and fixed (DECISIONS #438).** The K.8 close-out's named follow-up (worker-isolated e2e DBs or workflow-level retries). Two instrumented runs proved: ~40 specs' direct better-sqlite3 connections commit seeds to the single shared e2e file; a commit landing between a server action's first read and first write dooms its write upgrade to a FULL 15s busy_timeout burn (BUSY_SNAPSHOT never clears for that snapshot); the Prisma engine serializes per connection, so burns stack — 97 POSTs never resolved in one run, 6–19.4s+ action latencies, 3 tests failing on 20–30s response timeouts, and the ledger's 12 CI reads across 8 tests. Fix: `SQLITE_BUSY_TIMEOUT_MS` env (e2e=500ms, default 15s unchanged) + Playwright `retries: 2`. Verified: 4 local draws — 319/1-flaky/0 and gate GREEN 318/2-flaky/0 (the 2 flaky were CSV wedge members, retry-passed; :638's retry-1 reproduced the exact 180s CI signature locally). Residuals recorded: the C.14/C.15 severed-flight wedge has a NON-DB component (stalls before writing; ~1 draw in 4 locally, retry-absorbed) — if it persists on CI after this ship, instrument the CSV import path next (the temp hook recipe is in this row's ledger entry); and the combine-connections 500 is a real engine race (concurrent combines → H.6b(a) carry at combine-connections.ts:1042) needing an engine-side serialization/idempotency fix (Opus 5 — money-adjacent). | **DeepSeek V4 Flash** | medium | 60k | **[x] DONE 2026-08-10 (DECISIONS #438, verify GREEN).** |

## Wave D — Docs diet: the ledgers are burning session context (owner request 2026-08-04)

Owner, verbatim: *"please consolidate readme files if needed, we don't want massive token
burning context."*

Measured this session (bytes; ÷4 ≈ tokens): **4.88 MB of markdown repo-wide.** The archives
(2.1 MB) are already out of the read path — the problem is the LIVE set:
`docs/DECISIONS.md` **1,118 KB**, `docs/EDGE_CASES.md` 210 KB, `TASKS.md` 144 KB,
`docs/DECISIONS_INDEX.md` **141 KB** (an "index" costing ~35k tokens defeats its purpose),
`PROGRESS.md` 93 KB, `docs/STATUS.md` 79 KB, `REGRESSION_LEDGER.md` 62 KB. Five `[x] DONE`
C-rows were rotated to `TASKS_DONE_ARCHIVE.md` this session per this file's own rule
(TASKS.md → 136 KB); the rest is below. Consolidation must not delete evidence — everything
moves to `docs/archive/`, nothing is discarded.

| # | Task | Owner/Agent | Effort | Est. budget | Status |
|---|------|-------------|--------|------------|--------|
| D.1 | **Rotate the working ledgers and set a ceiling.** STATUS/PROGRESS/REGRESSION entries older than the current wave move to the dated archives (pattern exists: `docs/archive/*_2026-06_to_2026-07.md` — add an August file); done TASKS rows move on every close, not in batches. Then make it stick: add to CLAUDE.md conventions that each live ledger stays under ~40 KB and rotating is part of a slice's close, like the verify. | Sonnet | medium | 40k | **[ ] OPEN** |
| D.2 | **Split DECISIONS.md; shrink its index to one line per decision.** 1.1 MB in one file makes every targeted lookup a heavy read even via the index. Range-split into `docs/archive/DECISIONS_<range>.md` keeping the newest ~50 decisions live; DECISIONS_INDEX becomes strictly `#id — one-line title → file` (no prose), so INDEX-then-target stays the read path but the index itself is cheap. Update the CLAUDE.md pointer; verify no in-repo reference breaks (`grep -rn 'DECISIONS' --include='*.md'` for anchored links). | Sonnet | medium | 50k | **[ ] OPEN** |
| D.3 | **Audit the standing session reads.** CLAUDE.md mandates LOOP_ENGINEERING.md + lessons INDEX up front, and every session loads TASKS.md; measure the actual token cost of that standing set (explorer subagent), then trim: closed-wave preambles out of TASKS.md with their rows, lessons-INDEX entries to one line each, EDGE_CASES sections referenced from tests moved nearer the tests. Deliverable = before/after token counts in STATUS, not a vibe. | Opus 5 | small | 30k | **[ ] OPEN — after D.1/D.2** |
